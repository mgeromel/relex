earthquake ;; Catastrophe || tsunami ;; Catastrophe || occurred ;; Presence
shock ;; Catastrophe || perceived ;; Know
earthquake ;; Catastrophe || earthquake ;; Catastrophe || due ;; Causation || unaffected ;; Causation || tsunami ;; Catastrophe || shaking ;; Motion || loss ;; Damaging || resulting ;; Causation || tsunami ;; Catastrophe || effects ;; Influence || inundated ;; Destroying
earthquake ;; Catastrophe || centered ;; Placing
earthquake ;; Catastrophe || earthquake ;; Catastrophe || relative ;; Protest || tsunami ;; Catastrophe || tsunami ;; Catastrophe || rupture ;; Destroying || led to ;; Causation
observed ;; Know || resulted in ;; Causation || tsunami ;; Catastrophe || deaths ;; Death
damage ;; Damaging || loss ;; Death || occurred ;; Presence
warning ;; Warning || arrived ;; Arriving || shock ;; Catastrophe || felt ;; Perception_active
posted ;; Sending || delivered ;; Sending || undetected ;; Preventing_or_letting || tsunami ;; Catastrophe
Battle ;; Hostile_encounter || took place ;; Process_start
ambushed ;; Attack || expedition ;; Traveling || Battle ;; Hostile_encounter
spotted ;; Know || ambush ;; Attack || waiting ;; Change_event_time
received ;; Receiving || dropped ;; Motion || bombing ;; Attack || ensued ;; Coming_to_be
dead ;; Death || seeing ;; Traveling || reported ;; Reporting || wounded ;; Bodily_harm
reached ;; Arriving || attacked ;; Attack
confirmed ;; Check || suspected ;; Suspicion
battle ;; Hostile_encounter || driven ;; Motion
found ;; Placing || dead ;; Death || casualties ;; Catastrophe || suffer ;; Bodily_harm
killed ;; Killing || losing ;; Earnings_and_losses || casualties ;; Catastrophe || raid ;; Attack || include ;; Cause_to_be_included || battle ;; Hostile_encounter || admit ;; Statement || wounded ;; Bodily_harm
exaggerate ;; Cause_change_of_position_on_a_scale
captured ;; Conquering || released ;; Releasing || dead ;; Death
killed ;; Killing || captured ;; Conquering
took place ;; Process_start || convinced ;; Coming_to_believe
attack ;; Attack
redeem ;; Recovering || attack ;; Attack
adopt ;; Choosing || wage ;; Using || warfare ;; Hostile_encounter
Battle ;; Hostile_encounter || fell ;; Motion
operation ;; Military_operation
declared ;; Expressing_publicly || controlled ;; Control || prepared ;; GetReady
centred ;; Placing || administered ;; Control
governed ;; Control
supported ;; Supporting
stationed ;; Placing || protect ;; Defending
took ;; Conquering || engaged ;; Forming_relationships || mobilised ;; Becoming_a_member
resist ;; Defending || instigated ;; Process_start || imposed ;; Action
constructed ;; Building
began ;; Process_start || defences ;; Defending || cut off ;; Removing
requested ;; Request || followed ;; Self_motion || surrender ;; Surrendering
Battle ;; Hostile_encounter || used ;; Using || fought ;; Hostile_encounter || defeat ;; Hostile_encounter || commanded ;; Control
ensured ;; Deciding || control ;; Control || War ;; Military_operation
battle ;; Hostile_encounter || fought ;; Hostile_encounter
training ;; Education_teaching || battlefield ;; Hostile_encounter || created ;; Creating || moved ;; Motion
attack ;; Attack || realize ;; Know || took ;; Conquering
erasing ;; Emptying || gained ;; Getting || preceding ;; Process_start || destroyed ;; Destroying || campaigning ;; Military_operation
surrender ;; Surrendering || laid ;; Placing || resulted in ;; Causation || siege ;; Besieging
battle ;; Hostile_encounter || command ;; Control || placed ;; Placing || commanded ;; Control || appointed ;; Choosing
battle ;; Hostile_encounter || established ;; Creating
refused ;; Agree_or_refuse_to_act || participate ;; Participation
accident ;; Catastrophe || occurred ;; Presence
damage ;; Damaging || arriving ;; Arriving || stop ;; Motion || stop ;; Self_motion || collided ;; Motion || sustaining ;; Causation
killed ;; Killing || injured ;; Bodily_harm
exceeded ;; Cause_change_of_position_on_a_scale || buffer ;; Process_end || stop ;; Motion
crash ;; Catastrophe || occurred ;; Presence
occur ;; Presence
realise ;; Know || terminated ;; Process_end || signalled ;; Body_movement
approached ;; Arriving || braked ;; Self_motion
presented ;; Presence || telescoped ;; Expansion
attacked ;; Attack || killed ;; Killing
attacked ;; Attack || walking ;; Self_motion
ran ;; Motion || used ;; Using || death ;; Death
dragged ;; Motion || arrived ;; Arriving || remained ;; Presence
told ;; Telling || killed ;; Killing || killing ;; Killing || armed ;; Bearing_arms
received ;; Receiving || arrived at ;; Arriving || set up ;; Building || call ;; Name_conferral
arrived ;; Arriving || armed ;; Bearing_arms
wounded ;; Bodily_harm || brandishing ;; Body_movement || fired ;; Use_firearm
apprehended ;; Know
raised ;; Cause_change_of_position_on_a_scale || converted ;; Change
found ;; Know || attackers ;; Attack
sentenced ;; Legal_rulings || given ;; Giving || order ;; Arranging || ordered ;; Arranging
attack ;; Attack || condemned ;; Committing_crime
launched ;; Process_start || lasted ;; Process_end
siege ;; Besieging || celebrate ;; Social_event
began ;; Process_start || included ;; Cause_to_be_included || siege ;; Besieging
siege ;; Besieging || arrival ;; Arriving || led to ;; Causation
siege ;; Besieging || made ;; Manufacturing
siege ;; Besieging || conflict ;; Hostile_encounter || explain ;; Statement
marrying ;; Forming_relationships || concluded ;; Process_end
make ;; Manufacturing || prompted ;; Causation
homage ;; Body_movement
taken ;; Creating || holding ;; Defending
marriage ;; Forming_relationships
engaged ;; Forming_relationships
marriage ;; Forming_relationships
aid ;; Assistance || besieging ;; Besieging || switch ;; Change
visited ;; Traveling || attacking ;; Attack || passing ;; Motion_directional
crossed ;; Motion_directional
called ;; Name_conferral || issued ;; Creating
begun ;; Process_start
captured ;; Conquering || joined ;; Becoming_a_member
buried ;; Placing || occur ;; Coming_to_be || occurred ;; Coming_to_be
surrounded ;; Surrounding || rebel ;; Hostile_encounter || traveled ;; Traveling
assaulting ;; Attack || brought ;; Bringing || siege ;; Besieging || plundered ;; Robbery
arrived ;; Arriving
reasons ;; Protest || challenged ;; Competition
broke ;; Bodily_harm || wounded ;; Bodily_harm || died ;; Death || asked ;; Request || combat ;; Hostile_encounter || went out ;; Self_motion
brought ;; Bringing
disputed ;; Quarreling || shared ;; Communication
siege ;; Besieging || blockaded ;; Besieging
lost ;; Earnings_and_losses || dragged on ;; Change_event_time || held ;; Defending
remained ;; Presence || end ;; Process_end || issued ;; Creating
forces ;; Hostile_encounter || called ;; Name_conferral
contains ;; Containing || siege ;; Besieging || besieging ;; Besieging || drafted ;; Writing
confirmed ;; Check
spent ;; Expend_resource || known ;; Know
scheduled ;; Hold || operated ;; Being_in_operation
crashed ;; Catastrophe || registered ;; Recording
struck ;; Attack
struck ;; Attack || destroyed ;; Destroying || plunged ;; Motion_directional
carrying ;; Carry_goods
rescued ;; Rescuing || crash ;; Catastrophe
rescued ;; Rescuing || rescue ;; Rescuing || assisted ;; Assistance || drowned ;; Death
killed ;; Killing
rescued ;; Rescuing
acts ;; Action || commended ;; Judgment_communication
determined ;; Deciding || cause ;; Causation
used ;; Using || use ;; Using || failed ;; Earnings_and_losses || abort ;; Process_end || takeoff ;; Motion || switch ;; Change_tool || takeoff ;; Self_motion || buildup ;; Cause_change_of_position_on_a_scale || failed ;; Agree_or_refuse_to_act || detecting ;; Check || seeing ;; Perception_active
held ;; Hold || closing ;; Process_end || event ;; Competition || hosted ;; Social_event
host ;; Social_event
participated ;; Participation || events ;; Competition
host ;; Social_event || event ;; Competition
opened ;; Process_start
final ;; Process_end || led ;; Getting
records ;; Recording || broken ;; Destroying
cost ;; Cost || spent ;; Expend_resource || competition ;; Competition || management ;; Arranging || arrangement ;; Arranging || transport ;; Change_tool || promotion ;; Cause_change_of_position_on_a_scale || deemed ;; Know || host ;; Social_event
departed ;; Departing
Operating ;; Being_in_operation || registered ;; Recording
causing ;; Causation || burst ;; Destroying || landing ;; Arriving || close ;; Process_end
notified ;; Telling || change ;; Change_tool
crashed ;; Catastrophe || entered ;; Arriving
broke ;; Destroying
killed ;; Killing || crash ;; Damaging || injured ;; Bodily_harm
conflict ;; Hostile_encounter || armed ;; Bearing_arms
began ;; Process_start || abandon ;; GiveUp || seized ;; Motion || forced ;; Hostile_encounter || evacuated ;; Removing
changed ;; Change_of_leadership || focused ;; Aiming || control ;; Control
ended ;; Process_end
fled ;; Escaping || controlled ;; Control
relocated ;; Change
reduced ;; Cause_change_of_position_on_a_scale || control ;; Control
captured ;; Conquering
forces ;; Hostile_encounter || evacuated ;; Removing
produced ;; Manufacturing || wrestling ;; Competition
took place ;; Process_start
contested ;; Competition
changed ;; Change_tool || contested ;; Competition
defeat ;; Conquering || interfered ;; Hindering || retaining ;; Preserving
defeating ;; Conquering
drew ;; Create_artwork
took place ;; Process_start || sinking ;; Motion_directional || killing ;; Killing
explosion ;; Attack || explosion ;; Attack || search ;; Scrutiny || initiate ;; Process_start || realise ;; Know || occurred ;; Presence
declared ;; Expressing_publicly || dead ;; Death
took ;; Conquering || sunken ;; Self_motion || disabled ;; Destroying || locate ;; Placing
attach ;; Connect || diving ;; Self_motion || escape ;; Escaping
criticised ;; Judgment_communication || response ;; Response
refused ;; Agree_or_refuse_to_act || help ;; Assistance || misled ;; Hiding_objects || manipulated ;; Control
authorised ;; Preventing_or_letting || assistance ;; Assistance || accept ;; Receiving
opened ;; Openness || sinking ;; Motion_directional || escape ;; Escaping || flooded ;; Motion || find ;; Know
criticism ;; Judgment_communication || came ;; Causation
preparing ;; GetReady || investigation ;; Scrutiny || leaked ;; Motion || causing ;; Causation || load ;; Placing || explode ;; Bodily_harm || raised ;; Cause_change_of_position_on_a_scale
incapacitated ;; Destroying || damaged ;; Damaging || explosion ;; Attack || ignited ;; Lighting || control ;; Control || killed ;; Killing || control ;; Control || destroyed ;; Destroying
prevent ;; Preventing_or_letting || insisting ;; Statement || described ;; Statement || challenged ;; Confronting_problem
drew ;; Motion || caused ;; Causation || use ;; Using || known ;; Know || sinking ;; Motion_directional
accused ;; Judgment_communication || protect ;; Defending || train ;; Education_teaching
explosion ;; Attack || concluded ;; Coming_to_believe || contributed ;; Causation
triggered ;; Causation
explosion ;; Attack
collapsed ;; Destroying || destroyed ;; Destroying || killed ;; Killing || tore ;; Destroying
shut down ;; Being_in_operation
took ;; Conquering || analysts ;; Scrutiny || concluded ;; Coming_to_believe || survived ;; Preserving
fell ;; Motion_directional || contact ;; Connect || replace ;; Change_of_leadership || exploded ;; Use_firearm
killed ;; Killing || consumed ;; Expend_resource || resulting ;; Causation || triggered ;; Causation || remaining ;; Presence || remaining ;; Presence || suffocating ;; Killing
victims ;; Catastrophe || meeting ;; Social_event || engaged ;; Participation || elected ;; Choosing
removed ;; Removing || meeting ;; Social_event || meeting ;; Social_event || sanitised ;; Emptying || leaked ;; Reveal_secret || shocking ;; Response || sedated ;; Action
raise ;; Cause_change_of_position_on_a_scale || contracted ;; Renting
developed ;; Coming_to_be || remains ;; Presence || recovered ;; Recovering || buried ;; Placing
sinking ;; Motion_directional || investigation ;; Criminal_investigation || disaster ;; Catastrophe || completed ;; Process_end
revealed ;; Reveal_secret || maintained ;; Preserving
concluded ;; Process_end || delayed ;; Change_event_time || respond ;; Response || rescue ;; Rescuing || disaster ;; Catastrophe || unprepared ;; Response
commended ;; Judgment_communication || served ;; Assistance
died ;; Death
became ;; Change || cause ;; Causation
assault ;; Attack || victim ;; Bodily_harm || taken ;; Bringing || injury ;; Bodily_harm || said ;; Telling
prevent ;; Preventing_or_letting || escorted ;; Patrolling || arrested ;; Arrest
placed ;; Placing || dragged ;; Motion || carried ;; Carry_goods
chatted ;; Communication || speculated ;; Coming_to_believe
noticed ;; Know || pronounced ;; Statement || resuscitation ;; Rescuing || dead ;; Death || breathing ;; Breathing
killed ;; Killing || indicated ;; Check || injury ;; Bodily_harm
captured ;; Conquering
killed ;; Killing || verdict ;; Legal_rulings || returned ;; Giving
trial ;; Criminal_investigation || charged ;; Judgment_communication || acquitted ;; Legal_rulings
died ;; Death || guilty ;; Committing_crime || report ;; Reporting || concluded ;; Coming_to_believe
preserving ;; Preserving || apologised ;; Judgment_communication || admitting ;; Reveal_secret
inquiry ;; Criminal_investigation || admitted ;; Reveal_secret
arresting ;; Arrest || died ;; Death
use ;; Using || widespread ;; Dispersal || generated ;; Creating || raised ;; Creating
approached ;; Arriving || selling ;; Commerce_sell
told ;; Telling || selling ;; Commerce_sell || arrest ;; Arrest || harassed ;; Change_sentiment
refused ;; Agree_or_refuse_to_act || pulled ;; Motion || placed ;; Placing || cooperate ;; Collaboration
placed ;; Placing || wrestled ;; Body_movement
breathe ;; Breathing || restraining ;; Hindering || lying ;; Placing
ease ;; Cure || breathing ;; Breathing || lost ;; Earnings_and_losses || turned ;; Motion
waited ;; Temporary_stay || remained ;; Temporary_stay || arrive ;; Arriving || lying ;; Placing
pronounced ;; Statement || dead ;; Death
death ;; Death || placed ;; Placing
death ;; Death || ruled ;; Legal_rulings
death ;; Death || caused ;; Causation || use ;; Using || committed ;; Commitment
death ;; Death || resulted ;; Causation || positioning ;; Placing || compression ;; Motion || indicated ;; Statement || hold ;; Defending
cited ;; Statement || contributing ;; Causation
decided ;; Deciding || indict ;; Extradition
decision ;; Deciding || made ;; Manufacturing || charges ;; Judgment_communication || stirred ;; Motion
response ;; Response || held ;; Defending
announced ;; Expressing_publicly || pay ;; Commerce_pay
bring ;; Bringing || charges ;; Judgment_communication || declined ;; Cause_change_of_position_on_a_scale
recommended ;; Convincing || terminated ;; Process_end
death ;; Death || fired ;; Process_end
attacks ;; Attack
attacks ;; Attack
made ;; Manufacturing || lodgements ;; Placing
began ;; Process_start || turned ;; Becoming
caused ;; Causation || increased ;; Cause_change_of_position_on_a_scale || casualties ;; Catastrophe || fled ;; Escaping || go ;; Motion
used ;; Using || used ;; Using
blamed ;; Judgment_communication || penetrate ;; Filling
put out ;; Communication || allay ;; Cause_change_of_position_on_a_scale
worked ;; Being_in_operation || accelerated ;; Change_event_time
hard ;; Confronting_problem || Tour ;; Traveling || tour ;; Traveling
began ;; Process_start || started ;; Process_start || marking ;; Recording || recording ;; Recording || tour ;; Traveling
show ;; Social_event
opened ;; Openness || tour ;; Traveling
Tour ;; Traveling || toured ;; Traveling
marked ;; Recording || tour ;; Traveling || trip ;; Traveling
opened ;; Openness
opened ;; Openness || show ;; Social_event
effects ;; Achieve || known ;; Know || show ;; Social_event
died ;; Death
found ;; Know || treated ;; Cure || pronounced ;; Statement || breathing ;; Breathing || weak ;; Cause_change_of_strength || dead ;; Death || administered ;; Control || said ;; Communication
death ;; Death || homicide ;; Committing_crime || concluded ;; Coming_to_believe
administered ;; Control
convicted ;; Legal_rulings || served ;; Assistance
triggered ;; Causation || sales ;; Commerce_sell || creating ;; Creating || unprecedented ;; Catastrophe || reactions ;; Response || boosting ;; Cause_change_of_position_on_a_scale
concerts ;; Social_event || preparing ;; GetReady
rehearsed ;; Practice || concerts ;; Social_event || death ;; Death
broadcast ;; Expressing_publicly || attracting ;; Influence
fought ;; Hostile_encounter || involving ;; Cause_to_be_included || command ;; Control || Battle ;; Hostile_encounter || battle ;; Hostile_encounter
played ;; Participation
use ;; Using || marked ;; Recording || battle ;; Hostile_encounter
attack ;; Attack || break ;; Destroying || capture ;; Conquering || plan ;; Arranging
War ;; Military_operation || attack ;; Attack || retreating ;; Escaping || caught ;; Getting || offensive ;; Attack || launched ;; Process_start
attack ;; Attack || meant ;; Communication || coming up ;; Motion || proved ;; Coming_to_believe
attack ;; Attack || established ;; Creating || War ;; Military_operation || supplying ;; Supply || move ;; Motion || demonstrated ;; Coming_to_believe
attack ;; Attack || left ;; GiveUp
attack ;; Attack || ordered ;; Request || lead to ;; Causation || realized ;; Know || march ;; Motion_directional || Offensive ;; Attack
League ;; Competition || postponed ;; Change_event_time
competition ;; Competition || scheduled ;; Arranging
postponed ;; Change_event_time || held ;; Hold || allegations ;; Judgment_communication || corruption ;; Committing_crime
decided ;; Deciding || control ;; Control || tournament ;; Competition || allow ;; Preventing_or_letting || play ;; Participation
tournament ;; Competition || included ;; Cause_to_be_included || representing ;; Presence
captained ;; Control
tournament ;; Competition
occurred ;; Coming_to_be || incident ;; Catastrophe || accident ;; Catastrophe
caused ;; Causation || resulted in ;; Causation || death ;; Death || accident ;; Catastrophe || trapped ;; Placing
statement ;; Statement || left ;; Departing || party ;; Participation || drove ;; Self_motion
swam ;; Self_motion || left ;; Departing || report ;; Reporting || accident ;; Catastrophe
gave ;; Giving || conflicts ;; Quarreling || report ;; Reporting
died ;; Death
recovered ;; Recovering
received ;; Receiving || leaving ;; Departing || accident ;; Catastrophe || pleaded ;; Request || charge ;; Judgment_communication || sentence ;; Legal_rulings
statement ;; Statement || said ;; Statement || conduct ;; Action || accident ;; Catastrophe || accident ;; Catastrophe || report ;; Reporting || regarded ;; Protest
said ;; Statement || became ;; Becoming || becoming ;; Becoming || run ;; Action || incident ;; Catastrophe || undermined ;; Hindering || influenced ;; Influence
decided ;; Deciding || earned ;; Earnings_and_losses || lost ;; Earnings_and_losses || party ;; Participation || enter ;; Becoming_a_member
unrest ;; Violence || unrest ;; Violence || took ;; Change || prolonged ;; Change_event_time
started ;; Process_start || demonstrations ;; Self_motion || reform ;; Reforming_a_system
applied ;; Using || protests ;; Protest
acts ;; Action || rioting ;; Violence || demonstrations ;; Self_motion
decided ;; Deciding || precipitated ;; Causation
changed ;; Change
marked ;; Recording || crisis ;; Confronting_problem
referendum ;; Change_of_leadership || referendum ;; Change_of_leadership || showed ;; Statement || due ;; Causation || dismissal ;; Removing
accused ;; Judgment_communication || warned ;; Warning || made ;; Creating || powers ;; Influence || referendum ;; Change_of_leadership
election ;; Change_of_leadership || named ;; Name_conferral || sweeping ;; Scouring || supported ;; Supporting
marked ;; Recording || protests ;; Protest
causes ;; Causation || demonstrations ;; Self_motion
took ;; Conquering || shout ;; Request
strikes ;; Motion || triggered ;; Causation
demonstrators ;; Self_motion || clashes ;; Hostile_encounter || degenerated ;; Change || protests ;; Protest
accused ;; Judgment_communication || force ;; Request
demanded ;; Request || started ;; Process_start || protests ;; Self_motion
collapsed ;; Destroying
merged ;; Cause_to_amalgamate || party ;; Participation || party ;; Participation || left ;; Departing || form ;; Creating
election ;; Change_of_leadership || launched ;; Motion
election ;; Change_of_leadership
due ;; Causation || voting ;; Choosing
election ;; Change_of_leadership || prevented ;; Preventing_or_letting || electoral ;; Change_of_leadership || vote ;; Choosing || due ;; Causation || protested ;; Self_motion
Tournament ;; Competition || held ;; Hold
defending ;; Defending
suffered ;; Bodily_harm || collapses ;; Catastrophe
lost ;; Earnings_and_losses || tournament ;; Competition || culminating ;; Come_together || hit ;; Attack || led ;; Conquering || built ;; Creating
overtake ;; Motion || shot ;; Motion
became ;; Becoming || win ;; Getting
final ;; Process_end || appearance ;; Presence
fought ;; Hostile_encounter || battle ;; Hostile_encounter || War ;; Hostile_encounter
battle ;; Hostile_encounter || War ;; Hostile_encounter
invaded ;; Attack || armored ;; Wearing || advanced ;; Self_motion
alerted ;; Warning || withdrawal ;; Departing
formed ;; Coming_to_be
battled ;; Hostile_encounter
leaving ;; Causation || stuck ;; Motion || withdrew ;; Escaping || run out of ;; Expend_resource
prevented ;; Preventing_or_letting || allow ;; Preventing_or_letting || battle ;; Hostile_encounter || left ;; Causation || pursue ;; Participation
situated ;; Placing
gave ;; Giving || nickname ;; Name_conferral
destroy ;; Destroying || attacks ;; Attack || attacks ;; Attack || fail ;; Confronting_problem
losses ;; Earnings_and_losses || cost ;; Cost || advanced ;; Cause_to_make_progress || conflict ;; Hostile_encounter || inflicted ;; Bodily_harm || kill ;; Killing || claiming ;; Statement
changes ;; Change || training ;; Education_teaching || led to ;; Causation
destroyed ;; Destroying || using ;; Using || advanced ;; Cause_to_make_progress
organized ;; Arranging || bring ;; Bringing || movement ;; Social_event
culminated ;; Process_end || campaign ;; Social_event || led ;; Causation || change ;; Change
enforced ;; Action
legal ;; Legality || draw ;; Communication
called ;; Name_conferral
began ;; Process_start || pressure ;; Convincing || open ;; Openness || employment ;; Employment || end ;; Process_end
resisted ;; Defending || agreed ;; Agree_or_refuse_to_act || assist ;; Assistance
joined ;; Connect || began ;; Process_start || provoke ;; Causation || arrests ;; Arrest
become ;; Becoming || campaign ;; Social_event
asked ;; Request || trained ;; Education_teaching || participate ;; Participation || talk to ;; Communication || directed ;; Using
arrests ;; Arrest || filled ;; Filling || arrested ;; Arrest || holding ;; Defending || used ;; Using || resulted in ;; Causation
hold ;; Defending || held ;; Defending || avowed ;; Statement
drew ;; Motion || allowing ;; Preventing_or_letting || praise ;; Judgment_communication || participate ;; Participation || put ;; Motion
drew ;; Motion || protest ;; Response
ousted ;; Change_of_leadership || forced ;; Motion || prohibited ;; Preventing_or_letting || burnished ;; Motion || paved ;; Motion
crashed ;; Catastrophe || killing ;; Killing || charter ;; Renting
flying ;; Self_motion || decreased ;; Cause_change_of_position_on_a_scale
suffered ;; Bodily_harm
crash ;; Catastrophe || resulted in ;; Causation
travel ;; Traveling || chartered ;; Renting
returning ;; Arriving
occur ;; Coming_to_be || involving ;; Participation || made ;; Causation
fought ;; Hostile_encounter || remaining ;; Presence
held ;; Defending || end ;; Process_end
expanded ;; Expansion || stretching ;; Expansion
invasion ;; Attack
fell ;; Motion_directional || grew ;; Expansion
weakened ;; Cause_change_of_strength
established ;; Creating || ruled ;; Legal_rulings
facing ;; Confronting_problem || expand ;; Expansion || took ;; Creating
conquered ;; Conquering
raising ;; Cause_change_of_position_on_a_scale || defeated ;; Conquering
received ;; Receiving || death ;; Death || blessing ;; Rite
shaken ;; Motion
competition ;; Competition || withstand ;; Defending
held ;; Defending
used ;; Using
supported ;; Supporting
became ;; Becoming || hand ;; Giving || hired ;; Employment
lost ;; Earnings_and_losses || plundered ;; Robbery
grew ;; Expansion || took ;; Creating
provoked ;; Causation || supported ;; Supporting
defeated ;; Conquering
sent ;; Sending || burn ;; Destroying || defeated ;; Conquering
help ;; Assistance || pleaded ;; Request
devastated ;; Destroying || marched ;; Self_motion
defeated ;; Conquering
perished ;; Death || leaving ;; Departing || left ;; Departing
control ;; Control || regained ;; Recovering || hampered ;; Hindering || established ;; Building
invaded ;; Attack || raiding ;; Attack || capturing ;; Conquering
setting ;; Placing
ravaged ;; Destroying
launched ;; Process_start
prevailed ;; Conquering || suffered ;; Catastrophe
death ;; Death || reach ;; Arriving
captured ;; Conquering
conquered ;; Conquering || commanded ;; Control || marched ;; Self_motion || garrisoned ;; Hostile_encounter || hired ;; Employment
decided ;; Deciding || reply ;; Response || raised ;; Cause_change_of_position_on_a_scale
joined ;; Becoming_a_member || set out ;; Departing || headed ;; Self_motion
reach ;; Arriving || allowed ;; Preventing_or_letting || remained ;; Presence || concentrate ;; Aiming
began ;; Process_start
bombarded ;; Attack || driven ;; Motion || breaching ;; Committing_crime || attacking ;; Attack || determined ;; Deciding
repulsed ;; Defending || killed ;; Killing || injured ;; Bodily_harm || bombarded ;; Attack
pay ;; Commerce_pay || withdrew ;; Escaping || lifted ;; Motion || leaving ;; Departing
defeat ;; Conquering || loss ;; Earnings_and_losses || invasion ;; Attack
undertaken ;; Action || concert ;; Social_event
finished ;; Process_end || recording ;; Recording
demonstrated ;; Protest
hired ;; Employment
confirmed ;; Check
performed ;; Hold
returned ;; Arriving || visits ;; Traveling || took ;; Motion
made ;; Manufacturing || interrupted ;; Hindering || suffered ;; Bodily_harm
stops ;; Motion || return ;; Arriving
elicited ;; Causation
sold ;; Commerce_sell
recorded ;; Recording || tour ;; Traveling || select ;; Choosing
exhausted ;; Expend_resource
achieve ;; Achieve || tour ;; Traveling
conflict ;; Hostile_encounter || began ;; Process_start || overthrew ;; Change_of_leadership || War ;; Hostile_encounter || invasion ;; Attack
emerged ;; Coming_to_be || oppose ;; Agree_or_refuse_to_act
killed ;; Killing
withdrawn ;; Escaping || hired ;; Employment || remain ;; Presence
became ;; Becoming
occurred ;; Coming_to_be
use ;; Using || choose ;; Choosing || authorized ;; Preventing_or_letting
began ;; Process_start || joined ;; Becoming_a_member || launched ;; Process_start
overwhelmed ;; Destroying
captured ;; Conquering || led to ;; Causation
widespread ;; Dispersal || led to ;; Causation
supported ;; Supply
responded ;; Response
gave ;; Giving
accelerated ;; Change_event_time
withdrew ;; Escaping
posed ;; Causation || viewed ;; Perception_active
accused ;; Judgment_communication || supporting ;; Supporting || end ;; Process_end || bring ;; Bringing || cited ;; Adducing
said ;; Statement
found ;; Know
pushed ;; Motion || made ;; Manufacturing
faced ;; Confronting_problem
published ;; Publishing || inquiry ;; Scrutiny || underestimated ;; Judgment_communication || concluded ;; Process_end
invasion ;; Attack || held ;; Hold || elections ;; Choosing
became ;; Becoming
enacted ;; Action
declared ;; Expressing_publicly || eliciting ;; Causation
caused ;; Causation
occurred ;; Coming_to_be
War ;; Hostile_encounter || fought ;; Hostile_encounter
leased ;; Renting
war ;; Hostile_encounter || create ;; Creating || feared ;; Change_sentiment
exchange ;; Exchange || offered ;; Self_motion || recognition ;; Judgment_communication || recognize ;; Ratification
refused ;; Agree_or_refuse_to_act || demanded ;; Request
expansion ;; Expansion || plans ;; Arranging || perceived ;; Know || chose ;; Choosing
negotiations ;; Communication || attacking ;; Attack || attack ;; Attack || broke down ;; Process_end || opened ;; Process_start
chose ;; Choosing || remain ;; Presence || preserve ;; Preserving || averting ;; Preventing_or_letting || war ;; Hostile_encounter || suffered ;; Bodily_harm || win ;; Getting || engaged ;; Participation
rejected ;; Agree_or_refuse_to_act || agree ;; Agree_or_refuse_to_act || dispute ;; Quarreling || bring ;; Causation
concluded ;; Process_end
transformed ;; Change || resulting ;; Causation
victory ;; Getting
war ;; Hostile_encounter || debate ;; Quarreling
Battle ;; Hostile_encounter || engagement ;; Hostile_encounter
conflict ;; Hostile_encounter || expedition ;; Attack || reassert ;; Control || formed ;; Becoming
beginning ;; Process_start || ruled ;; Control || recognised ;; Know
incorporate ;; Cause_to_be_included
launched ;; Process_start || purchase ;; Commerce_buy || military ;; Military_operation
responded ;; Response || reached ;; Arriving || aggression ;; Attack
negotiations ;; Communication || anchor ;; Motion || broke down ;; Process_end || occupied ;; Control
driven ;; Motion
salvaging ;; Rescuing || arrived ;; Arriving
commanded ;; Control
gathered ;; Come_together
advance ;; Self_motion || rejoin ;; Cause_to_amalgamate || threatened ;; Bodily_harm
fled ;; Escaping || believed ;; Know || retreating ;; Escaping || running ;; Self_motion
took up a position ;; Temporary_stay || held ;; Defending || fighting ;; Hostile_encounter
skirmishing ;; Hostile_encounter || recaptured ;; Conquering
withdrew ;; Escaping || reboarded ;; Self_motion
collect ;; Come_together || returned ;; Arriving
sailed ;; Self_motion
battle ;; Hostile_encounter || involved ;; Cause_to_be_included
campaign ;; Military_operation || ill ;; Bodily_harm || died ;; Death || taken ;; Conquering || planned ;; Arranging || spending ;; Expend_resource
death ;; Death || signed ;; Sign_agreement || battle ;; Hostile_encounter || treaty ;; Statement || leasing ;; Renting
became ;; Becoming || paying ;; Commerce_pay || became ;; Becoming || lease ;; Renting || wars ;; Military_operation
battle ;; Hostile_encounter || transformed ;; Change || considered ;; Deciding
accord ;; Statement || campaign ;; Military_operation
battle ;; Hostile_encounter || commemorated ;; Social_event || held ;; Defending
War ;; Military_operation || succession ;; Change_of_leadership || resulted ;; Causation || conflict ;; Hostile_encounter
named ;; Name_conferral || named ;; Name_conferral || granted ;; Giving
designated ;; Choosing
died ;; Death || succeeded ;; Change_of_leadership
began ;; Process_start || subvert ;; Change_of_leadership || Encouraged ;; Convincing || sidelined ;; Process_end
response ;; Response || support ;; Supporting || sought ;; Hold || assert ;; Statement
widened ;; Expansion || declared ;; Expressing_publicly || rift ;; Quarreling || assembled ;; Cause_to_amalgamate
invaded ;; Attack || besieged ;; Besieging || marched ;; Self_motion || defeated ;; Conquering || Battle ;; Hostile_encounter
became ;; Becoming || executed ;; Killing
chose ;; Choosing || remain ;; Temporary_stay || coming to ;; Motion
allowed ;; Preventing_or_letting || war ;; Military_operation || fostered ;; Creating || grow ;; Expansion
alienated ;; Influence || marginalized ;; Cause_change_of_position_on_a_scale
proclaimed ;; Statement || forcing ;; Request || intervene ;; Response
assassinated ;; Killing || entered ;; Arriving || left ;; GiveUp
pacification ;; Cause_to_make_progress || consolidation ;; Cause_to_amalgamate || completed ;; Process_end
rebellions ;; Change_of_leadership || dragged on ;; Motion
control ;; Control || besotted ;; Change_sentiment || product ;; Manufacturing || struggle ;; Violence || rivalry ;; Competition || extension ;; Expansion || conflict ;; Violence || interpreted ;; Protest || conflict ;; Violence
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || engagement ;; Hostile_encounter || War ;; Military_operation || campaign ;; Military_operation
fought ;; Hostile_encounter
battles ;; Hostile_encounter || fought ;; Hostile_encounter
pitted ;; Hostile_encounter || campaign ;; Military_operation
perfect ;; Cause_to_make_progress || battle ;; Hostile_encounter || decision ;; Deciding
casualties ;; Catastrophe || product ;; Manufacturing || tempered ;; Cause_change_of_position_on_a_scale
requiring ;; Imposing_obligation || left ;; Departing || amputated ;; Removing || hit ;; Attack
died ;; Death || likened ;; Judgment_communication || losing ;; Earnings_and_losses || loss ;; Earnings_and_losses
withdrawal ;; Escaping || prevent ;; Preventing_or_letting || lost ;; Earnings_and_losses || replacing ;; Change_of_leadership || led to ;; Causation
began ;; Process_start || campaign ;; Military_operation || crossing ;; Motion_directional
began ;; Process_start || raid ;; Attack
operation ;; Military_operation
Crossing ;; Motion_directional || concentrated ;; Preserving
planned ;; Arranging || attacking ;; Attack
attacked ;; Attack || leaving ;; Departing || deter ;; Hindering || advanced ;; Self_motion || split ;; Destroying
withdrew ;; Escaping || defensive ;; Defending || ceding ;; GiveUp
routed ;; Attack || sending ;; Sending
performing ;; Hold || replaced ;; Change_of_leadership || reconnaissance ;; Scrutiny || wounded ;; Bodily_harm
losses ;; Earnings_and_losses || launched ;; Process_start || attacks ;; Attack || fighting ;; Hostile_encounter
advanced ;; Self_motion || defeated ;; Getting || moved ;; Self_motion
surrounding ;; Surrounding || delaying ;; Change_event_time || Battle ;; Hostile_encounter || fought ;; Hostile_encounter || driven ;; Motion
withdrew ;; Escaping || withdrew ;; Escaping
ended ;; Process_end || reached ;; Arriving || campaign ;; Military_operation
event ;; Social_event || produced ;; Creating
took place ;; Process_start
event ;; Social_event || promoted ;; Cause_to_make_progress
matches ;; Competition || event ;; Social_event || took place ;; Process_start
faced ;; Perception_active || event ;; Social_event
defending ;; Defending || Matches ;; Competition
defended ;; Defending || event ;; Social_event
event ;; Social_event || drew ;; Getting || attended ;; Participation
matches ;; Competition || match ;; Competition || criticizing ;; Judgment_communication || event ;; Social_event
injuries ;; Bodily_harm
began ;; Process_start || found ;; Know
brought ;; Bringing || rest ;; Placing
allowed ;; Preventing_or_letting || go ;; Motion || supply ;; Supply || increase ;; Expansion || reinvigorated ;; Recovering
disease ;; Catastrophe || effects ;; Influence || resulted in ;; Causation || decline ;; Agree_or_refuse_to_act
ended ;; Process_end
selected ;; Choosing || party ;; Social_event
effects ;; Influence
attacked ;; Attack || pushed ;; Motion || called ;; Name_conferral
arrive ;; Arriving
hardships ;; Hindering || arrived ;; Arriving || came ;; Arriving || came to ;; Motion
arrived ;; Arriving || attracted ;; Expansion
expanded ;; Expansion
grew ;; Expansion
built ;; Building
written ;; Statement
chosen ;; Choosing || adopted ;; Choosing
became ;; Becoming
developed ;; Coming_to_be || beginning ;; Process_start || rush ;; Motion
using ;; Using || panning ;; Judgment_communication
developed ;; Coming_to_be || caused ;; Causation || adopted ;; Choosing
developed ;; Coming_to_be || came ;; Motion || service ;; Supply
built ;; Building
increasing ;; Expansion || reached ;; Achieve
participated ;; Participation || earned ;; Earnings_and_losses || led to ;; Causation || started ;; Process_start || recovered ;; Recovering
played ;; Competition || match ;; Competition
scored ;; Getting || took ;; Getting
create ;; Creating || possession ;; Control
asked ;; Request || wearing ;; Wearing || play ;; Competition || change ;; Exchange
allowing ;; Preventing_or_letting || used ;; Using || head ;; Motion
furious ;; Response || smile ;; Change_sentiment || lobbed ;; Motion
touched ;; Connect || noticed ;; Know || play ;; Competition || scored ;; Getting
took ;; Conquering || scored ;; Getting || shot ;; Motion
coming ;; Arriving || hit ;; Connect
stayed ;; Temporary_stay || allowing ;; Giving || win ;; Preventing_or_letting
ended ;; Process_end || riot ;; Violence
filled ;; Filling || blown up ;; Destroying
attendance ;; Participation || come to ;; Motion || see ;; Perception_active || explosion ;; Destroying || rushed ;; Self_motion
damaged ;; Damaging || playing ;; Competition || explosion ;; Attack || forfeit ;; Rewards_and_punishments || required ;; Imposing_obligation
hit ;; Motion
sparked ;; Causation
fill ;; Filling || shock ;; Change_sentiment
turned in ;; Giving || destroy ;; Destroying || admission ;; Preventing_or_letting || discounted ;; Commerce_sell || record ;; Recording || collected ;; Come_together || explosion ;; Attack
closed ;; Openness || sneak ;; Self_motion
records ;; Recording || flying ;; Motion || collected ;; Come_together || thrown ;; Motion
dispersed ;; Dispersal || collected ;; Come_together || remained ;; Temporary_stay || blew up ;; Destroying || stormed ;; Damaging
postponed ;; Change_event_time || forfeited ;; Preventing_or_letting
helped ;; Assistance || preceded ;; Process_start || described ;; Statement || precipitate ;; Motion || decline ;; Cause_change_of_position_on_a_scale
remains ;; Temporary_stay || well known ;; Know
intervened ;; Response
opening ;; Openness || occurred ;; Presence
known ;; Know || occurred ;; Presence
occurred ;; Presence || shot down ;; Use_firearm
scored ;; Getting
scored ;; Getting
flying ;; Motion
fired ;; Use_firearm || shooting down ;; Use_firearm || travelling ;; Traveling
killed ;; Killing
impact ;; Influence || died ;; Death
brief ;; Telling || shot down ;; Use_firearm
opening ;; Openness
pressed ;; Motion || crashed ;; Catastrophe || flying ;; Motion
fired ;; Use_firearm
kill ;; Killing
flying ;; Motion || take ;; Conquering || shot ;; Use_firearm
warfare ;; Hostile_encounter || bombing ;; Attack || war ;; Military_operation
went ;; Motion || unarmed ;; Bearing_arms
fired ;; Use_firearm
piloted ;; Motion || protect ;; Preventing_or_letting
avoided ;; Preventing_or_letting || launching ;; Motion || launched ;; Motion || turning ;; Change
decided ;; Deciding || evade ;; Escaping
pulled ;; Motion
fired ;; Use_firearm || bombing ;; Attack
gave ;; Giving || forced ;; Motion
fired ;; Use_firearm || bombing ;; Attack || made it ;; Causation
made it ;; Causation
using ;; Using
stayed ;; Temporary_stay
fired ;; Use_firearm || bombing ;; Attack || run ;; Motion || damaging ;; Damaging
using ;; Using || brief ;; Telling || shot down ;; Use_firearm
destroyed ;; Destroying
gave ;; Giving || piloted ;; Motion || detected ;; Know
piloted ;; Motion || turned ;; Change
flew ;; Motion
fired ;; Use_firearm || killing ;; Killing || scoring ;; Getting
defensive ;; Defending || gained ;; Getting || throwing ;; Motion
break ;; Bodily_harm || dropped ;; Motion
seeing ;; Traveling || fled ;; Escaping || disengaged ;; Escaping || shot down ;; Use_firearm
turned ;; Change || reversed ;; Change || set off ;; Departing || prompting ;; Causation
gained ;; Getting || due ;; Causation || shoot down ;; Use_firearm
shot down ;; Use_firearm
sliced ;; Bodily_harm || causing ;; Causation
committed ;; Commitment || climbed ;; Motion || identify ;; Check
passed ;; Process_end || identified ;; Check || turned ;; Change
get ;; Getting || firing ;; Use_firearm || failed ;; Earnings_and_losses || turning ;; Becoming
turning ;; Becoming
obtain ;; Getting
rescued ;; Rescuing || broke ;; Bodily_harm || evacuated ;; Removing
fired ;; Use_firearm || shot down ;; Use_firearm
destroyed ;; Destroying
shooting ;; Use_firearm || shooting ;; Use_firearm || occurred ;; Coming_to_be
wounded ;; Bodily_harm || killed ;; Killing || shot ;; Use_firearm
injured ;; Bodily_harm || escape ;; Escaping
committed ;; Killing || suicide ;; Killing || shooting ;; Use_firearm || stormed ;; Catastrophe
shooting ;; Use_firearm
committed ;; Commitment || shooting ;; Use_firearm || shooting ;; Use_firearm || surpassed ;; Cause_change_of_position_on_a_scale
received ;; Receiving || attacks ;; Use_firearm || criticism ;; Judgment_communication
violence ;; Violence || issues ;; Creating || issues ;; Creating || sparked ;; Lighting
criticized ;; Judgment_communication || aired ;; Expressing_publicly
diagnosed ;; Check
received ;; Receiving || support ;; Supporting
enrolled ;; Becoming_a_member || graduating ;; Education_teaching
granted ;; Giving
accused ;; Judgment_communication || stalking ;; Self_motion
declared ;; Expressing_publicly || allowed ;; Preventing_or_letting || investigation ;; Criminal_investigation || institutionalized ;; Institutionalization || purchase ;; Commerce_buy || ordered ;; Request || attend ;; Participation
prompted ;; Causation || allowed ;; Preventing_or_letting || legal ;; Legality || close ;; Openness || purchase ;; Commerce_buy || shooting ;; Use_firearm
control ;; Control || led to ;; Causation
strengthening ;; Cause_change_of_strength || signed ;; Sign_agreement
assigned ;; Choosing
criticized ;; Judgment_communication || reduced ;; Cause_change_of_position_on_a_scale || casualties ;; Death
report ;; Reporting || pointed out ;; Reporting || left ;; Causation
occurred ;; Coming_to_be
stored ;; Preserving || dismantled ;; Destroying || recovered ;; Recovering
exposed ;; Reveal_secret || sought ;; Motion
suspect ;; Suspicion || notified ;; Telling
sent ;; Sending || locate ;; Placing || contain ;; Containing
found ;; Know || died ;; Death || resulted in ;; Causation || hospitalized ;; Institutionalization
flight ;; Self_motion
rolling ;; Motion || take-off ;; Self_motion || felt ;; Perception_active || aborted ;; Process_end
lost ;; Earnings_and_losses || stop ;; Motion || losing ;; Earnings_and_losses || crossed ;; Motion_directional || hit ;; Motion || hitting ;; Motion || hitting ;; Motion || bursting ;; Destroying
injuries ;; Bodily_harm || died ;; Death || carried out ;; Action || burns ;; Bodily_harm
hospitalized ;; Institutionalization
cause ;; Causation || creating ;; Creating
passed ;; Motion_directional || takeoff ;; Self_motion || knowing ;; Know || aborted ;; Process_end
determined ;; Deciding || investigations ;; Criminal_investigation
noted ;; Know || training ;; Education_teaching
recording ;; Recording || recordings ;; Recording
became ;; Becoming || report ;; Reporting || began ;; Process_start || going wrong ;; Damaging
played ;; Self_motion
disaster ;; Catastrophe || accident ;; Catastrophe || occurred ;; Presence
disaster ;; Catastrophe || disaster ;; Catastrophe || disasters ;; Catastrophe
started ;; Process_start || used ;; Using || accident ;; Catastrophe || test ;; Testing
aid ;; Assistance || provide ;; Supply || test ;; Testing
identified ;; Check || operating ;; Being_in_operation || cause ;; Causation || overheat ;; Destroying
conducted ;; Action || provide ;; Supply || tests ;; Testing
present ;; Presence || test ;; Testing || operating ;; Being_in_operation || shift ;; Change || delayed ;; Change_event_time || prepared ;; GetReady
combined ;; Cause_to_amalgamate || creating ;; Creating || operating ;; Being_in_operation || disabling ;; Destroying || test ;; Testing || resulted in ;; Causation
destructive ;; Destroying || superheated ;; Change || cooling ;; Change || rupturing ;; Destroying || explosion ;; Attack
contained ;; Containing
explosion ;; Attack
rising ;; Cause_change_of_position_on_a_scale || created ;; Creating
evacuated ;; Emptying
increased ;; Cause_change_of_position_on_a_scale || evacuated ;; Emptying
spread ;; Dispersal || protect ;; Preventing_or_letting || wreckage ;; Catastrophe || reduce ;; Cause_change_of_position_on_a_scale
protective ;; Preventing_or_letting
built ;; Building || finished ;; Process_end || provide ;; Supply || protection ;; Preventing_or_letting || undamaged ;; Destroying
Due ;; Causation
allows ;; Preventing_or_letting
scheduled ;; Statement
killed ;; Killing || operating ;; Being_in_operation
hospitalized ;; Institutionalization || response ;; Response || acute ;; Bodily_harm || due ;; Causation || ionizing ;; Change || absorbing ;; Ingestion
died ;; Death || deaths ;; Death
documented ;; Recording || deaths ;; Death
found ;; Know || published ;; Publishing || Due ;; Causation || increased ;; Cause_change_of_position_on_a_scale || deaths ;; Death || reviewed ;; Scrutiny || exposure ;; Causation
Determining ;; Deciding || used ;; Using || deaths ;; Death || exposure ;; Causation
death ;; Death
cost ;; Cost || casualties ;; Catastrophe || disaster ;; Catastrophe
cost ;; Cost || safeguard ;; Preventing_or_letting || accident ;; Catastrophe || involved ;; Cause_to_be_included
remaining ;; Temporary_stay || accident ;; Catastrophe || prompted ;; Cause_change_of_position_on_a_scale
movement ;; Social_event || creation ;; Creating || empowerment ;; Cause_change_of_position_on_a_scale
movement ;; Social_event || ranging ;; Cause_to_be_included || armed ;; Hold || movement ;; Social_event || grew ;; Coming_to_be || experimented ;; Testing || struggle ;; Hostile_encounter
movement ;; Social_event || movement ;; Social_event || changing ;; Change || served ;; Causation
founded ;; Know || printing ;; Change_sentiment || services ;; Supply || Motivated ;; Causation
movement ;; Social_event || includes ;; Cause_to_be_included || impact ;; Influence || revolution ;; Reforming_a_system
movement ;; Social_event || party ;; Participation || influenced ;; Influence || seen ;; Perception_active
events ;; Social_event || decolonization ;; Institutionalization || revolution ;; Reforming_a_system || influenced ;; Influence
abandon ;; GiveUp || movement ;; Social_event || movement ;; Social_event || killed ;; Killing || prompting ;; Cause_change_of_position_on_a_scale || conflicts ;; Violence
express ;; Expressing_publicly || crashed ;; Catastrophe
crashed ;; Catastrophe
perished ;; Death
noted ;; Know
approach ;; Arriving || lowered ;; Cause_change_of_position_on_a_scale
reduce ;; Cause_change_of_position_on_a_scale || looming ;; Coming_to_be || impact ;; Influence
struck ;; Attack
skidded ;; Motion || curved ;; Motion_directional || coming to ;; Motion || struck ;; Motion
elevation ;; Cause_change_of_position_on_a_scale || summit ;; Social_event
died ;; Death || died ;; Death
hospitalized ;; Institutionalization || died ;; Death
suffered ;; Bodily_harm || injuries ;; Bodily_harm
lost ;; Earnings_and_losses
met ;; Come_together || searching ;; Scrutiny || managed ;; Control
became ;; Becoming || broken ;; Destroying || recovered ;; Recovering
investigated ;; Criminal_investigation
coupled ;; Cause_to_amalgamate || attributed ;; Causation
shooting ;; Use_firearm || died ;; Death || took place ;; Process_start
hospitalized ;; Institutionalization || rehabilitation ;; Cure || suffered ;; Bodily_harm
declared ;; Expressing_publicly || paralyzed ;; Bodily_harm
identified ;; Check || posted ;; Placing
identify ;; Check || confirmed ;; Check || apprehended ;; Arrest
shooting ;; Use_firearm || used ;; Using
began ;; Process_start || started ;; Process_start || shooting ;; Use_firearm || shooting ;; Use_firearm || revealed ;; Reveal_secret
told ;; Telling || sitting ;; Placing || chosen ;; Choosing || stated ;; Statement || targeted ;; Aiming || know ;; Know
shooting ;; Use_firearm || occurred ;; Presence || left ;; Departing
stated ;; Statement || arrested ;; Arrest
indicted ;; Criminal_investigation
detained ;; Arrest || pending ;; Change_event_time
prompted ;; Causation
held ;; Building || established ;; Building
attack ;; Attack || attended ;; Participation || spoke ;; Expressing_publicly
gave ;; Giving
began ;; Process_start || shooting ;; Use_firearm || attacks ;; Attack
determined ;; Deciding
decision ;; Deciding || made ;; Creating || charge ;; Legality
received ;; Receiving || pleaded ;; Request
escaped ;; Escaping
recaptured ;; Conquering
took place ;; Process_start || uprising ;; Change_of_leadership || revolt ;; Attack
removed ;; Removing || assumption ;; Earnings_and_losses || protest ;; Protest
rebels ;; Change_of_leadership || events ;; Social_event || occurred ;; Coming_to_be || called ;; Name_conferral
uprising ;; Change_of_leadership || took place ;; Process_start || suppressed ;; Hindering
renamed ;; Name_conferral || event ;; Social_event || name ;; Name_conferral || name ;; Name_conferral || changed ;; Change
reformation ;; Reforming_a_system || encouraged ;; Influence
creating ;; Creating || rapprochement ;; Recovering || encouraged ;; Influence
reform ;; Reforming_a_system
helped ;; Assistance || reform ;; Reforming_a_system || formulation ;; Creating
increased ;; Cause_change_of_position_on_a_scale
developed ;; Coming_to_be || invading ;; Attack || feared ;; Change_sentiment || plans ;; Arranging || reorganization ;; Cause_to_amalgamate
Returned ;; Arriving || exile ;; Removing || task ;; Confronting_problem || appointed ;; Change_of_leadership || reforming ;; Reforming_a_system
asked ;; Request || constitution ;; Writing
abolition ;; GiveUp
repressions ;; Control || liberalisation ;; Reforming_a_system || unrest ;; Violence || led to ;; Causation
committing ;; Commitment || Wars ;; Attack || indignities ;; Change_sentiment || wearing ;; Wearing || dance ;; Body_movement || spurred ;; Causation || realising ;; Coming_to_believe || forced ;; Causation || rejecting ;; Response || indicating ;; Causation
captured ;; Conquering || embrace ;; Motion || movements ;; Social_event
declaration ;; Expressing_publicly || influenced ;; Influence || movement ;; Social_event || independence ;; Change_of_leadership || revolution ;; Social_event || noted ;; Labeling
name ;; Name_conferral
Constitution ;; Writing || Constitution ;; Writing || written ;; Writing
become ;; Becoming
revolutionary ;; Change_of_leadership || approved ;; Agree_or_refuse_to_act
revolutionary ;; Change_of_leadership || requested ;; Request || conceding ;; Agree_or_refuse_to_act || partaking ;; Participation || agree ;; Agree_or_refuse_to_act || establishment ;; Creating || involvement ;; Participation || federalization ;; Institutionalization
founded ;; Creating
revolutionary ;; Change_of_leadership || joined ;; Becoming_a_member || repressions ;; Control || acquired ;; Getting || annihilation ;; Destroying
supported ;; Supporting || transfer ;; Change || liberate ;; Conquering || consent ;; Commitment || influence ;; Influence
revolution ;; Change_of_leadership || kill ;; Killing
refused ;; Agree_or_refuse_to_act || kill ;; Killing || left ;; Departing || consisting ;; Cause_to_be_included
revolutionary ;; Change_of_leadership || illegal ;; Legality || created ;; Creating || plans ;; Arranging
joined ;; Becoming_a_member
decided ;; Deciding || mutiny ;; Change_of_leadership || suspend ;; Process_end
function ;; Being_in_operation
replaced ;; Change_of_leadership || aims ;; Aiming || abolition ;; GiveUp || abolition ;; GiveUp || excluding ;; Preventing_or_letting
rest ;; Placing || establish ;; Creating || influence ;; Influence || abolish ;; GiveUp || redistribute ;; Dispersal || taking ;; Bringing
established ;; Creating
program ;; Social_event
joined ;; Becoming_a_member || exchange ;; Exchange || program ;; Social_event || adopted ;; Choosing
occurred ;; Coming_to_be || crash ;; Catastrophe || crashed ;; Catastrophe
fire ;; Catastrophe || struck ;; Attack || extinguished ;; Destroying
killed ;; Killing
injured ;; Bodily_harm
hospitalized ;; Institutionalization || fire ;; Catastrophe || burns ;; Bodily_harm
stated ;; Statement || crash ;; Catastrophe
flying ;; Motion || determine ;; Deciding || crash ;; Catastrophe
armed ;; Bearing_arms || organized ;; Arranging
social ;; Social_event || party ;; Participation
resist ;; Defending || overthrow ;; Change_of_leadership || launched ;; Process_start
defeated ;; Conquering
uprising ;; Change_of_leadership || hijack ;; Kidnapping
presented ;; Presence || initiated ;; Process_start || governed ;; Control
uprising ;; Change_of_leadership || oust ;; Removing || reestablish ;; Institutionalization || claim ;; Statement || dominated ;; Control
fought ;; Hostile_encounter || uprising ;; Change_of_leadership
support ;; Supporting
participants ;; Participation
invasion ;; Attack || assault ;; Attack || support ;; Supporting || led ;; Causation
topple ;; Change_of_leadership || invasion ;; Attack || step down ;; Change_of_leadership || defiance ;; Agree_or_refuse_to_act || refused ;; Agree_or_refuse_to_act || election ;; Change_of_leadership || objective ;; Aiming
independence ;; Change_of_leadership
invasion ;; Attack || occurred ;; Presence
control ;; Control || declared ;; Expressing_publicly
requested ;; Request || escape ;; Escaping || managed ;; Hold
held ;; Defending || brought ;; Bringing
rejected ;; Agree_or_refuse_to_act || request ;; Request || extradited ;; Extradition || ruled ;; Legal_rulings
overthrew ;; Change_of_leadership || bombing ;; Attack || signed ;; Sign_agreement
bombing ;; Attack || invasion ;; Attack
defend ;; Defending || struck ;; Attack
disclosing ;; Reveal_secret || locations ;; Placing || defected ;; Escaping
attacked ;; Attack || attacks ;; Attack
destruction ;; Destroying || attacks ;; Attack || control ;; Control || widespread ;; Dispersal || resulted in ;; Causation || command ;; Control || casualties ;; Death
bombing ;; Attack || attacks ;; Attack || preceded ;; Process_start
bombing ;; Attack || targets ;; Aiming || struck ;; Attack || loss ;; Earnings_and_losses || burned ;; Destroying
raids ;; Attack || surrendered ;; Surrendering || bombing ;; Attack || attacks ;; Attack || carried out ;; Action
captured ;; Conquering || bombing ;; Attack || bombing ;; Attack || end ;; Process_end || war ;; Hostile_encounter || war ;; Hostile_encounter
arrested ;; Arrest || war ;; Hostile_encounter
extradited ;; Extradition || found ;; Know
erected ;; Building || defense ;; Defending || killed ;; Killing
bombing ;; Attack
departed ;; Departing
included ;; Cause_to_be_included
demanded ;; Request || hijacked ;; Kidnapping
claimed ;; Statement
convinced ;; Convincing
landing ;; Motion || overpowered ;; Hold
extradited ;; Extradition || sentenced ;; Legal_rulings
carrying ;; Sending || crashed ;; Catastrophe
killed ;; Killing || crash ;; Catastrophe || inspired ;; Influence
deported ;; Extradition || returned ;; Arriving
reports ;; Reporting || listed ;; Writing || listed ;; Writing
identified ;; Check
marked ;; Recording || placed ;; Placing || accident ;; Catastrophe
forces ;; Hostile_encounter || opposing ;; Agree_or_refuse_to_act || Operation ;; Military_operation
aimed ;; Aiming
forces ;; Hostile_encounter || consisting ;; Cause_to_be_included || fighting ;; Hostile_encounter || took place ;; Process_start || employed ;; Employment || taken ;; Conquering
captured ;; Conquering || arrived ;; Arriving || create ;; Creating || assist ;; Assistance || rebels ;; Hostile_encounter || stopped ;; Hindering || evacuate ;; Escaping
left ;; Departing || evacuated ;; Escaping
transferred ;; Giving || control ;; Control || fighting ;; Hostile_encounter || resulted ;; Causation
blaming ;; Judgment_communication || fighting ;; Hostile_encounter || fighting ;; Hostile_encounter || deaths ;; Death || threatened ;; Warning
killed ;; Killing || treatment ;; Cure
filed ;; Submitting_documents || injuring ;; Bodily_harm || killing ;; Killing || attack ;; Attack || deaths ;; Death || leading ;; Causation || resulted in ;; Causation
arrested ;; Arrest || extradition ;; Extradition
assassinate ;; Killing || conspiracy ;; Committing_crime
assembled ;; Come_together || appointed ;; Request
charges ;; Judgment_communication || investigated ;; Scrutiny || headed ;; Self_motion || prosecution ;; Legality || marriages ;; Forming_relationships
included ;; Cause_to_be_included
obtained ;; Getting || murder ;; Killing || planned ;; Arranging || purchase ;; Commerce_buy
discovered ;; Know || assassination ;; Killing || investigation ;; Scrutiny || result ;; Causation || enforcement ;; Participation || carried out ;; Attack
began ;; Process_start || brought ;; Bringing || Prosecution ;; Legality
fled ;; Escaping || completed ;; Process_end || extradition ;; Extradition || conviction ;; Legal_rulings || prosecution ;; Legality
agreed ;; Agree_or_refuse_to_act || visit ;; Traveling || allowed ;; Preventing_or_letting || return ;; Arriving || convicted ;; Legal_rulings
received ;; Receiving || pleaded ;; Request
agreeing ;; Agree_or_refuse_to_act || left ;; Departing || pleading ;; Legality || prosecuted ;; Legality
killings ;; Killing || massacres ;; Killing
massacres ;; Killing || murdered ;; Killing || began ;; Process_start || setting ;; Placing || on fire ;; Damaging
murdered ;; Killing
killed ;; Killing
killings ;; Killing || massacred ;; Killing
killings ;; Killing || war ;; Hostile_encounter || testify ;; Testing
captured ;; Conquering || extradited ;; Extradition || committed ;; Killing || flee ;; Escaping || forces ;; Military_operation
escaped ;; Escaping || moved ;; Motion || killed ;; Killing || assumed ;; Choosing || service ;; Supply
massacres ;; Killing || killed ;; Killing
killings ;; Killing || erected ;; Building || built ;; Building || victims ;; Catastrophe || commemorate ;; Social_event
removed ;; Removing || independence ;; Creating
dismantled ;; Destroying || War ;; Hostile_encounter || Independence ;; Change_of_leadership || restore ;; Preserving
converted ;; Change
took place ;; Process_start
wounded ;; Bodily_harm || shot ;; Use_firearm || entering ;; Arriving
struck ;; Attack || suffered ;; Bodily_harm
apprehended ;; Arrest
assassination ;; Committing_crime
request ;; Request || deported ;; Extradition
took place ;; Process_start || War ;; Hostile_encounter || conflict ;; Hostile_encounter || Wars ;; Hostile_encounter
convince ;; Convincing || open ;; Process_start || War ;; Hostile_encounter || cancel ;; Change_event_time || granted ;; Preventing_or_letting
promised ;; Commitment || help ;; Assistance || suppress ;; Hindering || rebellion ;; Violence || recover ;; Recovering || lost ;; Earnings_and_losses
declared ;; Expressing_publicly || marched ;; Self_motion || war ;; Hostile_encounter
declare ;; Expressing_publicly || surrender ;; Surrendering || pressured ;; Convincing || Operation ;; Military_operation || expel ;; Extradition || war ;; Hostile_encounter
entered ;; Arriving || anchored ;; Connect || commanded ;; Control || rejection ;; Agree_or_refuse_to_act || destroyed ;; Destroying
erected ;; Building || strengthened ;; Cause_change_of_strength || help ;; Assistance
sail ;; Self_motion
embarked ;; Self_motion || occupied ;; Control || expedition ;; Traveling || defeated ;; Conquering || evacuate ;; Removing || siege ;; Besieging || support ;; Supporting || war ;; Hostile_encounter
secure ;; Getting || compliance ;; Action || agreement ;; Agree_or_refuse_to_act
concluded ;; Process_end || war ;; Hostile_encounter
died ;; Death || diving ;; Self_motion
charged ;; Judgment_communication || murder ;; Killing
guilty ;; Committing_crime || imprisonment ;; Prison || pleaded ;; Legality || manslaughter ;; Killing || sentenced ;; Legal_rulings
presented ;; Presence || included ;; Cause_to_be_included || differing ;; Change || trial ;; Criminal_investigation || accounts ;; Justifying || diving ;; Self_motion
charge ;; Judgment_communication || murder ;; Killing || intention ;; Aiming || serving ;; Assistance || flagged ;; Action
release ;; Releasing || death ;; Death || murder ;; Killing || guilty ;; Committing_crime || deported ;; Extradition || sentenced ;; Legal_rulings || found ;; Know
murder ;; Killing || due ;; Causation || put on ;; Placing || trial ;; Criminal_investigation || dismissed ;; Dispersal
war ;; Hostile_encounter || emergency ;; Emergency || uprising ;; Change_of_leadership
comprised ;; Cause_to_be_included || fought ;; Hostile_encounter || Dominated ;; Control
survived ;; Preserving || driven ;; Motion || capture ;; Conquering || defeat ;; Conquering
killed ;; Killing || attained ;; Arriving
capture ;; Conquering
cite ;; Adducing || due ;; Causation || support ;; Supporting || suggests ;; Convincing
manipulating ;; Hold || served ;; Assistance || authored ;; Create_artwork
applied ;; Using || developed ;; Coming_to_be
created ;; Creating || resulted in ;; Causation
cost ;; Cost || caused ;; Causation || Suppressing ;; Control || deaths ;; Death || order ;; Arranging
included ;; Cause_to_be_included || use ;; Using
floods ;; Catastrophe || struck ;; Attack
deaths ;; Death || reported ;; Reporting
affected ;; Influence
flooded ;; Catastrophe
given ;; Giving || rainfall ;; Catastrophe || affected ;; Influence
flooded ;; Catastrophe
flooded ;; Catastrophe
formed ;; Coming_to_be || floods ;; Catastrophe || rainfall ;; Catastrophe || affected ;; Influence
destroyed ;; Destroying || left ;; Departing
saved ;; Rescuing
caused ;; Causation || occurred ;; Presence || floods ;; Catastrophe
flooded ;; Catastrophe
havoc ;; Destroying || made ;; Causation
flooded ;; Catastrophe
damaged ;; Damaging || torn ;; Destroying
transformed ;; Change
entered ;; Arriving
formation ;; Coming_to_be || affected ;; Influence
collapse ;; Catastrophe || affected ;; Influence || leading to ;; Causation
localities ;; Placing || left ;; Departing
due ;; Causation || flooded ;; Catastrophe || shows ;; Statement
flooded ;; Catastrophe || due ;; Causation
damage ;; Damaging || hailstorms ;; Catastrophe
flooded ;; Catastrophe
left ;; Departing
Landslides ;; Catastrophe || affected ;; Influence
formed ;; Coming_to_be || fallen ;; Motion_directional
deposits ;; Placing || destroyed ;; Destroying || evacuated ;; Removing
damages ;; Damaging || destruction ;; Destroying || flooded ;; Catastrophe || following ;; Motion || evacuated ;; Removing || sheltered ;; Supply
affected ;; Influence || left ;; Departing
registered ;; Recording || affected ;; Influence
flooded ;; Catastrophe
rushed ;; Motion || hampered ;; Hindering || whirling ;; Self_motion
flooding ;; Catastrophe || exceeded ;; Cause_change_of_position_on_a_scale || collapsed ;; Destroying || said ;; Statement
record ;; Recording || registered ;; Recording
flooded ;; Catastrophe
blocked ;; Hindering || rainfall ;; Catastrophe
flooded ;; Catastrophe
storm ;; Catastrophe || suffered ;; Bodily_harm
storm ;; Catastrophe || uprooted ;; Removing
affected ;; Influence
floods ;; Catastrophe || disasters ;; Catastrophe || occurring ;; Coming_to_be
stalled ;; Temporary_stay || slow-moving ;; Motion || clashing ;; Connect
flooding ;; Catastrophe || rain ;; Motion || resulted in ;; Causation
intensified ;; Cause_change_of_strength
hit ;; Attack || recorded ;; Recording || recorded ;; Recording
reflecting ;; Influence || Flood ;; Catastrophe || event ;; Causation || referred ;; Name_conferral
stated ;; Statement || rainfall ;; Motion
affecting ;; Influence || spread ;; Dispersal
declared ;; Expressing_publicly || emergency ;; Emergency || disaster ;; Catastrophe
emergency ;; Emergency || declarations ;; Expressing_publicly || covered ;; Filling
movement ;; Social_event || arose ;; Process_start
gained ;; Getting || support ;; Supporting || establishment ;; Creating || channeling ;; Bringing
presented ;; Presence || reached ;; Arriving || renewal ;; Recovering || jumble ;; Cause_to_amalgamate || contracts ;; Sign_agreement || movement ;; Social_event || Consisting ;; Cause_to_be_included
spread ;; Dispersal || strikes ;; Attack || rising ;; Cause_change_of_position_on_a_scale || marked ;; Recording || outburst ;; Process_start || violent ;; Violence || conflict ;; Hostile_encounter || rejection ;; Agree_or_refuse_to_act
control ;; Control || movement ;; Social_event
asked ;; Request || declared ;; Expressing_publicly || dissolving ;; Dispersal || restoring ;; Recovering || arresting ;; Arrest || state ;; Statement || emergency ;; Emergency || order ;; Arranging || use ;; Using
reforms ;; Reforming_a_system
strikes ;; Attack || increase ;; Cause_change_of_position_on_a_scale || suppression ;; Hindering || led to ;; Causation
causing ;; Causation || hurricane ;; Catastrophe || damage ;; Damaging || surrounding ;; Surrounding || made ;; Creating
caused ;; Causation || fatal ;; Killing || flooding ;; Motion || flood ;; Catastrophe || loss ;; Earnings_and_losses || precipitated ;; Causation
storm ;; Catastrophe || hurricane ;; Catastrophe || record ;; Recording || hurricane ;; Catastrophe || make ;; Creating
storm ;; Catastrophe || originated ;; Coming_to_be
strengthening ;; Cause_change_of_strength || intensified ;; Cause_change_of_strength || storm ;; Catastrophe || hurricane ;; Catastrophe || headed ;; Self_motion || making ;; Creating
emerged ;; Coming_to_be || began ;; Process_start || intensify ;; Cause_change_of_strength || storm ;; Catastrophe || weakening ;; Cause_change_of_strength
strengthened ;; Cause_change_of_strength || weakened ;; Cause_change_of_strength || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || making ;; Creating
devastating ;; Destroying || made ;; Creating || held ;; Bringing || slammed ;; Motion
caused ;; Causation || died ;; Death || destruction ;; Destroying || casualties ;; Catastrophe || hurricane ;; Catastrophe || making ;; Causation
reached ;; Arriving || damage ;; Damaging || occurred ;; Process_start || rammed ;; Damaging || pushing ;; Self_motion
damage ;; Damaging || damage ;; Damaging || wrought ;; Causation || tying ;; Connect
destruction ;; Destroying || surrounding ;; Surrounding || cause ;; Causation || death ;; Death || breaches ;; Damaging
became ;; Becoming || flooded ;; Motion
became ;; Becoming || damaged ;; Damaging || disabled ;; Destroying || flooding ;; Catastrophe || evacuated ;; Escaping
provoked ;; Causation || evacuated ;; Emptying || disaster ;; Catastrophe || response ;; Response || rescue ;; Rescuing
built ;; Building || control ;; Control || storm ;; Catastrophe || investigations ;; Scrutiny || concluded ;; Coming_to_believe || designed ;; Creating || ruled ;; Legal_rulings
investigations ;; Scrutiny || responses ;; Response || criticisms ;; Judgment_communication || resignations ;; Change_of_leadership || resulted in ;; Causation
criticized ;; Judgment_communication || responses ;; Response
service ;; Supply || commended ;; Judgment_communication
found ;; Know || provided ;; Supply || hurricane ;; Catastrophe
bushfires ;; Catastrophe || bushfires ;; Catastrophe || broke out ;; Process_start
coupled ;; Cause_to_amalgamate || fires ;; Catastrophe || contributed ;; Influence
caused ;; Causation || downed ;; Motion_directional
fires ;; Catastrophe || service ;; Supply || tasked ;; Request || overwhelmed ;; Control
requested ;; Request || assistance ;; Assistance
volunteered ;; Assistance
contained ;; Containing || change ;; Change || fires ;; Catastrophe || assistance ;; Assistance || thanks ;; Judgment_communication || widespread ;; Dispersal
caused ;; Causation || destruction ;; Destroying || damage ;; Damaging || deaths ;; Death || resulted in ;; Causation || fires ;; Catastrophe || buildings ;; Building || destroyed ;; Destroying
escape ;; Escaping
escaped ;; Escaping || death ;; Death || sheltering ;; Placing
stretching ;; Expansion || put ;; Placing
event ;; Social_event || produced ;; Creating || took place ;; Process_start
event ;; Social_event || event ;; Social_event
event ;; Social_event || show ;; Social_event || promoted ;; Cause_change_of_position_on_a_scale
matches ;; Competition || match ;; Competition || involved ;; Participation
fought ;; Hostile_encounter || event ;; Social_event
match ;; Competition || emergency ;; Emergency || cleared ;; Protest || wrestle ;; Body_movement
match ;; Competition || determine ;; Deciding || held ;; Hold || replacement ;; Exchange
show ;; Social_event || defeat ;; Conquering || win ;; Earnings_and_losses
match ;; Competition || defended ;; Defending
event ;; Social_event
retain ;; Preserving || defeated ;; Conquering
held ;; Hold || defeated ;; Conquering
defended ;; Defending || show ;; Social_event
competed ;; Competition || match ;; Competition || determine ;; Deciding
earn ;; Getting || match ;; Competition || encounter ;; Hostile_encounter
removed ;; Removing || event ;; Social_event || remembered ;; Change_sentiment || victory ;; Earnings_and_losses
becoming ;; Becoming || ranked ;; Placing
died ;; Death || event ;; Social_event || dedicated ;; Giving
show ;; Social_event
received ;; Receiving || held ;; Hold || given ;; Giving || event ;; Social_event
became ;; Becoming || launch ;; Process_start || stream ;; Supply || event ;; Social_event
kidnapped ;; Kidnapping || stop ;; Process_end || hitchhiking ;; Self_motion
kidnapping ;; Kidnapping || report ;; Reporting || called ;; Communication
recording ;; Recording || order ;; Arranging || leaked ;; Reveal_secret
kidnapped ;; Kidnapping || recorded ;; Recording || whispered ;; Telling || shouting ;; Vocalizations
killed ;; Killing || knew ;; Know || suspected ;; Suspicion || dumped ;; Placing
defense ;; Defending || initiated ;; Process_start || operation ;; Military_operation || search ;; Scouring
arrested ;; Arrest || operation ;; Military_operation
killed ;; Killing || operation ;; Military_operation
kidnapped ;; Kidnapping || said ;; Telling
kidnapping ;; Kidnapping
stated ;; Statement || occur ;; Presence || crime ;; Committing_crime || kidnappings ;; Kidnapping
reported ;; Reporting
used ;; Using
organized ;; Arranging || arrested ;; Arrest || said ;; Communication || killing ;; Killing || suspected ;; Suspicion
financed ;; Supply
stated ;; Statement || confessed ;; Reveal_secret
stated ;; Statement || interrogation ;; Criminal_investigation || kidnapping ;; Kidnapping
released ;; Releasing || kidnapping ;; Kidnapping
terrorism ;; Terrorism || kidnapping ;; Kidnapping || arrested ;; Arrest || said ;; Telling || kidnapping ;; Kidnapping || stated ;; Statement
abduction ;; Kidnapping || record ;; Recording || said ;; Communication
found ;; Know
dead ;; Death || abduction ;; Kidnapping || shot ;; Use_firearm
response ;; Response || vowed ;; Commitment || killings ;; Killing
armed ;; Bearing_arms || kidnapping ;; Kidnapping || kidnapping ;; Kidnapping || organization ;; Arranging || operation ;; Military_operation || identified ;; Check || murder ;; Killing
kidnapping ;; Kidnapping || headed ;; Motion || investigating ;; Criminal_investigation
investigation ;; Criminal_investigation || arrested ;; Arrest || capture ;; Arrest || delayed ;; Change_event_time
made ;; Manufacturing || comments ;; Statement || conference ;; Communication
defense ;; Defending || kidnapping ;; Kidnapping || boasting ;; Statement
kidnapped ;; Kidnapping || murdered ;; Killing || stated ;; Statement || taken ;; Conquering || investigation ;; Scrutiny || said ;; Telling
involved ;; Cause_to_be_included || wing ;; Motion
release ;; Releasing
contains ;; Containing || strengthens ;; Cause_change_of_strength || organized ;; Arranging || incident ;; Killing
manage ;; Control
ready ;; GetReady || appears ;; Presence
killed ;; Killing || end ;; Process_end
jailed ;; Prison || murders ;; Killing || sentenced ;; Legal_rulings
pay ;; Commerce_pay
crashed ;; Catastrophe || crash ;; Catastrophe || flight ;; Motion || accident ;; Catastrophe || occurred ;; Presence
departure ;; Departing || heading ;; Self_motion || climbing ;; Motion_directional || turned on ;; Motion
began ;; Process_start || descent ;; Motion || turned ;; Becoming
crashed ;; Catastrophe
died ;; Death
control ;; Control || emergency ;; Emergency || established ;; Coming_to_be
carrying ;; Bringing || sank ;; Motion_directional
reported ;; Reporting || sailed ;; Self_motion
response ;; Response || resulted in ;; Causation || rescue ;; Rescuing || emergency ;; Emergency || involving ;; Cause_to_be_included
confirmed ;; Check || reported ;; Reporting || reported ;; Reporting || deaths ;; Death || death ;; Death || figure ;; Perception_active || searching ;; Scouring
occurred ;; Coming_to_be || rescue ;; Rescuing || search ;; Scouring
confirmed ;; Check || carrying ;; Bringing || dead ;; Death
began ;; Process_start
experienced ;; Perception_active
fell ;; Motion_directional
warnings ;; Warning || issued ;; Expressing_publicly
fell ;; Motion_directional
affected ;; Influence || surrounding ;; Surrounding || settled ;; Creating
recorded ;; Recording || fell ;; Motion_directional
widespread ;; Judgment_communication || affected ;; Influence
fallen ;; Motion_directional
fallen ;; Motion_directional
removed ;; Removing || service ;; Supply || delays ;; Change_event_time
services ;; Supply || services ;; Supply || cancelled ;; Change_event_time
operated ;; Being_in_operation || reduced ;; Cause_change_of_position_on_a_scale || service ;; Supply
occurred ;; Presence || Connect ;; Connect || services ;; Supply || services ;; Supply || services ;; Supply || services ;; Supply || disruption ;; Preventing_or_letting
services ;; Supply || disrupted ;; Hindering || disrupted ;; Hindering
departures ;; Departing || closed ;; Process_end || cancelled ;; Change_event_time
affected ;; Influence
service ;; Supply || suspended ;; Change
allow ;; Preventing_or_letting || delaying ;; Change_event_time || closed ;; Process_end
cancelled ;; Change_event_time
effects ;; Influence || included ;; Cause_to_be_included || lost ;; Earnings_and_losses || disruption ;; Preventing_or_letting
work ;; Employment || underestimated ;; Judgment_communication
caused ;; Causation || close ;; Process_end
caused ;; Causation || swept ;; Scrutiny || bringing ;; Bringing || storm ;; Catastrophe || flooding ;; Catastrophe
closed ;; Process_end
recorded ;; Recording || recorded ;; Recording
reported ;; Reporting
disrupted ;; Hindering
leaving ;; Departing
moved ;; Motion || learned ;; Education_teaching || attack ;; Attack || desertions ;; Escaping
break ;; Destroying || venturing ;; Risk || prevent ;; Preventing_or_letting
withdrew ;; Escaping
withdrew ;; Escaping
postponed ;; Change_event_time || sending ;; Sending || saved ;; Rescuing || stationed ;; Placing
starting ;; Process_start || wars ;; Hostile_encounter || involved ;; Cause_to_be_included
fought ;; Hostile_encounter || controlled ;; Control || allied ;; Collaboration
defeated ;; Conquering || aided ;; Assistance
found ;; Know || crowned ;; Change_of_leadership || stated ;; Statement
invaded ;; Attack || led to ;; Causation || occupied ;; Control
wars ;; Hostile_encounter || war ;; Hostile_encounter
helped ;; Assistance || spark ;; Causation || wars ;; Hostile_encounter || risen ;; Cause_change_of_position_on_a_scale || revolt ;; Change_of_leadership
resist ;; Defending || covenant ;; Commitment
expressed ;; Expressing_publicly || become ;; Becoming || covenant ;; Commitment || practice ;; Practice
resisted ;; Defending || impose ;; Action || raised ;; Cause_change_of_position_on_a_scale
triggered ;; Causation || uprising ;; Change_of_leadership
persuade ;; Convincing || pay ;; Commerce_pay
raising ;; Cause_change_of_position_on_a_scale || proposed ;; Creating || return ;; Arriving
alarmed ;; Warning || invade ;; Attack || threatened ;; Warning
massacres ;; Killing || response ;; Response || launched ;; Process_start || degenerated ;; Change
sparked ;; Causation || used ;; Using || command ;; Control || fearing ;; Risk
broke out ;; Process_start
protect ;; Defending || sent ;; Sending
signing ;; Sign_agreement || covenant ;; Commitment || fight ;; Hostile_encounter || sent ;; Sending
raid ;; Attack || bombing ;; Attack
raid ;; Attack
daring ;; Risk || attack ;; Attack || filmed ;; Create_artwork
requested ;; Request || debate ;; Quarreling || attack ;; Attack
destroying ;; Destroying
killed ;; Killing || escaped ;; Escaping || recaptured ;; Conquering || wounded ;; Bodily_harm
Battle ;; Hostile_encounter || forces ;; Causation || battle ;; Hostile_encounter || War ;; Military_operation
retreat ;; Escaping || forces ;; Causation || garnered ;; Getting || meet ;; Come_together
organise ;; Arranging
broke ;; Destroying || fled ;; Escaping || battle ;; Hostile_encounter || reinforced ;; Cause_change_of_strength || encounter ;; Hostile_encounter
riding ;; Self_motion || seeing ;; Traveling || regrouping ;; Cause_to_amalgamate || danger ;; Risk || inspire ;; Change_sentiment
made ;; Manufacturing || failed ;; Earnings_and_losses || prisoner ;; Arrest
gained ;; Getting || control ;; Control || pushed ;; Self_motion
Battle ;; Hostile_encounter || returning ;; Arriving
Tournament ;; Competition || Tournament ;; Competition || championships ;; Competition
held ;; Hold
tied ;; Connect
leave ;; Departing || entered ;; Arriving || withdrew ;; Escaping
win ;; Earnings_and_losses
finished ;; Process_end || remained ;; Presence
Festival ;; Social_event || revived ;; Recovering || Festival ;; Social_event
sales ;; Commerce_sell || attendance ;; Participation || festival ;; Social_event || increase ;; Cause_change_of_position_on_a_scale || growing ;; Expansion || festival ;; Social_event || testament ;; Recording || success ;; Achieve
sponsorship ;; Supply || likes ;; Perception_active
grace ;; Rewards_and_punishments
began ;; Process_start || festival ;; Social_event || festivals ;; Social_event || claim ;; Statement
line-up ;; Arranging || Attracting ;; Influence
event ;; Social_event || event ;; Social_event || highlights ;; Achieve || filmed ;; Create_artwork || shown ;; Publishing
include ;; Cause_to_be_included || scheduled ;; Arranging || pull out ;; GiveUp || replaced ;; Change_of_leadership
killings ;; Killing || took place ;; Process_start
shot ;; Use_firearm || killed ;; Killing
terrorist ;; Terrorism || attack ;; Attack || aimed ;; Aiming || carried out ;; Action || called ;; Name_conferral || fear ;; Risk
criticized ;; Judgment_communication
sentenced ;; Legal_rulings
imprisoned ;; Arrest
ordered ;; Arranging
found ;; Know || established ;; Creating
defense ;; Defending || used ;; Using || insisted ;; Statement
took place ;; Process_start || disrupt ;; Hindering
arrived in ;; Arriving
driven ;; Motion || sailed ;; Self_motion
took ;; Bringing || sheltering ;; Placing
avoid ;; Preventing_or_letting || escorting ;; Patrolling || left ;; Departing
sailing ;; Self_motion
met ;; Come_together
fled ;; Escaping || decided ;; Deciding || knew ;; Know || overwhelm ;; Destroying
resembled ;; Change_sentiment || lead ;; Causation || formed ;; Coming_to_be
ordered ;; Arranging || advance ;; Self_motion
retreated ;; Escaping || declining ;; Agree_or_refuse_to_act || risk ;; Risk || facing ;; Confronting_problem || risk ;; Risk
declined ;; Agree_or_refuse_to_act || reconsider ;; Deciding || attack ;; Attack || withdrew ;; Escaping
learned ;; Know
lost ;; Earnings_and_losses || reached ;; Arriving || wrecked ;; Destroying
shot ;; Use_firearm || killing ;; Killing || drew ;; Motion || injuring ;; Bodily_harm
became ;; Becoming || described ;; Statement || incident ;; Attack || riot ;; Attack
combined ;; Cause_to_amalgamate
gave ;; Giving || agree ;; Agree_or_refuse_to_act || give ;; Giving || hire ;; Employment || discontinue ;; Process_end || fire ;; Process_end || regard ;; Know || accept ;; Agree_or_refuse_to_act || meetings ;; Social_event
refused ;; Agree_or_refuse_to_act || locked ;; Self_motion || accept ;; Agree_or_refuse_to_act
break up ;; Destroying || suppress ;; Control || authorised ;; Preventing_or_letting || introduced ;; Statement || designed ;; Creating
demonstrated ;; Protest || taken over ;; Change_of_leadership
protect ;; Defending || allow ;; Preventing_or_letting || called in ;; Come_together
marched ;; Self_motion
defensive ;; Defending || met with ;; Come_together || bearing ;; Bearing_arms || clashes ;; Hostile_encounter
heads ;; Self_motion || fired ;; Process_end || draw ;; Motion || order ;; Arranging
received ;; Receiving || fatal ;; Killing
risked ;; Risk || help ;; Assistance || ran ;; Self_motion
described ;; Statement || telegraph ;; Communication || shocked ;; Response
returned ;; Arriving || reduced ;; Cause_change_of_position_on_a_scale || capitulated ;; Sign_agreement
break ;; Bodily_harm || resolve ;; Resolve_problem
restoring ;; Recovering || used ;; Using
trademarked ;; Sign_agreement
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
granted ;; Giving || become ;; Becoming || dynasty ;; Change_of_leadership || installed ;; Placing
legitimate ;; Legality || married ;; Forming_relationships
claimed ;; Statement || rising ;; Cause_change_of_position_on_a_scale || followed ;; Removing
married ;; Forming_relationships
received ;; Receiving || quarrels ;; Quarreling || settled ;; Resolve_problem || aid ;; Assistance || installed ;; Change_of_leadership
revolted ;; Change_of_leadership || campaigned ;; Military_operation || rekindled ;; Recovering || conflict ;; Hostile_encounter
allied ;; Collaboration || marched ;; Self_motion
lost ;; Earnings_and_losses || devastation ;; Destroying || invasion ;; Attack || recover ;; Recovering
gathered ;; Come_together || victims ;; Death || estimate ;; Statement
eyed ;; Perception_active || met ;; Come_together
cross ;; Motion_directional || proposed ;; Statement || reach ;; Arriving || agreement ;; Sign_agreement || give ;; Giving || go ;; Self_motion || dared ;; Risk || troops ;; Motion || withdraw ;; Escaping
reached ;; Arriving || attack ;; Attack || pulled ;; Self_motion || started ;; Process_start || went ;; Motion
injured ;; Bodily_harm || forces ;; Causation || managed ;; Resolve_problem || repulse ;; Attack
drowned ;; Death || returning ;; Arriving || on the run ;; Escaping
fight ;; Hostile_encounter || battles ;; Hostile_encounter || supplying ;; Supply || considered ;; Statement
arranged ;; Arranging || marriage ;; Forming_relationships || victory ;; Earnings_and_losses || renounced ;; Surrendering
continued ;; Motion || challenge ;; Confronting_problem
includes ;; Cause_to_be_included || produced ;; Manufacturing || referred ;; Adducing || Fest ;; Social_event
festival ;; Social_event || made ;; Creating || debut ;; Process_start || expanded ;; Expansion
entitled ;; Name_conferral || shows ;; Social_event || co-founded ;; Collaboration || spotlighted ;; Perception_active || co-curated ;; Hold || inspired ;; Influence || culminated ;; Process_end
build ;; Building || provide ;; Supply || acknowledged ;; Statement || festival ;; Social_event || sought ;; Preventing_or_letting || provide ;; Supply
include ;; Cause_to_be_included || known ;; Know || shifted ;; Becoming || represent ;; Presence || reflect ;; Perception_active || attract ;; Bringing || expanded ;; Expansion || attracting ;; Bringing
changes ;; Change || allowed ;; Preventing_or_letting || build ;; Cause_to_make_progress
decision ;; Deciding || alterations ;; Change || end ;; Process_end || deviated ;; Cause_change_of_position_on_a_scale || made ;; Giving
festival ;; Social_event || introduced ;; Choosing || broadening ;; Expansion
Attack ;; Attack || attack ;; Attack || Operation ;; Military_operation || Battle ;; Military_operation
killed ;; Killing || damaged ;; Damaging || loss ;; Earnings_and_losses || killed ;; Killing || shot ;; Use_firearm || bombardment ;; Attack
attack ;; Attack || conducted ;; Action || signed ;; Sign_agreement
hand ;; Giving || feared ;; Change_sentiment || giving ;; Giving || assistance ;; Assistance || Battle ;; Military_operation || Battle ;; Military_operation
promised ;; Commitment || judged ;; Deciding || risk ;; Risk || control ;; Control || take-over ;; Conquering
raided ;; Attack || attack ;; Attack || become ;; Becoming || retaliation ;; Revenge || severed ;; Process_end || Battle ;; Military_operation
attack ;; Attack || demonstrated ;; Protest || intended ;; Deciding || fight ;; Hostile_encounter || created ;; Causation
attack ;; Attack || debated ;; Quarreling
control ;; Control || wrote ;; Writing || risk ;; Risk || seizing ;; Getting || invasion ;; Attack
acting ;; Action || armistice ;; Process_end || turn ;; Bringing || convinced ;; Know
installed ;; Change_of_leadership
considered ;; Deciding
begun ;; Process_start || capture ;; Conquering || scuttling ;; Destroying || Operation ;; Military_operation || Operation ;; Military_operation || invasion ;; Attack || foiled ;; Removing
Battle ;; Hostile_encounter || fought ;; Hostile_encounter
taken ;; Conquering || supported ;; Supporting || destroyed ;; Destroying || commanded ;; Control || command ;; Control || wounded ;; Bodily_harm
ventured ;; Risk || reaching ;; Arriving || intention ;; Aiming || increase ;; Cause_change_of_position_on_a_scale || command ;; Control || produced ;; Manufacturing
ambushed ;; Attack
retreated ;; Escaping || headed ;; Self_motion || blocked ;; Hindering
headed ;; Self_motion
escape ;; Escaping || came ;; Self_motion || fire ;; Attack
captured ;; Conquering || killed ;; Killing || forced ;; Causation
destruction ;; Destroying || recapturing ;; Conquering || heard ;; Perception_active
occurred ;; Coming_to_be || freedom ;; Change_of_leadership || demonstration ;; Social_event
joined ;; Becoming_a_member || form ;; Cause_to_amalgamate || spanning ;; Expansion
demonstration ;; Social_event || originated ;; Coming_to_be || held ;; Hold
marked ;; Recording || anniversary ;; Social_event
led to ;; Causation || occupation ;; Control || divided ;; Creating
organised ;; Arranging || event ;; Social_event
demonstrating ;; Protest || draw ;; Bringing || independence ;; Change_of_leadership || designed ;; Creating
illustrated ;; Protest
described ;; Statement || campaign ;; Social_event || scene ;; Statement
event ;; Social_event || publicise ;; Dispersal || position ;; Placing || presented ;; Presence
responded ;; Response || widening ;; Expansion || event ;; Social_event || failed ;; Earnings_and_losses || take ;; Giving || actions ;; Response || bridge ;; Connect
became ;; Becoming || declare ;; Expressing_publicly || independence ;; Change_of_leadership
become ;; Becoming || Revolutions ;; Change_of_leadership
wars ;; Hostile_encounter || conflict ;; Hostile_encounter
fought ;; Hostile_encounter || planned ;; Arranging || instigated ;; Causation || interfering ;; Hindering || invasion ;; Attack
began ;; Process_start || convinced ;; Convincing || influenced ;; Influence || issue ;; Creating
sent ;; Sending || meet ;; Come_together
slain ;; Killing || battle ;; Hostile_encounter || defeated ;; Conquering || battle ;; Attack
battlefield ;; Hostile_encounter || victory ;; Getting
prevented ;; Preventing_or_letting || junction ;; Military_operation
come to ;; Motion || invasion ;; Attack || invade ;; Attack || return ;; Arriving || forcing ;; Causation
fight ;; Hostile_encounter || win ;; Getting
retreating ;; Escaping || fighting ;; Hostile_encounter || recruited ;; Becoming_a_member
took ;; Bringing
fell ;; Motion_directional || repelling ;; Defending
viewed ;; Perception_active
sent ;; Sending || invade ;; Attack || paid ;; Commerce_pay || coming ;; Motion || thought ;; Coming_to_believe || invited ;; Request
performed ;; Hold || declare ;; Expressing_publicly
use ;; Using || put ;; Placing
deaths ;; Death || vacated ;; Escaping
demanded ;; Request
requests ;; Request || denied ;; Statement
resist ;; Defending
forbidden ;; Preventing_or_letting || entering ;; Arriving
entered ;; Becoming_a_member
cost ;; Cost
decided ;; Deciding || uprising ;; Change_of_leadership || planned ;; Arranging || flee ;; Escaping
called ;; Name_conferral || defeat ;; Conquering
killed ;; Killing || planned ;; Arranging || assassinate ;; Killing || struck ;; Attack || seize ;; Hold
erupted ;; Process_start
took over ;; Change_of_leadership
seemed ;; Change_sentiment || resist ;; Control
limited ;; Limiting || surrounding ;; Surrounding
reached ;; Arriving
declared ;; Expressing_publicly || allow ;; Preventing_or_letting || called ;; Name_conferral || revived ;; Recovering
associated ;; Collaboration || united ;; Cause_to_amalgamate
ready ;; GetReady || cause ;; Causation || join ;; Becoming_a_member
revived ;; Recovering
saw ;; Self_motion
recovered ;; Recovering || end ;; Process_end || planned ;; Arranging || take ;; Conquering || inspired ;; Influence || designs ;; Creating
seemed ;; Change_sentiment
murder ;; Killing || avenge ;; Revenge || sanctioned ;; Revenge
assassinated ;; Killing
broke up ;; Destroying || death ;; Death
retreated ;; Escaping || serve ;; Giving
engagement ;; Hostile_encounter || Wars ;; Hostile_encounter
engaged ;; Hostile_encounter || captured ;; Conquering || operation ;; Military_operation || comprised ;; Containing || raiding ;; Attack || passing ;; Motion_directional || posed ;; Causation
advance ;; Self_motion || oppose ;; Agree_or_refuse_to_act
left ;; Departing || cruising ;; Patrolling || sailing ;; Self_motion || commanded ;; Control
movements ;; Control || concentrated ;; Aiming
attack ;; Attack || raiding ;; Attack || sailed ;; Self_motion
removing ;; Removing || spotted ;; Know
sent ;; Sending || drive ;; Motion || attack ;; Attack
achieve ;; Achieve || battle ;; Hostile_encounter || drive ;; Motion
engaging ;; Hostile_encounter || damage ;; Damaging || preventing ;; Preventing_or_letting || formed ;; Cause_to_amalgamate || inflicting ;; Causation
suffered ;; Damaging || disengaged ;; Escaping || retiring ;; Process_end || anchored ;; Placing
called ;; Name_conferral || Pogrom ;; Killing || pogrom ;; Killing || forces ;; Hostile_encounter || carried out ;; Military_operation
intervening ;; Control || looked ;; Scrutiny
shards ;; Destroying || broken ;; Destroying || smashed ;; Destroying
ransacked ;; Robbery || demolished ;; Destroying
destroyed ;; Destroying
destroyed ;; Destroying || arrested ;; Arrest || incarcerated ;; Arrest || damaged ;; Damaging
sent ;; Sending || reported ;; Reporting || working ;; Being_in_operation || wrote ;; Writing
observed ;; Perception_active || burnings ;; Bodily_harm || beatings ;; Bodily_harm || assaults ;; Attack
attacks ;; Attack || assassination ;; Killing
varied ;; Change || caused ;; Causation || attacks ;; Attack
reports ;; Reporting || estimated ;; Statement || murdered ;; Killing
deaths ;; Death || suicides ;; Killing || suicide ;; Killing || deaths ;; Death || included ;; Cause_to_be_included || death ;; Death || puts ;; Communication
view ;; Perception_active || murder ;; Killing
began ;; Process_start || conquered ;; Conquering || operations ;; Military_operation || partitioned ;; Forming_relationships || forces ;; Military_operation
fought ;; Hostile_encounter || established ;; Building || forces ;; Military_operation || war ;; Military_operation
resisted ;; Defending
collaborated ;; Collaboration || forces ;; Military_operation || forces ;; Military_operation
destroy ;; Destroying || coming ;; Motion || mounted ;; Connect
remained ;; Presence || conference ;; Communication || gaining ;; Getting
control ;; Control || support ;; Supporting || gained ;; Getting
cost ;; Cost
agreed ;; Agree_or_refuse_to_act || victims ;; Catastrophe || dispute ;; Quarreling
included ;; Cause_to_be_included || perished ;; Death
run ;; Motion
committed ;; Commitment
violence ;; Violence
executions ;; Rewards_and_punishments || carried out ;; Action
massacred ;; Killing
massacred ;; Killing || raid ;; Attack
fleeing ;; Escaping || associated ;; Collaboration || associated ;; Collaboration || forced ;; Causation
fought ;; Hostile_encounter || Raid ;; Military_operation
suffered ;; Bodily_harm
scattered ;; Dispersal
investigate ;; Research || ordered ;; Arranging || eliminate ;; Removing
discovered ;; Know || arrived ;; Arriving
launched ;; Motion
resisted ;; Defending || captured ;; Conquering || casualties ;; Killing || suffering ;; Bodily_harm
abandoned ;; GiveUp || fled ;; Escaping || seized ;; Conquering
join ;; Becoming_a_member || sailing ;; Self_motion
driven ;; Motion || overwhelmed ;; Destroying
battles ;; Hostile_encounter || refers ;; Adducing || resulted in ;; Causation || prompted ;; Cause_change_of_position_on_a_scale
starting ;; Process_start || occurred ;; Coming_to_be
implemented ;; Action || operation ;; Military_operation
captured ;; Conquering || suppress ;; Control
withdrawal ;; Escaping || captured ;; Conquering
published ;; Publishing || hit ;; Attack
raids ;; Attack || responded ;; Response
increased ;; Cause_change_of_position_on_a_scale || target ;; Aiming || smuggling ;; Committing_crime
bombed ;; Attack
noted ;; Know || exhausted ;; Expend_resource || end ;; Process_end || said ;; Statement
release ;; Releasing || said ;; Statement
launched ;; Process_start
withdrawal ;; Escaping || ended ;; Process_end || ceasefire ;; Process_end || reached ;; Arriving
broke down ;; Destroying
fought ;; Hostile_encounter
resulted in ;; Causation
Casualties ;; Killing
ravaged ;; Destroying || died ;; Death
became ;; Becoming || served ;; Assistance || excluded ;; Preventing_or_letting || surpassed ;; Cause_change_of_position_on_a_scale
developed ;; Coming_to_be || conflict ;; Hostile_encounter
employed ;; Employment || became ;; Becoming
impose ;; Action || forcing ;; Hostile_encounter || preceded ;; Process_start || election ;; Choosing
form ;; Coming_to_be || granted ;; Giving
ruled ;; Legal_rulings
caused ;; Causation || revolt ;; Change_of_leadership || triggered ;; Causation
started ;; Process_start || deposed ;; Change_of_leadership || estates ;; Besieging || gathering ;; Come_together
ousted ;; Change_of_leadership || elected ;; Choosing
formed ;; Coming_to_be || expel ;; Removing
perceived ;; Know || crushed ;; Destroying
condemned ;; Judgment_communication
committed ;; Commitment || gave ;; Giving || decided ;; Deciding || support ;; Supporting || fight ;; Hostile_encounter
transforming ;; Change || curb ;; Hindering || intervened ;; Hindering
crush ;; Bodily_harm
entered ;; Arriving || tolerate ;; Control || counter ;; Communication
devastated ;; Destroying || resulting in ;; Causation
imposed ;; Action || fighting ;; Hostile_encounter || extorted ;; Kidnapping || get ;; Getting || looted ;; Robbery
obtained ;; Getting || revolt ;; Change_of_leadership || controlled ;; Control || concluding ;; Process_end
became ;; Becoming
ended ;; Process_end || treaty ;; Ratification
altered ;; Change
created ;; Creating || emerging ;; Coming_to_be || strengthened ;; Cause_change_of_strength || rise ;; Motion_directional
called ;; Name_conferral || elected ;; Choosing || established ;; Building
supported ;; Supporting
rejected ;; Response || backed ;; Self_motion || accepted ;; Receiving || election ;; Choosing || elections ;; Choosing
refused ;; Agree_or_refuse_to_act || controlled ;; Control
established ;; Building || controlled ;; Control
signed ;; Sign_agreement
agreed ;; Agree_or_refuse_to_act || unite ;; Cause_to_amalgamate
began ;; Process_start || arrived in ;; Arriving
agreed ;; Agree_or_refuse_to_act || recognized ;; Know
expelled ;; Removing || defeated ;; Conquering || change ;; Change || defeated ;; Conquering
capture ;; Arrest || launched ;; Military_operation
resulted in ;; Causation || held ;; Hold
governed ;; Control || elections ;; Choosing
made ;; Creating
reach ;; Achieve || faced ;; Confronting_problem
impacted ;; Influence
included ;; Cause_to_be_included || associated ;; Collaboration
conduct ;; Action || accused ;; Judgment_communication
began ;; Process_start || use ;; Using || declare ;; Expressing_publicly || increased ;; Cause_change_of_position_on_a_scale || suppressed ;; Control || hold ;; Hold || expired ;; Process_end || elections ;; Choosing
confrontation ;; Hostile_encounter || conflict ;; Hostile_encounter || known ;; Know
included ;; Cause_to_be_included
war ;; Hostile_encounter || occurring ;; Coming_to_be
set ;; Placing || conflict ;; Hostile_encounter || combat ;; Hostile_encounter || restrained ;; Control
conducted ;; Action || Combat ;; Hostile_encounter
unraveling ;; Resolve_problem || proposed ;; Creating || campaign ;; Military_operation || infiltrations ;; Attack
conduct ;; Action || patrols ;; Patrolling || straddling ;; Surrounding || forces ;; Military_operation || forced ;; Causation
deployment ;; Arranging || transport ;; Sending || advantage ;; Earnings_and_losses || resupply ;; Supply || operating ;; Being_in_operation || forces ;; Military_operation
used ;; Using || transport ;; Sending
conducted ;; Action || combat ;; Hostile_encounter || support ;; Supporting || ensured ;; Preventing_or_letting || forces ;; Military_operation || played ;; Participation || forces ;; Military_operation || forces ;; Military_operation
provided ;; Supply || increased ;; Cause_change_of_position_on_a_scale || defensive ;; Defending || combined ;; Cause_to_amalgamate || stationed ;; Placing || forces ;; Military_operation || forces ;; Military_operation
trained ;; Education_teaching || attacks ;; Attack
became ;; Becoming || organised ;; Arranging || forces ;; Military_operation || forces ;; Military_operation
responded ;; Response || disrupt ;; Destroying || growing ;; Expansion || launching ;; Motion || infiltrations ;; Attack
announcing ;; Expressing_publicly || expanded ;; Expansion || launched ;; Motion
commit ;; Commitment || combat ;; Hostile_encounter || deployed ;; Arranging || forces ;; Military_operation || forces ;; Military_operation || saw ;; Self_motion || forces ;; Military_operation
began ;; Process_start || conflict ;; Hostile_encounter || rise ;; Cause_change_of_position_on_a_scale || subside ;; Cause_change_of_strength
began ;; Process_start || negotiations ;; Communication || recognising ;; Know || signed on ;; Sign_agreement
uprising ;; Change_of_leadership || functioned ;; Being_in_operation
began ;; Process_start
erupted ;; Process_start || incidents ;; Catastrophe || occurred ;; Presence
posed ;; Causation || defeat ;; Conquering || contained ;; Hindering || threat ;; Warning
declare ;; Expressing_publicly || involved ;; Cause_to_be_included || ended ;; Process_end || granted ;; Giving || murder ;; Killing
known ;; Know
reforms ;; Reforming_a_system || fed ;; Supply || brought about ;; Causation
fought ;; Hostile_encounter || rose ;; Cause_change_of_position_on_a_scale || remained ;; Temporary_stay
inflicted ;; Causation || betrayed ;; Reveal_secret || fighting ;; Violence
declared ;; Expressing_publicly || reached ;; Arriving
captured ;; Conquering
response ;; Response
help ;; Assistance || retaken ;; Conquering
took ;; Conquering || suppressed ;; Control
controlled ;; Control
helped ;; Assistance
join ;; Becoming_a_member || serving ;; Assistance
revolt ;; Change_of_leadership
proclaimed ;; Statement
proved ;; Creating
led to ;; Causation || forced ;; Hostile_encounter
administered ;; Control
promised ;; Commitment || issued ;; Creating
marked ;; Recording || administered ;; Control || established ;; Building || established ;; Building || beginning ;; Process_start
consolidated ;; Cause_change_of_strength || Battle ;; Hostile_encounter || defeated ;; Conquering
granted ;; Giving || defeat ;; Conquering
expanded ;; Expansion || led to ;; Causation || control ;; Control
sparked ;; Causation || created ;; Creating
began ;; Process_start || became ;; Becoming || expansion ;; Expansion || turn ;; Change
achieved ;; Achieve
created ;; Creating
became ;; Becoming || sold ;; Commerce_sell || annexed ;; Forming_relationships
caused ;; Causation || dispute ;; Quarreling || influence ;; Influence || brought ;; Bringing
added ;; Cause_to_be_included
launched ;; GetReady
bombing ;; Attack || support ;; Supporting || provide ;; Supply || neutralize ;; Control || command ;; Control
support ;; Supporting || Beginning ;; Process_start
attack ;; Attack
losses ;; Earnings_and_losses || defeated ;; Conquering
secured ;; Preventing_or_letting
secure ;; Preventing_or_letting
landing ;; Self_motion
retreat ;; Escaping || Battle ;; Hostile_encounter || fought ;; Hostile_encounter
losses ;; Earnings_and_losses || inflicted ;; Causation
collect ;; Come_together || supplies ;; Supply || retreat ;; Escaping || scattered ;; Dispersal || objective ;; Aiming || resume ;; Process_start
refrained ;; Control || launching ;; Motion || forces ;; Causation || fighting ;; Hostile_encounter
occurred ;; Presence || delay ;; Change_event_time || final ;; Process_end || attack ;; Attack || feint ;; Defending || induced ;; Causation
seized ;; Conquering || withdraw ;; Escaping
saving ;; Rescuing || destruction ;; Destroying || success ;; Achieve || encounter ;; Hostile_encounter || ruinous ;; Catastrophe
captured ;; Conquering || suffered ;; Bodily_harm
abandon ;; GiveUp || compelled ;; Request
caused ;; Causation || service ;; Supply || joint ;; Collaboration || warning ;; Warning || weak ;; Cause_change_of_strength || flooding ;; Motion
Forming ;; Coming_to_be
preventing ;; Preventing_or_letting || intensifying ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength || succumbing ;; Death || dissipated ;; Dispersal
impacted ;; Influence || flooding ;; Motion
lost ;; Earnings_and_losses || perished ;; Death
damage ;; Damaging || Following ;; Self_motion || determined ;; Deciding
caused ;; Causation || replaced ;; Change_of_leadership
conflict ;; Hostile_encounter || skirmish ;; Hostile_encounter || occurred ;; Coming_to_be || taking place ;; Process_start
war ;; Military_operation || leading ;; Bringing
return ;; Arriving || ordered ;; Arranging
submitted ;; Submitting_documents || approved ;; Agree_or_refuse_to_act || staff ;; Employment
deployment ;; Arranging || control ;; Control || upgrade ;; Cause_to_make_progress || forces ;; Military_operation
defend ;; Defending || developed ;; Creating
control ;; Control || screening ;; Scrutiny || building ;; Building || involved ;; Participation
ready ;; GetReady || built ;; Building || forces ;; Military_operation
taken ;; Action || resume ;; Process_start
returned ;; Arriving
attack ;; Attack || casus ;; Causation || occupy ;; Control
abandon ;; GiveUp || decided ;; Deciding || war ;; Military_operation || battle ;; Hostile_encounter || defensive ;; Defending || prepare ;; GetReady
decided ;; Deciding || defend ;; Defending
defence ;; Defending
went to ;; Motion
stayed ;; Temporary_stay || forces ;; Military_operation || returned ;; Arriving
followed ;; Action
found ;; Know || forces ;; Military_operation || preceded ;; Process_start || set up ;; Building
determined ;; Deciding || nominated ;; Change_of_leadership || press ;; Request || decisions ;; Deciding || taken ;; Bringing
exercise ;; Practice || conduct ;; Action || positions ;; Employment || see ;; Perception_active
exercise ;; Practice || landed ;; Motion
forces ;; Military_operation || occupied ;; Attack
moved up ;; Motion || responded ;; Response || alarmed ;; Warning || positions ;; Employment || forces ;; Military_operation || forces ;; Military_operation
caused ;; Causation || clashes ;; Hostile_encounter
combat ;; Military_operation || forces ;; Military_operation || engage ;; Participation
defeat ;; Conquering || prepared ;; GetReady || campaign ;; Military_operation
claimed ;; Statement || protect ;; Defending || campaigned ;; Military_operation
setting out ;; Departing || truce ;; Process_end
became ;; Becoming || returning ;; Arriving || took place ;; Process_start
sent ;; Sending || attack ;; Attack
battles ;; Hostile_encounter || making it ;; Causation || win ;; Earnings_and_losses || defeated ;; Conquering
access ;; Having_or_lacking_access || due ;; Causation || prepared ;; GetReady
established ;; Building
deny ;; Agree_or_refuse_to_act || ordered ;; Arranging || seize ;; Conquering
carried out ;; Action || became ;; Becoming
reach ;; Arriving || access ;; Having_or_lacking_access
refused ;; Agree_or_refuse_to_act
refused ;; Agree_or_refuse_to_act || surrender ;; Surrendering || travelling ;; Traveling || collect ;; Come_together || drove ;; Self_motion || promised ;; Commitment || appeared ;; Presence
declared ;; Expressing_publicly || took ;; Creating
start ;; Process_start || siege ;; Besieging || battle ;; Hostile_encounter
defense ;; Defending || operation ;; Military_operation || march ;; Self_motion || march ;; Self_motion || operation ;; Military_operation
violence ;; Violence || outbreak ;; Process_start || War ;; Hostile_encounter
carried out ;; Action || march ;; Self_motion || targeted ;; Aiming || strike ;; Attack || killing ;; Killing || resistance ;; Convincing
killed ;; Killing || injuring ;; Bodily_harm || strike ;; Attack
attacks ;; Attack || killed ;; Killing
preparing ;; GetReady || claimed ;; Statement || said ;; Statement
attacks ;; Attack || retaliated ;; Revenge || striking ;; Attack
injured ;; Bodily_harm || protect ;; Defending
defense ;; Defending || aimed ;; Aiming || intercepted ;; Preventing_or_letting || shooting down ;; Use_firearm
killing ;; Killing || attacked ;; Attack || manufacturing ;; Manufacturing || strikes ;; Attack || launching ;; Self_motion || training ;; Education_teaching
killed ;; Killing
injured ;; Bodily_harm || conflict ;; Hostile_encounter
casualties ;; Catastrophe || deaths ;; Death || injuries ;; Bodily_harm || reported ;; Reporting || clashes ;; Hostile_encounter
condemned ;; Judgment_communication || attacks ;; Attack || defend ;; Defending || stressed ;; Statement
condemned ;; Judgment_communication || responsive ;; Response || strikes ;; Attack
devastating ;; Destroying || participate ;; Participation || fighting ;; Hostile_encounter || war ;; Hostile_encounter
given ;; Giving || Operation ;; Military_operation
developed ;; Coming_to_be
sent ;; Sending
initiated ;; Process_start
prepared ;; GetReady
shipped ;; Sending || used ;; Using
held ;; Defending || shipped ;; Sending
trained ;; Education_teaching
developed ;; Coming_to_be || held ;; Defending
raid ;; Attack || employ ;; Employment || carried out ;; Action
raid ;; Attack || uprising ;; Change_of_leadership
called ;; Name_conferral
prepare ;; GetReady
raid ;; Attack || took place ;; Process_start
strengthening ;; Cause_change_of_strength || start ;; Process_start || included ;; Cause_to_be_included || replacing ;; Change_of_leadership || apartheid ;; Hindering
Hurricane ;; Catastrophe || hurricane ;; Catastrophe
forming ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || originated ;; Coming_to_be
upgraded ;; Cause_to_make_progress || storm ;; Catastrophe || reached ;; Arriving
storm ;; Catastrophe || weaken ;; Cause_change_of_strength || began ;; Process_start || entered ;; Arriving || downgraded ;; Cause_change_of_position_on_a_scale
began ;; Process_start || began ;; Process_start || reorganize ;; Recovering || lessened ;; Cause_change_of_position_on_a_scale || advisories ;; Convincing
regained ;; Recovering || storm ;; Catastrophe || Moving ;; Motion || deepened ;; Cause_change_of_position_on_a_scale || attain ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength || began ;; Process_start || entered ;; Arriving || transition ;; Change || completed ;; Process_end
warnings ;; Warning || storm ;; Catastrophe || issued ;; Creating
prepare ;; GetReady || flooding ;; Motion || urged ;; Convincing
rescues ;; Rescuing || made ;; Manufacturing || approach ;; Arriving || produced ;; Creating || leading to ;; Causation
recorded ;; Recording || damage ;; Damaging || caused ;; Causation || storm ;; Catastrophe || storm ;; Catastrophe
floods ;; Catastrophe || destroyed ;; Destroying || contributed ;; Assistance || buildings ;; Building
prepare ;; GetReady || urged ;; Convincing
produced ;; Manufacturing || combination ;; Cause_to_amalgamate
deaths ;; Death || reported ;; Reporting || damage ;; Damaging
overthrow ;; Change_of_leadership
interrupted ;; Hindering
organized ;; Arranging || joined ;; Becoming_a_member
arrived at ;; Arriving || armed ;; Bearing_arms
joined ;; Becoming_a_member
refused ;; Agree_or_refuse_to_act || demanded ;; Request || change ;; Change
arrested ;; Arrest || ordered ;; Arranging || urged ;; Convincing
end ;; Process_end || prepared ;; GetReady || prepared ;; GetReady
given ;; Giving
grew ;; Expansion || consumption ;; Expend_resource
gave ;; Giving || punished ;; Rewards_and_punishments || urged ;; Convincing || promised ;; Commitment
dispersed ;; Dispersal || arrested ;; Arrest
disbanded ;; Dispersal
lose ;; Earnings_and_losses || coming ;; Motion || improve ;; Cause_change_of_position_on_a_scale
armed ;; Bearing_arms || preparing ;; GetReady || shortened ;; Change_event_time || takeoff ;; Motion || attacked ;; Attack
due ;; Causation
died ;; Death || attack ;; Attack || injures ;; Bodily_harm || injured ;; Bodily_harm
damaged ;; Damaging
opened ;; Openness || killed ;; Killing || attackers ;; Attack || terrorist ;; Terrorism || disaster ;; Catastrophe || averted ;; Preventing_or_letting || stationed ;; Placing
arrested ;; Arrest
found ;; Know || given ;; Giving || guilty ;; Committing_crime || sentences ;; Legal_rulings || acquitted ;; Legal_rulings
retake ;; Conquering || attack ;; Attack || took place ;; Process_start || fell ;; Motion_directional
managed ;; Control || advance ;; Cause_to_make_progress || holding ;; Defending
conducted ;; Action || supply ;; Supply || centered ;; Placing
attack ;; Attack || launched ;; Military_operation || joined ;; Becoming_a_member
splitting ;; Destroying || broke ;; Bodily_harm || capturing ;; Conquering
losses ;; Earnings_and_losses || fell ;; Motion_directional || retreat ;; Escaping
counterattack ;; Attack || prepare ;; GetReady || launched ;; Military_operation
began ;; Process_start || captured ;; Conquering
became ;; Becoming || captured ;; Conquering
Fighting ;; Hostile_encounter || gains ;; Getting || died ;; Death
loss ;; Earnings_and_losses || inflicted ;; Causation || fighting ;; Hostile_encounter
given ;; Giving
killed ;; Killing || attack ;; Attack || break out ;; Process_start
attack ;; Attack || subsequent ;; Process_end || start ;; Process_start
negotiated ;; Communication || fighting ;; Hostile_encounter || talks ;; Telling || held ;; Hold
proved ;; Convincing || fighting ;; Quarreling || resumed ;; Recovering
bringing ;; Bringing || end ;; Process_end
called ;; Request || due ;; Causation || halt ;; Process_end
request ;; Request || sent ;; Sending || snubbed ;; Judgment_communication || ordered ;; Arranging || relieve ;; Cause_change_of_position_on_a_scale
chose ;; Choosing || blocked ;; Hindering || besieged ;; Besieging || supplies ;; Supply || convoy ;; Patrolling || airdrop ;; Bringing
resolve ;; Resolve_problem || reports ;; Reporting || prepared ;; GetReady || faced ;; Motion || talks ;; Telling || dispute ;; Quarreling || completion ;; Process_end || Following ;; Self_motion || held ;; Hold || forces ;; Military_operation
lifted ;; Process_end || negotiations ;; Communication || result ;; Causation || signed on ;; Sign_agreement
handed over ;; Giving || named ;; Name_conferral || control ;; Control || withdraw ;; Removing || force ;; Military_operation
brought about ;; Causation || end ;; Process_end
prevent ;; Preventing_or_letting || declared ;; Expressing_publicly || aim ;; Aiming || acquired ;; Getting || downgrading ;; Cause_change_of_position_on_a_scale
operating ;; Being_in_operation || support ;; Supporting
emerged ;; Coming_to_be || developed ;; Coming_to_be || declared ;; Expressing_publicly || support ;; Supporting
shifted ;; Change || increasing ;; Cause_change_of_position_on_a_scale
forming ;; Coming_to_be || transport ;; Sending || pursuing ;; Aiming
serve ;; Assistance
transferring ;; Military_operation || creating ;; Creating || using ;; Using || convinced ;; Coming_to_believe || take over ;; Military_operation
warned ;; Warning
discovered ;; Know || built ;; Building
began ;; Process_start || observed ;; Perception_active || exchange ;; Exchange || reaffirmed ;; Statement || led to ;; Causation
preparing ;; GetReady
strengthening ;; Cause_change_of_strength || told ;; Telling || establishing ;; Building
say ;; Statement || create ;; Creating
clashed ;; Hostile_encounter
suspected ;; Suspicion
accused ;; Judgment_communication || striking ;; Attack || transporting ;; Sending || took place ;; Process_start
attributed ;; Causation
reports ;; Reporting
refused ;; Agree_or_refuse_to_act || targeting ;; Aiming
suspected ;; Suspicion || attack ;; Attack
targeting ;; Aiming || attack ;; Attack || claimed ;; Statement
stated ;; Statement || targeting ;; Aiming
shot down ;; Use_firearm || damaged ;; Damaging || claim ;; Statement
reported ;; Reporting
confirmed ;; Check || strike ;; Attack
issued ;; Creating
aiding ;; Assistance || claims ;; Statement
contact ;; Connect || fight ;; Hostile_encounter || said ;; Telling
declared ;; Expressing_publicly || attack ;; Attack
evolved ;; Coming_to_be || beginning ;; Process_start || support ;; Supporting || supported ;; Supporting || known ;; Know
marked ;; Recording || invasion ;; Attack
began ;; Process_start || signing ;; Sign_agreement
invaded ;; Attack
ended ;; Process_end
invaded ;; Attack
advanced ;; Cause_to_make_progress
withdrew ;; Escaping || advanced ;; Cause_to_make_progress || established ;; Building
gained ;; Getting || defeat ;; Conquering
withdrew ;; Escaping || defence ;; Defending || support ;; Supporting || relief ;; Assistance || prepared ;; GetReady
declared ;; Expressing_publicly || invaded ;; Attack || end ;; Process_end || aid ;; Assistance || limited ;; Limiting
invaded ;; Attack || defence ;; Defending || rendered ;; Supply
defence ;; Defending || ordered ;; Arranging
gained ;; Getting || defeat ;; Conquering || control ;; Control
surrendered ;; Surrendering || marked ;; Recording || end ;; Process_end
placed ;; Placing || established ;; Building
started ;; Process_start || acquired ;; Getting
formed ;; Coming_to_be || invasion ;; Attack || resistance ;; Defending
joined ;; Becoming_a_member || managed ;; Control
stolen ;; Theft || hacked ;; Theft
access ;; Having_or_lacking_access || change ;; Change || incident ;; Catastrophe || encouraged ;; Convincing
confirmed ;; Coming_to_believe || hacked ;; Theft
said ;; Statement || find ;; Know
stored ;; Preserving || testing ;; Testing || cracked ;; Bodily_harm || posted ;; Placing
warned ;; Warning || criminals ;; Legality
statement ;; Statement || said ;; Statement || instructions ;; Education_teaching || instructions ;; Education_teaching || email ;; Sending || reset ;; Change
discovered ;; Know || claimed ;; Statement
changed ;; Change || response ;; Response || invalidated ;; Change
rose ;; Cause_change_of_position_on_a_scale
support ;; Supporting
arrest ;; Arrest
detained ;; Arrest
send ;; Sending || ordered ;; Arranging || arrest ;; Arrest || pronounced ;; Statement
seized ;; Hold || seized ;; Hold || stole ;; Theft || captured ;; Conquering || began ;; Process_start || cut off ;; Having_or_lacking_access
arrested ;; Arrest || disarmed ;; Emptying
sent ;; Sending
refused ;; Agree_or_refuse_to_act
issued ;; Creating || announced ;; Expressing_publicly
surrounded ;; Surrounding || regained ;; Recovering
fled ;; Escaping || sent ;; Sending || disarmed ;; Emptying
hacked ;; Theft
leaked ;; Reveal_secret || addresses ;; Name_conferral
used ;; Using || penetrate ;; Defending
use ;; Using || said ;; Statement || encrypted ;; Communication
addresses ;; Name_conferral || download ;; Having_or_lacking_access
described ;; Communication
criticized ;; Judgment_communication || tell ;; Telling || said ;; Statement || integrating ;; Cause_to_amalgamate
conflicts ;; Hostile_encounter
besieged ;; Besieging
followed ;; Motion || lifted ;; Cause_change_of_position_on_a_scale
renewed ;; Recovering
looted ;; Theft || captured ;; Getting || burned ;; Destroying
sided ;; Self_motion
fled ;; Escaping || left ;; Departing
involvement ;; Participation || tried ;; Self_motion
confirmed ;; Check || resulted in ;; Causation || based ;; Causation
cleansing ;; Emptying
convicted ;; Legal_rulings || committed ;; Commitment
campaigns ;; Military_operation
Uprising ;; Change_of_leadership || Truce ;; Process_end || Wars ;; Hostile_encounter || comprising ;; Cause_to_be_included
invasion ;; Attack || invasion ;; Attack || War ;; Hostile_encounter || occupation ;; Control || called ;; Name_conferral
deluge ;; Catastrophe
wars ;; Hostile_encounter || invasions ;; Attack || due ;; Causation || lost ;; Death
destruction ;; Destroying || destruction ;; Destroying || deluge ;; Catastrophe || War ;; Hostile_encounter
robbed ;; Robbery || stolen ;; Theft || invaders ;; Attack || claims ;; Statement || returned ;; Arriving
destroyed ;; Destroying || war ;; Hostile_encounter
losses ;; Earnings_and_losses
invaders ;; Attack || destroyed ;; Destroying
given ;; Giving || protest ;; Attack || organized ;; Arranging
organized ;; Arranging || organized ;; Arranging || transport ;; Sending
army ;; Hostile_encounter || Beginning ;; Process_start
stolen ;; Theft || conflict ;; Hostile_encounter || brought ;; Bringing
ended ;; Process_end || began ;; Process_start || gained ;; Getting || prevented ;; Preventing_or_letting || support ;; Supporting || march ;; Self_motion
melted ;; Change || splitting ;; Destroying
arriving in ;; Arriving
called ;; Name_conferral || attack ;; Attack || attack ;; Attack || took place ;; Process_start || murdered ;; Killing
decapitated ;; Killing
target ;; Aiming
arrested ;; Arrest
breaking ;; Destroying || stealing ;; Theft || indicted ;; Criminal_investigation || commit ;; Committing_crime
confessed ;; Reveal_secret || expressing ;; Expressing_publicly
condemned ;; Judgment_communication
praised ;; Judgment_communication || stated ;; Statement
stated ;; Statement
supported ;; Supporting || indicated ;; Statement || opposed ;; Agree_or_refuse_to_act
described ;; Statement
assassination ;; Killing || assassination ;; Killing || took place ;; Process_start
purchasing ;; Commerce_buy || kidnapping ;; Kidnapping || murder ;; Killing || assassination ;; Killing || use ;; Using
sought ;; Hold || spent ;; Expend_resource
due ;; Causation || obtained ;; Getting || assassination ;; Killing || attracted ;; Influence || ordered ;; Request || carried out ;; Military_operation || holding ;; Hold
placed ;; Placing
found ;; Know || used ;; Using
stole ;; Theft
taken ;; Conquering || provided ;; Supply || employees ;; Employment
claim ;; Statement || comment ;; Statement
drugged ;; Bodily_harm || electrocuted ;; Bodily_harm || reports ;; Reporting || suffocated ;; Killing
tracked ;; Motion || said ;; Statement
arrived ;; Arriving || departed ;; Departing || assassination ;; Killing || assassination ;; Killing || stayed ;; Temporary_stay || detected ;; Know || suspected ;; Suspicion
assassination ;; Killing || said ;; Statement || work ;; Action
hiding ;; Hiding_objects || suspects ;; Committing_crime || stated ;; Statement
ask ;; Request || confirmed ;; Check || involved ;; Cause_to_be_included || assassination ;; Killing || arrest ;; Arrest || said ;; Statement || warrant ;; Protest
holds ;; Defending || vowed ;; Commitment
terrorist ;; Terrorism || terrorist ;; Terrorism || requested ;; Request || added ;; Cause_change_of_position_on_a_scale || organization ;; Being_in_operation || involved ;; Cause_to_be_included || assassination ;; Killing
presented ;; Presence || request ;; Request || assassination ;; Killing || say ;; Statement || arrest ;; Arrest || said ;; Statement
associate ;; Collaboration
blamed ;; Judgment_communication || helping ;; Assistance
expelled ;; Removing || discovered ;; Know || forged ;; Causation
expelled ;; Removing || assassination ;; Killing
taken ;; Conquering
refused ;; Agree_or_refuse_to_act || forces ;; Causation || assassination ;; Killing
received ;; Receiving || linked ;; Connect || death ;; Death || unconfirmed ;; Check || uncovering ;; Reveal_secret || existed ;; Presence || assassination ;; Killing || said ;; Statement
attack ;; Attack || ambushed ;; Attack || killing ;; Killing || stealing ;; Theft || used ;; Using || infiltrate ;; Self_motion || occurred ;; Presence
exploded ;; Destroying
engaged ;; Hostile_encounter || killed ;; Killing || firefight ;; Hostile_encounter
injured ;; Bodily_harm
attack ;; Attack || condemnations ;; Judgment_communication || led to ;; Causation
attack ;; Attack || killed ;; Killing || praised ;; Judgment_communication || condolences ;; Judgment_communication || handling ;; Resolve_problem || expressed ;; Expressing_publicly
declared ;; Expressing_publicly || warned ;; Warning || vowed ;; Commitment || retake ;; Conquering || terrorists ;; Terrorism || dealing ;; Resolve_problem || mourning ;; Social_event || incident ;; Attack
closed ;; Process_end
Battle ;; Hostile_encounter || victory ;; Earnings_and_losses
broke out ;; Process_start || fought ;; Hostile_encounter || war ;; Hostile_encounter
embarked ;; Process_start || emerging ;; Coming_to_be || defeat ;; Earnings_and_losses || campaign ;; Military_operation || defeats ;; Earnings_and_losses || suffering ;; Bodily_harm
invade ;; Attack
sail ;; Self_motion || failing ;; Earnings_and_losses || intention ;; Aiming || sacking ;; Theft || land ;; Arriving
received ;; Receiving || capture ;; Conquering || death ;; Death || sail ;; Self_motion || set ;; Action || burial ;; Rite || establish ;; Creating || settlement ;; Statement || died ;; Death || abandoned ;; GiveUp
resulting in ;; Causation || loss ;; Earnings_and_losses || shot down ;; Use_firearm || service ;; Supply || deaths ;; Death
stolen ;; Theft || disappearance ;; Departing
attacked ;; Attack || carrying out ;; Action
crash-landed ;; Self_motion
registered ;; Recording || operated ;; Being_in_operation
invaded ;; Attack || forces ;; Causation
transferred ;; Giving
given ;; Giving || referred ;; Adducing
protest ;; Attack || occurring ;; Coming_to_be || revolution ;; Change_of_leadership || party ;; Social_event
die ;; Death
demanding ;; Request
killed ;; Killing || injured ;; Bodily_harm
attacked ;; Attack || ruling ;; Legal_rulings || party ;; Social_event
demolished ;; Destroying || looted ;; Theft || broke ;; Destroying
generated ;; Creating || intervention ;; Influence
aimed ;; Aiming || reforms ;; Reforming_a_system || looting ;; Theft || saving ;; Rescuing || bringing ;; Bringing
leave ;; Departing || remain ;; Presence || suck ;; Ingestion
demanded ;; Request || change ;; Change || reforms ;; Reforming_a_system || using ;; Using || hinder ;; Hindering
demanded ;; Request || subsided ;; Change || guarantees ;; Commitment || restoration ;; Recovering
ended ;; Process_end || party ;; Participation || march ;; Self_motion
held ;; Hold || March ;; Social_event || demonstration ;; Social_event || protest ;; Self_motion
formed ;; Coming_to_be || Organised ;; Arranging || planned ;; Arranging || cuts ;; Cause_change_of_position_on_a_scale || march ;; Social_event
demonstration ;; Social_event || estimated ;; Statement || attended ;; Participation
described ;; Statement || rally ;; Social_event || protest ;; Self_motion || protest ;; Self_motion
addressed ;; Expressing_publicly || marched ;; Self_motion || took place ;; Process_start
assembled ;; Come_together || vandalised ;; Theft || protesting ;; Self_motion || moved ;; Self_motion || march ;; Social_event || clashed ;; Hostile_encounter
reported ;; Reporting || clashes ;; Hostile_encounter
arrested ;; Arrest || injured ;; Bodily_harm
stole ;; Theft || assault ;; Attack || attack ;; Attack || attack ;; Attack || filled ;; Filling || stabbing ;; Attack || carried out ;; Attack || rammed ;; Destroying
began ;; Process_start || stabbing ;; Attack || crashing ;; Attack || jumped ;; Self_motion
attack ;; Attack || injured ;; Bodily_harm
attack ;; Attack
present ;; Presence || strike ;; Attack || plan ;; Arranging
fatal ;; Killing || avoided ;; Preventing_or_letting || stabbing ;; Attack || incident ;; Attack || response ;; Response
violence ;; Violence || faced ;; Perception_active
increased ;; Cause_change_of_position_on_a_scale
died ;; Death || identified ;; Check || attacks ;; Attack
dead ;; Death || killed ;; Killing || attacks ;; Attack
attacks ;; Attack
begin ;; Process_start || attacks ;; Attack || prompted ;; Causation
conducted ;; Action || research ;; Research || viewed ;; Perception_active || taking ;; Creating || benefits ;; Earnings_and_losses || crime ;; Committing_crime
increased ;; Cause_change_of_position_on_a_scale
fled ;; Escaping || end ;; Process_end
allowed ;; Preventing_or_letting || created ;; Creating || granted ;; Giving
equipment ;; Supply || varied ;; Change || banning ;; Preventing_or_letting || support ;; Supporting
found ;; Know || denied ;; Agree_or_refuse_to_act || access ;; Having_or_lacking_access
saw ;; Self_motion || emigrate ;; Removing
found ;; Know || entitled ;; Name_conferral || denied ;; Agree_or_refuse_to_act
election ;; Choosing || increased ;; Cause_change_of_position_on_a_scale || perceived ;; Know
published ;; Publishing || social ;; Social_event || embarked ;; Self_motion
become ;; Becoming
found ;; Know || complete ;; Process_end || expressing ;; Expressing_publicly || survey ;; Scrutiny
complete ;; Process_end
service ;; Supply
found ;; Know || involved ;; Participation || study ;; Education_teaching
combined ;; Cause_to_amalgamate || illegal ;; Legality || violence ;; Violence || argued ;; Quarreling || led to ;; Causation
found ;; Know || stealing ;; Theft || drugs ;; Cure || meeting ;; Social_event || making ;; Manufacturing || claimed ;; Statement
meeting ;; Social_event || mistreated ;; Bodily_harm
occurred ;; Presence
stated ;; Statement || drug ;; Committing_crime || decapitated ;; Killing || mutilated ;; Bodily_harm
death ;; Death || reported ;; Reporting
found ;; Know
heads ;; Self_motion || killed ;; Killing || cut off ;; Bodily_harm
stuffed ;; Placing || killed ;; Killing
arrested ;; Arrest || indicated ;; Statement || ruled out ;; Legal_rulings
found ;; Placing || decapitated ;; Killing || dismembered ;; Killing
illegal ;; Legality || drugs ;; Bodily_harm
supplying ;; Supply || underworld ;; Committing_crime || thieves ;; Theft
traffickers ;; Committing_crime || treasured ;; Preserving
trafficking ;; Committing_crime || control ;; Control || massacre ;; Killing || fighting ;; Hostile_encounter
control ;; Control || reach ;; Arriving || fight ;; Competition
warnings ;; Warning || rivals ;; Competition || discovery ;; Know || murder ;; Killing || left ;; Departing
blamed ;; Judgment_communication || violence ;; Violence || deserted ;; Escaping
starting ;; Process_start || named ;; Name_conferral || inspired ;; Influence || response ;; Response || protests ;; Self_motion || assemblies ;; Come_together
suggested ;; Statement || shouting ;; Vocalizations
blamed ;; Judgment_communication
beaten ;; Bodily_harm || change ;; Change || arrested ;; Arrest || strolling ;; Self_motion || minimize ;; Cause_change_of_position_on_a_scale || sustaining ;; Preserving || urged ;; Request
determined ;; Check
became ;; Becoming || Protest ;; Self_motion || noted ;; Statement
mounted ;; Process_start || operation ;; Military_operation
beaten ;; Bodily_harm || reported ;; Reporting
reported ;; Reporting || lost ;; Earnings_and_losses
arrested ;; Arrest
arrest ;; Arrest || prevented ;; Preventing_or_letting
arrested ;; Arrest || inciting ;; Convincing || charged ;; Judgment_communication || subversion ;; Change_of_leadership
fought ;; Hostile_encounter || battle ;; Hostile_encounter || battle ;; Hostile_encounter || revolution ;; Change_of_leadership
Led ;; Conquering || engaged ;; Participation || defeated ;; Conquering || lasted ;; Process_end
written ;; Writing || battle ;; Hostile_encounter
cited ;; Adducing || discussed ;; Communication
escaped ;; Escaping || battle ;; Hostile_encounter
captured ;; Conquering
signed ;; Sign_agreement || leave ;; Departing || become ;; Becoming || held ;; Arrest
recognize ;; Know || lobby ;; Convincing
became ;; Becoming || cries ;; Vocalizations
became ;; Becoming
release ;; Publishing
released ;; Publishing
contains ;; Containing || composes ;; Come_together
produced ;; Manufacturing || release ;; Publishing
get ;; Getting
supporting ;; Supporting || slow ;; Change_event_time || slow ;; Change_event_time
love ;; Perception_active || song ;; Vocalizations
finds ;; Know || releasing ;; Publishing || going ;; Motion || available ;; Having_or_lacking_access
fought ;; Hostile_encounter || beginning ;; Process_start || broke out ;; Process_start
refused ;; Agree_or_refuse_to_act || supported ;; Supporting || abolished ;; GiveUp
claimed ;; Statement
outraged ;; Judgment_communication || trading ;; Exchange
response ;; Response || attacks ;; Attack || conduct ;; Action || leading ;; Causation || authorized ;; Preventing_or_letting || retaliate ;; Revenge || seizing ;; Self_motion
called ;; Statement
attacked ;; Attack || involved ;; Cause_to_be_included
involved ;; Cause_to_be_included
negotiations ;; Communication || attacking ;; Attack || fighting ;; Hostile_encounter || concentrated ;; Aiming || growing ;; Expansion || reopen ;; Openness || overthrow ;; Change_of_leadership || led ;; Causation
control ;; Control || feuded ;; Quarreling
rejecting ;; Agree_or_refuse_to_act || offering ;; Supply
sent ;; Sending || negotiate ;; Communication || cried ;; Vocalizations
ended ;; Process_end || signing ;; Sign_agreement
cost ;; Cost || beginning ;; Process_start || protests ;; Protest || demonstrations ;; Social_event || deterioration ;; Agree_or_refuse_to_act || involving ;; Cause_to_be_included || opposing ;; Response
demand ;; Request || demonstrations ;; Social_event || cry ;; Vocalizations || rallying ;; Come_together
began ;; Process_start || focus ;; Aiming || expanded ;; Cause_change_of_position_on_a_scale
began ;; Process_start || cost ;; Cost || began ;; Process_start || led ;; Causation || housing ;; Building || gained ;; Getting || establish ;; Creating
spread ;; Dispersal || began ;; Process_start || protest ;; Protest || establishing ;; Building
participated ;; Participation || demonstrations ;; Social_event
changing ;; Change || movement ;; Social_event
funding ;; Supply || Criticism ;; Judgment_communication || accusations ;; Judgment_communication || includes ;; Cause_to_be_included
planning ;; Arranging || claimed ;; Statement || orchestrated ;; Control
accused ;; Judgment_communication || publicizing ;; Dispersal || Criticism ;; Judgment_communication || impact ;; Influence || damaging ;; Damaging || focus ;; Aiming
proposed ;; Statement || demands ;; Request || announced ;; Expressing_publicly || solve ;; Resolve_problem || response ;; Response || protests ;; Protest || taken ;; Bringing
negotiate ;; Communication || appointed ;; Deciding
movement ;; Social_event || leave ;; GiveUp || perceived ;; Know || cause ;; Causation || governing ;; Action || triggering ;; Causation || prompting ;; Causation || elections ;; Choosing
restart ;; Process_start || housing ;; Building
given ;; Giving || arrested ;; Arrest || arrived ;; Arriving || dismantle ;; Destroying || confiscated ;; Surrendering || resisted ;; Defending
death ;; Death || service ;; Rite || held ;; Hold
preceded ;; Process_start || gathering ;; Come_together || close ;; Self_motion || service ;; Rite || followed ;; Self_motion
arrived ;; Arriving || placed ;; Placing || service ;; Rite
began ;; Process_start
filled ;; Filling
beginning ;; Process_start || end ;; Process_end
sitting ;; Placing || wore ;; Wearing
sang ;; Vocalizations
gave ;; Giving
received ;; Receiving || told ;; Telling || standing ;; Placing
deal ;; Resolve_problem
gathered ;; Come_together || end ;; Process_end || offer ;; Supply
told ;; Telling || crying ;; Vocalizations || give ;; Giving || say ;; Telling || say ;; Telling || love ;; Perception_active || born ;; Coming_to_be
leave ;; Departing || said ;; Telling
started ;; Process_start
sing ;; Vocalizations
became ;; Becoming || known ;; Know
made ;; Manufacturing
started ;; Process_start || sang ;; Vocalizations
sang ;; Vocalizations
began ;; Process_start || ended ;; Process_end
performed ;; Hold
recalled ;; Statement
put ;; Placing
walked ;; Self_motion
performed ;; Hold
played ;; Action
suffered ;; Bodily_harm
resumed ;; Process_start
wrote ;; Writing
performed ;; Hold
sung ;; Vocalizations
given ;; Giving || Massacre ;; Killing || name ;; Name_conferral
killed ;; Killing || betraying ;; Reveal_secret
ruled ;; Control || rang ;; Vocalizations
avoid ;; Preventing_or_letting || crossing ;; Body_movement
afraid ;; Causation
held ;; Defending || existence ;; Presence || celebration ;; Social_event || Celebrations ;; Social_event
existence ;; Presence || mark ;; Recording
set up ;; Reforming_a_system
co-operation ;; Collaboration || charge ;; Judgment_communication
organised ;; Arranging || celebration ;; Social_event || culminating ;; Coming_to_be || mark ;; Recording
compound ;; Cause_to_amalgamate || held ;; Hold
exhibition ;; Social_event || organised ;; Arranging || events ;; Social_event
Festival ;; Social_event
include ;; Cause_to_be_included || competition ;; Competition || Festival ;; Social_event || singing ;; Vocalizations || relay ;; Self_motion || celebration ;; Social_event
success ;; Resolve_problem || fighting ;; Hostile_encounter || education ;; Education_teaching || marked ;; Recording || showcased ;; Social_event
collect ;; Come_together || donations ;; Giving || celebration ;; Social_event || usage ;; Using || coming ;; Arriving || set up ;; Reforming_a_system
held ;; Hold || parties ;; Social_event || use ;; Using || Tests ;; Testing
illegal ;; Legality || made ;; Action
coined ;; Writing || used ;; Using || Test ;; Testing || test ;; Testing
began ;; Process_start || throwing ;; Motion
organizing ;; Arranging
involved ;; Cause_to_be_included
read ;; Communication || advertised ;; Commerce_sell
test ;; Testing || popularized ;; Expansion
performances ;; Hold
influence ;; Influence || transition ;; Change || movement ;; Social_event || Tests ;; Testing
song ;; Vocalizations || Tests ;; Testing
fought ;; Hostile_encounter || defeat ;; Conquering
deployed ;; Arranging || engaged ;; Forming_relationships
assembly ;; Building || culminated ;; Process_end
abandoned ;; GiveUp || signed ;; Sign_agreement || referred ;; Adducing
assembly ;; Building || decided ;; Deciding || declared ;; Expressing_publicly || left ;; Departing
came to ;; Self_motion || end ;; Process_end || created ;; Creating
signed ;; Sign_agreement
occupy ;; Control || controlling ;; Control || occupy ;; Control
dismantle ;; Destroying
dismantling ;; Destroying
followed ;; Self_motion || consisting ;; Cause_to_be_included || deploying ;; Arranging || entered ;; Arriving
took place ;; Process_start
joint ;; Collaboration || occupied ;; Control
occupied ;; Control
began ;; Process_start || seizures ;; Control
started ;; Process_start
meeting ;; Social_event || set ;; Placing
established ;; Building || signed ;; Sign_agreement
promised ;; Commitment
control ;; Control
control ;; Control || control ;; Control
promised ;; Commitment || exchange ;; Exchange || signed ;; Sign_agreement
claim ;; Statement || crumbling ;; Destroying
sent ;; Sending || commanding ;; Control || embarked ;; Self_motion || occupied ;; Control || forces ;; Military_operation
conference ;; Social_event || walking ;; Self_motion || resulting in ;; Causation || led ;; Bringing
sending ;; Sending || responded ;; Response
landed ;; Self_motion
conference ;; Social_event || talks ;; Telling || sway ;; Control
began ;; Process_start || began ;; Process_start || landing ;; Motion
fired ;; Use_firearm || refusing ;; Agree_or_refuse_to_act || marked ;; Recording || killing ;; Killing || wounding ;; Bodily_harm || murder ;; Committing_crime || shout ;; Vocalizations
moved ;; Motion || commands ;; Control || situated ;; Placing
given ;; Giving || uprising ;; Change_of_leadership
killed ;; Killing || raid ;; Military_operation || clear ;; Emptying || launched ;; Military_operation || injured ;; Bodily_harm
demand ;; Request || reform ;; Reforming_a_system || led ;; Causation || calling ;; Request
described ;; Statement
withdraw ;; Escaping || fired ;; Use_firearm || covered ;; Filling || refused ;; Agree_or_refuse_to_act
attacked ;; Attack
broke out ;; Process_start || raid ;; Attack || clashes ;; Attack
deployed ;; Arranging || set up ;; Building
continued ;; Cause_change_of_position_on_a_scale || chanted ;; Vocalizations
accused ;; Judgment_communication || injuries ;; Bodily_harm || pull ;; Motion || forces ;; Causation || attacking ;; Attack || insisted ;; Statement
submitted ;; Submitting_documents || play ;; Competition || described ;; Statement || raid ;; Attack || dismissed ;; Statement
expressed ;; Expressing_publicly || support ;; Supporting
violence ;; Violence || expressed ;; Expressing_publicly || used ;; Using
announced ;; Expressing_publicly || revoke ;; Removing
criticised ;; Judgment_communication
strike ;; Attack || strike ;; Attack || led ;; Causation || strike ;; Attack
pay ;; Commerce_pay || shortening ;; Cause_change_of_position_on_a_scale || spread ;; Filling || growing ;; Cause_change_of_position_on_a_scale || strike ;; Attack
enforce ;; Action || started ;; Process_start || Starting ;; Process_start || allowed ;; Preventing_or_letting
reduced ;; Cause_change_of_position_on_a_scale || pay ;; Commerce_pay || cut ;; Cause_change_of_position_on_a_scale || found out ;; Know
strike ;; Attack || united ;; Collaboration
organized ;; Arranging || divided ;; Change || strike ;; Attack
killed ;; Killing || arrested ;; Arrest || framed ;; Bodily_harm || murder ;; Killing
came to ;; Motion || strike ;; Attack || run ;; Motion
sending ;; Sending
violence ;; Violence || stopped ;; Process_end || leading to ;; Causation || drew ;; Causation
giving ;; Giving || settle ;; Resolve_problem || decided ;; Deciding || strike ;; Attack || raises ;; Cause_change_of_position_on_a_scale
collapsed ;; Destroying
strike ;; Attack || strike ;; Attack
strike ;; Attack
appearing ;; Coming_to_be || published ;; Publishing || strike ;; Attack
stuck ;; Preserving
cry ;; Vocalizations || strike ;; Attack || battle ;; Hostile_encounter
concerts ;; Social_event
departure ;; Departing || search ;; Scrutiny
introduced ;; Statement
recorded ;; Recording
ended ;; Process_end || performed ;; Action
moving ;; Motion || says ;; Statement
shouted ;; Vocalizations
performing ;; Hold
played ;; Action
flown ;; Self_motion
hired ;; Employment
decided ;; Deciding || avoid ;; Preventing_or_letting
asked ;; Request || open ;; Openness || played ;; Action || joined ;; Becoming_a_member || shows ;; Social_event
ensure ;; Preventing_or_letting
released ;; Publishing || recorded ;; Recording
returned ;; Arriving || playing ;; Action
opened ;; Openness
show up ;; Social_event
events ;; Social_event
organized ;; Arranging
demanded ;; Request || production ;; Manufacturing || using ;; Using || end ;; Process_end || end ;; Process_end || end ;; Process_end || profit ;; Earnings_and_losses || caused ;; Causation
demonstrated ;; Protest
assembled ;; Come_together || express ;; Expressing_publicly || chanting ;; Vocalizations
result ;; Causation || said ;; Communication
fought ;; Hostile_encounter
laying ;; Placing || planned ;; Arranging || siege ;; Besieging || counterattack ;; Attack
mistakes ;; Incident || fell ;; Earnings_and_losses
committed ;; Killing || suicide ;; Killing
suffered ;; Bodily_harm || dead ;; Death || battle ;; Military_operation || casualties ;; Death
battle ;; Military_operation
bombings ;; Attack
exploded ;; Attack || exploded ;; Attack
injured ;; Bodily_harm || killed ;; Killing
bombings ;; Attack || attack ;; Attack || attack ;; Attack
dead ;; Death
force ;; Causation || bombings ;; Attack || claimed ;; Statement
attacks ;; Attack || launched ;; Motion
helped ;; Assistance || taken ;; Conquering || inquiries ;; Criminal_investigation || forces ;; Causation || bombings ;; Attack
forces ;; Causation
terrorism ;; Terrorism || forces ;; Causation || called ;; Request || attacks ;; Attack || involving ;; Cause_to_be_included
bombings ;; Attack || lifted ;; Motion
bombings ;; Attack
strike ;; Attack || called ;; Request || opposed ;; Agree_or_refuse_to_act
proposed ;; Convincing || opposed ;; Agree_or_refuse_to_act
helped ;; Assistance || bring about ;; Causation
assembly ;; Building || strike ;; Attack || brought ;; Bringing
charged ;; Judgment_communication || bombings ;; Attack
inquiry ;; Criminal_investigation || led to ;; Causation
criticised ;; Judgment_communication || report ;; Reporting || investigation ;; Criminal_investigation || stopped ;; Process_end || work ;; Being_in_operation || said ;; Telling
criticised ;; Judgment_communication || bombings ;; Attack
report ;; Reporting || involved ;; Cause_to_be_included || said ;; Telling
hindered ;; Hindering || inquiry ;; Criminal_investigation || refusal ;; Agree_or_refuse_to_act || release ;; Publishing
release ;; Publishing || victims ;; Catastrophe || campaign ;; Social_event
attack ;; Attack || strike ;; Attack || service ;; Supply || united ;; Cause_to_amalgamate
attack ;; Attack || entry ;; Becoming_a_member || War ;; Military_operation || led to ;; Causation
attack ;; Attack || planning ;; Arranging || Operation ;; Military_operation || Operation ;; Military_operation || Operation ;; Military_operation
attack ;; Attack || preventive ;; Preventing_or_letting || planned ;; Arranging || interfering ;; Hindering
attacks ;; Attack
strike ;; Attack || viewpoint ;; Perception_active || seen ;; Traveling
commenced ;; Process_start || attack ;; Attack
attacked ;; Attack || launched ;; Process_start
damaged ;; Damaging || sunk ;; Self_motion
fight ;; Hostile_encounter || raised ;; Cause_change_of_position_on_a_scale || returned ;; Arriving
damaged ;; Damaging || ship ;; Sending || sank ;; Motion_directional
destroyed ;; Destroying || killed ;; Killing || wounded ;; Bodily_harm
attacked ;; Attack || building ;; Building
losses ;; Earnings_and_losses || lost ;; Earnings_and_losses || killed ;; Killing
captured ;; Conquering
announced ;; Expressing_publicly || declaration ;; Expressing_publicly || delivered ;; Sending
declared ;; Expressing_publicly
declared ;; Expressing_publicly || responded ;; Response || declaration ;; Expressing_publicly
warning ;; Warning || negotiations ;; Communication || led ;; Causation || proclaim ;; Statement
warning ;; Warning || attack ;; Attack || attack ;; Attack || judged ;; Legal_rulings || declaration ;; Expressing_publicly
engagement ;; Hostile_encounter || wars ;; Military_operation || fought ;; Hostile_encounter
begun ;; Process_start || deployed ;; Arranging || watch ;; Perception_active || disrupting ;; Hindering || aim ;; Aiming || operations ;; Military_operation || seen ;; Perception_active || raiding ;; Attack
sighted ;; Know || approaching ;; Arriving || waiting ;; Motion
engage ;; Hostile_encounter || isolate ;; Removing || subject ;; Using || barrage ;; Use_firearm || moved ;; Motion || managed ;; Control
responded ;; Response || achieved ;; Achieve || damage ;; Damaging || suffering ;; Bodily_harm || damage ;; Damaging || inflicting ;; Causation || battered ;; Damaging || casualties ;; Death
losses ;; Earnings_and_losses || wounded ;; Bodily_harm || accident ;; Incident
hold ;; Defending || surrender ;; Surrendering || arrival ;; Arriving || forced ;; Request
success ;; Earnings_and_losses || repaired ;; Recovering || commissioned ;; Request || knighted ;; Change_of_leadership
occurred ;; Coming_to_be
died ;; Death || caused ;; Causation || injured ;; Bodily_harm
crash ;; Catastrophe || accident ;; Incident
occurred ;; Coming_to_be || open ;; Openness || died ;; Death
unveiled ;; Hold
crash ;; Catastrophe || killed ;; Killing || attendance ;; Participation
caused ;; Causation || damage ;; Damaging
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || cyclone ;; Catastrophe || wave ;; Motion
strengthening ;; Cause_change_of_strength || allowed ;; Preventing_or_letting || become ;; Becoming || storm ;; Catastrophe || hurricane ;; Catastrophe || occurred ;; Coming_to_be
peaked ;; Self_motion
landfalls ;; Arriving || re-entering ;; Arriving
moved ;; Motion || storm ;; Catastrophe || weakened ;; Cause_change_of_strength
strengthening ;; Cause_change_of_strength || approaching ;; Arriving || hurricane ;; Catastrophe || weakened ;; Cause_change_of_strength
downgraded ;; Cause_change_of_position_on_a_scale || dissipating ;; Expend_resource
caused ;; Causation || damage ;; Damaging
damage ;; Damaging || reaching ;; Arriving || blackouts ;; Lighting || reported ;; Reporting
damaged ;; Damaging || precipitation ;; Motion || mudslides ;; Catastrophe || resulted in ;; Causation
suffered ;; Bodily_harm || damage ;; Damaging || flooded ;; Catastrophe
accidents ;; Incident
fell ;; Motion_directional || remove ;; Removing || reported ;; Reporting
Damage ;; Damaging || reached ;; Arriving
caused ;; Causation || defense ;; Defending || flooding ;; Catastrophe || evacuation ;; Escaping || reported ;; Reporting
caused ;; Causation || damage ;; Damaging
Impact ;; Influence || squalls ;; Motion
brought ;; Bringing || impacting ;; Influence
perpetrated ;; Committing_crime || attacks ;; Attack || massacre ;; Killing || disaster ;; Attack
attacks ;; Attack || killed ;; Killing || injured ;; Bodily_harm
building ;; Building || going off ;; Process_end || murdered ;; Committing_crime || firebombed ;; Attack
Arriving ;; Arriving || died ;; Death || concealed ;; Hiding_objects || explosion ;; Attack || detonated ;; Attack
election ;; Change_of_leadership || increased ;; Cause_change_of_position_on_a_scale || defeat ;; Earnings_and_losses
planned ;; Arranging || thought ;; Coming_to_believe || revenge ;; Revenge || defeat ;; Earnings_and_losses
foreclosed ;; Preventing_or_letting || going to ;; Motion || notified ;; Telling
noticed ;; Know || suicide ;; Killing || stopped ;; Process_end || working ;; Employment || planning ;; Arranging || thought ;; Coming_to_believe
purchased ;; Commerce_buy || planted ;; Placing
murdered ;; Killing
causing ;; Causation || destroyed ;; Destroying || homestead ;; Placing || blasts ;; Use_firearm || detonated ;; Attack || fires ;; Catastrophe
devastated ;; Destroying || killing ;; Killing || explosion ;; Attack || Consolidated ;; Cause_change_of_strength || building ;; Building
used ;; Using || planted ;; Placing || detonate ;; Attack
began ;; Process_start || drove ;; Motion || used ;; Using || killing ;; Killing || working ;; Employment || stopped ;; Process_end || detonate ;; Attack || injuring ;; Bodily_harm
discovered ;; Know || connected ;; Connect || rescue ;; Rescuing || hidden ;; Hiding_objects || unexploded ;; Attack || timing ;; Incident || set ;; Placing || detonate ;; Attack
destroy ;; Destroying || blow up ;; Destroying
concert ;; Social_event || rock ;; Social_event || Tour ;; Traveling || tour ;; Traveling
support ;; Supporting || open ;; Openness || launched ;; Process_start
visited ;; Traveling
festivals ;; Social_event || support ;; Supporting || Patrol ;; Patrolling || tours ;; Traveling || tour ;; Traveling
commenced ;; Process_start || tour ;; Traveling || called ;; Name_conferral
visit ;; Traveling || saw ;; Motion || tour ;; Traveling
supported ;; Supporting || headlined ;; Statement
shoulder ;; Placing || cancellations ;; Change_event_time || tour ;; Traveling || marred ;; Hindering || injury ;; Bodily_harm
events ;; Confronting_problem || affected ;; Influence
cancelled ;; Change_event_time || drumming ;; Motion || broke ;; Bodily_harm || shows ;; Social_event || pitched ;; Motion
released ;; Publishing || arrested ;; Arrest || concert ;; Social_event || drug ;; Bodily_harm || charges ;; Legality
healed ;; Cure || sat ;; Placing || shows ;; Social_event || injury ;; Bodily_harm
visited ;; Traveling || concert ;; Social_event || played ;; Competition || tour ;; Traveling
given ;; Giving || introduced ;; Statement || concert ;; Social_event || Tour ;; Traveling || covering ;; Hiding_objects
finished ;; Process_end || tour ;; Traveling
spanned ;; Expansion || tour ;; Traveling || cancellations ;; Change_event_time
defending ;; Defending || qualified ;; Becoming_a_member || host ;; Social_event
carrying ;; Bringing || advancing ;; Cause_to_make_progress
ended up ;; Process_end || defeating ;; Conquering || winning ;; Getting
claimed ;; Statement || win ;; Getting || defeated ;; Conquering
placement ;; Placing
award ;; Giving || took ;; Conquering
present ;; Presence
secured ;; Getting || winning ;; Getting || qualified ;; Becoming_a_member
occurred ;; Coming_to_be || incidents ;; Incident
injured ;; Bodily_harm || set on fire ;; Damaging || damaged ;; Damaging || attacks ;; Attack
injured ;; Bodily_harm || thrown ;; Motion
attacks ;; Terrorism || homicide ;; Killing || committed ;; Committing_crime
murdered ;; Killing
slit ;; Bodily_harm || decapitated ;; Bodily_harm
worth ;; Cost || covering ;; Filling || left ;; Placing || left ;; Placing
random ;; Incident || knew ;; Statement || murders ;; Killing || said ;; Statement
described ;; Statement || murdered ;; Killing || bombings ;; Terrorism
bombings ;; Terrorism || revelations ;; Reveal_secret || reexamined ;; Coming_to_believe
placed ;; Placing || said ;; Communication || responsible ;; Commitment || homicide ;; Killing || connected ;; Connect
killed ;; Killing || said ;; Statement || took place ;; Process_start || believes ;; Know || killings ;; Killing || reported ;; Reporting
killed ;; Killing || interviewing ;; Communication || bombings ;; Terrorism || knew ;; Coming_to_believe || murders ;; Killing || shot ;; Bodily_harm
identified ;; Check || alleged ;; Statement || killed ;; Killing || made ;; Creating || statements ;; Statement || saying ;; Communication || committed ;; Commitment || crime ;; Killing || murders ;; Killing || murders ;; Killing || robbery ;; Robbery
involving ;; Cause_to_be_included || separate ;; Forming_relationships || War ;; Military_operation || took place ;; Process_start || Incident ;; Incident
fire ;; Attack || incident ;; Incident
conducted ;; Action || complained ;; Judgment_communication || involved ;; Cause_to_be_included || incident ;; Hostile_encounter || occurred ;; Coming_to_be || operations ;; Military_operation || incident ;; Incident || striking ;; Attack
brought ;; Bringing || incidents ;; Incident || case ;; Criminal_investigation || case ;; Criminal_investigation
decision ;; Deciding || pay ;; Commerce_pay || rendered ;; Legal_rulings
aimed ;; Aiming || talks ;; Communication || incidents ;; Incident || broke ;; Process_end || establishing ;; Creating
restored ;; Recovering
crisis ;; Killing
caused ;; Causation || disagreement ;; Quarreling || issues ;; Creating || conscripted ;; Employment || fight ;; Hostile_encounter || brought ;; Bringing
opposed ;; Agree_or_refuse_to_act || felt ;; Change_sentiment
felt ;; Change_sentiment || Led ;; Conquering
supported ;; Supporting
began ;; Process_start || service ;; Supply || enforce ;; Action
caused ;; Causation
drawn ;; Motion || sparked ;; Causation || rioting ;; Attack || occurred ;; Presence
began ;; Process_start || present ;; Presence || detained ;; Arrest || failed ;; Agree_or_refuse_to_act
release ;; Releasing || held ;; Arrest
ransacked ;; Robbery
organized ;; Arranging || conflict ;; Hostile_encounter
causing ;; Causation || disperse ;; Dispersal || fire ;; Attack || ordered ;; Arranging
casualties ;; Catastrophe || name ;; Name_conferral || debated ;; Communication || reports ;; Reporting || killed ;; Killing
injured ;; Bodily_harm
injuries ;; Bodily_harm || recorded ;; Recording || deaths ;; Death
casualties ;; Catastrophe || marked ;; Recording || damage ;; Damaging || end ;; Process_end
invaded ;; Attack || lost ;; Earnings_and_losses || lasted ;; Process_end
end ;; Process_end || followed ;; Motion
named ;; Name_conferral
war ;; Hostile_encounter || dominated ;; Control
allowed ;; Preventing_or_letting || coupled ;; Cause_to_amalgamate || gain ;; Getting || control ;; Control
signed ;; Sign_agreement || married ;; Forming_relationships || made ;; Manufacturing || regent ;; Change_of_leadership
achieved ;; Achieve || claims ;; Statement
refused ;; Agree_or_refuse_to_act || recognize ;; Know || enforce ;; Action
death ;; Death || brought ;; Bringing || crowned ;; Change_of_leadership
war ;; Hostile_encounter || dominated ;; Control
counterattacked ;; Attack || losing ;; Earnings_and_losses
crowned ;; Change_of_leadership || ensued ;; Incident
lose ;; Earnings_and_losses || captured ;; Conquering || expelled ;; Removing
war ;; Hostile_encounter || war ;; Hostile_encounter
claim ;; Statement
destroyed ;; Destroying || killing ;; Killing || war ;; Military_operation || incident ;; Incident || engaged ;; Participation
lookout ;; Patrolling || occupying ;; Control || misidentified ;; Check
blamed ;; Judgment_communication || found ;; Know || loss ;; Earnings_and_losses || inquiry ;; Criminal_investigation || cause ;; Causation
stationed ;; Placing || provide ;; Supply || intercept ;; Preventing_or_letting || support ;; Supporting
making ;; Action || contact ;; Communication || detected ;; Scrutiny || delivery ;; Military_operation
contact ;; Communication || assumed ;; Deciding
fired ;; Use_firearm || destroying ;; Destroying
discovered ;; Know || loss ;; Earnings_and_losses || attributed ;; Coming_to_believe
tests ;; Testing || suspected ;; Suspicion || proved ;; Coming_to_believe
held ;; Hold || inquiry ;; Research
stated ;; Statement || ascertain ;; Check || claim ;; Statement || lost ;; Damaging || cause ;; Causation || anguish ;; Change_sentiment || Defending ;; Defending || shot down ;; Use_firearm
confirmed ;; Check || fell ;; Motion_directional
recommended ;; Convincing || negligence ;; Rewards_and_punishments || blame ;; Judgment_communication || attributed ;; Coming_to_believe || identified ;; Check
meant ;; Causation || flight ;; Motion || notified ;; Telling
informed ;; Telling || ambush ;; Attack || travelling ;; Motion || changed ;; Change
caused ;; Causation || turned off ;; Being_in_operation || interference ;; Preventing_or_letting
criticism ;; Judgment_communication || response ;; Response || incident ;; Destroying || prompted ;; Causation
operated ;; Being_in_operation
involved ;; Cause_to_be_included || hijacking ;; Committing_crime
robbery ;; Robbery || motive ;; Causation
confessed ;; Reveal_secret || hijacking ;; Committing_crime || labelled ;; Labeling || shot ;; Use_firearm || jumping ;; Self_motion || hit ;; Attack
hold ;; Defending
brought ;; Bringing || suggested ;; Convincing || brought in ;; Bringing || registered ;; Recording || prosecution ;; Legality
stated ;; Statement
released ;; Releasing || claimed ;; Statement || deported ;; Removing
tour ;; Competition
died ;; Death || race ;; Competition || collapsed ;; Motion || ascent ;; Self_motion
fell ;; Motion || Tour ;; Competition
encouraged ;; Influence || quit ;; Departing || race ;; Competition || pressure ;; Influence
fell ;; Motion
riding ;; Self_motion || collapsed ;; Motion
pronounced ;; Statement || dead ;; Death || airlifted ;; Bringing
found ;; Know || fatal ;; Killing || combined ;; Cause_to_amalgamate || proved ;; Incident || hard ;; Confronting_problem || climb ;; Self_motion || combination ;; Cause_to_amalgamate || examination ;; Scrutiny || taken ;; Ingestion
came to ;; Motion || service ;; Rite
died ;; Death || become ;; Becoming || pilgrimage ;; Rite
dedicated ;; Supply
Concert ;; Social_event || release ;; Publishing
varying ;; Change
celebrated ;; Social_event || remembered ;; Know
killed ;; Killing
concert ;; Social_event || play ;; Participation
Planning ;; Arranging
contacted ;; Communication
involvement ;; Participation || got ;; Getting || introduced ;; Statement
performed ;; Hold || create ;; Creating
perform ;; Hold || record ;; Recording || constructed ;; Building || projected ;; Arranging
telephoned ;; Communication
ready ;; GetReady
takeoff ;; Self_motion
appalled ;; Change_sentiment
decided ;; Deciding || cancel ;; Change_event_time
telephoned ;; Communication
held ;; Defending || pay ;; Commerce_pay || lost ;; Earnings_and_losses || cancel ;; Change_event_time || asked ;; Request
run-up ;; Self_motion || go ;; Motion
poured ;; Motion || breaking ;; Bodily_harm || destroying ;; Destroying || repaired ;; Resolve_problem || ruining ;; Destroying
ended up ;; Process_end || made ;; Manufacturing || causing ;; Causation || crashing ;; Catastrophe
turned ;; Becoming || exhausted ;; Expend_resource || demanding ;; Request || complaining ;; Judgment_communication
stop ;; Process_end || put ;; Placing || threatened ;; Warning
surveillance ;; Scrutiny || causing ;; Causation || visiting ;; Traveling || installed ;; Placing
started ;; Process_start
told ;; Telling || cancelled ;; Change_event_time
change ;; Change || wind ;; Motion
agreed ;; Agree_or_refuse_to_act
used ;; Using || cause ;; Causation || covering ;; Hiding_objects || rain ;; Motion || concert ;; Social_event
began ;; Process_start || stop ;; Process_end || injured ;; Bodily_harm
noted ;; Know || falling ;; Motion_directional
Operation ;; Military_operation || operation ;; Military_operation || War ;; Hostile_encounter
completed ;; Arranging
support ;; Supporting || operation ;; Military_operation || detain ;; Arrest || murder ;; Killing || forces ;; Military_operation
suspected ;; Suspicion || detained ;; Arrest || attacks ;; Attack || operation ;; Military_operation
identified ;; Check || detainees ;; Arrest || reports ;; Reporting
threat ;; Warning || eliminate ;; Removing || said ;; Communication
combined ;; Cause_to_amalgamate
stationed ;; Placing || said ;; Communication
growing ;; Expansion || serving ;; Assistance
killed ;; Killing || result ;; Causation || reports ;; Reporting || injured ;; Bodily_harm
deployed ;; Arranging || apprehend ;; Arrest || shooting ;; Use_firearm || operation ;; Military_operation || on the run ;; Escaping || took place ;; Process_start
killed ;; Killing || remained ;; Temporary_stay
released ;; Releasing || shot ;; Use_firearm || release ;; Releasing
recognised ;; Know || contained ;; Containing || leading to ;; Causation || on the run ;; Escaping
negotiation ;; Communication || shoot ;; Use_firearm || causing ;; Causation || dead ;; Death || shot ;; Use_firearm || pronounced ;; Statement
suicide ;; Killing || ruled ;; Legal_rulings || death ;; Death || inquest ;; Criminal_investigation || found ;; Know
operation ;; Military_operation || took place ;; Process_start || covers ;; Filling
began ;; Process_start || manhunt ;; Arrest || shootings ;; Use_firearm
shooting ;; Use_firearm || parked ;; Placing || linked ;; Connect
believed ;; Know || lied ;; Telling
release ;; Releasing || posted ;; Sending || threats ;; Warning
targeted ;; Aiming || insured ;; Imposing_obligation || confiscated ;; Rewards_and_punishments
made ;; Giving || kill ;; Killing || stop ;; Preventing_or_letting
made ;; Action || appeals ;; Request
armed ;; Bearing_arms || sighting ;; Perception_active || believed ;; Know || announced ;; Expressing_publicly
final ;; Process_end || sightings ;; Perception_active || confrontation ;; Hostile_encounter || remained ;; Temporary_stay
response ;; Communication || manhunt ;; Military_operation || involving ;; Cause_to_be_included
reconnaissance ;; Scrutiny || used ;; Using
raids ;; Attack || hunt ;; Military_operation
found ;; Know || abandoned ;; GiveUp || evaded ;; Escaping || sleeping ;; Self_motion
armed ;; Bearing_arms || posed ;; Causation || believed ;; Know || posted ;; Sending || announced ;; Expressing_publicly
evading ;; Escaping || arrested ;; Arrest || assisting ;; Assistance || selecting ;; Choosing || hunt ;; Military_operation || death ;; Death || suspected ;; Suspicion
told ;; Telling || harm ;; Bodily_harm || announced ;; Expressing_publicly
commission ;; Causation
conducted ;; Action || include ;; Cause_to_be_included || leading ;; Causation || ruled out ;; Removing || investigating ;; Criminal_investigation || manhunt ;; Military_operation || death ;; Death || confrontation ;; Hostile_encounter || investigation ;; Criminal_investigation || expanded ;; Expansion
bombings ;; Catastrophe || explosions ;; Catastrophe || took place ;; Process_start
injuries ;; Bodily_harm || attacks ;; Attack || resulted in ;; Causation || fatalities ;; Death
attacks ;; Attack || organized ;; Arranging || reported ;; Reporting || coordinated ;; Arranging
helped ;; Assistance || ordered ;; Request || bombings ;; Catastrophe || believed ;; Know
gave ;; Giving || death ;; Death || death ;; Death || prison ;; Prison || suspected ;; Suspicion
arrested ;; Arrest || suspects ;; Scrutiny || tried ;; Causation
rejected ;; Agree_or_refuse_to_act || raised ;; Hold || saying ;; Statement
killing ;; Killing
began ;; Process_start
perpetrated ;; Committing_crime || targeted ;; Aiming
victims ;; Catastrophe || collaboration ;; Collaboration || suspected ;; Suspicion
thrown ;; Self_motion
convicted ;; Legal_rulings || charged ;; Imposing_obligation
convicted ;; Legal_rulings
convicted ;; Legal_rulings || granted ;; Giving || returned ;; Arriving || acquitted ;; Judgment_communication
built ;; Building || dedicated ;; Giving || victims ;; Catastrophe
known ;; Know || circulate ;; Dispersal
reported ;; Reporting
reported ;; Reporting || dropped ;; Motion_directional
died ;; Death || reported ;; Reporting
began ;; Process_start || began ;; Process_start || strike ;; Attack || known ;; Know
brought ;; Bringing
began ;; Process_start
reported ;; Reporting
died ;; Death
cause ;; Causation
identified ;; Check
resulted in ;; Causation
commented ;; Statement
proposed ;; Creating
link ;; Connect || established ;; Building
reported ;; Reporting || suspected ;; Suspicion
kill ;; Killing || makes ;; Causation
spread ;; Dispersal || announced ;; Expressing_publicly || said ;; Telling
assassinated ;; Killing || riding ;; Self_motion
shot ;; Use_firearm || building ;; Building || firing ;; Use_firearm || ambush ;; Attack || riding ;; Self_motion
wounded ;; Bodily_harm || attack ;; Attack
pronounced ;; Statement || shooting ;; Use_firearm || rushed ;; Motion || dead ;; Death || recovered ;; Recovering || injuries ;; Bodily_harm
shooting ;; Use_firearm || arrested ;; Arrest
murder ;; Killing || shot ;; Use_firearm || assassination ;; Killing || charged ;; Judgment_communication
shot ;; Use_firearm || transfer ;; Sending || covering ;; Hiding_objects
died ;; Death || taken ;; Bringing
murder ;; Killing || died ;; Death || appeal ;; Protest || trial ;; Legal_rulings || convicted ;; Legal_rulings || overturned ;; Change
killing ;; Killing || investigation ;; Criminal_investigation || concluded ;; Statement || assassinated ;; Killing
assassinated ;; Killing || die ;; Death
death ;; Death || assumed ;; Choosing
agreed ;; Agree_or_refuse_to_act || injuries ;; Bodily_harm || fired ;; Use_firearm || caused ;; Causation || assassinated ;; Killing || recording ;; Recording || shots ;; Use_firearm || conspiracy ;; Committing_crime || gunshot ;; Use_firearm || investigation ;; Criminal_investigation || concluded ;; Coming_to_believe || existence ;; Presence
involved ;; Cause_to_be_included || conspiracy ;; Committing_crime || identify ;; Know
conspiracy ;; Committing_crime || found ;; Know || investigations ;; Criminal_investigation
conspiracy ;; Committing_crime || recommended ;; Convincing || re-examined ;; Judgment_communication || rejected ;; Agree_or_refuse_to_act
identified ;; Check || support ;; Supporting || support ;; Supporting || assassination ;; Killing || conspiracy ;; Committing_crime || reports ;; Reporting || conclusion ;; Coming_to_believe || concluded ;; Coming_to_believe || determining ;; Deciding || investigations ;; Criminal_investigation
assassination ;; Killing || debate ;; Quarreling
conducted ;; Action || plot ;; Committing_crime || cover-up ;; Wearing || found ;; Know || suspected ;; Suspicion
committed ;; Commitment || massacre ;; Killing
gathered ;; Come_together || opened ;; Openness || worn ;; Wearing || participate ;; Participation || talk ;; Statement
collaborated ;; Collaboration || attack ;; Attack || suspected ;; Suspicion
found ;; Know || released ;; Releasing || surrendered ;; Surrendering
resulted in ;; Causation
provided ;; Supply || aid ;; Assistance || response ;; Response
filled ;; Filling || killing ;; Killing || occurred ;; Coming_to_be || injuring ;; Bodily_harm || bombing ;; Attack || leaving ;; Departing
killed ;; Killing || casualties ;; Catastrophe || injured ;; Bodily_harm
occurred ;; Coming_to_be || made ;; Manufacturing
arrested ;; Arrest
suspected ;; Suspicion
presented ;; Presence || acquitted ;; Committing_crime
reopened ;; Openness
killings ;; Killing || murders ;; Killing || shot ;; Use_firearm || dead ;; Death || occurred ;; Coming_to_be
shot ;; Use_firearm || attack ;; Attack || broke ;; Bodily_harm
shot ;; Use_firearm || assaulted ;; Attack || survived ;; Rescuing
wore ;; Wearing || said ;; Statement || suspected ;; Suspicion
reprisal ;; Revenge || killing ;; Killing
created ;; Creating || War ;; Military_operation
ended ;; Process_end || fighting ;; Hostile_encounter || truce ;; Process_end || war ;; Military_operation || conflict ;; Violence
attacks ;; Attack || reprisal ;; Revenge || implicated ;; Bodily_harm
killed ;; Killing || attack ;; Attack
bombing ;; Attack || reduce ;; Cause_change_of_position_on_a_scale || seen ;; Perception_active || violence ;; Violence || Operation ;; Military_operation || plan ;; Arranging
announced ;; Expressing_publicly || increase ;; Cause_change_of_position_on_a_scale
planned ;; Arranging || forces ;; Military_operation || support ;; Supporting || put ;; Placing
patrols ;; Patrolling || increased ;; Cause_change_of_position_on_a_scale
terrorist ;; Terrorism || raid ;; Attack || disrupt ;; Hindering || suspected ;; Suspicion
attacks ;; Attack || violence ;; Violence || increase ;; Cause_change_of_position_on_a_scale || occurred ;; Coming_to_be || failed ;; Agree_or_refuse_to_act
attacks ;; Attack || occurred ;; Coming_to_be || bombings ;; Terrorism || bombing ;; Terrorism
attacks ;; Attack || suspected ;; Suspicion
explosions ;; Attack || occurred ;; Coming_to_be
found ;; Know || defused ;; Resolve_problem
injured ;; Bodily_harm || bombs ;; Terrorism
occurred ;; Coming_to_be || blasts ;; Attack
ceremony ;; Social_event || play ;; Participation || scheduled ;; Arranging || took place ;; Process_start || bombings ;; Terrorism
formed ;; Creating || moved ;; Motion
killed ;; Killing
became ;; Becoming
suspected ;; Suspicion || shot ;; Killing
turned ;; Becoming || storm ;; Catastrophe
caused ;; Causation || storm ;; Attack
moving ;; Motion
became ;; Becoming || weakened ;; Cause_change_of_strength
killed ;; Killing
bombed ;; Attack
killed ;; Killing || injured ;; Bodily_harm || bombing ;; Attack || called ;; Name_conferral || end ;; Process_end
hit ;; Attack
rocked ;; Motion
killed ;; Killing || injured ;; Bodily_harm || resulted in ;; Causation || damage ;; Damaging
blaming ;; Judgment_communication || claimed ;; Statement || perceived ;; Perception_active
said ;; Telling || take ;; Creating || going ;; Self_motion
blamed ;; Judgment_communication
statement ;; Statement || threatened ;; Warning || knew ;; Know || saying ;; Telling
monitoring ;; Scrutiny || know ;; Know || said ;; Telling
suspected ;; Suspicion || arrested ;; Arrest || appeared ;; Presence
attack ;; Attack || suspected ;; Suspicion
placing ;; Placing || charged ;; Judgment_communication
hijacked ;; Kidnapping || take off ;; Departing
demanded ;; Request || release ;; Releasing || took ;; Conquering
rejects ;; Response
endured ;; Change_sentiment
threatened ;; Warning || beaten ;; Bodily_harm
moved ;; Removing
murdered ;; Killing || thrown ;; Motion
released ;; Releasing || held ;; Defending || demands ;; Request || met ;; Come_together || hostage ;; Kidnapping
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || fought ;; Hostile_encounter
attacked ;; Attack
losing ;; Earnings_and_losses || suffering ;; Bodily_harm || fought ;; Hostile_encounter
sent ;; Sending || support ;; Supporting || called up ;; Request || receiving ;; Receiving
ready ;; GetReady || Arriving ;; Arriving || sail ;; Self_motion || departing ;; Departing
effected ;; Influence || set off ;; Placing
attack ;; Attack || instructed ;; Request || Finding ;; Know || falling ;; Motion_directional || separate ;; Forming_relationships
sailed ;; Motion
capturing ;; Conquering || setting ;; Placing
spreading ;; Dispersal || determine ;; Deciding || engaged ;; Participation
loss ;; Earnings_and_losses || exploded ;; Attack
attacking ;; Attack || developed ;; Cause_to_make_progress
allowing ;; Preventing_or_letting || damage ;; Damaging || fighting ;; Hostile_encounter || suffered ;; Bodily_harm || driven ;; Motion || return ;; Arriving
restored ;; Recovering || returning ;; Arriving || action ;; Response || towed ;; Body_movement
restore ;; Recovering || inflicted ;; Change_sentiment || brought ;; Bringing || contribute ;; Giving
losses ;; Earnings_and_losses || celebrated ;; Social_event || promoted ;; Cause_to_make_progress
took place ;; Process_start
capturing ;; Conquering || taking ;; Conquering || daunted ;; Change_sentiment
guarded ;; Patrolling
used ;; Using || climb ;; Self_motion
inflicted ;; Causation
prelude ;; Process_start
pulled ;; Motion || records ;; Recording || capturing ;; Conquering || holding ;; Hold || rule ;; Control
raided ;; Attack
raid ;; Attack
retreated ;; Escaping || besieged ;; Besieging || arrived ;; Arriving
diverted ;; Change_sentiment
raided ;; Attack || encountering ;; Hostile_encounter
destroyed ;; Destroying || damaged ;; Damaging || organize ;; Arranging || supplies ;; Supply || met with ;; Come_together
sailed ;; Self_motion
surrendered ;; Surrendering
affected ;; Influence || Cyclone ;; Catastrophe
formed ;; Coming_to_be || storm ;; Catastrophe || Originating ;; Dispersal || plagued ;; Bodily_harm
received ;; Receiving || organised ;; Arranging || name ;; Name_conferral
existence ;; Presence || cyclone ;; Catastrophe || caught ;; Causation
drifted ;; Motion || cyclone ;; Catastrophe || building ;; Building || drove ;; Motion
intensifying ;; Cause_change_of_strength || landfall ;; Arriving || sustained ;; Damaging || cyclone ;; Catastrophe
weakened ;; Cause_change_of_strength || cyclone ;; Catastrophe || diminishing ;; Cause_change_of_position_on_a_scale || progressed ;; Achieve
production ;; Manufacturing || exporting ;; Sending || mining ;; Participation || cyclone ;; Catastrophe || halted ;; Process_end
delayed ;; Change_event_time || contributing ;; Giving || inflated ;; Cause_change_of_position_on_a_scale
production ;; Manufacturing || exports ;; Sending || suspended ;; Process_end
attributed ;; Causation || lost ;; Earnings_and_losses
produced ;; Manufacturing || damage ;; Damaging || storm ;; Catastrophe || rainfall ;; Motion || widespread ;; Judgment_communication || limited ;; Limiting
damaged ;; Damaging || brought ;; Bringing || storm ;; Catastrophe || storm ;; Catastrophe || damage ;; Damaging || reported ;; Reporting
rainfall ;; Motion || endured ;; Change_sentiment
provided ;; Supply || cyclone ;; Catastrophe || retired ;; Removing
held ;; Hold || festival ;; Social_event
festival ;; Social_event || held ;; Hold || making ;; Causation
festival ;; Social_event || making it ;; Causation || choice ;; Choosing
festival ;; Social_event || attend ;; Participation || see ;; Perception_active || ways ;; Action || tickets ;; Rewards_and_punishments || party ;; Social_event
camping ;; Placing || immerse ;; Change_sentiment
festival ;; Social_event || held ;; Hold
played ;; Competition || Shield ;; Competition
finished ;; Process_end || involved ;; Participation
Watched ;; Perception_active
vowed ;; Commitment || embroiled ;; Change_sentiment || withdrew ;; Escaping
appearance ;; Presence || replacement ;; Removing || signed ;; Statement || made ;; Action
started ;; Process_start || named ;; Name_conferral
crossed ;; Motion_directional || hit ;; Connect
assisted ;; Assistance || score ;; Getting || converted ;; Change
marked ;; Recording || defeat ;; Earnings_and_losses
win ;; Getting || beat ;; Competition
described ;; Statement || showed ;; Protest || ready ;; GetReady || beat ;; Competition
believed ;; Statement || highlighted ;; Justifying
began ;; Process_start || lasted ;; Process_end || abandoned ;; GiveUp
commanded ;; Control
took place ;; Process_start
ended ;; Process_end || restored ;; Recovering
defeat ;; Conquering || inflicted ;; Change_sentiment
captured ;; Conquering
siege ;; Besieging || undertook ;; Action
realized ;; Coming_to_believe || concentrated ;; Aiming || reducing ;; Cause_change_of_position_on_a_scale
evacuated ;; Emptying || fell ;; Motion_directional
happened ;; Presence
festival ;; Social_event || dance ;; Body_movement || dancing ;; Motion || held ;; Hold
taking place ;; Process_start || shopping ;; Commerce_buy || celebration ;; Social_event || aim ;; Aiming || inspiring ;; Change_sentiment
Initiated ;; Process_start || delivered ;; Sending || delivered ;; Sending || partnership ;; Collaboration || events ;; Social_event || inspiration ;; Influence
election ;; Change_of_leadership || elected ;; Change_of_leadership || took place ;; Process_start || festival ;; Social_event
selected ;; Choosing || receiving ;; Receiving || festivals ;; Social_event || deliver ;; Sending
delivered ;; Sending || partnership ;; Collaboration
delivered ;; Sending || partnership ;; Collaboration
Festival ;; Social_event || Olympiad ;; Competition || Games ;; Competition
festival ;; Social_event
final ;; Process_end || bringing ;; Bringing || festival ;; Social_event || finale ;; Process_end
invasion ;; Attack || called ;; Name_conferral || followed ;; Self_motion
remove ;; Removing || aims ;; Aiming || control ;; Control || regain ;; Recovering
withdrawal ;; Escaping || fighting ;; Hostile_encounter || started ;; Process_start || led to ;; Causation
strengthened ;; Cause_change_of_strength || humiliated ;; Change_sentiment
invaded ;; Attack
joint ;; Collaboration || issued ;; Creating
landed ;; Motion
block ;; Hiding_objects || defeated ;; Conquering
became ;; Becoming || invasion ;; Attack || attack ;; Attack || planned ;; Arranging
attained ;; Getting
withdrawal ;; Escaping || led to ;; Causation
warned ;; Warning || damage ;; Damaging || selling ;; Commerce_sell || threatened ;; Warning
signified ;; Statement || end ;; Process_end || conclude ;; Statement
closed ;; Process_end
freedom ;; Change_of_leadership || blocked ;; Hiding_objects || attaining ;; Getting || fulfilled ;; Achieve
created ;; Creating || conflict ;; Hostile_encounter || emboldened ;; Influence || resigned ;; Change_of_leadership
formed ;; Coming_to_be
cut ;; Removing
co-operated ;; Collaboration
confront ;; Hostile_encounter
attacked ;; Attack || fought ;; Hostile_encounter || attacked ;; Attack
captured ;; Conquering || reinforce ;; Cause_change_of_strength || broke ;; Bodily_harm
diverted ;; Change_sentiment
suspended ;; Process_end
gained ;; Getting || diverted ;; Removing
ended ;; Process_end || stopped ;; Process_end
advanced ;; Cause_to_make_progress
suffered ;; Bodily_harm
resulted in ;; Causation || shortened ;; Change_event_time || capture ;; Conquering || Battle ;; Hostile_encounter || invasion ;; Attack
capturing ;; Conquering || advanced ;; Motion
surprised ;; Change_sentiment || see ;; Perception_active
wired ;; Connect
detonated ;; Attack || blow it ;; Damaging
captured ;; Conquering || expanded ;; Expansion || forces ;; Military_operation || planned ;; Arranging
prevented ;; Preventing_or_letting || regrouping ;; Cause_to_amalgamate || consolidating ;; Cause_change_of_strength || positions ;; Placing
caused ;; Causation || forces ;; Military_operation || employ ;; Employment
used ;; Using || capture ;; Conquering || failure ;; Earnings_and_losses || destroy ;; Destroying
included ;; Cause_to_be_included
attacked ;; Attack || developed ;; Creating || using ;; Using
positioned ;; Placing || protect ;; Defending || leading to ;; Causation
attacking ;; Attack
shot down ;; Use_firearm || dispatched ;; Sending || claimed ;; Statement
failed ;; Agree_or_refuse_to_act
destroy ;; Destroying || fire ;; Use_firearm || ordered ;; Request
marked ;; Recording || used ;; Using || fired ;; Use_firearm
killed ;; Killing || launched ;; Motion
sent ;; Sending || ready ;; GetReady || breathing ;; Breathing
captured ;; Conquering || killed ;; Killing || detected ;; Know || deployed ;; Arranging
capture ;; Conquering
alter ;; Change || end ;; Process_end || allowed ;; Preventing_or_letting
transport ;; Bringing
bombing ;; Attack || endured ;; Change_sentiment || hits ;; Attack
killing ;; Killing || wounding ;; Bodily_harm || collapsed ;; Destroying
building ;; Building || finished ;; Process_end
broke out ;; Process_start || crossed ;; Motion_directional
captured ;; Conquering
agreed ;; Agree_or_refuse_to_act || shortened ;; Change_event_time || capturing ;; Conquering
rebuilt ;; Recovering || war ;; Hostile_encounter
occurred ;; Presence || incident ;; Killing
rebuff ;; Agree_or_refuse_to_act || advances ;; Self_motion || come to ;; Self_motion || services ;; Supply
stabbed ;; Bodily_harm || death ;; Death || fight ;; Hostile_encounter || resulting in ;; Causation
refused ;; Agree_or_refuse_to_act || arrested ;; Arrest || grant ;; Preventing_or_letting || charged ;; Legality || homicide ;; Committing_crime
came to ;; Motion || case ;; Killing || enraged ;; Change_sentiment
case ;; Killing || anger ;; Judgment_communication || corruption ;; Committing_crime || garnered ;; Getting
limiting ;; Limiting || incident ;; Killing || downplay ;; Preventing_or_letting || discussion ;; Communication
charged ;; Judgment_communication || murder ;; Killing || granted ;; Giving || assault ;; Attack || petitions ;; Request || bail ;; Legality || protests ;; Protest || offense ;; Attack
receive ;; Receiving || due ;; Causation || sentence ;; Prison || guilty ;; Legal_rulings || found ;; Coming_to_believe
involved ;; Cause_to_be_included || response ;; Response || surviving ;; Presence || incident ;; Killing || sacked ;; Process_end
match ;; Competition || played ;; Competition
match ;; Competition || beat ;; Competition
match ;; Competition || described ;; Statement || inflicted ;; Change_sentiment || defeat ;; Earnings_and_losses
startling ;; Know || inflicted ;; Change_sentiment || defeat ;; Earnings_and_losses || assumed ;; Causation
played ;; Competition || drawn ;; Causation
champions ;; Competition || defeated ;; Earnings_and_losses
fought ;; Hostile_encounter
deposed ;; Change_of_leadership || re-establish ;; Recovering
destabilised ;; Change
collapsed ;; Destroying || defeated ;; Conquering
became ;; Becoming
war ;; Military_operation
noted ;; Know || referred ;; Name_conferral
fight ;; Hostile_encounter || campaigned ;; Military_operation
defeated ;; Conquering || defeating ;; Conquering || established ;; Creating
knew ;; Know
war ;; Hostile_encounter || blames ;; Judgment_communication || inspiring ;; Change_sentiment
destroyed ;; Destroying || destroyed ;; Destroying || defeated ;; Conquering
decided ;; Deciding || war ;; Hostile_encounter || led ;; Causation || pulled out ;; Motion
Revolution ;; Change_of_leadership || insurrection ;; Change_of_leadership
began ;; Process_start || ended ;; Process_end
involved ;; Participation || emerging ;; Coming_to_be
ruled ;; Control || uprising ;; Change_of_leadership || founding ;; Creating
defining ;; Statement || seen ;; Statement
effects ;; Influence || felt ;; Change_sentiment
defense ;; Defending || end ;; Process_end || rule ;; Control || collaboration ;; Collaboration || followed ;; Motion
uprising ;; Change_of_leadership || revolt ;; Change_of_leadership
alleged ;; Statement || challenged ;; Competition || achieve ;; Achieve || maintain ;; Cause_to_make_progress
frightened ;; Influence || inspired ;; Change_sentiment || shocked ;; Response
standoff ;; Hostile_encounter || crisis ;; Hostile_encounter
revolution ;; Change_of_leadership || took over ;; Conquering || supported ;; Supporting || held ;; Defending
described ;; Statement || crisis ;; Hostile_encounter
terrorism ;; Terrorism || called ;; Name_conferral || hostage-taking ;; Terrorism
revolution ;; Change_of_leadership || seen ;; Perception_active || support ;; Supporting || influence ;; Influence || perceived ;; Perception_active || overthrown ;; Change_of_leadership
treatment ;; Cure || cancer ;; Cure || overthrown ;; Change_of_leadership
demanded ;; Request || accused ;; Judgment_communication || trial ;; Criminal_investigation || reign ;; Change_of_leadership || committing ;; Committing_crime
accused ;; Judgment_communication || help ;; Assistance || committing ;; Committing_crime
demands ;; Request || decision ;; Deciding || grant ;; Giving || rejected ;; Agree_or_refuse_to_act
granted ;; Giving || arrest ;; Arrest || saw ;; Self_motion || violation ;; Legality || made ;; Causation
negotiations ;; Communication || release ;; Releasing || reached ;; Arriving || win ;; Getting || crisis ;; Hostile_encounter
ordered ;; Arranging || rescue ;; Rescuing || using ;; Using || included ;; Cause_to_be_included || patrolling ;; Patrolling
death ;; Death || deaths ;; Death || crashed ;; Catastrophe || resulted in ;; Causation
resigned ;; Change_of_leadership
rescued ;; Rescuing || capture ;; Conquering || evaded ;; Attack || joint ;; Collaboration
granted ;; Giving || died ;; Death || cancer ;; Cure || left ;; Departing
invaded ;; Attack || beginning ;; Process_start || War ;; Hostile_encounter
negotiations ;; Communication || led ;; Causation
crisis ;; Hostile_encounter
released ;; Releasing || loss ;; Earnings_and_losses || signing ;; Sign_agreement || analysts ;; Scrutiny || cited ;; Adducing || standoff ;; Hostile_encounter || downfall ;; Change_of_leadership || election ;; Change_of_leadership || sworn ;; Commitment
strengthened ;; Cause_change_of_strength || opposed ;; Agree_or_refuse_to_act || crisis ;; Hostile_encounter
weakened ;; Cause_change_of_strength || crisis ;; Hostile_encounter || sanctions ;; Rewards_and_punishments || led to ;; Causation
held ;; Hold || tournament ;; Competition
tournament ;; Competition || tournaments ;; Competition
declared ;; Expressing_publicly || given ;; Giving || tournament ;; Competition || competing ;; Participation || match ;; Competition || match ;; Competition || elimination ;; Removing
holds ;; Hold || tournament ;; Competition
tournament ;; Competition || matches ;; Competition
tournament ;; Competition || event ;; Competition || work ;; Action || positions ;; Employment
match ;; Competition || gets ;; Getting || awarded ;; Award || match ;; Competition || reward ;; Rewards_and_punishments || increased ;; Cause_change_of_position_on_a_scale || winning ;; Earnings_and_losses
tournaments ;; Competition || tournament ;; Competition || tournaments ;; Competition || winning ;; Getting
tournaments ;; Competition || tournaments ;; Competition || win ;; Getting
appeared ;; Presence || appeared ;; Presence || appearing ;; Presence || tournaments ;; Competition
participated ;; Participation
appeared ;; Presence
appeared ;; Presence || given ;; Giving
participated ;; Participation || tournament ;; Competition || opening ;; Openness || worked ;; Being_in_operation
change ;; Change || acknowledged ;; Statement || adopting ;; Choosing
became ;; Becoming || tournament ;; Competition || participation ;; Participation || winning ;; Getting
disaster ;; Catastrophe || fire ;; Catastrophe
caused ;; Causation || died ;; Death || smoke ;; Ingestion || falling ;; Motion_directional || jumping ;; Self_motion
victims ;; Catastrophe || known ;; Know || victim ;; Catastrophe
building ;; Building || Building ;; Building
escape ;; Escaping || practice ;; Practice || taking ;; Creating || reduce ;; Cause_change_of_position_on_a_scale || jumped ;; Self_motion
helped ;; Assistance || led to ;; Causation || working ;; Employment || growth ;; Cause_change_of_position_on_a_scale || improved ;; Cause_to_make_progress || fought ;; Hostile_encounter
building ;; Building || designated ;; Choosing
named ;; Name_conferral || sexually assaulted ;; Committing_crime
pleaded ;; Request || abuse ;; Committing_crime
released ;; Releasing || attackers ;; Attack || argued ;; Quarreling || punished ;; Rewards_and_punishments || instructed ;; Causation
developed ;; Coming_to_be || dropped ;; Motion || regarding ;; Know || held ;; Defending
requested ;; Request || said ;; Communication || sentencing ;; Legal_rulings || revise ;; Change
removed ;; Removing || ordered ;; Arranging || served ;; Assistance || reaching ;; Arriving || expunged ;; Removing
deal ;; Resolve_problem
abuse ;; Committing_crime || pleaded ;; Request
agreed ;; Agree_or_refuse_to_act || service ;; Supply
attended ;; Perception_active
follow ;; Self_motion || admonished ;; Judgment_communication
accused ;; Judgment_communication || state ;; Statement
said ;; Communication || discussed ;; Communication || took ;; Creating || indicating ;; Causation
talk ;; Telling
permitted ;; Preventing_or_letting || victim ;; Catastrophe
takes place ;; Process_start
perform ;; Hold || record ;; Recording
formed ;; Coming_to_be
learn ;; Education_teaching || hear ;; Perception_active || participate ;; Participation || writing ;; Creating || training ;; Education_teaching
players ;; Competition || targeted ;; Aiming
participated ;; Participation || Founded ;; Creating
produced ;; Manufacturing || aired ;; Expressing_publicly
gave ;; Giving || party ;; Participation
died ;; Death || stroke ;; Bodily_harm
honoured ;; Rewards_and_punishments
evoked ;; Causation || death ;; Death || praise ;; Judgment_communication || criticism ;; Judgment_communication || mixed ;; Cause_to_amalgamate
cost ;; Cost || procession ;; Self_motion
paid ;; Commerce_pay || attendance ;; Participation
ceremony ;; Social_event || buried ;; Placing
festival ;; Social_event || performed ;; Action
Beginning ;; Process_start || separate ;; Hold
festival ;; Social_event || festival ;; Social_event || held ;; Hold
acts ;; Action || festival ;; Social_event || expanded ;; Expansion || limited ;; Limiting || include ;; Cause_to_be_included
includes ;; Cause_to_be_included
supported ;; Supporting || presented ;; Presence
sales ;; Commerce_sell || due ;; Causation || changed ;; Change || cancelled ;; Change_event_time
announced ;; Expressing_publicly || returning ;; Arriving
supporting ;; Supporting || presenting ;; Presence
acts ;; Action || open ;; Openness || established ;; Creating
occurred ;; Coming_to_be || hit ;; Attack || rally ;; Social_event || bombing ;; Attack
killed ;; Killing || injured ;; Bodily_harm || blast ;; Attack
death ;; Death || rose ;; Cause_change_of_position_on_a_scale
arrested ;; Arrest || bombings ;; Terrorism || attack ;; Attack
attacks ;; Attack || happened ;; Presence
reported ;; Reporting || decline ;; Cause_change_of_position_on_a_scale || attack ;; Attack || assisted ;; Assistance
battle ;; Hostile_encounter || battle ;; Hostile_encounter || monitor ;; Scrutiny || battle ;; Hostile_encounter || battle ;; Hostile_encounter || war ;; Military_operation
fought ;; Hostile_encounter
battle ;; Hostile_encounter || break ;; Destroying || blockade ;; Besieging || cut off ;; Having_or_lacking_access
battle ;; Hostile_encounter || meeting ;; Come_together
built ;; Building || consisted ;; Cause_to_be_included
battle ;; Hostile_encounter || opposed ;; Agree_or_refuse_to_act
destroy ;; Destroying || attack ;; Attack || run ;; Self_motion
falling ;; Motion_directional || included ;; Cause_to_be_included || battle ;; Hostile_encounter || halted ;; Process_end || retired ;; Escaping || repair ;; Recovering
destruction ;; Destroying || absence ;; Presence || returned ;; Self_motion
defend ;; Defending || monitor ;; Scrutiny || arrived ;; Arriving
monitor ;; Scrutiny || approached ;; Self_motion || intercepted ;; Attack
damage ;; Damaging || inflict ;; Bodily_harm || fought ;; Hostile_encounter
ended ;; Process_end || defending ;; Defending || monitor ;; Scrutiny || strengthening ;; Cause_change_of_strength || returning ;; Arriving
fight ;; Hostile_encounter || blockade ;; Besieging
received ;; Receiving || battle ;; Hostile_encounter || effects ;; Achieve
halted ;; Process_end || powers ;; Influence || construction ;; Building
battle ;; Hostile_encounter || arrived ;; Arriving || engaged ;; Participation || race ;; Competition || signalled ;; Statement
monitor ;; Scrutiny || produced ;; Creating
mounted ;; Connect || became ;; Becoming || monitor ;; Scrutiny || demonstrated ;; Coming_to_be
incorporated ;; Cause_to_be_included
tour ;; Traveling || debut ;; Process_start || concert ;; Social_event || recording ;; Recording
tour ;; Traveling || supported ;; Supporting || dead ;; Death
tour ;; Traveling || travelling ;; Traveling || took place ;; Process_start
targeted ;; Aiming || support ;; Supporting
began ;; Process_start || tour ;; Traveling || relationship ;; Forming_relationships
ended ;; Process_end
released ;; Publishing || tour ;; Traveling || mixed ;; Cause_to_amalgamate || concerts ;; Social_event || concert ;; Social_event || inserted ;; Connect || contains ;; Containing
rowing ;; Self_motion || held ;; Defending
rowing ;; Self_motion
brings ;; Bringing
races ;; Competition
race ;; Competition || sculling ;; Self_motion
include ;; Cause_to_be_included || rowing ;; Self_motion
competitive ;; Competition
included ;; Cause_to_be_included
conducted ;; Action
beginning ;; Process_start
relieving ;; Cure
inspecting ;; Scrutiny || killed ;; Killing || fell ;; Motion_directional || command ;; Control
surrendered ;; Surrendering || withdrew ;; Escaping
promoted ;; Cause_to_make_progress || promoted ;; Cause_to_make_progress
occurred ;; Coming_to_be || murder ;; Committing_crime || attending ;; Participation
assaulted ;; Attack || reviewing ;; Scrutiny || called ;; Name_conferral
left ;; Departing || kicked ;; Self_motion
found ;; Know || damage ;; Damaging || crushed ;; Bodily_harm
murder ;; Committing_crime || pleaded ;; Request || sentenced ;; Legal_rulings
released ;; Releasing || serving ;; Assistance
pleaded ;; Request || pleaded ;; Request
murder ;; Committing_crime || charged ;; Judgment_communication || charged ;; Judgment_communication
match ;; Competition
recommending ;; Convincing || investigation ;; Criminal_investigation || investigation ;; Criminal_investigation || reviewed ;; Research
received ;; Receiving
focusing ;; Aiming || murder ;; Committing_crime || concentrated ;; Aiming
cited ;; Adducing || murder ;; Committing_crime || blaming ;; Judgment_communication
renamed ;; Name_conferral || entitled ;; Preventing_or_letting || revised ;; Change || inspired ;; Influence
artist ;; Create_artwork || concert ;; Social_event || recording ;; Recording || tour ;; Traveling || Tour ;; Traveling
support ;; Supporting || launched ;; Process_start
promoted ;; Cause_to_make_progress || marking ;; Recording || tour ;; Traveling || tour ;; Traveling
postponed ;; Change_event_time || announced ;; Expressing_publicly || tour ;; Traveling || kicked off ;; Process_start
announced ;; Expressing_publicly || leg ;; Traveling || tour ;; Traveling
explained ;; Statement || coming ;; Motion || directed ;; Action || choreographed ;; Create_artwork || show ;; Social_event
united ;; Cause_to_amalgamate || composed ;; Come_together || designed ;; Create_artwork
developed ;; Coming_to_be || device ;; Supply || Inspired ;; Influence || flying ;; Motion || travel ;; Traveling
supporting ;; Supporting || felt ;; Change_sentiment
included ;; Cause_to_be_included || remixed ;; Cause_to_amalgamate
show ;; Social_event
hits ;; Bodily_harm || gyrating ;; Body_movement || setting ;; Placing || opened ;; Openness || show ;; Social_event || hanging ;; Body_movement || performing ;; Hold || jumping ;; Motion || flying ;; Self_motion || dancing ;; Self_motion
effects ;; Achieve
pumped ;; Motion || tour ;; Traveling
changes ;; Change || included ;; Cause_to_be_included || made ;; Manufacturing || unreleased ;; Publishing || leg ;; Traveling || remixed ;; Cause_to_amalgamate || premiered ;; Social_event
received ;; Receiving || praised ;; Judgment_communication || acclaim ;; Judgment_communication || show ;; Social_event || dismissed ;; Judgment_communication || taking ;; Bringing
bringing ;; Bringing || sold ;; Commerce_sell || gross ;; Earnings_and_losses || gross ;; Earnings_and_losses || shows ;; Social_event || show ;; Social_event
sold ;; Commerce_sell || grossed ;; Earnings_and_losses || shows ;; Social_event || Tour ;; Traveling
performed ;; Hold
Concert ;; Social_event || grossed ;; Earnings_and_losses || show ;; Social_event
left ;; Departing || storm ;; Catastrophe || canceled ;; Change_event_time || due ;; Causation || lightning ;; Lighting || show ;; Social_event || show ;; Social_event || angered ;; Causation
broadcast ;; Expressing_publicly || tour ;; Traveling || win ;; Earnings_and_losses
titled ;; Name_conferral || released ;; Publishing
festival ;; Social_event || festival ;; Social_event || held ;; Hold
festival ;; Social_event || festival ;; Social_event || cited ;; Adducing || cited ;; Adducing
called ;; Name_conferral || ran ;; Being_in_operation || sponsored ;; Supply
promoted ;; Cause_to_make_progress || sponsored ;; Supply
festival ;; Social_event || expanded ;; Expansion
travelling ;; Traveling || took place ;; Process_start
takes place ;; Process_start
festival ;; Social_event || travelling ;; Traveling || attending ;; Participation || attracted ;; Influence || experiencing ;; Change_sentiment
festival ;; Social_event || spoken ;; Statement
festival ;; Social_event || attend ;; Participation
attracted ;; Influence
attributed ;; Causation || access ;; Having_or_lacking_access
festivals ;; Social_event || festivals ;; Social_event || named ;; Name_conferral || included ;; Cause_to_be_included || leading ;; Causation || describe ;; Statement
described ;; Statement || established ;; Building || aimed ;; Aiming
received ;; Receiving || mention ;; Statement
announced ;; Expressing_publicly || going ;; Self_motion || citing ;; Adducing
played ;; Participation || Championship ;; Competition
contract ;; Sign_agreement || televised ;; Communication || launching ;; Process_start
became ;; Becoming || champion ;; Competition
became ;; Becoming || passing ;; Motion_directional || champion ;; Competition || win ;; Getting
win ;; Getting
played ;; Participation || Championship ;; Competition || played ;; Competition || hosted ;; Social_event
planned ;; Arranging || run ;; Self_motion
competition ;; Competition
ended ;; Process_end || brought ;; Bringing
qualify ;; Getting
brought ;; Bringing || changed ;; Change || limit ;; Limiting
enter ;; Becoming_a_member || reach ;; Arriving || pass ;; Motion_directional
selected ;; Choosing || become ;; Becoming || elimination ;; Removing || moving ;; Motion
allows ;; Preventing_or_letting || compete ;; Competition || competing ;; Competition
increased ;; Cause_change_of_position_on_a_scale
given ;; Giving || draw ;; Create_artwork || determine ;; Deciding || order ;; Arranging
performed ;; Hold || given ;; Giving || advanced ;; Cause_to_make_progress
vote ;; Choosing
supported ;; Supporting || forces ;; Military_operation || invading ;; Attack || took place ;; Process_start
pitted ;; Hostile_encounter
reported ;; Reporting || fought ;; Hostile_encounter
disputed ;; Quarreling || occurred ;; Coming_to_be
involved ;; Participation || lasted ;; Process_end
gains ;; Getting || occurred ;; Coming_to_be
destroying ;; Destroying
disputed ;; Quarreling || killed ;; Killing || losses ;; Earnings_and_losses || fighting ;; Hostile_encounter || injured ;; Bodily_harm || believed ;; Know || taken ;; Conquering
slaughtered ;; Killing
includes ;; Cause_to_be_included || massacres ;; Killing || interview ;; Social_event
says ;; Communication || perished ;; Death
halting ;; Process_end
marked ;; Recording || credited ;; Earnings_and_losses || revival ;; Recovering || defeat ;; Conquering
sent ;; Sending || punish ;; Rewards_and_punishments || shaken ;; Body_movement || re-establish ;; Recovering
ensued ;; Coming_to_be || met ;; Come_together
disintegrated ;; Change || killing ;; Killing
Identifying ;; Check || cause ;; Causation || disaster ;; Catastrophe || occurred ;; Presence || accident ;; Catastrophe || proved ;; Coming_to_believe
examining ;; Testing || begun ;; Process_start || left ;; Departing || accident ;; Catastrophe || conclude ;; Coming_to_believe || investigated ;; Criminal_investigation || interviewing ;; Communication
caused ;; Causation || destroyed ;; Destroying || determine ;; Deciding || bending ;; Motion || investigation ;; Criminal_investigation || flutter ;; Motion || determined ;; Deciding || investigation ;; Criminal_investigation || stalled ;; Change_event_time
cause ;; Causation || accident ;; Catastrophe || flutter ;; Motion || progress ;; Cause_to_make_progress || identifying ;; Check
cause ;; Causation || accident ;; Catastrophe || breakthrough ;; Achieve || unlocking ;; Having_or_lacking_access || came ;; Arriving || crash ;; Catastrophe
disintegrated ;; Change || losing ;; Earnings_and_losses
crash ;; Catastrophe || end ;; Process_end || cause ;; Causation || investigation ;; Criminal_investigation || discovered ;; Know || coupling ;; Cause_to_amalgamate || identified ;; Check
Flight ;; Self_motion || accident ;; Catastrophe || report ;; Reporting || issued ;; Statement
established ;; Creating
endorsed ;; Ratification
promote ;; Cause_change_of_position_on_a_scale || recognizes ;; Agree_or_refuse_to_act
used ;; Using
decided ;; Deciding || designate ;; Choosing
assembly ;; Building || view ;; Perception_active || found ;; Know || saying ;; Statement || promotes ;; Cause_to_make_progress
rebuild ;; Building || relationship ;; Forming_relationships || cause ;; Causation || uniting ;; Collaboration
fought ;; Hostile_encounter || conflict ;; Hostile_encounter
conflict ;; Hostile_encounter
began ;; Process_start || revolt ;; Change_of_leadership
ended ;; Process_end || insurrection ;; Change_of_leadership
fought ;; Hostile_encounter
allied ;; Collaboration || war ;; Hostile_encounter || subdued ;; Conquering
sent ;; Sending || war ;; Hostile_encounter || reignited ;; Recovering
siege ;; Besieging || take ;; Conquering || subjugated ;; Conquering
negotiated ;; Communication || suffered ;; Bodily_harm || defeats ;; Conquering
negotiating ;; Communication || arrived ;; Arriving || finish ;; Process_end || came to ;; Motion || disavowed ;; Agree_or_refuse_to_act
decided ;; Deciding || sent ;; Sending || war ;; Hostile_encounter || ignore ;; Agree_or_refuse_to_act
assaulted ;; Attack || repulsed ;; Defending || accept ;; Agree_or_refuse_to_act
sent ;; Sending || ratify ;; Ratification
avoided ;; Preventing_or_letting || conflict ;; Hostile_encounter
sent ;; Sending || end ;; Process_end || war ;; Hostile_encounter
recruited ;; Employment
built ;; Building || beginning ;; Process_start || siege ;; Besieging
suffering ;; Bodily_harm
ushered in ;; Bringing
war ;; Hostile_encounter || launched ;; Bringing
present ;; Presence
negotiate ;; Communication || selected ;; Choosing
war ;; Hostile_encounter || fought ;; Hostile_encounter
rape ;; Committing_crime || involved ;; Cause_to_be_included || rape ;; Committing_crime || assault ;; Attack || occurred ;; Presence
beaten ;; Bodily_harm || tortured ;; Bodily_harm || took place ;; Process_start || gang ;; Committing_crime || incident ;; Killing || travelling ;; Traveling
beat ;; Bodily_harm
treatment ;; Cure || assault ;; Attack || transferred ;; Sending || succumbed ;; Death || injuries ;; Bodily_harm
widespread ;; Judgment_communication || incident ;; Killing || generated ;; Causation
took place ;; Process_start || provide ;; Supply || protests ;; Protest || clashed ;; Attack
took place ;; Hold || protests ;; Protest
publish ;; Publishing || death ;; Death || become ;; Becoming || allow ;; Preventing_or_letting || struggle ;; Confronting_problem
accused ;; Judgment_communication || arrested ;; Arrest || sexual assault ;; Committing_crime || murder ;; Killing || charged ;; Judgment_communication
accused ;; Judgment_communication || suicide ;; Killing || died ;; Death
published ;; Publishing || defense ;; Defending || reports ;; Reporting || murdered ;; Committing_crime || hanged ;; Self_motion
presenting ;; Presence || accused ;; Judgment_communication || finished ;; Process_end || trial ;; Criminal_investigation || prosecution ;; Judgment_communication
given ;; Giving || rape ;; Committing_crime || imprisonment ;; Prison || convicted ;; Legal_rulings || murder ;; Committing_crime
found ;; Know || rape ;; Committing_crime || death ;; Death || hanging ;; Motion || murder ;; Committing_crime || sentenced ;; Legal_rulings
death ;; Death || death ;; Death
investigation ;; Criminal_investigation || suggestions ;; Convincing || set up ;; Reforming_a_system || study ;; Scrutiny || amend ;; Ratification || provide ;; Supply || prosecution ;; Judgment_communication
submitted ;; Submitting_documents || suggestions ;; Convincing || report ;; Reporting || cause ;; Causation || considering ;; Deciding || indicated ;; Statement
promulgated ;; Statement || passed ;; Process_end || created ;; Creating
agree ;; Agree_or_refuse_to_act || increase ;; Cause_change_of_position_on_a_scale || report ;; Reporting || improvement ;; Cause_to_make_progress || argue ;; Protest || resulted in ;; Causation || prosecute ;; Legality
said ;; Communication || victim ;; Catastrophe || reform ;; Reforming_a_system || attack ;; Attack || bring ;; Bringing || called ;; Request
attack ;; Attack || titled ;; Name_conferral || broadcast ;; Expressing_publicly
exploring ;; Scrutiny || incident ;; Attack
search ;; Scrutiny
revolution ;; Reforming_a_system || overthrow ;; Change_of_leadership
organized ;; Arranging
changing ;; Change || assassination ;; Killing || movement ;; Reforming_a_system || adapt ;; Cause_to_make_progress || led to ;; Causation || pressure ;; Request
party ;; Social_event || found ;; Creating
party ;; Social_event || lost ;; Conquering || presented ;; Presence
combat ;; Hostile_encounter || proclaimed ;; Statement
revolution ;; Reforming_a_system || approval ;; Ratification
changed ;; Change || establishment ;; Coming_to_be
revolution ;; Reforming_a_system || advance ;; Cause_to_make_progress || produced ;; Causation
revolution ;; Change_of_leadership || revolution ;; Change_of_leadership || led to ;; Causation || fall ;; Motion
began ;; Process_start || elections ;; Choosing
alleged ;; Statement
fled ;; Escaping
statement ;; Statement || signed ;; Sign_agreement
ratified ;; Ratification
War ;; Hostile_encounter || support ;; Supporting || support ;; Supporting || War ;; Hostile_encounter || War ;; Hostile_encounter || war ;; Hostile_encounter
began ;; Process_start || invaded ;; Attack || war ;; Hostile_encounter || clashes ;; Hostile_encounter
War ;; Hostile_encounter || product ;; Manufacturing || set ;; Placing
state ;; Statement || state ;; Statement || established ;; Creating
claimed ;; Statement || legitimate ;; Legality
warfare ;; Hostile_encounter || conflict ;; Hostile_encounter || advanced ;; Cause_to_make_progress
Command ;; Request || repel ;; Defending || authorized ;; Ratification || dispatch ;; Sending || recognized ;; Know || invasion ;; Attack
providing ;; Supply || contributed ;; Assistance
defeat ;; Conquering || war ;; Hostile_encounter || dispatched ;; Sending
retreated ;; Escaping || defensive ;; Defending
cut off ;; Removing || launched ;; Military_operation
escaped ;; Escaping || capture ;; Conquering || forced ;; Request
invaded ;; Attack || war ;; Hostile_encounter || crossed ;; Motion_directional || moved ;; Motion || entered ;; Arriving
retreat ;; Escaping || triggered ;; Causation
became ;; Becoming || war ;; Hostile_encounter || fighting ;; Hostile_encounter || battles ;; Hostile_encounter || changed ;; Change
war ;; Hostile_encounter
bombing ;; Attack || campaign ;; Military_operation
defense ;; Defending || combat ;; Hostile_encounter || confronted ;; Confronting_problem || flew ;; Self_motion
ended ;; Process_end || fighting ;; Hostile_encounter || signed ;; Sign_agreement || agreement ;; Sign_agreement
created ;; Creating || separate ;; Forming_relationships || allowed ;; Preventing_or_letting
signed ;; Sign_agreement || war ;; Hostile_encounter || conflict ;; Hostile_encounter || engaged ;; Participation
agreed ;; Agree_or_refuse_to_act || end ;; Process_end || met ;; Come_together || work ;; Being_in_operation
War ;; Hostile_encounter || destructive ;; Destroying || war ;; Hostile_encounter || death ;; Death || conflicts ;; Hostile_encounter
destruction ;; Destroying || killing ;; Killing || war ;; Hostile_encounter || command ;; Control || torture ;; Bodily_harm || starvation ;; Killing || incurred ;; Causation
became ;; Becoming
begun ;; Process_start || became ;; Becoming || rest ;; Placing || discussion ;; Communication || proposal ;; Statement || conference ;; Communication || conference ;; Communication
meetings ;; Social_event || ceremonies ;; Social_event
commitments ;; Commitment || instructed ;; Request || conference ;; Communication || attended ;; Participation
invited ;; Request || send ;; Sending
send ;; Sending
invited ;; Request || proposed ;; Statement || declined ;; Agree_or_refuse_to_act
sent ;; Sending || conference ;; Communication
proposed ;; Statement || agreed ;; Agree_or_refuse_to_act || decisions ;; Deciding || made ;; Manufacturing
issues ;; Creating || seemed ;; Change_sentiment || opposed ;; Agree_or_refuse_to_act || discussing ;; Communication || approved ;; Ratification || designed ;; Using
made ;; Manufacturing || conference ;; Communication || agreed ;; Agree_or_refuse_to_act || conference ;; Communication || proposal ;; Agree_or_refuse_to_act
took place ;; Process_start || Expedition ;; Self_motion
conquer ;; Conquering || sailed ;; Self_motion || landed ;; Motion || ruled ;; Control
aiming ;; Aiming || conquer ;; Conquering || venture ;; Self_motion
plebiscite ;; Ratification || conquest ;; Conquering || expedition ;; Self_motion || creation ;; Creating || concluded ;; Process_end || brought ;; Bringing
decided ;; Deciding || venture ;; Self_motion || action ;; Self_motion
utilized ;; Using || Expedition ;; Self_motion || influence ;; Influence || bolster ;; Cause_to_make_progress || unification ;; Cause_to_amalgamate || instigated ;; Process_start
participated ;; Participation || achieve ;; Achieve || end ;; Process_end || distribution ;; Dispersal || expedition ;; Self_motion || united ;; Cause_to_amalgamate
fought ;; Hostile_encounter
ended ;; Process_end || ratified ;; Ratification
ratified ;; Ratification || refused ;; Agree_or_refuse_to_act || repudiated ;; Agree_or_refuse_to_act || threatened ;; Warning
flee ;; Escaping || watch ;; Motion || negotiated ;; Communication
ratified ;; Ratification || negotiated ;; Communication
marked ;; Recording || stopped ;; Process_end || paying ;; Commerce_pay
built ;; Building || match ;; Competition
debates ;; Communication || Conference ;; Social_event || Conference ;; Social_event
sent ;; Sending
conference ;; Social_event || met ;; Come_together
existing ;; Presence || issued ;; Expressing_publicly || restated ;; Statement
went into effect ;; Influence || signatories ;; Sign_agreement || ratify ;; Ratification
pushed ;; Motion || enforced ;; Action || declaration ;; Expressing_publicly || war ;; Hostile_encounter
ended ;; Process_end || revolution ;; Change_of_leadership || carried out ;; Action || operation ;; Military_operation || deposed ;; Removing || elected ;; Choosing
installed ;; Change_of_leadership || dictatorship ;; Control
began ;; Process_start || toppled ;; Change_of_leadership
elected ;; Change_of_leadership || election ;; Choosing
turned ;; Becoming || introduced ;; Statement
reforms ;; Reforming_a_system || instituted ;; Action || granted ;; Giving
disliked ;; Change_sentiment || see ;; Perception_active
elected ;; Change_of_leadership || party ;; Social_event || legalized ;; Legality
engaged ;; Participation || influential ;; Influence || affected ;; Influence || practices ;; Practice || persuade ;; Convincing || overthrow ;; Change_of_leadership || business ;; Commerce_sell
topple ;; Change_of_leadership || aborted ;; Process_end || authorized ;; Ratification
elected ;; Change_of_leadership || promising ;; Commitment || links ;; Communication
drew ;; Motion || conclusions ;; Statement
authorized ;; Ratification || carry out ;; Action
armed ;; Bearing_arms || funded ;; Commerce_pay || trained ;; Education_teaching
preceded ;; Process_start || criticize ;; Judgment_communication
invaded ;; Attack || warfare ;; Hostile_encounter || backed ;; Self_motion || campaign ;; Military_operation
broadcast ;; Expressing_publicly || propaganda ;; Military_operation || rebellion ;; Hostile_encounter
invasion ;; Attack || defeated ;; Conquering
refused ;; Agree_or_refuse_to_act
resist ;; Defending || invasion ;; Attack || resigning ;; GiveUp
became ;; Becoming || negotiations ;; Communication
criticized ;; Judgment_communication || strengthened ;; Cause_change_of_strength
justify ;; Justifying || launched ;; Motion || sought ;; Motion || Operation ;; Military_operation
imprisoning ;; Arrest || torturing ;; Bodily_harm || reforms ;; Reforming_a_system || banning ;; Preventing_or_letting || reversing ;; Change || assumed ;; Choosing
fought ;; Hostile_encounter
competition ;; Competition || compete ;; Competition || approval ;; Ratification || sketching ;; Create_artwork || based ;; Causation
competitions ;; Competition || include ;; Cause_to_be_included || grown ;; Cause_to_make_progress
involves ;; Cause_to_be_included
determined ;; Deciding || match ;; Competition || change ;; Change
participating ;; Participation
mentions ;; Statement
put on ;; Presence || debuting ;; Process_start
took place ;; Hold
race ;; Competition
performance ;; Cause_to_make_progress || recognizing ;; Check || noted ;; Communication || given ;; Giving
reported ;; Reporting || support ;; Supporting
sending ;; Sending || ratified ;; Ratification || said ;; Statement
certified ;; Check || reject ;; Agree_or_refuse_to_act || indicated ;; Communication || record ;; Recording
reserved ;; Preserving || filled ;; Filling
marathon ;; Competition || focus ;; Aiming || said ;; Statement || recruiting ;; Becoming_a_member
announced ;; Expressing_publicly || appearance ;; Presence || participating ;; Participation
Battle ;; Hostile_encounter
led ;; Causation || authorized ;; Ratification
involved ;; Participation || called ;; Name_conferral
fought ;; Quarreling || capture ;; Conquering || kill ;; Killing
requested ;; Request || transferred ;; Giving || fought ;; Quarreling || began ;; Process_start
deposed ;; Change_of_leadership || fought ;; Hostile_encounter
conflict ;; Hostile_encounter || War ;; Military_operation || War ;; Military_operation || War ;; Military_operation || War ;; Military_operation || called ;; Name_conferral || known ;; Know || military ;; Military_operation
ended ;; Process_end || started ;; Process_start || conflict ;; Hostile_encounter || went into effect ;; Resolve_problem || blockade ;; Besieging
support ;; Supporting || war ;; Military_operation || consider ;; Coming_to_believe || conflict ;; Hostile_encounter || continuation ;; Expansion || conflict ;; Hostile_encounter || military ;; Military_operation
conflict ;; Hostile_encounter || raid ;; Military_operation
fired ;; Use_firearm || attack ;; Use_firearm || patrolling ;; Patrolling
ambush ;; Attack || left ;; Causation || dead ;; Death
taken ;; Conquering || abducted ;; Kidnapping
killed ;; Killing
demanded ;; Request || held ;; Hold || release ;; Releasing || exchange ;; Exchange || release ;; Releasing
refused ;; Agree_or_refuse_to_act || responded ;; Response || airstrikes ;; Attack || fire ;; Use_firearm
attacked ;; Attack
invasion ;; Attack || launched ;; Military_operation
blockade ;; Besieging || imposed ;; Action
warfare ;; Hostile_encounter || launched ;; Military_operation || engaged ;; Participation
killed ;; Killing || conflict ;; Hostile_encounter || believed ;; Statement
damaged ;; Damaging || displaced ;; Change
approved ;; Agree_or_refuse_to_act || end ;; Process_end || hostilities ;; Hostile_encounter
deployment ;; Arranging || resolution ;; Action || approved ;; Agree_or_refuse_to_act || called ;; Request || disarmament ;; Removing || enlarged ;; Cause_change_of_position_on_a_scale || withdrawal ;; Removing
given ;; Giving || mandate ;; Imposing_obligation || use ;; Using || ensure ;; Preventing_or_letting || used ;; Using || activities ;; Hostile_encounter || prevent ;; Preventing_or_letting || resist ;; Preventing_or_letting || discharging ;; Imposing_obligation
began ;; Process_start || deploying ;; Arranging
lifted ;; Process_end || blockade ;; Besieging
withdrew ;; Escaping || continued ;; Hold || occupy ;; Control
enactment ;; Ratification || stated ;; Statement || disarm ;; Escaping
captured ;; Conquering || remains ;; Presence || exchange ;; Exchange || returned ;; Arriving
driven ;; Self_motion || came to ;; Motion || Movement ;; Social_event || associated ;; Collaboration
service ;; Supply || agreement ;; Agree_or_refuse_to_act || passing ;; Motion_directional
argued ;; Quarreling || ratifying ;; Ratification || perceived ;; Know || increased ;; Cause_change_of_position_on_a_scale || provide ;; Supply || damage ;; Damaging || leave ;; Departing || worked out ;; Know || pull out ;; GiveUp || hurt ;; Bodily_harm
demanded ;; Request || reinstated ;; Recovering || passing ;; Motion_directional || allowing ;; Preventing_or_letting || close ;; Self_motion || discussing ;; Communication || agreement ;; Agree_or_refuse_to_act || changing ;; Change || rejection ;; Agree_or_refuse_to_act
open ;; Openness || returned ;; Arriving || rejected ;; Agree_or_refuse_to_act
saying ;; Communication || formed ;; Coming_to_be || joint ;; Collaboration || boycott ;; Agree_or_refuse_to_act
rejected ;; Agree_or_refuse_to_act
asked ;; Request || citing ;; Adducing
turned down ;; Agree_or_refuse_to_act
marked ;; Recording || occupied ;; Control
held ;; Hold || hosted ;; Participation
disagreement ;; Agree_or_refuse_to_act || held ;; Hold || held ;; Hold || removed ;; Removing || demanding ;; Request || segregation ;; Removing
change ;; Change || give ;; Giving || become ;; Becoming || attended ;; Participation || approved ;; Ratification
opposed ;; Agree_or_refuse_to_act
oppose ;; Agree_or_refuse_to_act
proposed ;; Creating || condemning ;; Judgment_communication || re-admitted ;; Judgment_communication || joined ;; Becoming_a_member
became ;; Becoming || left ;; Departing || rejected ;; Agree_or_refuse_to_act || withdrew ;; Escaping
expressed ;; Statement
expressed ;; Statement || disarmament ;; Removing
join ;; Becoming_a_member || approved ;; Agree_or_refuse_to_act
joined ;; Becoming_a_member
became ;; Becoming || accepted ;; Agree_or_refuse_to_act
began ;; Process_start || uprising ;; Change_of_leadership || Rebellion ;; Violence || protest ;; Self_motion || campaign ;; Motion
started ;; Process_start || protests ;; Self_motion || call ;; Request || led to ;; Causation || inspired ;; Influence
mobilized ;; Influence || Calling ;; Request || said ;; Statement
starting ;; Process_start || called ;; Request
gained ;; Getting || support ;; Supporting
legal ;; Legality || warned ;; Warning || participate ;; Participation || protests ;; Self_motion || promising ;; Commitment
protests ;; Self_motion || reported ;; Reporting || stepped up ;; Cause_change_of_strength || campaigns ;; Military_operation
called ;; Request
submitted ;; Submitting_documents || terrorist ;; Terrorism || calling ;; Request || banning ;; Preventing_or_letting || protests ;; Self_motion || stripping ;; Removing || convicted ;; Legal_rulings
endorsed ;; Ratification
asked ;; Request || warnings ;; Warning || protests ;; Self_motion || issued ;; Creating || carry out ;; Action
arrested ;; Arrest || prevented ;; Preventing_or_letting || entering ;; Arriving || deported ;; Removing || encircled ;; Besieging
arrests ;; Arrest || targeted ;; Aiming
gathered ;; Come_together || signatures ;; Sign_agreement || support ;; Supporting || protests ;; Self_motion || said ;; Statement
deployment ;; Arranging || gathered ;; Come_together || used ;; Using
response ;; Response || call ;; Request || strike ;; Attack
demonstrations ;; Motion || reported ;; Reporting
cutting ;; Removing || accused ;; Judgment_communication
provided ;; Supply || covering ;; Hiding_objects || protests ;; Self_motion || targeted ;; Aiming || blocked ;; Hindering
arrested ;; Arrest || injured ;; Bodily_harm || said ;; Statement
preventing ;; Preventing_or_letting || protests ;; Attack
dispersed ;; Dispersal || occurred ;; Coming_to_be || injuries ;; Bodily_harm
praised ;; Judgment_communication || protests ;; Self_motion || said ;; Statement
affect ;; Influence || protests ;; Self_motion || said ;; Statement
violence ;; Violence || supported ;; Supporting || voiced ;; Expressing_publicly || said ;; Statement
see ;; Perception_active || divided ;; Change
blaming ;; Judgment_communication || divided ;; Change
began ;; Process_start || published ;; Publishing || finalised ;; Process_end || participation ;; Participation || increase ;; Cause_change_of_position_on_a_scale || electoral ;; Change_of_leadership || reform ;; Reforming_a_system || Selecting ;; Choosing || elections ;; Choosing || broaden ;; Expansion
modifying ;; Change || electing ;; Choosing || ways ;; Action || electing ;; Choosing || decision ;; Deciding || Standing ;; Placing
attacked ;; Attack || proposals ;; Statement || authorised ;; Preventing_or_letting || rejected ;; Agree_or_refuse_to_act || said ;; Statement
consultations ;; Communication || precipitated ;; Causation || legislators ;; Participation || took place ;; Process_start
debate ;; Quarreling || attempts ;; Hold || secure ;; Preventing_or_letting || resulted in ;; Causation || renewing ;; Recovering || resulted in ;; Causation || ceased ;; Process_end
accepted ;; Receiving || party ;; Participation || concerning ;; Causation || opposed ;; Agree_or_refuse_to_act
voted ;; Choosing || party ;; Participation || party ;; Participation || block ;; Hindering || amidst ;; Surrounding
decision ;; Deciding || ratified ;; Ratification || assented ;; Ratification
fought ;; Hostile_encounter || War ;; Hostile_encounter
see ;; Perception_active
enforced ;; Action || outbreak ;; Process_start || choke ;; Self_motion
pressed ;; Motion
incidents ;; Catastrophe || grew ;; Expansion || happened ;; Presence
died ;; Death
raided ;; Attack || supplied ;; Supply || hindering ;; Hindering || provoking ;; Causation
debate ;; Communication || contributed ;; Giving || annex ;; Attack || go to ;; Motion
expansion ;; Expansion || supported ;; Supporting || threatened ;; Warning
pressure ;; Convincing || signed ;; Sign_agreement
limited ;; Limiting || fighting ;; Hostile_encounter || adopted ;; Choosing
suffered ;; Bodily_harm || prosecution ;; Legality
defeats ;; Conquering || seize ;; Conquering
invade ;; Attack || capture ;; Conquering || failed ;; Agree_or_refuse_to_act
gaining ;; Getting || defeated ;; Conquering
fought ;; Quarreling || made ;; Manufacturing
cutting off ;; Bodily_harm || raid ;; Attack || blockaded ;; Besieging || allowing ;; Preventing_or_letting
invade ;; Attack || burned ;; Bodily_harm || repulsed ;; Defending
attacking ;; Attack || defeated ;; Conquering
ended ;; Process_end || took place ;; Process_start
demanded ;; Request || reopen ;; Openness
ended ;; Process_end || rendering ;; Causation || issue ;; Creating || abdication ;; Change_of_leadership
began ;; Process_start || increase ;; Cause_change_of_position_on_a_scale
began ;; Process_start || negotiations ;; Communication || treaty ;; Ratification || signed on ;; Sign_agreement
reach ;; Arriving
reached ;; Arriving
beginning ;; Process_start || celebrated ;; Social_event
ending ;; Process_end || ratified ;; Ratification
took place ;; Process_start
referred ;; Name_conferral || clear ;; Emptying
aimed ;; Aiming || flushing ;; Emptying
given ;; Giving
infiltration ;; Attack || disguised ;; Wearing
blamed ;; Judgment_communication
supported ;; Supporting || recaptured ;; Conquering
withdrew ;; Escaping || Facing ;; Confronting_problem
conducted ;; Action || conducted ;; Action || developing ;; Cause_to_make_progress
earthquake ;; Catastrophe || struck ;; Bodily_harm
earthquake ;; Catastrophe || originate ;; Presence
epicenter ;; Process_start
earthquake ;; Catastrophe
caused ;; Causation || damage ;; Damaging
earthquake ;; Catastrophe || reached ;; Arriving
poses ;; Causation
earthquake ;; Catastrophe || occur ;; Coming_to_be
earthquake ;; Catastrophe || remains ;; Presence
shot ;; Use_firearm || Massacre ;; Killing || march ;; Social_event || incident ;; Attack
killed ;; Killing || injuries ;; Bodily_harm || died ;; Death || death ;; Death || attributed ;; Causation
fleeing ;; Escaping || help ;; Assistance || shot ;; Use_firearm || shot ;; Use_firearm
injured ;; Bodily_harm || run ;; Self_motion
shot ;; Use_firearm
organised ;; Arranging || march ;; Social_event
involved ;; Cause_to_be_included || killing ;; Killing || shootings ;; Use_firearm
investigations ;; Criminal_investigation || held ;; Giving
cleared ;; Removing
described ;; Statement || shooting ;; Use_firearm || claims ;; Statement || accepted ;; Agree_or_refuse_to_act
criticised ;; Judgment_communication
incident ;; Catastrophe || Inquiry ;; Criminal_investigation || established ;; Creating
concluded ;; Coming_to_believe || inquiry ;; Criminal_investigation || report ;; Reporting || killings ;; Killing
found ;; Know || shot ;; Use_firearm || firing ;; Use_firearm || posing ;; Causation || put ;; Placing || thrown ;; Self_motion
shooting ;; Use_firearm || shooting ;; Use_firearm || denied ;; Response || denied ;; Response
publication ;; Publishing || made ;; Giving
began ;; Process_start || investigation ;; Criminal_investigation || killings ;; Killing
killed ;; Killing
killed ;; Killing || incident ;; Catastrophe || conflict ;; Violence
conflict ;; Violence || fueled ;; Cause_change_of_position_on_a_scale || worsened ;; Cause_change_of_position_on_a_scale
Support ;; Supporting || recruitment ;; Employment || rose ;; Expansion
killing ;; Killing || broke apart ;; Destroying
disintegrated ;; Destroying
began ;; Process_start || joint ;; Collaboration || disintegration ;; Dispersal || liftoff ;; Motion_directional
caused ;; Causation || joint ;; Collaboration || designed ;; Create_artwork || existed ;; Presence
caused ;; Causation || allowing ;; Preventing_or_letting || reach ;; Arriving || joint ;; Collaboration
separation ;; Forming_relationships || joint ;; Collaboration || led to ;; Causation
broke up ;; Destroying
search ;; Scrutiny || recovered ;; Recovering
death ;; Death || known ;; Know
escape ;; Escaping || impact ;; Influence
investigate ;; Research || appointed ;; Choosing || resulted in ;; Change
found ;; Know || contributing ;; Giving
contained ;; Containing || known ;; Know || design ;; Create_artwork || failed ;; Earnings_and_losses
warnings ;; Warning || launching ;; Process_start || posed ;; Causation || report ;; Reporting || disregarded ;; Response
reported ;; Reporting || surveyed ;; Scrutiny
used ;; Using || discussions ;; Communication
caused ;; Causation
developed ;; Coming_to_be || named ;; Name_conferral || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe
reaching ;; Arriving || hurricane ;; Catastrophe || intensification ;; Cause_to_make_progress
began ;; Process_start || deepen ;; Cause_change_of_strength || curving ;; Motion_directional
hurricane ;; Catastrophe
weakened ;; Cause_change_of_strength || hurricane ;; Catastrophe
passed ;; Motion_directional
posed ;; Causation || curved ;; Motion_directional || avoiding ;; Preventing_or_letting || remaining ;; Presence
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || threatened ;; Warning || turned ;; Motion
passed ;; Motion_directional || transitioned ;; Change || storm ;; Catastrophe || accelerated ;; Cause_change_of_strength
caused ;; Causation || flooding ;; Motion
winds ;; Self_motion || downed ;; Motion
surf ;; Self_motion || damaged ;; Damaging
postponed ;; Change_event_time || independence ;; Change_of_leadership || passage ;; Self_motion
storm ;; Catastrophe || affected ;; Influence || waves ;; Self_motion || produced ;; Creating
washed ;; Removing || capsized ;; Destroying
damage ;; Damaging || passing ;; Motion_directional || produced ;; Creating
caused ;; Causation || deaths ;; Death || due ;; Causation || drowning ;; Death
storm ;; Catastrophe || damage ;; Damaging || losses ;; Earnings_and_losses || produced ;; Creating
record ;; Recording || noted ;; Statement
moved ;; Motion || develop ;; Coming_to_be || started ;; Process_start || remained ;; Presence
declared ;; Expressing_publicly || named ;; Name_conferral || intensified ;; Cause_change_of_strength
moved ;; Motion || intensify ;; Cause_change_of_strength || reached ;; Achieve
moving ;; Motion || posed ;; Causation || recurved ;; Motion || hit ;; Attack
started ;; Process_start || starting ;; Process_start || cause ;; Causation || accelerate ;; Cause_change_of_strength || hit ;; Attack || passed ;; Motion_directional
move ;; Motion || started ;; Process_start || starting ;; Process_start || interact ;; Connect
monitored ;; Scrutiny || bringing ;; Bringing || combined ;; Cause_to_amalgamate || noted ;; Statement
hits ;; Bodily_harm || caused ;; Causation || associated ;; Collaboration || reported ;; Reporting
gathering ;; Come_together || destroyed ;; Destroying || died ;; Death || included ;; Cause_to_be_included || hit ;; Attack || secure ;; Preventing_or_letting
War ;; Hostile_encounter
became ;; Becoming || losses ;; Earnings_and_losses || alarmed ;; Warning || posed ;; Causation
entering ;; Arriving || war ;; Hostile_encounter || attacked ;; Attack
took ;; Conquering || seized ;; Hold
forced ;; Motion || defeated ;; Conquering || inflicted ;; Bodily_harm || invasions ;; Attack
taking ;; Conquering || changed ;; Exchange
handed over ;; Giving || withdrawal ;; Escaping || returned ;; Arriving
losses ;; Earnings_and_losses || ceded ;; Surrendering
conducted ;; Action || attack ;; Attack
carry out ;; Action || attack ;; Attack || designed ;; Creating
response ;; Communication || evaluating ;; Check || focused ;; Aiming || use ;; Using
establish ;; Creating || posed ;; Causation || increasing ;; Cause_change_of_position_on_a_scale
designed ;; Creating
contingency ;; Warning || create ;; Creating
emerges ;; Coming_to_be || keep ;; Preventing_or_letting
result in ;; Cause_change_of_position_on_a_scale
result in ;; Causation || emerge ;; Coming_to_be
providing ;; Supply
set out ;; Military_operation || establish ;; Building || ordered ;; Request
strike ;; Attack || expand ;; Expansion || threat ;; Warning
defeating ;; Conquering || succeeded ;; Conquering || driving ;; Hold || seizing ;; Hold
defeat ;; Conquering || flee ;; Escaping || forced ;; Hostile_encounter
expansion ;; Expansion || posed ;; Causation || threat ;; Warning || led to ;; Causation
become ;; Becoming || secure ;; Preventing_or_letting || instructed ;; Request
began ;; Process_start || connected ;; Connect || stationed ;; Placing || defenses ;; Defending
conquered ;; Conquering
killed ;; Killing || fighting ;; Hostile_encounter || collecting ;; Come_together || explosives ;; Attack || distribution ;; Dispersal || suicide bomber ;; Terrorism || detonated ;; Attack || injured ;; Bodily_harm
dressed ;; Wearing || detonate ;; Attack || resisting ;; Defending || grenade ;; Attack || explosives ;; Attack || reported ;; Reporting || thrown ;; Motion || search ;; Scouring || proceeded ;; Self_motion
confirmed ;; Check || wearing ;; Wearing || conduct ;; Action || suicide attack ;; Terrorism || occurrence ;; Presence || opposed ;; Agree_or_refuse_to_act || disguise ;; Wearing
supported ;; Supporting || known ;; Know || operations ;; Military_operation
suicide attack ;; Terrorism || injured ;; Bodily_harm || taken ;; Bringing
claimed ;; Statement
death ;; Death || rise ;; Cause_change_of_position_on_a_scale || stated ;; Statement || suicide attack ;; Terrorism || injured ;; Bodily_harm
bombing ;; Attack || suicide attacks ;; Terrorism || occurred ;; Presence
claimed ;; Statement || suicide attack ;; Terrorism || suspects ;; Committing_crime
suicide attacks ;; Terrorism || impunity ;; Rewards_and_punishments || operations ;; Military_operation || seen ;; Perception_active
suicide attack ;; Terrorism || claimed ;; Statement || condemned ;; Judgment_communication || offensives ;; Attack
statement ;; Statement || approval ;; Preventing_or_letting || destroy ;; Destroying || pressured ;; Influence || launch ;; Process_start
suicide attack ;; Terrorism || distribution ;; Supply || suspended ;; Change_event_time
suicide bombing ;; Terrorism || condemned ;; Judgment_communication
claimed ;; Statement || suicide attack ;; Terrorism
declared ;; Expressing_publicly || suicide bombing ;; Terrorism || suicide attack ;; Terrorism || involve ;; Cause_to_be_included
internment ;; Arrest || incarceration ;; Arrest || War ;; Hostile_encounter || relocation ;; Removing
internees ;; Arrest
ordered ;; Arranging || attack ;; Attack
attack ;; Attack
incarcerated ;; Arrest
forced ;; Request
composed ;; Come_together || interned ;; Arrest
posed ;; Causation || internment ;; Arrest || resulted ;; Causation
internment ;; Arrest || drop ;; Motion_directional || placed ;; Placing || internment ;; Arrest
authorized ;; Preventing_or_letting || allowed ;; Preventing_or_letting || incarceration ;; Arrest || deportation ;; Removing || issued ;; Statement || designate ;; Choosing || excluded ;; Preventing_or_letting
used ;; Using || declare ;; Expressing_publicly || required ;; Imposing_obligation || leave ;; Departing
relocated ;; Removing || arrested ;; Arrest || attack ;; Attack || custody ;; Arrest
relocated ;; Removing
assisted ;; Assistance || spying ;; Committing_crime || providing ;; Supply || internment ;; Arrest
became ;; Becoming || denied ;; Statement
ruling ;; Legal_rulings || order ;; Arranging || removal ;; Removing || appeal ;; Protest || violating ;; Preventing_or_letting || exclusion ;; Removing
limited ;; Limiting || orders ;; Arranging || avoiding ;; Preventing_or_letting || due ;; Causation || incarceration ;; Arrest || decision ;; Deciding || exclusion ;; Removing
justified ;; Justifying || put ;; Placing || pressure ;; Influence || investigation ;; Criminal_investigation || decision ;; Deciding || opened ;; Process_start || determine ;; Deciding
internment ;; Arrest || appointed ;; Choosing || investigate ;; Criminal_investigation
found ;; Know || product ;; Manufacturing || incarceration ;; Arrest || titled ;; Name_conferral || report ;; Reporting || concluded ;; Coming_to_believe
recommended ;; Convincing || pay ;; Commerce_pay
signed ;; Sign_agreement || authorized ;; Preventing_or_letting || payment ;; Commerce_pay || internment ;; Arrest || apologized ;; Judgment_communication
race ;; Competition || war ;; Hostile_encounter || admitted ;; Statement
interned ;; Arrest
contributed ;; Giving || flooding ;; Motion || affected ;; Influence
formed ;; Creating
moved ;; Motion || strengthening ;; Cause_change_of_strength
posed ;; Causation || passing ;; Motion_directional
died ;; Death || hurricane ;; Catastrophe || destroyed ;; Destroying
produced ;; Manufacturing || reaching ;; Arriving
caused ;; Causation || destroyed ;; Destroying || damage ;; Damaging
affecting ;; Influence || reached ;; Arriving || observed ;; Perception_active
weakened ;; Cause_change_of_strength || slowed ;; Change_event_time || turned ;; Becoming || hurricane ;; Catastrophe || striking ;; Attack
damaging ;; Damaging || hurricane ;; Catastrophe
moving ;; Motion || storm ;; Catastrophe || progressed ;; Cause_to_make_progress
warnings ;; Warning || spurring ;; Influence || evacuations ;; Escaping || storm ;; Catastrophe || issued ;; Creating || widespread ;; Dispersal
produced ;; Manufacturing || moved ;; Motion || causing ;; Causation || damage ;; Damaging || deaths ;; Death
caused ;; Causation || killed ;; Killing
killing ;; Killing || change ;; Change || prompting ;; Causation
deaths ;; Death || deaths ;; Death || record ;; Recording || flooded ;; Motion
storm ;; Catastrophe || lost ;; Earnings_and_losses
Damage ;; Damaging
contributed ;; Giving || caused ;; Causation || flooding ;; Motion || damage ;; Damaging
killed ;; Killing || destroyed ;; Destroying
impacts ;; Influence || death ;; Death || hurricane ;; Catastrophe || naming ;; Name_conferral
competition ;; Competition || held ;; Hold
competition ;; Competition
defended ;; Defending || tournament ;; Competition || due ;; Causation || defeated ;; Conquering || dismissed ;; Response
tournament ;; Competition || matches ;; Competition || saw ;; Becoming || finish ;; Process_end || overhaul ;; Reforming_a_system || played ;; Competition
earn ;; Earnings_and_losses || elimination ;; Removing || losing ;; Earnings_and_losses || defeated ;; Conquering || final ;; Competition
finishing ;; Process_end || tournament ;; Competition
tournament ;; Competition || final ;; Process_end || due ;; Causation || blowing ;; Motion || scored ;; Getting || defeated ;; Conquering
tournament ;; Competition
agreed ;; Agree_or_refuse_to_act || play ;; Competition
looked ;; Know || bowl ;; Body_movement
selected ;; Choosing || coached ;; Education_teaching
Champions ;; Competition
hosting ;; Social_event || face ;; Confronting_problem || tournament ;; Competition || bullish ;; Cause_change_of_position_on_a_scale || losses ;; Earnings_and_losses || saying ;; Statement || making it ;; Causation
scored ;; Getting
award ;; Award
declared ;; Expressing_publicly
pose ;; Causation || tournament ;; Competition || warning ;; Warning || play ;; Participation || rest ;; Action || test ;; Testing
affected ;; Influence
observed ;; Perception_active
intensifying ;; Cause_change_of_strength || passing ;; Motion_directional || formed ;; Coming_to_be || posing ;; Causation || passed ;; Motion_directional
moved ;; Motion || hitting ;; Attack
observed ;; Perception_active || threaten ;; Warning
discovered ;; Know || flood ;; Catastrophe || believed ;; Know || swept ;; Scouring
launched ;; Process_start || Tour ;; Traveling || tour ;; Traveling
designed ;; Creating || tour ;; Traveling
sponsored ;; Supply
ended ;; Process_end || tour ;; Traveling || divided ;; Creating
accused ;; Judgment_communication || denied ;; Statement || synching ;; Wearing || tour ;; Traveling
recorded ;; Recording || broadcast ;; Expressing_publicly
entitled ;; Name_conferral
released ;; Publishing
tour ;; Traveling
tour ;; Traveling || ended ;; Process_end
tour ;; Traveling || flying ;; Motion
float ;; Motion
floating ;; Motion || floating ;; Motion || aim ;; Aiming || blow it ;; Damaging || appear ;; Presence || shoot ;; Use_firearm
involved ;; Cause_to_be_included || seen ;; Perception_active || mock ;; Wearing || shooting ;; Use_firearm || designed ;; Creating || putting on ;; Wearing
involved ;; Cause_to_be_included || citing ;; Adducing
dubbed ;; Name_conferral || tour ;; Traveling || decline ;; Motion_directional
cancelled ;; Change_event_time
tour ;; Traveling || confirmed ;; Check
stop ;; Process_end
performed ;; Hold || performed ;; Hold || performed ;; Hold || performed ;; Hold
tour ;; Traveling || replaced ;; Change
performed ;; Hold || rehearsed ;; Practice
stated ;; Statement || becoming ;; Becoming
dressed ;; Wearing
turned off ;; Agree_or_refuse_to_act || result ;; Causation || alienated ;; GiveUp
tour ;; Traveling || put ;; Placing
bombing ;; Attack || Fall ;; Motion_directional || occurred ;; Presence
bombing ;; Attack || carried out ;; Action || captured ;; Conquering || using ;; Using || flown ;; Motion || bombed ;; Attack
concluded ;; Statement
curtailed ;; Cause_change_of_position_on_a_scale || due ;; Causation
posed ;; Causation || deployed ;; Arranging || prevented ;; Preventing_or_letting || support ;; Supporting || reconnaissance ;; Scrutiny
captured ;; Conquering || renewed ;; Recovering || swept ;; Scrutiny || handicapped ;; Hindering || lost ;; Earnings_and_losses
initiated ;; Process_start
usurping ;; Change_of_leadership || holding ;; Defending || claimed ;; Statement || establishing ;; Creating
remove ;; Removing || justified ;; Justifying
shifting ;; Motion || led to ;; Causation
end ;; Process_end
incidents ;; Catastrophe || slaying ;; Killing
reenacted ;; Wearing
killed ;; Killing || battle ;; Hostile_encounter
fighting ;; Hostile_encounter || defeated ;; Conquering || battle ;; Hostile_encounter
developed ;; Coming_to_be || hurricane ;; Catastrophe || storm ;; Catastrophe
affected ;; Influence || storm ;; Catastrophe || weakening ;; Cause_change_of_strength || moving ;; Motion || reaching ;; Arriving || hurricane ;; Catastrophe
hurricane ;; Catastrophe || storm ;; Catastrophe || moved ;; Self_motion
re-intensified ;; Cause_change_of_strength || turning ;; Change || hurricane ;; Catastrophe || landfall ;; Motion
became ;; Becoming || entering ;; Arriving
spread ;; Dispersal || dissipating ;; Dispersal
attributed ;; Causation || deaths ;; Death || dumped ;; Bringing
affected ;; Influence || posed ;; Causation || moving ;; Self_motion
Damage ;; Damaging || prompting ;; Causation
damage ;; Damaging
named ;; Name_conferral || Hurricane ;; Catastrophe || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
formed ;; Coming_to_be || reached ;; Arriving || storm ;; Catastrophe
bringing ;; Bringing || producing ;; Creating || damage ;; Damaging || hurricane ;; Catastrophe || hurricane ;; Catastrophe || brushed ;; Damaging || turning ;; Motion || striking ;; Attack
making ;; Manufacturing || landfall ;; Catastrophe
posed ;; Causation || evacuations ;; Escaping || warning ;; Warning || occurred ;; Coming_to_be || hurricane ;; Catastrophe || issued ;; Creating || forecast ;; Reporting
discontinued ;; Process_end || warnings ;; Warning || turned ;; Motion
upgraded ;; Cause_to_make_progress || warning ;; Warning || approached ;; Arriving || hurricane ;; Catastrophe || hurricane ;; Catastrophe || issued ;; Creating
discontinued ;; Process_end || warning ;; Warning || hurricane ;; Catastrophe || tracked ;; Recording
warning ;; Warning || hurricane ;; Catastrophe || effect ;; Influence
reported ;; Reporting || storm ;; Catastrophe || injuries ;; Bodily_harm
dropped ;; Motion
invasion ;; Attack || planned ;; Arranging || planned ;; Arranging || War ;; Military_operation || operations ;; Military_operation || swept ;; Body_movement
limited ;; Limiting || fortified ;; Cause_change_of_strength || campaign ;; Military_operation || success ;; Earnings_and_losses || raiding ;; Attack || failed ;; Earnings_and_losses
plan ;; Arranging || operations ;; Military_operation
campaign ;; Military_operation
recapture ;; Conquering || attack ;; Attack || spring ;; Self_motion || prepared ;; GetReady
allied ;; Collaboration || gained ;; Getting || control ;; Control || war ;; Military_operation || joined ;; Participation
planned ;; Arranging
directed ;; Aiming
taking ;; Conquering || joined ;; Becoming_a_member || descended ;; Motion_directional || captured ;; Conquering || proceed ;; Motion
monitor ;; Scrutiny || lead ;; Bringing
plan ;; Arranging || proved ;; Convincing
captured ;; Conquering || captured ;; Conquering || posed ;; Causation
repulsed ;; Defending || expedition ;; Attack
withdrew ;; Escaping || approached ;; Arriving || forces ;; Causation
signed ;; Sign_agreement || treaty ;; Sign_agreement || called ;; Name_conferral
allowing ;; Preventing_or_letting || choose ;; Choosing || struggle ;; Hostile_encounter || ended ;; Process_end || made ;; Creating
credited ;; Justifying || ending ;; Process_end
chose ;; Choosing || increased ;; Cause_change_of_position_on_a_scale || practice ;; Practice || practiced ;; Practice || endorsed ;; Agree_or_refuse_to_act
allowed ;; Preventing_or_letting
Battle ;; Hostile_encounter || took place ;; Process_start || War ;; Hostile_encounter || War ;; Hostile_encounter
Battle ;; Hostile_encounter || called ;; Name_conferral
battle ;; Hostile_encounter || defeated ;; Conquering || augmented ;; Cause_to_amalgamate
included ;; Cause_to_be_included || opposing ;; Agree_or_refuse_to_act
achieve ;; Achieve || complete ;; Process_end || employed ;; Employment || order ;; Request
refused ;; Agree_or_refuse_to_act || send ;; Sending || Battle ;; Hostile_encounter || increased ;; Cause_change_of_position_on_a_scale || marked ;; Recording || support ;; Supporting || success ;; Achieve || War ;; Hostile_encounter || victory ;; Getting
battle ;; Hostile_encounter || left ;; Departing || marched ;; Self_motion
met ;; Come_together || Battle ;; Hostile_encounter || employed ;; Employment || defeat ;; Conquering
masterpieces ;; Achieve
casualties ;; Catastrophe || suffering ;; Catastrophe || crippled ;; Destroying
responding ;; Response || played ;; Bringing || changing ;; Change
justifying ;; Justifying || battle ;; Hostile_encounter || War ;; Hostile_encounter || War ;; Hostile_encounter || conclusion ;; Coming_to_believe || contributed ;; Giving
Conference ;; Social_event || met ;; Social_event || attended ;; Participation
determining ;; Deciding || rights ;; Legality
surveyed ;; Research || dealt ;; Resolve_problem || allowing ;; Preventing_or_letting || speak ;; Expressing_publicly || conference ;; Social_event || discussion ;; Communication || held ;; Hold || resolution ;; Resolve_problem || feel ;; Change_sentiment
conference ;; Social_event || occurred ;; Presence
Crisis ;; Hostile_encounter || conflict ;; Hostile_encounter
declare ;; Expressing_publicly || crisis ;; Hostile_encounter || issued ;; Statement
disavowed ;; Agree_or_refuse_to_act || declare ;; Expressing_publicly || referred ;; Adducing || declared ;; Expressing_publicly
support ;; Supporting
declared ;; Expressing_publicly || negotiated ;; Communication || participate ;; Participation
negotiated ;; Communication || sign on ;; Sign_agreement || involving ;; Cause_to_be_included || allowing ;; Preventing_or_letting
conduct ;; Action || practice ;; Practice || signing ;; Sign_agreement || departure ;; Departing
adopt ;; Hold
allowing ;; Preventing_or_letting || make ;; Action || argued ;; Quarreling
negotiate ;; Communication || sign ;; Sign_agreement || affirmed ;; Justifying || pursue ;; Aiming
obliged ;; Imposing_obligation || avoid ;; Preventing_or_letting || injure ;; Bodily_harm || taking ;; Action || commit ;; Imposing_obligation || recognised ;; Know
affirmed ;; Judgment_communication
lobbied ;; Convincing || arrangements ;; Arranging || make ;; Change || changes ;; Change || preference ;; Choosing
arrangements ;; Arranging || demands ;; Request || argued ;; Protest || production ;; Creating || filled ;; Filling
scheme ;; Arranging || introduce ;; Creating || election ;; Choosing
repudiated ;; Judgment_communication
founded ;; Creating || research ;; Research || conference ;; Social_event || trade ;; Exchange || restructured ;; Reforming_a_system || promoting ;; Cause_to_make_progress || coordinate ;; Control
started ;; Process_start || conflict ;; Hostile_encounter
fought ;; Hostile_encounter
war ;; Hostile_encounter || controlled ;; Control || support ;; Supporting || support ;; Supporting
formed ;; Coming_to_be || initiated ;; Process_start || attacks ;; Attack || fight ;; Hostile_encounter
claimed ;; Statement
acquired ;; Getting || looted ;; Robbery || rebellion ;; Attack
began ;; Process_start || attacks ;; Attack || resulted in ;; Causation || killed ;; Killing
justifying ;; Justifying
precipitated ;; Change || fight ;; Hostile_encounter
recovered ;; Recovering || remove ;; Removing || administered ;; Control || found ;; Know || investigations ;; Scrutiny
ended ;; Process_end || agreeing ;; Agree_or_refuse_to_act || war ;; Hostile_encounter || withdraw ;; Escaping
form ;; Coming_to_be || fight ;; Hostile_encounter || joining ;; Becoming_a_member
documented ;; Recording || killed ;; Killing || war ;; Hostile_encounter || compiled ;; Writing
caused ;; Causation
fled ;; Escaping || war ;; Hostile_encounter || abuse ;; Violence
became ;; Becoming
caused ;; Causation || gain ;; Getting || bombing ;; Attack || approval ;; Preventing_or_letting || deaths ;; Death
administered ;; Control
attend ;; Participation
receive ;; Receiving || found ;; Know
linked ;; Connect
include ;; Cause_to_be_included
assist ;; Assistance || tours ;; Traveling
built ;; Building
killed ;; Killing || attended ;; Participation
renamed ;; Name_conferral
awarded ;; Award
promoting ;; Cause_change_of_position_on_a_scale || recognize ;; Justifying || demonstrated ;; Coming_to_be || awarded ;; Award
occurred ;; Coming_to_be || invitation ;; Social_event
sparked ;; Causation || represented ;; Presence
give ;; Giving
demonstrated ;; Protest || defended ;; Justifying || stating ;; Statement || lead ;; Causation || join ;; Becoming_a_member
extended ;; Cause_change_of_strength
made ;; Creating || blocked ;; Hindering
aired ;; Expressing_publicly || appeared ;; Presence
took place ;; Process_start
watched ;; Perception_active
encompassed ;; Surrounding || conflict ;; Hostile_encounter
called ;; Name_conferral
battles ;; Hostile_encounter
destruction ;; Destroying || loss ;; Earnings_and_losses || characterized ;; Justifying
death ;; Death
occurred ;; Presence || attributed ;; Causation
determining ;; Deciding || serving ;; Supply || defeat ;; Conquering
provided ;; Supply || aid ;; Assistance || engaged ;; Participation
considered ;; Statement
considered ;; Statement
enforcing ;; Action || provide ;; Supply || charged ;; Imposing_obligation
began ;; Process_start
provided ;; Supply || support ;; Supporting
included ;; Cause_to_be_included
allowed ;; Preventing_or_letting
approved ;; Agree_or_refuse_to_act || become ;; Becoming || indicated ;; Communication
combat ;; Hostile_encounter
attack ;; Attack || announced ;; Expressing_publicly || recognize ;; Justifying
fired ;; Use_firearm || patrolling ;; Patrolling
destroyed ;; Destroying || fired ;; Use_firearm || response ;; Response || launched ;; Military_operation || dropped ;; Attack
came ;; Motion
using ;; Using || responded ;; Response || bombing ;; Attack
conducted ;; Action || combat ;; Hostile_encounter || use ;; Using
patrols ;; Patrolling || flew ;; Self_motion || fired ;; Use_firearm
offering ;; Supply || shot down ;; Use_firearm || reward ;; Rewards_and_punishments
conflict ;; Hostile_encounter || response ;; Response || declined ;; Cause_change_of_position_on_a_scale || invasion ;; Attack
occurred ;; Coming_to_be
concluded ;; Process_end
flown ;; Self_motion || deployed ;; Arranging
combat ;; Hostile_encounter
committed ;; Commitment || invasion ;; Attack || occupation ;; Control || murder ;; Killing
genocides ;; Killing || justified ;; Justifying || depicted ;; Create_artwork || threat ;; Warning
kill ;; Killing || plan ;; Arranging || plans ;; Arranging || developed ;; Creating || eliminate ;; Cause_change_of_position_on_a_scale || murder ;; Killing || deemed ;; Coming_to_believe
forced ;; Hostile_encounter || murdered ;; Killing || cleansed ;; Emptying
genocides ;; Killing || claimed ;; Statement
death ;; Death || deaths ;; Death || directed ;; Aiming || absence ;; Presence
committed ;; Commitment || plan ;; Arranging || war ;; Hostile_encounter || crimes ;; Committing_crime || crimes ;; Committing_crime
turned ;; Becoming || plan ;; Arranging || expulsion ;; Removing || remaining ;; Presence
crimes ;; Committing_crime || established ;; Building
start ;; Process_start || acquire ;; Getting || war ;; Hostile_encounter || plan ;; Arranging || realize ;; Know || set out ;; Creating
combined ;; Cause_to_amalgamate || plan ;; Arranging
gave ;; Giving || invasion ;; Attack || kill ;; Killing
conducted ;; Action
stated ;; Statement || killed ;; Killing
added ;; Cause_to_be_included
stated ;; Statement || complex ;; Cause_to_amalgamate
consider ;; Deciding || elimination ;; Removing
confirmed ;; Check || end ;; Process_end || plan ;; Arranging
lost ;; Earnings_and_losses || war ;; Hostile_encounter || concluded ;; Process_end || aim ;; Aiming || extermination ;; Killing
obtained ;; Getting || raids ;; Attack || fought ;; Hostile_encounter || gain ;; Getting || engaged ;; Participation
altered ;; Change || resulted in ;; Causation || deaths ;; Killing || enslavement ;; Bodily_harm || imposition ;; Rewards_and_punishments
wars ;; Hostile_encounter || fatal ;; Killing || impact ;; Influence || contact ;; Communication || seen ;; Perception_active
changes ;; Change || benefited ;; Earnings_and_losses || warfare ;; Hostile_encounter || use ;; Using || led to ;; Causation || design ;; Being_in_operation || engaged ;; Participation
devastating ;; Destroying || raids ;; Attack || used ;; Using || relying ;; Supply
raids ;; Attack || dressing ;; Wearing || work ;; Being_in_operation || trade ;; Exchange || launched ;; Military_operation || taking ;; Conquering || put ;; Placing || cultivating ;; Cause_to_make_progress
violence ;; Violence || spread ;; Dispersal || defence ;; Defending || peaked ;; Self_motion || procure ;; Getting || mount ;; Placing || prompted ;; Preventing_or_letting
devastating ;; Destroying || fighting ;; Hostile_encounter || raids ;; Attack || went ;; Motion || launched ;; Military_operation
applied ;; Using || wars ;; Hostile_encounter || destructive ;; Destroying || conflict ;; Hostile_encounter || widespread ;; Dispersal || use ;; Using || continuation ;; Expansion || suggested ;; Convincing || questioned ;; Justifying || suggesting ;; Convincing
targets ;; Aiming || bombings ;; Terrorism
issue ;; Resolve_problem || bombings ;; Terrorism
found ;; Know || guilty ;; Committing_crime || death ;; Death || bombings ;; Terrorism || sentenced ;; Legal_rulings
given ;; Giving || prison ;; Prison || imprisonment ;; Prison || sentenced ;; Legal_rulings
lived ;; Connect || movement ;; Participation || blamed ;; Judgment_communication
carried out ;; Action || viewed ;; Change_sentiment || attacks ;; Attack
impression ;; Change_sentiment || knew ;; Coming_to_believe || attack ;; Attack || give ;; Communication
denied ;; Agree_or_refuse_to_act
immigrate ;; Escaping || bombings ;; Terrorism || movement ;; Participation || suggest ;; Justifying || encourage ;; Convincing || assign ;; Telling
immigrate ;; Escaping || left ;; Departing || bombings ;; Terrorism || bombs ;; Terrorism || events ;; Participation || raised ;; Influence || monitoring ;; Scrutiny || mention ;; Adducing || mentioned ;; Adducing || registered ;; Recording
immigration ;; Escaping || given ;; Communication || claimed ;; Statement || accelerate ;; Change_event_time || struggling ;; Resolve_problem || cope ;; Confronting_problem || evacuation ;; Escaping || raised ;; Cause_change_of_position_on_a_scale || raised ;; Cause_change_of_position_on_a_scale || taken ;; Action
payment ;; Cost
marketing ;; Commerce_sell
paid ;; Commerce_pay || operating ;; Using || export ;; Supply
found ;; Know || exchange ;; Exchange || paying ;; Commerce_pay
contradiction ;; Justifying
delivered ;; Sending || questioned ;; Criminal_investigation
found ;; Know
launched ;; Process_start || recommended ;; Convincing
dropped ;; Removing
cases ;; Scrutiny
discovered ;; Know || losing ;; Earnings_and_losses || payments ;; Cost || restructuring ;; Reforming_a_system || exports ;; Supply || appointing ;; Change_of_leadership
suffer ;; Earnings_and_losses
earned ;; Earnings_and_losses || implicated ;; Check
battle ;; Hostile_encounter || Battle ;; Hostile_encounter
took place ;; Process_start || ended ;; Process_end
battle ;; Hostile_encounter || abridged ;; Cause_change_of_position_on_a_scale
withdraw ;; Escaping || start ;; Process_start
withdrawing ;; Escaping || kill ;; Killing || allow ;; Preventing_or_letting || fire ;; Use_firearm
withdraw ;; Escaping || emerge ;; Coming_to_be || attack ;; Attack || shooting ;; Use_firearm
reaches ;; Arriving
reform ;; Reforming_a_system
make ;; Manufacturing || detour ;; Motion
challenges ;; Confronting_problem
challenge ;; Confronting_problem || taken up ;; Getting
agreed ;; Agree_or_refuse_to_act || become ;; Becoming || fighting ;; Hostile_encounter || stops ;; Process_end
arranged ;; Arranging || separate ;; Forming_relationships
declared ;; Expressing_publicly || fighting ;; Hostile_encounter
killed ;; Killing || prisoner ;; Prison || taken ;; Placing
given ;; Giving || send ;; Sending || arranged ;; Arranging || trampled ;; Destroying || fighting ;; Hostile_encounter || get up ;; Self_motion || captured ;; Conquering || thrown ;; Motion
starts ;; Process_start || Fighting ;; Hostile_encounter
taken ;; Conquering || prisoner ;; Prison || fight ;; Hostile_encounter
stitched ;; Connect || joined ;; Becoming_a_member
knocks ;; Attack || rolls ;; Motion || seizes ;; Hold
begin ;; Process_start || retreat ;; Escaping || begins ;; Process_start
begins ;; Process_start || starts ;; Process_start || withdraw ;; Escaping || becomes ;; Becoming
killed ;; Killing || taken ;; Conquering || pursued ;; Influence || prisoner ;; Prison
hides ;; Hiding_objects
request ;; Request || given ;; Giving || absolving ;; Judgment_communication || loss ;; Damaging || lost ;; Earnings_and_losses || accounted ;; Justifying
fought ;; Hostile_encounter
invasion ;; Attack || War ;; Military_operation
began ;; Process_start || invaded ;; Attack || operations ;; Military_operation
declared ;; Expressing_publicly || election ;; Change_of_leadership || End ;; Process_end || war ;; Military_operation || operations ;; Military_operation || established ;; Creating || ended ;; Process_end
remained ;; Temporary_stay || withdrawal ;; Removing
sent ;; Sending
arrived ;; Arriving
involved ;; Participation
invasion ;; Attack || preparation ;; GetReady || assembled ;; Come_together
received ;; Receiving || support ;; Supporting
support ;; Supporting || disarm ;; Removing || end ;; Process_end || free ;; Releasing || terrorism ;; Terrorism || aimed ;; Aiming
changing ;; Change || place ;; Placing || impact ;; Influence || played ;; Competition || attacks ;; Attack || rise ;; Cause_change_of_position_on_a_scale
disarm ;; Removing || trigger ;; Causation || failure ;; Earnings_and_losses || threat ;; Catastrophe || final ;; Process_end || alleged ;; Statement || take ;; Getting
increase ;; Cause_change_of_position_on_a_scale || war ;; Military_operation || poll ;; Research || find ;; Creating || terrorism ;; Terrorism || believed ;; Know || directed ;; Aiming || approved ;; Agree_or_refuse_to_act || wanted ;; Request || threat ;; Terrorism
opposed ;; Agree_or_refuse_to_act || invasion ;; Attack
invading ;; Attack || justified ;; Justifying || argued ;; Quarreling
found ;; Know || produced ;; Manufacturing || invasion ;; Attack || rule ;; Control || determined ;; Deciding
discovered ;; Know || discoveries ;; Know || support ;; Supporting || War ;; Military_operation || built ;; Building || abandoned ;; GiveUp
invasion ;; Attack || protests ;; Judgment_communication || rally ;; Social_event || rally ;; Social_event || listed ;; Recording
protests ;; Judgment_communication || took part ;; Dispersal
invasion ;; Attack || airstrike ;; Attack || preceded ;; Process_start
launched ;; Process_start || incursion ;; Attack || forces ;; Conquering
moved ;; Motion || assault ;; Attack || launched ;; Process_start || secure ;; Getting || surrounding ;; Surrounding || Battle ;; Hostile_encounter || occupying ;; Control || engaging ;; Participation
prevented ;; Preventing_or_letting || strikes ;; Attack || threw ;; Causation || resistance ;; Defending || defending ;; Defending || command-and-control ;; Judgment_communication
fought ;; Hostile_encounter || actions ;; Conquering || secure ;; Getting || joined ;; Becoming_a_member
drive ;; Motion || resistance ;; Defending
defeated ;; Conquering || occupied ;; Control
attack ;; Attack || occupation ;; Control || occurred ;; Coming_to_be || capture ;; Conquering || capture ;; Control
hiding ;; Escaping || completed ;; Process_end || occupation ;; Control || leadership ;; Supporting
declared ;; Expressing_publicly || began ;; Process_start || end ;; Process_end || operations ;; Military_operation || ended ;; Process_end
murder ;; Killing || kidnapping ;; Kidnapping || occurred ;; Presence
forced ;; Control
reported ;; Reporting || charred ;; Destroying
beaten ;; Bodily_harm || autopsy ;; Scrutiny || burnt ;; Bodily_harm || suggested ;; Convincing
murder ;; Killing || attack ;; Attack || response ;; Response || murder ;; Killing || abduction ;; Kidnapping || explained ;; Justifying
murders ;; Killing || conflict ;; Hostile_encounter || hostilities ;; Hostile_encounter || contributed ;; Causation
murder ;; Killing || stating ;; Statement || revenge ;; Revenge || took ;; Conquering || suspects ;; Committing_crime || custody ;; Arrest || interrogation ;; Criminal_investigation || carried out ;; Action
put on ;; Prison || confessed ;; Reveal_secret || remand ;; Arrest
murder ;; Killing || reenacted ;; Placing || confessed ;; Reveal_secret || crime ;; Committing_crime
released ;; Releasing || murderers ;; Killing || heard ;; Perception_active || suspects ;; Committing_crime || crime ;; Committing_crime || alleged ;; Statement
blamed ;; Judgment_communication || demanded ;; Request || murder ;; Killing || condemn ;; Judgment_communication || condemned ;; Judgment_communication || kidnapping ;; Kidnapping
condemned ;; Judgment_communication || attack ;; Attack
murder ;; Killing || blamed ;; Judgment_communication || visit ;; Traveling || rejected ;; Agree_or_refuse_to_act
arrested ;; Arrest || murder ;; Killing || suspects ;; Committing_crime || condemn ;; Judgment_communication || called ;; Request || offer ;; Giving
violence ;; Violence || loss ;; Earnings_and_losses || oppose ;; Agree_or_refuse_to_act || said ;; Telling
sent ;; Sending || murdered ;; Killing || murder ;; Killing || condemned ;; Judgment_communication
beaten ;; Bodily_harm || murder ;; Killing || aftermath ;; Catastrophe || assault ;; Attack
detained ;; Arrest || protest ;; Protest
accused ;; Judgment_communication || arrest ;; Arrest || arrest ;; Arrest || notify ;; Telling
obtained ;; Getting || included ;; Cause_to_be_included || mount ;; Motion_directional || victims ;; Catastrophe || terror ;; Terrorism || removal ;; Removing
improve ;; Cause_to_make_progress || rebuffed ;; Agree_or_refuse_to_act || killed ;; Killing || stated ;; Statement || remove ;; Removing || inclusion ;; Cause_to_be_included || suspects ;; Committing_crime || trial ;; Criminal_investigation || contravened ;; Imposing_obligation || threatening ;; Warning || forced ;; Causation || dragged out ;; Change_event_time || complained ;; Judgment_communication || memorialized ;; Rite || dismissed ;; Response
involved ;; Cause_to_be_included || murder ;; Killing || guilty ;; Legality || imprisonment ;; Prison || found ;; Coming_to_believe || sentenced ;; Legal_rulings
prison ;; Prison || sentenced ;; Legal_rulings
attack ;; Attack || assault ;; Attack || members ;; Becoming_a_member || commanded ;; Control
killed ;; Killing || injured ;; Bodily_harm || army ;; Bearing_arms
investigations ;; Scrutiny || assault ;; Attack || preventing ;; Preventing_or_letting || planned ;; Arranging || opposed ;; Agree_or_refuse_to_act || carried out ;; Action || pretense ;; Justifying || officers ;; Criminal_investigation || alleged ;; Statement || coup ;; Change_of_leadership
declared ;; Expressing_publicly || uprising ;; Change_of_leadership || attack ;; Attack || led to ;; Causation || carried ;; Bringing || sparking ;; Causation || war ;; Hostile_encounter
freed ;; Releasing || Given ;; Giving || sentence ;; Legal_rulings || imprisoned ;; Arrest
died ;; Death || awaiting ;; GetReady || surgery ;; Cure
attacks ;; Attack || attacks ;; Attack || killed ;; Killing || referred ;; Adducing
attack ;; Attack || car bomb ;; Terrorism || explosion ;; Attack
placed ;; Placing || block ;; Hiding_objects || bomb ;; Use_firearm
killed ;; Killing || explosion ;; Catastrophe || injured ;; Bodily_harm
occurred ;; Coming_to_be || attack ;; Attack
organized ;; Arranging || party ;; Participation
dressed ;; Wearing || killing ;; Killing || participants ;; Participation || opened ;; Action || injuring ;; Bodily_harm
dead ;; Death
shooting ;; Killing || attack ;; Attack || shooting ;; Killing
affected ;; Influence || found ;; Know
attacks ;; Attack || expressed ;; Expressing_publicly || condemned ;; Judgment_communication
bombing ;; Attack || attacks ;; Attack || Report ;; Statement || concluded ;; Coming_to_believe || prevented ;; Preventing_or_letting || effects ;; Influence
attacks ;; Attack || arrested ;; Arrest || charged ;; Judgment_communication
defense ;; Defending || attacks ;; Attack || denied ;; Justifying || claimed ;; Statement || admitted ;; Telling || took place ;; Process_start || carrying out ;; Action
charged ;; Judgment_communication || sentenced ;; Rewards_and_punishments || sentence ;; Rewards_and_punishments || prison ;; Prison
sentence ;; Rewards_and_punishments || deemed ;; Know || extended ;; Change_event_time
occurred ;; Coming_to_be
facilitated ;; Cause_to_make_progress
use ;; Using || fund ;; Supply
prohibited ;; Preventing_or_letting
connected ;; Connect || held ;; Hold
pay ;; Commerce_pay || resupply ;; Supply || ship ;; Sending
achieve ;; Achieve || promised ;; Commitment
began ;; Process_start || taken ;; Conquering || documented ;; Recording
excuse ;; Justifying || ruled out ;; Removing
fund ;; Supply || diverted ;; Removing
disputed ;; Quarreling || authorized ;; Preventing_or_letting
taken ;; Conquering
free ;; Releasing || wrote ;; Writing || answer ;; Response || answer ;; Response || said ;; Telling || passed ;; Motion_directional
revealed ;; Reveal_secret || stated ;; Statement || appeared ;; Presence || occurred ;; Coming_to_be
impeded ;; Hindering || destroyed ;; Destroying
began ;; Process_start || made ;; Creating || deteriorated ;; Change
investigated ;; Criminal_investigation
found ;; Know || knew ;; Know
end ;; Process_end || indicted ;; Criminal_investigation
vacated ;; Removing || resulted ;; Causation
pardoned ;; Rewards_and_punishments || indicted ;; Criminal_investigation || convicted ;; Legal_rulings
festival ;; Social_event
compete ;; Competition
evolved ;; Coming_to_be
due ;; Causation || reviving ;; Recovering || left ;; Departing
support ;; Supporting || clarified ;; Justifying || interview ;; Communication
damage ;; Damaging
announced ;; Expressing_publicly
attack ;; Attack || damage ;; Damaging
deployed ;; Arranging || struck ;; Attack || protect ;; Defending || attacks ;; Attack || escorted ;; Sending
sank ;; Motion_directional || blew ;; Attack
saved ;; Rescuing || towed ;; Sending
recovered ;; Recovering
planned ;; Arranging || found ;; Know
helped ;; Assistance || agree ;; Agree_or_refuse_to_act || ending ;; Process_end
protect ;; Defending || ruled ;; Legal_rulings || justified ;; Justifying
treaty ;; Statement || dismissed ;; Agree_or_refuse_to_act
include ;; Cause_to_be_included
marked ;; Recording
damaged ;; Damaging
riot ;; Violence || took place ;; Process_start
alleged ;; Statement || riot ;; Violence || occurred ;; Coming_to_be || arrest ;; Arrest
rioting ;; Violence || involved ;; Cause_to_be_included
stated ;; Statement || legitimise ;; Justifying || connected ;; Connect || tension ;; Quarreling || excuse ;; Protest || criminal ;; Committing_crime || crime ;; Committing_crime || behaviour ;; Action || saying ;; Statement || committing ;; Commitment
rioting ;; Violence || riots ;; Violence
arrest ;; Arrest || wrongdoing ;; Committing_crime || involved ;; Cause_to_be_included || questioned ;; Quarreling || cleared ;; Removing
bomb ;; Attack || exploded ;; Attack
begin ;; Process_start || attacked ;; Attack || carrying ;; Carry_goods
killed ;; Killing || said ;; Statement || injured ;; Bodily_harm
killed ;; Killing || attack ;; Attack || claimed ;; Statement
began ;; Process_start || bombing ;; Attack || uprising ;; Change_of_leadership
killed ;; Killing || bombing ;; Attack
blamed ;; Judgment_communication || attack ;; Attack || attack ;; Attack
accused ;; Judgment_communication || justify ;; Justifying || uprising ;; Change_of_leadership || attacks ;; Attack
claimed ;; Statement || formed ;; Creating
started ;; Process_start || tour ;; Traveling
anniversary ;; Social_event || tour ;; Traveling || celebrated ;; Social_event
tour ;; Traveling
structure ;; Building
tour ;; Traveling || guest ;; Temporary_stay
tour ;; Traveling
die ;; Death
Tour ;; Traveling
support ;; Supporting || releases ;; Publishing
played ;; Participation || performing ;; Hold || curated ;; Create_artwork
performed ;; Hold || inspired ;; Influence
performed ;; Hold || performed ;; Hold || changed ;; Change || set ;; Placing
tour ;; Traveling || performances ;; Cause_to_make_progress || ceased ;; Process_end
ended ;; Process_end || caused ;; Causation || fall ;; Motion_directional || excessive ;; Cause_change_of_position_on_a_scale || due ;; Causation || referred ;; Name_conferral
referred ;; Name_conferral
play ;; Action
promote ;; Cause_change_of_position_on_a_scale || issued ;; Creating
tour ;; Traveling
visited ;; Traveling || support ;; Supporting || Staged ;; Arranging
built ;; Building
constructed ;; Building
toured ;; Traveling || claimed ;; Statement
income ;; Earnings_and_losses
sold ;; Commerce_sell || tour ;; Traveling
required ;; Imposing_obligation
increased ;; Cause_change_of_position_on_a_scale
incorporated ;; Cause_to_amalgamate
displayed ;; Create_artwork
performed ;; Hold || adjusted ;; Change || debuted ;; Process_start
began ;; Process_start || concluded ;; Process_end
visited ;; Traveling
postponed ;; Change_event_time || suffered ;; Bodily_harm
concert ;; Social_event || streamed ;; Having_or_lacking_access || set ;; Causation
received ;; Receiving
set ;; Causation
party ;; Social_event || festival ;; Social_event || Festival ;; Social_event || celebrate ;; Social_event || started ;; Process_start || anniversary ;; Social_event || aimed ;; Aiming
event ;; Social_event || celebrating ;; Social_event
Fest ;; Social_event || Hurricane ;; Catastrophe || held ;; Defending || held ;; Defending || effect ;; Influence
held ;; Defending
performing ;; Participation
festival ;; Social_event || produced ;; Creating
festival ;; Social_event || production ;; Manufacturing || facelift ;; Change || contracted ;; Sign_agreement || designed ;; Create_artwork
became ;; Becoming || Starting ;; Process_start || Festival ;; Social_event
Festival ;; Social_event || broadcast ;; Expressing_publicly
festival ;; Social_event || returned ;; Arriving
caused ;; Causation || storm ;; Catastrophe || flood ;; Catastrophe || flood ;; Catastrophe || occurred ;; Presence
floods ;; Catastrophe || struck ;; Attack
combination ;; Cause_to_amalgamate || caused ;; Causation || storm ;; Catastrophe || combination ;; Cause_to_amalgamate || led to ;; Causation
caused ;; Causation || overwhelmed ;; Destroying || flood ;; Catastrophe || flooding ;; Motion
deaths ;; Death || damage ;; Damaging || defences ;; Defending || recording ;; Recording || widespread ;; Dispersal || affected ;; Influence
casualties ;; Death || occurred ;; Presence
killed ;; Killing
killed ;; Killing
killed ;; Killing
deaths ;; Death || occurred ;; Presence
fatalities ;; Death || lost ;; Earnings_and_losses
Realising ;; Coming_to_believe || reoccur ;; Coming_to_be || defences ;; Defending || strengthening ;; Cause_change_of_strength || carried out ;; Military_operation || studies ;; Research
storm ;; Catastrophe || developed ;; Coming_to_be
constructed ;; Building || storm ;; Catastrophe || barriers ;; Hindering
festival ;; Social_event
held ;; Hold || festival ;; Social_event
described ;; Statement
festival ;; Social_event
festival ;; Social_event || grow ;; Expansion
start ;; Process_start || said ;; Statement
concentrates ;; Aiming
close ;; Self_motion || take place ;; Process_start
used ;; Using
tour ;; Traveling
tour ;; Traveling || continuation ;; Expansion
performed ;; Hold || toured ;; Traveling
teach ;; Education_teaching || selected ;; Choosing
filmed ;; Create_artwork
titled ;; Name_conferral || released ;; Publishing
festival ;; Social_event
events ;; Social_event || undertaken ;; Action
held ;; Hold
innovation ;; Create_artwork || launched ;; Process_start
composed ;; Come_together
attracting ;; Influence || held ;; Process_start
operating ;; Being_in_operation
flying ;; Motion || crashed ;; Catastrophe || loss ;; Earnings_and_losses
crash ;; Catastrophe || involving ;; Participation
left ;; Departing || scheduled ;; Arranging
climbed ;; Motion_directional || coming ;; Motion
contacted ;; Connect || reporting ;; Reporting
noted ;; Know || requested ;; Request || stay ;; Temporary_stay
gave ;; Giving || acknowledged ;; Statement
noting ;; Know || slowed ;; Change_event_time
sent ;; Sending || entered ;; Arriving || lost ;; Earnings_and_losses || entered ;; Arriving
reached ;; Arriving || certified ;; Check || demonstrated ;; Protest || believed ;; Know
failed ;; Earnings_and_losses
caused ;; Causation || break ;; Destroying || cause ;; Causation || combined ;; Cause_to_amalgamate || pitch ;; Motion
destroyed ;; Destroying || left ;; Departing || separated ;; Forming_relationships
left ;; Departing || gone ;; Removing || induced ;; Causation || yawing ;; Motion_directional
weakened ;; Cause_change_of_strength || attached ;; Connect || losing ;; Earnings_and_losses || came ;; Motion
caused ;; Causation || opening ;; Openness || disintegrate ;; Destroying || leading to ;; Causation
plunged ;; Motion_directional || crumbled ;; Destroying || left ;; Departing
cause ;; Causation || descent ;; Motion_directional || determined ;; Deciding || created ;; Creating || designed ;; Create_artwork
lasted ;; Process_end
fought ;; Hostile_encounter
making it ;; Causation
killed ;; Killing || fled ;; Escaping
reforms ;; Reforming_a_system || initiating ;; Process_start || took ;; Getting
reforms ;; Reforming_a_system || established ;; Building
led to ;; Causation || suppressed ;; Preventing_or_letting
murdered ;; Committing_crime || orders ;; Request || soured ;; Cause_change_of_position_on_a_scale
decided ;; Deciding
Arriving ;; Arriving || killing ;; Killing || installing ;; Change_of_leadership
deployment ;; Arranging || called ;; Name_conferral
withdrawal ;; Escaping || adopted ;; Choosing
supported ;; Supporting || vote ;; Choosing || vote ;; Choosing
began ;; Process_start || paid ;; Commerce_pay || receive ;; Receiving || training ;; Education_teaching
documented ;; Recording || invasion ;; Attack || played ;; Self_motion || asserting ;; Statement || designed ;; Create_artwork || frustrate ;; Hindering
reach ;; Arriving
operating ;; Being_in_operation || occupied ;; Control
used ;; Using || deal ;; Resolve_problem || destroying ;; Destroying || rebels ;; Hostile_encounter || levelling ;; Cause_change_of_position_on_a_scale || laying ;; Placing
imposed ;; Action
led ;; Causation
planned ;; Arranging || withdraw ;; Removing || secure ;; Getting
increased ;; Cause_change_of_position_on_a_scale || cost ;; Cost || increased ;; Cause_change_of_position_on_a_scale
start ;; Process_start || withdrawing ;; Escaping || announced ;; Expressing_publicly || meetings ;; Social_event
withdrawal ;; Escaping || started ;; Process_start || ended ;; Process_end || leaving ;; Departing || collapsed ;; Destroying
referred ;; Adducing
thought ;; Coming_to_believe
detonated ;; Attack || explosives ;; Attack || car bomb ;; Terrorism
injuring ;; Bodily_harm || bomb ;; Attack || exploded ;; Attack
caused ;; Causation || spread ;; Dispersal
caused ;; Causation || explosion ;; Attack || escaped ;; Escaping || leaving ;; Departing || target ;; Aiming || injury ;; Bodily_harm || damage ;; Damaging || bomb ;; Attack
arising ;; Coming_to_be || car bomb ;; Terrorism || damaged ;; Damaging || ruptured ;; Destroying || flooding ;; Damaging
injuries ;; Bodily_harm || bomb ;; Attack || cause ;; Causation || designed ;; Create_artwork || regarded ;; Perception_active
attack ;; Attack || bombing ;; Attack || planned ;; Arranging || massacre ;; Killing || cause ;; Causation || carnage ;; Killing || anti-terrorist ;; Terrorism || claimed ;; Statement
bombing ;; Attack || bombing ;; Attack || signed ;; Sign_agreement || campaign ;; Social_event || ending ;; Process_end
took place ;; Process_start
held ;; Defending
said ;; Communication || said ;; Communication || used ;; Using || carry out ;; Action
arts ;; Create_artwork || practice ;; Practice || refer ;; Adducing
competition ;; Competition || practised ;; Practice || refer ;; Adducing
changed ;; Change || practised ;; Practice
forms ;; Coming_to_be || vary ;; Change
struck ;; Attack
disasters ;; Catastrophe || sinking ;; Motion || resulted ;; Causation
received ;; Receiving || travelling ;; Motion || sighted ;; Motion
opened ;; Openness || suffered ;; Bodily_harm || buckled ;; Connect
designed ;; Create_artwork || realised ;; Coming_to_believe || sink ;; Removing
used ;; Using || attract ;; Bringing || put ;; Placing
hold ;; Defending || designed ;; Create_artwork || sinking ;; Motion
evacuation ;; Escaping
sank ;; Motion
drowned ;; Death || died ;; Death || jumped ;; Self_motion || fell ;; Motion_directional
rescued ;; Rescuing || arrived ;; Arriving || collision ;; Catastrophe || sinking ;; Catastrophe
caused ;; Causation || disaster ;; Catastrophe
recommended ;; Convincing || inquiries ;; Criminal_investigation || establishment ;; Creating
support ;; Supporting || concert ;; Social_event || Tour ;; Traveling || tour ;; Traveling || released ;; Publishing
began ;; Process_start || tour ;; Traveling || leg ;; Social_event || culminated ;; Coming_to_be || kicked off ;; Motion
began ;; Process_start || commenced ;; Process_start || leg ;; Social_event
tour ;; Traveling
tour ;; Traveling || returned ;; Arriving
began ;; Process_start || ended ;; Process_end || jaunt ;; Traveling
concerts ;; Social_event || issued ;; Creating || filmed ;; Create_artwork || release ;; Publishing || entitled ;; Name_conferral
reissued ;; Publishing
entitled ;; Name_conferral || released ;; Publishing
recordings ;; Recording || concert ;; Social_event || recorded ;; Recording || release ;; Publishing
promote ;; Cause_change_of_position_on_a_scale || included ;; Cause_to_be_included || leg ;; Social_event || Tour ;; Traveling
Tour ;; Traveling || tour ;; Traveling
undertaken ;; Action
documenting ;; Recording || work ;; Action
works ;; Action
exhibits ;; Create_artwork || satire ;; Judgment_communication
became ;; Becoming || show ;; Presence || stirred ;; Cause_to_amalgamate || defacement ;; Damaging || competing ;; Competition
illegal ;; Legality || praised ;; Judgment_communication || took ;; Action || protect ;; Preventing_or_letting || reported ;; Reporting
received ;; Receiving || reviews ;; Judgment_communication || drew ;; Causation
titled ;; Name_conferral || released ;; Publishing
tour ;; Traveling
organized ;; Arranging || swing ;; Self_motion || tour ;; Traveling
included ;; Cause_to_be_included
stated ;; Statement || using ;; Using || gone ;; Motion || tour ;; Traveling || appeal ;; Influence
hear ;; Perception_active || singing ;; Expressing_publicly || think ;; Know
began ;; Process_start || concluded ;; Process_end || tour ;; Traveling
played ;; Competition
tour ;; Traveling || kicked off ;; Process_start
Ball ;; Social_event
beginning ;; Process_start || Located ;; Placing || designed ;; Create_artwork || referred ;; Adducing
start ;; Process_start || resting ;; Placing
preceded ;; Process_start
organized ;; Arranging || promote ;; Cause_change_of_position_on_a_scale || designed ;; Create_artwork || held ;; Hold
drop ;; Motion_directional || held ;; Hold || held ;; Hold
lighting ;; Lighting || lit ;; Lighting || updated ;; Cause_to_make_progress || constructed ;; Building
lighting ;; Lighting
contain ;; Containing
used ;; Using || displayed ;; Creating
organized ;; Arranging || televised ;; Communication || celebrations ;; Social_event || attended ;; Participation
inspired ;; Influence || use ;; Using || drops ;; Motion || drop ;; Motion
promote ;; Cause_change_of_position_on_a_scale || tour ;; Traveling
tour ;; Traveling
grossed ;; Earnings_and_losses || replacing ;; Change
tour ;; Traveling || Tour ;; Traveling
said ;; Communication || remarked ;; Statement
started ;; Process_start || died ;; Death
talk ;; Statement || answered ;; Communication
design ;; Create_artwork
festival ;; Social_event || Festival ;; Social_event || referred ;; Name_conferral || spanned ;; Change_event_time
Festival ;; Social_event || Established ;; Creating || takes place ;; Process_start
called ;; Name_conferral
access ;; Having_or_lacking_access || participate ;; Participation || performing ;; Hold || festival ;; Social_event
spoken ;; Telling
due ;; Causation
sells ;; Commerce_sell || supported ;; Supporting || support ;; Supporting || Festival ;; Social_event || Festival ;; Social_event || tickets ;; Preventing_or_letting || publishes ;; Publishing || offers ;; Giving
participants ;; Participation || support ;; Supporting || dedicated ;; Supply || providing ;; Supply || festival ;; Social_event || manage ;; Control
participants ;; Participation || drawn ;; Motion
held ;; Hold || serve ;; Assistance || Elections ;; Choosing
appoints ;; Deciding || assumed ;; Choosing
operates ;; Control
takes up residence ;; Process_start
brings together ;; Come_together || curated ;; Create_artwork || fusing ;; Cause_to_amalgamate || making ;; Creating || design ;; Creating
accessible ;; Having_or_lacking_access || brands ;; Labeling || made ;; Manufacturing || drink ;; Ingestion || eat ;; Ingestion || view ;; Change_sentiment || meet ;; Come_together || makers ;; Manufacturing || buildings ;; Building || see ;; Perception_active || products ;; Manufacturing || learn ;; Education_teaching || go ;; Self_motion
founding ;; Creating || supported ;; Supporting
held ;; Hold || attended ;; Participation
show ;; Social_event
organised ;; Arranging
used ;; Using || became ;; Becoming || operational ;; Using || receive ;; Receiving || closed ;; Self_motion || defence ;; Defending || preparing ;; GetReady
includes ;; Cause_to_be_included || production ;; Creating
manufactured ;; Manufacturing || include ;; Cause_to_be_included || flying ;; Self_motion
made ;; Manufacturing || landing ;; Self_motion
event ;; Social_event
visitors ;; Traveling || visit ;; Traveling || purchasing ;; Commerce_buy || meant ;; Statement || invited ;; Request
open ;; Openness
held ;; Defending || held ;; Defending
developed ;; Coming_to_be || festival ;; Social_event || events ;; Social_event
presents ;; Presence || situated ;; Placing
Performances ;; Social_event || take place ;; Hold
reading ;; Statement || produce ;; Creating || allows ;; Preventing_or_letting
acts ;; Action || include ;; Cause_to_be_included
based ;; Using
beginning ;; Process_start || held ;; Hold
Presents ;; Statement
Presents ;; Statement || held ;; Hold
Presents ;; Statement
Presents ;; Statement || held ;; Hold
Presents ;; Statement
Presents ;; Statement || held ;; Hold
Presents ;; Statement
Presents ;; Statement || held ;; Hold
Presents ;; Statement
Presents ;; Statement || held ;; Hold
Presents ;; Statement
Presents ;; Statement || held ;; Hold
Presents ;; Statement
Presents ;; Statement || held ;; Hold
Presents ;; Statement
Presents ;; Statement || held ;; Hold
held ;; Hold || promoted ;; Cause_change_of_position_on_a_scale
included ;; Cause_to_be_included
aired ;; Expressing_publicly
pitted ;; Hostile_encounter
strike ;; Attack || shut down ;; Having_or_lacking_access
began ;; Process_start || began ;; Process_start || strike ;; Attack || conflict ;; Hostile_encounter
murdered ;; Killing
designed ;; Create_artwork
hire ;; Employment
used ;; Using
lowered ;; Cause_change_of_position_on_a_scale || reduce ;; Cause_change_of_position_on_a_scale || laid off ;; Placing || called ;; Request
strike ;; Attack || control ;; Control || refusal ;; Agree_or_refuse_to_act
formed ;; Coming_to_be
Founded ;; Creating
brought in ;; Bringing || signed up ;; Employment
refused ;; Agree_or_refuse_to_act || strike ;; Attack
decided ;; Deciding || stop ;; Process_end || strike ;; Attack
strike ;; Attack
called ;; Request
affected ;; Influence || involved ;; Cause_to_be_included
opposed ;; Agree_or_refuse_to_act
caused ;; Causation || killed ;; Killing || damages ;; Damaging
obtained ;; Getting || interfering ;; Hindering || stop ;; Process_end
refused ;; Agree_or_refuse_to_act || stop ;; Process_end || obstructing ;; Hindering || ordered ;; Request
collapsed ;; Destroying
Defended ;; Defending || convicted ;; Legal_rulings || dissolved ;; Dispersal
coordinated ;; Arranging
capture ;; Conquering || designed ;; Create_artwork
started ;; Process_start
crushed ;; Destroying
favored ;; Response
fighting ;; Hostile_encounter
exhausted ;; Expend_resource
weakened ;; Cause_change_of_strength
seized ;; Hold || established ;; Building
began ;; Process_start
pushed ;; Motion || halted ;; Process_end
destroyed ;; Destroying || limited ;; Limiting
invasion ;; Attack
end ;; Process_end || took place ;; Process_start
provided ;; Supply || transport ;; Military_operation || sweeping ;; Scouring || force ;; Military_operation
objectives ;; Aiming || move ;; Motion || capture ;; Conquering || establish ;; Building || link ;; Connect || forces ;; Military_operation || forces ;; Military_operation
attacking ;; Attack || faced ;; Confronting_problem
stationed ;; Placing
beginning ;; Process_start
commenced ;; Process_start || got ;; Getting
landing ;; Self_motion || planned ;; Arranging
hits ;; Attack || disabled ;; Destroying
surrendered ;; Surrendering || firing ;; Use_firearm
provide ;; Supply || attacks ;; Attack || hit ;; Attack
fired ;; Use_firearm || modified ;; Change || damage ;; Damaging || bomb ;; Attack
began ;; Process_start || fortified ;; Cause_change_of_strength || targets ;; Aiming || clearing ;; Removing || advanced ;; Motion
captured ;; Conquering || Battle ;; Hostile_encounter || advanced ;; Motion
captured ;; Conquering || contact ;; Connect || forces ;; Military_operation
received ;; Receiving || attacking ;; Attack || awarded ;; Award
captured ;; Conquering
casualties ;; Catastrophe
casualties ;; Death
cyclone ;; Catastrophe || produced ;; Creating
developed ;; Coming_to_be || storm ;; Catastrophe
classified ;; Check || storm ;; Catastrophe || producing ;; Creating
re-curved ;; Self_motion
peaked ;; Arriving
weakened ;; Cause_change_of_strength || turned ;; Change
remained ;; Presence || cyclone ;; Catastrophe
moved ;; Motion || dissipating ;; Dispersal
produced ;; Creating
killed ;; Killing || flooding ;; Catastrophe || affected ;; Influence
died ;; Death || flooding ;; Catastrophe
fell ;; Motion_directional
died ;; Death
exacerbated ;; Cause_change_of_strength || flooding ;; Catastrophe
rescues ;; Rescuing || inundated ;; Destroying || required ;; Imposing_obligation
occurred ;; Coming_to_be
damage ;; Damaging || left ;; Causation || flooding ;; Catastrophe
swept ;; Scouring || occurred ;; Coming_to_be
inundated ;; Destroying
drowning ;; Death
flooding ;; Catastrophe
damage ;; Damaging
crusades ;; Military_operation
induced ;; Convincing || maintain ;; Statement || starting ;; Process_start || defend ;; Defending || endeavoured ;; Self_motion || expand ;; Expansion
commissioned ;; Request || organise ;; Arranging || increased ;; Cause_change_of_position_on_a_scale
sent ;; Sending || engage ;; Participation || joined ;; Participation
published ;; Publishing
began ;; Process_start || ended ;; Process_end || capturing ;; Conquering
abandoned ;; GiveUp || believed ;; Know || celebrate ;; Social_event
swept ;; Scouring || killed ;; Killing
organise ;; Arranging || spurred ;; Influence
set out ;; Placing
negotiate ;; Communication || kept ;; Preserving || direct ;; Aiming || occupied ;; Hold
ended ;; Process_end
crashed ;; Catastrophe || originating ;; Coming_to_be || flying ;; Motion
crash ;; Catastrophe || killed ;; Killing || named ;; Name_conferral || assigned ;; Choosing
received ;; Receiving || reports ;; Reporting || exhausted ;; Expend_resource
launched ;; Motion || search ;; Scouring
became ;; Becoming || search ;; Scouring || rescue ;; Rescuing
recovered ;; Recovering
investigations ;; Scrutiny
find ;; Know || support ;; Supporting
conducted ;; Action || investigation ;; Scrutiny || determine ;; Deciding
affected ;; Influence || damaging ;; Damaging
Threatening ;; Commitment || triggering ;; Process_start || deviated ;; Cause_change_of_position_on_a_scale
effects ;; Influence || wrought ;; Causation
traveling ;; Traveling || impact ;; Influence || strengthen ;; Cause_change_of_strength || developed ;; Cause_change_of_strength || entered ;; Arriving
strike ;; Attack || veered ;; Self_motion || stalled ;; Temporary_stay
moving ;; Motion || causing ;; Causation || damage ;; Damaging || remained ;; Temporary_stay
moving ;; Motion || weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
created ;; Creating || considered ;; Deciding
occurred ;; Coming_to_be || leave ;; Departing || forced ;; Hostile_encounter
stretched ;; Body_movement || return ;; Giving || orders ;; Arranging
fled ;; Escaping || storm ;; Catastrophe || contributing ;; Supply
warnings ;; Warning || issued ;; Expressing_publicly || adjusted ;; Change || stressed ;; Convincing
slow ;; Change_event_time || damage ;; Damaging || resulted in ;; Causation
spawned ;; Coming_to_be || hurricane ;; Catastrophe || swept ;; Scouring
devastated ;; Destroying || destroying ;; Destroying || leaving ;; Departing || killing ;; Killing
storm ;; Catastrophe || endured ;; Change_event_time
inflicted ;; Causation || damage ;; Damaging || damaged ;; Damaging || demolished ;; Destroying
diminished ;; Cause_change_of_position_on_a_scale || damage ;; Damaging
damaged ;; Damaging || destroyed ;; Destroying
damage ;; Damaging
damage ;; Damaging
extended ;; Change_event_time || damage ;; Damaging
died ;; Death || hurricane ;; Catastrophe || drownings ;; Bodily_harm
storm ;; Catastrophe || totaled ;; Earnings_and_losses || affected ;; Influence
declared ;; Expressing_publicly || making ;; Manufacturing || storm ;; Catastrophe || aid ;; Assistance
effects ;; Achieve
engagement ;; Hostile_encounter
engagement ;; Hostile_encounter || campaign ;; Military_operation || campaign ;; Military_operation || formed ;; Cause_to_amalgamate || sailed ;; Self_motion
convoy ;; Patrolling || capture ;; Conquering || looted ;; Robbery || orders ;; Request || carry ;; Sending || continued ;; Arriving || detached ;; Removing
sent ;; Sending || augment ;; Cause_change_of_strength || hunting ;; Aiming || detached ;; Removing
gave ;; Giving || fleeing ;; Escaping || Lookouts ;; Scrutiny || chase ;; Motion
pursuit ;; Aiming || catch ;; Connect || defeat ;; Conquering || inflicting ;; Causation || casualties ;; Death
captured ;; Conquering || wounded ;; Bodily_harm || storming ;; Catastrophe || owned ;; Employment
captured ;; Conquering || fitted ;; Building || downgraded ;; Cause_change_of_position_on_a_scale
invade ;; Attack || search ;; Scouring || relay ;; Communication || provided ;; Supply || Battle ;; Hostile_encounter
operated ;; Being_in_operation || registered ;; Know
originated ;; Departing || carrying ;; Sending
headed ;; Self_motion || landed ;; Self_motion || exploded ;; Destroying
died ;; Death || proven ;; Coming_to_believe
explosion ;; Destroying || reported ;; Reporting
crashed ;; Catastrophe || explosion ;; Destroying
smashed ;; Destroying || combed ;; Scouring || confronted ;; Confronting_problem
riddled ;; Motion
found ;; Know
declassified ;; Reveal_secret
launched ;; Military_operation
named ;; Name_conferral || thrust ;; Body_movement
began ;; Process_start || launched ;; Military_operation
reduce ;; Cause_change_of_position_on_a_scale || clear ;; Emptying
capturing ;; Conquering || seizing ;; Conquering || clearing ;; Emptying || support ;; Assistance || disrupted ;; Damaging
launched ;; Process_start
created ;; Creating || killing ;; Killing || capturing ;; Conquering || destroying ;; Destroying || uncovering ;; Scouring || continued ;; Preventing_or_letting
affect ;; Influence
noted ;; Know || embedded ;; Connect
developed ;; Coming_to_be || named ;; Name_conferral || classified ;; Check
strengthened ;; Cause_change_of_strength || acquired ;; Getting || performed ;; Bringing
moved ;; Motion || peaked ;; Arriving
affect ;; Influence || became ;; Becoming || started ;; Process_start || weaken ;; Cause_change_of_strength || passed ;; Motion_directional
passed ;; Motion_directional || passed ;; Motion_directional || degenerated ;; Change
turned ;; Becoming || entered ;; Arriving || associated ;; Collaboration || remained ;; Presence
renamed ;; Name_conferral || regenerated ;; Recovering
intensified ;; Cause_change_of_strength || reached ;; Achieve
moved ;; Motion || started ;; Process_start || weaken ;; Cause_change_of_strength || maintained ;; Preserving
degenerated ;; Change || dissipated ;; Dispersal || noted ;; Statement
swept ;; Scouring || evacuated ;; Emptying
caused ;; Causation || damage ;; Damaging
affected ;; Influence || destroyed ;; Destroying || damaged ;; Damaging || damaged ;; Damaging
destroyed ;; Destroying || destroyed ;; Destroying || generated ;; Creating
caused ;; Causation || approach ;; Arriving || made ;; Causation
incurred ;; Causation
help ;; Assistance || borrowed ;; Assistance || built ;; Creating
retired ;; GiveUp
collapsed ;; Catastrophe
called ;; Labeling || collapse ;; Catastrophe
killed ;; Killing
ended ;; Process_end || search ;; Scouring
constructed ;; Building || construction ;; Building || acquisition ;; Commerce_buy || reported ;; Reporting
arrested ;; Arrest
illegal ;; Legality || records ;; Recording || manage ;; Control || use ;; Using || order ;; Arranging || indicate ;; Statement
made ;; Manufacturing
illegal ;; Legality || began ;; Process_start || campaign ;; Social_event || demolish ;; Removing || focusing ;; Aiming
address ;; Change || encroachment ;; Damaging || said ;; Statement
illegal ;; Legality || established ;; Creating || accept ;; Receiving
forming ;; Coming_to_be
formed ;; Coming_to_be
developed ;; Coming_to_be || remain ;; Presence || storm ;; Catastrophe || strengthened ;; Cause_change_of_strength || landfall ;; Self_motion
storm ;; Catastrophe || strike ;; Attack
caused ;; Causation || killing ;; Killing || causing ;; Causation
killed ;; Killing
swept ;; Scouring || died ;; Death
uprising ;; Change_of_leadership || War ;; Military_operation
opposed ;; Agree_or_refuse_to_act || evasion ;; Escaping || desertion ;; Escaping || conducted ;; Action || leading to ;; Causation
drafted ;; Writing
evaded ;; Escaping
rode ;; Body_movement || search ;; Scouring || shot ;; Use_firearm
begun ;; Process_start || built ;; Building
manned ;; Action
hearing ;; Perception_active || gathered ;; Come_together
began ;; Process_start || traveling ;; Traveling
searched ;; Scouring
find ;; Know || arrested ;; Arrest
taken ;; Conquering
reaches ;; Arriving || heading ;; Self_motion
find ;; Know || returned ;; Arriving || remained ;; Temporary_stay
released ;; Releasing || arrest ;; Arrest
events ;; Social_event || covered ;; Communication
event ;; Social_event || reported ;; Reporting
began ;; Process_start || study ;; Research || mentioned ;; Communication
researched ;; Research
hurricane ;; Catastrophe || affected ;; Influence
formed ;; Coming_to_be || emerged ;; Coming_to_be
formed ;; Coming_to_be || strengthening ;; Cause_change_of_strength || hurricane ;; Catastrophe || storm ;; Catastrophe || storm ;; Catastrophe
moved ;; Motion || hurricane ;; Catastrophe || struck ;; Attack || hit ;; Attack
hurricane ;; Catastrophe || hurricane ;; Catastrophe || emerging ;; Coming_to_be || accelerated ;; Motion
fluctuating ;; Motion || oscillated ;; Self_motion
moved ;; Motion || passed ;; Motion_directional || storm ;; Catastrophe || transitioned ;; Change
killed ;; Killing || caused ;; Causation || left ;; GiveUp
hurricane ;; Catastrophe || due ;; Causation || Damage ;; Damaging || totaled ;; Earnings_and_losses
destroying ;; Destroying || damaging ;; Damaging || affected ;; Influence
damage ;; Damaging || damaged ;; Damaging
evacuated ;; Escaping
swept ;; Scouring || drain ;; Emptying || storm ;; Catastrophe || died ;; Death
fueled ;; Supply || contributed ;; Giving || storm ;; Catastrophe || flooded ;; Motion || struck ;; Attack || driving ;; Self_motion
damaged ;; Damaging || Damage ;; Damaging || limited ;; Limiting
damaged ;; Damaging || damage ;; Damaging || causing ;; Causation || struck ;; Attack || produced ;; Creating
operation ;; Military_operation
designed ;; Creating || support ;; Assistance
launched ;; Military_operation
lost ;; Earnings_and_losses
held ;; Hold || remaining ;; Presence
inflicted ;; Causation
attacked ;; Attack || operated ;; Using
reached ;; Arriving || occupied ;; Control
consequences ;; Causation
lost ;; Earnings_and_losses || extended ;; Expansion || broken ;; Destroying
produce ;; Creating || exacerbated ;; Cause_change_of_position_on_a_scale || contributed ;; Causation
operating ;; Using
suggested ;; Convincing || improve ;; Cause_to_make_progress || hamper ;; Hindering
locate ;; Scouring || destroy ;; Destroying || returned ;; Arriving || flown ;; Self_motion
ordered ;; Request || met with ;; Response || felt ;; Change_sentiment
sanctioned ;; Agree_or_refuse_to_act
presented ;; Presence || waited ;; Temporary_stay || begin ;; Process_start || attacked ;; Attack
achieve ;; Achieve || failed ;; Confronting_problem
hurricane ;; Catastrophe || hurricane ;; Catastrophe
developed ;; Coming_to_be || storm ;; Catastrophe
moved ;; Motion || intensified ;; Cause_change_of_strength || storm ;; Catastrophe
peaked ;; Arriving || deepened ;; Cause_change_of_strength
began ;; Process_start
began ;; Process_start || turned ;; Becoming
storm ;; Catastrophe || accelerated ;; Cause_to_make_progress
struck ;; Attack || hurricane ;; Catastrophe || becoming ;; Becoming
caused ;; Causation || flooding ;; Catastrophe || fell ;; Motion_directional
occurred ;; Coming_to_be || losing ;; Earnings_and_losses
impacted ;; Influence
disrupted ;; Hindering || washed ;; Removing
damaged ;; Damaging || swept ;; Scouring || making ;; Manufacturing
damage ;; Damaging || storm ;; Catastrophe || loss ;; Earnings_and_losses || destroyed ;; Destroying
damage ;; Damaging
impact ;; Influence || occurred ;; Coming_to_be
damage ;; Damaging || storm ;; Catastrophe || left ;; Departing
caused ;; Causation || hurricane ;; Catastrophe
fought ;; Hostile_encounter || War ;; Military_operation || battle ;; Military_operation || Battle ;; Military_operation || Battle ;; Military_operation
take place ;; Process_start || Campaign ;; Military_operation || engagement ;; Hostile_encounter
wounded ;; Bodily_harm || dead ;; Death
attacks ;; Attack || pursuing ;; Aiming || launched ;; Process_start
assault ;; Attack || mounted ;; Process_start
swirled ;; Motion || Attacks ;; Attack || counterattacks ;; Attack || fighting ;; Hostile_encounter || swept ;; Scouring
pierced ;; Destroying || assaults ;; Attack
capturing ;; Conquering || entered ;; Becoming_a_member || advancing ;; Cause_to_make_progress
arrived ;; Arriving || ending ;; Process_end || counterattack ;; Attack || battle ;; Hostile_encounter || launched ;; Process_start || driving ;; Preventing_or_letting
sent ;; Sending || fight ;; Hostile_encounter || enabling ;; Preventing_or_letting || committed ;; Commitment || standstill ;; Change
consolidated ;; Cause_change_of_strength
removing ;; Removing || skirmish ;; Hostile_encounter || casualties ;; Death
achieve ;; Achieve || moving ;; Motion || attacks ;; Attack || failed ;; Earnings_and_losses || counter ;; Response || meet ;; Response || challenge ;; Confronting_problem || allowed ;; Preventing_or_letting || shifting ;; Exchange
deployed ;; Arranging || destroy ;; Destroying || exploit ;; Control || successes ;; Earnings_and_losses || failed ;; Earnings_and_losses
campaign ;; Military_operation || contributed ;; Causation
invasion ;; Military_operation || interference ;; Hindering || halted ;; Process_end || withdraw ;; Escaping
removal ;; Removing || refusal ;; Agree_or_refuse_to_act || pursue ;; Aiming || led to ;; Causation || command ;; Control
abandoned ;; GiveUp || victory ;; Earnings_and_losses || making it ;; Causation || invasion ;; Military_operation || battle ;; Military_operation || withdrawn ;; Escaping
victory ;; Earnings_and_losses || give ;; Giving || plans ;; Arranging || recognize ;; Know || pursuing ;; Aiming || announce ;; Expressing_publicly || discouraged ;; Hindering
hurricane ;; Catastrophe || hurricane ;; Catastrophe || form ;; Coming_to_be
given names ;; Name_conferral || hurricanes ;; Catastrophe || named ;; Name_conferral
form ;; Coming_to_be || form ;; Creating
hurricane ;; Catastrophe || called ;; Name_conferral || landing ;; Self_motion || veered ;; Motion
prompt ;; Causation || evacuations ;; Escaping || cause ;; Causation || came ;; Motion
affect ;; Influence || hurricane ;; Catastrophe
sweeping ;; Scouring || caused ;; Causation || preceded ;; Process_start || preceded ;; Process_start || proceeded ;; Self_motion
hurricane ;; Catastrophe || referred ;; Adducing
damage ;; Damaging || record ;; Recording || surpassed ;; Cause_change_of_position_on_a_scale
named ;; Name_conferral || landfall ;; Self_motion
became ;; Becoming || hurricane ;; Catastrophe || spanning ;; Body_movement
killed ;; Killing || storm ;; Catastrophe
developed ;; Coming_to_be || strengthened ;; Cause_change_of_strength || upgraded ;; Cause_to_make_progress || wave ;; Motion
moved ;; Motion || intensified ;; Cause_change_of_strength
became ;; Becoming || strengthened ;; Cause_change_of_strength || re-emerged ;; Coming_to_be || landfall ;; Self_motion
weakened ;; Cause_change_of_strength || hit ;; Attack
moved ;; Motion
weakened ;; Cause_change_of_strength
moved ;; Motion || curved ;; Motion_directional
weakening ;; Cause_change_of_strength || drifting ;; Self_motion || approaching ;; Arriving
caused ;; Causation || killed ;; Killing || damage ;; Damaging || blew ;; Motion
causing ;; Causation || caused ;; Causation || brought ;; Bringing || killing ;; Killing || leaving ;; Departing
swept ;; Scouring
damage ;; Damaging || destroying ;; Destroying || killing ;; Killing || causing ;; Causation || flooding ;; Motion
caused ;; Causation || damage ;; Damaging
damage ;; Damaging || affected ;; Influence
hit ;; Attack || flooding ;; Motion || cutting ;; Bodily_harm
Damage ;; Damaging
killed ;; Killing || caused ;; Causation
began ;; Process_start || disguised ;; Wearing || fighting ;; Hostile_encounter || infiltrated ;; Arriving
stopped ;; Preventing_or_letting
killing ;; Killing || search ;; Scouring || entered ;; Arriving || opened ;; Process_start
poured ;; Filling
abandoning ;; GiveUp || fighting ;; Hostile_encounter || routed ;; Motion || leaving ;; Departing
killed ;; Killing
advanced ;; Getting
responsible ;; Imposing_obligation
resulted in ;; Causation || formation ;; Coming_to_be
upgraded ;; Cause_to_make_progress
hurricane ;; Catastrophe || intensify ;; Cause_change_of_strength || upgraded ;; Cause_to_make_progress || developed ;; Coming_to_be
bypassing ;; Self_motion || reached ;; Achieve
began ;; Process_start || Hurricane ;; Catastrophe || weaken ;; Cause_change_of_strength || encountered ;; Hostile_encounter
downgraded ;; Cause_change_of_position_on_a_scale
downgraded ;; Cause_change_of_position_on_a_scale
dissipated ;; Removing
brought ;; Causation
swept ;; Motion
moved ;; Motion || resulting in ;; Causation
hit ;; Attack || washed ;; Removing
causing ;; Causation || spotted ;; Know
destroyed ;; Destroying
left ;; Departing
swept ;; Scouring || died ;; Death || killed ;; Killing || swept ;; Motion || reported ;; Reporting || returning ;; Motion
moved ;; Motion || affecting ;; Influence || hurricane ;; Catastrophe
flooded ;; Motion
battles ;; Hostile_encounter || fought ;; Hostile_encounter
formed ;; Coming_to_be || launched ;; Motion || resulted in ;; Causation || pushed ;; Motion
gained ;; Getting || hampered ;; Hindering || achieved ;; Achieve
preceded ;; Process_start || landing ;; Self_motion
advance ;; Cause_to_make_progress || advancing ;; Cause_to_make_progress
attacks ;; Attack || mounted ;; Self_motion
flooding ;; Motion || advance ;; Cause_to_make_progress || slowed ;; Hindering
escaped ;; Escaping || defence ;; Defending || fall ;; Conquering || entered ;; Arriving
landing ;; Self_motion || aimed ;; Aiming || capturing ;; Conquering || opened ;; Process_start
moving ;; Motion || landing ;; Self_motion || fighting ;; Hostile_encounter || set about ;; Placing
counterattack ;; Attack || defeated ;; Conquering || resulted in ;; Causation
began ;; Process_start || regained ;; Recovering || withdrew ;; Escaping
began ;; Process_start || fighting ;; Hostile_encounter || advance ;; Cause_to_make_progress
advance ;; Cause_to_make_progress || overcoming ;; Conquering || fighting ;; Hostile_encounter || delaying ;; Change_event_time || completed ;; Process_end
break ;; Destroying
captured ;; Conquering || advance ;; Cause_to_make_progress
landed ;; Self_motion
captured ;; Conquering || mopping ;; Scouring || undertaken ;; Action
clashed ;; Hostile_encounter
fighting ;; Hostile_encounter || opened ;; Process_start
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || fought ;; Hostile_encounter || War ;; Hostile_encounter
forced ;; Motion || retreat ;; Escaping || encountered ;; Hostile_encounter || reinforced ;; Cause_change_of_strength
patrol ;; Patrolling
discovered ;; Know || escape ;; Escaping
retreat ;; Escaping || advanced ;; Cause_to_make_progress || intelligence ;; Perception_active || order ;; Arranging || cut off ;; Having_or_lacking_access || prompted ;; Preventing_or_letting
engaged ;; Participation
captured ;; Conquering || escape ;; Escaping || routed ;; Motion || blocking ;; Hindering
defeated ;; Conquering
arrival ;; Arriving || solidified ;; Cause_change_of_strength
swept ;; Scouring || escaped ;; Escaping || defense ;; Defending
attacked ;; Attack || retreating ;; Escaping
held off ;; Defending
aggression ;; Hostile_encounter || prevailed ;; Conquering
choice ;; Choosing || fight ;; Hostile_encounter || Battle ;; Hostile_encounter || Prevented ;; Preventing_or_letting || retreating ;; Escaping
Battle ;; Hostile_encounter || combined ;; Cause_to_amalgamate || commanded ;; Control || commanded ;; Control || took place ;; Process_start || defeated ;; Hostile_encounter
landing ;; Motion || taken ;; Action || report ;; Reporting
landed ;; Motion || recaptured ;; Conquering || leaving ;; Departing || holed ;; Motion
linked ;; Connect || sailed ;; Motion || forming ;; Coming_to_be
plan ;; Arranging || land ;; Motion
searched ;; Scouring || found ;; Know
bombarded ;; Attack || attack ;; Attack || sent ;; Sending || challenge ;; Hostile_encounter
arranged ;; Arranging || commanded ;; Control || battle ;; Hostile_encounter
surrounded ;; Surrounding
said ;; Communication || fight ;; Hostile_encounter || come ;; Motion
carried ;; Carry_goods
damaging ;; Damaging || retreated ;; Escaping || become ;; Becoming || attack ;; Attack || attacked ;; Attack || followed ;; Action || weathered ;; Confronting_problem
fled ;; Escaping || captured ;; Conquering || battle ;; Hostile_encounter || leaving ;; Causation
taking place ;; Process_start
breaking out ;; Process_start
involved ;; Cause_to_be_included || clashes ;; Hostile_encounter
wars ;; Hostile_encounter || Conflicts ;; Hostile_encounter || taking place ;; Process_start || conflict ;; Hostile_encounter || fueled ;; Cause_change_of_position_on_a_scale
casualties ;; Catastrophe || clashes ;; Hostile_encounter || resulted in ;; Causation
broke out ;; Process_start
occurred ;; Coming_to_be || killing ;; Killing || injuring ;; Bodily_harm
caused ;; Causation || surrounding ;; Surrounding || set off ;; Placing || ignited ;; Causation
causing ;; Causation || reached ;; Arriving
engulfed ;; Filling
growth ;; Expansion || escape ;; Departing || blocked ;; Hiding_objects || hindered ;; Hindering || shows ;; Presence
killed ;; Killing || escaped ;; Escaping || injured ;; Bodily_harm
developed ;; Coming_to_be
deaths ;; Death || surpassed ;; Cause_change_of_position_on_a_scale
named ;; Name_conferral || storm ;; Catastrophe
developed ;; Coming_to_be
weakened ;; Cause_change_of_strength || encountering ;; Hostile_encounter || discontinued ;; Process_end || remaining ;; Presence
storm ;; Catastrophe
develop ;; Coming_to_be || storm ;; Catastrophe || storm ;; Catastrophe
named ;; Name_conferral || storm ;; Catastrophe
damage ;; Damaging || storm ;; Catastrophe || causing ;; Causation || produced ;; Creating
eroded ;; Change || leaving ;; Causation
rescued ;; Rescuing || injuries ;; Bodily_harm || damaged ;; Damaging || combined ;; Cause_to_amalgamate
reported ;; Reporting || rainfall ;; Self_motion
storm ;; Catastrophe || drowned ;; Killing
storm ;; Catastrophe || causing ;; Causation || hurricane ;; Catastrophe || struck ;; Attack || hurricane ;; Catastrophe || cyclone ;; Catastrophe || damage ;; Damaging
storm ;; Catastrophe || formed ;; Coming_to_be
completed ;; Process_end || attaining ;; Achieve || hurricane ;; Catastrophe || turned ;; Motion
hurricane ;; Catastrophe || made ;; Causation || Texas ;; Arriving || continued ;; Giving || landfall ;; Arriving || winds ;; Motion || weakened ;; Cause_change_of_position_on_a_scale || headed ;; Self_motion
storm ;; Catastrophe || destroyed ;; Destroying || Damage ;; Damaging
storm ;; Catastrophe || damage ;; Damaging || city ;; Placing || suffered ;; Bodily_harm || passed ;; Motion
hurricane ;; Catastrophe || disrupted ;; Destroying
became ;; Becoming || killing ;; Killing || passed ;; Motion
lost ;; Death || due ;; Causation || cyclone ;; Catastrophe
storm ;; Catastrophe || produced ;; Creating
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe
joint ;; Collaboration || warning ;; Warning || storm ;; Catastrophe || classified ;; Check
curving ;; Motion_directional || storm ;; Catastrophe || traveled ;; Traveling || turning ;; Change
made ;; Manufacturing
moving ;; Motion || remained ;; Presence
weakened ;; Cause_change_of_strength || dissipating ;; Dispersal
storm ;; Catastrophe || peaking ;; Self_motion || produced ;; Manufacturing
killed ;; Killing || injured ;; Bodily_harm || storm ;; Catastrophe || produced ;; Manufacturing
widens ;; Expansion
took place ;; Process_start || engaged ;; Participation
forced ;; Motion || advanced ;; Cause_to_make_progress || stopped ;; Process_end
became ;; Becoming || Battle ;; Hostile_encounter || used ;; Using
built ;; Building || began ;; Process_start || ran ;; Motion
rain ;; Motion || attacked ;; Attack || due ;; Causation
attacked ;; Attack || form ;; Cause_to_amalgamate
began ;; Process_start || probe ;; Criminal_investigation || established ;; Building
held back ;; Hindering || attack ;; Attack || captured ;; Conquering
taken ;; Conquering || revealed ;; Reveal_secret
maintained ;; Statement || Allied ;; Causation
probed ;; Criminal_investigation || forces ;; Causation
captured ;; Conquering || moved ;; Motion || assist ;; Assistance
recaptured ;; Conquering
recaptured ;; Conquering
operated ;; Being_in_operation || training ;; Education_teaching || crashed ;; Motion
starting ;; Process_start || arced ;; Motion
made it ;; Causation || created ;; Creating || maintain ;; Preserving || smoke ;; Ingestion || control ;; Control
killed ;; Killing
installed ;; Placing
referred ;; Adducing || crash ;; Catastrophe
started ;; Process_start
conquering ;; Conquering || acquire ;; Getting || stemmed ;; Creating || use ;; Using || annihilate ;; Killing
signed ;; Sign_agreement
began ;; Process_start || planning ;; Arranging || authorized ;; Preventing_or_letting
operation ;; Control
deployed ;; Arranging
marked ;; Recording
opened ;; Openness || committed ;; Commitment
became ;; Becoming
influenced ;; Influence
captured ;; Conquering || returned ;; Arriving
killed ;; Killing || starved ;; Killing
aimed ;; Aiming || replacing ;; Change
resulted in ;; Causation || carried out ;; Action
proved ;; Convincing
achieved ;; Achieve || occupied ;; Control || inflicted ;; Bodily_harm
stalled ;; Cause_change_of_position_on_a_scale || pushed ;; Self_motion
unprepared ;; Response || absorbed ;; Ingestion || forced ;; Causation
mounted ;; Military_operation
demand ;; Request || failed ;; Earnings_and_losses || drove ;; Influence
caused ;; Causation || damage ;; Damaging || flooding ;; Motion
developed ;; Coming_to_be || named ;; Name_conferral
moved ;; Motion || strengthened ;; Cause_change_of_strength
deepened ;; Cause_change_of_strength || maintained ;; Preserving || curving ;; Motion_directional
re-curved ;; Motion
began ;; Process_start || became ;; Becoming
made ;; Manufacturing
weakened ;; Cause_change_of_strength
situated ;; Placing
destroyed ;; Destroying || repaired ;; Recovering
damage ;; Damaging || spawned ;; Coming_to_be
caused ;; Causation || killing ;; Killing || warning ;; Warning || due ;; Causation || fell ;; Motion
damage ;; Damaging || reported ;; Reporting
flooding ;; Motion || brought ;; Bringing
flooded ;; Motion || washed ;; Removing
reached ;; Arriving || Damage ;; Damaging
damage ;; Damaging || occurred ;; Coming_to_be
evacuate ;; Escaping
damage ;; Damaging || resulted in ;; Causation
concert ;; Social_event
support ;; Supporting || launched ;; Bringing
Beginning ;; Process_start || played ;; Action
placed ;; Placing
earned ;; Earnings_and_losses
earning ;; Earnings_and_losses || ranked ;; Placing
cyclone ;; Catastrophe || cyclones ;; Catastrophe || causing ;; Causation || damage ;; Damaging || passed ;; Motion_directional || Cyclone ;; Catastrophe
formed ;; Coming_to_be || tracking ;; Motion || reached ;; Arriving
generated ;; Creating || weakened ;; Cause_change_of_strength || accelerated ;; Cause_change_of_position_on_a_scale
transitioned ;; Change || passing ;; Motion_directional || storm ;; Catastrophe
weakened ;; Cause_change_of_strength || absorbed ;; Ingestion
cyclone ;; Catastrophe || affected ;; Influence || produced ;; Creating
damage ;; Damaging || destroyed ;; Destroying || caused ;; Causation || dropped ;; Motion
caused ;; Causation || damage ;; Damaging || peaked ;; Cause_change_of_position_on_a_scale
Damage ;; Damaging
killed ;; Killing || flooding ;; Catastrophe || threatened ;; Influence || evacuated ;; Escaping
accounted ;; Causation || fatality ;; Killing || tie ;; Connect
storm ;; Catastrophe || suffered ;; Bodily_harm || died ;; Death
used ;; Using || retired ;; GiveUp
drunk ;; Ingestion || died ;; Death || drugged ;; Bodily_harm
attracted ;; Influence
said ;; Communication || reported ;; Reporting
attacked ;; Attack
reported ;; Reporting
opened ;; Openness
killed ;; Killing || announced ;; Expressing_publicly
killed ;; Killing || killed ;; Killing || stated ;; Statement
fired ;; Use_firearm || occurred ;; Coming_to_be
injured ;; Bodily_harm
struck ;; Attack || realize ;; Coming_to_believe || took ;; Conquering
summoned ;; Request || informed ;; Telling || find out ;; Know || service ;; Rite || shooting ;; Killing
destruction ;; Destroying || violence ;; Violence || firearm ;; Use_firearm || crime ;; Committing_crime || avoiding ;; Preventing_or_letting || assassinate ;; Killing || pleaded ;; Legality
published ;; Publishing || shooting ;; Killing || service ;; Rite || made ;; Manufacturing || crime ;; Committing_crime || led to ;; Causation
service ;; Rite || resigned ;; Change_of_leadership
shooting ;; Killing || assassination ;; Killing
storm ;; Catastrophe || storm ;; Catastrophe || storm ;; Catastrophe
named ;; Name_conferral || classified ;; Check
storm ;; Catastrophe || gained ;; Getting || developed ;; Coming_to_be
storm ;; Catastrophe || crossing ;; Motion_directional || becoming ;; Becoming
damages ;; Damaging || fatalities ;; Killing || reported ;; Reporting
went on ;; Motion || become ;; Becoming || Hurricane ;; Catastrophe || affected ;; Influence
performing ;; Hold || review ;; Research || named ;; Name_conferral || storms ;; Catastrophe || storm ;; Catastrophe || identified ;; Check
storm ;; Catastrophe
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || forces ;; Military_operation || led ;; Causation || victory ;; Getting
defense ;; Defending || forces ;; Military_operation || scored ;; Getting || raised ;; Creating
attacked ;; Attack
attack ;; Attack || remained ;; Temporary_stay || victory ;; Getting || wounded ;; Bodily_harm
declined ;; Agree_or_refuse_to_act || died ;; Death || command ;; Control || command ;; Control || lead ;; Communication
led to ;; Causation || took over ;; Change_of_leadership
took ;; Causation || take ;; Getting
confronted ;; Confronting_problem || drank ;; Ingestion
forces ;; Military_operation || get ;; Getting
occurring ;; Coming_to_be || interfere ;; Hindering || intercede ;; Judgment_communication || forces ;; Military_operation
funded ;; Supply || quashed ;; Removing
taking over ;; Change_of_leadership || struggled ;; Resolve_problem
found ;; Know || realized ;; Coming_to_believe
took over ;; Change_of_leadership || forces ;; Military_operation || lead ;; Communication || order ;; Request || conflict ;; Quarreling || war ;; Military_operation
War ;; Military_operation || experienced ;; Confronting_problem
Battles ;; Hostile_encounter || trouble ;; Confronting_problem || led ;; Causation || lead ;; Communication || got ;; Getting || deal ;; Arranging
Arguments ;; Quarreling || get ;; Getting || incident ;; Quarreling || forces ;; Military_operation || led to ;; Causation || led to ;; Causation || used ;; Using || created ;; Creating || developed ;; Cause_to_make_progress || taken over ;; Change_of_leadership
hurricanes ;; Catastrophe
crossed ;; Motion_directional || weak ;; Cause_change_of_strength
organized ;; Arranging || declared ;; Expressing_publicly
turned ;; Becoming || upgraded ;; Cause_to_make_progress
storm ;; Catastrophe
strengthened ;; Cause_change_of_strength || becoming ;; Becoming || attain ;; Getting
reached ;; Arriving
approached ;; Arriving || made ;; Manufacturing
tracked ;; Self_motion
cyclone ;; Catastrophe
killed ;; Killing || sank ;; Motion
flooding ;; Motion || damage ;; Damaging
destroyed ;; Destroying || occurred ;; Coming_to_be
disrupted ;; Hindering || flow ;; Motion
received ;; Receiving || damage ;; Damaging
damage ;; Damaging
caused ;; Causation || deaths ;; Death
caused ;; Causation || occurred ;; Coming_to_be || flooding ;; Motion || moved ;; Motion
killed ;; Killing
struck ;; Attack
formed ;; Coming_to_be
moved ;; Motion || intensified ;; Cause_change_of_strength
spawned ;; Coming_to_be
Developing ;; Creating || intensified ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength || reaching ;; Achieve || struck ;; Attack
moved ;; Motion || brushed ;; Motion || accelerating ;; Cause_change_of_position_on_a_scale || Turning ;; Motion
became ;; Becoming || moved ;; Motion
dropped ;; Placing
caused ;; Causation || compounded ;; Cause_to_amalgamate
destroyed ;; Destroying || damaged ;; Damaging
compounded ;; Cause_to_amalgamate || struck ;; Attack
affected ;; Influence
affected ;; Influence
killed ;; Killing
destroyed ;; Destroying || flooded ;; Destroying
occurred ;; Coming_to_be || crashed ;; Catastrophe || disaster ;; Catastrophe || landing ;; Self_motion || make ;; Motion
filling ;; Filling || left ;; Departing || conditioning ;; Education_teaching || smoke ;; Ingestion || broke ;; Destroying || disrupting ;; Destroying
used ;; Using || believed ;; Know || indicated ;; Statement || ventilated ;; Emptying
burst ;; Motion || shut down ;; Being_in_operation || pumped ;; Motion
died ;; Death || injuries ;; Bodily_harm
caused ;; Causation || tested ;; Testing || inquiry ;; Scrutiny || upgraded ;; Cause_to_make_progress || attributed ;; Causation
hijacking ;; Committing_crime || occurred ;; Coming_to_be
took place ;; Process_start
drunk ;; Ingestion
hijacking ;; Committing_crime || take place ;; Process_start || deaths ;; Death || injuries ;; Bodily_harm
detention ;; Arrest || sentenced ;; Legal_rulings
told ;; Telling || threatened ;; Warning || air ;; Expressing_publicly || planned ;; Arranging || gun ;; Use_firearm
placed ;; Placing
statement ;; Statement || demands ;; Request || talk to ;; Communication
surrounded ;; Surrounding || landed ;; Motion
released ;; Releasing || moved ;; Motion || exchange ;; Exchange || building ;; Building
drank ;; Ingestion || consumed ;; Ingestion || surrendered ;; Surrendering || exchange ;; Exchange
arrested ;; Arrest || stormed ;; Attack
hurricane ;; Catastrophe || using ;; Using || joint ;; Collaboration || cyclone ;; Motion || discontinuation ;; Process_end || designated ;; Creating
brought ;; Bringing || damage ;; Damaging
storm ;; Catastrophe || hurricane ;; Catastrophe || form ;; Coming_to_be || cyclone ;; Motion || form ;; Coming_to_be
formed ;; Coming_to_be || depression ;; Hindering
allowed ;; Preventing_or_letting || Tracking ;; Scrutiny || strengthening ;; Cause_change_of_strength
storm ;; Catastrophe || hurricane ;; Catastrophe || reached ;; Arriving
strengthening ;; Cause_change_of_strength || hurricane ;; Catastrophe || reached ;; Arriving
moving ;; Motion || made ;; Creating || weakened ;; Cause_change_of_strength
emerged ;; Coming_to_be
evacuations ;; Escaping || hurricane ;; Catastrophe || took place ;; Process_start || widespread ;; Dispersal
hurricane ;; Catastrophe || hurricane ;; Catastrophe
causing ;; Causation || storm ;; Catastrophe || damaging ;; Damaging || produced ;; Creating
hurricane ;; Catastrophe || associated ;; Collaboration || observed ;; Scrutiny
causing ;; Causation || widespread ;; Dispersal || blew out ;; Damaging || downed ;; Motion
rainfall ;; Motion || peaking ;; Cause_change_of_position_on_a_scale || reported ;; Reporting
resulting in ;; Causation || opposed ;; Agree_or_refuse_to_act || led to ;; Causation || flooding ;; Motion || damage ;; Damaging || rains ;; Motion
spawned ;; Coming_to_be || causing ;; Causation || damage ;; Damaging
death ;; Death || damage ;; Damaging || responsible ;; Imposing_obligation
occurred ;; Coming_to_be || attack ;; Attack
end ;; Process_end || took place ;; Process_start
captured ;; Conquering || taken ;; Conquering || demolished ;; Destroying || trained ;; Education_teaching || defence ;; Defending || rushed ;; Motion
counter-attacks ;; Attack || repulsed ;; Defending || see ;; Perception_active || smoke ;; Ingestion
crossed ;; Motion_directional || counter-attack ;; Attack || support ;; Supporting || organised ;; Arranging || conducted ;; Action
abandoned ;; GiveUp || counter-attack ;; Attack || made ;; Manufacturing
consolidated ;; Cause_change_of_strength || exposed ;; Reveal_secret || lost ;; Earnings_and_losses || defeated ;; Conquering
Battle ;; Hostile_encounter || violent ;; Violence
violent ;; Violence || event ;; Social_event || clashes ;; Quarreling || strike ;; Attack
changed ;; Change || defining ;; Communication || conduct ;; Action || characterised ;; Judgment_communication
violence ;; Violence || described ;; Statement || battle ;; Hostile_encounter || chase ;; Motion || rout ;; Destroying || example ;; Protest
depicted ;; Statement || attack ;; Attack || come ;; Motion || intimidation ;; Violence || reports ;; Reporting || upholding ;; Defending
violent ;; Violence || riot ;; Violence || charged ;; Judgment_communication
riot ;; Violence || punishable ;; Rewards_and_punishments
given ;; Giving || collapsed ;; Destroying || deemed ;; Know
breaking ;; Destroying || used ;; Using || called ;; Request || assist ;; Assistance || example ;; Protest || example ;; Protest || riot ;; Violence || said ;; Statement || strike ;; Motion
paid ;; Commerce_pay || unlawful ;; Legality || arrest ;; Arrest || prosecution ;; Legality
violence ;; Violence || violence ;; Violence || giving ;; Giving || arrested ;; Arrest || exaggerating ;; Cause_change_of_position_on_a_scale || prosecute ;; Criminal_investigation || reported ;; Reporting
used ;; Using || admitted ;; Response
published ;; Publishing || inquest ;; Criminal_investigation || documents ;; Writing || suggesting ;; Convincing
held ;; Defending || renewed ;; Recovering || led to ;; Causation || calls ;; Request
statement ;; Statement || announced ;; Expressing_publicly || campaign ;; Military_operation
Hurricane ;; Catastrophe || hurricane ;; Catastrophe || observed ;; Perception_active
formed ;; Coming_to_be || named ;; Name_conferral || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
moved ;; Motion || becoming ;; Becoming || striking ;; Attack || storm ;; Catastrophe || hurricane ;; Catastrophe
reached ;; Arriving || making ;; Causation
crossed ;; Motion_directional || passing ;; Motion_directional || hurricane ;; Catastrophe || state ;; Statement
became ;; Becoming
prompted ;; Causation || evacuations ;; Escaping || hurricane ;; Catastrophe || widespread ;; Dispersal
caused ;; Causation || damaged ;; Damaging || flash ;; Lighting || flooding ;; Catastrophe
deaths ;; Death || Damage ;; Damaging
hurricane ;; Catastrophe || passing ;; Motion_directional || damage ;; Damaging || cut ;; Bodily_harm || produced ;; Creating
reached ;; Arriving || made ;; Causation || gusted ;; Motion
caused ;; Causation || flooding ;; Motion || outages ;; Attack || knocked down ;; Damaging
damage ;; Damaging || killed ;; Killing
caused ;; Causation || flooding ;; Catastrophe
breaking ;; Destroying || storm ;; Catastrophe || damage ;; Damaging || paralleling ;; Self_motion || proved ;; Convincing
killed ;; Killing || caused ;; Causation || damage ;; Damaging
cyclone ;; Catastrophe || impact ;; Influence || Cyclone ;; Catastrophe || devastating ;; Destroying || record ;; Recording
developed ;; Coming_to_be || Forming ;; Coming_to_be || tracked ;; Self_motion
intensified ;; Cause_change_of_strength || attained ;; Achieve || storm ;; Catastrophe
storm ;; Catastrophe || underwent ;; Bodily_harm || transition ;; Change || neared ;; Placing || turning ;; Change
losing ;; Earnings_and_losses || storm ;; Catastrophe || bringing ;; Bringing || brushed ;; Damaging
caused ;; Causation || combination ;; Cause_to_amalgamate || damage ;; Damaging
lost ;; Earnings_and_losses || winds ;; Motion || flooded ;; Destroying
worsened ;; Cause_change_of_position_on_a_scale || brought ;; Bringing || storm ;; Catastrophe || bushfires ;; Catastrophe
burned ;; Bodily_harm || destroyed ;; Destroying || fires ;; Attack
resulted ;; Causation || fatalities ;; Death || attributed ;; Causation || fires ;; Catastrophe
damage ;; Damaging || resulting ;; Causation || losses ;; Earnings_and_losses || reaching ;; Achieve
held ;; Hold
work ;; Being_in_operation || held ;; Hold
works ;; Being_in_operation || holds ;; Hold || tournament ;; Competition
prestigious ;; Achieve
tournament ;; Competition
final ;; Process_end
matches ;; Competition || held ;; Hold
modified ;; Change || final ;; Process_end || eliminate ;; Cause_change_of_position_on_a_scale || tournament ;; Competition
given ;; Giving || mask ;; Wearing
returned ;; Arriving || tournament ;; Competition
Festival ;; Social_event
Festival ;; Social_event
sell ;; Commerce_sell || clothes ;; Wearing
include ;; Cause_to_be_included
donations ;; Supply || Festival ;; Social_event || sponsored ;; Supply
build ;; Building || help ;; Assistance || raised ;; Cause_change_of_position_on_a_scale
festival ;; Social_event || celebrating ;; Social_event || Festival ;; Social_event || held ;; Hold
held ;; Hold
festival ;; Social_event || takes place ;; Process_start
festival ;; Social_event || described ;; Statement || transported ;; Sending
festival ;; Social_event || costumes ;; Wearing || involving ;; Cause_to_be_included || displays ;; Change_sentiment
include ;; Cause_to_be_included
expansion ;; Expansion || began ;; Process_start || ended ;; Process_end || admission ;; Becoming_a_member || settlements ;; Statement || comprises ;; Cause_to_be_included
encouraged ;; Influence || giving rise to ;; Cause_change_of_position_on_a_scale || migration ;; Motion || settlement ;; Statement
settlement ;; Statement
arguing ;; Quarreling || asserted ;; Statement || promoted ;; Cause_change_of_position_on_a_scale || went ;; Self_motion || formation ;; Coming_to_be || Leading ;; Participation
expansion ;; Expansion || development ;; Cause_to_make_progress || furnish ;; Supply || forces ;; Causation || theorized ;; Know
inspired ;; Influence || conquest ;; Conquering || explore ;; Scrutiny || settlement ;; Statement
called ;; Name_conferral || focused ;; Aiming
violence ;; Violence || exaggerated ;; Cause_change_of_position_on_a_scale
spilled ;; Motion || inspired ;; Influence || shows ;; Presence || games ;; Competition || costumes ;; Wearing
defense ;; Defending || use ;; Using || formation ;; Coming_to_be || creation ;; Creating || defined ;; Statement || tells ;; Telling || development ;; Cause_to_make_progress
gave ;; Giving || conquest ;; Conquering || explain ;; Statement
treaties ;; Sign_agreement || conquest ;; Conquering || establishment ;; Creating || building ;; Building || expanded ;; Expansion || digging ;; Emptying || fulfilling ;; Achieve || military ;; Military_operation || compromise ;; Communication
violence ;; Violence || theorized ;; Statement || transformed ;; Change || focused ;; Aiming
took ;; Action || passed ;; Motion_directional || hold ;; Hold
choosing ;; Choosing || construct ;; Creating || taken ;; Action || produced ;; Creating
events ;; Social_event
conflicts ;; Hostile_encounter || involves ;; Participation
existed ;; Presence
led to ;; Causation || rivalling ;; Competition || taking ;; Creating
engagement ;; Hostile_encounter || Wars ;; Hostile_encounter
passing ;; Motion_directional || besieged ;; Besieging || encountered ;; Hostile_encounter || returning ;; Arriving
recognised ;; Know || flee ;; Escaping || turned ;; Change || closed ;; Self_motion
escape ;; Escaping || join ;; Participation || engagement ;; Hostile_encounter || gunshot ;; Attack || decided ;; Deciding || disabling ;; Destroying || rest ;; Preserving
found ;; Know || surrounded ;; Surrounding || manoeuvres ;; Self_motion || Turning ;; Change || respond ;; Response
destruction ;; Destroying || surrendered ;; Surrendering || escape ;; Escaping
Battle ;; Hostile_encounter || control ;; Control || destroyed ;; Destroying
reinforce ;; Cause_change_of_strength || expeditions ;; Attack || ended ;; Process_end || resupply ;; Supply
Battle ;; Hostile_encounter || recaptured ;; Conquering || rejoined ;; Becoming_a_member || fought ;; Hostile_encounter
loss ;; Earnings_and_losses || service ;; Supply || acquitted ;; Legal_rulings || returned ;; Arriving
fought ;; Hostile_encounter
attacked ;; Attack
reported ;; Reporting || formed ;; Creating
deployed ;; Arranging || response ;; Response
withdraw ;; Escaping || block ;; Hindering || inserted ;; Hiding_objects || destroying ;; Destroying
deployed ;; Arranging || armoured ;; Wearing
withdrawing ;; Escaping || ambush ;; Attack || remained ;; Presence
contacted ;; Communication
broke ;; Destroying || withdrew ;; Escaping
launched ;; Process_start || opposed ;; Agree_or_refuse_to_act
reinforced ;; Cause_change_of_strength || faced ;; Confronting_problem
assaulted ;; Attack || supported ;; Supporting
came ;; Motion
Surrounded ;; Surrounding || receiving ;; Receiving || advance ;; Cause_to_make_progress
used ;; Using || dispatched ;; Sending || secure ;; Getting
began ;; Process_start || arrived ;; Arriving || wounded ;; Bodily_harm
repulsed ;; Defending || counter-attacks ;; Attack
disabled ;; Destroying || killing ;; Killing
withdrew ;; Escaping || broke ;; Destroying
covered ;; Filling || pounded ;; Attack
avoiding ;; Preventing_or_letting || dead ;; Death || wounded ;; Bodily_harm || dragging ;; Motion || returned ;; Self_motion || find ;; Know || left ;; Departing
inflicting ;; Causation || matched ;; Connect
prevailed ;; Convincing || came to ;; Bringing
benefit ;; Earnings_and_losses || called ;; Name_conferral
marks ;; Recording || exhibit ;; Social_event || opening ;; Openness
exhibition ;; Social_event || exhibition ;; Social_event || sets ;; Placing || choose ;; Choosing || dress ;; Wearing || event ;; Social_event || exhibit ;; Social_event || celebrates ;; Social_event || match ;; Connect
mark ;; Recording || raise ;; Cause_change_of_position_on_a_scale || exhibit ;; Social_event || established ;; Creating || opening ;; Openness
well known ;; Know || left ;; Giving || event ;; Social_event || become ;; Becoming
social ;; Social_event || regarded ;; Perception_active
raised ;; Cause_change_of_position_on_a_scale || record ;; Recording || following ;; Self_motion
funding ;; Supply || contributions ;; Giving || surpass ;; Cause_change_of_position_on_a_scale || event ;; Social_event
observed ;; Perception_active || Hurricane ;; Catastrophe
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
became ;; Becoming || strengthened ;; Cause_change_of_strength || due ;; Causation || storm ;; Catastrophe
became ;; Becoming || entering ;; Arriving || hurricane ;; Catastrophe || hurricane ;; Catastrophe
strengthening ;; Cause_change_of_strength || storm ;; Catastrophe
became ;; Becoming
began ;; Process_start || developed ;; Coming_to_be || storm ;; Catastrophe || tracked ;; Self_motion
allowed ;; Preventing_or_letting || intensify ;; Cause_change_of_strength || hurricane ;; Catastrophe || reaching ;; Arriving
began ;; Process_start || weaken ;; Cause_change_of_strength || passed ;; Motion_directional || race ;; Competition || merging ;; Cause_to_amalgamate
reported ;; Reporting
observed ;; Perception_active || light ;; Lighting
hurricane ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
storm ;; Catastrophe || crossed ;; Motion_directional || situated ;; Placing || cyclone ;; Catastrophe || formed ;; Coming_to_be
storm ;; Catastrophe || strength ;; Cause_change_of_strength || oscillations ;; Motion || proved ;; Coming_to_believe
hurricane ;; Catastrophe || peaked ;; Cause_change_of_position_on_a_scale || dissipating ;; Dispersal
storm ;; Catastrophe
named ;; Name_conferral || storm ;; Catastrophe || hurricane ;; Catastrophe
caused ;; Causation || hurricane ;; Catastrophe || recorded ;; Recording || damage ;; Damaging || due ;; Causation || landfall ;; Arriving || track ;; Self_motion || fatality ;; Death || approached ;; Arriving || resulted in ;; Causation
targeted ;; Aiming || killing ;; Killing || overall ;; Wearing || suicide bombings ;; Terrorism
carried out ;; Action || bombing ;; Attack || attacks ;; Attack
bombing ;; Attack
suicide ;; Killing || bombing ;; Attack || took place ;; Process_start
killed ;; Killing || injured ;; Bodily_harm || hospitalized ;; Cure
occurred ;; Presence || attack ;; Attack
contained ;; Containing
captured ;; Conquering || explosion ;; Attack || station ;; Placing
attack ;; Attack || suicide bomber ;; Terrorism || known ;; Know || carried out ;; Attack
identified ;; Check
identity ;; Check || stating ;; Statement
reported ;; Reporting
reported ;; Reporting || convert ;; Change || changed ;; Change
appealed ;; Request || violence ;; Violence || abstain ;; Removing || find ;; Placing
reported ;; Reporting || bombing ;; Attack || involved ;; Cause_to_be_included
reported ;; Reporting || Increasing ;; Cause_change_of_position_on_a_scale
suicide ;; Killing || militant ;; Hostile_encounter || called ;; Request || terror ;; Terrorism || attacks ;; Attack || supporting ;; Supporting || attacks ;; Attack || attacks ;; Attack
attack ;; Attack || took ;; Action
treatment ;; Cure || flown ;; Motion || injured ;; Bodily_harm || ordered ;; Request
Battle ;; Hostile_encounter || fought ;; Hostile_encounter || battle ;; Hostile_encounter || taking place ;; Process_start
battle ;; Hostile_encounter || opposing ;; Agree_or_refuse_to_act || sighted ;; Know || fired ;; Attack
defensive ;; Defending || decided ;; Deciding || invade ;; Attack || occupy ;; Control || strengthen ;; Cause_change_of_strength
involved ;; Cause_to_be_included || plan ;; Arranging || accomplish ;; Achieve
included ;; Cause_to_be_included || provide ;; Supply || air ;; Expressing_publicly || cover ;; Hiding_objects
overall ;; Wearing || command ;; Control
sent ;; Sending || plan ;; Arranging || oppose ;; Response || learned ;; Know
command ;; Control
invaded ;; Attack || damaged ;; Damaging || occupied ;; Control || supporting ;; Supporting || attacks ;; Attack
destroying ;; Destroying || aware ;; Know || advanced ;; Arriving || locating ;; Placing
chosen ;; Choosing || searches ;; Scrutiny || opposing ;; Agree_or_refuse_to_act || brought ;; Causation
engaged ;; Participation || Beginning ;; Process_start
attacking ;; Attack || attacking ;; Attack || damaged ;; Damaging || sank ;; Motion_directional || believed ;; Know || scuttled ;; Destroying
found ;; Know || damaged ;; Damaging || damaged ;; Damaging || damaged ;; Damaging || scuttled ;; Destroying
damaged ;; Damaging || battle ;; Hostile_encounter || suffered ;; Bodily_harm || disengaged ;; Escaping || retired ;; Departing
recalled ;; Statement || loss ;; Earnings_and_losses || intending ;; Deciding
battle ;; Hostile_encounter || reasons ;; Protest || prove ;; Convincing
checked ;; Preventing_or_letting || marked ;; Recording || battle ;; Hostile_encounter
depleted ;; Expend_resource || damaged ;; Damaging || battle ;; Hostile_encounter || participate ;; Participation || participate ;; Participation || ensuring ;; Assistance || contributing ;; Bringing
helped ;; Assistance || prevented ;; Preventing_or_letting || invade ;; Attack || prompt ;; Causation
broke ;; Destroying || defenses ;; Defending || launched ;; Military_operation || surrender ;; Surrendering || contributing ;; Causation
known ;; Know || killing ;; Killing || damage ;; Damaging || storm ;; Catastrophe || leaving ;; Departing
formed ;; Coming_to_be
moved ;; Motion || struck ;; Attack || dropping ;; Motion
weakened ;; Cause_change_of_strength || strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || crossing ;; Motion_directional || storm ;; Catastrophe
caused ;; Causation || weakened ;; Cause_change_of_strength || increasing ;; Cause_change_of_position_on_a_scale || dissipate ;; Removing
killed ;; Killing || damage ;; Damaging || impact ;; Influence
caused ;; Causation || damaged ;; Damaging || destroyed ;; Destroying || left ;; Departing
impeded ;; Hindering || damage ;; Damaging || occurred ;; Coming_to_be
sent ;; Sending || relief ;; Assistance || clothing ;; Wearing
storm ;; Catastrophe || supply ;; Supply
struck ;; Attack || deaths ;; Death || flooding ;; Motion
affected ;; Influence || storm ;; Catastrophe
Campaign ;; Military_operation || Campaign ;; Military_operation || War ;; Hostile_encounter || operation ;; Military_operation
supply ;; Supply || operation ;; Military_operation || designed ;; Creating || cut ;; Removing
forces ;; Military_operation || Operation ;; Military_operation
advanced ;; Self_motion
destroy ;; Destroying || forces ;; Military_operation || forces ;; Military_operation || advancing ;; Self_motion || advancing ;; Self_motion
forces ;; Military_operation || dispatched ;; Sending
enabled ;; Preventing_or_letting || defence ;; Defending || forces ;; Military_operation || state ;; Statement || challenges ;; Competition || faced ;; Confronting_problem
Battle ;; Hostile_encounter || forces ;; Military_operation
forces ;; Military_operation || squeezed ;; Motion
operating ;; Being_in_operation || took ;; Conquering
supplies ;; Supply || reaching ;; Arriving || besieged ;; Besieging
evacuated ;; Removing
operating ;; Being_in_operation || transports ;; Sending || held ;; Defending
conduct ;; Action || withdrawal ;; Escaping || prevent ;; Preventing_or_letting || prevent ;; Preventing_or_letting || ordered ;; Arranging || resupply ;; Supply || states ;; Statement
begin ;; Process_start || gather ;; Come_together
put up ;; Placing || battles ;; Hostile_encounter || destroying ;; Destroying || took place ;; Process_start
escaping ;; Escaping || battle ;; Hostile_encounter || forces ;; Military_operation || losses ;; Earnings_and_losses || offensive ;; Hostile_encounter || suffered ;; Bodily_harm || evacuating ;; Removing || massacre ;; Killing || took place ;; Process_start
support ;; Supporting || operation ;; Military_operation
Battle ;; Hostile_encounter || losses ;; Earnings_and_losses || recover ;; Recovering || inflicted ;; Causation || transport ;; Sending
typhoon ;; Catastrophe
lightning ;; Lighting
moved ;; Motion || strengthened ;; Cause_change_of_strength || formed ;; Coming_to_be
strengthened ;; Cause_change_of_strength || making it ;; Causation || reach ;; Arriving || equaled ;; Cause_change_of_position_on_a_scale
turned ;; Becoming || storm ;; Catastrophe || reaching ;; Arriving || skirting ;; Wearing || landfall ;; Self_motion
became ;; Becoming
caused ;; Causation || damage ;; Damaging
amounted ;; Destroying
reported ;; Reporting
Cyclone ;; Catastrophe || cyclone ;; Catastrophe
identified ;; Check || moved ;; Motion || cyclone ;; Catastrophe || reaching ;; Arriving
weakened ;; Cause_change_of_strength || intensified ;; Cause_change_of_strength || preceding ;; Process_start
cyclone ;; Catastrophe || intensity ;; Cause_change_of_position_on_a_scale || intensity ;; Cause_change_of_position_on_a_scale || observed ;; Scrutiny
begun ;; Process_start || intensity ;; Cause_change_of_position_on_a_scale || result ;; Causation || noted ;; Statement
impact ;; Influence || result ;; Causation || lessened ;; Cause_change_of_position_on_a_scale
cyclone ;; Catastrophe || landfall ;; Arriving || damage ;; Damaging
impacts ;; Influence || losses ;; Earnings_and_losses || reached ;; Causation || suffered ;; Bodily_harm
Steeplechase ;; Competition
attracted ;; Causation
trained ;; Education_teaching
wore ;; Wearing
purchase ;; Commerce_buy
finishing ;; Process_end || rode ;; Body_movement
race ;; Competition || marred ;; Damaging || taking ;; Causation
spanned ;; Expansion
spawned ;; Coming_to_be || slow ;; Change_event_time || develop ;; Coming_to_be || gathered ;; Come_together
began ;; Process_start || became ;; Becoming || reaching ;; Arriving || organize ;; Arranging || depression ;; Catastrophe
hurricane ;; Catastrophe || strengthened ;; Cause_change_of_strength || landfall ;; Arriving
disrupted ;; Hindering
reformed ;; Change
picked up ;; Choosing || extended ;; Expansion
passed ;; Motion_directional || strengthened ;; Cause_change_of_strength || moved ;; Motion || disrupted ;; Hindering
upgraded ;; Cause_to_make_progress || landfall ;; Arriving || Reaching ;; Arriving || brushed ;; Motion
regained ;; Recovering
traveling ;; Traveling || encountered ;; Motion
counter ;; Response || landfall ;; Arriving
Moving ;; Motion || weakened ;; Cause_change_of_strength
bombing ;; Attack || carried out ;; Action
aimed ;; Aiming || attending ;; Participation
carrying ;; Carry_goods || detonated ;; Attack || disguised ;; Wearing || entered ;; Arriving
killed ;; Killing
wounded ;; Bodily_harm
ended ;; Process_end || meeting ;; Social_event
bombing ;; Attack || bombing ;; Attack || casualties ;; Catastrophe || attacks ;; Attack || attack ;; Attack
attacks ;; Attack || killed ;; Killing || followed ;; Self_motion || resulted in ;; Causation
attack ;; Attack || massacre ;; Killing
record ;; Recording || produced ;; Causation
developed ;; Coming_to_be || pressure ;; Convincing || remained ;; Temporary_stay || moving ;; Motion
reached ;; Arriving || verified ;; Check
winds ;; Motion || due ;; Causation || left ;; Departing
storm ;; Catastrophe || storm ;; Catastrophe || storm ;; Catastrophe || remained ;; Temporary_stay
peaked ;; Cause_change_of_position_on_a_scale || reached ;; Arriving
caused ;; Causation || storm ;; Catastrophe || combination ;; Cause_to_amalgamate || flooding ;; Catastrophe
storm ;; Catastrophe || due ;; Causation
caused ;; Causation || damaged ;; Damaging || damage ;; Damaging
increased ;; Cause_change_of_position_on_a_scale || due ;; Causation
reached ;; Arriving || flooded ;; Destroying
destroyed ;; Destroying || left ;; Causation
lost ;; Earnings_and_losses || storm ;; Catastrophe
destroyed ;; Destroying || flooding ;; Catastrophe || reached ;; Arriving
storm ;; Catastrophe || produced ;; Causation
totals ;; Destroying
caused ;; Causation || storm ;; Catastrophe || damage ;; Damaging || deaths ;; Death || storm ;; Catastrophe
caused ;; Causation || Storm ;; Catastrophe || damage ;; Damaging
forces ;; Causation || related ;; Cause_to_be_included || engagements ;; Hostile_encounter || took place ;; Process_start
taking place ;; Process_start
included ;; Cause_to_be_included || called ;; Request
attacks ;; Attack || attacks ;; Attack || defend ;; Defending || attacks ;; Attack || forces ;; Causation || forces ;; Causation || called ;; Name_conferral || supply ;; Supply || reporting ;; Reporting || guarded ;; Patrolling || directed ;; Aiming
attacked ;; Attack || command ;; Control
forces ;; Causation || attacks ;; Attack || destroy ;; Destroying || disrupt ;; Destroying || attacks ;; Attack || retreating ;; Escaping
attacks ;; Attack || command ;; Control
escaped ;; Escaping || surrounded ;; Surrounding || forces ;; Causation || took ;; Conquering || losses ;; Earnings_and_losses || landing ;; Motion
crossed ;; Motion_directional || attacked ;; Attack || forces ;; Causation || command ;; Control || casualties ;; Damaging
retreat ;; Escaping || resulted in ;; Causation || forced ;; Motion || planned ;; Arranging || hindered ;; Preventing_or_letting
took place ;; Process_start
ordered ;; Arranging || occupy ;; Control
used ;; Using || consolidated ;; Cause_to_amalgamate
armoured ;; Wearing || defeated ;; Conquering
spoil ;; Destroying || captured ;; Conquering
write ;; Writing || prepare ;; GetReady
administered ;; Control || annexed ;; Becoming_a_member
unrest ;; Violence || annexation ;; Change_of_leadership || revolution ;; Change_of_leadership || took place ;; Process_start
service ;; Supply || meeting ;; Social_event || discuss ;; Communication || extrication ;; Rescuing || convened ;; Come_together
remarked ;; Statement || meeting ;; Social_event || start ;; Process_start || returning ;; Arriving
demonstrations ;; Social_event || held ;; Hold
took over ;; Conquering || captured ;; Conquering || conducting ;; Action || masked ;; Wearing || led to ;; Causation || installation ;; Change_of_leadership || declaration ;; Expressing_publicly || independence ;; Change_of_leadership
incorporated ;; Cause_to_amalgamate
condemned ;; Judgment_communication || annexation ;; Change_of_leadership || consider ;; Deciding || violation ;; Legality || establishing ;; Creating
sanctions ;; Rewards_and_punishments || led to ;; Causation || suspending ;; Change_event_time
rejected ;; Agree_or_refuse_to_act || assembly ;; Building || recognised ;; Know || annexation ;; Change_of_leadership || vote ;; Change_of_leadership || affirming ;; Judgment_communication || adopting ;; Choosing
annexation ;; Change_of_leadership || resolution ;; Statement || underscores ;; Protest || referendum ;; Change_of_leadership || alteration ;; Change || form ;; Coming_to_be || calls ;; Expressing_publicly || imply ;; Coming_to_believe || recognition ;; Supporting || recognize ;; Coming_to_believe
reaffirmed ;; Statement || condemned ;; Judgment_communication || assembly ;; Building || annexation ;; Change_of_leadership || occupation ;; Control
opposes ;; Agree_or_refuse_to_act || referendum ;; Change_of_leadership || defending ;; Defending
said ;; Statement || integrated ;; Cause_to_amalgamate
hurricane ;; Catastrophe || caused ;; Causation
drifted ;; Motion || arriving ;; Arriving || detected ;; Know
approached ;; Arriving || hurricane ;; Catastrophe
reached ;; Arriving || departing ;; Departing || gale ;; Motion
caused ;; Causation || coming ;; Motion || damaged ;; Damaging || destroyed ;; Destroying || storm ;; Catastrophe || hurricane ;; Catastrophe || impact ;; Influence || grounded ;; Connect || capsized ;; Destroying
noted ;; Writing || damage ;; Damaging
Flood ;; Catastrophe || brought ;; Bringing
existence ;; Presence || hurricane ;; Catastrophe || inflicted ;; Causation
competition ;; Competition || match ;; Competition
match ;; Competition || played ;; Competition
match ;; Competition || contested ;; Competition
win ;; Getting
lost ;; Earnings_and_losses || dressing ;; Wearing || wore ;; Wearing || play ;; Competition
watched ;; Perception_active || gathered ;; Come_together
named ;; Name_conferral || took over ;; Change_of_leadership
changes ;; Change || replaced ;; Change
declared ;; Expressing_publicly
Tour ;; Traveling
supported ;; Supporting
planned ;; Arranging || restricted ;; Hindering
decided ;; Deciding || became ;; Becoming || send ;; Sending
began ;; Process_start || announcement ;; Expressing_publicly || production ;; Creating
collaborated ;; Collaboration
signed ;; Sign_agreement || opening ;; Openness || record ;; Recording
lighting ;; Lighting || included ;; Cause_to_be_included
set ;; Placing
moved ;; Motion
ended ;; Process_end || performing ;; Hold || wedding ;; Forming_relationships
received ;; Receiving || critics ;; Judgment_communication
announced ;; Expressing_publicly || sold ;; Commerce_sell
bought ;; Commerce_buy || flooded ;; Motion
reported ;; Reporting || end ;; Process_end || reporting ;; Reporting
released ;; Publishing || received ;; Receiving || tour ;; Traveling || recorded ;; Recording
attend ;; Participation || attired ;; Wearing
gave rise to ;; Causation || called ;; Name_conferral || recognized ;; Know
hurricane ;; Catastrophe
named ;; Name_conferral || formed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricanes ;; Catastrophe
Moving ;; Motion || storm ;; Catastrophe || intensified ;; Cause_change_of_strength || reaching ;; Arriving || hurricane ;; Catastrophe || strength ;; Cause_change_of_strength
intensified ;; Cause_change_of_strength || reaching ;; Arriving || hurricane ;; Catastrophe || open ;; Openness
hurricane ;; Catastrophe || curved ;; Motion_directional || fluctuated ;; Motion
passed ;; Process_end || turned ;; Becoming
affecting ;; Influence || storm ;; Catastrophe || Weakening ;; Cause_change_of_strength || reached ;; Arriving || transitioned ;; Change || dissipated ;; Dispersal
caused ;; Causation || Due ;; Causation || damage ;; Damaging
reconnaissance ;; Scrutiny || postponed ;; Change_event_time || hurricane ;; Catastrophe || passed ;; Motion_directional || causing ;; Causation || damage ;; Damaging || hurricane ;; Catastrophe || due ;; Causation || damage ;; Damaging
storm ;; Catastrophe || transitioning ;; Change || encountered ;; Hostile_encounter || resulting in ;; Causation || deaths ;; Death
storm ;; Catastrophe || brought ;; Bringing || storm ;; Catastrophe || claimed ;; Statement
helped ;; Assistance || hurricane ;; Catastrophe || open ;; Openness || attain ;; Getting || hurricane ;; Catastrophe || records ;; Recording
tour ;; Traveling
performed ;; Hold || performed ;; Hold
cost ;; Cost || estimated ;; Statement
grossed ;; Earnings_and_losses
tour ;; Traveling
opened ;; Openness || moved ;; Motion
began ;; Process_start || ended ;; Process_end
ending ;; Process_end || included ;; Cause_to_be_included || resuming ;; Process_start
ending ;; Process_end || confirmed ;; Check || beginning ;; Process_start || returning ;; Arriving
production ;; Manufacturing
returned ;; Arriving
confirming ;; Check || claimed ;; Statement
requested ;; Request || send ;; Sending || incorporated ;; Cause_to_amalgamate || died ;; Death
confirmed ;; Check
confirmed ;; Check || withdrew ;; Escaping
replaced ;; Change_of_leadership
announced ;; Expressing_publicly
confirmed ;; Check || performed ;; Hold
playing ;; Participation || appeared ;; Presence || joined ;; Becoming_a_member
made ;; Manufacturing
rehearsing ;; Practice || reported ;; Reporting
used ;; Using || work out ;; Know
invited ;; Request || performance ;; Hold
directed ;; Action
called ;; Request || Revolution ;; Change_of_leadership || fought ;; Hostile_encounter || combined ;; Cause_to_amalgamate || forces ;; Hostile_encounter
changed ;; Change || fight ;; Hostile_encounter || opposed ;; Agree_or_refuse_to_act
wars ;; Hostile_encounter || lasted ;; Process_end
formed ;; Coming_to_be || included ;; Cause_to_be_included
wars ;; Hostile_encounter || defeated ;; Conquering || proclaimed ;; Statement
hand ;; Giving || use ;; Using || hand-held ;; Bringing
ended ;; Process_end || fighting ;; Hostile_encounter || defeated ;; Conquering
agreed ;; Agree_or_refuse_to_act || submit ;; Submitting_documents || allowed ;; Preventing_or_letting || practice ;; Practice
sinking ;; Motion_directional || resulted in ;; Causation
escorted ;; Patrolling
patrolling ;; Patrolling || searching ;; Scrutiny || sighted ;; Know
exercises ;; Practice || warfare ;; Hostile_encounter
flanked ;; Defending || get ;; Getting
intercept ;; Attack
spotted ;; Know || flying ;; Motion || sighted ;; Know
approach ;; Arriving || felt ;; Change_sentiment
watch ;; Perception_active || sighted ;; Know
fired ;; Use_firearm
shifted ;; Change || spotted ;; Know
hit ;; Attack
decided ;; Deciding || head ;; Self_motion
counterattacked ;; Defending
began ;; Process_start || fire ;; Use_firearm || sink ;; Motion_directional
detonated ;; Attack
picked up ;; Choosing || lost ;; Death
slip ;; Motion_directional || returned ;; Arriving
bombing ;; Attack || using ;; Using || using ;; Using || operation ;; Military_operation
bombing ;; Attack || support ;; Supporting || air ;; Expressing_publicly || used ;; Using || close ;; Openness || command ;; Control
operating ;; Being_in_operation || bombing ;; Attack || provided ;; Supply || command ;; Control || control ;; Control || bomb ;; Attack || designated ;; Choosing
included ;; Cause_to_be_included || wind ;; Motion || Planning ;; Arranging || providing ;; Supply || tracking ;; Recording || bomb ;; Attack || radiating ;; Lighting
begin ;; Process_start || solve ;; Resolve_problem || designated ;; Choosing
commands ;; Control || bomb ;; Attack || adjust ;; Using
directed ;; Aiming
training ;; Education_teaching || practice ;; Practice
authorized ;; Preventing_or_letting
recorded ;; Recording || strike ;; Attack || communications ;; Communication
attacks ;; Attack || start ;; Process_start || conflict ;; Hostile_encounter || led ;; Causation
decided ;; Deciding || start ;; Process_start || insurgency ;; Violence || held ;; Hold || preparing ;; GetReady
sent ;; Sending || Training ;; Practice || opened ;; Process_start || contact ;; Communication || propaganda ;; Communication
attacks ;; Attack || launched ;; Military_operation || preparation ;; GetReady
attack ;; Attack || led ;; Causation
killed ;; Killing || attacked ;; Attack || injured ;; Bodily_harm
killed ;; Killing || attacked ;; Attack || injured ;; Bodily_harm
raid ;; Attack || killed ;; Killing || killed ;; Killing || attacks ;; Attack || followed ;; Arriving || attack ;; Attack || attack ;; Attack || ambush ;; Attack
violence ;; Violence || attacks ;; Attack || escalated ;; Cause_change_of_position_on_a_scale
killed ;; Killing || conflict ;; Hostile_encounter
rose ;; Cause_change_of_position_on_a_scale || puts ;; Placing || insurgency ;; Violence || killed ;; Killing
organization ;; Arranging || held ;; Hold
Located ;; Placing || site ;; Placing
attendance ;; Traveling
using ;; Using || practices ;; Practice
begins ;; Process_start
organization ;; Collaboration
serve ;; Assistance
fall ;; Motion_directional || work ;; Employment
collision ;; Catastrophe || collided ;; Catastrophe
taken off ;; Departing || carrying ;; Sending
operated ;; Being_in_operation
exercise ;; Practice
collided ;; Catastrophe
fell ;; Motion_directional
blamed ;; Judgment_communication || crash ;; Catastrophe || accident ;; Catastrophe || accident ;; Catastrophe || investigation ;; Criminal_investigation || concluded ;; Coming_to_believe
assault ;; Attack
withdrew ;; Escaping || lost ;; Death || assault ;; Attack || defeated ;; Conquering || Launched ;; Motion || party ;; Participation
carried ;; Carry_goods || amputated ;; Removing || wounded ;; Bodily_harm
strike ;; Attack || defeated ;; Conquering || failed ;; Earnings_and_losses
frustrated ;; Influence || escape ;; Escaping || sailed ;; Self_motion
demanded ;; Request || subdue ;; Hindering || battered ;; Attack || orders ;; Arranging || blockade ;; Besieging
thought ;; Coming_to_believe || guaranteed ;; Commitment || attack ;; Attack
proved ;; Coming_to_believe
besieged ;; Besieging || repelled ;; Removing
organized ;; Arranging
savaged ;; Attack || striking ;; Attack || manoeuvred ;; Practice
opened ;; Openness || causing ;; Causation || attackers ;; Attack || drove ;; Motion || joined ;; Cause_to_amalgamate || allowing ;; Preventing_or_letting
results ;; Causation || spread ;; Dispersal || stretched ;; Body_movement
arrived ;; Arriving || gaze ;; Perception_active || shifted ;; Change || hearing ;; Know
sent ;; Sending
dispatched ;; Sending || attack ;; Attack || promoted ;; Cause_to_make_progress || Encouraged ;; Influence
occurred ;; Coming_to_be || meltdown ;; Catastrophe || leak ;; Reveal_secret
began ;; Process_start || followed ;; Motion
recognize ;; Know || inadequate ;; Cause_to_make_progress || training ;; Practice || compounded ;; Cause_to_amalgamate
believed ;; Know || hidden ;; Hiding_objects || causing ;; Causation || led to ;; Causation
crystallized ;; Change || resulted in ;; Causation
cited ;; Judgment_communication
resulted in ;; Causation
expressed ;; Expressing_publicly || effects ;; Influence
determined ;; Deciding || linking ;; Connect || substantiated ;; Check || increase ;; Cause_change_of_position_on_a_scale || studies ;; Scrutiny || analyzing ;; Know
started ;; Process_start || ended ;; Process_end || cost ;; Cost
end ;; Process_end
bombarded ;; Attack || command ;; Control
suppress ;; Hold || campaign ;; Military_operation
free ;; Releasing || stop ;; Process_end
freed ;; Releasing || signed ;; Sign_agreement || bombardment ;; Attack
end ;; Process_end || conquest ;; Conquering || practice ;; Practice
signed ;; Sign_agreement || end ;; Process_end || conflicts ;; Hostile_encounter
decided ;; Deciding || created ;; Creating || religion ;; Rite || religion ;; Rite
jurisdiction ;; Control || practiced ;; Practice || recognized ;; Know
kept ;; Temporary_stay || treaty ;; Sign_agreement || secret ;; Hiding_objects || publicized ;; Publishing
plan ;; Arranging || victories ;; Earnings_and_losses || Edict ;; Legal_rulings || War ;; Hostile_encounter || overturned ;; Process_end
helped ;; Assistance || rekindle ;; Recovering || changing ;; Change || conflict ;; Hostile_encounter || persecution ;; Bodily_harm || War ;; Hostile_encounter || war ;; Hostile_encounter || overturning ;; Process_end
concerts ;; Social_event || support ;; Supporting || Held ;; Social_event || campaign ;; Social_event
performed ;; Hold
recorded ;; Recording
Wearing ;; Wearing
sold ;; Commerce_sell
received ;; Receiving || commended ;; Judgment_communication || perform ;; Self_motion
released ;; Publishing || concert ;; Social_event || titled ;; Name_conferral || traveling ;; Traveling || partying ;; Social_event || rehearsal ;; Practice || performances ;; Hold || wedding ;; Forming_relationships
Capture ;; Conquering || attack ;; Attack
repulsed ;; Defending || attacks ;; Attack
began ;; Process_start || became ;; Becoming || bombardment ;; Attack || bombardment ;; Attack || bombardment ;; Attack || fired ;; Use_firearm || shelling ;; Use_firearm || soften ;; Cause_change_of_strength
held ;; Defending
conduct ;; Action || attack ;; Attack
attacked ;; Attack || attack ;; Attack || concealed ;; Hiding_objects || made ;; Action
took ;; Conquering || captured ;; Conquering || attack ;; Attack || advanced ;; Self_motion
reach ;; Arriving || keeping ;; Communication || turned ;; Motion
took ;; Conquering || established ;; Building
counter-attack ;; Attack || recover ;; Recovering
ascribed ;; Coming_to_believe || training ;; Practice || barrages ;; Use_firearm || depressed ;; Cause_change_of_position_on_a_scale || thickened ;; Change || blinded ;; Perception_active
advanced ;; Self_motion
began ;; Process_start || called ;; Name_conferral || involved ;; Cause_to_be_included || shooting ;; Use_firearm
sparked ;; Process_start || debate ;; Quarreling
expanded ;; Expansion || include ;; Cause_to_be_included
deployed ;; Arranging || shooting ;; Use_firearm || emerged ;; Coming_to_be || established ;; Creating
unrest ;; Violence || shooting ;; Use_firearm || looting ;; Robbery
reports ;; Reporting || dealing ;; Resolve_problem
shooting ;; Use_firearm
shot ;; Use_firearm
conducted ;; Action || response ;; Response || shooting ;; Use_firearm
conduct ;; Action || determined ;; Deciding || announced ;; Expressing_publicly || misconduct ;; Action || applying ;; Using || practice ;; Practice || engaged ;; Participation || discriminating ;; Judgment_communication
found ;; Know || generated ;; Creating || depended ;; Cause_to_make_progress
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || Campaign ;; Military_operation || called ;; Name_conferral || War ;; Military_operation
supply ;; Supply || used ;; Using || took place ;; Process_start
military ;; Military_operation || ambushes ;; Attack
Campaign ;; Military_operation || supply ;; Supply || clearing ;; Emptying
battle ;; Hostile_encounter || campaign ;; Military_operation || gain ;; Getting || used ;; Using || test ;; Testing
battle ;; Hostile_encounter || resulted in ;; Causation
devastated ;; Destroying || ceased ;; Process_end || exist ;; Preserving
War ;; Military_operation
fought ;; Hostile_encounter || control ;; Control || forces ;; Military_operation || forces ;; Military_operation || re-establish ;; Recovering || War ;; Military_operation
forces ;; Military_operation || dealing ;; Arranging
improved ;; Cause_to_make_progress || led ;; Causation || defeated ;; Conquering
gave ;; Giving || support ;; Supporting || party ;; Participation || provide ;; Giving || training ;; Practice
military ;; Military_operation || offensive ;; Attack || launched ;; Process_start
control ;; Control || giving ;; Giving || developed ;; Cause_to_make_progress
became ;; Becoming
supply ;; Supply || lay ;; Placing || overwhelmed ;; Destroying || cutting ;; Change
took ;; Conquering || retook ;; Conquering
grew ;; Cause_to_make_progress
beginning ;; Process_start || forces ;; Military_operation || support ;; Supporting || comprised ;; Cause_to_be_included
began ;; Process_start || harassing ;; Attack
responded ;; Response || dismantling ;; Destroying || forces ;; Military_operation
assault ;; Attack || planned ;; Arranging || launch ;; Process_start
attacked ;; Attack
garrisoned ;; Besieging
escaped ;; Escaping || fighting ;; Hostile_encounter || overrun ;; Attack
killed ;; Killing || taken ;; Conquering || prisoner ;; Arrest
reinforced ;; Cause_change_of_strength || parachuted ;; Self_motion
assembled ;; Come_together || waited ;; Temporary_stay
comprised ;; Cause_to_be_included
planned ;; Arranging || said ;; Statement || raid ;; Attack || capturing ;; Arrest || offensive ;; Attack || launched ;; Process_start
decided ;; Deciding || evacuate ;; Emptying
destroy ;; Destroying || ordered ;; Request || evacuate ;; Emptying
fight ;; Hostile_encounter || plan ;; Arranging || retake ;; Conquering || holding ;; Conquering
comprised ;; Cause_to_be_included
competing ;; Competition || took place ;; Process_start
known ;; Know
sent ;; Sending
competed ;; Competition || finals ;; Process_end || qualified ;; Becoming_a_member
decided ;; Deciding
connected ;; Connect || included ;; Cause_to_be_included || held ;; Hold
included ;; Cause_to_be_included || put ;; Placing
participants ;; Participation || final ;; Process_end || excluded ;; Preventing_or_letting || qualifiers ;; Preventing_or_letting || invited ;; Choosing
served ;; Giving || qualifying ;; Becoming_a_member
compete ;; Competition || compete ;; Competition || eligible ;; Becoming_a_member || eligible ;; Becoming_a_member
practiced ;; Practice || shooting ;; Use_firearm
took place ;; Process_start
contested ;; Competition || performing ;; Hold
final ;; Process_end || qualifying ;; Becoming_a_member
occurred ;; Coming_to_be || Incident ;; Catastrophe || landed ;; Arriving
operating ;; Being_in_operation || undertaking ;; Process_start || exercise ;; Practice
launched ;; Motion || tasked ;; Request || locating ;; Placing || switched ;; Motion
search ;; Scrutiny || completing ;; Process_end || return ;; Departing || locate ;; Placing
contact ;; Connect || stopped ;; Process_end || Running ;; Motion || headed ;; Motion
noticed ;; Know || provided ;; Supply || planned ;; Arranging || landing ;; Motion || eject ;; Motion
delivered ;; Sending || carrying ;; Carry_goods
approach ;; Arriving || shipping ;; Sending || spare ;; Expend_resource || landed ;; Arriving
began ;; Process_start || touched down ;; Arriving || slide ;; Motion
parked ;; Placing || retract ;; Removing || arrest ;; Arrest || slipped ;; Motion_directional
held up ;; Hindering || stopped ;; Preventing_or_letting
arrived at ;; Arriving || perched ;; Placing
event ;; Confronting_problem || received ;; Receiving
awarded ;; Giving
sent ;; Sending || found ;; Know || completed ;; Process_end || training ;; Education_teaching
blamed ;; Judgment_communication || criticised ;; Judgment_communication
reprimanded ;; Judgment_communication || reassigned ;; Employment
flying ;; Motion || returned ;; Arriving || accrued ;; Come_together || resigning ;; Change_of_leadership
converted ;; Change || retired ;; Departing
stroke ;; Motion || birdied ;; Body_movement
victories ;; Getting
open ;; Openness
played ;; Competition
tied ;; Connect || made ;; Getting
injury ;; Bodily_harm || play ;; Participation
received ;; Receiving || lost ;; Earnings_and_losses || practice ;; Practice
informed ;; Telling || allowed ;; Preventing_or_letting || self-reported ;; Action || occurred ;; Presence
tied ;; Connect
modified ;; Change || include ;; Cause_to_be_included
made ;; Getting || made ;; Getting
introduced ;; Creating
named ;; Name_conferral || rave ;; Social_event
founded ;; Creating
designed ;; Creating
exercise ;; Practice || drugs ;; Bodily_harm || form ;; Coming_to_be || reengage ;; Influence || impacts ;; Influence
event ;; Social_event || occurs ;; Presence
expanded ;; Expansion
includes ;; Cause_to_be_included || event ;; Social_event
event ;; Social_event
play ;; Participation
support ;; Supporting || coming ;; Motion || perform ;; Action
flight ;; Motion || training ;; Education_teaching || accident ;; Catastrophe || crash ;; Catastrophe || crashed ;; Catastrophe || occurred ;; Coming_to_be
accident ;; Catastrophe || exercise ;; Practice || took place ;; Process_start
died ;; Death
sent ;; Sending || forces ;; Causation
sent ;; Sending || assist ;; Assistance
found ;; Placing
crash ;; Catastrophe || investigation ;; Statement || concluded ;; Coming_to_believe
decide ;; Deciding || game ;; Competition || conference ;; Social_event || conference ;; Social_event
defeated ;; Conquering
win ;; Getting || lost ;; Earnings_and_losses
reached ;; Arriving || play ;; Competition || return ;; Arriving || lose ;; Earnings_and_losses
played ;; Competition
postponed ;; Change_event_time || attacks ;; Attack || moved ;; Motion || Following ;; Self_motion
becoming ;; Becoming || played ;; Competition || rescheduled ;; Arranging
headlined ;; Communication
terrorist ;; Terrorism || attacks ;; Attack || following ;; Self_motion
practice ;; Practice || replaced ;; Change_of_leadership || established ;; Creating
played ;; Competition || slammed ;; Motion
marked ;; Recording
posted ;; Sending || record ;; Recording
defense ;; Defending || ended ;; Process_end || posting ;; Sending || record ;; Recording || scoring ;; Getting || clinched ;; Getting
built ;; Building
run ;; Self_motion || remaining ;; Presence || negated ;; Preventing_or_letting || threw ;; Motion
set up ;; Competition || expired ;; Death
passes ;; Motion_directional || named ;; Name_conferral || completed ;; Process_end
shot down ;; Use_firearm
carried ;; Carry_goods
visiting ;; Traveling
defense ;; Defending || took place ;; Process_start || exercises ;; Practice
research ;; Research || exercises ;; Practice || training ;; Practice || held ;; Defending
armed ;; Bearing_arms || fired ;; Use_firearm || caused ;; Causation
ended up ;; Process_end
event ;; Social_event || erected ;; Building
experiment ;; Testing || influenced ;; Influence
organized ;; Arranging || takes ;; Getting || occurs ;; Coming_to_be || burning ;; Damaging || built ;; Creating || spanning ;; Change_event_time
ran ;; Process_start || enjoyed ;; Perception_active || created ;; Creating || explores ;; Testing
elaborated ;; Statement
Said ;; Statement || considered ;; Coming_to_believe || gathering ;; Come_together || evolved ;; Coming_to_be
Participation ;; Participation || giving ;; Giving || encouraged ;; Preventing_or_letting
include ;; Cause_to_be_included
chosen ;; Choosing || inspired ;; Influence
organized ;; Arranging || considered ;; Coming_to_believe || succeeded ;; Influence || formed ;; Creating
attended ;; Participation
sold ;; Commerce_sell || Attendance ;; Participation || attendance ;; Participation || rose ;; Cause_change_of_position_on_a_scale
events ;; Social_event || endorses ;; Agree_or_refuse_to_act || inspired ;; Influence
devastated ;; Destroying || War ;; Hostile_encounter || conflict ;; Hostile_encounter
Reformation ;; Reforming_a_system || Revolt ;; Hostile_encounter || Wars ;; Hostile_encounter || occurred ;; Presence
tested ;; Testing || included ;; Cause_to_be_included || called ;; Name_conferral
excluded ;; Preventing_or_letting || determining ;; Deciding || reserved ;; Preserving
converted ;; Change || resign ;; Change_of_leadership || force ;; Request || stipulated ;; Statement
converted ;; Change
required ;; Imposing_obligation
declared ;; Expressing_publicly || convert ;; Change || married ;; Forming_relationships
elected ;; Change_of_leadership
competing ;; Competition || fought ;; Hostile_encounter
obligations ;; Imposing_obligation || held ;; Hold || holding ;; Hold
magnified ;; Expansion
expanded ;; Expansion || support ;; Supporting || involvement ;; Participation
encouraging ;; Influence
war ;; Hostile_encounter || conclusion ;; Coming_to_believe || revival ;; Recovering || consolidated ;; Cause_change_of_strength || encouraged ;; Influence
set ;; Placing
test ;; Testing
conducted ;; Action || joint ;; Collaboration
preceded ;; Process_start
test ;; Testing
defense ;; Defending || tested ;; Testing || used ;; Using
demonstrated ;; Protest || test ;; Testing
conducted ;; Action || controlled ;; Control || using ;; Using || limits ;; Limiting
increases ;; Cause_change_of_position_on_a_scale || surrounding ;; Surrounding
named ;; Name_conferral || shots ;; Use_firearm
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || Liberation ;; Change_of_leadership
cross ;; Motion_directional || tasked ;; Request
named ;; Name_conferral
chosen ;; Choosing || crossing ;; Motion_directional
placed ;; Placing || support ;; Supporting || supply ;; Supply || advance ;; Self_motion
laid ;; Giving
provided ;; Supply || fire ;; Use_firearm || required ;; Supply
hold ;; Defending || crossing ;; Sending || crossing ;; Motion_directional || place ;; Placing
support ;; Supporting
provide ;; Supply || crossing ;; Sending || moved ;; Self_motion || test ;; Testing
left ;; Departing || command ;; Control || commanding ;; Control
arranged ;; Arranging || crossing ;; Motion_directional || ordered ;; Arranging || stating ;; Statement
simulate ;; Hold || carried out ;; Military_operation || test ;; Testing
conducted ;; Action || participation ;; Participation
examined ;; Testing
detonated ;; Attack
moved ;; Motion || transit ;; Motion || test ;; Testing
left ;; Departing || destroyed ;; Destroying
determine ;; Deciding || stated ;; Statement || using ;; Using || speculation ;; Coming_to_believe
included ;; Cause_to_be_included || used ;; Using
Tournament ;; Competition || tournament ;; Competition || held ;; Hold
tournament ;; Competition || test ;; Testing || involve ;; Cause_to_be_included
match ;; Competition || defeated ;; Conquering || qualify ;; Getting
tournament ;; Competition || known ;; Coming_to_believe
final ;; Process_end || event ;; Competition || defeated ;; Conquering || win ;; Earnings_and_losses
compete ;; Competition || compete ;; Competition || tournament ;; Competition || teams ;; Participation || teams ;; Competition || inaugurated ;; Process_start
caused ;; Causation || posted ;; Sending || proven ;; Convincing
examined ;; Testing || suggestions ;; Convincing || reviewed ;; Testing || made ;; Manufacturing
recognized ;; Know || trial ;; Testing
saved ;; Rescuing || suffered ;; Bodily_harm || damage ;; Damaging
solved ;; Resolve_problem || drew ;; Motion
became ;; Becoming
carried out ;; Action || tests ;; Testing || bombs ;; Attack
initiated ;; Process_start || becoming ;; Becoming || defence ;; Defending
merged ;; Cause_to_amalgamate
participated ;; Participation
lose ;; Earnings_and_losses || resumed ;; Process_start || research ;; Research
test ;; Testing
decided ;; Deciding || develop ;; Coming_to_be
produced ;; Manufacturing || research ;; Research
test ;; Research || tests ;; Testing
dropped ;; Motion_directional
confirm ;; Check || become ;; Becoming || explosion ;; Attack || reports ;; Reporting || hailed ;; Judgment_communication
test ;; Testing
made it ;; Causation || weapon ;; Bearing_arms
achieved ;; Achieve
tested ;; Testing
test ;; Research || required ;; Imposing_obligation
test ;; Testing
exceeded ;; Cause_change_of_position_on_a_scale
test ;; Testing || tested ;; Testing
tested ;; Testing || remains ;; Presence
making it ;; Causation || bomb ;; Attack || understood ;; Know
tested ;; Testing || caused ;; Causation || tests ;; Testing || boosting ;; Cause_to_make_progress || making ;; Manufacturing
test ;; Research
testing ;; Testing || testing ;; Testing || came into effect ;; Change
involved ;; Participation || Operation ;; Military_operation || test ;; Testing
conducted ;; Action
involved ;; Participation
purpose ;; Aiming || determine ;; Deciding || detonated ;; Attack
embarked ;; Process_start
test ;; Testing || test ;; Testing || conducted ;; Action
shot ;; Use_firearm
detained ;; Arrest || reports ;; Reporting || Responding ;; Response
shot ;; Use_firearm || drew ;; Motion
pronounced ;; Statement || rushed ;; Motion
captured ;; Conquering
became ;; Becoming || disseminated ;; Dispersal
took place ;; Process_start
caused ;; Causation || recognized ;; Know || recognize ;; Know || work ;; Employment || shooting ;; Use_firearm
break up ;; Destroying
announced ;; Expressing_publicly || stopped ;; Process_end || contacted ;; Communication
split ;; Destroying || started ;; Process_start || walking ;; Self_motion
started ;; Process_start || got ;; Getting || pick ;; Choosing || took place ;; Process_start
walked ;; Self_motion || ripped ;; Destroying
involved ;; Cause_to_be_included || fight ;; Hostile_encounter || yelling ;; Motion || threw ;; Motion
walked ;; Self_motion || involved ;; Cause_to_be_included || remained ;; Presence || looked ;; Change_sentiment
dressed ;; Wearing || removed ;; Removing || sitting ;; Motion
shooting ;; Use_firearm || murder ;; Killing || charged ;; Judgment_communication
resigned ;; GiveUp || pleaded ;; Judgment_communication
began ;; Process_start || trial ;; Testing
found ;; Know || murder ;; Killing
organized ;; Arranging || destruction ;; Destroying || incidents ;; Catastrophe
arrested ;; Arrest
sentenced ;; Legal_rulings
served ;; Assistance || held ;; Hold
released ;; Releasing || serving ;; Assistance
filed ;; Submitting_documents || death ;; Death
settled ;; Resolve_problem
damages ;; Damaging || settled ;; Resolve_problem || sued ;; Request
determined ;; Deciding || imprisoned ;; Arrest || result in ;; Causation || suit ;; Legality
exploded ;; Attack || storage ;; Preserving || bomb ;; Attack || planted ;; Placing || flying ;; Self_motion
stop-over ;; Placing || acting ;; Action || orders ;; Request || planted ;; Placing || disembarking ;; Departing
detonated ;; Attack || destroyed ;; Destroying || bomb ;; Attack || stop-over ;; Placing || flying ;; Self_motion
killed ;; Killing || perished ;; Death
occurred ;; Coming_to_be || agreement ;; Agree_or_refuse_to_act || attack ;; Attack || ended ;; Process_end || war ;; Military_operation
took ;; Conquering || taken ;; Conquering || bombers ;; Attack || hidden ;; Hiding_objects || traced ;; Arriving || realized ;; Know
died ;; Death || confessed ;; Reveal_secret || bombing ;; Terrorism
death ;; Death || deemed ;; Coming_to_believe || brainwashed ;; Influence || sentenced ;; Prison || put on trial ;; Legality || attack ;; Terrorism || pardoned ;; Releasing
incident ;; Attack || implicated ;; Influence
terrorist ;; Terrorism || included ;; Cause_to_be_included || terrorism ;; Terrorism || state ;; Statement || state ;; Statement || bombing ;; Terrorism
improved ;; Cause_to_make_progress || form ;; Coming_to_be || attack ;; Attack || made ;; Creating
told ;; Telling || trained ;; Education_teaching || espionage ;; Perception_active || run ;; Self_motion || attack ;; Attack || released ;; Releasing || recalled ;; Statement || carry out ;; Action
branded ;; Labeling || became ;; Becoming || seeing ;; Perception_active
kill ;; Killing || fearing ;; Perception_active || exile ;; Escaping
fight ;; Hostile_encounter || said ;; Statement || conference ;; Social_event
sense ;; Perception_active || sense ;; Perception_active
killing ;; Killing || massacre ;; Killing || involved ;; Cause_to_be_included
killed ;; Killing
killed ;; Killing || deemed ;; Coming_to_believe
killing ;; Killing || committed ;; Commitment
controlled ;; Control || command ;; Control
proclaimed ;; Statement
prevented ;; Preventing_or_letting || voting ;; Choosing || support ;; Supporting || passed ;; Process_end || boycotted ;; Agree_or_refuse_to_act
organized ;; Arranging || established ;; Creating || response ;; Response
provoked ;; Causation || fighting ;; Hostile_encounter
took over ;; Conquering || searches ;; Scrutiny || pillaging ;; Robbery || followed ;; Motion
described ;; Statement
carried out ;; Action || erase ;; Emptying || concluded ;; Process_end
investigating ;; Research || removed ;; Removing || massacred ;; Killing || arrival ;; Arriving || tasked ;; Motion || transpired ;; Coming_to_be
deaths ;; Death || verify ;; Check
investigations ;; Scrutiny || documented ;; Recording
carried out ;; Action || demolished ;; Destroying || established ;; Creating
causes ;; Causation || died ;; Death || death ;; Death || listed ;; Writing
carried out ;; Action || killings ;; Killing
carrying out ;; Action || included ;; Cause_to_be_included || died ;; Death || trial ;; Testing || indicted ;; Criminal_investigation || charged ;; Criminal_investigation
cleansing ;; Emptying || convicted ;; Legal_rulings || followed ;; Motion
committed ;; Commitment || convicted ;; Legal_rulings
defense ;; Defending || named ;; Name_conferral || celebrate ;; Social_event
conducted ;; Action
fueled ;; Supply || Detonated ;; Attack || detonated ;; Attack
involving ;; Cause_to_be_included || led to ;; Causation
detonation ;; Attack || spread ;; Dispersal || fell ;; Motion_directional
evacuated ;; Removing || suffered ;; Bodily_harm
contaminated ;; Destroying
testing ;; Testing
competition ;; Competition
allow ;; Preventing_or_letting || began ;; Process_start
introduced ;; Causation || Trials ;; Testing || season ;; Competition
matches ;; Competition || tournament ;; Competition
defeating ;; Earnings_and_losses || final ;; Competition
named ;; Name_conferral || runs ;; Motion || wickets ;; Body_movement
took ;; Getting
rose ;; Cause_change_of_position_on_a_scale || reached ;; Cause_change_of_position_on_a_scale || peaked ;; Cause_change_of_position_on_a_scale
increases ;; Cause_change_of_position_on_a_scale || demand ;; Supply || showing ;; Statement || soaring ;; Motion || falling ;; Motion_directional || reports ;; Reporting || attributed ;; Causation || tension ;; Hostile_encounter || decline ;; Cause_change_of_position_on_a_scale
conflict ;; Hostile_encounter || disasters ;; Catastrophe || events ;; Confronting_problem || effects ;; Influence || tests ;; Testing || plans ;; Arranging || Katrina ;; Catastrophe || prices ;; Cost || Hurricane ;; Catastrophe
recession ;; Cause_change_of_position_on_a_scale || impact ;; Influence || onset ;; Process_start || prices ;; Cost || pressures ;; Influence
caused ;; Causation || recession ;; Cause_change_of_position_on_a_scale || demand ;; Supply || shrink ;; Cause_change_of_position_on_a_scale || collapsing ;; Destroying
disputed ;; Quarreling || drop ;; Cause_change_of_position_on_a_scale
remained ;; Temporary_stay || returning ;; Change
tested ;; Testing || intercepted ;; Preventing_or_letting
reports ;; Reporting || addressed ;; Expressing_publicly
tested ;; Testing || addressed ;; Expressing_publicly
mailed ;; Sending || included ;; Cause_to_be_included
die ;; Death
stop ;; Preventing_or_letting
expose ;; Reveal_secret || see ;; Perception_active || become ;; Becoming
approve ;; Agree_or_refuse_to_act
received ;; Receiving || opened ;; Openness || contained ;; Containing || sent ;; Sending || mailed ;; Sending || testing ;; Testing
testing ;; Testing || tested ;; Testing
explosions ;; Use_firearm || test ;; Testing || test ;; Research
conducted ;; Action || testing ;; Testing || test ;; Testing
achieved ;; Achieve || build ;; Building || giving ;; Giving
described ;; Statement || carried out ;; Action
test ;; Research || used ;; Using || established ;; Creating
bomb ;; Use_firearm
initiated ;; Process_start
convened ;; Come_together || declare ;; Expressing_publicly || detonated ;; Attack
resulted in ;; Causation
called ;; Name_conferral || assigned ;; Choosing || designated ;; Choosing
explosion ;; Attack
crashed ;; Catastrophe || killing ;; Killing
crash ;; Catastrophe
perished ;; Death
crash ;; Catastrophe || remained ;; Presence
remains ;; Presence
broke ;; Destroying || occurred ;; Coming_to_be || control ;; Control || causing ;; Causation || open ;; Openness || severed ;; Destroying
opened ;; Openness || open ;; Openness || forced ;; Motion || leading to ;; Causation
prevent ;; Preventing_or_letting || used ;; Using
pressed ;; Motion || align ;; Motion || positioned ;; Placing
noticed ;; Know || occurred ;; Coming_to_be
allowed ;; Preventing_or_letting || forced ;; Motion
involved ;; Participation || installed ;; Placing || change ;; Change || install ;; Placing || ordered ;; Request || make it ;; Causation || completed ;; Process_end
noted ;; Know || filed ;; Submitting_documents || make it ;; Causation
performed ;; Hold || provided ;; Supply || speak ;; Telling || latching ;; Motion
upgraded ;; Cause_to_make_progress
found ;; Know || caused ;; Causation || occurred ;; Coming_to_be || service ;; Supply || investigation ;; Criminal_investigation || testing ;; Testing || entered ;; Arriving
occurred ;; Coming_to_be || occurred ;; Coming_to_be || result in ;; Causation
warning ;; Warning || correct ;; Resolve_problem
lawsuits ;; Legality
tournament ;; Competition
played ;; Competition || played ;; Competition
competition ;; Competition
came ;; Change || beat ;; Competition || final ;; Competition || final ;; Competition
blamed ;; Judgment_communication || cited ;; Adducing || test ;; Testing || defeat ;; Earnings_and_losses || rotate ;; Motion
named ;; Name_conferral || tournament ;; Competition
detonated ;; Attack
became ;; Becoming || success ;; Achieve
commenced ;; Process_start || merged ;; Cause_to_amalgamate
ended ;; Process_end || worked ;; Employment
decided ;; Deciding || resume ;; Process_start || build ;; Building || losing ;; Earnings_and_losses || response ;; Response
research ;; Research || project ;; Creating || called ;; Name_conferral || directed ;; Action
test ;; Testing || develop ;; Coming_to_be
sent ;; Sending || request ;; Request || suggested ;; Statement
agreed ;; Agree_or_refuse_to_act || test ;; Research || used ;; Using
announced ;; Expressing_publicly || test ;; Testing || occur ;; Coming_to_be
assembled ;; Come_together || included ;; Cause_to_be_included
appointed ;; Deciding
taken ;; Conquering || assembled ;; Come_together || transport ;; Sending
joined ;; Becoming_a_member || ships ;; Sending || reaching ;; Arriving
test ;; Testing || anchored ;; Connect || effects ;; Achieve || exploded ;; Destroying
explosion ;; Destroying || occurred ;; Presence
play ;; Competition || following ;; Motion || remained ;; Temporary_stay || test ;; Testing
received ;; Receiving || led ;; Causation || opening ;; Openness || dub ;; Name_conferral || went to ;; Motion
win ;; Getting || named ;; Name_conferral
absence ;; Presence || providing ;; Supply || withstand ;; Defending || shared ;; Getting
announced ;; Expressing_publicly || final ;; Process_end || test ;; Research || met with ;; Come_together || compiling ;; Come_together
drawn ;; Motion
retained ;; Preserving
achieved ;; Achieve
asked ;; Request || start ;; Process_start
promoted ;; Cause_to_make_progress
began ;; Process_start || complaining ;; Judgment_communication
began ;; Process_start || sent ;; Sending || payments ;; Commerce_pay || received ;; Receiving || receiving ;; Receiving
began ;; Process_start || found ;; Know || checks ;; Scrutiny || investigating ;; Criminal_investigation || using ;; Using
confronted ;; Confronting_problem || bought ;; Commerce_buy || shot ;; Use_firearm || lured ;; Influence
revive ;; Recovering || dead ;; Death || loss ;; Earnings_and_losses || arrest ;; Arrest || pronounced ;; Statement
held ;; Defending || prayers ;; Rite || death ;; Death || visit ;; Traveling || crime ;; Committing_crime || affected ;; Influence || traveled ;; Traveling
break ;; Destroying || interrupted ;; Hindering
reaction ;; Response || death ;; Death || deaths ;; Death
criticized ;; Judgment_communication || received ;; Receiving || murder ;; Killing
criticized ;; Judgment_communication || murder ;; Killing || playing ;; Competition || causing ;; Causation || mocked ;; Judgment_communication
declared ;; Expressing_publicly
death ;; Death
became ;; Becoming || Dreaming ;; Coming_to_believe || called ;; Name_conferral
death ;; Death || waned ;; Cause_change_of_position_on_a_scale
suicide ;; Killing || trial ;; Testing || shot ;; Use_firearm || said ;; Expressing_publicly
found ;; Know || murder ;; Killing || sentenced ;; Legal_rulings
achieved ;; Achieve || release ;; Publishing
conducted ;; Action
performed ;; Hold
response ;; Response || timing ;; Deciding || tests ;; Testing
powers ;; Influence || tests ;; Testing || resulted in ;; Causation || sanctions ;; Preventing_or_letting
became ;; Becoming || testing ;; Testing || test ;; Testing
test ;; Testing || followed ;; Self_motion
conducted ;; Action || tests ;; Testing
bombing ;; Attack || tests ;; Testing
investigate ;; Research
held ;; Hold || announced ;; Expressing_publicly || observed ;; Perception_active
conducted ;; Action || developed ;; Coming_to_be
assembled ;; Come_together || hit ;; Attack
test ;; Testing
named ;; Name_conferral || dropped ;; Motion_directional
caused ;; Causation || detonated ;; Attack
test ;; Testing
detonated ;; Attack
caused ;; Causation
test ;; Testing || canceled ;; Removing
scrapped ;; Destroying
conducted ;; Action || rescheduled ;; Change_event_time
agreed ;; Agree_or_refuse_to_act || evacuate ;; Escaping || evacuated ;; Escaping
rendered ;; Causation
remains ;; Presence || visited ;; Traveling
protect ;; Defending || showed ;; Protest || reduced ;; Cause_change_of_position_on_a_scale
called ;; Statement
Championship ;; Competition || hosted ;; Social_event || Championship ;; Competition
used ;; Using || test ;; Testing || event ;; Competition
began ;; Process_start || played ;; Competition || game ;; Competition
defending ;; Defending || defeating ;; Conquering
lost ;; Earnings_and_losses || host ;; Social_event || game ;; Competition
records ;; Recording || tournament ;; Competition || set ;; Action
collided ;; Catastrophe || disintegrate ;; Destroying || killing ;; Killing || catch fire ;; Damaging || flight ;; Self_motion || land ;; Motion || crash ;; Catastrophe
concluded ;; Statement || inquiry ;; Criminal_investigation || accident ;; Catastrophe || judgement ;; Deciding
tested ;; Testing || led to ;; Causation || accident ;; Catastrophe || analysis ;; Scrutiny || judgement ;; Deciding || crash ;; Catastrophe
enact ;; Action || called ;; Request || testing ;; Testing || accident ;; Catastrophe || investigated ;; Scrutiny || crash ;; Catastrophe
disaster ;; Catastrophe || occurred ;; Coming_to_be || crash ;; Catastrophe
disaster ;; Catastrophe || occur ;; Coming_to_be || crash ;; Catastrophe || crash ;; Catastrophe || crash ;; Catastrophe
festival ;; Social_event || held ;; Defending || cultural ;; Social_event || events ;; Social_event
festival ;; Social_event || initiative ;; Arranging || encouraging ;; Influence
illuminating ;; Lighting || event ;; Social_event || event ;; Social_event
tours ;; Traveling
become ;; Becoming || inception ;; Coming_to_be || attracting ;; Influence
Attendance ;; Participation || rose ;; Cause_change_of_position_on_a_scale
support ;; Supporting || tour ;; Traveling
joined ;; Becoming_a_member || recitals ;; Social_event || played ;; Self_motion || played ;; Self_motion
began ;; Process_start || supported ;; Supporting || play ;; Self_motion
held ;; Hold || tour ;; Traveling
joined ;; Becoming_a_member || Tour ;; Traveling || perform ;; Hold
played ;; Self_motion
throws ;; Body_movement
breaks ;; Destroying
asks ;; Request || light ;; Lighting
performs ;; Hold || throws ;; Body_movement || gets up ;; Self_motion || plays ;; Self_motion
perform ;; Hold
crashed ;; Catastrophe || fly ;; Motion || scheduled ;; Arranging
killing ;; Killing || crashed ;; Catastrophe || approached ;; Arriving || operated ;; Being_in_operation
caused ;; Causation || crash ;; Catastrophe || concluded ;; Deciding || led to ;; Causation
believed ;; Know || crash ;; Catastrophe || unnoticed ;; Causation
blamed ;; Judgment_communication || lighting ;; Lighting || experienced ;; Change_sentiment || vetoed ;; Statement
requested ;; Request || changes ;; Change || report ;; Reporting || made ;; Action
possessed ;; Getting || exacerbated ;; Cause_change_of_strength
fought ;; Hostile_encounter
opposed ;; Agree_or_refuse_to_act
used ;; Using
killed ;; Killing || starved ;; Death
destroyed ;; Destroying
accused ;; Judgment_communication
deployed ;; Arranging
ended ;; Process_end
began ;; Process_start || culminated ;; Process_end
formed ;; Coming_to_be
flared ;; Lighting
developed ;; Coming_to_be
storm ;; Catastrophe || hurricane ;; Catastrophe || originated ;; Presence
intensified ;; Cause_change_of_strength || meandered ;; Motion || designated ;; Request
became ;; Becoming || organized ;; Arranging || attained ;; Getting || storm ;; Catastrophe
curved ;; Motion_directional || maintained ;; Preserving || approached ;; Arriving
came ;; Arriving
weakened ;; Cause_change_of_strength || moved ;; Motion || storm ;; Catastrophe || dissipated ;; Dispersal || turned ;; Change
sparked ;; Lighting
issued ;; Creating || warnings ;; Warning || arrival ;; Arriving
flooding ;; Motion || produced ;; Creating || led to ;; Causation
become ;; Becoming || stranded ;; Removing || damaged ;; Damaging
interrupted ;; Hindering || shut down ;; Having_or_lacking_access
caused ;; Causation || damage ;; Damaging
injuries ;; Bodily_harm || resulted in ;; Causation || deaths ;; Death
Damage ;; Damaging || damage ;; Damaging || storm ;; Catastrophe || reached ;; Arriving
criticized ;; Judgment_communication || delivered ;; Sending || warning ;; Warning || warnings ;; Warning || issued ;; Creating
poured ;; Motion || praying ;; Rite || attacks ;; Attack || occurred ;; Presence
protesting ;; Attack
caused ;; Causation || attacks ;; Attack
campaigned ;; Military_operation || flying ;; Motion || killed ;; Killing || forces ;; Hostile_encounter || prevented ;; Preventing_or_letting
prompted ;; Causation || withdraw ;; Escaping || took ;; Conquering || support ;; Supporting || reduced ;; Cause_change_of_position_on_a_scale || aid ;; Assistance || light ;; Lighting
determined ;; Deciding || attack ;; Attack || inquiry ;; Scrutiny || used ;; Using
exonerated ;; Judgment_communication || used ;; Using
negotiations ;; Communication || attack ;; Attack || forced ;; Hostile_encounter || meet with ;; Social_event || regarding ;; Perception_active
provided ;; Supply || implemented ;; Action || talks ;; Telling || signing ;; Sign_agreement || joint ;; Collaboration || communique ;; Communication || changes ;; Change || widespread ;; Dispersal || protests ;; Attack || leading to ;; Causation
Hurricane ;; Catastrophe || cyclones ;; Catastrophe || reach ;; Arriving
developed ;; Coming_to_be || intensified ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength || due ;; Causation || deteriorated ;; Cause_change_of_position_on_a_scale
crossing ;; Motion_directional || prevented ;; Preventing_or_letting || strengthening ;; Cause_change_of_strength || progressing ;; Self_motion
moved ;; Motion || dissipated ;; Dispersal
storm ;; Catastrophe || brought ;; Bringing || winds ;; Motion || rainfall ;; Catastrophe
weakening ;; Cause_change_of_strength || deaths ;; Death || causing ;; Causation || produced ;; Creating || flooding ;; Catastrophe || evacuate ;; Escaping
caused ;; Causation || storm ;; Catastrophe || spread ;; Dispersal || damage ;; Damaging || killing ;; Killing || thunderstorms ;; Lighting
spread ;; Dispersal || storm ;; Catastrophe
crashed ;; Catastrophe || take off ;; Departing
knots ;; Connect
deployment ;; Arranging || indicated ;; Coming_to_believe
illuminated ;; Lighting
aborted ;; Process_end || takeoff ;; Self_motion
slow ;; Change_event_time || veered ;; Motion
struck ;; Attack
traveled ;; Traveling || caught fire ;; Damaging || coming to ;; Motion
killed ;; Killing
involved ;; Cause_to_be_included || killing ;; Killing
took place ;; Process_start || declared ;; Expressing_publicly || supported ;; Supporting
control ;; Control || surrounding ;; Surrounding || occurred ;; Coming_to_be
opened ;; Openness || guarding ;; Patrolling || walk ;; Self_motion || abuse ;; Bodily_harm
control ;; Control || required ;; Imposing_obligation || mark ;; Recording || using ;; Using || established ;; Building
destroyed ;; Destroying || forced ;; Hostile_encounter || torched ;; Lighting || looted ;; Robbery
victims ;; Catastrophe
rebuilt ;; Building || shrunk ;; Cause_change_of_position_on_a_scale
included ;; Cause_to_be_included || killing ;; Killing
died ;; Death
ordered ;; Arranging || retrial ;; Criminal_investigation || convicted ;; Legal_rulings
indicted ;; Criminal_investigation
declared ;; Expressing_publicly || stand ;; Placing || acquitted ;; Legal_rulings
crash ;; Catastrophe || occurred ;; Coming_to_be
caused ;; Causation || starting ;; Process_start || forgot ;; GiveUp || waiting ;; Motion || return ;; Arriving
set ;; Placing
block ;; Hiding_objects || light ;; Lighting || cleared ;; Removing || set off ;; Departing
stand ;; Placing || travelling ;; Motion || following ;; Motion || express ;; Motion || travelling ;; Motion
caught ;; Getting || derailed ;; Self_motion
broke out ;; Process_start || ruptured ;; Destroying || made ;; Creating || telescoping ;; Motion || fed ;; Supply || leaking ;; Motion
lost ;; Earnings_and_losses || burned ;; Bodily_harm || death ;; Death
fought ;; Hostile_encounter
wars ;; Military_operation
established ;; Building || occupied ;; Control
carried out ;; Attack || sacked ;; Destroying || defeated ;; Conquering
flared ;; Lighting
overthrew ;; Change_of_leadership || replaced ;; Change_of_leadership
began ;; Process_start || interfere ;; Hindering || wrote ;; Writing || secured ;; Preventing_or_letting || preparing ;; GetReady
instigated ;; Convincing
weakened ;; Cause_change_of_strength || dispatch ;; Sending
set ;; Placing || complete ;; Process_end || landed ;; Self_motion
met ;; Come_together || gathered ;; Come_together
destroy ;; Destroying || drive ;; Self_motion || encircle ;; Besieging || occupy ;; Control || flight ;; Self_motion
withdrew ;; Escaping || returned ;; Arriving || gathered ;; Come_together
met ;; Come_together
resulted in ;; Causation
fled ;; Escaping || surrendered ;; Surrendering || blockaded ;; Besieging
returned ;; Arriving || enjoy ;; Perception_active
reached ;; Arriving || rose ;; Cause_change_of_position_on_a_scale || proved ;; Convincing || accept ;; Receiving
restored ;; Recovering || established ;; Building || wars ;; Military_operation
wildfires ;; Catastrophe || burned ;; Damaging
occurred ;; Coming_to_be
fires ;; Catastrophe || burned ;; Damaging || covering ;; Filling
began ;; Process_start || fire ;; Catastrophe
storm ;; Catastrophe || started ;; Process_start || lightning ;; Lighting || fires ;; Catastrophe
declared ;; Expressing_publicly || activating ;; Preventing_or_letting
fires ;; Catastrophe || lightning ;; Lighting || started ;; Process_start || strikes ;; Bodily_harm
storm ;; Catastrophe || blizzard ;; Catastrophe || affected ;; Influence || dropping ;; Motion
snowstorm ;; Catastrophe || hit ;; Attack || suffered ;; Bodily_harm
began ;; Process_start || storm ;; Catastrophe || dropping ;; Motion
moved ;; Motion || affecting ;; Influence || states ;; Statement
storm ;; Catastrophe || entered ;; Arriving
storm ;; Catastrophe || coupled ;; Cause_to_amalgamate || increased ;; Cause_change_of_position_on_a_scale || strength ;; Cause_change_of_strength || light ;; Lighting || created ;; Causation
storm ;; Catastrophe || due ;; Causation || covering ;; Hiding_objects || impairing ;; Bodily_harm
storm ;; Catastrophe || traveling ;; Traveling || hit ;; Attack || causing ;; Causation || deaths ;; Death
operated ;; Being_in_operation
crashed ;; Catastrophe || registered ;; Writing
killed ;; Killing
investigation ;; Scrutiny || opened ;; Openness
revealed ;; Reveal_secret || become ;; Becoming
limited ;; Limiting
caused ;; Causation || lose ;; Earnings_and_losses
warnings ;; Warning || lighting ;; Lighting || noted ;; Know
crash ;; Motion
involving ;; Participation
fatal ;; Killing || killed ;; Killing || crash ;; Catastrophe || happened ;; Presence
travelling ;; Traveling || consisting ;; Containing
failed ;; Earnings_and_losses || leaving ;; Departing || uncoupled ;; Motion || rolled ;; Body_movement || collided ;; Bodily_harm
rushed ;; Motion || woke ;; Earnings_and_losses
wounded ;; Bodily_harm || open ;; Openness || heard ;; Perception_active || screams ;; Telling || split ;; Destroying || stacked ;; Motion
became ;; Becoming || taking ;; Conquering || injured ;; Bodily_harm || stopped ;; Preventing_or_letting
cutting ;; Bodily_harm || used ;; Using || rescue ;; Rescuing || illuminated ;; Lighting || injured ;; Bodily_harm || worked ;; Employment
recovered ;; Rescuing
disaster ;; Catastrophe || death ;; Death || injured ;; Bodily_harm || resulting ;; Causation
caused ;; Causation
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
due ;; Causation || hurricane ;; Catastrophe || storm ;; Catastrophe || remained ;; Temporary_stay || tracked ;; Motion || intensified ;; Cause_change_of_position_on_a_scale
storm ;; Catastrophe || strengthened ;; Cause_change_of_strength || weakened ;; Cause_change_of_strength
re-intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe
began ;; Process_start || curved ;; Motion_directional || parallel ;; Motion
peaked ;; Cause_change_of_position_on_a_scale
weakened ;; Cause_change_of_strength
storm ;; Catastrophe || weakened ;; Cause_change_of_strength
curved ;; Motion_directional || transitioned ;; Change
rescued ;; Rescuing || capsized ;; Catastrophe
effects ;; Influence || reported ;; Reporting
light ;; Lighting || produced ;; Manufacturing
damaged ;; Damaging || downed ;; Motion
caused ;; Causation || flooding ;; Motion
attributed ;; Causation || damage ;; Damaging
hurricane ;; Catastrophe || hurricane ;; Catastrophe || make ;; Creating
storm ;; Catastrophe || took place ;; Process_start
killed ;; Killing || destroyed ;; Destroying || injured ;; Bodily_harm
disconnected ;; Destroying || Falling ;; Motion_directional
hailstorms ;; Catastrophe || causing ;; Causation || rain ;; Self_motion || surprising ;; Change_sentiment || jams ;; Filling || struck ;; Attack
hurricane ;; Catastrophe || lightning ;; Lighting || packed ;; Bringing || lasted ;; Process_start || thunder ;; Lighting || battered ;; Damaging
reported ;; Reporting
capture ;; Conquering
caused ;; Causation || burning ;; Lighting || resulted in ;; Causation
fought ;; Hostile_encounter || due ;; Causation
destruction ;; Destroying || resulted in ;; Causation || resisted ;; Defending
made ;; Manufacturing || arrived ;; Arriving || withdrawing ;; Removing
defense ;; Defending || depleted ;; Expend_resource || recruited ;; Becoming_a_member
produced ;; Manufacturing || devastated ;; Destroying
flash ;; Lighting || produced ;; Creating
reported ;; Reporting || resulted ;; Causation
caused ;; Causation || re-used ;; Using
festival ;; Social_event || held ;; Hold
event ;; Social_event || took place ;; Hold
Flaming ;; Lighting || event ;; Social_event
issues ;; Creating || bob ;; Body_movement || due ;; Causation || scheduled ;; Arranging || injury ;; Bodily_harm || withdrew ;; Removing || suffered ;; Bodily_harm
attracted ;; Motion || festival ;; Social_event || left ;; Departing || organizers ;; Arranging || generated ;; Creating || claim ;; Statement
services ;; Supply || claimed ;; Statement
resolve ;; Resolve_problem || used ;; Using || festival ;; Social_event || pay ;; Commerce_pay
erupted ;; Process_start || drought ;; Catastrophe
ignited ;; Lighting || preceded ;; Process_start
declared ;; Expressing_publicly || set ;; Placing
causes ;; Causation || formed ;; Coming_to_be || joint ;; Collaboration || investigation ;; Criminal_investigation
reported ;; Reporting
named ;; Name_conferral || burning ;; Bodily_harm
prompted ;; Causation
declared ;; Expressing_publicly
declared ;; Expressing_publicly || affected ;; Influence
postponed ;; Change_event_time || forced ;; Hostile_encounter || close ;; Self_motion || cancelled ;; Change_event_time
extinguished ;; Removing
burned ;; Bodily_harm || destroyed ;; Destroying || extinguished ;; Removing
cost ;; Cost || destruction ;; Destroying || costs ;; Cost || damage ;; Damaging || Damage ;; Damaging || support ;; Supporting
crashed ;; Catastrophe
died ;; Death
killed ;; Killing || crash ;; Catastrophe || crash ;; Catastrophe || accident ;; Catastrophe
incident ;; Catastrophe
found ;; Know || investigation ;; Scrutiny || caused ;; Causation || left ;; Departing || incident ;; Catastrophe
control ;; Control || damaged ;; Damaging || severed ;; Destroying
losing ;; Earnings_and_losses || causing ;; Causation || control ;; Control || controlled ;; Control || damaged ;; Damaging || destroyed ;; Destroying || leaking ;; Motion || ignited ;; Lighting || resulting in ;; Causation
conducted ;; Action || drive ;; Self_motion || attack ;; Attack
used ;; Using
surrounding ;; Surrounding || fortified ;; Cause_change_of_strength || created ;; Creating
call ;; Name_conferral || led ;; Causation || compare ;; Statement
moving ;; Motion || made it ;; Causation
moved up ;; Motion || forces ;; Hostile_encounter || isolating ;; Removing || overrunning ;; Destroying
built ;; Building || command ;; Control || overrun ;; Destroying
begun ;; Process_start || attack ;; Attack || relocate ;; Military_operation || clearing ;; Emptying
attack ;; Attack || caught ;; Hold
destroyed ;; Destroying || loss ;; Earnings_and_losses || blow ;; Motion
sunk ;; Motion
damage ;; Damaging || supply ;; Supply || inflicted ;; Bodily_harm
surrounded ;; Surrounding || remained ;; Presence || bypassed ;; Departing || cut off ;; Having_or_lacking_access
built ;; Building || took over ;; Control || scratch ;; Bodily_harm
killed ;; Killing || explosions ;; Attack
explosion ;; Attack
requested ;; Request || moved ;; Motion || questioned ;; Communication
destroyed ;; Destroying
reported ;; Reporting || reported ;; Reporting || occurred ;; Presence
killed ;; Killing || fell ;; Motion_directional || said ;; Telling || injured ;; Bodily_harm || flattened ;; Damaging
effects ;; Influence || explosions ;; Attack
reported ;; Reporting
reported ;; Reporting || separated ;; Forming_relationships
ordered ;; Request
fled ;; Escaping
began ;; Process_start || service ;; Rite || calling ;; Request || resignation ;; Change_of_leadership
investigation ;; Research || launched ;; Process_start
occurred ;; Presence
dismissing ;; Request
tweeted ;; Statement
attending ;; Participation || discuss ;; Communication
taken ;; Conquering || return ;; Arriving
said ;; Communication || armed ;; Bearing_arms || forces ;; Causation || called ;; Request
forces ;; Causation
reported ;; Reporting || attacked ;; Attack
announced ;; Expressing_publicly || return ;; Motion || regained ;; Recovering
arrested ;; Arrest || charge ;; Imposing_obligation
caused ;; Causation || storm ;; Catastrophe || flooding ;; Catastrophe
originated ;; Coming_to_be || storm ;; Catastrophe || cyclone ;; Catastrophe
intensified ;; Cause_change_of_strength || cyclone ;; Catastrophe || storm ;; Catastrophe || moved ;; Motion
landfall ;; Arriving || made ;; Motion
weakened ;; Cause_change_of_strength || moving ;; Motion || turned ;; Change
dissipating ;; Change || moved ;; Motion
caused ;; Causation || damage ;; Damaging
rainfall ;; Catastrophe || dropped ;; Motion_directional
resulted in ;; Causation || flooding ;; Catastrophe || forced ;; Causation || evacuate ;; Emptying
reported ;; Reporting || flooding ;; Catastrophe
storm ;; Catastrophe || reported ;; Reporting || losses ;; Earnings_and_losses || reached ;; Achieve || thrown ;; Motion
capture ;; Conquering || breaking ;; Bodily_harm
delivered ;; Military_operation
broken ;; Bodily_harm
captured ;; Conquering || repulsed ;; Defending
achieved ;; Achieve || began ;; Process_start || inflicted ;; Causation || captured ;; Conquering
began ;; Process_start || achieved ;; Achieve || suspended ;; Process_end || force ;; Causation
led to ;; Causation || recuperated ;; Recovering
conducted ;; Action || abandon ;; GiveUp || forced ;; Causation || retire ;; Escaping
fighting ;; Hostile_encounter
used ;; Using
avoided ;; Preventing_or_letting || describe ;; Statement || conflicts ;; Hostile_encounter
resolving ;; Resolve_problem || delays ;; Change_event_time || opposed ;; Agree_or_refuse_to_act || fuelled ;; Cause_change_of_position_on_a_scale || launched ;; Process_start
became ;; Becoming
conflict ;; Hostile_encounter || differed ;; Change || taken ;; Conquering || challenged ;; Confronting_problem
adopting ;; Choosing || attacks ;; Attack || working ;; Action || took ;; Removing || driving ;; Motion
built ;; Building || advanced ;; Motion
opposed ;; Agree_or_refuse_to_act || clashed ;; Hostile_encounter || offered ;; Giving
left ;; Departing
urged ;; Request || leave ;; Departing || replaced ;; Change_of_leadership
embarked ;; Process_start
argued ;; Quarreling
defending ;; Defending || fought ;; Hostile_encounter
ended ;; Process_end
fulfilled ;; Action
sale ;; Commerce_sell || sparked ;; Causation || launched ;; Process_start
given ;; Giving || flew ;; Motion || resupply ;; Supply
presented ;; Presence || Creating ;; Creating || trained ;; Education_teaching || moving ;; Motion || equipped ;; Supply || existed ;; Presence
made ;; Manufacturing
given ;; Giving || command ;; Control
assigned ;; Choosing || build ;; Building || included ;; Cause_to_be_included || founding ;; Creating || operating ;; Being_in_operation || planning ;; Arranging || direct ;; Aiming
carrying out ;; Action || command ;; Control || command ;; Control || referred ;; Name_conferral
blocked ;; Hiding_objects || began ;; Process_start || began ;; Process_start || scale ;; Cause_change_of_position_on_a_scale
forces ;; Hostile_encounter || augmented ;; Cause_change_of_position_on_a_scale || procured ;; Getting
return ;; Motion || flown ;; Self_motion
delivered ;; Sending || cost ;; Cost
made ;; Manufacturing
elected ;; Choosing
election ;; Choosing || elected ;; Choosing
advice ;; Convincing || elected ;; Choosing || march ;; Self_motion || convened ;; Come_together
dissolved ;; Dispersal || request ;; Request
election ;; Choosing || return ;; Change || return ;; Change || return ;; Change
sitting ;; Placing || elected ;; Choosing
became ;; Becoming
formed ;; Coming_to_be || party ;; Social_event || achieved ;; Achieve
become ;; Becoming || election ;; Choosing
secured ;; Getting
became ;; Becoming
became ;; Becoming
formed ;; Coming_to_be || failed ;; Earnings_and_losses || gain ;; Getting || following ;; Self_motion || election ;; Choosing || Left ;; Departing || speaking ;; Perception_active || composed ;; Collaboration || win ;; Getting
lost ;; Earnings_and_losses || poll ;; Scrutiny || sitting ;; Placing || election ;; Choosing || retired ;; Process_end
elected ;; Choosing
led ;; Causation || War ;; Military_operation || conflict ;; Violence
erupted ;; Process_start || crossed ;; Motion_directional || war ;; Military_operation
bloodshed ;; Violence || disputed ;; Agree_or_refuse_to_act || avoid ;; Preventing_or_letting || ceded ;; Surrendering || Treaty ;; Sign_agreement || resettling ;; Placing
mobilized ;; Arranging || hostile ;; Hostile_encounter || opened ;; Body_movement || fire ;; Use_firearm || convinced ;; Coming_to_believe
responded ;; Response || Battle ;; Hostile_encounter || attacking ;; Attack
led ;; Causation || forces ;; Military_operation || secure ;; Arriving || pursued ;; Self_motion
raids ;; Attack || conducted ;; Action || unprotected ;; Having_or_lacking_access
raids ;; Attack || avoid ;; Preventing_or_letting || took part ;; Dispersal || conflict ;; Violence
supported ;; Supporting || Commanded ;; Request || tracked ;; Self_motion
Battle ;; Hostile_encounter || caught ;; Self_motion || defeated ;; Conquering
weakened ;; Cause_change_of_strength || death ;; Death || retreated ;; Escaping
killing ;; Killing || Battle ;; Hostile_encounter || attacked ;; Attack || capturing ;; Conquering
escaped ;; Escaping || surrendered ;; Surrendering || imprisoned ;; Arrest
gave ;; Giving || participated ;; Participation || battle ;; Hostile_encounter || service ;; Supply || War ;; Military_operation
became ;; Becoming || participants ;; Participation || included ;; Cause_to_be_included
gave ;; Giving || sell ;; Commerce_sell || pressured ;; Causation || war ;; Military_operation || move ;; Self_motion || stay ;; Temporary_stay
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
aimed ;; Aiming || expedition ;; Attack || sack ;; Destroying || launched ;; Process_start
confronted ;; Confronting_problem
resolved ;; Resolve_problem || attack ;; Attack || killed ;; Killing || caused ;; Causation
fled ;; Escaping || counterattack ;; Attack || broke ;; Destroying
escape ;; Escaping || besieged ;; Besieging
opened ;; Process_start || sack ;; Destroying || Wars ;; Attack || suffered ;; Bodily_harm
competition ;; Competition
defeated ;; Conquering
competition ;; Competition || finished ;; Process_end || losing ;; Earnings_and_losses
winning ;; Earnings_and_losses || edged ;; Motion || passing ;; Motion_directional
met ;; Come_together || final ;; Competition
competition ;; Competition || finished ;; Process_end || matches ;; Competition || amassing ;; Come_together
claimed ;; Statement || taking ;; Getting
competition ;; Competition || return ;; Arriving
described ;; Statement || retirement ;; Departing || seen ;; Perception_active
negotiations ;; Communication || play ;; Participation
played ;; Participation || insisted ;; Request
announced ;; Expressing_publicly || join ;; Becoming_a_member
scored ;; Getting || debut ;; Process_start
citing ;; Adducing || added ;; Cause_change_of_position_on_a_scale || left ;; Motion
held ;; Hold
held ;; Hold
combined ;; Cause_to_amalgamate || returned ;; Change
results ;; Causation || used ;; Using
combined ;; Cause_to_amalgamate || run ;; Action || last ;; Process_end
combined ;; Cause_to_amalgamate
upset ;; Preventing_or_letting
combined ;; Cause_to_amalgamate
led ;; Causation
due ;; Causation || scheduled ;; Arranging || lack ;; Causation
held ;; Hold || returned ;; Change
killed ;; Killing || massacre ;; Violence
attacked ;; Attack || rebel ;; Attack || known ;; Know
causing ;; Causation || bombed ;; Attack || forces ;; Causation || advanced ;; Cause_to_make_progress || flee ;; Escaping
killed ;; Killing || captured ;; Conquering
surrounded ;; Surrounding || opened ;; Openness || called ;; Request || close ;; Openness
death ;; Death || reported ;; Reporting || confirm ;; Check || threw ;; Body_movement
dead ;; Death || included ;; Cause_to_be_included
reported ;; Reporting
determined ;; Deciding || stated ;; Statement || investigated ;; Criminal_investigation || defense ;; Defending || taken place ;; Process_start
asking ;; Request || filed ;; Submitting_documents
existence ;; Presence || documented ;; Recording || closed ;; Process_end
existence ;; Presence || acknowledged ;; Statement
began ;; Process_start || caused ;; Causation || floods ;; Catastrophe || overflow ;; Motion
snowfall ;; Motion || snowmelt ;; Motion || rains ;; Motion || occurred ;; Presence || flooding ;; Catastrophe
stayed ;; Temporary_stay || broke ;; Destroying || reaching ;; Arriving
caused ;; Causation || floods ;; Catastrophe || affected ;; Influence || damage ;; Damaging
declared ;; Expressing_publicly || damage ;; Damaging
reported ;; Reporting || damage ;; Damaging
supplied ;; Supply || supplies ;; Supply || affected ;; Influence || flooding ;; Catastrophe
requested ;; Request || build ;; Building || help ;; Assistance || evacuate ;; Emptying || protect ;; Defending
rebellion ;; Violence
fuelled ;; Supply || revolt ;; Violence
looted ;; Robbery || ravaged ;; Destroying
said ;; Communication || proved ;; Coming_to_believe
defeated ;; Conquering || slaying ;; Killing
married ;; Forming_relationships || made ;; Manufacturing || recognize ;; Know || entry ;; Arriving || forced ;; Request
intervened ;; Hindering || exploit ;; Control
sent ;; Sending || claim ;; Statement
attack ;; Attack || forcing ;; Hostile_encounter || fight ;; Hostile_encounter || incited ;; Preventing_or_letting
defeated ;; Conquering || besieged ;; Besieging
opened ;; Openness
broke ;; Destroying || fled ;; Escaping || siege ;; Besieging
sent ;; Sending || defeated ;; Conquering || rebels ;; Hostile_encounter || turn ;; Change
proclaimed ;; Statement
fled ;; Escaping || murdered ;; Killing || warfare ;; Hostile_encounter || support ;; Supporting || aid ;; Assistance || Surrounded ;; Surrounding
demise ;; Death || support ;; Supporting || appeared ;; Presence || enjoyed ;; Getting
engagement ;; Hostile_encounter || Wars ;; Hostile_encounter || took place ;; Process_start
raiding ;; Attack || attack ;; Attack || sailing ;; Motion || sailing ;; Motion || sailing ;; Motion || approached ;; Self_motion
escorted ;; Patrolling || protect ;; Preventing_or_letting
mistaken ;; Coming_to_believe || battle ;; Hostile_encounter || formed ;; Becoming
ordered ;; Request || escape ;; Escaping || pursuit ;; Self_motion || turned ;; Becoming
preserve ;; Preserving || chase ;; Self_motion || ordered ;; Request || join ;; Participation
leaving ;; Departing || ran ;; Self_motion || surrender ;; Surrendering
flee ;; Escaping || pursued ;; Self_motion
falling ;; Motion_directional || giving ;; Giving || pursuit ;; Self_motion || came ;; Arriving
surrendered ;; Surrendering || outgunned ;; Hostile_encounter || discovering ;; Know || came ;; Arriving
captured ;; Conquering || war ;; Military_operation
engagement ;; Hostile_encounter || Wars ;; Hostile_encounter
intercepted ;; Attack || returning ;; Arriving
encountered ;; Hostile_encounter || returning ;; Self_motion || sailed ;; Self_motion
warning ;; Warning || assigned ;; Choosing || blockade ;; Besieging || approach ;; Self_motion
escape ;; Escaping || outrun ;; Escaping || separated ;; Preventing_or_letting || split ;; Destroying || caught ;; Getting
compelled ;; Action || coming ;; Motion || fought ;; Hostile_encounter || surrender ;; Surrendering || driven ;; Motion
lying ;; Placing || battle ;; Hostile_encounter || Battle ;; Hostile_encounter || campaign ;; Military_operation
played ;; Participation || match ;; Competition || supported ;; Supporting
played ;; Participation || game ;; Competition || final ;; Competition || beat ;; Competition
Watched ;; Perception_active || match ;; Competition
appearance ;; Presence
met ;; Come_together
use ;; Using || event ;; Competition || following ;; Causation
match ;; Competition || recruitment ;; Employment || questioned ;; Communication || defended ;; Defending
match ;; Competition || selected ;; Choosing || made ;; Action
began ;; Process_start || game ;; Competition || scored ;; Hold || took ;; Action
finished ;; Process_end || extended ;; Change_event_time || move ;; Body_movement
match ;; Competition || shot ;; Motion || dipped ;; Motion_directional || scored ;; Getting
Shield ;; Competition || victory ;; Earnings_and_losses
provide ;; Supply || felt ;; Perception_active || win ;; Earnings_and_losses || pleased ;; Change_sentiment || coming ;; Motion
linked ;; Connect || reaction ;; Response || show ;; Presence || believed ;; Protest || indifferent ;; Action
defeating ;; Conquering
combined ;; Cause_to_amalgamate || defeat ;; Conquering
set ;; Self_motion
captured ;; Conquering
established ;; Building || ruins ;; Destroying || marks ;; Recording
made ;; Causation
joined ;; Becoming_a_member
known ;; Know
arrived in ;; Arriving || negotiations ;; Communication || overcame ;; Resolve_problem || took up ;; Conquering
reached ;; Arriving
expansion ;; Expansion || Capturing ;; Conquering || capturing ;; Conquering || took ;; Conquering
taken captive ;; Conquering || claim ;; Statement || suggest ;; Statement
sent ;; Sending || return ;; Arriving || exceeded ;; Cause_change_of_position_on_a_scale || left ;; Departing || deal ;; Communication
allowed ;; Preventing_or_letting || celebrated ;; Social_event || closed ;; Openness
contains ;; Containing
massacre ;; Killing
killed ;; Killing || murdered ;; Killing || agree ;; Agree_or_refuse_to_act
killed ;; Killing || seen ;; Perception_active
killed ;; Killing
fled ;; Escaping || returned ;; Arriving
weakened ;; Cause_change_of_strength || returned ;; Self_motion
made it ;; Causation || conquer ;; Conquering
combat ;; Hostile_encounter || seen ;; Perception_active
participated ;; Participation
became ;; Becoming
conquest ;; Conquering
conquest ;; Conquering || recount ;; Communication
accomplishments ;; Achieve || denigrating ;; Judgment_communication || laud ;; Judgment_communication
storm ;; Catastrophe || killed ;; Killing || floods ;; Catastrophe || cyclone ;; Catastrophe || leaving ;; Departing
stated ;; Statement || flood ;; Catastrophe
declared ;; Expressing_publicly
began ;; Process_start || Flooding ;; Catastrophe
became ;; Becoming || caused ;; Causation || deaths ;; Death || flooding ;; Catastrophe || landslides ;; Catastrophe || leading to ;; Causation
recorded ;; Recording || flooding ;; Catastrophe || rainfall ;; Self_motion
causing ;; Causation || research ;; Research || shown ;; Presence || rainfall ;; Self_motion || droughts ;; Catastrophe || floods ;; Catastrophe || said ;; Statement
attributed ;; Causation || damage ;; Damaging || effects ;; Influence
Battle ;; Hostile_encounter || defeated ;; Conquering || fought ;; Hostile_encounter || fought ;; Hostile_encounter
invasions ;; Attack || allied ;; Collaboration || record ;; Recording || reasoning ;; Protest
battle ;; Hostile_encounter || victory ;; Getting
hidden ;; Hiding_objects || met ;; Come_together
break ;; Destroying
opposed ;; Agree_or_refuse_to_act || displaced ;; Change
held ;; Defending || Championship ;; Competition || tournament ;; Competition
Championship ;; Competition
tournament ;; Competition || playoff ;; Competition || beating ;; Competition
force ;; Causation || playoff ;; Competition || played ;; Competition || going ;; Motion
ended ;; Process_end || playoff ;; Competition || tied ;; Connect || returned ;; Motion
put ;; Placing || shot ;; Use_firearm
Defending ;; Defending || finished ;; Process_end || tied ;; Connect
brought ;; Bringing || flooding ;; Catastrophe
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe
crossed ;; Motion_directional || entered ;; Arriving
intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe || headed ;; Motion
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || hurricane ;; Catastrophe
curved ;; Motion_directional || storm ;; Catastrophe || struck ;; Attack
killed ;; Killing || Flooding ;; Motion || left ;; Departing
killed ;; Killing || left ;; Departing || washed ;; Removing
made ;; Manufacturing || landfall ;; Catastrophe
caused ;; Causation || lead to ;; Causation || turn ;; Change || flee ;; Escaping || resulted in ;; Causation || flooding ;; Catastrophe
drowned ;; Death || services ;; Supply || disrupted ;; Hindering
became ;; Becoming || moving ;; Motion || storm ;; Catastrophe || storm ;; Catastrophe || weakened ;; Cause_change_of_strength
reached ;; Arriving || re-strengthened ;; Cause_change_of_strength
began ;; Process_start || approaching ;; Arriving || weaken ;; Cause_change_of_position_on_a_scale
made ;; Manufacturing || storm ;; Catastrophe || storm ;; Catastrophe || landfall ;; Catastrophe
weakened ;; Cause_change_of_position_on_a_scale || dissipated ;; Dispersal
caused ;; Causation || flooding ;; Catastrophe
went ;; Self_motion || fell ;; Motion || presumed ;; Coming_to_believe || drowned ;; Death
caused ;; Causation || damage ;; Damaging || Hurricane ;; Catastrophe
developed ;; Coming_to_be || named ;; Name_conferral || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
declared ;; Expressing_publicly || moved ;; Motion || storm ;; Catastrophe || intensified ;; Cause_change_of_strength
hurricane ;; Catastrophe || upgraded ;; Cause_change_of_position_on_a_scale
storm ;; Catastrophe || strengthened ;; Cause_change_of_strength || attained ;; Getting
made ;; Creating
storm ;; Catastrophe || dissipated ;; Dispersal || weakened ;; Cause_change_of_position_on_a_scale
moving ;; Motion || evacuated ;; Emptying
making ;; Manufacturing || storm ;; Catastrophe || produced ;; Creating
caused ;; Causation || destroyed ;; Destroying || storm ;; Catastrophe || flooding ;; Motion || peaked ;; Cause_change_of_position_on_a_scale
occurred ;; Coming_to_be || deaths ;; Death || died ;; Death || reported ;; Reporting
impacted ;; Influence || Flooding ;; Motion
flooded ;; Motion
destroying ;; Destroying || hurricane ;; Catastrophe || spawned ;; Creating
caused ;; Causation || Hurricane ;; Catastrophe || damage ;; Damaging
Meeting ;; Social_event || Meeting ;; Social_event
hosted ;; Come_together
terrorist ;; Terrorism || postponed ;; Change_event_time || attacks ;; Attack || hosted ;; Come_together || due ;; Causation || Planned ;; Arranging
meeting ;; Social_event || agenda ;; Arranging || election ;; Change_of_leadership
sent ;; Sending || meeting ;; Social_event || attended ;; Participation || invited ;; Request
represented ;; Presence
Battle ;; Hostile_encounter || fought ;; Hostile_encounter
fought ;; Hostile_encounter
given ;; Giving || battle ;; Hostile_encounter || planned ;; Arranging || carry out ;; Action || eliminate ;; Removing
tracked ;; Motion || engaged ;; Hostile_encounter
captured ;; Conquering || scattered ;; Dispersal || fighting ;; Hostile_encounter || giving ;; Giving || threat ;; Terrorism || ending ;; Process_end
battle ;; Hostile_encounter || rise ;; Cause_change_of_position_on_a_scale || becoming ;; Becoming || signalled ;; Labeling
performed ;; Hold
achieved ;; Achieve || show ;; Social_event || opened ;; Process_start || set ;; Placing || received ;; Receiving
award ;; Award
concluding ;; Process_end || extended ;; Change_event_time
credited ;; Labeling || adapted ;; Change || tour ;; Traveling || Following ;; Self_motion
conducted ;; Action || Census ;; Scrutiny
began ;; Process_start || collection ;; Come_together
collected ;; Come_together || used ;; Using || issue ;; Statement
conducted ;; Action
conducted ;; Action || collected ;; Come_together || Census ;; Scrutiny
released ;; Publishing || increased ;; Cause_change_of_position_on_a_scale || reports ;; Reporting
increased ;; Cause_change_of_position_on_a_scale
census ;; Scrutiny || Spread ;; Dispersal || covered ;; Filling
visited ;; Traveling || classifying ;; Hold
cost ;; Cost || comes to ;; Earnings_and_losses
Conducted ;; Action || census ;; Scrutiny || faced ;; Confronting_problem
included ;; Cause_to_be_included || demands ;; Request
collected ;; Come_together
census ;; Scrutiny || downgrade ;; Cause_change_of_position_on_a_scale || exaggerated ;; Judgment_communication || gaining ;; Getting
conducted ;; Action || census ;; Scrutiny || find ;; Know || speculation ;; Judgment_communication
conducted ;; Action || Census ;; Scrutiny || accepted ;; Agree_or_refuse_to_act || revealed ;; Statement
survey ;; Scrutiny || report ;; Reporting
conducted ;; Action || assess ;; Scrutiny
published ;; Publishing || census ;; Scrutiny || termed ;; Labeling
armed ;; Bearing_arms || conflict ;; Quarreling
called ;; Name_conferral || beginning ;; Process_start || revolution ;; Change_of_leadership || took place ;; Process_start
conflict ;; Hostile_encounter || escalated ;; Cause_change_of_position_on_a_scale
change ;; Change
reported ;; Reporting || conflict ;; Hostile_encounter
called ;; Name_conferral || crossed ;; Motion_directional
occurred ;; Coming_to_be || control ;; Control || control ;; Control || Crossings ;; Motion_directional
positions ;; Employment || reported ;; Reporting
described ;; Statement || service ;; Supply
confirmed ;; Check || deploy ;; Arranging || defend ;; Defending || forced ;; Motion || denied ;; Agree_or_refuse_to_act
regained ;; Recovering || lost ;; Earnings_and_losses
establish ;; Building || called ;; Name_conferral || signed ;; Sign_agreement
became ;; Becoming
took ;; Creating || leading to ;; Causation || control ;; Control
collapsed ;; Destroying || renewed ;; Recovering
agreed ;; Agree_or_refuse_to_act || called ;; Name_conferral
withdraw ;; Escaping || signing ;; Sign_agreement || launched ;; Process_start
changes ;; Change || fall ;; Motion_directional || contact ;; Connect || occurred ;; Presence
killed ;; Killing || labelled ;; Labeling || stayed ;; Temporary_stay || conflict ;; Hostile_encounter || led to ;; Causation
died ;; Death || remaining ;; Presence
allowed ;; Preventing_or_letting || dress ;; Wearing || end ;; Process_end || accounted ;; Cost || crossing ;; Motion_directional
start ;; Process_start || conflict ;; Hostile_encounter || held ;; Defending || stopped ;; Preventing_or_letting
fighting ;; Hostile_encounter || came ;; Self_motion || led to ;; Causation
hosted ;; Social_event
included ;; Cause_to_be_included || release ;; Publishing || party ;; Social_event || held ;; Hold || served ;; Giving
confirmed ;; Check || revealed ;; Reveal_secret || introduced ;; Choosing
contained ;; Containing || created ;; Creating
assembled ;; Building
production ;; Manufacturing || sponsored ;; Supply
performed ;; Hold || streamed ;; Motion
consisted ;; Cause_to_be_included
called ;; Request || party ;; Social_event || aired ;; Expressing_publicly || hosted ;; Social_event
aired ;; Expressing_publicly || party ;; Social_event || release ;; Publishing
received ;; Receiving || criticised ;; Judgment_communication || production ;; Manufacturing || noted ;; Labeling
held ;; Hold || ceremony ;; Social_event || Symphony ;; Social_event || also known as ;; Know
created ;; Creating || ceremony ;; Social_event
began ;; Process_start || broadcast ;; Expressing_publicly || finished ;; Process_end
turned ;; Becoming
cost ;; Cost || partook ;; Participation
closed ;; Process_end || Olympics ;; Competition || games ;; Competition || called ;; Labeling
extinguished ;; Removing || ceremony ;; Social_event || included ;; Cause_to_be_included || handover ;; Giving || Olympics ;; Competition || lowered ;; Motion_directional
acts ;; Action || symphony ;; Social_event || appeared ;; Presence
included ;; Cause_to_be_included || Tributes ;; Judgment_communication
created ;; Creating || handover ;; Giving || marked ;; Communication
thanked ;; Judgment_communication || speech ;; Expressing_publicly || gave ;; Giving
watched ;; Perception_active || estimated ;; Scrutiny || event ;; Social_event
ceremony ;; Social_event || noted ;; Know
questioned ;; Communication || nods ;; Judgment_communication || understood ;; Know
choice ;; Choosing || criticised ;; Judgment_communication || criticised ;; Judgment_communication || behaviour ;; Action || demonstrating ;; Statement
acts ;; Action || ceremony ;; Social_event || appear ;; Presence || turned down ;; Agree_or_refuse_to_act || invitations ;; Request || perform ;; Participation
criticised ;; Judgment_communication || broadcasts ;; Expressing_publicly
Olympics ;; Competition || featuring ;; Cause_to_be_included
undertook ;; Action || raid ;; Attack
fighting ;; Hostile_encounter || attack ;; Attack || took place ;; Process_start || countered ;; Response || reports ;; Reporting
killed ;; Killing || reported ;; Reporting || raid ;; Attack || sentenced ;; Legal_rulings
confined ;; Hindering || reached ;; Arriving || War ;; Hostile_encounter
rejected ;; Agree_or_refuse_to_act || labeled ;; Labeling || war ;; Hostile_encounter || conflict ;; Hostile_encounter
experienced ;; Confronting_problem || fighting ;; Hostile_encounter
Protests ;; Judgment_communication || occurred ;; Coming_to_be || victory ;; Getting || election ;; Choosing
began ;; Process_start || announcement ;; Expressing_publicly || Protests ;; Judgment_communication || irregularities ;; Confronting_problem
manipulated ;; Control || election ;; Change_of_leadership || claimed ;; Statement || rigged ;; Committing_crime || complaints ;; Judgment_communication
lodging ;; Placing || announced ;; Expressing_publicly || appeal ;; Protest || surrender ;; Surrendering
ordered ;; Request || requested ;; Request || claims ;; Statement || investigation ;; Criminal_investigation
protests ;; Judgment_communication || called ;; Statement || election ;; Choosing
beaten ;; Bodily_harm || tortured ;; Bodily_harm || suppress ;; Hindering || sprayed ;; Dispersal || arrested ;; Arrest || shot ;; Use_firearm
uploaded ;; Placing || broadcast ;; Expressing_publicly
tortured ;; Bodily_harm || arrested ;; Arrest || alleging ;; Statement || reported ;; Reporting || rape ;; Violence
confirmed ;; Check || allege ;; Statement || reports ;; Reporting || deaths ;; Death || deaths ;; Death || election ;; Choosing
claimed ;; Statement || forced ;; Request || sign ;; Sign_agreement
blocked ;; Hiding_objects || banned ;; Preventing_or_letting || closed ;; Process_end || cutoff ;; Removing
creation ;; Creating || developed ;; Cause_to_make_progress
communicate ;; Communication || termed ;; Labeling
killed ;; Killing || known ;; Know || Massacre ;; Killing || Incident ;; Attack || confrontation ;; Violence || shot ;; Use_firearm
publicized ;; Dispersal
stationed ;; Placing || support ;; Supporting || enforce ;; Action
formed ;; Coming_to_be
supported ;; Supporting || hit ;; Attack
killing ;; Killing || wounding ;; Bodily_harm || died ;; Death || wounds ;; Bodily_harm || fired ;; Use_firearm || orders ;; Request
promised ;; Commitment || inquiry ;; Criminal_investigation || withdrawal ;; Escaping || dispersed ;; Dispersal || prompting ;; Causation
arrested ;; Arrest || murder ;; Killing || charged ;; Legality || defended ;; Defending
given ;; Giving || acquitted ;; Legal_rulings || convicted ;; Legal_rulings || sentences ;; Prison
branding ;; Labeling || found ;; Know || sentenced ;; Rewards_and_punishments
heightened ;; Cause_change_of_strength || produced ;; Manufacturing
produced ;; Manufacturing || wrestling ;; Competition
took place ;; Process_start
brands ;; Labeling
match ;; Competition
performing ;; Hold
match ;; Competition || performing ;; Hold
started ;; Process_start || competed ;; Competition || died ;; Death || final ;; Process_end
take place ;; Process_start || held ;; Defending || promoted ;; Cause_to_make_progress
sold ;; Commerce_sell || making it ;; Causation
sales ;; Commerce_sell || making it ;; Causation || grossed ;; Earnings_and_losses || drew ;; Create_artwork
seen ;; Perception_active
ousted ;; Change_of_leadership || fled ;; Escaping || called ;; Name_conferral || response ;; Response || calls ;; Request || recognized ;; Know || due ;; Causation || intervention ;; Attack || invasion ;; Attack
contested ;; Competition || storm ;; Attack || said ;; Statement || charter ;; Renting
consisted ;; Forming_relationships || bombing ;; Attack || deployment ;; Arranging || forces ;; Military_operation || saw ;; Self_motion
attacked ;; Attack || supported ;; Supporting || conflict ;; Violence
forces ;; Military_operation
provided ;; Supply || support ;; Supporting
sale ;; Commerce_sell
deployed ;; Arranging || strikes ;; Attack || command ;; Control || control ;; Control
received ;; Receiving || reached ;; Arriving || labelled ;; Labeling || war ;; Hostile_encounter || catastrophe ;; Catastrophe
conflict ;; Hostile_encounter || described ;; Statement
defense ;; Defending || battle ;; Hostile_encounter || war ;; Hostile_encounter || took place ;; Process_start
research ;; Research || fought ;; Hostile_encounter
using ;; Using || specialized ;; Education_teaching || labeled ;; Labeling
began ;; Process_start || battle ;; Hostile_encounter || allow ;; Preventing_or_letting || crossing ;; Motion_directional || laid ;; Placing || launched ;; Motion || establish ;; Coming_to_be
attacked ;; Attack
losses ;; Earnings_and_losses || Determined ;; Deciding || slow ;; Change_event_time || suffered ;; Bodily_harm || made ;; Causation
reinforced ;; Cause_change_of_strength || seize ;; Conquering || managing ;; Control
Suffering ;; Catastrophe
became ;; Becoming || tasked ;; Motion || fire ;; Use_firearm || pinned ;; Placing || clearing ;; Removing
equipment ;; Supply || move ;; Motion || drew ;; Getting
attack ;; Attack || restore ;; Recovering
battle ;; Hostile_encounter || pushed ;; Motion
depleted ;; Expend_resource || opening ;; Openness || fighting ;; Hostile_encounter || control ;; Control || relinquished ;; GiveUp
battle ;; Hostile_encounter || battles ;; Hostile_encounter || war ;; Hostile_encounter || remembered ;; Know
concert ;; Social_event || support ;; Supporting
announced ;; Expressing_publicly || tour ;; Traveling
began ;; Process_start || ended ;; Process_end
encompassed ;; Surrounding
developed ;; Coming_to_be || Labelled ;; Labeling || minimized ;; Cause_change_of_position_on_a_scale || touring ;; Traveling
opening ;; Openness || joining ;; Becoming_a_member || served ;; Assistance || select ;; Choosing
received ;; Receiving || production ;; Creating || praised ;; Judgment_communication
recorded ;; Recording || concert ;; Social_event || broadcast ;; Expressing_publicly
bombing ;; Attack || occurred ;; Presence
target ;; Aiming || building ;; Building || attack ;; Attack || collapsed ;; Destroying
killed ;; Killing || injured ;; Bodily_harm
labeling ;; Labeling || bombing ;; Attack || terrorist ;; Terrorism || claimed ;; Statement || attack ;; Attack || organization ;; Arranging || blame ;; Causation
received ;; Receiving || stated ;; Statement || support ;; Supporting
released ;; Statement || statement ;; Statement || sounded ;; Change_sentiment || determined ;; Deciding || intensified ;; Cause_change_of_strength || claiming ;; Statement || crackdown ;; Attack || organization ;; Arranging
bombings ;; Attack || incident ;; Attack || turning ;; Change || remain ;; Temporary_stay || clashes ;; Attack || taking place ;; Process_start
bombings ;; Attack || explosions ;; Attack
died ;; Death || injured ;; Bodily_harm
killed ;; Killing || perpetrator ;; Committing_crime || reported ;; Reporting || leading ;; Causation || label ;; Labeling || suicide bombing ;; Terrorism
confirmed ;; Check || died ;; Death
occurred ;; Coming_to_be || explosions ;; Catastrophe
occurred ;; Coming_to_be || hit ;; Attack || blast ;; Attack || explosion ;; Attack
parked ;; Placing || placed ;; Placing
bombs ;; Catastrophe || bombing ;; Catastrophe || posted ;; Sending || removed ;; Removing || saying ;; Statement || planted ;; Placing
coup ;; Change_of_leadership || took place ;; Process_start
remove ;; Removing || suspended ;; Change_event_time
resolve ;; Resolve_problem
declared ;; Expressing_publicly || arrested ;; Arrest
announcement ;; Expressing_publicly || demonstrations ;; Social_event || clashes ;; Hostile_encounter || move ;; Hold || followed ;; Motion
supported ;; Supporting
reactions ;; Response || events ;; Social_event
condemned ;; Judgment_communication
refused ;; Agree_or_refuse_to_act || describe ;; Check || coup ;; Change_of_leadership
removal ;; Removing || condemned ;; Judgment_communication || expressed ;; Statement
interruption ;; Hindering || suspended ;; Process_end
events ;; Social_event || labeling ;; Labeling || debate ;; Quarreling
described ;; Statement || coup ;; Change_of_leadership || revolution ;; Reforming_a_system
massacre ;; Killing || killed ;; Killing || favour ;; Supporting || sit-ins ;; Social_event || unrest ;; Violence || protests ;; Agree_or_refuse_to_act || suppressed ;; Defending || dispersal ;; Dispersal
claim ;; Statement || deaths ;; Death || killings ;; Killing || documented ;; Statement || calling ;; Name_conferral || puts ;; Communication
Conference ;; Social_event || gathering ;; Come_together || organised ;; Arranging
conference ;; Social_event || included ;; Cause_to_be_included || camping ;; Social_event
began ;; Process_start || reports ;; Reporting || varying ;; Change
grew ;; Expansion || rising ;; Cause_change_of_position_on_a_scale
attended ;; Participation
conference ;; Social_event || organised ;; Arranging
brought ;; Bringing || Incorporated ;; Forming_relationships || using ;; Using || trademarked ;; Labeling || lawsuit ;; Legality
denied ;; Agree_or_refuse_to_act
produced ;; Manufacturing || Wrestling ;; Competition || event ;; Social_event
invented ;; Creating
due ;; Causation || Wrestling ;; Competition || held ;; Hold || held ;; Hold
revived ;; Recovering
pay ;; Commerce_pay || competitors ;; Competition || brands ;; Labeling || made ;; Giving
rebranded ;; Labeling
replaced ;; Change_of_leadership
used ;; Using
concert ;; Social_event
support ;; Supporting || launched ;; Process_start
visited ;; Traveling
spanned ;; Expansion
performed ;; Hold || covers ;; Filling
issues ;; Creating || led to ;; Causation || cancellations ;; Change_event_time || scheduled ;; Arranging
citing ;; Adducing || banned ;; Preventing_or_letting
noted ;; Labeling
received ;; Receiving || Described ;; Statement || praised ;; Judgment_communication
earning ;; Earnings_and_losses
filmed ;; Create_artwork || airing ;; Expressing_publicly
released ;; Publishing
fought ;; Hostile_encounter || Massacre ;; Killing
disputed ;; Quarreling || used ;; Using || assaults ;; Attack || forces ;; Hostile_encounter || hostile ;; Hostile_encounter
fled ;; Escaping || declared ;; Expressing_publicly || occurred ;; Presence || planted ;; Placing || incident ;; Killing || ran ;; Self_motion || recounted ;; Statement
disputed ;; Quarreling || attackers ;; Attack || battle ;; Hostile_encounter
wrote ;; Writing || termed ;; Labeling
commented ;; Statement || battle ;; Hostile_encounter
battle ;; Hostile_encounter || cleaned ;; Emptying || made it ;; Causation
killed ;; Killing || massacres ;; Killing
killed ;; Killing || death ;; Death || death ;; Death || Wounded ;; Bodily_harm || survivors ;; Catastrophe || Massacre ;; Killing
included ;; Cause_to_be_included
possessed ;; Getting
fighting ;; Hostile_encounter || limited ;; Limiting || use ;; Using || brought ;; Bringing || gunfire ;; Use_firearm || bear ;; Confronting_problem || contributed ;; Giving
engagement ;; Forming_relationships || command ;; Control
populated ;; Filling || assaulted ;; Committing_crime
defended ;; Defending || assault ;; Attack || held ;; Hold
disaster ;; Catastrophe || forces ;; Hostile_encounter || engagement ;; Forming_relationships
battle ;; Hostile_encounter || massacre ;; Killing
killed ;; Killing || killed ;; Killing || casualties ;; Death
began ;; Process_start || illegal ;; Legality || transport ;; Sending
enforce ;; Action || established ;; Creating || called ;; Labeling
negotiated ;; Communication || give ;; Giving || search ;; Scrutiny || ships ;; Sending || applied ;; Deciding || intercept ;; Preventing_or_letting
refused ;; Agree_or_refuse_to_act
enforced ;; Action || escaped ;; Escaping || Prohibiting ;; Preventing_or_letting || slave ;; Arrest || abolished ;; Action
made ;; Manufacturing || prevent ;; Preventing_or_letting
patrols ;; Patrolling || consisted ;; Cause_to_be_included || due ;; Causation
agreed ;; Agree_or_refuse_to_act || treaty ;; Statement || work together ;; Collaboration || deemed ;; Protest
gave ;; Giving || beginning ;; Process_start
remained ;; Hold
attack ;; Attack
injuries ;; Bodily_harm || resulted in ;; Causation || deaths ;; Death
attack ;; Attack || signing ;; Sign_agreement || halt ;; Process_end
labeled ;; Labeling || terror ;; Terrorism || rebels ;; Hostile_encounter || incident ;; Catastrophe
destruction ;; Destroying || thought ;; Coming_to_believe
hit ;; Attack || denied ;; Agree_or_refuse_to_act
inspecting ;; Scrutiny || fired ;; Use_firearm || assessed ;; Testing || caused ;; Causation
named ;; Name_conferral || accessing ;; Having_or_lacking_access
festival ;; Social_event || held ;; Creating
celebrate ;; Social_event || aims ;; Aiming || select ;; Choosing
intention ;; Aiming || turn ;; Motion || heads ;; Self_motion || omitted ;; Removing
designed ;; Creating || fest ;; Social_event || highlight ;; Statement || emerging ;; Coming_to_be || returning ;; Arriving || met ;; Come_together
presenting ;; Presence || festival ;; Social_event || stays ;; Temporary_stay || setting ;; Placing
expands ;; Expansion
labels ;; Labeling
festival ;; Social_event || held ;; Creating
festival ;; Social_event || staffed ;; Being_in_operation || close ;; Openness
nicknamed ;; Name_conferral || held ;; Hold
cut ;; Removing || interrupted ;; Hindering || set ;; Placing
held ;; Defending || opening ;; Openness || closing ;; Openness || taken place ;; Process_start
camping ;; Social_event || camping ;; Social_event
partnered ;; Collaboration
performing ;; Hold
produced ;; Creating
took place ;; Process_start
included ;; Cause_to_be_included
brands ;; Labeling
performing ;; Hold
jumping ;; Motion
forcing ;; Motion
directed ;; Self_motion || campaign ;; Military_operation || intent ;; Aiming || establish ;; Creating || inhabited ;; Placing
ended ;; Process_end || expansion ;; Expansion || extended ;; Cause_change_of_position_on_a_scale
killed ;; Killing || displaced ;; Hold
developed ;; Coming_to_be || made ;; Manufacturing || turning ;; Change
called ;; Request || conquest ;; Conquering
describe ;; Statement || label ;; Labeling || Conquest ;; Conquering || genocide ;; Catastrophe
occurred ;; Coming_to_be || found ;; Know || massacre ;; Committing_crime
kidnapped ;; Kidnapping || killed ;; Killing
challenging ;; Competition || election ;; Choosing || elections ;; Choosing
identified ;; Check || included ;; Cause_to_be_included
called ;; Name_conferral || massacre ;; Killing
died ;; Death || known ;; Know
labeled ;; Labeling || massacre ;; Killing
erupted ;; Process_start || rebellion ;; Violence || completion ;; Process_end || revolution ;; Reforming_a_system
became ;; Becoming || uprisings ;; Violence || rebellions ;; Violence
movements ;; Social_event || emphasize ;; Cause_change_of_position_on_a_scale
branded ;; Labeling || independence ;; Change_of_leadership || criticizing ;; Judgment_communication || calling ;; Request || calling ;; Request
turned ;; Becoming || deteriorated ;; Cause_change_of_position_on_a_scale
party ;; Social_event || conflict ;; Violence || gains ;; Earnings_and_losses || made ;; Earnings_and_losses || ousted ;; Change_of_leadership || reversed ;; Cause_change_of_position_on_a_scale
rebellion ;; Violence || eruption ;; Process_start || crush ;; Bodily_harm || uprisings ;; Violence || rebellions ;; Violence || subdued ;; Hindering
attacks ;; Attack || ousted ;; Change_of_leadership
clashes ;; Violence
killed ;; Killing || rebellion ;; Violence || rebellion ;; Violence || executed ;; Killing
assassination ;; Killing || resurged ;; Recovering
produced ;; Manufacturing || wrestling ;; Competition
took place ;; Process_start
brands ;; Labeling
match ;; Competition
retained ;; Preserving || pinning ;; Connect
ended ;; Process_end || match ;; Competition || following ;; Self_motion || interference ;; Hindering
perform ;; Hold || interference ;; Hindering || retain ;; Preserving
crowned ;; Change_of_leadership || included ;; Cause_to_be_included
produced ;; Manufacturing
held ;; Defending
titled ;; Name_conferral
used ;; Using || debut ;; Process_start || show ;; Perception_active
extending ;; Change_event_time || shows ;; Perception_active || held ;; Defending
shows ;; Perception_active || held ;; Defending
held ;; Defending || held ;; Defending
select ;; Choosing || occur ;; Coming_to_be
branded ;; Labeling || show ;; Perception_active
earthquake ;; Catastrophe || occurred ;; Presence
killing ;; Killing || injuring ;; Bodily_harm || shock ;; Catastrophe || disasters ;; Catastrophe
adjusted ;; Cause_change_of_position_on_a_scale || loss ;; Earnings_and_losses
effects ;; Influence
reported ;; Reporting || Damage ;; Damaging
damage ;; Damaging || Death ;; Death
death ;; Death || damage ;; Damaging || killed ;; Killing || occurred ;; Presence || collapsed ;; Destroying
death ;; Death || crushed ;; Motion || collapsed ;; Destroying
shock ;; Catastrophe || death ;; Death
caused ;; Causation || caused ;; Causation || earthquake ;; Catastrophe || damage ;; Damaging || damage ;; Damaging || damage ;; Damaging || damage ;; Damaging
earthquake ;; Catastrophe || lower ;; Cause_change_of_position_on_a_scale || strike ;; Attack
earthquake ;; Catastrophe || interview ;; Communication || struck ;; Attack
attended ;; Participation || march ;; Self_motion || March ;; Self_motion || survivors ;; Death || held ;; Hold
complete ;; Arriving
participants ;; Participation || open ;; Openness || armed ;; Bearing_arms || forces ;; Causation
certified ;; Check
competitors ;; Competition || completing ;; Process_end || congratulate ;; Judgment_communication
attend ;; Participation
complete ;; Process_end || taking ;; Cost
participated ;; Participation || record ;; Recording
became ;; Becoming || competed ;; Competition || due ;; Causation
participants ;; Participation || altering ;; Change || trained ;; Education_teaching || running ;; Motion || race ;; Competition || reconsider ;; Deciding || canceling ;; Removing || put out ;; Judgment_communication
participated ;; Participation
carrying ;; Carry_goods || hit ;; Attack || crossing ;; Motion_directional || occurred ;; Coming_to_be
died ;; Death || crash ;; Catastrophe || injured ;; Bodily_harm
crossing ;; Motion_directional || closed ;; Self_motion || occurred ;; Coming_to_be || reported ;; Reporting
began ;; Process_start || crash ;; Catastrophe || find ;; Know || knew ;; Know
scattered ;; Dispersal
arrive ;; Arriving || dead ;; Death || accident ;; Catastrophe || came ;; Self_motion
resigned ;; Change_of_leadership
pledged ;; Commitment
detained ;; Arrest || awarded ;; Award || crossing ;; Motion_directional || relief ;; Assistance || dead ;; Death || injured ;; Bodily_harm || said ;; Telling
occurred ;; Coming_to_be
detonated ;; Attack
killed ;; Killing
died ;; Death || attack ;; Attack
injured ;; Bodily_harm || awarded ;; Award || survived ;; Rescuing
convicted ;; Legal_rulings || jailed ;; Arrest
released ;; Releasing || served ;; Assistance || quashed ;; Removing
began ;; Process_start || sent ;; Sending || murder ;; Killing || attack ;; Attack || charged ;; Judgment_communication || collapsed ;; Destroying
bombing ;; Attack || charged ;; Judgment_communication
recording ;; Recording || concert ;; Social_event || tours ;; Traveling || opening ;; Openness || opening ;; Openness || embarked ;; Process_start
performed ;; Hold
records ;; Recording || spanning ;; Body_movement || records ;; Recording || set ;; Placing || extended ;; Expansion || sold ;; Commerce_sell
earned ;; Earnings_and_losses || awards ;; Award
received ;; Receiving
ranked ;; Placing
hijacking ;; Kidnapping || hijacking ;; Kidnapping
occurred ;; Coming_to_be
consisted ;; Forming_relationships
attached ;; Connect || sticks ;; Motion || suspended ;; Process_end
attached ;; Connect
fired ;; Use_firearm || pulled ;; Motion || demanding ;; Request || disabled ;; Destroying || punched ;; Bodily_harm
assisted ;; Assistance || travelling ;; Traveling
awarded ;; Award || commended ;; Judgment_communication
destroying ;; Destroying || causing ;; Causation || faced ;; Confronting_problem || detonating ;; Attack
sentenced ;; Legal_rulings || destroy ;; Destroying
argued ;; Quarreling || appealed ;; Request
murder ;; Committing_crime || served ;; Assistance || arrested ;; Arrest
convicted ;; Legal_rulings || sentenced ;; Legal_rulings
held ;; Hold
earned ;; Earnings_and_losses || match ;; Competition || share ;; Getting
qualifying ;; Getting
match ;; Competition || winning ;; Getting || win ;; Getting || joining ;; Becoming_a_member
play ;; Competition || stroke ;; Hold || changed ;; Change
defeated ;; Conquering
advanced ;; Cause_to_make_progress
played ;; Competition || played ;; Competition
held ;; Hold
competition ;; Competition || Championship ;; Competition
divided ;; Creating
play ;; Participation || playing ;; Competition
awarded ;; Award || ranking ;; Competition || onward ;; Process_start
competition ;; Competition || altered ;; Change
seen ;; Know
finished ;; Process_end || replaced ;; Change
seen ;; Know
replaced ;; Change
placed ;; Placing || promoted ;; Cause_change_of_position_on_a_scale || champions ;; Competition || relegated ;; Change
defended ;; Defending
undertaken ;; Action
targeted ;; Aiming || running ;; Self_motion || force ;; Request || return ;; Motion
crossed ;; Motion_directional || rammed ;; Self_motion
packed ;; Filling || detonated ;; Damaging || putting ;; Causation
destroy ;; Destroying || landed ;; Arriving
sank ;; Self_motion || set ablaze ;; Causation
surrounded ;; Surrounding || surrendered ;; Surrendering || defending ;; Defending || escape ;; Escaping || fought ;; Hostile_encounter || ran out of ;; Expend_resource
killed ;; Killing || became ;; Becoming || returned ;; Motion
killed ;; Killing || included ;; Cause_to_be_included || exploded ;; Use_firearm
recognise ;; Know || awarded ;; Award
awarded ;; Award
called ;; Name_conferral
tournament ;; Competition
held ;; Hold
competition ;; Competition || defeated ;; Earnings_and_losses
finished ;; Process_end || defeating ;; Earnings_and_losses
viewed ;; Change_sentiment || seen ;; Change_sentiment
failed ;; Earnings_and_losses || failure ;; Earnings_and_losses || winning ;; Earnings_and_losses || adjust ;; Confronting_problem || used ;; Using || blaming ;; Judgment_communication
recovered ;; Recovering || win ;; Earnings_and_losses || leaving ;; Departing || scored ;; Hold
left ;; Causation
topped ;; Placing
finished ;; Process_end || qualify ;; Getting || topped ;; Placing || defeating ;; Earnings_and_losses || drew ;; Motion || drew ;; Motion
ended up ;; Process_end || losing ;; Earnings_and_losses
replay ;; Competition || settle ;; Response
dominated ;; Control || show ;; Presence || scored ;; Hold
strike ;; Body_movement || found ;; Know || going ;; Motion
pressed ;; Self_motion
came ;; Arriving || shots ;; Motion || made ;; Action || intercepted ;; Preventing_or_letting
flew ;; Motion || denied ;; Agree_or_refuse_to_act || scored ;; Hold || scored ;; Hold
awarded ;; Award || going out ;; Self_motion || deflected ;; Motion || forcing ;; Causation || blocked ;; Hindering || scored ;; Hold
send ;; Sending || going ;; Arriving || attack ;; Hostile_encounter || settled ;; Response || scored ;; Hold
hammering ;; Hold
clinched ;; Getting || made ;; Action
revenge ;; Revenge || defeated ;; Earnings_and_losses
started ;; Process_start || scored ;; Hold
began ;; Process_start || find ;; Know
disputed ;; Quarreling || claimed ;; Statement || flown ;; Motion || ruled ;; Legal_rulings || counted ;; Judgment_communication || brushed ;; Removing
create ;; Creating
allowed ;; Preventing_or_letting || hung ;; Body_movement
captured ;; Conquering
launched ;; Process_start
delivered ;; Military_operation
abandoned ;; GiveUp || fled ;; Escaping
called ;; Name_conferral
captured ;; Conquering || process ;; Self_motion
recovered ;; Recovering
defending ;; Defending || killed ;; Killing
killed ;; Killing
attack ;; Attack || massacre ;; Killing || War ;; Hostile_encounter || took place ;; Process_start
killed ;; Killing
deaths ;; Death || used ;; Using || announced ;; Expressing_publicly || massacre ;; Killing || resulted ;; Causation || executed ;; Killing || incidents ;; Catastrophe
stated ;; Statement || massacre ;; Killing || reported ;; Reporting || committed ;; Commitment
stated ;; Statement || killing ;; Killing || massacre ;; Killing || released ;; Publishing || report ;; Reporting || concluding ;; Coming_to_believe
access ;; Having_or_lacking_access || understanding ;; Know || victims ;; Catastrophe
bolstered ;; Supporting || supporting ;; Supporting
alleged ;; Statement || terrorist ;; Terrorism || warned ;; Warning || killings ;; Killing || speak ;; Expressing_publicly || forces ;; Military_operation
received ;; Receiving || published ;; Publishing || published ;; Publishing || support ;; Supporting || coverage ;; Social_event || report ;; Reporting || report ;; Reporting
stated ;; Statement || massacre ;; Killing || reported ;; Reporting || claimed ;; Statement
named ;; Name_conferral || described ;; Statement || entered ;; Arriving
killers ;; Killing
condemned ;; Judgment_communication || firing ;; Use_firearm
expelled ;; Removing
massacre ;; Killing || condemn ;; Judgment_communication || called ;; Request || inquiry ;; Criminal_investigation || voted ;; Deciding || voting ;; Choosing
published ;; Publishing || culpability ;; Committing_crime || report ;; Reporting || attacks ;; Attack
states ;; Statement || rule out ;; Removing
noted ;; Know || hampered ;; Hindering || investigation ;; Criminal_investigation || viewed ;; Perception_active || access ;; Having_or_lacking_access || report ;; Reporting
organized ;; Arranging || championship ;; Competition || held ;; Hold
competed ;; Competition || compete ;; Competition || granted ;; Giving
planning ;; Arranging
canceled ;; Change_event_time
played ;; Competition || playing ;; Competition || final ;; Competition
awarded ;; Award
awarded ;; Award || bowling ;; Social_event || batting ;; Motion
rotated ;; Motion || held ;; Hold
received ;; Receiving || test ;; Testing || beat ;; Competition || become ;; Becoming
awarded ;; Award
received ;; Receiving
derailed ;; Self_motion
caused ;; Causation || killed ;; Killing || injured ;; Bodily_harm || damage ;; Damaging
deadliest ;; Killing
investigations ;; Criminal_investigation || gone ;; Motion || derailed ;; Motion || posted ;; Getting || limit ;; Limiting || found ;; Know
reaching ;; Arriving || gone ;; Motion || admitted ;; Statement
investigating ;; Criminal_investigation || prevented ;; Preventing_or_letting || control ;; Control || installed ;; Placing || said ;; Statement
requires ;; Imposing_obligation
demanded ;; Request || began ;; Process_start || involving ;; Cause_to_be_included || improved ;; Cause_change_of_position_on_a_scale || implementing ;; Hold
released ;; Publishing || final ;; Process_end || accident ;; Catastrophe || report ;; Reporting
found ;; Know || reiterating ;; Statement || prevented ;; Preventing_or_letting || accident ;; Catastrophe || cause ;; Causation || entered ;; Arriving
contributing ;; Giving
hampered ;; Hindering || working ;; Employment || begun ;; Process_start || shift ;; Change || examination ;; Scrutiny || diagnosed ;; Check || adjust ;; Change
report ;; Reporting || employees ;; Employment || positions ;; Placing || faulted ;; Judgment_communication
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || control ;; Control || fought ;; Hostile_encounter || referred ;; Adducing || War ;; Military_operation
combined ;; Cause_to_amalgamate
fought ;; Hostile_encounter || control ;; Control
establishing ;; Creating || relied ;; Supply || called ;; Name_conferral
battle ;; Hostile_encounter || led to ;; Causation || actions ;; Emptying || formation ;; Coming_to_be
join ;; Participation || prevented ;; Preventing_or_letting || executed ;; Killing
banded together ;; Come_together || response ;; Response || drive ;; Self_motion
formed ;; Coming_to_be
battle ;; Hostile_encounter || awarded ;; Award
threw ;; Motion
rescuing ;; Rescuing || injured ;; Bodily_harm || awarded ;; Award
battle ;; Hostile_encounter || marked ;; Recording || use ;; Using || war ;; Military_operation
detonated ;; Committing_crime || injuring ;; Bodily_harm
competition ;; Competition || event ;; Social_event
match ;; Competition || defeated ;; Conquering
winning ;; Getting || awarded ;; Award
contacting ;; Communication || created ;; Creating || started ;; Process_start || became ;; Becoming || folded ;; Hold
developed ;; Coming_to_be || organized ;; Arranging
selecting ;; Choosing || competitions ;; Competition || flying ;; Self_motion
competition ;; Competition || announced ;; Expressing_publicly || chose ;; Choosing || played ;; Competition || cancelled ;; Removing
brought in ;; Bringing
competition ;; Competition || operate ;; Being_in_operation || selected ;; Choosing || running ;; Hold
competing ;; Competition
built ;; Building || flown ;; Motion || competed ;; Competition
used ;; Using || competition ;; Competition || played ;; Competition || equipped ;; Supply
use ;; Using
open ;; Openness || competitions ;; Competition || orchestrated ;; Arranging || watched ;; Perception_active
drive ;; Motion || discovered ;; Know
agreed ;; Agree_or_refuse_to_act || underwrite ;; Commitment
festival ;; Social_event || festival ;; Social_event || held ;; Hold
received ;; Receiving || conference ;; Communication || award ;; Award || festival ;; Social_event
combine ;; Cause_to_amalgamate || host ;; Come_together || add up ;; Cause_change_of_position_on_a_scale
evolved ;; Coming_to_be || started ;; Process_start || become ;; Becoming || festival ;; Social_event
called ;; Name_conferral || festival ;; Social_event
festival ;; Social_event || dropped ;; Motion_directional || taken place ;; Process_start
festival ;; Social_event || sold ;; Commerce_sell
remains ;; Presence
organized ;; Arranging || festival ;; Social_event
rising ;; Cause_change_of_position_on_a_scale || controversy ;; Judgment_communication
heckled ;; Judgment_communication
include ;; Cause_to_be_included || festival ;; Social_event
open ;; Openness || open ;; Openness || arriving ;; Arriving || camping ;; Social_event || festival ;; Social_event
allowing ;; Preventing_or_letting || arrive ;; Arriving || festival ;; Social_event || tickets ;; Commerce_pay || camp ;; Placing
become ;; Becoming || attracted ;; Influence
go to ;; Self_motion
occurred ;; Presence || bombings ;; Terrorism
killed ;; Killing || attacks ;; Attack || injured ;; Bodily_harm || bomb ;; Terrorism
activated ;; Process_start || concealed ;; Hiding_objects || found ;; Know || explosives ;; Attack
defused ;; Resolve_problem || detonate ;; Attack
violent ;; Violence || killing ;; Killing || clash ;; Violence || bombings ;; Attack || riots ;; Violence || led ;; Causation || firing ;; Use_firearm || murder ;; Killing
riots ;; Violence || named ;; Name_conferral || happened ;; Presence || murder ;; Killing
found ;; Know
target ;; Aiming || found out ;; Know || party ;; Participation || election ;; Change_of_leadership || blasts ;; Attack
formed ;; Coming_to_be || judicial ;; Legality
confirmed ;; Check || attacks ;; Attack || probe ;; Scrutiny
accepted ;; Receiving || recommendations ;; Convincing || report ;; Reporting || assembly ;; Participation || tabled ;; Bringing
began ;; Process_start || trial ;; Legality || examined ;; Check
found ;; Know || hatching ;; Creating || trigger ;; Causation || convicted ;; Commitment
killed ;; Killing || blasts ;; Attack || suspected ;; Criminal_investigation
awarded ;; Award || loss ;; Earnings_and_losses || reported ;; Reporting || victims ;; Bodily_harm
awarded ;; Award || victim ;; Bodily_harm || fixed ;; Resolve_problem
played ;; Competition || Final ;; Competition
final ;; Process_end || competition ;; Competition
appearing ;; Presence || lost ;; Earnings_and_losses
appearing ;; Presence || final ;; Competition
competition ;; Competition || entered ;; Arriving
victory ;; Getting
victory ;; Getting || beat ;; Competition
controlled ;; Control || Watched ;; Perception_active || exchanges ;; Exchange || scored ;; Getting || took ;; Conquering
scored ;; Getting || extend ;; Expansion
scored ;; Getting || reduce ;; Cause_change_of_position_on_a_scale
score ;; Getting || attacks ;; Attack
competition ;; Competition
named ;; Name_conferral || awarded ;; Award
praised ;; Rewards_and_punishments
play ;; Competition || competition ;; Competition || final ;; Process_end || win ;; Getting || qualified ;; Becoming_a_member
competed ;; Competition || championship ;; Competition
pulled out ;; GiveUp || tournament ;; Competition
played ;; Competition || matches ;; Competition
awarded ;; Award
awarded ;; Giving || bowling ;; Body_movement || batting ;; Body_movement
qualified ;; Competition || beating ;; Competition
win ;; Getting || beat ;; Competition
occurred ;; Coming_to_be
fell ;; Motion_directional
died ;; Death || damaged ;; Damaging || said ;; Telling
storm ;; Catastrophe || dropping ;; Motion
affected ;; Influence
victims ;; Death || running ;; Motion
hit ;; Attack || reported ;; Reporting
reached ;; Arriving || escape ;; Escaping || submerging ;; Motion
deaths ;; Death
race ;; Competition || held ;; Hold
named ;; Name_conferral || race ;; Competition
hosted ;; Social_event
organized ;; Arranging || race ;; Competition
founded ;; Creating
forbidden ;; Preventing_or_letting
awarded ;; Award
rescue ;; Rescuing
sent ;; Sending || surrender ;; Surrendering
shifted ;; Removing
executed ;; Killing
send ;; Sending || rescue ;; Rescuing || developed ;; Coming_to_be
reach ;; Arriving
cover ;; Hiding_objects || raid ;; Attack
killed ;; Killing || attack ;; Attack || suffered ;; Confronting_problem
escorted ;; Patrolling
rescue ;; Rescuing || tell ;; Telling || allowed ;; Preventing_or_letting || sparked ;; Causation
recognized ;; Know || awarded ;; Award
raid ;; Attack || depicted ;; Create_artwork
battle ;; Hostile_encounter
attacked ;; Attack || entered ;; Arriving || made ;; Manufacturing
failed ;; Earnings_and_losses || due ;; Causation || captured ;; Conquering
captured ;; Conquering
Helped ;; Assistance || reached ;; Arriving || occupied ;; Control
averted ;; Preventing_or_letting || went to ;; Motion || went to ;; Motion || surrendering ;; Surrendering || acquired ;; Getting || concluded ;; Process_end
order ;; Arranging || awarded ;; Award
awarded ;; Award || mutinied ;; Change_of_leadership
bombing ;; Attack || bombing ;; Attack || bombing ;; Attack || referred ;; Name_conferral || occurred ;; Presence || detonated ;; Committing_crime
devastated ;; Destroying || killed ;; Killing || damage ;; Damaging || blast ;; Attack || causing ;; Causation
sent ;; Sending || warnings ;; Warning || evacuated ;; Emptying
killed ;; Killing || injured ;; Bodily_harm
attack ;; Attack || ceasefire ;; Process_end || marked ;; Recording || came ;; Action || declaring ;; Expressing_publicly
agreed ;; Agree_or_refuse_to_act || negotiations ;; Communication || demanded ;; Request || bombing ;; Attack || resumed ;; Process_start || ceasefire ;; Process_end || campaign ;; Military_operation || allowed ;; Preventing_or_letting
bombing ;; Attack || dropped ;; Removing
detonated ;; Committing_crime
bombing ;; Attack || convicted ;; Rewards_and_punishments
released ;; Releasing || agreement ;; Sign_agreement
Assassination ;; Killing || murder ;; Killing
awarded ;; Award
ambushed ;; Attack || shot ;; Use_firearm
death ;; Death || condemned ;; Judgment_communication
called ;; Statement || death ;; Death || met ;; Hold || service ;; Assistance
death ;; Death || declaration ;; Expressing_publicly || helped ;; Assistance || contribute ;; Causation
preparing ;; GetReady
become ;; Becoming
includes ;; Cause_to_be_included
awarded ;; Award
naked ;; Wearing
organized ;; Arranging || promote ;; Cause_change_of_position_on_a_scale
draws ;; Create_artwork
done ;; Self_motion
Participants ;; Participation
participating ;; Participation
hide ;; Hiding_objects || cover ;; Hiding_objects || bear ;; Bringing || wear ;; Wearing
runs ;; Self_motion || ran ;; Self_motion || celebrate ;; Social_event
criticized ;; Judgment_communication || prohibited ;; Preventing_or_letting
investigation ;; Criminal_investigation || called ;; Name_conferral || determine ;; Deciding
contested ;; Competition
appeared ;; Arriving
scored ;; Getting
became ;; Becoming || score ;; Getting
decided ;; Deciding || select ;; Choosing || scored ;; Getting
final ;; Competition || met ;; Come_together || rearranged ;; Change_event_time
selected ;; Choosing || scoring ;; Getting
wearing ;; Wearing || announced ;; Expressing_publicly || due ;; Causation
wear ;; Wearing || wore ;; Wearing || allowed ;; Preventing_or_letting || came ;; Arriving
played ;; Competition || change ;; Change || wear ;; Wearing || instructed ;; Request
died ;; Death
died ;; Death
tour ;; Traveling || concert ;; Social_event || tour ;; Traveling
stated ;; Statement || Tour ;; Traveling || continuation ;; Expansion || tour ;; Traveling || visited ;; Traveling
existing ;; Presence || revising ;; Change || assembled ;; Come_together
mounted ;; Connect || support ;; Supporting || tour ;; Traveling
set ;; Placing || comprised ;; Cause_to_be_included || created ;; Creating || written ;; Writing
wore ;; Wearing || designed ;; Creating
given ;; Giving || choosing ;; Choosing || tour ;; Traveling
concert ;; Social_event
supported ;; Supporting
confirmed ;; Check || going out ;; Self_motion
hired on ;; Employment
supporting ;; Supporting || started ;; Process_start || consisted ;; Cause_to_be_included
statement ;; Statement || prompting ;; Causation || go to ;; Motion
concert ;; Social_event
playing ;; Competition || dancing ;; Social_event || accompanied ;; Arranging
garnered ;; Getting
prompting ;; Causation || sold ;; Commerce_sell || announced ;; Expressing_publicly || add ;; Cause_to_be_included
ending ;; Process_end || dubbed ;; Name_conferral || grossing ;; Earnings_and_losses
concert ;; Social_event || recognized ;; Know
received ;; Receiving || production ;; Manufacturing || concert ;; Social_event || touring ;; Traveling
hanging on ;; Connect || wearing ;; Wearing || met with ;; Response
condemned ;; Judgment_communication || performance ;; Hold
responded ;; Response || dying ;; Death || bring ;; Bringing || performance ;; Hold || saying ;; Statement
recorded ;; Recording || broadcast ;; Statement
recording ;; Recording || titled ;; Name_conferral || Tour ;; Traveling || released ;; Publishing
found ;; Know || called ;; Name_conferral || festivities ;; Social_event || span ;; Expansion
provide ;; Supply || closed to ;; Having_or_lacking_access || set up ;; Building
dressed ;; Wearing
double ;; Cause_change_of_position_on_a_scale
limit ;; Limiting || allows ;; Preventing_or_letting || stay ;; Temporary_stay
spends ;; Commerce_pay
help ;; Assistance || brings ;; Bringing || surrounding ;; Surrounding || control ;; Control
Parking ;; Placing || available ;; Having_or_lacking_access
permit ;; Preventing_or_letting || blocked off ;; Hindering
costs ;; Cost || parking ;; Placing || offset ;; Cause_change_of_position_on_a_scale || charged ;; Commerce_pay
provides ;; Giving
Steeplechase ;; Competition || race ;; Competition || running ;; Self_motion
attracted ;; Influence
race ;; Competition || trained ;; Education_teaching
wore ;; Wearing
set ;; GetReady
leading to ;; Causation || race ;; Competition || taking ;; Causation
achieved ;; Achieve || painting ;; Create_artwork
killed ;; Killing || murderer ;; Killing || using ;; Using
planted ;; Placing || delivered ;; Sending
identified ;; Check || dress ;; Wearing || coming to ;; Motion || wearing ;; Wearing || spotted ;; Know
command ;; Control || deliver ;; Sending || convinced ;; Request
obtain ;; Getting
death ;; Death || convicted ;; Legal_rulings || sentenced ;; Legal_rulings || executed ;; Rewards_and_punishments
tour ;; Traveling || Tour ;; Traveling
included ;; Cause_to_be_included || confirmed ;; Check || spanned ;; Expansion || show ;; Social_event
festivals ;; Social_event || venues ;; Placing
included ;; Cause_to_be_included || played ;; Competition || shows ;; Social_event || bands ;; Collaboration
wore ;; Wearing || masks ;; Hiding_objects || band ;; Collaboration
promote ;; Cause_change_of_position_on_a_scale || changed ;; Change || streamed ;; Motion
streams ;; Motion || members ;; Participation || view ;; Perception_active
recorded ;; Recording || tour ;; Traveling || television ;; Communication || aired ;; Expressing_publicly || aired ;; Expressing_publicly || shows ;; Presence
perform ;; Hold || Festival ;; Social_event || Estranged ;; Forming_relationships || took ;; Motion
final ;; Process_end
tour ;; Traveling || concert ;; Social_event
tour ;; Traveling || festivals ;; Social_event || shows ;; Social_event
tour ;; Traveling || replaced ;; Removing
tour ;; Traveling || started ;; Process_start || set ;; Process_end
twisted ;; Bodily_harm
leave ;; Departing || transport ;; Sending
restricted ;; Hindering || wear ;; Wearing
suffered ;; Bodily_harm || cancel ;; Change_event_time || forced ;; Causation
competition ;; Competition || held ;; Hold
tour ;; Traveling || supported ;; Supporting
revolt ;; Change_of_leadership
emerged ;; Coming_to_be || suppressed ;; Hindering
suppressed ;; Hindering || took ;; Conquering
marked ;; Recording || association ;; Collaboration || got ;; Getting || wore ;; Wearing
used ;; Using
concert ;; Social_event || Tour ;; Traveling || tour ;; Traveling
marked ;; Recording || renewed ;; Recovering || tour ;; Traveling || replaced ;; Change_of_leadership
attendance ;; Participation || tour ;; Traveling || tour ;; Traveling || grossing ;; Earnings_and_losses
due ;; Causation || tour ;; Traveling
tour ;; Traveling || played ;; Action || spanned ;; Expansion
selections ;; Choosing || shied ;; Change_sentiment || included ;; Cause_to_be_included || performing ;; Hold || returning ;; Arriving || wearing ;; Wearing || playing ;; Action
shelved ;; Placing || reappeared ;; Coming_to_be
released ;; Publishing || tour ;; Traveling
concert ;; Social_event || tour ;; Traveling
wore ;; Wearing || leg ;; Process_end || released ;; Publishing || performed ;; Hold
referred ;; Name_conferral
starts ;; Process_start || ritual ;; Rite || smashing ;; Body_movement
expanded ;; Expansion
includes ;; Cause_to_be_included
attend ;; Participation
dress up ;; Wearing
included ;; Cause_to_be_included
criticized ;; Judgment_communication || questioned ;; Communication
defended ;; Defending
given ;; Giving
abandon ;; GiveUp || putsch ;; Change_of_leadership || putsch ;; Change_of_leadership || coup ;; Change_of_leadership || force ;; Request
organised ;; Arranging || war ;; Military_operation || took place ;; Process_start
negotiations ;; Communication || opposed ;; Agree_or_refuse_to_act || started ;; Process_start
joined ;; Becoming_a_member || stated ;; Statement || coup ;; Change_of_leadership || coup ;; Change_of_leadership || planning ;; Arranging || considered ;; Coming_to_believe || put ;; Placing
control ;; Control || coup ;; Change_of_leadership
operation ;; Military_operation || led ;; Conquering || descending on ;; Arriving
refused ;; Agree_or_refuse_to_act || coup ;; Change_of_leadership || join ;; Becoming_a_member || demand ;; Request
coup ;; Change_of_leadership || forbidden ;; Preventing_or_letting || order ;; Request || given ;; Giving || resist ;; Defending
dressed ;; Wearing || help ;; Assistance || made ;; Action
centred ;; Placing
named ;; Name_conferral || worn ;; Wearing
supported ;; Supporting || involved ;; Cause_to_be_included
safeguard ;; Preserving
withdrew ;; Escaping || retaliated ;; Revenge || fought ;; Hostile_encounter || held ;; Hold
fought ;; Hostile_encounter
reported ;; Reporting
killed ;; Killing
killed ;; Killing
found ;; Know || assorted ;; Cause_to_amalgamate
attacked ;; Attack
attacked ;; Attack || captured ;; Arrest
fought ;; Hostile_encounter
found ;; Know || hampered ;; Hindering || build ;; Building
saw ;; Self_motion
supported ;; Supporting
end ;; Process_end
Involved ;; Participation
shootout ;; Use_firearm || confrontation ;; Hostile_encounter
killed ;; Killing || injured ;; Bodily_harm || damaged ;; Damaging || destroyed ;; Destroying
entered ;; Arriving || robbed ;; Robbery
confronted ;; Confronting_problem || exited ;; Departing || shootout ;; Use_firearm || ensued ;; Coming_to_be
exchange ;; Exchange || flee ;; Escaping
wounded ;; Bodily_harm || succumbed ;; Death || wound ;; Bodily_harm || death ;; Death || arrival ;; Arriving || incapacitated ;; Destroying
using ;; Using || taking ;; Conquering || modified ;; Change || robbed ;; Robbery || control ;; Control || firing ;; Use_firearm || control ;; Control
carried ;; Carry_goods || equipped ;; Supply
carried ;; Carry_goods || modified ;; Change
responding ;; Response || protected ;; Defending || wore ;; Wearing
arrived ;; Arriving || wounded ;; Bodily_harm || commandeered ;; Using
sparked ;; Causation || debate ;; Communication || upgrade ;; Cause_to_make_progress
injuries ;; Bodily_harm || fired ;; Use_firearm || used ;; Using || shootout ;; Hostile_encounter || regarded ;; Judgment_communication || battles ;; Hostile_encounter
fired ;; Use_firearm || Combined ;; Cause_to_amalgamate
fired ;; Use_firearm
match ;; Competition || Final ;; Competition || final ;; Process_end || played ;; Competition
match ;; Competition || host ;; Social_event || played ;; Competition
encounter ;; Hostile_encounter || Final ;; Competition
scoring ;; Getting || tallying ;; Getting || drop ;; Motion_directional || drop ;; Motion_directional || drop ;; Motion_directional || victory ;; Earnings_and_losses || sealed ;; Getting || scored ;; Getting
match ;; Competition || wearing ;; Wearing || end ;; Process_end
presented ;; Presence
support ;; Supporting || Tour ;; Traveling || launched ;; Process_start
commenced ;; Process_start
planned ;; Arranging || end ;; Process_end || tour ;; Traveling
sold ;; Commerce_sell || extended ;; Expansion || beginning ;; Process_start || announcing ;; Expressing_publicly
tour ;; Traveling || visited ;; Traveling
concluded ;; Process_end
tour ;; Traveling || taken ;; Creating || composed ;; Come_together
see ;; Perception_active
set ;; Placing
wore ;; Wearing
wore ;; Wearing || dress ;; Wearing || included ;; Cause_to_be_included
received ;; Receiving || praised ;; Judgment_communication || shows ;; Social_event
tour ;; Traveling
sales ;; Commerce_sell || ranked ;; Placing || grossed ;; Earnings_and_losses
sales ;; Commerce_sell || ranked ;; Placing
brings ;; Bringing || shows ;; Social_event
released ;; Publishing
fought ;; Hostile_encounter
fought ;; Hostile_encounter || revolt ;; Change_of_leadership || achieved ;; Achieve || elected ;; Choosing
viewed ;; Perception_active || revolution ;; Change_of_leadership
dress ;; Wearing || war ;; Hostile_encounter || called ;; Name_conferral
ended ;; Process_end || ruled ;; Legal_rulings || death ;; Death
began ;; Process_start
supported ;; Supporting
supported ;; Supporting || party ;; Social_event
killed ;; Killing || emerged ;; Coming_to_be || return ;; Arriving
supported ;; Supporting
gain ;; Getting || rebelling ;; Hostile_encounter || control ;; Control || remained ;; Presence
left ;; Departing || divided ;; Change
fought ;; Hostile_encounter || control ;; Control
received ;; Receiving || received ;; Receiving || support ;; Supporting || support ;; Supporting
followed ;; Self_motion
participated ;; Participation || conflict ;; Hostile_encounter
fought ;; Hostile_encounter
capturing ;; Conquering || advanced ;; Cause_to_make_progress
besieged ;; Besieging
captured ;; Conquering || became ;; Becoming || cut off ;; Removing
declared ;; Expressing_publicly || conflict ;; Hostile_encounter || entered ;; Arriving
fled ;; Escaping
stayed ;; Temporary_stay || associated ;; Collaboration || persecuted ;; Killing
fused ;; Cause_to_amalgamate
became ;; Becoming || inspired ;; Influence || occurred ;; Coming_to_be
captured ;; Conquering || occurred ;; Coming_to_be
varying ;; Change || controlled ;; Control || took place ;; Process_start || killings ;; Killing
performed ;; Hold
called ;; Name_conferral
called ;; Name_conferral || got ;; Getting || tour ;; Traveling || says ;; Telling
got ;; Getting || gone back ;; Recovering
focusing ;; Aiming || wearing ;; Wearing || talking ;; Telling
talking ;; Telling
talking ;; Telling
put out ;; Placing
ran ;; Self_motion
recorded ;; Recording
tour ;; Traveling || took ;; Creating
performed ;; Hold
performed ;; Hold
performed ;; Hold
performing ;; Hold || came to ;; Motion
express ;; Expressing_publicly || Election ;; Choosing || election ;; Choosing || election ;; Choosing
claiming ;; Statement
chosen ;; Choosing
claimed ;; Statement || attended ;; Participation
accusing ;; Judgment_communication || election ;; Choosing
claimed ;; Statement || election ;; Choosing
gaining ;; Getting || losing ;; Earnings_and_losses || elections ;; Choosing
urged ;; Convincing || wear ;; Wearing
Drowned ;; Death || concert ;; Social_event || support ;; Supporting || tour ;; Traveling
delayed ;; Change_event_time || married ;; Forming_relationships || gave ;; Giving || start ;; Process_start || birth ;; Presence || filming ;; Create_artwork
planned ;; Arranging || decided ;; Deciding || appointed ;; Choosing || signing up ;; Employment
designed ;; Creating || indicated ;; Statement
studied ;; Education_teaching || included ;; Cause_to_be_included
released ;; Publishing || added ;; Cause_to_be_included
wearing ;; Wearing || displayed ;; Perception_active
wore ;; Wearing || performed ;; Hold
performed ;; Hold
dancing ;; Social_event
complimented ;; Judgment_communication || appreciated ;; Change_sentiment
become ;; Becoming
selling ;; Commerce_sell || grossed ;; Earnings_and_losses || playing ;; Action
received ;; Receiving || lost ;; Earnings_and_losses || production ;; Manufacturing
concert ;; Social_event || broadcast ;; Expressing_publicly
released ;; Publishing || titled ;; Name_conferral
occurred ;; Coming_to_be || shooting ;; Use_firearm
ran ;; Self_motion || fire ;; Use_firearm || wearing ;; Wearing
fired ;; Use_firearm || killing ;; Killing || wounding ;; Bodily_harm
suicide ;; Killing || entered ;; Arriving || dropping ;; Body_movement || descending ;; Motion_directional || committed ;; Committing_crime || drawn ;; Motion
violence ;; Violence || believed ;; Know
prohibiting ;; Preventing_or_letting
tour ;; Traveling
released ;; Publishing || began ;; Process_start || support ;; Supporting || debut ;; Process_start || tour ;; Traveling || launched ;; GetReady
began ;; Process_start
arrested ;; Arrest || donning ;; Wearing || tour ;; Traveling || taken ;; Motion
freed ;; Releasing || appeared ;; Presence
played ;; Competition || concluded ;; Process_end
played ;; Participation
produced ;; Manufacturing
took place ;; Process_start || involved ;; Participation
change ;; Change || produced ;; Manufacturing || use ;; Using
win ;; Getting || defeated ;; Conquering
match ;; Competition || defeated ;; Conquering
match ;; Competition || defeated ;; Conquering || defeated ;; Conquering || retain ;; Preserving || defeated ;; Conquering
fought ;; Hostile_encounter
intervened ;; Hindering
invaded ;; Attack || occupied ;; Control || occupied ;; Control
became ;; Becoming || known ;; Know
became ;; Becoming || surrendered ;; Surrendering || fled ;; Escaping
supported ;; Supporting || allied ;; Collaboration || receiving ;; Receiving
organizing ;; Arranging
declared ;; Expressing_publicly || pushed ;; Motion || took ;; Action
granted ;; Giving || invaded ;; Attack || aid ;; Assistance || allied ;; Collaboration
found ;; Know || crossed ;; Motion_directional
triggered ;; Causation
granted ;; Giving || left ;; Departing
gave ;; Giving || abandon ;; GiveUp || fight ;; Hostile_encounter
marching ;; Self_motion || crossing ;; Motion_directional || surrender ;; Surrendering || surprised ;; Change_sentiment
abandon ;; GiveUp
ravaged ;; Destroying
decided ;; Deciding || attack ;; Attack || returning ;; Self_motion
attack ;; Attack || withstood ;; Defending || pursued ;; Self_motion
defending ;; Defending || gained ;; Getting || negotiated ;; Agree_or_refuse_to_act
died ;; Death || settled ;; Creating
became ;; Becoming || keep ;; Preserving || returned ;; Self_motion
concluded ;; Process_end
attack ;; Attack || War ;; Hostile_encounter || occurred ;; Presence
struck ;; Attack
death ;; Death
attack ;; Attack || use ;; Using || War ;; Hostile_encounter
attack ;; Attack || requested ;; Request || attack ;; Attack || allow ;; Preventing_or_letting || visit ;; Traveling || ceasefire ;; Process_end || investigate ;; Research || called ;; Request
request ;; Request || visited ;; Traveling || granted ;; Giving || investigated ;; Research
confirmed ;; Check || found ;; Know || use ;; Using || delivered ;; Sending || used ;; Using || attack ;; Attack || targeting ;; Aiming || causing ;; Causation || investigation ;; Scrutiny || report ;; Reporting || casualties ;; Death
used ;; Using || manipulate ;; Hold || indicated ;; Statement || access ;; Having_or_lacking_access
stated ;; Statement || used ;; Using || attack ;; Attack || used ;; Using
attack ;; Attack || stated ;; Statement || carried out ;; Action || forces ;; Military_operation
blamed ;; Judgment_communication || attack ;; Attack || attack ;; Attack || war ;; Hostile_encounter || operation ;; Attack || draw ;; Removing
characterized ;; Statement
debated ;; Quarreling || forces ;; Military_operation
filed ;; Submitting_documents || attack ;; Attack || use ;; Using || response ;; Response || resolution ;; Writing
destruction ;; Destroying || declared ;; Expressing_publicly || stockpiles ;; Preserving || intention ;; Aiming || join ;; Becoming_a_member || averted ;; Preventing_or_letting || accepted ;; Agree_or_refuse_to_act
declared ;; Expressing_publicly || destroyed ;; Destroying || noted ;; Know || production ;; Creating
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || become ;; Becoming
controlling ;; Control || control ;; Control || surrender ;; Surrendering || licensed ;; Preventing_or_letting || come ;; Arriving
battle ;; Hostile_encounter || started ;; Process_start || crushed ;; Destroying
Fighting ;; Hostile_encounter || concentrated ;; Preserving || district ;; Placing
deaths ;; Death || cross-fire ;; Hostile_encounter || made ;; Manufacturing
defeated ;; Conquering || disbanded ;; Removing || collapsed ;; Destroying
concert ;; Social_event
Visiting ;; Traveling || supported ;; Supporting
backing ;; Supporting || backed ;; Action || accompanied ;; Arranging
selected ;; Choosing || comprised ;; Containing
accessible ;; Having_or_lacking_access || shows ;; Presence || garnered ;; Getting || performance ;; Influence || emphasised ;; Expressing_publicly
Recurring ;; Motion || led to ;; Causation
uninterrupted ;; Motion
scheduled ;; Arranging || canceled ;; Change_event_time || diagnosed ;; Deciding
continuing ;; Preserving || postponement ;; Change_event_time || prompted ;; Causation
rescheduled ;; Change_event_time || resumed ;; Recovering
caused ;; Causation || due ;; Causation || bleeding ;; Bodily_harm || cancelled ;; Change_event_time
forced ;; Motion || hemorrhaging ;; Bodily_harm || cancel ;; Removing || undergo ;; Action
transported ;; Sending || turns ;; Change || turns ;; Change || traveling ;; Traveling
attacked ;; Attack
takes place ;; Process_start || stock ;; Preserving
attack ;; Attack || said ;; Statement || backed ;; Motion
killed ;; Killing || injured ;; Bodily_harm || terrorists ;; Terrorism
injured ;; Bodily_harm || dead ;; Death
destroyed ;; Destroying
identified ;; Check
prevent ;; Preventing_or_letting || designed ;; Creating
faced ;; Confronting_problem
destroy ;; Destroying || burn ;; Bodily_harm
named ;; Name_conferral
mount ;; Process_start
recognised ;; Know || evacuate ;; Emptying
ordered ;; Request
achieve ;; Achieve || restricted ;; Limiting || release ;; Releasing
intervene ;; Hindering || moved ;; Self_motion
leave ;; Departing || organised ;; Arranging || evacuate ;; Emptying
intercepted ;; Preventing_or_letting || sunk ;; Self_motion
raided ;; Attack || intercepted ;; Preventing_or_letting
painted ;; Filling || threat ;; Warning || attacks ;; Attack
captured ;; Conquering || escaped ;; Escaping
surrendered ;; Surrendering || facing ;; Confronting_problem || supplies ;; Supply || squeezed ;; Motion_directional
readied ;; GetReady || cleared ;; Emptying || support ;; Supporting
cleared ;; Emptying || removing ;; Removing
signalled ;; Communication || started ;; Process_start
reopened ;; Openness
murder ;; Killing || murdered ;; Killing || murder ;; Killing || involving ;; Cause_to_be_included || returning ;; Arriving || shopping ;; Commerce_buy
searches ;; Scrutiny || search ;; Scrutiny || draining ;; Emptying || police ;; Patrolling || involving ;; Cause_to_be_included || buildings ;; Building || drainage ;; Emptying || inquiry ;; Criminal_investigation || halting ;; Hindering
discovered ;; Know
stored ;; Preserving || examination ;; Scrutiny || indicated ;; Statement
discovery ;; Know || known ;; Know || taken ;; Getting
led ;; Bringing
received ;; Receiving || kidnap ;; Kidnapping || spend ;; Expend_resource || rest ;; Placing || murder ;; Killing || telling ;; Telling || prison ;; Prison || sentences ;; Legal_rulings || pleaded ;; Request
end ;; Process_end || brought ;; Bringing || murders ;; Killing || murder ;; Killing || spoke ;; Statement
received ;; Receiving || committed ;; Commitment || attacks ;; Attack || murder ;; Killing || sentence ;; Legal_rulings || pleaded ;; Request
case ;; Protest || investigations ;; Scrutiny || murder ;; Killing || inquiry ;; Scrutiny || murder ;; Killing || re-opened ;; Process_start
death ;; Death || held ;; Defending
introduced ;; Statement
applied ;; Using || enacted ;; Action
brought ;; Bringing || gathering ;; Come_together || surpassed ;; Cause_change_of_position_on_a_scale
attended ;; Participation
gatherings ;; Come_together || gathering ;; Come_together
celebrated ;; Social_event || masses ;; Rite
prayers ;; Rite || offered ;; Giving
reserved ;; Preserving || attended ;; Participation
broke ;; Destroying
attended ;; Participation
attacks ;; Attack
attacks ;; Attack || fighting ;; Hostile_encounter
held ;; Defending || received ;; Receiving || attack ;; Attack
exploded ;; Destroying
given ;; Giving || deserted ;; Escaping || began ;; Process_start || alert ;; Warning || warned ;; Warning || attack ;; Attack || work ;; Being_in_operation || blow ;; Attack || suspended ;; Process_end
began ;; Process_start || drifted ;; Motion || attack ;; Attack
attack ;; Attack || used ;; Using || used ;; Using
given ;; Giving || beginning ;; Process_start || obtain ;; Getting || establish ;; Building || mass-produced ;; Manufacturing || replace ;; Change_of_leadership
neutralise ;; Cure || issued ;; Supply
equipped ;; Supply
caused ;; Causation || attack ;; Attack || destroy ;; Destroying || repulsed ;; Defending || entrances ;; Arriving || raiding ;; Attack || fatalities ;; Death || casualties ;; Bodily_harm
caused ;; Causation || repulsed ;; Defending || patrols ;; Patrolling || casualties ;; Bodily_harm
occurred ;; Presence
dealt ;; Resolve_problem
sitting ;; Placing || assembly ;; Building || assembly ;; Building || lodging ;; Placing || burned ;; Bodily_harm
rebelled ;; Hostile_encounter
given ;; Giving || evolved ;; Coming_to_be || lose ;; Earnings_and_losses
achieved ;; Achieve || play ;; Participation
organized ;; Arranging || extended ;; Expansion
rebellions ;; Change_of_leadership || offer ;; Giving || losses ;; Earnings_and_losses || permitting ;; Preventing_or_letting || suffered ;; Catastrophe
decided ;; Deciding || remained ;; Temporary_stay || assembly ;; Building || demonstrate ;; Protest || passed ;; Having_or_lacking_access
lost ;; Earnings_and_losses || destroyed ;; Destroying || kept ;; Preserving
shot ;; Use_firearm || resolve ;; Resolve_problem || committed ;; Committing_crime || proceeded ;; Self_motion || fought ;; Defending || maintained ;; Preserving
released ;; Releasing || established ;; Creating
crushing ;; Bodily_harm || reform ;; Reforming_a_system || developing ;; Coming_to_be
shifted ;; Change
operates ;; Being_in_operation || Located ;; Placing
retained ;; Preserving
includes ;; Cause_to_be_included
takes ;; Getting
joint ;; Collaboration || targeting ;; Aiming || assault ;; Attack || offensive ;; Hostile_encounter
invasion ;; Attack || assault ;; Attack || military ;; Military_operation
kidnapping ;; Kidnapping || killing ;; Killing
bombing ;; Attack || killing ;; Killing || set off ;; Damaging || claimed ;; Statement
captured ;; Conquering || caches ;; Preserving || military ;; Military_operation || forces ;; Military_operation || shells ;; Attack || explosives ;; Attack || said ;; Statement
military ;; Military_operation
stated ;; Statement || create ;; Creating
released ;; Releasing || netted ;; Earnings_and_losses
achieved ;; Achieve || Military ;; Military_operation || reports ;; Reporting
suggested ;; Convincing || due ;; Causation || targets ;; Aiming || reports ;; Reporting
reported ;; Reporting
use ;; Using || invasion ;; Attack || strikes ;; Attack || occurred ;; Coming_to_be || contested ;; Competition || indicating ;; Causation || reports ;; Reporting
moving ;; Motion || assault ;; Attack || military ;; Military_operation
involved ;; Cause_to_be_included || reports ;; Reporting
use ;; Using || assault ;; Attack || airstrikes ;; Attack
given ;; Giving || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Capture ;; Conquering || name ;; Name_conferral || operation ;; Military_operation
forms ;; Coming_to_be
avoid ;; Preventing_or_letting || called ;; Name_conferral || force ;; Military_operation
gave ;; Giving || damaged ;; Damaging || attacked ;; Attack || listening ;; Perception_active || bombardment ;; Attack || conversation ;; Communication
failed ;; Earnings_and_losses || capture ;; Conquering || destroyed ;; Destroying || lost ;; Earnings_and_losses || gaining ;; Earnings_and_losses || explosion ;; Catastrophe
attack ;; Attack || reached ;; Arriving || preceding ;; Process_start || fire ;; Use_firearm || advance ;; Cause_to_make_progress || advanced ;; Cause_to_make_progress || seen ;; Perception_active || engaged ;; Participation || inflicted ;; Bodily_harm
counter-attack ;; Attack || rushed ;; Self_motion || reserve ;; Preserving
captured ;; Conquering || attack ;; Attack || operation ;; Military_operation || completing ;; Process_end
held ;; Defending || event ;; Social_event
held ;; Defending || take ;; Creating || making it ;; Causation || attracts ;; Influence || takes place ;; Process_start
flying ;; Self_motion || includes ;; Cause_to_be_included
put on ;; Presence || start ;; Process_start
involving ;; Cause_to_be_included || includes ;; Cause_to_be_included
operational ;; Being_in_operation || due ;; Causation || attend ;; Participation
made ;; Creating
went ;; Self_motion
cancelled ;; Change_event_time || caused ;; Causation
included ;; Cause_to_be_included
included ;; Cause_to_be_included || escorted ;; Patrolling
began ;; Process_start || called ;; Name_conferral || change ;; Change
ushered in ;; Bringing || parallels ;; Forming_relationships
became ;; Becoming
come to ;; Motion
define ;; Statement
found ;; Know || began ;; Process_start || constructed ;; Building || occurred ;; Presence
spurring ;; Influence || strike ;; Attack || struck ;; Attack
strike ;; Attack || launched ;; Process_start
effect ;; Influence
broken ;; Destroying || industrialized ;; Creating
became ;; Becoming
grew ;; Expansion
developed ;; Coming_to_be || began ;; Process_start || changed ;; Change
influential ;; Influence
became ;; Becoming
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || blockade ;; Besieging || took place ;; Process_start || War ;; Military_operation
Battle ;; Hostile_encounter || established ;; Creating || defensive ;; Defending || victory ;; Getting
used ;; Using || close ;; Process_end || shipping ;; Sending || isolate ;; Hindering || positions ;; Placing || protecting ;; Defending
constructed ;; Building
position ;; Placing
shelled ;; Attack || commanded ;; Control || commanded ;; Control
shelled ;; Attack || damaged ;; Damaging || commanding ;; Control || commanding ;; Control || gaining ;; Getting
point ;; Placing || approached ;; Arriving
found ;; Know || party ;; Participation || abandoned ;; GiveUp || blown up ;; Destroying || destroyed ;; Destroying
abandoned ;; GiveUp || sealing ;; Preserving || retired ;; Escaping || withdrawal ;; Escaping
response ;; Response
control ;; Control || deployed ;; Arranging
control ;; Control || control ;; Control || performed ;; Hold || surveillance ;; Scrutiny || functions ;; Being_in_operation || included ;; Cause_to_be_included
control ;; Control || control ;; Control || deploying ;; Arranging || reporting ;; Reporting || support ;; Supporting || consisting ;; Containing
expansion ;; Expansion || deployed ;; Arranging
control ;; Control || provided ;; Supply || deployment ;; Arranging || command ;; Control
conducted ;; Action || involving ;; Cause_to_be_included || met ;; Come_together
deployment ;; Arranging || equipment ;; Supply || floating ;; Motion || arriving ;; Arriving || validated ;; Check
conduct ;; Action || deployment ;; Arranging || deployed ;; Arranging || arrival ;; Arriving || command ;; Control || combat ;; Hostile_encounter || validated ;; Control
responded ;; Response || combat ;; Hostile_encounter || reserve ;; Preserving
provide ;; Supply || response ;; Response || combat ;; Hostile_encounter || sortied ;; Departing || remained ;; Presence
response ;; Response || convinced ;; Convincing || withdraw ;; Escaping
killed ;; Killing || bombing ;; Attack || occurred ;; Presence || injured ;; Bodily_harm || explosion ;; Attack
explosion ;; Attack || happened ;; Presence
found ;; Know || explosion ;; Catastrophe || bomb ;; Use_firearm
asserted ;; Statement || explosion ;; Catastrophe || aimed ;; Aiming || ordered ;; Request
attack ;; Attack || classified ;; Giving
confessed ;; Reveal_secret || bombing ;; Attack || suspects ;; Committing_crime || arrested ;; Arrest
pleaded ;; Request || bombings ;; Attack
confirmed ;; Check || investigation ;; Criminal_investigation
confirmed ;; Check || committed ;; Commitment || bombings ;; Attack
questioned ;; Communication || attracted ;; Bringing
bombing ;; Attack || protests ;; Self_motion
tortured ;; Bodily_harm || planted ;; Placing || rigged ;; Hold || suspects ;; Committing_crime
bomb ;; Use_firearm || investigation ;; Criminal_investigation || expressed ;; Sending
shooting ;; Killing || executed ;; Action
occurred ;; Presence || attacked ;; Attack
bombarding ;; Attack || sank ;; Removing || entered ;; Arriving
resisted ;; Defending
destroyed ;; Destroying || stored ;; Preserving || start ;; Process_start || seize ;; Conquering
lost ;; Earnings_and_losses || undamaged ;; Damaging
destroyed ;; Destroying || disrupted ;; Damaging
disclosure ;; Reveal_secret || positions ;; Employment || Battle ;; Hostile_encounter || led to ;; Causation || defeated ;; Conquering
destruction ;; Destroying || depletion ;; Expend_resource || Battle ;; Hostile_encounter || contributed ;; Causation
codenamed ;; Military_operation
create ;; Creating || held ;; Hold || put ;; Placing || capturing ;; Arrest
forces ;; Hostile_encounter || command ;; Control
commenced ;; Process_start || targeting ;; Aiming || force ;; Hostile_encounter || defences ;; Defending || codenamed ;; Military_operation
attacked ;; Attack || left ;; Departing
achieved ;; Achieve || set ;; Placing || war ;; Hostile_encounter || contributing ;; Cause_to_make_progress
aided ;; Assistance || disagreement ;; Quarreling || War ;; Hostile_encounter || advances ;; Cause_to_make_progress || contributed ;; Cause_to_make_progress
deaths ;; Death || resulted in ;; Causation
convicted ;; Legal_rulings || crimes ;; Committing_crime
filed ;; Submitting_documents || Defence ;; Defending || crimes ;; Committing_crime || committed ;; Committing_crime || alleging ;; Judgment_communication
social ;; Social_event || demonstration ;; Social_event || taking place ;; Process_start
took ;; Conquering
requests ;; Request || withdrawal ;; Escaping || describes ;; Statement || end ;; Process_end || return ;; Arriving
raid ;; Attack || dismantled ;; Destroying
vowed ;; Commitment || fight ;; Hostile_encounter
found ;; Know || heard ;; Perception_active
written ;; Writing
arrested ;; Arrest || alleged ;; Statement
transported ;; Sending || fell ;; Body_movement || taken ;; Bringing
injuries ;; Bodily_harm || died ;; Death || death ;; Death
pay ;; Commerce_pay || investigation ;; Criminal_investigation || suspended ;; Process_end || incident ;; Killing
injuries ;; Bodily_harm || suggested ;; Convincing || involved ;; Cause_to_be_included || used ;; Using || involved ;; Cause_to_be_included || claim ;; Statement || denied ;; Agree_or_refuse_to_act
injuries ;; Bodily_harm || arrest ;; Arrest || secure ;; Preventing_or_letting || preceding ;; Process_start || custody ;; Prison || reported ;; Reporting || driving ;; Self_motion
found ;; Placing || injuries ;; Bodily_harm || investigation ;; Criminal_investigation || transport ;; Bringing
death ;; Death || ruled ;; Legal_rulings || homicide ;; Killing || concluded ;; Coming_to_believe
announced ;; Expressing_publicly || ruled ;; Legal_rulings || death ;; Death || homicide ;; Killing || charges ;; Judgment_communication || filed ;; Submitting_documents || report ;; Reporting
stated ;; Statement || cause ;; Causation || involved ;; Cause_to_be_included || death ;; Death || charges ;; Judgment_communication
illegal ;; Legality || killed ;; Killing || arrest ;; Arrest || murder ;; Killing || crimes ;; Committing_crime || manslaughter ;; Killing || driving ;; Self_motion || charged ;; Judgment_communication || charged ;; Judgment_communication
filed ;; Submitting_documents || illegal ;; Legality || added ;; Cause_change_of_position_on_a_scale || indicted ;; Criminal_investigation || arrest ;; Arrest || involved ;; Cause_to_be_included || charges ;; Judgment_communication || imprisonment ;; Prison || charges ;; Judgment_communication
death ;; Death || resulted in ;; Causation || hospitalization ;; Cure || protests ;; Protest
arrests ;; Arrest || injuries ;; Bodily_harm || protest ;; Protest || turned ;; Becoming || resulting in ;; Causation
deployment ;; Arranging || intensified ;; Cause_change_of_strength || looting ;; Robbery || culminating ;; Process_end || funeral ;; Rite || declaration ;; Expressing_publicly || establishment ;; Creating || burning ;; Destroying
started ;; Process_start || withdrawing ;; Escaping || lifted ;; Motion
decided ;; Deciding || accused ;; Judgment_communication || trials ;; Criminal_investigation
ended ;; Process_end || trial ;; Criminal_investigation
acquitted ;; Judgment_communication
remaining ;; Temporary_stay || charges ;; Judgment_communication || dropped ;; Motion_directional
bring ;; Bringing || involved ;; Cause_to_be_included || arrest ;; Arrest || death ;; Death || announced ;; Expressing_publicly || charges ;; Judgment_communication
announced ;; Expressing_publicly || serve ;; Assistance || trials ;; Criminal_investigation
destroyed ;; Destroying || killing ;; Killing || operating ;; Control
killing ;; Killing || crashed ;; Catastrophe
killed ;; Killing || attack ;; Attack
issued ;; Expressing_publicly
handed over ;; Giving
jailed ;; Prison || found ;; Know
diagnosed ;; Check
died ;; Death || convicted ;; Arrest
paid ;; Commerce_pay || accepted ;; Agree_or_refuse_to_act || given ;; Giving || maintained ;; Preserving
accept ;; Agree_or_refuse_to_act || said ;; Statement
claimed ;; Statement || denied ;; Agree_or_refuse_to_act || reported ;; Reporting || believed ;; Know || ordered ;; Arranging
planted ;; Placing || sent ;; Sending
operating ;; Being_in_operation
hosted ;; Social_event
hosted ;; Social_event
admitted ;; Reveal_secret
expansion ;; Expansion || founding ;; Creating || omitted ;; Removing || reflect ;; Statement || bear ;; Bringing || kept ;; Preserving || provide ;; Supply
replaced ;; Removing || used ;; Using
opened ;; Openness || closed ;; Openness
final ;; Process_end
fought ;; Hostile_encounter
reserved ;; Preserving || signed ;; Sign_agreement || settled on ;; Temporary_stay
began ;; Process_start
involved ;; Participation || conflict ;; Hostile_encounter
joined ;; Becoming_a_member
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || occurred ;; Presence || took place ;; Process_start
ordered ;; Arranging || cross ;; Motion_directional
retreat ;; Escaping || held ;; Hold || made ;; Causation || celebrate ;; Social_event
became ;; Becoming || demanded ;; Request
decided ;; Deciding || plan ;; Arranging || keep ;; Preserving
Seeing ;; Perception_active || attack ;; Attack || launch ;; Military_operation || retreated ;; Escaping || prepared ;; GetReady
crossed ;; Motion_directional || encamping ;; Placing || assault ;; Attack || led ;; Bringing
seize ;; Conquering || war ;; Hostile_encounter || cross ;; Motion_directional || direct ;; Action
destroying ;; Destroying || took off ;; Conquering
covered ;; Hiding_objects || guarded ;; Patrolling
entered ;; Arriving
agree ;; Agree_or_refuse_to_act || negotiations ;; Communication || reduce ;; Cause_change_of_position_on_a_scale || demands ;; Request || suspended ;; Process_end
decided ;; Deciding || moved ;; Motion || launch ;; Military_operation || offensive ;; Attack || beat ;; Competition || convinced ;; Coming_to_believe
gathered ;; Come_together || invade ;; Attack || connect ;; Connect || cross ;; Motion_directional
garrisoned ;; Besieging || facing ;; Confronting_problem
guarding ;; Patrolling || garrisoned ;; Besieging
captured ;; Conquering || crossed ;; Motion_directional || assaulted ;; Attack
casualties ;; Catastrophe || killed ;; Killing
attacked ;; Attack || encircled ;; Besieging || forces ;; Military_operation
received ;; Receiving || escape ;; Escaping
negotiations ;; Communication || escape ;; Escaping || encircled ;; Besieging || take part ;; Dispersal
contacted ;; Communication || negotiations ;; Communication || escape ;; Escaping || congratulate ;; Judgment_communication
reinforcements ;; Cause_change_of_strength
took ;; Conquering || response ;; Response || surrounding ;; Surrounding || supply ;; Supply || encircled ;; Besieging || cut ;; Removing
supply ;; Supply || encircled ;; Besieging || disease ;; Cure || cut off ;; Having_or_lacking_access || threatened ;; Influence
proposed ;; Statement || allow ;; Preventing_or_letting || supplying ;; Supply
agree ;; Agree_or_refuse_to_act || negotiate ;; Communication || keeping ;; Temporary_stay || explained ;; Statement || holding ;; Defending
plan ;; Arranging || signed ;; Sign_agreement
assassinated ;; Killing || attending ;; Participation || play ;; Participation
died ;; Death || watched ;; Perception_active || shot ;; Use_firearm
assassinated ;; Killing || marked ;; Recording || extended ;; Expansion || funeral ;; Rite || burial ;; Rite
War ;; Military_operation || assassination ;; Killing || revive ;; Recovering
killing ;; Killing || kill ;; Killing || assigned ;; Request || tasked ;; Request
lost ;; Earnings_and_losses || death ;; Death || wounded ;; Bodily_harm
killed ;; Killing || escape ;; Escaping
took place ;; Process_start
ended ;; Process_end
struggle ;; Hostile_encounter || advised ;; Convincing
named ;; Name_conferral
created ;; Creating || seen ;; Perception_active
led ;; Causation || chronicle ;; Recording || forces ;; Causation
suggests ;; Convincing
indicate ;; Statement
given ;; Giving
led to ;; Causation || transmitted ;; Dispersal || survives ;; Coming_to_be || complete ;; Process_end
attached ;; Connect || assisted ;; Assistance
burned ;; Damaging
transcribed ;; Writing || described ;; Writing
lost ;; Earnings_and_losses
split ;; Destroying || responding ;; Response
paying ;; Commerce_pay || fighting ;; Hostile_encounter || favoured ;; Choosing || favoured ;; Choosing
held ;; Defending || suggests ;; Convincing
sailed ;; Motion || called ;; Name_conferral
begins ;; Process_start || ordering ;; Request || stand ;; Placing || hold ;; Defending
send ;; Sending || ordered ;; Request || fought ;; Hostile_encounter
sailed ;; Sending
leaves ;; Departing
site ;; Placing
promising ;; Commitment || paid ;; Commerce_pay || addressed ;; Expressing_publicly
replied ;; Response || pay ;; Commerce_pay
assault ;; Attack || forces ;; Causation || began ;; Process_start
blocked ;; Hindering || pressed ;; Motion
requested ;; Request || allow ;; Preventing_or_letting
let ;; Preventing_or_letting
fled ;; Escaping || joined ;; Cause_to_amalgamate
fled ;; Escaping
add ;; Cause_to_be_included || given ;; Giving || stated ;; Statement || marked ;; Recording
killing ;; Killing || losing ;; Death
found ;; Know
celebrate ;; Social_event
describe ;; Statement || sin ;; Committing_crime || held ;; Defending || bolstered ;; Supporting
dying ;; Killing || representing ;; Choosing || prayer ;; Rite
lost ;; Death || protecting ;; Defending || created ;; Causation || lifting ;; Process_end
raiding ;; Attack || gather ;; Come_together || forces ;; Causation
paying ;; Commerce_pay || raided ;; Attack || forces ;; Causation
allow ;; Preventing_or_letting || protect ;; Preventing_or_letting
party ;; Participation || advocated ;; Convincing || response ;; Response || attacks ;; Attack
festival ;; Social_event || gathering ;; Come_together
associated ;; Collaboration || stretched ;; Body_movement
called ;; Request
marched ;; Self_motion || highlight ;; Statement || delivered ;; Sending
include ;; Cause_to_be_included || service ;; Rite
named ;; Name_conferral || hijacked ;; Terrorism || flying ;; Motion
burnt ;; Destroying || flown ;; Self_motion
hijacking ;; Terrorism || service ;; Rite
hijacking ;; Terrorism || burning ;; Destroying || retaliated ;; Revenge || banning ;; Preventing_or_letting
impact ;; Influence || occurring ;; Presence || ban ;; Preventing_or_letting
reacted ;; Response || charging ;; Judgment_communication
weakened ;; Cause_change_of_strength || crackdown ;; Action
moved ;; Motion || established ;; Creating
wounded ;; Bodily_harm
declared ;; Expressing_publicly || election ;; Change_of_leadership
pronounced ;; Statement || shot ;; Use_firearm || dead ;; Death
televised ;; Communication || spoke ;; Expressing_publicly || elections ;; Choosing
fired ;; Use_firearm || exiting ;; Departing || wounded ;; Bodily_harm || leaving ;; Departing
died ;; Death
murdering ;; Killing || convicted ;; Legal_rulings || death ;; Death || sentenced ;; Legal_rulings
sentence ;; Legal_rulings || commuted ;; Rewards_and_punishments
recorded ;; Recording || shooting ;; Use_firearm || captured ;; Conquering
viewing ;; Perception_active || mass ;; Rite || taken ;; Bringing
view ;; Perception_active || traveled ;; Traveling
interred ;; Placing
death ;; Death || prompted ;; Causation
election ;; Change_of_leadership || lost ;; Earnings_and_losses || win ;; Getting
emerged ;; Coming_to_be || led to ;; Causation || assassination ;; Killing || assassination ;; Killing
assassinated ;; Killing || sitting ;; Placing
battle ;; Hostile_encounter || culmination ;; Confronting_problem
opposed ;; Agree_or_refuse_to_act
impose ;; Action || campaign ;; Military_operation || Rising ;; Change_of_leadership
beating ;; Bodily_harm || attending ;; Participation || uprising ;; Change_of_leadership || fine ;; Rewards_and_punishments || began ;; Process_start || defaulted ;; Commerce_pay || services ;; Rite
interrupted ;; Hindering || supported ;; Supporting || disarmed ;; Removing
skirmish ;; Hostile_encounter || rising ;; Change_of_leadership || gathered ;; Come_together || raised ;; Bringing
captured ;; Conquering
aided ;; Assistance || present ;; Presence || took ;; Bringing
deserted ;; Escaping || choice ;; Choosing || received ;; Receiving || reception ;; Social_event
diminished ;; Cause_change_of_position_on_a_scale || dispersed ;; Dispersal || headed ;; Self_motion
commanded ;; Control || rebels ;; Hostile_encounter || included ;; Cause_to_be_included
decided ;; Deciding || parade ;; Social_event || review ;; Testing || hold ;; Hold || forces ;; Military_operation
cut ;; Bodily_harm || confront ;; Hostile_encounter
tortured ;; Bodily_harm || treated ;; Giving || hanged ;; Rewards_and_punishments || quartered ;; Attack
added ;; Cause_to_be_included
campaigns ;; Military_operation || War ;; Hostile_encounter
ended ;; Process_end || defeat ;; Conquering || ending ;; Process_end
campaigns ;; Military_operation || launched ;; Motion || broken ;; Destroying
war ;; Hostile_encounter || conflict ;; Hostile_encounter || unified ;; Cause_to_amalgamate
pay ;; Commerce_pay || conquered ;; Conquering || avoid ;; Preventing_or_letting || attacked ;; Attack || tribute ;; Rite || bled ;; Motion
conquer ;; Conquering || war ;; Hostile_encounter || use ;; Using || sieges ;; Besieging
surrendered ;; Surrendering || forces ;; Hostile_encounter
war ;; Hostile_encounter || joint ;; Collaboration
war ;; Hostile_encounter
provided ;; Supply || presence ;; Presence || collaboration ;; Collaboration
consolidated ;; Cause_change_of_strength || offered ;; Giving
ended ;; Process_end || rest ;; Placing || convert ;; Change || become ;; Becoming || war ;; Hostile_encounter || spread ;; Dispersal || convert ;; Change
accused ;; Judgment_communication
final ;; Process_end || fall ;; Motion_directional || war ;; Hostile_encounter || celebrated ;; Social_event
War ;; Hostile_encounter
conquered ;; Conquering
enclosed ;; Placing || existed ;; Presence || fall ;; Motion_directional || wrote ;; Writing
fought ;; Quarreling
began ;; Process_start || death ;; Death
struggles ;; Hostile_encounter || war ;; Hostile_encounter
controlled ;; Control
control ;; Control || control ;; Control
fighting ;; Hostile_encounter || weakened ;; Cause_change_of_strength
declined ;; Agree_or_refuse_to_act || disrupted ;; Hindering || challenged ;; Confronting_problem
defenses ;; Defending || support ;; Supporting || imposed ;; Action
paid ;; Commerce_pay
imposed ;; Action
support ;; Supporting
state ;; Statement
intermixing ;; Cause_to_amalgamate
governed ;; Control || demands ;; Request || seen ;; Perception_active
intervened ;; Hindering || controlled ;; Control || warfare ;; Hostile_encounter
War ;; Hostile_encounter || died ;; Death
setting ;; Placing || war ;; Hostile_encounter || supporters ;; Supporting || supporters ;; Supporting
ask ;; Request || obtain ;; Getting || raid ;; Attack
agreed ;; Agree_or_refuse_to_act
concluded ;; Process_end
helped ;; Assistance || married ;; Forming_relationships || war ;; Hostile_encounter || stand ;; Placing
occurred ;; Coming_to_be
passes ;; Motion_directional || bury ;; Rite || called ;; Name_conferral || line ;; Connect || lies ;; Placing
hauled ;; Self_motion
struck ;; Attack || overturned ;; Motion || smashed ;; Destroying
fell ;; Motion || fell ;; Motion
killed ;; Killing
occurred ;; Coming_to_be || crash ;; Catastrophe || casualties ;; Death
revealed ;; Reveal_secret || causes ;; Causation || investigation ;; Criminal_investigation || twofold ;; Judgment_communication
passed ;; Motion_directional
runnings ;; Self_motion || signal ;; Warning || showed ;; Presence
reached ;; Arriving || appeared ;; Presence || cleared ;; Removing
checking ;; Scrutiny || stopped ;; Process_end || allowing ;; Preventing_or_letting
operated ;; Being_in_operation || block ;; Hiding_objects || system ;; Control
operated ;; Being_in_operation || revealed ;; Reveal_secret || records ;; Recording
bombing ;; Terrorism || suicide bombing ;; Terrorism || occurred ;; Coming_to_be
died ;; Death || injured ;; Bodily_harm
explosion ;; Attack || killed ;; Killing || occurred ;; Coming_to_be
killed ;; Killing || blast ;; Attack || happened ;; Presence
happened ;; Presence || attack ;; Terrorism
suicide bomber ;; Terrorism || arrive ;; Arriving || proceed ;; Self_motion || detonate ;; Terrorism || demonstration ;; Protest
attack ;; Attack || target ;; Aiming
released ;; Statement || statement ;; Statement || suicide bomber ;; Terrorism || blew ;; Bodily_harm
wounded ;; Bodily_harm
declared ;; Expressing_publicly || mourning ;; Rite || bombing ;; Terrorism
fought ;; Hostile_encounter
resulted in ;; Causation
resisted ;; Defending
accepted ;; Receiving
saved ;; Rescuing
baptized ;; Rite || buried ;; Placing
occurred ;; Coming_to_be || revolution ;; Change_of_leadership
modify ;; Change || proposals ;; Convincing
demands ;; Request || escalated ;; Cause_change_of_strength || resignation ;; Change_of_leadership
vowed ;; Commitment || control ;; Control || rendered ;; Causation || defections ;; Escaping
took place ;; Process_start || demonstration ;; Social_event
announced ;; Expressing_publicly || pass ;; Motion_directional
held ;; Defending
killed ;; Killing || called ;; Request
killed ;; Killing || held ;; Defending
deaths ;; Death || culminating ;; Self_motion || fired ;; Use_firearm
agreed ;; Agree_or_refuse_to_act || Starting ;; Process_start || deal ;; Resolve_problem
declared ;; Expressing_publicly
declared ;; Expressing_publicly || refused ;; Agree_or_refuse_to_act || armed ;; Bearing_arms || sign ;; Sign_agreement || conflict ;; Hostile_encounter || support ;; Supporting
shelling ;; Attack || ensued ;; Coming_to_be || fighting ;; Hostile_encounter
killed ;; Killing || bombing ;; Attack || prayer ;; Rite || compound ;; Cause_to_amalgamate || used ;; Using || injured ;; Bodily_harm || services ;; Rite || ripped ;; Destroying
caused ;; Causation || conflicted ;; Hostile_encounter
flew ;; Motion || treated ;; Cure || took over ;; Change_of_leadership
celebrated ;; Social_event || insisted ;; Statement || absence ;; Presence
transferring ;; Giving || elections ;; Choosing || demands ;; Request || rejected ;; Agree_or_refuse_to_act
announced ;; Expressing_publicly || uprising ;; Change_of_leadership || said ;; Communication || response ;; Response || joint ;; Collaboration || match ;; Competition
signed ;; Sign_agreement || transfer ;; Giving || leave ;; Departing || exchange ;; Exchange || prosecution ;; Legality
accepted ;; Agree_or_refuse_to_act || rejected ;; Agree_or_refuse_to_act
election ;; Choosing || held ;; Hold
election ;; Choosing || claims ;; Statement || receiving ;; Receiving
attend ;; Participation || returned ;; Arriving
transferred ;; Giving || marking ;; Recording || resigned ;; Change_of_leadership
defensive ;; Defending
ran ;; Self_motion
defensive ;; Defending || exhausted ;; Expend_resource || Battle ;; Hostile_encounter || battle ;; Hostile_encounter || leaving ;; Departing || failure ;; Earnings_and_losses
inflicted ;; Causation || losses ;; Earnings_and_losses
placed ;; Placing || war ;; Hostile_encounter || war ;; Hostile_encounter
begun ;; Process_start || increase ;; Cause_change_of_position_on_a_scale || attacks ;; Attack
built ;; Building || positions ;; Employment || defensive ;; Defending || withdrawal ;; Escaping
withdrawal ;; Escaping || demolishing ;; Destroying || buildings ;; Building || forcing ;; Hostile_encounter || dislocate ;; Placing || advance ;; Self_motion || destroying ;; Destroying
rebuild ;; Building || abandoned ;; GiveUp || attack ;; Attack
defensive ;; Defending || casualties ;; Catastrophe || incorporating ;; Cause_to_amalgamate || dispersal ;; Dispersal || defence ;; Defending || reduced ;; Cause_change_of_position_on_a_scale || held ;; Defending
recuperated ;; Cure || protected ;; Defending || planned ;; Arranging || warfare ;; Hostile_encounter || losses ;; Earnings_and_losses || bombing ;; Attack || positions ;; Placing
made ;; Manufacturing || beginning ;; Process_start
increased ;; Cause_change_of_position_on_a_scale || reducing ;; Cause_change_of_position_on_a_scale
provide ;; Supply
production ;; Manufacturing || increase ;; Cause_change_of_position_on_a_scale
production ;; Creating || supply ;; Supply || failed ;; Agree_or_refuse_to_act || service ;; Rite || rejected ;; Agree_or_refuse_to_act
took place ;; Process_start || attacks ;; Attack
demolitions ;; Destroying || left ;; GiveUp || blows ;; Confronting_problem
transferred ;; Military_operation || attacked ;; Attack || knew ;; Know || positions ;; Placing || work ;; Being_in_operation
released ;; Military_operation || increased ;; Cause_change_of_position_on_a_scale
broken ;; Bodily_harm || attacked ;; Attack
service ;; Rite
took off ;; Departing || arrived at ;; Arriving || fell ;; Motion_directional || crashed ;; Catastrophe || located ;; Know
caused ;; Causation || deaths ;; Death || flew ;; Motion || war ;; Hostile_encounter || disaster ;; Catastrophe
named ;; Name_conferral
conducted ;; Action || service ;; Rite
attributed ;; Causation || service ;; Rite || disaster ;; Catastrophe || carried out ;; Military_operation
concluded ;; Coming_to_believe || crash ;; Catastrophe
wars ;; Hostile_encounter
wars ;; Hostile_encounter || fighting ;; Hostile_encounter || supporters ;; Supporting || supporters ;; Supporting || supporters ;; Supporting || pitted ;; Hostile_encounter || saw ;; Self_motion
ended ;; Process_end || Battle ;; Hostile_encounter
rule ;; Legal_rulings || trial ;; Legal_rulings || execution ;; Killing || exile ;; Departing || replacement ;; Change || outcome ;; Change
ended ;; Process_end || consolidated ;; Cause_to_amalgamate || established ;; Creating || Protestant ;; Rite
wars ;; Hostile_encounter || revolution ;; Change_of_leadership || established ;; Creating || established ;; Creating || govern ;; Action
died ;; Death || suffering ;; Bodily_harm
death ;; Death || acknowledged ;; Statement
given ;; Giving || mourning ;; Rite || buried ;; Placing
death ;; Death
sent ;; Sending || attended ;; Participation
delivered ;; Sending
bombing ;; Attack || suicide attack ;; Terrorism
attacker ;; Attack || strapped ;; Connect || claimed ;; Statement || detonated ;; Attack
terrorism ;; Terrorism || terrorist ;; Terrorism || acts ;; Action || commit ;; Committing_crime || violence ;; Violence || attack ;; Attack
terrorism ;; Terrorism || attack ;; Attack
terrorism ;; Terrorism || terrorist ;; Terrorism || plans ;; Arranging || inflict ;; Damaging || casualties ;; Death
terrorism ;; Terrorism
transform ;; Change || regional ;; Rite
Battle ;; Hostile_encounter || advance ;; Arriving || War ;; Hostile_encounter || occurred ;; Process_start || defeat ;; Hostile_encounter
arrive in ;; Arriving || secured ;; Getting || command ;; Control
efforts ;; Arranging || dislodge ;; Conquering || prepare ;; GetReady || ordered ;; Request || advance ;; Arriving
service ;; Rite || reinforced ;; Cause_change_of_strength || carrying out ;; Action || assist ;; Assistance
reach ;; Arriving || cross ;; Motion_directional
began ;; Process_start || started ;; Process_start || withdraw ;; Escaping
Fighting ;; Hostile_encounter || reached ;; Arriving
halted ;; Process_end || returned ;; Arriving || operations ;; Military_operation || war ;; Hostile_encounter || prepare ;; GetReady
occurred ;; Coming_to_be
crash ;; Catastrophe || suffered ;; Bodily_harm
killing ;; Killing || injuring ;; Bodily_harm || crashed ;; Catastrophe
ejected ;; Removing
contributed ;; Causation
erected ;; Building
service ;; Rite || attended ;; Participation
spoke ;; Expressing_publicly || saying ;; Expressing_publicly || saddened ;; Bodily_harm || lost ;; Death
dramatized ;; Cause_change_of_position_on_a_scale
completed ;; Process_end
revealed ;; Reveal_secret || emerged ;; Coming_to_be
played ;; Participation || service ;; Rite || operated ;; Being_in_operation
control ;; Control || exploiting ;; Control || fraud ;; Committing_crime
champions ;; Competition
mass ;; Rite || began ;; Process_start || attacked ;; Attack || massacre ;; Killing || killing ;; Killing
stormed ;; Attack || detonate ;; Attack
wounded ;; Bodily_harm || maimed ;; Bodily_harm || killed ;; Killing
condemned ;; Judgment_communication
accused ;; Judgment_communication || arrested ;; Arrest
escape ;; Escaping || killed ;; Killing || attempt ;; Self_motion
sentenced ;; Rewards_and_punishments
confirmed ;; Check
murdered ;; Killing || perform ;; Hold
killed ;; Killing || service ;; Rite
assassination ;; Killing || shot ;; Use_firearm
shooting ;; Use_firearm || arrested ;; Arrest || murder ;; Killing || crimes ;; Committing_crime || charged ;; Legality
confessed ;; Reveal_secret || telling ;; Telling || killing ;; Killing || shot ;; Use_firearm
found ;; Know || sentenced ;; Legal_rulings || murder ;; Killing || assault ;; Attack || imprisonment ;; Prison || parole ;; Releasing || guilty ;; Committing_crime
assassinated ;; Killing
accommodating ;; Assistance
reached ;; Arriving || meetings ;; Social_event || prayer ;; Rite || conducting ;; Action
began ;; Process_start || walk ;; Self_motion || stepped ;; Self_motion || fired ;; Use_firearm
fell ;; Motion_directional
announce ;; Expressing_publicly || death ;; Death || emerged ;; Presence || carried ;; Bringing
handed over ;; Giving || captured ;; Arrest
murder ;; Killing || opened ;; Process_start || trial ;; Criminal_investigation
assassination ;; Killing || trial ;; Criminal_investigation || avoid ;; Preventing_or_letting || scrutiny ;; Scrutiny || prevent ;; Preventing_or_letting || attributed ;; Causation
sentenced ;; Legal_rulings || death ;; Death
hanged ;; Rewards_and_punishments
named ;; Name_conferral || entered ;; Arriving || service ;; Rite
informed ;; Telling || started ;; Process_start || approach ;; Arriving || approach ;; Arriving || view ;; Perception_active || came ;; Motion
approach ;; Arriving || run ;; Self_motion || initiated ;; Process_start
decided ;; Deciding || got ;; Getting || view ;; Perception_active || beginning ;; Process_start || go ;; Motion
struck ;; Attack || applied ;; Using
damaged ;; Damaging || gain ;; Getting || causing ;; Causation || crash ;; Catastrophe
investigation ;; Criminal_investigation || faulted ;; Judgment_communication || diverting ;; Removing
conflict ;; Hostile_encounter || represented ;; Presence || War ;; Military_operation || War ;; Military_operation
planned ;; Arranging || assassinated ;; Killing || invasion ;; Military_operation || implementing ;; Action
joined ;; Becoming_a_member || captured ;; Conquering || defeated ;; Preventing_or_letting
began ;; Process_start
Joining ;; Becoming_a_member || invasion ;; Military_operation || reached ;; Achieve
ended ;; Process_end || campaign ;; Military_operation || defeat ;; Earnings_and_losses
retreat ;; Escaping || states ;; Statement || caught ;; Body_movement || saluted ;; Rite || set ;; Placing || weeping ;; Body_movement || embracing ;; Hold
became ;; Becoming || negotiated ;; Communication || war ;; Military_operation || draw ;; Process_end
engagement ;; Hostile_encounter
fought ;; Hostile_encounter || led ;; Bringing || commanded ;; Control
escaped ;; Escaping || destroyed ;; Destroying || prisoner ;; Prison || became ;; Becoming || putting up ;; Placing
defeating ;; Conquering || separated ;; Forming_relationships
threat ;; Preventing_or_letting
spread ;; Dispersal || allowed ;; Giving || hold off ;; Defending || massed ;; Motion
blow ;; Attack || lunged ;; Self_motion
siege ;; Besieging
besieged ;; Besieging || besieged ;; Besieging
declared ;; Expressing_publicly || create ;; Creating || include ;; Cause_to_be_included
assaulted ;; Attack
blockaded ;; Besieging
break ;; Destroying || start ;; Process_start || conflict ;; Hostile_encounter || equipped ;; Supply
killed ;; Killing
suffered ;; Bodily_harm || killed ;; Killing
siege ;; Besieging || indicates ;; Communication
siege ;; Besieging
siege ;; Besieging
committed ;; Commitment || terrorism ;; Terrorism
imprisonment ;; Prison
mutiny ;; Change_of_leadership || revolt ;; Change_of_leadership || guarding ;; Patrolling
killing ;; Killing || rebelling ;; Change_of_leadership || took over ;; Military_operation
fired ;; Use_firearm || looted ;; Robbery || held ;; Arrest || vandalised ;; Destroying
spread ;; Dispersal
ended ;; Process_end || surrendered ;; Surrendering || released ;; Releasing || negotiations ;; Communication || mutiny ;; Change_of_leadership || discussions ;; Communication
received ;; Receiving || mutiny ;; Change_of_leadership || death ;; Death || imprisonment ;; Prison || sentences ;; Legal_rulings || sentenced ;; Legal_rulings
charged ;; Imposing_obligation || acquitted ;; Legal_rulings
condemned ;; Judgment_communication || designed ;; Creating || revenge ;; Revenge || watch ;; Perception_active || charged ;; Legality
bludgeoning ;; Bodily_harm || homicide ;; Killing || murders ;; Killing || occurred ;; Presence
troubled ;; Resolve_problem || relationship ;; Forming_relationships || shared ;; Communication
concert ;; Social_event || attended ;; Participation
discovered ;; Know || locate ;; Placing || alerted ;; Warning || murders ;; Killing
arrested ;; Arrest || murders ;; Killing || prison ;; Prison || convicted ;; Legal_rulings || sentenced ;; Legal_rulings
sentence ;; Legal_rulings
bombs ;; Use_firearm || exploded ;; Attack
injured ;; Bodily_harm || blasts ;; Attack
defused ;; Resolve_problem
confirmed ;; Check || damage ;; Damaging
condemned ;; Judgment_communication || attacks ;; Attack
announced ;; Expressing_publicly || bombings ;; Attack || terrorist ;; Terrorism || Investigation ;; Criminal_investigation
imprisonment ;; Prison || investigation ;; Criminal_investigation || sentenced ;; Legal_rulings || accused ;; Rewards_and_punishments
bombings ;; Attack || killing ;; Killing || carried out ;; Action || exploded ;; Attack || injuring ;; Bodily_harm
confessed ;; Reveal_secret || admitted ;; Reveal_secret || involvement ;; Participation || bombings ;; Attack
delivering ;; Sending || warning ;; Warning || bombings ;; Attack || claimed ;; Statement || deaths ;; Death || caused ;; Causation || services ;; Supply
blasts ;; Attack || bombings ;; Attack || arrested ;; Arrest || imprisonment ;; Prison || sentenced ;; Legal_rulings
became ;; Becoming || signing ;; Sign_agreement || abuse ;; Bodily_harm || coerced ;; Convincing
declared ;; Expressing_publicly || convictions ;; Legal_rulings || quashed ;; Removing
miscarriages ;; Legal_rulings || legal ;; Legality || seen ;; Know
bombings ;; Attack || occur ;; Coming_to_be || bombings ;; Attack || terrorism ;; Terrorism || acts ;; Action || War ;; Military_operation
occurred ;; Presence || jumped ;; Motion || entered ;; Arriving || stopped ;; Preventing_or_letting || arrested ;; Arrest
stated ;; Statement || found ;; Know || collapsing ;; Catastrophe
service ;; Supply || resigned ;; Change_of_leadership
building ;; Building || restricted ;; Hindering || indicted ;; Criminal_investigation
carrying ;; Carry_goods || charged ;; Judgment_communication
carrying ;; Carry_goods || assaulting ;; Attack
probation ;; Prison
destroyed ;; Destroying
happened ;; Presence
occurred ;; Coming_to_be
death ;; Death || explosion ;; Catastrophe
rescued ;; Rescuing || died ;; Death
killed ;; Killing || reelection ;; Change_of_leadership
charged ;; Judgment_communication || arrested ;; Arrest
sentenced ;; Prison
causing ;; Causation || explosion ;; Catastrophe || fired ;; Process_end || sabotaged ;; Committing_crime
involved ;; Participation
stopped ;; Process_end || waiting ;; Temporary_stay
shot ;; Use_firearm || shot ;; Use_firearm
initiating ;; Process_start
arrested ;; Arrest || sentenced ;; Rewards_and_punishments
died ;; Death
released ;; Releasing || convicted ;; Legal_rulings || killed ;; Killing
became ;; Becoming || serving ;; Assistance || prisoners ;; Prison
resulted in ;; Causation || assist ;; Assistance || died ;; Death
prevented ;; Preventing_or_letting || took place ;; Process_start || festival ;; Social_event || battle ;; Hostile_encounter
enforcing ;; Action || obtained ;; Getting || taking place ;; Process_start || festival ;; Social_event || injunction ;; Action || prohibiting ;; Preventing_or_letting
operation ;; Military_operation
encountered ;; Hostile_encounter || block ;; Hindering || resistance ;; Defending || heading ;; Self_motion
push ;; Motion || claim ;; Statement || rammed ;; Damaging
arrested ;; Arrest || smashed ;; Bodily_harm
broke ;; Bodily_harm || developed ;; Coming_to_be
attack ;; Attack || said ;; Statement || pelted ;; Bodily_harm
states ;; Statement || suggesting ;; Statement
violence ;; Violence || attack ;; Attack || taken place ;; Process_start || launched ;; Process_start || purported ;; Statement
clubbed ;; Bodily_harm || hitting ;; Attack
states ;; Statement || escape ;; Escaping || stop ;; Preventing_or_letting || threw ;; Body_movement || driving ;; Motion
injured ;; Bodily_harm
arrested ;; Arrest
arrest ;; Arrest || War ;; Hostile_encounter
injuries ;; Bodily_harm || event ;; Social_event || guilty ;; Legal_rulings || Harm ;; Bodily_harm || Battle ;; Hostile_encounter || found ;; Know
damages ;; Damaging || damage ;; Damaging || arrest ;; Arrest || imprisonment ;; Prison || awarded ;; Giving
legal ;; Legality || legal ;; Legality || costs ;; Cost || award ;; Judgment_communication || award ;; Giving
War ;; Military_operation || Envelopment ;; Besieging || Battle ;; Hostile_encounter || Raid ;; Attack || raids ;; Attack
seize ;; Conquering || maneuver ;; Military_operation
captured ;; Conquering || losses ;; Earnings_and_losses
resulted in ;; Causation || Raid ;; Attack || battle ;; Hostile_encounter || disruption ;; Damaging
became ;; Becoming || lost ;; Earnings_and_losses || prisoners ;; Prison
allowed ;; Preventing_or_letting || capture ;; Conquering
taught ;; Education_teaching || offensive ;; Attack || maneuver ;; Military_operation || advent ;; Coming_to_be || executed ;; Action
began ;; Process_start || protests ;; Judgment_communication || convicted ;; Legality || punishment ;; Rewards_and_punishments || sentenced ;; Legality
sentenced ;; Legality || convicted ;; Legality
banning ;; Preventing_or_letting || election ;; Change_of_leadership || politics ;; Participation || boycott ;; Agree_or_refuse_to_act
sentence ;; Prison || crimes ;; Committing_crime
called ;; Request || protests ;; Judgment_communication
gave rise to ;; Cause_to_make_progress || protests ;; Judgment_communication || demonstration ;; Social_event || joined ;; Participation
launched ;; Process_start || demanding ;; Request || counter-protest ;; Judgment_communication || movement ;; Social_event || release ;; Releasing || trial ;; Legal_rulings
support ;; Supporting || expressed ;; Expressing_publicly
warning ;; Warning || demanding ;; Request || protest ;; Judgment_communication || punishment ;; Rewards_and_punishments || movement ;; Social_event || mileage ;; Earnings_and_losses
killed ;; Killing || protests ;; Judgment_communication
arrested ;; Arrest || killing ;; Killing || involvement ;; Participation
convicted ;; Legality || sentenced ;; Legality
protested ;; Judgment_communication || clashes ;; Violence
killed ;; Killing || confrontations ;; Violence
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || took place ;; Process_start || War ;; Military_operation
coming ;; Motion || provide ;; Supply || besieged ;; Besieging || invested ;; Expend_resource || relief ;; Assistance
guarded ;; Bearing_arms || surprised ;; Change_sentiment
told ;; Telling || commanding ;; Control
decided ;; Deciding || attack ;; Attack
attacked ;; Attack || attack ;; Attack || disallowed ;; Preventing_or_letting || mount ;; Connect || annihilated ;; Killing
killing ;; Killing || fighting ;; Hostile_encounter || killed ;; Killing || took ;; Conquering || taking ;; Conquering || flight ;; Self_motion || prisoners ;; Prison || decimated ;; Destroying
hated ;; Judgment_communication || left ;; Placing
battle ;; Hostile_encounter || took place ;; Process_start
states ;; Statement || losses ;; Earnings_and_losses || suffered ;; Earnings_and_losses
losses ;; Earnings_and_losses || gives ;; Giving || suffered ;; Earnings_and_losses
caused ;; Causation || disruption ;; Damaging || hold ;; Hold || victory ;; Earnings_and_losses
withdraw ;; Escaping || forced ;; Causation || marching ;; Self_motion || reroute ;; Motion
surrendered ;; Surrendering || forces ;; Causation
killed ;; Killing || killings ;; Killing || shot ;; Use_firearm
refused ;; Agree_or_refuse_to_act || lobbied ;; Convincing || resigned ;; Departing
helped ;; Assistance || became ;; Becoming || bring ;; Bringing || notice ;; Know
murder ;; Committing_crime || convicted ;; Legal_rulings
sparked ;; Causation || led to ;; Causation || state ;; Statement
described ;; Statement || legend ;; Adducing || reports ;; Reporting || defense ;; Defending || attributed ;; Causation || diminished ;; Cause_change_of_position_on_a_scale || led to ;; Causation || effects ;; Influence
committed ;; Killing || release ;; Releasing || suicide ;; Committing_crime || prison ;; Prison
uprising ;; Hostile_encounter || conflict ;; Hostile_encounter
resulted in ;; Causation
found ;; Know || confrontation ;; Hostile_encounter || inquiry ;; Criminal_investigation || existed ;; Presence || cause ;; Causation || stemmed ;; Causation
bury ;; Placing
demanded ;; Request
wounded ;; Bodily_harm || shot ;; Use_firearm
began ;; Process_start || attack ;; Attack || violence ;; Violence || launched ;; Process_start
erupted ;; Process_start
disputed ;; Quarreling || claiming ;; Statement
attacked ;; Judgment_communication
attack ;; Attack || launch ;; Process_start || said ;; Statement
surrounded ;; Surrounding || barricaded ;; Having_or_lacking_access
captured ;; Conquering || custody ;; Prison || transferred ;; Giving
executed ;; Killing
cause ;; Causation || suggests ;; Convincing
refused ;; Agree_or_refuse_to_act || murdered ;; Killing
warned ;; Warning
warnings ;; Warning
strengthening ;; Cause_change_of_strength || overthrow ;; Change_of_leadership || elected ;; Change_of_leadership || orchestrated ;; Arranging
overthrow ;; Change_of_leadership
audit ;; Scrutiny || limit ;; Limiting
expel ;; Removing || refusal ;; Agree_or_refuse_to_act || voted ;; Choosing
vote ;; Choosing || instigated ;; Influence || boycott ;; Response || pressure ;; Hold
tighten ;; Cause_change_of_strength || control ;; Control || opted ;; Choosing || seize ;; Hold || undermine ;; Destroying
opposed ;; Agree_or_refuse_to_act || overthrow ;; Change_of_leadership || coup ;; Change_of_leadership || decided ;; Deciding
initiating ;; Process_start || assist ;; Assistance || coup ;; Change_of_leadership || planning ;; Arranging
formed ;; Coming_to_be || coup ;; Change_of_leadership || rule ;; Control
hold on to ;; Defending
hired ;; Employment || riots ;; Violence
took over ;; Military_operation || brought ;; Bringing
killed ;; Killing || conflict ;; Hostile_encounter
arrested ;; Arrest || convicted ;; Legality
sentenced ;; Prison
received ;; Receiving || imprisoned ;; Arrest || death ;; Death
coup ;; Change_of_leadership || revolution ;; Change_of_leadership || overthrown ;; Change_of_leadership || rule ;; Control
coup ;; Change_of_leadership || coup ;; Change_of_leadership || acknowledged ;; Statement || releasing ;; Statement || planning ;; Arranging
coup ;; Change_of_leadership || conceived ;; Creating || approved ;; Agree_or_refuse_to_act || carried out ;; Military_operation
attacks ;; Attack || attacks ;; Attack
killed ;; Killing || drove ;; Motion || hijacked ;; Kidnapping
got out ;; Motion || stabbed ;; Bodily_harm || injured ;; Bodily_harm || death ;; Death
attackers ;; Attack || killed ;; Killing || brought ;; Bringing || custody ;; Arrest
started ;; Process_start || explosions ;; Damaging || fire ;; Catastrophe
killed ;; Killing || injuring ;; Bodily_harm
killed ;; Killing || escaped ;; Escaping || detained ;; Arrest || dead ;; Death || arrest ;; Arrest || shot ;; Use_firearm
says ;; Communication || attackers ;; Attack || confessed ;; Reveal_secret || terrorist ;; Terrorism || attackers ;; Attack || Movement ;; Social_event || claims ;; Statement || frustrated ;; Hindering || protest ;; Request
patrolled ;; Patrolling || closed down ;; Openness
acknowledged ;; Statement || attack ;; Attack || attack ;; Attack
given ;; Giving || death ;; Death || attacks ;; Attack || sentences ;; Rewards_and_punishments || involvement ;; Participation || prison ;; Prison
fought ;; Hostile_encounter || engagement ;; Hostile_encounter
shipping ;; Sending || operation ;; Military_operation || engaged ;; Hostile_encounter
sent ;; Sending || eliminate ;; Removing || threat ;; Terrorism
alerted ;; Warning || prisoners ;; Prison || sailing ;; Self_motion
discovered ;; Know || captured ;; Conquering
killed ;; Killing || prepare ;; GetReady || receive ;; Receiving || attack ;; Attack
surrendering ;; Surrendering || wounded ;; Bodily_harm || resist ;; Defending || reduced ;; Cause_change_of_position_on_a_scale || killed ;; Killing
wounds ;; Bodily_harm || struck ;; Attack || suffered ;; Bodily_harm || death ;; Death
escaped ;; Escaping || repaired ;; Recovering
took ;; Bringing || commissioned ;; Change_of_leadership || wrecked ;; Catastrophe
attacked ;; Terrorism
wounded ;; Bodily_harm || killed ;; Killing
claimed ;; Statement || attack ;; Terrorism
works ;; Action
supplied ;; Supply || joint ;; Prison || attack ;; Terrorism || attack ;; Terrorism || confirmed ;; Check || executed ;; Action || admitted ;; Statement
attack ;; Terrorism || strikes ;; Attack || conducted ;; Action
killed ;; Killing
attacks ;; Attack || killed ;; Killing
bombed ;; Attack || strikes ;; Attack || intensified ;; Process_start
killed ;; Killing || reported ;; Reporting || attack ;; Terrorism
killed ;; Killing || reported ;; Reporting
killed ;; Killing || attack ;; Terrorism
culminated ;; Process_end || operation ;; Military_operation || operation ;; Military_operation
Battle ;; Hostile_encounter || forces ;; Causation || took place ;; Process_start
War ;; Military_operation
killed ;; Killing || warfare ;; Hostile_encounter || rebel ;; Attack || siege ;; Besieging
fighting ;; Hostile_encounter || forces ;; Causation || destruction ;; Destroying || resulted in ;; Causation
taken ;; Conquering || prisoner ;; Prison || survived ;; Preserving
died ;; Death || destroyed ;; Destroying || looted ;; Robbery || torture ;; Bodily_harm
declared ;; Expressing_publicly || War ;; Military_operation || independence ;; Change_of_leadership || operations ;; Military_operation || combat ;; Hostile_encounter || fought ;; Hostile_encounter
war ;; Military_operation || War ;; Military_operation || Aggression ;; Hostile_encounter || referred ;; Name_conferral
War ;; Military_operation || used ;; Using
leave ;; Departing || become ;; Becoming || opposed ;; Agree_or_refuse_to_act || wanted ;; Request || wanted ;; Request || supported ;; Supporting
conquer ;; Conquering || federation ;; Collaboration || sought ;; Statement
declared ;; Expressing_publicly || agreed ;; Agree_or_refuse_to_act || cut ;; Removing || independence ;; Change_of_leadership || postpone ;; Change_event_time
occupying ;; Control
forces ;; Causation || established ;; Creating
deployed ;; Arranging || became ;; Becoming || ceasefire ;; Process_end || recognition ;; Judgment_communication || force ;; Causation || combat ;; Hostile_encounter
encompassed ;; Surrounding
offensives ;; Attack || offensives ;; Attack || operation ;; Military_operation || operation ;; Military_operation || storm ;; Attack || launched ;; Process_start || ended ;; Process_end || war ;; Military_operation
reintegrated ;; Becoming_a_member
ended ;; Process_end || achieved ;; Achieve || declared ;; Expressing_publicly || preservation ;; Preserving || war ;; Military_operation || war ;; Military_operation || independence ;; Change_of_leadership
costs ;; Cost || ruined ;; Destroying || damaged ;; Damaging
killed ;; Killing || war ;; Hostile_encounter
began ;; Process_start || cooperate ;; Collaboration || remain ;; Temporary_stay || filed ;; Submitting_documents || lawsuits ;; Legality
verdict ;; Legal_rulings || create ;; Creating || returned ;; Arriving || colluded ;; Collaboration
involvement ;; Participation || operation ;; Military_operation || storm ;; Self_motion || alleged ;; Statement || prosecuted ;; Action
acquitted ;; Legal_rulings || convictions ;; Prison || overturned ;; Change
dismissed ;; Agree_or_refuse_to_act
reaffirmed ;; Statement || present ;; Presence || ruled ;; Using || taken place ;; Process_start
fires ;; Catastrophe || deployed ;; Arranging || fight ;; Defending
fires ;; Catastrophe || deployed ;; Arranging || help ;; Assistance || joint ;; Prison
declared ;; Expressing_publicly || fires ;; Catastrophe
asked ;; Request || assist ;; Assistance || fire ;; Attack
announced ;; Expressing_publicly || become ;; Becoming || fire ;; Attack
called ;; Statement
ended ;; Process_end || started ;; Process_start || siege ;; Besieging || crisis ;; Catastrophe || massacre ;; Killing || imprisonment ;; Prison || deaths ;; Death || illegal ;; Legality || lasted ;; Process_end || involved ;; Cause_to_be_included
began ;; Process_start || crisis ;; Catastrophe || occupied ;; Control
sent ;; Sending || demanded ;; Request || withdrawal ;; Escaping || recognition ;; Supporting || independence ;; Change_of_leadership
stormed ;; Attack || use ;; Using || standoff ;; Hostile_encounter
killed ;; Killing
reforms ;; Reforming_a_system || contributed ;; Causation || strengthening ;; Cause_change_of_strength || led to ;; Causation
escaped ;; Escaping || crisis ;; Catastrophe || involved ;; Cause_to_be_included
negotiations ;; Communication || allowed ;; Preventing_or_letting || crisis ;; Catastrophe || crisis ;; Catastrophe || report ;; Reporting || allocation ;; Deciding || used ;; Using || persisted ;; Coming_to_be
criticized ;; Judgment_communication || concluding ;; Process_end || taking ;; Action || using ;; Using || siege ;; Besieging
occurred ;; Coming_to_be || rape ;; Committing_crime || held ;; Hold || raped ;; Bodily_harm
faced ;; Confronting_problem || rape ;; Committing_crime
faced ;; Confronting_problem || faced ;; Confronting_problem || imprisonment ;; Prison || charges ;; Arrest || upheld ;; Legal_rulings
pleaded ;; Request
incident ;; Attack || received ;; Receiving
aware ;; Perception_active || notified ;; Reporting
began ;; Process_start || trials ;; Criminal_investigation || pleading ;; Judgment_communication || sentenced ;; Prison
sentenced ;; Prison
copulation ;; Body_movement || convicted ;; Legal_rulings
bombing ;; Attack || took place ;; Process_start || Affair ;; Catastrophe || Riot ;; Violence || Riot ;; Violence || demonstration ;; Protest || Massacre ;; Killing
began ;; Process_start || support ;; Supporting || killed ;; Killing || rally ;; Social_event || striking ;; Attack || injured ;; Bodily_harm
disperse ;; Dispersal || deaths ;; Death || bomb ;; Attack || meeting ;; Social_event || bomb ;; Attack || blast ;; Attack || gunfire ;; Use_firearm || threw ;; Self_motion || resulted in ;; Causation || wounded ;; Bodily_harm
legal ;; Legality || conspiracy ;; Committing_crime || convicted ;; Legal_rulings
built ;; Building || bomb ;; Attack || trial ;; Criminal_investigation || thrown ;; Self_motion
death ;; Death || prison ;; Prison || sentenced ;; Legal_rulings
suicide ;; Killing || sentences ;; Legal_rulings || prison ;; Prison || jail ;; Prison || face ;; Confronting_problem || committed ;; Killing
hanged on ;; Killing
criticized ;; Judgment_communication || trial ;; Criminal_investigation || pardoned ;; Legal_rulings
Affair ;; Catastrophe
sculpture ;; Create_artwork || dedicated ;; Giving || incident ;; Catastrophe || designated ;; Choosing
designated ;; Choosing
found ;; Know || parked ;; Placing || charred ;; Bodily_harm
death ;; Death || charged ;; Judgment_communication
shooting ;; Use_firearm || killing ;; Killing || convicted ;; Legal_rulings || sentenced ;; Legal_rulings
charges ;; Legality || convicted ;; Legal_rulings || sentenced ;; Legal_rulings
obstructing ;; Hindering || burning ;; Bodily_harm || came to ;; Motion || aid ;; Assistance
prison ;; Prison
charged ;; Judgment_communication || lying ;; Placing
charged ;; Judgment_communication || lying ;; Placing
prison ;; Prison
accused ;; Judgment_communication || attacking ;; Attack
accused ;; Judgment_communication
death ;; Death || misconduct ;; Action
ordering ;; Arranging || convictions ;; Legal_rulings || vacated ;; Removing
found ;; Know || conducted ;; Action
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
renamed ;; Name_conferral || attack ;; Attack || battle ;; Hostile_encounter || Battle ;; Hostile_encounter
attacks ;; Attack || Battle ;; Hostile_encounter || press ;; Motion || regroup ;; Cause_to_amalgamate || recover ;; Recovering || give ;; Giving || pause ;; Process_end
gave ;; Giving || ended ;; Process_end || battle ;; Hostile_encounter || battles ;; Hostile_encounter || operations ;; Military_operation
attack ;; Attack || fulfil ;; Achieve
muted ;; Preventing_or_letting || victory ;; Earnings_and_losses
obtain ;; Getting || capture ;; Conquering || redeem ;; Recovering || failure ;; Earnings_and_losses
attack ;; Attack || bombardment ;; Attack
captured ;; Conquering || threatened ;; Warning
battle ;; Hostile_encounter || called ;; Name_conferral
beaten ;; Bodily_harm
relieved ;; Process_end || suffered ;; Confronting_problem || prisoner ;; Prison || taken ;; Bringing || casualties ;; Bodily_harm
bombing ;; Attack
killed ;; Killing || injured ;; Bodily_harm || happened ;; Presence || destroyed ;; Destroying
burned ;; Bodily_harm || damaged ;; Damaging || destroyed ;; Destroying || destroyed ;; Destroying || causing ;; Causation
received ;; Receiving || engaged ;; Participation
rescue ;; Rescuing
attack ;; Attack
arrested ;; Arrest || stopped ;; Process_end
arrested ;; Arrest || linked ;; Connect || charged ;; Judgment_communication
identified ;; Check
detonated ;; Attack
assisted ;; Assistance
timed ;; Process_start
collected ;; Come_together
convicted ;; Criminal_investigation
executed ;; Rewards_and_punishments || sentenced ;; Prison
sentenced ;; Prison || received ;; Receiving || prosecution ;; Legality
passed ;; Process_end || tightened ;; Cause_change_of_strength || increase ;; Cause_change_of_position_on_a_scale
dedicated ;; Giving
entered ;; Arriving
ended ;; Process_end || restored ;; Recovering
seized ;; Hold
established ;; Building
attacks ;; Attack
transferred ;; Military_operation
given ;; Giving || terminated ;; Process_end
ceased ;; Process_end || disabled ;; Destroying
occupied ;; Control
rebuilt ;; Building
violence ;; Violence
withdrawn ;; Escaping || fell ;; Motion_directional
called ;; Name_conferral || said ;; Telling
killing ;; Killing || impact ;; Influence || crashed ;; Motion || bursting ;; Destroying
died ;; Death
published ;; Publishing || resulted in ;; Causation || incorporated ;; Cause_to_amalgamate
worked ;; Employment || transfer ;; Giving || control ;; Control || found ;; Coming_to_believe || attempted ;; Hold || initiated ;; Process_start
Corruption ;; Committing_crime
learned ;; Know || obfuscating ;; Hiding_objects || provided ;; Giving
reported ;; Reporting || received ;; Receiving
included ;; Cause_to_be_included || obtained ;; Getting
documented ;; Recording
said ;; Communication || cited ;; Judgment_communication
prohibited ;; Preventing_or_letting || operating ;; Being_in_operation
re-branded ;; Name_conferral
conducted ;; Action || operations ;; Military_operation || Campaign ;; Military_operation || War ;; Military_operation || commanded ;; Control
began ;; Process_start || decided ;; Deciding || taking part ;; Dispersal || attach ;; Connect || used ;; Using || invasion ;; Attack || forces ;; Causation || planning ;; Arranging || Operation ;; Military_operation || invasion ;; Attack
training ;; Education_teaching || brought ;; Bringing || deployed ;; Arranging || operational ;; Being_in_operation
dropped ;; Motion_directional || fighting ;; Hostile_encounter || linking ;; Connect || seizing ;; Conquering || supporting ;; Supporting
retreat ;; Escaping || retreat ;; Escaping || casualties ;; Catastrophe || attacked ;; Attack || reached ;; Arriving || link ;; Connect || forced ;; Request || taken ;; Getting
took ;; Conquering || used ;; Using || serving ;; Assistance || forces ;; Causation || advancing ;; Cause_to_make_progress || suffered ;; Bodily_harm || casualties ;; Death
left ;; Departing || invasion ;; Attack || rejoin ;; Becoming_a_member || train ;; Education_teaching || Operation ;; Military_operation || transferred ;; Military_operation
began ;; Process_start || defeated ;; Earnings_and_losses
commanded ;; Control
murder ;; Killing || ravaged ;; Destroying || siege ;; Besieging || ruined ;; Destroying || turned ;; Change
attacked ;; Attack
help ;; Assistance || forced ;; Motion
transferred ;; Sending
concluded ;; Process_end
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || invasion ;; Attack || War ;; Hostile_encounter
conflicts ;; Hostile_encounter || invasion ;; Attack || invasions ;; Attack
transferred ;; Military_operation || Break ;; Destroying
establish ;; Achieve
deployed ;; Arranging || attack ;; Attack
involved ;; Cause_to_be_included || war ;; Hostile_encounter || operation ;; Military_operation || campaign ;; Military_operation
occurred ;; Coming_to_be
storm ;; Catastrophe || dropped ;; Motion
storm ;; Catastrophe || brought ;; Bringing || flooding ;; Motion
brought ;; Bringing
storm ;; Catastrophe || combination ;; Cause_to_amalgamate
transferred ;; Giving || enabling ;; Preventing_or_letting || tracked ;; Motion || intensification ;; Expansion || storm ;; Catastrophe
storm ;; Catastrophe || prevented ;; Preventing_or_letting || exiting ;; Departing
influenced ;; Influence || state ;; Statement || looping ;; Motion || resulted in ;; Causation || took ;; Conquering
beginning ;; Process_start || festival ;; Social_event || event ;; Social_event || marks ;; Recording
festival ;; Social_event || arranged ;; Arranging || celebrated ;; Social_event
started ;; Process_start || festival ;; Social_event || presented ;; Presence || spread ;; Dispersal
spread ;; Dispersal || festival ;; Social_event
festival ;; Social_event || carry out ;; Action
transferred ;; Giving || festival ;; Social_event || break ;; Destroying
carried out ;; Military_operation
became ;; Becoming
cleared ;; Removing || entered ;; Arriving
split ;; Destroying
pushed ;; Motion || pushed ;; Motion
renamed ;; Name_conferral || remained ;; Presence
cut ;; Removing
use ;; Using
replaced ;; Change_of_leadership || defeat ;; Conquering
destroyed ;; Destroying || left ;; Departing
transfer ;; Military_operation || disintegrating ;; Destroying || incorporated ;; Cause_to_amalgamate || save ;; Rescuing || mobilizing ;; Cause_to_amalgamate || consisted ;; Cause_to_be_included
crossed ;; Motion_directional
attack ;; Attack
capture ;; Conquering || threatened ;; Warning
undertaken ;; Action
assault ;; Attack || followed ;; Motion || soften ;; Cause_change_of_strength
declared ;; Expressing_publicly
fight ;; Hostile_encounter
started ;; Process_start || cleared ;; Emptying
transferred ;; Military_operation || attacked ;; Attack
called ;; Request || led to ;; Causation
took ;; Conquering
eliminated ;; Removing
lasted ;; Process_end
ignited ;; Process_start
disagree ;; Agree_or_refuse_to_act
transferred ;; Giving
ended ;; Process_end || claimed ;; Statement
remained ;; Presence
withdrew ;; Escaping || threw ;; Self_motion || broke out ;; Process_start
changed ;; Change
re-installed ;; Change_of_leadership || deposed ;; Change_of_leadership || resumed ;; Process_start || killed ;; Killing
ruled ;; Control
deposed ;; Change_of_leadership || reigned ;; Change_of_leadership
occurred ;; Coming_to_be || sparked ;; Process_start
returned ;; Arriving
avoided ;; Preventing_or_letting
married ;; Forming_relationships || assumed ;; Receiving
ruled ;; Control
defeated ;; Conquering || killed ;; Killing
capture ;; Conquering || invading ;; Attack || Fall ;; Conquering
attackers ;; Attack || begun ;; Process_start || control ;; Control || siege ;; Besieging || commanded ;; Control || defeated ;; Conquering || commanded ;; Control || took ;; Getting || ending ;; Process_end
conquering ;; Conquering || transferred ;; Change || established ;; Creating
capture ;; Conquering || end ;; Process_end || marked ;; Recording
defence ;; Defending || conquest ;; Conquering || blow ;; Influence || left ;; Causation || advance ;; Self_motion || adversary ;; Hostile_encounter || dealt ;; Resolve_problem
used ;; Using || protect ;; Preventing_or_letting || invaders ;; Attack
use ;; Using || due ;; Causation
conquest ;; Conquering || end ;; Process_end || end ;; Process_end
invasions ;; Attack || conflicts ;; Hostile_encounter || conquest ;; Conquering || control ;; Control || led to ;; Causation || re-established ;; Recovering
began ;; Process_start || struggle ;; Hostile_encounter || incorporation ;; Cause_to_amalgamate || War ;; Military_operation
resistance ;; Defending || rule ;; Control || based ;; Causation
paid ;; Commerce_pay || deteriorated ;; Cause_change_of_position_on_a_scale || attack ;; Attack
collapse ;; Destroying || caught ;; Self_motion || seize ;; Conquering || pull ;; Motion || occupied ;; Control
abdicated ;; Change_of_leadership || announced ;; Expressing_publicly || controlled ;; Control || annexation ;; Change_of_leadership || control ;; Control || captured ;; Conquering || taken ;; Bringing
ruled ;; Control || offensive ;; Attack || launched ;; Process_start || regain ;; Getting
deaths ;; Death || took ;; Getting
inflicting ;; Causation || proved ;; Causation
campaigns ;; Military_operation || conquered ;; Conquering || expanded ;; Expansion
proclaimed ;; Statement || death ;; Death || legitimate ;; Legality
turned ;; Becoming || war ;; Military_operation
got ;; Getting || started ;; Process_start || seize ;; Conquering
campaigns ;; Military_operation || devastated ;; Destroying || stop ;; Preventing_or_letting
died ;; Death || battle ;; Hostile_encounter
reign ;; Control || reigns ;; Control || followed ;; Motion
negotiated ;; Communication || surrender ;; Surrendering
surrendered ;; Surrendering || transferred ;; Sending || assimilated ;; Cause_to_amalgamate
lost ;; Earnings_and_losses || remained ;; Presence
transformed ;; Change || ruled ;; Control
collapse ;; Destroying || domination ;; Control || control ;; Control || took ;; Conquering || replace ;; Change
campaign ;; Military_operation || involving ;; Cause_to_be_included || implemented ;; Action
created ;; Creating || eradicate ;; Removing || reverse ;; Cause_change_of_position_on_a_scale || suppress ;; Hindering
disputed ;; Quarreling
attributed ;; Causation || killed ;; Killing || imprisoned ;; Arrest
mentions ;; Statement || gives ;; Giving || killed ;; Killing || kidnapped ;; Kidnapping || killed ;; Killing || transferred ;; Sending || crossed ;; Motion_directional || tortured ;; Bodily_harm || executed ;; Killing
abducted ;; Kidnapping || tortured ;; Bodily_harm || murdered ;; Killing
included ;; Cause_to_be_included
described ;; Statement || used ;; Using
joined ;; Participation
provided ;; Supply || planning ;; Arranging || training ;; Education_teaching
crashed ;; Catastrophe || take-off ;; Departing || Flight ;; Motion || flight ;; Motion || scheduled ;; Hold || operated ;; Control
contacted ;; Communication || takeoff ;; Departing || caught ;; Causation || fire ;; Destroying || broke up ;; Destroying
flight ;; Motion || carried ;; Bringing
survived ;; Rescuing
heading ;; Motion
crash ;; Catastrophe || protest ;; Self_motion || sparked ;; Causation
change ;; Change || change ;; Change || complete ;; Process_end || cause ;; Causation
crash ;; Catastrophe || bringing ;; Bringing || death ;; Death
crash ;; Catastrophe || killed ;; Killing || taking ;; Action || control ;; Control || losing ;; Earnings_and_losses
faced ;; Confronting_problem || remove ;; Removing || transferred ;; Sending
disputed ;; Quarreling || controlled ;; Control || surrounding ;; Surrounding || recognized ;; Know
conflict ;; Hostile_encounter
decided ;; Deciding
began ;; Process_start || demanded ;; Request || transferred ;; Giving
provided ;; Supply || signed ;; Sign_agreement || increasing ;; Cause_change_of_position_on_a_scale || deteriorated ;; Cause_change_of_position_on_a_scale
fighting ;; Hostile_encounter
increased ;; Cause_change_of_position_on_a_scale
hijacked ;; Committing_crime || diverted ;; Removing
hijacking ;; Kidnapping || shot ;; Use_firearm || believed ;; Know
transferred ;; Sending
felt ;; Perception_active
executed ;; Killing
releasing ;; Releasing || accept ;; Agree_or_refuse_to_act
thrown ;; Body_movement || bled ;; Death
hijacked ;; Committing_crime
flown ;; Motion || land ;; Motion
condemned ;; Judgment_communication
demanded ;; Request || released ;; Releasing
accusing ;; Judgment_communication || shot ;; Use_firearm
released ;; Releasing
murdering ;; Killing || thrown ;; Motion || executed ;; Killing
said ;; Communication || buried ;; Placing || returned ;; Motion
avenging ;; Revenge || travelled ;; Traveling || join ;; Becoming_a_member
election ;; Change_of_leadership
contested ;; Hostile_encounter
ended ;; Process_end
seized ;; Hold
razing ;; Destroying
renewed ;; Recovering
transferred ;; Giving
assisting ;; Assistance || happened ;; Presence
collapsed ;; Process_end
inflicted ;; Causation || put ;; Placing || defeat ;; Conquering
agreed ;; Agree_or_refuse_to_act || broke down ;; Destroying || met ;; Come_together || sue ;; Legality
declared ;; Expressing_publicly || rebelled ;; Change_of_leadership
sent ;; Sending
occupied ;; Control
counter-offensive ;; Attack || drove ;; Bringing
siege ;; Besieging || defeated ;; Conquering
helped ;; Assistance
restored ;; Recovering || surrender ;; Surrendering || stormed ;; Attack
writing ;; Writing || reacted ;; Response
gave ;; Giving || sent ;; Sending || rebellion ;; Change_of_leadership
hijacked ;; Kidnapping
became ;; Becoming || forced ;; Motion || remote ;; Escaping
killed ;; Killing || reported ;; Reporting || injury ;; Bodily_harm
hijacking ;; Committing_crime || escape ;; Escaping
forced ;; Motion
handed over ;; Giving || shot ;; Use_firearm || killed ;; Killing || hijacking ;; Kidnapping || subdued ;; Conquering
prevented ;; Preventing_or_letting || hijacked ;; Committing_crime
taken ;; Conquering || hijacked ;; Kidnapping || pressure ;; Convincing
releasing ;; Releasing || transferred ;; Sending || keeping ;; Temporary_stay || freed ;; Releasing
defense ;; Defending || kept ;; Temporary_stay
convert ;; Change
announced ;; Expressing_publicly || destroy ;; Destroying
challenge ;; Competition
declared ;; Expressing_publicly || deployed ;; Arranging || became ;; Becoming || forces ;; Causation || triggering ;; Process_start || involving ;; Cause_to_be_included
enabled ;; Preventing_or_letting || released ;; Releasing || remaining ;; Temporary_stay || exchange ;; Exchange
transfer ;; Giving || issue ;; Expressing_publicly
allows ;; Preventing_or_letting || defence ;; Defending || administer ;; Control
became ;; Becoming || issue ;; Creating
became ;; Becoming || involving ;; Cause_to_be_included || suffrage ;; Change_of_leadership || case ;; Criminal_investigation || elect ;; Choosing || suffrage ;; Change_of_leadership || issues ;; Expressing_publicly || elections ;; Choosing
bring about ;; Causation || development ;; Cause_to_make_progress || threatened ;; Warning
rejected ;; Response || negotiating ;; Communication || discussions ;; Communication || Attempts ;; Preventing_or_letting
changing ;; Change || deal ;; Resolve_problem || faced ;; Confronting_problem
democratic ;; Social_event || come to ;; Motion || halt ;; Change || election ;; Change_of_leadership
triggered ;; Causation || seen ;; Traveling || said ;; Telling || electoral ;; Choosing || Revolution ;; Change_of_leadership || pronouncement ;; Statement || stalling ;; Hindering
offensive ;; Attack || fought ;; Hostile_encounter
fought ;; Hostile_encounter || formed ;; Creating
supported ;; Supporting
transferred ;; Giving || pulled out ;; GiveUp
advances ;; Cause_to_make_progress || surrounded ;; Surrounding
controlled ;; Control || contributed ;; Giving
designed ;; Creating || besieged ;; Besieging || draw ;; Removing
pushed ;; Motion || capturing ;; Conquering
failed ;; Change || achieve ;; Getting || brought ;; Causation
improved ;; Cause_to_make_progress || dominating ;; Control
allowing ;; Preventing_or_letting || secure ;; Getting || renewed ;; Recovering
provided ;; Supply
emerging ;; Coming_to_be
transferred ;; Giving
creating ;; Creating || combines ;; Cause_to_amalgamate
began ;; Process_start
become ;; Becoming
began ;; Process_start || travelled ;; Traveling || introduced ;; Creating
developing ;; Coming_to_be || introduced ;; Statement || enabled ;; Preventing_or_letting
chosen ;; Choosing || hosted ;; Social_event
hosted ;; Social_event
known ;; Know || offensive ;; Attack || liberation ;; Hostile_encounter || Offensive ;; Attack || fought ;; Hostile_encounter
split ;; Destroying || clear ;; Removing || offensive ;; Attack || occupied ;; Control
stretching ;; Expansion || involving ;; Cause_to_be_included || War ;; Hostile_encounter || offensives ;; Attack
operation ;; Military_operation || destroyed ;; Destroying || rebuilding ;; Building || reduced ;; Cause_change_of_position_on_a_scale || disbanded ;; Dispersal
abandonment ;; GiveUp || losses ;; Earnings_and_losses || lost ;; Earnings_and_losses
defeat ;; Conquering
split ;; Destroying || offensive ;; Attack
pushed ;; Motion || pushed ;; Motion
renamed ;; Name_conferral
save ;; Rescuing || transfer ;; Military_operation || command ;; Control || reinforcements ;; Cause_change_of_strength || collapse ;; Catastrophe || forced ;; Request || crumbling ;; Destroying
weakened ;; Cause_change_of_strength || stationed ;; Placing || transfers ;; Military_operation || played ;; Competition || forces ;; Hostile_encounter || Offensive ;; Attack || successes ;; Achieve || landings ;; Arriving || Operation ;; Military_operation || influencing ;; Influence
told ;; Telling || stationed ;; Placing || deprived ;; Cause_change_of_position_on_a_scale
troops ;; Hostile_encounter || assault ;; Attack || deprived ;; Cause_change_of_position_on_a_scale
defeat ;; Conquering || dismissed ;; Dispersal || replaced ;; Change
participated ;; Participation || war ;; Hostile_encounter || offensive ;; Attack
stationed ;; Placing || deployed ;; Arranging || stationed ;; Placing
crashed ;; Catastrophe
developed ;; Coming_to_be
crashed ;; Catastrophe
died ;; Death || transferred ;; Sending
burns ;; Bodily_harm
died ;; Death
began ;; Process_start || movement ;; Social_event || led ;; Causation
declared ;; Expressing_publicly || transferred ;; Giving || fled ;; Escaping || reached ;; Achieve
condemned ;; Judgment_communication
fell ;; Motion_directional || stopped ;; Preventing_or_letting
supervise ;; Scrutiny || designated ;; Request
run ;; Hold || relieve ;; Cause_change_of_strength || establish ;; Creating || elections ;; Choosing
approved ;; Agree_or_refuse_to_act || launching ;; Hold
appointed ;; Choosing
elections ;; Choosing
defined ;; Statement || forces ;; Military_operation || warfare ;; Hostile_encounter || defense ;; Defending || conflict ;; Hostile_encounter || conflict ;; Hostile_encounter || support ;; Supporting
refer ;; Adducing || beginning ;; Process_start || conflict ;; Hostile_encounter || transfer ;; Motion || following ;; Self_motion
fomented ;; Convincing
War ;; Hostile_encounter || seen ;; Perception_active
conflicts ;; Hostile_encounter || invasion ;; Attack || support ;; Supporting
departure ;; Departing || resulted in ;; Causation
cost ;; Cost || benefited ;; Influence
increased ;; Cause_change_of_position_on_a_scale || unorganized ;; Destroying || invasion ;; Attack || conflict ;; Hostile_encounter || resulted in ;; Causation
used ;; Using || casualties ;; Catastrophe
emerged ;; Coming_to_be || support ;; Supporting
resulted in ;; Causation || withdrew ;; Escaping || withdrawal ;; Escaping || passed ;; Motion_directional || election ;; Change_of_leadership || elected ;; Choosing
withdraws ;; Escaping || consider ;; Influence || withdrawal ;; Escaping
monopolized ;; Control || withdrawal ;; Escaping || control ;; Control
Capture ;; Conquering || engagement ;; Hostile_encounter
Offensive ;; Attack || fortified ;; Cause_change_of_strength || ended ;; Process_end
observation ;; Perception_active || contained ;; Containing
enforced ;; Action || Battle ;; Hostile_encounter || adopted ;; Choosing || defensive ;; Defending || defence ;; Defending || counter-attack ;; Attack || disaster ;; Attack
captured ;; Conquering || defeated ;; Conquering || attack ;; Attack
attack ;; Attack || repulsed ;; Defending
defended ;; Defending || counter-attack ;; Attack
attack ;; Attack || took place ;; Process_start
conducted ;; Action || advanced ;; Self_motion || attack ;; Attack || extending ;; Expansion
captured ;; Conquering || defeated ;; Conquering || bombardment ;; Attack || losses ;; Earnings_and_losses || counter-attacks ;; Attack
attack ;; Attack
postponed ;; Change_event_time || attack ;; Attack || encirclement ;; Besieging || Operations ;; Military_operation
attacks ;; Attack || offensive ;; Attack || operations ;; Military_operation || divert ;; Cause_change_of_position_on_a_scale || included ;; Cause_to_be_included || transfer ;; Military_operation || claims ;; Statement || preparing ;; GetReady
took place ;; Process_start
relieved ;; Cure || started ;; Process_start
gain ;; Getting
attack ;; Attack
delivered ;; Sending
given ;; Giving
permit ;; Preventing_or_letting || planned ;; Arranging || assure ;; Statement || took over ;; Conquering
captured ;; Conquering || ended ;; Process_end
asked ;; Request || sent ;; Sending || transferred ;; Military_operation || move ;; Motion
ended ;; Process_end
Uprising ;; Attack || War ;; Hostile_encounter || capitulation ;; Surrendering || led to ;; Causation || campaign ;; Military_operation
increased ;; Cause_change_of_position_on_a_scale || offensive ;; Attack || captured ;; Control || capturing ;; Control
offensive ;; Attack || operations ;; Military_operation || launching ;; Process_start
realized ;; Coming_to_believe || withdrawal ;; Escaping || attack ;; Attack || defend ;; Defending || ordered ;; Arranging
fled ;; Escaping || reaching ;; Arriving || retreat ;; Escaping
destruction ;; Destroying || led to ;; Causation || orders ;; Arranging || control ;; Control || command ;; Control || forces ;; Military_operation || exacerbated ;; Cause_change_of_position_on_a_scale
occurred ;; Presence || collapse ;; Conquering
transferred ;; Military_operation || collapse ;; Conquering || end ;; Process_end || forces ;; Military_operation || capture ;; Control || celebrate ;; Social_event
defended ;; Defending || became ;; Becoming || loss ;; Earnings_and_losses || fight ;; Hostile_encounter || regrouped ;; Cause_to_amalgamate
resigned ;; Change_of_leadership || reopen ;; Process_start || negotiations ;; Communication
taking over ;; Getting || preventing ;; Preventing_or_letting || remained ;; Presence
entering ;; Arriving || capitulated ;; Surrendering
suicide ;; Killing || surrender ;; Surrendering
conflict ;; Hostile_encounter || struggle ;; Hostile_encounter
signed ;; Sign_agreement || transferred ;; Giving || control ;; Control || conflict ;; Hostile_encounter || erupted ;; Process_start
organized ;; Arranging || entered ;; Arriving || establish ;; Building || escorted ;; Patrolling
warfare ;; Hostile_encounter || engaged ;; Participation
establish ;; Building || fight ;; Hostile_encounter
withdrew ;; Escaping || signing ;; Sign_agreement || conflict ;; Hostile_encounter
made ;; Manufacturing || take ;; Creating
reached ;; Arriving
death ;; Death || put ;; Placing
shifted ;; Change || conflict ;; Hostile_encounter
produced ;; Manufacturing || resolve ;; Resolve_problem || conflict ;; Hostile_encounter || agreement ;; Agree_or_refuse_to_act
control ;; Control || governed ;; Control
races ;; Competition
races ;; Competition || test ;; Testing || recognized ;; Know
founded ;; Creating || founded ;; Creating
race ;; Competition || held ;; Hold
race ;; Competition
held ;; Hold
Race ;; Competition || followed ;; Motion
races ;; Competition || race ;; Competition || called ;; Name_conferral || took place ;; Process_start || held ;; Hold
races ;; Competition || races ;; Competition || enter ;; Becoming_a_member || wish ;; Request || finish ;; Process_end || take part ;; Dispersal
races ;; Competition || races ;; Competition || staged ;; Participation || representing ;; Presence
participants ;; Participation || compete ;; Competition || members ;; Participation || events ;; Competition || growing ;; Cause_change_of_position_on_a_scale
released ;; Publishing || film ;; Writing
participants ;; Participation || directed ;; Action || complete ;; Process_end || follows ;; Self_motion || attempt ;; Self_motion
received ;; Receiving || awards ;; Award
helped ;; Assistance || tournament ;; Competition || held ;; Hold
crowned ;; Getting || cashed ;; Commerce_pay
tournament ;; Competition || win ;; Getting
competition ;; Competition || made ;; Action
tournament ;; Competition || participated ;; Participation || test ;; Testing || playing ;; Participation || hosts ;; Social_event
miss ;; Earnings_and_losses || taking part ;; Dispersal || finals ;; Competition
playoff ;; Hostile_encounter || took place ;; Process_start
revolution ;; Change_of_leadership || revolution ;; Change_of_leadership || replacement ;; Change || deposition ;; Legality
took ;; Conquering || casualties ;; Catastrophe || establishing ;; Building || led to ;; Causation
used ;; Using
became ;; Becoming || widespread ;; Dispersal || support ;; Supporting || feared ;; Change_sentiment || lead to ;; Causation
issue ;; Creating || marriage ;; Forming_relationships || remained ;; Presence || perceived ;; Know
raising ;; Cause_change_of_position_on_a_scale || changed ;; Change || birth ;; Coming_to_be
refused ;; Agree_or_refuse_to_act || test ;; Testing || caused ;; Causation || avoid ;; Preventing_or_letting || suspended ;; Process_end
support ;; Supporting || remained ;; Presence || seemed ;; Perception_active || assault ;; Attack || prosecution ;; Legality
widespread ;; Dispersal || destroyed ;; Destroying || led to ;; Causation
built ;; Building || defend ;; Defending
support ;; Supporting
advanced ;; Cause_to_make_progress || ordered ;; Arranging || reduced ;; Cause_change_of_position_on_a_scale || went ;; Motion
making ;; Manufacturing || joint ;; Collaboration
Revolution ;; Change_of_leadership
ended ;; Process_end || confirming ;; Check || dispute ;; Quarreling || established ;; Building
contained ;; Containing || removed ;; Removing || test ;; Testing || choice ;; Choosing || remained ;; Presence || remain ;; Presence
competition ;; Competition || competition ;; Competition || marked ;; Recording || anniversary ;; Social_event
competition ;; Competition || assured ;; Achieve || victory ;; Earnings_and_losses || win ;; Getting || remaining ;; Presence
played ;; Competition
tournament ;; Competition || result ;; Causation || television ;; Communication || organises ;; Arranging
competition ;; Competition || result ;; Causation || duration ;; Change_event_time || ran ;; Hold || extended ;; Change_event_time
matches ;; Competition || retaining ;; Earnings_and_losses
matches ;; Competition || winning ;; Getting
regained ;; Getting || winning ;; Getting || tests ;; Testing
named ;; Name_conferral || test ;; Testing
regained ;; Getting
recorded ;; Recording
match ;; Competition
brought ;; Bringing || Beginning ;; Process_start
end ;; Process_end || held ;; Hold
brought ;; Bringing
avoid ;; Preventing_or_letting || rescheduling ;; Change_event_time || clashes ;; Hostile_encounter || held ;; Hold
organized ;; Arranging || Conference ;; Social_event || conference ;; Social_event || held ;; Hold
Conference ;; Social_event || support ;; Supporting || become ;; Becoming || proposed ;; Creating
conference ;; Social_event || inaugurated ;; Process_start || prize ;; Rewards_and_punishments
Conference ;; Social_event || work ;; Action || cause ;; Causation || occupy ;; Hold || awards ;; Giving || advanced ;; Cause_to_make_progress || contributed ;; Cause_to_make_progress
conference ;; Social_event || discuss ;; Communication || legal ;; Legality || legal ;; Legality || judges ;; Legal_rulings || bring together ;; Come_together
Conference ;; Social_event || hosted ;; Response || hosted ;; Response
received ;; Receiving || Conference ;; Social_event || Conference ;; Social_event || Retired ;; Process_end
held ;; Hold
discussions ;; Communication || lectures ;; Expressing_publicly || Conference ;; Social_event || leading ;; Causation
Conference ;; Social_event || held ;; Hold || hosted ;; Social_event || presented ;; Presence
recipient ;; Receiving
began ;; Process_start || conference ;; Social_event || publishing ;; Publishing || beginning ;; Process_start || conference ;; Social_event || conference ;; Social_event
focus ;; Aiming || explore ;; Scrutiny
leading ;; Causation || written ;; Writing
analysis ;; Research || legal ;; Legality || explore ;; Scrutiny || provide ;; Supply || promoting ;; Cause_to_make_progress
receipt ;; Receiving || decisions ;; Deciding || reflect ;; Influence
Defining ;; Judgment_communication
conference ;; Social_event || leading ;; Causation || test ;; Testing || cited ;; Judgment_communication || held ;; Hold
matches ;; Competition
formed ;; Coming_to_be || matches ;; Competition || played ;; Action
regarded ;; Change_sentiment || sent ;; Sending
gave ;; Giving || lost ;; Earnings_and_losses
ended ;; Process_end || beat ;; Competition || test ;; Testing
defeat ;; Conquering
made ;; Creating
took ;; Bringing || test ;; Testing
played ;; Action
dominated ;; Control || defeat ;; Conquering
took ;; Conquering || began ;; Process_start
fight ;; Defending
averaging ;; Cause_change_of_position_on_a_scale || matches ;; Competition
fought ;; Hostile_encounter
control ;; Control || extended ;; Expansion || regain ;; Getting || move ;; Motion
begin ;; Process_start || control ;; Control || dispatched ;; Sending
assisted ;; Assistance
decided ;; Deciding || assessed ;; Testing
captured ;; Conquering || retreat ;; Escaping || flanked ;; Attack || drove ;; Influence
retreated ;; Escaping || occupied ;; Control
held ;; Defending || held off ;; Defending
took place ;; Process_start || Trophy ;; Competition
participated ;; Participation || tournament ;; Competition
combined ;; Cause_to_amalgamate || tournament ;; Competition
match ;; Competition
given ;; Giving || start ;; Process_start || score ;; Getting
testing ;; Testing || used ;; Using
defeating ;; Conquering
ended ;; Process_end || became ;; Becoming
bombings ;; Use_firearm || took place ;; Process_start || War ;; Hostile_encounter
killed ;; Killing || bombings ;; Use_firearm
explosions ;; Use_firearm || struck ;; Destroying
blamed ;; Judgment_communication || explosions ;; Use_firearm
strike ;; Attack
fleeing ;; Escaping || fighting ;; Hostile_encounter
claimed ;; Statement || fired ;; Use_firearm || hit ;; Attack || adding ;; Cause_change_of_position_on_a_scale || launched ;; Hold
strikes ;; Attack || attack ;; Attack || terrorist ;; Terrorism || called ;; Statement
gave ;; Giving || said ;; Statement || damaged ;; Damaging || terrorism ;; Terrorism || instructions ;; Education_teaching || exams ;; Testing || rehabilitate ;; Recovering || destroyed ;; Destroying || rehabilitate ;; Recovering || affected ;; Influence
condemned ;; Judgment_communication || blamed ;; Judgment_communication || attack ;; Attack || forces ;; Military_operation || deaths ;; Death || airstrikes ;; Attack
claim ;; Statement || claimed ;; Statement || attacks ;; Attack || statements ;; Statement || separate ;; Causation
released ;; Publishing || hit ;; Attack || heard ;; Perception_active || explosion ;; Use_firearm
identified ;; Check || claimed ;; Statement || hit ;; Attack
blamed ;; Judgment_communication || claims ;; Statement || attack ;; Attack || explosions ;; Use_firearm || said ;; Communication
fired ;; Use_firearm || said ;; Statement || armed ;; Connect || followed ;; Self_motion || dropped ;; Self_motion || exploded ;; Use_firearm
reported ;; Reporting || seeing ;; Perception_active || explosions ;; Use_firearm
explain ;; Statement || explosion ;; Use_firearm
condemned ;; Judgment_communication || attack ;; Attack || terrorist ;; Terrorism
statement ;; Statement || started ;; Process_start || terrorist ;; Terrorism || armed ;; Bearing_arms || stressed ;; Statement
expressed ;; Expressing_publicly
struck ;; Attack || earthquake ;; Catastrophe
felt ;; Perception_active || positioned ;; Placing
prompting ;; Causation || causing ;; Causation || swayed ;; Motion || evacuations ;; Escaping
assessments ;; Testing || patrolled ;; Patrolling || carried out ;; Action
damage ;; Damaging || suffered ;; Bodily_harm || casualties ;; Death
tournament ;; Competition || edition ;; Writing || organised ;; Arranging
tournament ;; Competition || final ;; Competition || participate ;; Participation || defeating ;; Conquering || earning ;; Getting || hosted ;; Social_event
win ;; Earnings_and_losses || title ;; Name_conferral || move ;; Motion
become ;; Becoming || team ;; Cause_to_amalgamate || champions ;; Getting || team ;; Cause_to_amalgamate
tournament ;; Competition || host ;; Social_event || host ;; Social_event || chosen ;; Choosing
matches ;; Competition || played ;; Competition
tournament ;; Competition || hosted ;; Social_event
decided ;; Deciding || tournament ;; Competition || finalists ;; Participation || hosts ;; Social_event || qualification ;; Testing || featuring ;; Cause_to_be_included || qualified ;; Participation || qualified ;; Participation
tournament ;; Competition || Played ;; Competition
games ;; Competition || played ;; Competition || advancing ;; Cause_change_of_position_on_a_scale
tournament ;; Competition || matches ;; Competition || competed ;; Competition || beginning ;; Process_start || ending ;; Process_end || match ;; Competition || teams ;; Participation || quarter-finals ;; Competition || final ;; Competition
match ;; Competition || teams ;; Participation || semi-finals ;; Competition || played ;; Competition
competition ;; Competition || tournament ;; Competition || going ;; Self_motion || defending ;; Defending
recorded ;; Recording || finish ;; Process_end || Cup ;; Competition || quarter-finals ;; Competition || knocked out ;; Competition
occur ;; Coming_to_be || election ;; Choosing
began ;; Process_start
raided ;; Attack || killed ;; Killing
killed ;; Killing || raid ;; Attack || injured ;; Bodily_harm
named ;; Name_conferral || detained ;; Arrest || estimated ;; Testing
promoted ;; Cause_change_of_position_on_a_scale
held ;; Hold || held ;; Hold
conceived ;; Creating || generate ;; Creating || hoped ;; Self_motion
created ;; Creating || testing ;; Testing || demand ;; Request
became ;; Becoming || elected ;; Change_of_leadership
used ;; Using
declared ;; Expressing_publicly || continued ;; Being_in_operation
held ;; Hold || renamed ;; Name_conferral
used ;; Using
earthquake ;; Catastrophe || earthquake ;; Catastrophe || struck ;; Attack
earthquake ;; Catastrophe
devastated ;; Destroying || killing ;; Killing || injuring ;; Bodily_harm || damage ;; Damaging || cause ;; Causation
damaged ;; Damaging || destroyed ;; Destroying || left ;; Causation
caused ;; Causation || destruction ;; Destroying || aftershocks ;; Catastrophe || drove ;; Influence
discovered ;; Know || caused ;; Causation || earthquake ;; Catastrophe
responded ;; Response || donations ;; Giving || Damage ;; Damaging || estimated ;; Testing
dispatched ;; Sending || finding ;; Know || Rescue ;; Rescuing || assist ;; Assistance
destruction ;; Destroying || changes ;; Change
suggested ;; Convincing || deaths ;; Resolve_problem || address ;; Statement
competition ;; Competition
began ;; Process_start || ended ;; Process_end || test ;; Testing || test ;; Testing
commence ;; Process_start || matches ;; Competition
allowing ;; Preventing_or_letting || scheduled ;; Arranging || finish ;; Process_end || start ;; Process_start || played ;; Competition
competition ;; Competition || reverted ;; Change
competition ;; Competition || reported ;; Reporting || admitted ;; Agree_or_refuse_to_act || brokering ;; Arranging
competition ;; Competition || became ;; Becoming || expanded ;; Expansion || ran ;; Self_motion || expire ;; Limiting
tournament ;; Competition || confirmed ;; Check || sending ;; Sending || put ;; Placing
carried out ;; Action || sinking ;; Motion || operation ;; Military_operation
test ;; Testing || sank ;; Motion_directional
drowned ;; Death || sinking ;; Motion_directional
captured ;; Conquering || commit ;; Commitment || arson ;; Committing_crime || charged ;; Judgment_communication || damage ;; Damaging || murder ;; Committing_crime
resulted in ;; Causation
sentenced ;; Legal_rulings || pleaded ;; Request
spent ;; Expend_resource || freed ;; Releasing
terrorism ;; Terrorism || terrorism ;; Terrorism || bombing ;; Attack || referred ;; Statement
struck ;; Attack
formed ;; Coming_to_be || began ;; Process_start || produce ;; Creating
strengthened ;; Cause_change_of_strength || given ;; Giving
intensified ;; Cause_change_of_strength
assessed ;; Testing
became ;; Becoming
weaken ;; Cause_change_of_strength
stopped ;; Process_end
made ;; Causation
abandoned ;; GiveUp || damaged ;; Damaging
caused ;; Causation
destroyed ;; Destroying || damaged ;; Damaging
received ;; Receiving
became ;; Becoming || declared ;; Expressing_publicly
census ;; Scrutiny || collects ;; Come_together
census ;; Scrutiny || compiled ;; Creating
answering ;; Telling
legislated ;; Action || disseminate ;; Dispersal || collect ;; Come_together
census ;; Scrutiny || censuses ;; Scrutiny || organised ;; Arranging || censuses ;; Scrutiny
introduced ;; Creating
Censuses ;; Scrutiny
cost ;; Cost
conducted ;; Action || included ;; Cause_to_be_included
census ;; Scrutiny || examines ;; Testing
departed ;; Departing
operating ;; Being_in_operation || crashed ;; Catastrophe || takeoff ;; Self_motion
died ;; Death
accident ;; Catastrophe
established ;; Creating || investigate ;; Criminal_investigation || accident ;; Catastrophe
establish ;; Creating || decided ;; Deciding || takeoff ;; Motion || caged ;; Placing
instructions ;; Education_teaching || took ;; Action
examined ;; Testing || found ;; Know
discovered ;; Know
contributed ;; Giving || accident ;; Catastrophe
recommended ;; Convincing
uncovered ;; Reveal_secret || made ;; Manufacturing
tournament ;; Competition || test ;; Testing || status ;; Becoming || tournament ;; Competition || awarded ;; Giving
tournament ;; Competition || wins ;; Getting || final ;; Process_end || defeating ;; Conquering || stretched ;; Expansion
tournament ;; Competition
participants ;; Participation || matches ;; Competition || lengthened ;; Change_event_time || qualifying ;; Participation
final ;; Process_end || played ;; Competition || played ;; Competition
scheduled ;; Arranging || game ;; Competition
participated ;; Participation || winner ;; Getting || runners-up ;; Change_of_leadership || test ;; Research || qualified ;; Participation || match ;; Competition
connecting ;; Connect || bombing ;; Terrorism
filled ;; Filling || passed ;; Motion_directional || set off ;; Damaging
killed ;; Killing || injured ;; Bodily_harm
included ;; Cause_to_be_included
found ;; Know
encased ;; Preserving || packed ;; Placing
allowed ;; Preventing_or_letting || unaffected ;; Influence || bombing ;; Terrorism
disrupt ;; Hindering || arrive in ;; Arriving || condemned ;; Judgment_communication || attack ;; Attack || speculated ;; Coming_to_believe
investigation ;; Criminal_investigation
attack ;; Attack || charged ;; Judgment_communication || affiliated ;; Connect
acquitted ;; Committing_crime
killed ;; Killing || alleged ;; Statement
scheduled ;; Arranging || resulted in ;; Causation
linked ;; Connect
concurred ;; Agree_or_refuse_to_act
attack ;; Attack || report ;; Reporting || involved ;; Cause_to_be_included
declared ;; Expressing_publicly || terrorist ;; Terrorism || consulting ;; Communication
arrested ;; Arrest || bombings ;; Terrorism || raised ;; Cause_change_of_position_on_a_scale || carrying ;; Carry_goods || seen ;; Perception_active
found ;; Know || stated ;; Statement || noted ;; Know
conducted ;; Action || stated ;; Statement || informing ;; Telling
test ;; Testing
caused ;; Causation || telling ;; Telling
find ;; Coming_to_believe || stated ;; Statement || involved ;; Cause_to_be_included || investigated ;; Criminal_investigation
found ;; Know
questioned ;; Communication
violence ;; Violence || riots ;; Violence || broke out ;; Process_start
reported ;; Reporting || taken place ;; Process_start
died ;; Death
reported ;; Reporting
blamed ;; Judgment_communication || deployment ;; Arranging
visited ;; Traveling || violence ;; Violence || called ;; Statement
visited ;; Traveling || taken ;; Action || review ;; Testing || relief ;; Assistance || rehabilitation ;; Cure
blamed ;; Judgment_communication || violence ;; Violence || illegal ;; Legality
Election ;; Choosing
published ;; Publishing || said ;; Statement || shown ;; Statement
criticised ;; Judgment_communication || illegal ;; Legality || dealing ;; Arranging
asserted ;; Statement || illegal ;; Legality
violence ;; Violence || led to ;; Causation || protests ;; Attack
demand ;; Request || detection ;; Know || deportation ;; Removing || illegal ;; Legality
decided ;; Deciding || form ;; Creating
said ;; Statement || illegal ;; Legality || existence ;; Presence || limited ;; Limiting || threatened ;; Hindering
acceded ;; Agree_or_refuse_to_act || ordered ;; Request
succeeded ;; Change_of_leadership || died ;; Death
supported ;; Supporting || rejected ;; Response || conquest ;; Conquering
invasions ;; Attack || resulted in ;; Causation || battles ;; Hostile_encounter || ordered ;; Request
sent ;; Sending
test ;; Testing || struggle ;; Hostile_encounter || campaign ;; Military_operation
became ;; Becoming || suffering ;; Bodily_harm
growing ;; Expansion || tried ;; Change
Realizing ;; Coming_to_believe || invasion ;; Attack || ordered ;; Request
knowing ;; Know || defeated ;; Conquering
attacked ;; Attack || defeated ;; Conquering || split ;; Destroying
killed ;; Killing || captured ;; Conquering || ransomed ;; Commerce_pay || battle ;; Hostile_encounter
easing ;; Cure || cajoled ;; Convincing
damage ;; Damaging || Cyclone ;; Catastrophe || storm ;; Catastrophe || cyclone ;; Catastrophe
formed ;; Coming_to_be
organized ;; Arranging || Moving ;; Motion || developed ;; Cause_change_of_position_on_a_scale
cyclone ;; Catastrophe || struck ;; Attack
estimated ;; Testing || winds ;; Self_motion || winds ;; Self_motion || joint ;; Forming_relationships
cyclone ;; Catastrophe || landfall ;; Motion || weakened ;; Cause_change_of_strength || dissipated ;; Change
evacuated ;; Emptying || storm ;; Catastrophe || storm ;; Catastrophe
cyclone ;; Catastrophe || produced ;; Creating || winds ;; Self_motion || made ;; Causation || landfall ;; Arriving || flooded ;; Damaging
destroyed ;; Destroying || damage ;; Damaging
destroyed ;; Destroying || killed ;; Killing || storm ;; Catastrophe
destroyed ;; Destroying || storm ;; Catastrophe || damaged ;; Damaging
damage ;; Damaging || cyclone ;; Catastrophe
deaths ;; Death || washed ;; Emptying || dead ;; Death || found ;; Know
helped ;; Assistance || provided ;; Supply || damage ;; Damaging || recover ;; Assistance || storm ;; Catastrophe || storms ;; Catastrophe
died ;; Death
departed ;; Departing
lost ;; Earnings_and_losses || launched ;; GetReady
recovered ;; Recovering
pinpoint ;; Placing
examined ;; Testing || believed ;; Know
believed ;; Know
formed ;; Coming_to_be
provoked ;; Causation
collision ;; Catastrophe || occurred ;; Coming_to_be
crash ;; Catastrophe
fouled ;; Motion || crashed ;; Motion
collision ;; Catastrophe
killed ;; Killing
determined ;; Deciding || avoided ;; Preventing_or_letting || stop ;; Motion
tested ;; Testing
served ;; Assistance
overhauled ;; Reforming_a_system
prompted ;; Causation || regulated ;; Limiting
equipped ;; Supply
identified with ;; Check || issues ;; Creating
injured ;; Bodily_harm || killed ;; Killing || surpassed ;; Cause_change_of_position_on_a_scale
Hurricane ;; Catastrophe || hurricane ;; Catastrophe || Hurricane ;; Catastrophe || strike ;; Attack
hurricane ;; Catastrophe
recorded ;; Recording || hurricane ;; Catastrophe || struck ;; Attack
named ;; Name_conferral || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || destruction ;; Destroying || hurricane ;; Catastrophe || wrought ;; Bringing
formed ;; Coming_to_be || storm ;; Catastrophe
made ;; Manufacturing || hurricane ;; Catastrophe || causing ;; Causation || damage ;; Damaging
damaged ;; Damaging || destroyed ;; Destroying || losses ;; Earnings_and_losses
storm ;; Catastrophe || killed ;; Killing
hurricane ;; Catastrophe || intensified ;; Cause_change_of_strength || tracked ;; Recording
led to ;; Causation || peaked ;; Arriving
Hurricane ;; Catastrophe || caused ;; Causation || forced ;; Request
storm ;; Catastrophe || assistance ;; Assistance
crossing ;; Motion_directional || strength ;; Cause_change_of_strength || weakened ;; Cause_change_of_strength
creating ;; Creating || widespread ;; Dispersal || damage ;; Damaging || downing ;; Motion || battered ;; Attack
restored ;; Recovering || storm ;; Catastrophe || service ;; Supply
hurricane ;; Catastrophe || damage ;; Damaging || impassable ;; Having_or_lacking_access
took part ;; Dispersal || storm ;; Catastrophe || revealed ;; Reveal_secret || assessments ;; Testing || cleanup ;; Emptying
injuries ;; Bodily_harm || suffered ;; Bodily_harm || losses ;; Earnings_and_losses || deaths ;; Death || reported ;; Reporting
Departing ;; Departing || passing ;; Motion_directional || becoming ;; Becoming || accelerated ;; Cause_change_of_position_on_a_scale
flooding ;; Motion
involving ;; Cause_to_be_included || killing ;; Killing || storm ;; Catastrophe || battered ;; Bodily_harm || hindering ;; Hindering
triggering ;; Causation || resulted in ;; Causation || flooding ;; Catastrophe
toured ;; Traveling || play ;; Participation || test ;; Testing
matches ;; Competition || retained ;; Preserving
achieved ;; Achieve || test ;; Testing || record ;; Recording || test ;; Testing
achieved ;; Achieve || test ;; Testing || test ;; Testing || match ;; Competition || match ;; Competition || bowling ;; Social_event || took ;; Conquering
dazed ;; Communication || summed up ;; Statement || skittled ;; Attack
forgotten ;; GiveUp || signed ;; Sign_agreement
complaints ;; Judgment_communication || help ;; Assistance || shaved ;; Removing
known ;; Know || impact ;; Influence || cyclone ;; Catastrophe
developed ;; Coming_to_be || Developing ;; Coming_to_be || tracked ;; Self_motion
intensify ;; Cause_change_of_strength || allowed ;; Preventing_or_letting
storm ;; Catastrophe || assessed ;; Testing
struck ;; Attack || weakening ;; Cause_change_of_strength
crossing ;; Motion_directional || entered ;; Arriving
attained ;; Getting
weakened ;; Cause_change_of_strength || brushing ;; Damaging
dissipated ;; Dispersal || landfall ;; Self_motion
produced ;; Manufacturing || triggered ;; Causation || flooding ;; Motion
killed ;; Killing || known ;; Know
placed ;; Placing || damage ;; Damaging
killed ;; Killing
damage ;; Damaging || damage ;; Damaging
killed ;; Killing || reported ;; Reporting
event ;; Competition || served ;; Assistance
participated ;; Participation || competition ;; Competition
competition ;; Competition || competition ;; Competition || served ;; Assistance || qualifications ;; Testing
played ;; Competition || took place ;; Process_start
playing ;; Competition
played ;; Competition
became ;; Becoming
matches ;; Competition
participate ;; Participation
Olympics ;; Competition || Olympic ;; Competition || took place ;; Process_start
contested ;; Competition || open ;; Openness || event ;; Competition
end ;; Process_end || scored ;; Getting
announced ;; Expressing_publicly || tested ;; Testing || excluded ;; Preventing_or_letting || Games ;; Competition || opening ;; Process_start || event ;; Competition
become ;; Becoming
allows ;; Preventing_or_letting || provides ;; Supply || compete ;; Competition || Olympics ;; Competition || turn ;; Becoming
tournament ;; Competition || Olympic ;; Competition || retirement ;; Departing || beating ;; Getting
demonstration ;; Social_event || limitations ;; Limiting || autonomy ;; Change_of_leadership || decision ;; Deciding || annul ;; Destroying
demonstration ;; Social_event || estimated ;; Testing
described ;; Statement
described ;; Statement || marches ;; Self_motion
involved ;; Participation || brought ;; Causation || demonstration ;; Social_event || dispute ;; Quarreling
demonstration ;; Social_event
decide ;; Deciding
conducted ;; Action || test ;; Testing
respond ;; Response || test ;; Testing || test ;; Testing || testing ;; Testing
conducted ;; Action || shots ;; Use_firearm || bombs ;; Attack || dropped ;; Motion_directional
confirm ;; Check || test ;; Testing || test ;; Testing || existing ;; Presence || shots ;; Use_firearm || effects ;; Influence || designs ;; Creating
used ;; Using || conduct ;; Action || called ;; Name_conferral || tests ;; Testing || lift ;; Body_movement || shots ;; Use_firearm
occurred ;; Coming_to_be || occurred ;; Coming_to_be || Invasion ;; Attack
announced ;; Expressing_publicly || included ;; Cause_to_be_included || testing ;; Testing || tests ;; Testing || end ;; Process_end || initiating ;; Process_start || recommenced ;; Process_start
responded ;; Response || authorizing ;; Preventing_or_letting
conducted ;; Action || conducted ;; Action || test ;; Testing || testing ;; Testing || limited ;; Limiting || signed ;; Sign_agreement
operation ;; Military_operation || joint ;; Collaboration
event ;; Social_event || took place ;; Hold
records ;; Recording || set ;; Placing
became ;; Becoming || win ;; Getting
medals ;; Achieve
filed ;; Submitting_documents
ruled ;; Control || rejected ;; Agree_or_refuse_to_act
produced ;; Manufacturing
announced ;; Expressing_publicly
coming ;; Motion
shutting down ;; Being_in_operation || informed ;; Telling
filed ;; Submitting_documents || canceled ;; Change_event_time
forcing ;; Hostile_encounter || resulted in ;; Causation
replaced ;; Change_of_leadership || purchasing ;; Commerce_buy || put on ;; Process_start
operated ;; Being_in_operation || crashed ;; Catastrophe
died ;; Death || accident ;; Catastrophe || making it ;; Causation
investigation ;; Criminal_investigation || disaster ;; Catastrophe || launched into ;; Process_start
lost ;; Earnings_and_losses || disconnected ;; Removing || became ;; Becoming || report ;; Criminal_investigation || released ;; Publishing || concluded ;; Coming_to_believe || troubleshooting ;; Resolve_problem
fatal ;; Killing || accident ;; Catastrophe
downgrading ;; Cause_change_of_position_on_a_scale || crash ;; Catastrophe || crash ;; Catastrophe || accidents ;; Catastrophe || resulted in ;; Causation || reforms ;; Change
banned ;; Preventing_or_letting || crash ;; Catastrophe || flying ;; Motion
banned ;; Preventing_or_letting || declared ;; Expressing_publicly || bankruptcy ;; Submitting_documents || flying ;; Motion
reconquer ;; Conquering || capture ;; Conquering
recapturing ;; Conquering || recapture ;; Conquering || reversing ;; Change
controlled ;; Control || engaged ;; Hostile_encounter || conflict ;; Hostile_encounter
employed ;; Employment || recapture ;; Conquering || reduce ;; Cause_change_of_position_on_a_scale || brought ;; Bringing
ended ;; Process_end || Spurred ;; Influence
meant ;; Causation || came ;; Motion || death ;; Death
responded ;; Response || reaching ;; Arriving || drowned ;; Death
caused ;; Causation || returned ;; Motion
driven ;; Influence
granted ;; Preventing_or_letting || allowed ;; Preventing_or_letting || visit ;; Traveling || finalized ;; Submitting_documents
departed ;; Departing
allowed ;; Preventing_or_letting || maintain ;; Preserving
inspired ;; Influence || regain ;; Recovering
tour ;; Traveling
support ;; Supporting
consisted ;; Cause_to_be_included
sale ;; Commerce_sell || sold ;; Commerce_sell
began ;; Process_start || finished ;; Process_end
collaborated ;; Collaboration || asking ;; Request || fought ;; Hostile_encounter || drives ;; Self_motion
began ;; Process_start || release ;; Publishing || held ;; Defending || called ;; Name_conferral || chosen ;; Choosing || performed ;; Hold
included ;; Cause_to_be_included || recorded ;; Recording || shortened ;; Change_event_time
collaborated ;; Collaboration || received ;; Receiving || consisted ;; Forming_relationships
received ;; Receiving || praised ;; Judgment_communication || performed ;; Hold
highlighted ;; Expressing_publicly
submitted ;; Submitting_documents || attending ;; Participation
injured ;; Bodily_harm
relocated ;; Removing || surrounding ;; Surrounding
released ;; Publishing || recorded ;; Recording
terrorist ;; Terrorism || attacks ;; Attack || attacks ;; Attack || occurred ;; Presence
killing ;; Killing || infecting ;; Bodily_harm || mailed ;; Sending
opened ;; Openness
became ;; Becoming
exonerated ;; Rewards_and_punishments
became ;; Becoming
stated ;; Statement || attacks ;; Judgment_communication || put ;; Placing
committed ;; Killing || suicide ;; Killing
declared ;; Expressing_publicly || leading to ;; Causation
investigation ;; Criminal_investigation || called ;; Request
investigation ;; Criminal_investigation || closed ;; Process_end
requested ;; Request || released ;; Publishing || investigation ;; Criminal_investigation || Investigation ;; Criminal_investigation
finding ;; Know || identified ;; Check || originated ;; Coming_to_be
asserted ;; Statement || said ;; Statement || reach ;; Achieve || conclude ;; Statement
filed ;; Submitting_documents || settled ;; Resolve_problem || lawsuits ;; Legality
statement ;; Statement || reached ;; Achieve || settlement ;; Resolve_problem || avoiding ;; Preventing_or_letting
operation ;; Military_operation || known ;; Know
send ;; Sending || evacuating ;; Removing || drew ;; Motion
commanded ;; Control
rescue ;; Rescuing || designated ;; Choosing
operation ;; Military_operation
submitted ;; Submitting_documents || rescued ;; Rescuing
produced ;; Manufacturing
took place ;; Process_start
hitting ;; Attack
test ;; Research || match ;; Competition
submit ;; Submitting_documents
match ;; Competition || match ;; Competition
match ;; Competition || following ;; Self_motion
marked ;; Recording || created ;; Creating
opened ;; Openness
decisions ;; Deciding || proposals ;; Statement || made ;; Creating || resubmit ;; Submitting_documents || conference ;; Social_event
rejected ;; Agree_or_refuse_to_act
proposed ;; Statement
proposed ;; Statement
rejecting ;; Agree_or_refuse_to_act
agreed ;; Agree_or_refuse_to_act
implemented ;; Action || conference ;; Social_event
challenge ;; Competition
chooses ;; Choosing || challenge ;; Competition || holds ;; Hold || take place ;; Process_start
bid ;; Competition || competitive ;; Competition || winners ;; Competition || put ;; Placing
eliminated ;; Removing || selection ;; Choosing
finalists ;; Competition
announced ;; Expressing_publicly
built ;; Building || house ;; Containing || designed ;; Creating
changed ;; Exchange || seemed ;; Change_sentiment || win ;; Getting || become ;; Becoming || winning ;; Conquering
winning ;; Getting || capture ;; Conquering
sweeps ;; Motion
submitted ;; Submitting_documents
selection ;; Choosing
culminated ;; Come_together || leading ;; Causation
became ;; Becoming || raced ;; Competition || Cup ;; Competition
held ;; Hold || held ;; Hold || held ;; Hold
representing ;; Presence || Cup ;; Competition || selection ;; Choosing
declared ;; Expressing_publicly || awarded ;; Award
included ;; Cause_to_be_included
results ;; Causation
run ;; Action
entering ;; Process_start || starting ;; Process_start || results ;; Causation
decided ;; Deciding
alternates ;; Exchange
shot ;; Use_firearm
shooting ;; Use_firearm || mixed ;; Cause_to_amalgamate || visiting ;; Traveling
shot ;; Use_firearm
injured ;; Bodily_harm || claimed ;; Statement
murder ;; Committing_crime || reported ;; Reporting || charged ;; Judgment_communication || claiming ;; Statement
filed ;; Submitting_documents || reviewed ;; Scrutiny || citing ;; Adducing
concert ;; Social_event || recording ;; Recording || Tour ;; Traveling || tour ;; Traveling
held ;; Hold || promote ;; Cause_to_make_progress || tour ;; Traveling || consisted ;; Cause_to_be_included
initiated ;; Process_start || concluded ;; Process_end || visiting ;; Traveling
promoted ;; Cause_to_make_progress || Concerts ;; Social_event || tour ;; Traveling
served ;; Assistance || act ;; Action || tour ;; Traveling || opening ;; Process_start
donated ;; Giving || sold ;; Commerce_sell || devoted ;; Giving || fight ;; Defending
Tour ;; Traveling || raised ;; Cause_change_of_position_on_a_scale
created ;; Creating || Tour ;; Traveling
act ;; Action || opening ;; Process_start || performing ;; Hold || performed ;; Action
used ;; Using || facilitate ;; Cause_to_make_progress || transition ;; Change
caused ;; Causation || become ;; Becoming || tour ;; Traveling || question ;; Communication
statement ;; Statement || stated ;; Statement || issued ;; Creating || denied ;; Statement || allegations ;; Judgment_communication || usage ;; Using
concert ;; Social_event
received ;; Receiving || responses ;; Response || preferred ;; Choosing || complimented ;; Judgment_communication || presence ;; Presence
grossing ;; Earnings_and_losses || tour ;; Traveling
awards ;; Judgment_communication || award ;; Judgment_communication
sold ;; Commerce_sell || found ;; Know
filed ;; Submitting_documents || became ;; Becoming || garnered ;; Getting || lawsuits ;; Legality
released ;; Publishing
received ;; Receiving || records ;; Recording || response ;; Response || grossing ;; Earnings_and_losses || concert ;; Social_event || setting ;; Placing
response ;; Response
Concert ;; Social_event || watched ;; Perception_active || televised ;; Communication
Concert ;; Social_event || recordings ;; Recording || concerts ;; Social_event || released ;; Publishing || released ;; Publishing
embarked ;; Self_motion
contained ;; Containing || limited ;; Limiting || included ;; Cause_to_be_included
cover ;; Hiding_objects || allowed ;; Preventing_or_letting || ran ;; Self_motion
drawing ;; Create_artwork || depicted ;; Statement
placed ;; Placing
create ;; Creating || submit ;; Submitting_documents
entered ;; Arriving
included ;; Cause_to_be_included || submitted ;; Submitting_documents
attack ;; Attack || attacked ;; Attack || carried out ;; Action
shot ;; Use_firearm
shot ;; Use_firearm || opening ;; Openness || killed ;; Killing
monitoring ;; Scrutiny || fired ;; Attack
filed ;; Submitting_documents || claiming ;; Statement || lawsuit ;; Legality
claimed ;; Statement || took ;; Conquering
verified ;; Check || inspired ;; Convincing || directed ;; Aiming || stated ;; Statement || appears ;; Change_sentiment
urged ;; Request || posted ;; Sending || attack ;; Attack
claimed ;; Statement
siege ;; Besieging || fought ;; Hostile_encounter
ended ;; Process_end || resulted in ;; Causation
began ;; Process_start || campaign ;; Military_operation || brought ;; Bringing
submitted ;; Submitting_documents || defected ;; Escaping
control ;; Control
renamed ;; Name_conferral || repaired ;; Recovering
functioned ;; Being_in_operation || moved ;; Motion
played ;; Action
negotiated ;; Communication || built ;; Building || ensured ;; Coming_to_believe
devised ;; Creating
rose ;; Cause_to_make_progress
renamed ;; Name_conferral || occurred ;; Presence || Battle ;; Hostile_encounter || engagements ;; Hostile_encounter
battle ;; Hostile_encounter || starting ;; Process_start || blockaded ;; Besieging || took place ;; Process_start
began ;; Process_start || equipped ;; Supply || supplies ;; Supply || objective ;; Aiming || neutralise ;; Creating || secure ;; Getting
revolted ;; Change_of_leadership || Battle ;; Hostile_encounter || escalation ;; Preventing_or_letting
achieving ;; Achieve || independence ;; Change_of_leadership
Battle ;; Hostile_encounter || preceded ;; Process_start || amended ;; Change || blockaded ;; Besieging || operation ;; Military_operation
relieved ;; Cure || curbed ;; Hold
captured ;; Conquering
provided ;; Supply || move ;; Motion || forces ;; Causation || supply ;; Supply || defending ;; Defending || proved ;; Convincing
surrendered ;; Surrendering || armed ;; Bearing_arms || fighting ;; Hostile_encounter || put up ;; Military_operation
caused ;; Causation || situated ;; Placing || casualties ;; Catastrophe
captured ;; Conquering || filed ;; Submitting_documents || killing ;; Killing || remain ;; Temporary_stay || charges ;; Judgment_communication || charges ;; Judgment_communication
blockade ;; Besieging || reached ;; Achieve || withdraw ;; Escaping
completed ;; Process_end
maintained ;; Statement
agreed ;; Agree_or_refuse_to_act || controlled ;; Control || plan ;; Arranging || withdrew ;; Removing || replaced ;; Change_of_leadership
found ;; Know || occurred ;; Coming_to_be || dead ;; Death
arrested ;; Arrest
released ;; Releasing
murder ;; Killing || charged ;; Judgment_communication || indicted ;; Criminal_investigation
filed ;; Submitting_documents || dispute ;; Quarreling || lawsuit ;; Legality
began ;; Process_start
found ;; Know || sentenced ;; Legal_rulings
explosion ;; Catastrophe || disaster ;; Catastrophe || occurred ;; Coming_to_be
killed ;; Killing || injured ;; Bodily_harm || occurred ;; Coming_to_be
reduce ;; Cause_change_of_position_on_a_scale || made ;; Manufacturing
contributed ;; Giving
explosion ;; Catastrophe || narrowed ;; Expansion
believed ;; Know || occurred ;; Coming_to_be
conducted ;; Action || ruled out ;; Removing
disaster ;; Catastrophe
declined ;; Cause_change_of_position_on_a_scale || closed down ;; Openness
completed ;; Process_end || return ;; Arriving || rebuild ;; Recovering
hired ;; Employment || damages ;; Damaging
explosion ;; Catastrophe || said ;; Statement || loss ;; Earnings_and_losses
saying ;; Statement || incident ;; Catastrophe || explosion ;; Catastrophe || released ;; Publishing
concluded ;; Coming_to_believe || explosion ;; Catastrophe || prevented ;; Preventing_or_letting
filed ;; Submitting_documents
settled ;; Resolve_problem
attack ;; Attack || attack ;; Attack
attacked ;; Attack || compound ;; Cause_to_amalgamate || deaths ;; Death || resulting in ;; Causation
killed ;; Killing
killing ;; Killing || attack ;; Attack || wounding ;; Bodily_harm || launched ;; Process_start
arose ;; Coming_to_be || attack ;; Attack || analysis ;; Causation || indicated ;; Statement
began ;; Process_start || attack ;; Attack || attacks ;; Attack || showed ;; Statement || investigations ;; Scrutiny || joined ;; Participation
participated ;; Participation || attack ;; Attack
began ;; Process_start || increased ;; Cause_change_of_position_on_a_scale || attack ;; Attack || investigating ;; Scrutiny
condemned ;; Judgment_communication || attacks ;; Attack
formed ;; Coming_to_be || staged ;; Hold || war ;; Hostile_encounter || condemning ;; Judgment_communication
find ;; Know || acted ;; Action || investigations ;; Scrutiny
criticized ;; Judgment_communication || requests ;; Request || attack ;; Attack
pressure ;; Influence || resigned ;; Change_of_leadership || suspended ;; Preventing_or_letting
took ;; Action
filed ;; Submitting_documents || involved ;; Cause_to_be_included || attacks ;; Attack || charges ;; Judgment_communication || reported ;; Reporting || alleged ;; Judgment_communication
described ;; Statement
designated ;; Deciding
captured ;; Arrest || acting ;; Action
captured ;; Arrest
held ;; Hold
join ;; Becoming_a_member || enrolled ;; Becoming_a_member
performed ;; Hold || performed ;; Hold || held ;; Hold || closed ;; Process_end
took place ;; Process_start
ended ;; Process_end || submitted ;; Submitting_documents
ended ;; Process_end
throw ;; Motion || broke ;; Bodily_harm
called ;; Name_conferral
sponsored ;; Participation
ended ;; Process_end || tournaments ;; Competition || tournaments ;; Competition || saw ;; Communication
competed ;; Competition || final ;; Competition
named ;; Name_conferral
tournament ;; Competition || held ;; Hold
divided ;; Creating
qualified ;; Getting || semi-finals ;; Process_end
played ;; Competition || semi-final ;; Process_end || faced ;; Hostile_encounter
played ;; Competition || matches ;; Competition || tournament ;; Competition || Final ;; Process_end
mean ;; Protest || success ;; Achieve || tournament ;; Competition
conducted ;; Action || tournament ;; Competition
recorded ;; Recording || occurred ;; Presence
submitted ;; Submitting_documents || named ;; Name_conferral || named ;; Name_conferral
investigated ;; Research || tournament ;; Competition || success ;; Achieve || announced ;; Expressing_publicly || match ;; Competition || allegations ;; Statement || match-fixing ;; Hostile_encounter
match ;; Competition || investigation ;; Scrutiny || announced ;; Expressing_publicly
supported ;; Supporting || established ;; Building
submit ;; Submitting_documents || selected ;; Choosing
bringing ;; Bringing || kicked off ;; Process_start
runs ;; Action || Modified ;; Change
attracted ;; Influence
shooting ;; Use_firearm || took place ;; Process_start
injured ;; Bodily_harm || shot ;; Use_firearm
shooting ;; Use_firearm
shot ;; Use_firearm
arraigned ;; Arrest || charged ;; Judgment_communication
began ;; Process_start
faced ;; Confronting_problem
found ;; Know || sentenced ;; Legal_rulings
revealed ;; Reveal_secret || monitored ;; Scrutiny || aware ;; Know || aware ;; Know
investigations ;; Scrutiny || hold ;; Defending || led ;; Causation
declined ;; Agree_or_refuse_to_act || motivated ;; Causation
filed ;; Submitting_documents || lawsuit ;; Legality
argued ;; Protest || harm ;; Bodily_harm
killed ;; Killing || injured ;; Bodily_harm || killing ;; Killing
began ;; Process_start || attack ;; Attack || death ;; Death || arrival ;; Arriving || stabbed ;; Killing
drove ;; Motion || failed ;; Earnings_and_losses || gain ;; Getting
died ;; Death || shot ;; Use_firearm
drove ;; Motion || death ;; Death || shot ;; Use_firearm
began ;; Process_start || speed ;; Cause_change_of_position_on_a_scale || wounding ;; Bodily_harm || striking ;; Attack || shooting ;; Use_firearm
receiving ;; Receiving || gunfire ;; Use_firearm || attack ;; Attack || gunshot ;; Use_firearm || exchanged ;; Exchange
ended ;; Process_end || stop ;; Process_end || rampage ;; Hostile_encounter || crashed ;; Attack
dead ;; Death || gunshot ;; Use_firearm || wound ;; Bodily_harm || found ;; Know
attack ;; Attack || driving ;; Motion || titled ;; Name_conferral || Retribution ;; Revenge || outlined ;; Reveal_secret || uploaded ;; Submitting_documents
punish ;; Rewards_and_punishments || punish ;; Rewards_and_punishments || explained ;; Statement || rejecting ;; Agree_or_refuse_to_act || envied ;; Judgment_communication
e-mailed ;; Sending
became ;; Becoming || titled ;; Name_conferral
described ;; Statement || described ;; Statement || retribution ;; Revenge
Operation ;; Military_operation || opening ;; Openness || directed ;; Aiming
conducted ;; Action || shootings ;; Use_firearm || use ;; Using || invasion ;; Attack || compiled ;; Submitting_documents
identified ;; Check || interned ;; Arrest || shot ;; Use_firearm
assisted ;; Assistance || preparing ;; GetReady
involved ;; Cause_to_be_included
killed ;; Killing || end ;; Process_end
killing ;; Killing || bombing ;; Attack || injuring ;; Bodily_harm
terrorist ;; Terrorism || bombing ;; Attack
marked ;; Recording
found ;; Coming_to_believe
impeached ;; Judgment_communication || charge ;; Imposing_obligation || removed ;; Removing
petition ;; Request || become ;; Becoming || bombing ;; Attack || sign ;; Sign_agreement
signatories ;; Sign_agreement || signatures ;; Writing || bombing ;; Attack
accused ;; Judgment_communication || directing ;; Action || carrying ;; Creating || bombing ;; Attack
decision ;; Deciding || targeted ;; Aiming || suspend ;; Change
disputed ;; Quarreling || negotiating ;; Communication || cooperation ;; Collaboration || occurred ;; Coming_to_be
filed ;; Submitting_documents || accusing ;; Judgment_communication || covering ;; Hiding_objects
murdered ;; Killing || consequence ;; Causation || testify ;; Protest || ruled ;; Deciding
accused ;; Judgment_communication || permit ;; Preventing_or_letting || covering ;; Hiding_objects || called ;; Request
bombing ;; Attack
bombing ;; Attack || commemorated ;; Social_event
sounded ;; Change_sentiment || bombing ;; Attack
declared ;; Expressing_publicly || terrorist ;; Terrorism
known ;; Know || considered ;; Agree_or_refuse_to_act
recognized ;; Know
submitted ;; Submitting_documents
brought ;; Bringing
reported ;; Reporting || occurred ;; Coming_to_be || landfall ;; Self_motion || affected ;; Influence
discovered ;; Know
investigation ;; Criminal_investigation || death ;; Death || autopsy ;; Check || concluded ;; Coming_to_believe
conduct ;; Action || died ;; Death || autopsy ;; Check || trauma ;; Bodily_harm || concluded ;; Coming_to_believe
announced ;; Expressing_publicly || open ;; Process_start || review ;; Criminal_investigation
filing ;; Submitting_documents || announced ;; Expressing_publicly || charges ;; Judgment_communication
filed ;; Submitting_documents || lawsuit ;; Legal_rulings
alleged ;; Statement || accused ;; Judgment_communication || death ;; Death || murder ;; Killing || homicide ;; Killing || cover ;; Hiding_objects || conspiracy ;; Committing_crime || lawsuit ;; Legal_rulings
lawsuit ;; Legal_rulings || withdrawn ;; Removing
pay ;; Commerce_pay || ordered ;; Arranging
accused ;; Judgment_communication || support ;; Supporting || claims ;; Statement
rose ;; Body_movement || suppressed ;; Hindering
sentenced ;; Legal_rulings
suppressed ;; Hindering
began ;; Process_start || sold ;; Commerce_sell
attacked ;; Attack
held ;; Hold
killed ;; Killing || provoked ;; Causation || inflamed ;; Causation
petitioned ;; Submitting_documents
allowed ;; Preventing_or_letting || select ;; Choosing
abducted ;; Kidnapping || took ;; Conquering
strengthened ;; Cause_change_of_strength || died ;; Death
forced ;; Request
laid ;; Placing
set up ;; Reforming_a_system
described ;; Statement
reached ;; Achieve
said ;; Telling
called ;; Request || rally ;; Social_event || rally ;; Social_event || protest ;; Protest
organised ;; Arranging || follow-up ;; Motion || rally ;; Social_event || rally ;; Social_event || rally ;; Social_event
organised ;; Arranging || supported ;; Supporting || rally ;; Social_event || coalition ;; Collaboration || elections ;; Choosing
endorsed ;; Agree_or_refuse_to_act
joined ;; Becoming_a_member || protesting ;; Preventing_or_letting || project ;; Creating
held ;; Defending || rally ;; Social_event || rallies ;; Social_event
reforms ;; Reforming_a_system || organised ;; Arranging || released ;; Publishing || proposals ;; Statement
submitted ;; Submitting_documents || demands ;; Request || included ;; Cause_to_be_included || recommendations ;; Convincing
implemented ;; Action || demand ;; Request || included ;; Cause_to_be_included || Committee ;; Committing_crime || campaign ;; Motion || said ;; Statement
campaign ;; Social_event || considered ;; Deciding || extended ;; Expansion
accused ;; Judgment_communication || reforms ;; Reforming_a_system || claimed ;; Statement || proposals ;; Statement || election ;; Choosing
gave ;; Giving || reforms ;; Reforming_a_system || stated ;; Statement || take place ;; Process_start || rally ;; Social_event || call ;; Process_end || guarantee ;; Commitment || elections ;; Change_of_leadership
sent ;; Sending || Operation ;; Military_operation || supply ;; Supply
cost ;; Cost || completed ;; Process_end
attacks ;; Attack || driven ;; Motion
submitting ;; Submitting_documents || revealed ;; Reveal_secret
broken ;; Bodily_harm || revealed ;; Reveal_secret
armed ;; Bearing_arms || social ;; Social_event || sought ;; Getting || conflict ;; Hostile_encounter || conflict ;; Hostile_encounter || conflict ;; Hostile_encounter
attacks ;; Attack || launched ;; Bringing || built ;; Creating
terrorist ;; Terrorism || proscribed ;; Name_conferral
used ;; Using || conflict ;; Hostile_encounter || present ;; Presence || took place ;; Process_start
conflict ;; Violence
referred ;; Name_conferral || war ;; Military_operation
terminology ;; Statement
preferred ;; Choosing || violence ;; Violence || opposed ;; Preventing_or_letting || conflict ;; Violence
draft ;; Writing || legitimate ;; Legality || terrorist ;; Terrorism || seeing ;; Motion || reject ;; Agree_or_refuse_to_act || fighting ;; Hostile_encounter
conflict ;; Hostile_encounter
participants ;; Participation || violence ;; Violence || included ;; Cause_to_be_included || forces ;; Military_operation || involved ;; Participation
fighting ;; Hostile_encounter
started ;; Process_start || armed ;; Bearing_arms || start ;; Process_start || debate ;; Quarreling || created ;; Coming_to_be
armed ;; Bearing_arms || resulted in ;; Causation || forces ;; Military_operation || conflict ;; Violence || deaths ;; Death
violence ;; Violence || kidnapped ;; Kidnapping || going ;; Motion || flee ;; Escaping || avoid ;; Preventing_or_letting || injured ;; Bodily_harm || exile ;; Escaping
announced ;; Expressing_publicly || armed ;; Bearing_arms || cessation ;; Process_end
move ;; Motion || described ;; Statement
took place ;; Process_start
led to ;; Causation
opened ;; Openness
fought ;; Hostile_encounter || changed ;; Change
died ;; Death
signed ;; Writing || died ;; Death
bombarded ;; Attack || abolished ;; Removing
led to ;; Causation
marched ;; Motion_directional || re-established ;; Creating || forced ;; Request
refers ;; Adducing
amended ;; Change_event_time
end ;; Process_end
describing ;; Statement
comprises ;; Cause_to_be_included
goal ;; Aiming || fighting ;; Defending
added ;; Cause_to_be_included
called ;; Name_conferral || made ;; Manufacturing || says ;; Communication || appearance ;; Coming_to_be
blame ;; Judgment_communication || growth ;; Expansion || destroying ;; Destroying || say ;; Communication || constitutes ;; Forming_relationships || argue ;; Quarreling
written in ;; Writing
using ;; Using || include ;; Cause_to_be_included
control ;; Control || devoted ;; Giving || gained ;; Getting || party ;; Social_event || starting ;; Process_start || writing ;; Writing || elected ;; Choosing
festival ;; Social_event || held ;; Hold
occurred ;; Coming_to_be
expanded ;; Cause_change_of_position_on_a_scale
organised ;; Arranging
held ;; Hold
held ;; Hold
arrests ;; Arrest || made ;; Creating
held ;; Hold
drugs ;; Bodily_harm || arrested ;; Arrest
contested ;; Competition
scored ;; Getting
postponed ;; Change_event_time
moved ;; Motion
employed ;; Employment || adjusted ;; Control
scored ;; Getting
made ;; Creating || scored ;; Getting
seen ;; Change_sentiment
formed ;; Coming_to_be || lifted ;; Process_end
formed ;; Coming_to_be
killing ;; Killing
murdered ;; Committing_crime || destroyed ;; Destroying
organized ;; Arranging || massacre ;; Killing
approved ;; Agree_or_refuse_to_act || control ;; Control || established ;; Building || conference ;; Social_event
opposed ;; Agree_or_refuse_to_act || known ;; Know || views ;; Perception_active || decision ;; Deciding || established ;; Building
relief ;; Assistance || working ;; Being_in_operation || cited ;; Adducing || occupied ;; Control
organized ;; Arranging || clash ;; Hostile_encounter || takes place ;; Process_start
blockaded ;; Besieging || found ;; Know
arrived ;; Arriving || occupied ;; Attack
festival ;; Social_event || festival ;; Social_event
festival ;; Social_event || held ;; Hold
festival ;; Social_event || involves ;; Cause_to_be_included || limited ;; Limiting || focuses ;; Aiming
given ;; Giving || festival ;; Social_event || performances ;; Achieve || performed ;; Action
concerts ;; Social_event || played ;; Action
festival ;; Social_event || played ;; Action
campaign ;; Military_operation || War ;; Military_operation || Battle ;; Military_operation
campaign ;; Military_operation
forces ;; Causation || fought ;; Hostile_encounter || battle ;; Military_operation
attack ;; Attack || launched ;; Process_start
withdraw ;; Escaping || forces ;; Causation || developments ;; Cause_to_make_progress || campaign ;; Military_operation || approaching ;; Arriving || secure ;; Preventing_or_letting || ordered ;; Request
conducted ;; Action || forces ;; Causation || maintained ;; Preserving
forces ;; Causation || positions ;; Placing || eject ;; Removing || deny ;; Preventing_or_letting
forces ;; Causation || prepared ;; GetReady || defence ;; Defending
developed ;; Coming_to_be
hampered ;; Hindering || Operations ;; Military_operation || lack ;; Confronting_problem || imposed ;; Limiting
attacks ;; Attack || positions ;; Placing || campaign ;; Military_operation || favoured ;; Supporting || applied ;; Using
limited ;; Limiting || battlefield ;; Hostile_encounter
opening ;; Openness || offensive ;; Attack || shortage ;; Cause_change_of_position_on_a_scale || faced ;; Confronting_problem
resolved ;; Resolve_problem || problem ;; Confronting_problem
exposed ;; Reveal_secret || equipment ;; Supply || battle ;; Military_operation || problems ;; Confronting_problem
criticised ;; Judgment_communication || forces ;; Causation
compounded ;; Cause_to_amalgamate || demands ;; Request || conclusion ;; Process_end
command ;; Control || demands ;; Request || secure ;; Preventing_or_letting
committed ;; Commitment || battle ;; Hostile_encounter || battle ;; Military_operation || increasing ;; Cause_change_of_position_on_a_scale || losses ;; Earnings_and_losses || lengthening ;; Change_event_time
interrupted ;; Hindering || reinforce ;; Cause_change_of_strength || resupply ;; Supply
made ;; Causation
dead ;; Death
withdrawn ;; Escaping || escaped ;; Escaping || closing ;; Body_movement
death ;; Death || fought ;; Hostile_encounter
resolve ;; Resolve_problem || defence ;; Defending || encountered ;; Hostile_encounter
fighting ;; Hostile_encounter || mark ;; Communication || war ;; Military_operation || battles ;; Military_operation
warfare ;; Hostile_encounter || conduct ;; Action
losses ;; Earnings_and_losses || battle ;; Military_operation
confronted ;; Confronting_problem || dead ;; Death
fires ;; Catastrophe || received ;; Receiving
warnings ;; Warning || released ;; Publishing
developing ;; Cause_to_make_progress || extended ;; Change_event_time || event ;; Confronting_problem
record ;; Recording
recorded ;; Recording
heatwave ;; Catastrophe || sweltered ;; Body_movement || set ;; Placing
records ;; Recording || included ;; Cause_to_be_included
declared ;; Expressing_publicly || fires ;; Catastrophe || affecting ;; Influence
starting ;; Process_start || fires ;; Catastrophe || burned ;; Destroying
damaged ;; Damaging || destroyed ;; Destroying || blazes ;; Catastrophe
destroyed ;; Destroying || blazes ;; Catastrophe
caused ;; Causation || fires ;; Catastrophe || burns ;; Attack
started ;; Process_start || commenced ;; Process_start || Fires ;; Catastrophe
fire ;; Catastrophe
damaged ;; Damaging || destroyed ;; Destroying || evacuated ;; Emptying
siege ;; Besieging || took place ;; Process_start || stormed ;; Attack
took ;; Conquering || campaigning ;; Social_event || hostage ;; Kidnapping || guarding ;; Patrolling
demanded ;; Request || release ;; Releasing
resolved ;; Resolve_problem || passage ;; Motion_directional || granted ;; Giving
demands ;; Request || release ;; Releasing || exchange ;; Exchange || broadcasting ;; Expressing_publicly
demands ;; Request || become ;; Becoming || siege ;; Besieging || meeting ;; Social_event
killed ;; Killing || hostages ;; Kidnapping || threw ;; Motion
conduct ;; Action || ordered ;; Arranging || hostages ;; Kidnapping || service ;; Supply || operation ;; Military_operation || rescue ;; Rescuing || remaining ;; Presence
building ;; Building || abseiled ;; Self_motion
killed ;; Killing || rescued ;; Rescuing || remaining ;; Presence || raid ;; Attack
killing ;; Killing || accusations ;; Judgment_communication || inquest ;; Scrutiny || deaths ;; Death || faced ;; Confronting_problem
remaining ;; Presence || served ;; Assistance || prosecuted ;; Judgment_communication
forgotten ;; GiveUp || cause ;; Causation
bolstered ;; Supporting || brought ;; Bringing
demand ;; Request || received ;; Receiving || inspired ;; Influence || overwhelmed ;; Destroying
reopened ;; Openness || broke out ;; Process_start || building ;; Building || suffered ;; Bodily_harm || damage ;; Damaging
played ;; Competition || end ;; Process_end
determined ;; Deciding || gain ;; Getting || promotion ;; Cause_change_of_position_on_a_scale
contested ;; Competition || finished ;; Process_end || finished ;; Process_end
final ;; Process_end || reached ;; Arriving
get to ;; Arriving
got ;; Getting
take ;; Causation || scored ;; Getting
captured ;; Conquering || started ;; Process_start || attacked ;; Attack
spread ;; Dispersal || casualties ;; Catastrophe || continued ;; Process_start
distinguish ;; Check || referenced ;; Adducing || includes ;; Cause_to_be_included
ended ;; Process_end || withdrew ;; Removing || take ;; Conquering || offensive ;; Defending || lasted ;; Process_end
retake ;; Control || launched ;; Attack || clashes ;; Hostile_encounter || failed ;; Agree_or_refuse_to_act || withdrawn ;; Escaping
captured ;; Conquering || launched ;; Attack || made ;; Getting
aborted ;; Process_end || offensive ;; Defending
resulting in ;; Causation || lasted ;; Process_end || fighting ;; Hostile_encounter || offensive ;; Defending || resumed ;; GetReady || make ;; Getting
entering ;; Arriving || control ;; Control || offensive ;; Defending || launched ;; Attack
gave ;; Giving || offensive ;; Defending || repelled ;; Removing || hand over ;; Giving || making ;; Getting
rejected ;; Response || led to ;; Causation || launched ;; Attack || provoking ;; Convincing || offensive ;; Defending || attacks ;; Attack
clashes ;; Hostile_encounter || returned ;; Arriving || forces ;; Causation || captured ;; Conquering || continued ;; Process_start || withdrew ;; Escaping
held ;; Defending || ended ;; Process_end || fighting ;; Hostile_encounter
turned on ;; Attack || broken ;; Destroying
violence ;; Violence || marked ;; Recording
violence ;; Violence || killed ;; Killing || said ;; Statement
fire ;; Catastrophe
prediction ;; Coming_to_believe
set ;; Placing
set ;; Placing
climbing ;; Cause_change_of_position_on_a_scale
found ;; Know
recorded ;; Recording || recorded ;; Recording
records ;; Recording || began ;; Process_start || beat ;; Competition
Championship ;; Competition || Championship ;; Competition || organised ;; Arranging
held ;; Hold || tournament ;; Competition || final ;; Competition
selected ;; Choosing || vote ;; Choosing
hosted ;; Social_event || Championship ;; Competition
win ;; Earnings_and_losses || final ;; Competition
captured ;; Getting || defeating ;; Conquering
secured ;; Getting || finished ;; Process_end || competing ;; Competition || winning ;; Getting
competition ;; Competition || marred ;; Damaging
support ;; Supporting || concert ;; Social_event || Tour ;; Traveling || tour ;; Traveling || Tour ;; Traveling
replaced ;; Change_of_leadership || founding ;; Creating || return ;; Forming_relationships || tour ;; Traveling
started ;; Process_start || gain ;; Getting || tours ;; Traveling || Tour ;; Traveling
filmed ;; Create_artwork
started ;; Process_start || party ;; Social_event || plan ;; Arranging || fired ;; Process_end || tour ;; Traveling || felt ;; Perception_active
replaced ;; Change_of_leadership || joined ;; Becoming_a_member || founding ;; Creating || complete ;; Arranging || tour ;; Traveling
toured ;; Traveling || marked ;; Recording
developed ;; Coming_to_be || given ;; Giving || rehabilitate ;; Recovering || tour ;; Traveling || problems ;; Confronting_problem || problems ;; Confronting_problem || fired ;; Process_end || tour ;; Traveling
open ;; Openness || tour ;; Traveling
coming ;; Motion || tour ;; Traveling
war ;; Hostile_encounter || took place ;; Process_start
overthrown ;; Change_of_leadership || established ;; Coming_to_be
war ;; Hostile_encounter || cause ;; Causation || proclamation ;; Statement
declared ;; Expressing_publicly || launched ;; Military_operation
declare ;; Expressing_publicly || defeats ;; Conquering || fractured ;; Destroying || led ;; Causation
forced ;; Request
resumed ;; Process_start || died ;; Death
show ;; Social_event
controversial ;; Judgment_communication
imposed ;; Action
died ;; Death || collapsing ;; Destroying || show ;; Social_event
stumbled ;; Self_motion || walking ;; Self_motion || fell ;; Motion_directional
received ;; Receiving || falling ;; Motion_directional || died ;; Death || help ;; Assistance || taken ;; Motion
Games ;; Competition || held ;; Hold || event ;; Competition || governed ;; Action
Games ;; Competition || event ;; Competition || event ;; Competition || hosted ;; Social_event || hosted ;; Social_event || marked ;; Recording
Games ;; Competition || introduction ;; Coming_to_be
resulted in ;; Causation || deconstruction ;; Destroying || events ;; Competition || lead-up ;; Process_start || attributed ;; Causation || cuts ;; Cause_change_of_position_on_a_scale
began ;; Process_start || increase ;; Cause_change_of_position_on_a_scale || sold ;; Commerce_sell || Games ;; Competition || Games ;; Competition || record ;; Recording || drew ;; Motion
affected ;; Influence || allowed ;; Preventing_or_letting || participate ;; Participation || banned ;; Preventing_or_letting || doping ;; Committing_crime || scandal ;; Committing_crime
participated ;; Participation || hosted ;; Social_event
topped ;; Placing || winning ;; Getting
competition ;; Competition
control ;; Control || present ;; Presence || covers ;; Hiding_objects || events ;; Social_event
existence ;; Presence || rose ;; Cause_to_make_progress || covered ;; Filling
took ;; Conquering || declining ;; Cause_change_of_position_on_a_scale || revolution ;; Change_of_leadership || win ;; Getting || kickstarted ;; Process_start || finals ;; Competition || reach ;; Arriving
end ;; Process_end || start ;; Process_start || flitting ;; Self_motion || rise ;; Cause_change_of_position_on_a_scale || followed ;; Motion || followed ;; Motion
voted ;; Choosing || managed ;; Hold || represented ;; Change_sentiment
held ;; Hold
Marathon ;; Competition || run ;; Hold || held ;; Hold
event ;; Competition
events ;; Competition || marathon ;; Competition || marathon ;; Competition || making it ;; Causation
Proceeds ;; Commerce_sell || go to ;; Arriving
partnership ;; Collaboration || raised ;; Earnings_and_losses
includes ;; Cause_to_be_included || contributed ;; Giving
marked ;; Recording || return ;; Arriving
includes ;; Cause_to_be_included || campaigns ;; Military_operation
used ;; Using
sitting ;; Placing || returned ;; Arriving
declared ;; Expressing_publicly || reached ;; Arriving || put ;; Self_motion
defeat ;; Conquering || Battle ;; Hostile_encounter || died ;; Death
concert ;; Social_event
support ;; Supporting || launched ;; Motion
visited ;; Traveling
support ;; Supporting || festivals ;; Social_event || tour ;; Traveling
commenced ;; Process_start || visiting ;; Traveling || legs ;; Social_event || shows ;; Social_event || spanned ;; Expansion
supporting ;; Supporting || support ;; Supporting || tour ;; Traveling || toured ;; Traveling
mini-tour ;; Social_event || marked ;; Recording
left ;; Departing || final ;; Process_end || leg ;; Social_event || tour ;; Traveling
debuts ;; Process_start || made ;; Manufacturing
patrol ;; Patrolling || tour ;; Traveling || released ;; Publishing
support ;; Supporting || leg ;; Social_event || tour ;; Traveling
chose ;; Choosing || touring ;; Traveling || end ;; Process_end
cancellations ;; Removing || tour ;; Traveling
conflict ;; Hostile_encounter || armed ;; Bearing_arms
began ;; Process_start
defeated ;; Conquering || entered ;; Arriving || interned ;; Arrest || arrested ;; Arrest
retreated ;; Escaping
resulted in ;; Causation
complete ;; Process_end || success ;; Achieve || suffered ;; Bodily_harm
reason ;; Protest || claim ;; Statement || reinforce ;; Cause_change_of_strength
Eve ;; Social_event || held ;; Hold
surrounding ;; Surrounding || televised ;; Communication || televised ;; Communication
present ;; Presence || explode ;; Attack
surrounding ;; Surrounding || chosen ;; Choosing || viewed ;; Perception_active
watched ;; Perception_active || reported ;; Reporting
televised ;; Communication
became ;; Becoming
increased ;; Cause_change_of_position_on_a_scale || following ;; Self_motion
went ;; Motion
present ;; Presence || run ;; Self_motion || reference ;; Adducing
celebrating ;; Social_event || Festival ;; Social_event || event ;; Social_event
festival ;; Social_event || welcomes ;; Choosing
issues ;; Creating || combating ;; Hostile_encounter || festival ;; Social_event || celebration ;; Social_event || heritage ;; Preserving || restricts ;; Preventing_or_letting || preservation ;; Preserving
benefit ;; Earnings_and_losses || accommodation ;; Temporary_stay
open ;; Openness || constructed ;; Building || event ;; Social_event
open ;; Openness || festival ;; Social_event
Festival ;; Social_event
organised ;; Arranging || selling ;; Commerce_sell
organised ;; Arranging || regarded ;; Change_sentiment
become ;; Becoming || reaching ;; Arriving
promote ;; Cause_change_of_position_on_a_scale
visit ;; Traveling || organising ;; Arranging || view ;; Perception_active || buy ;; Commerce_buy || enhance ;; Cause_to_make_progress || inviting ;; Request
growth ;; Expansion || grown ;; Expansion
developed ;; Coming_to_be
supporting ;; Supporting || tour ;; Traveling
included ;; Cause_to_be_included
sold ;; Commerce_sell || tour ;; Traveling
included ;; Cause_to_be_included || tour ;; Traveling
said ;; Telling
passed ;; Motion
agreed ;; Agree_or_refuse_to_act || put ;; Self_motion
changed ;; Change
festival ;; Social_event || Festival ;; Social_event || took place ;; Process_start
festival ;; Social_event
festival ;; Social_event || cancelled ;; Change_event_time || perform ;; Action
festivals ;; Social_event || Festivals ;; Social_event || wrote ;; Writing
see ;; Perception_active || act ;; Action
included ;; Cause_to_be_included
festival ;; Social_event || close ;; Self_motion || reported ;; Reporting || rocked ;; Self_motion
arriving at ;; Arriving || started ;; Process_start
violence ;; Violence || festival ;; Social_event || brought ;; Bringing || erupt ;; Process_start
profit ;; Earnings_and_losses || make ;; Creating
recalled ;; Statement || Festival ;; Social_event || debut ;; Process_start
told ;; Telling || thought ;; Coming_to_believe || got there ;; Arriving
know ;; Know
remember ;; Change_sentiment || got ;; Getting || throwing up ;; Body_movement || send ;; Sending
front ;; Participation || remember ;; Change_sentiment
hijacked ;; Kidnapping || crashed ;; Catastrophe || killing ;; Killing
controls ;; Control || allowing ;; Preventing_or_letting || breached ;; Attack || take over ;; Change_of_leadership
noticed ;; Know || changes ;; Change || turned ;; Change || deviated ;; Change
made ;; Action
provided ;; Supply || injuries ;; Bodily_harm || made ;; Action
crashed ;; Catastrophe
hijacking ;; Kidnapping || struck ;; Attack
seen ;; Perception_active || happened ;; Presence
caused ;; Causation || casualties ;; Bodily_harm
identified ;; Check || identified ;; Check || recovery ;; Rescuing || recovered ;; Rescuing
operated ;; Being_in_operation
crashed ;; Catastrophe || diverted ;; Removing || flying ;; Self_motion
fatal ;; Killing || killed ;; Killing || known ;; Know
hijacked ;; Kidnapping || reached ;; Arriving
hijacking ;; Kidnapping || crash ;; Catastrophe || remain ;; Presence
crash ;; Catastrophe || disaster ;; Catastrophe
operated ;; Being_in_operation || scheduled ;; Hold || referred ;; Adducing
control ;; Control || communicated ;; Communication || takeoff ;; Self_motion
lost ;; Earnings_and_losses || planned ;; Arranging || crossing ;; Motion_directional || tracked ;; Self_motion || deviating ;; Change
left ;; Departing
involving ;; Participation || surpassed ;; Cause_change_of_position_on_a_scale || shot down ;; Use_firearm || flying ;; Self_motion
caused ;; Causation || combined ;; Cause_to_amalgamate
became ;; Becoming || identified ;; Check || search ;; Scrutiny || focused ;; Aiming
criticism ;; Judgment_communication || prompted ;; Causation
confirmed ;; Check
joint ;; Collaboration || suspended ;; Process_end || search ;; Scrutiny
ended ;; Process_end || search ;; Scrutiny
dubbed ;; Name_conferral || disappearance ;; Departing
given ;; Giving || communicated ;; Communication || investigators ;; Criminal_investigation
hijacking ;; Kidnapping || investigation ;; Criminal_investigation || proposed ;; Statement
report ;; Reporting
prevent ;; Preventing_or_letting || loss ;; Earnings_and_losses || citing ;; Adducing || associated ;; Collaboration
lengthening ;; Expansion || recording ;; Recording || reporting ;; Reporting || open ;; Openness
murder ;; Killing || massacre ;; Killing || massacre ;; Killing
killed ;; Killing || buried ;; Placing || massacre ;; Killing || reported ;; Reporting || hijackings ;; Kidnapping
began ;; Process_start || investigations ;; Criminal_investigation
given ;; Giving || mentioned ;; Statement || find ;; Getting || kill ;; Killing || raped ;; Bodily_harm || fight ;; Hostile_encounter
buried ;; Placing || recruited ;; Becoming_a_member
fled ;; Escaping || massacre ;; Killing
responded ;; Response || sending ;; Sending || establishing ;; Creating
took over ;; Getting || worked ;; Being_in_operation
arrested ;; Arrest
returned ;; Arriving || fled ;; Escaping || violence ;; Violence
decided ;; Deciding || murder ;; Killing || bury ;; Placing || torture ;; Bodily_harm || abduct ;; Kidnapping
ransom ;; Surrendering || crossed ;; Motion_directional || speculate ;; Coming_to_believe || recruited ;; Becoming_a_member || hold ;; Hold || extort ;; Kidnapping
confessed ;; Reveal_secret || killed ;; Killing || abducted ;; Kidnapping || reinforcements ;; Cause_change_of_strength || getting ;; Getting
confessed ;; Reveal_secret || freed ;; Releasing || ordered ;; Request || investigation ;; Criminal_investigation
killed ;; Killing
claimed ;; Statement || involved ;; Participation || investigated ;; Criminal_investigation || determine ;; Deciding
received ;; Receiving || call ;; Request
sought ;; Preventing_or_letting || inspect ;; Scrutiny || sped ;; Self_motion || crashed ;; Damaging
killed ;; Killing || on the run ;; Escaping
kidnapped ;; Kidnapping || fleeing ;; Escaping
present ;; Presence || occurred ;; Coming_to_be
discovered ;; Know || searched ;; Scrutiny
injured ;; Bodily_harm || killed ;; Killing || shooting ;; Killing || incident ;; Attack || attack ;; Attack
kidnapped ;; Kidnapping || attack ;; Attack
told ;; Telling || attack ;; Attack || carried out ;; Attack
kidnapped ;; Kidnapping
trained ;; Education_teaching || denied ;; Agree_or_refuse_to_act || armed ;; Bearing_arms
revealed ;; Reveal_secret || killed ;; Killing || attack ;; Attack
kidnapped ;; Kidnapping
injured ;; Bodily_harm
released ;; Releasing
hijacked ;; Kidnapping
crashed ;; Catastrophe
killed ;; Killing || injured ;; Bodily_harm
flying ;; Self_motion
stormed ;; Attack
took ;; Action
took ;; Conquering
claimed ;; Statement
began ;; Process_start || crashed ;; Catastrophe || witnessed ;; Know
authorized ;; Preventing_or_letting
built ;; Building
situated ;; Placing || dedicated ;; Giving
killed ;; Killing || attack ;; Attack || hijacking ;; Kidnapping || massacre ;; Attack || wounded ;; Bodily_harm
attack ;; Attack || planned ;; Arranging || carried out ;; Attack
seize ;; Conquering || exchange ;; Exchange || held ;; Arrest || plan ;; Arranging
helped ;; Assistance || attack ;; Attack || plan ;; Arranging || damaging ;; Damaging || aimed ;; Aiming || talks ;; Communication || scuppering ;; Destroying
ended up ;; Process_end || forced ;; Causation || error ;; Confronting_problem || find ;; Getting
attack ;; Attack || characterized ;; Statement
killed ;; Killing || raid ;; Attack || called ;; Name_conferral || hijacking ;; Kidnapping || Operation ;; Attack
launched ;; Process_start || Operation ;; Attack || response ;; Response
conflict ;; Hostile_encounter
referred ;; Adducing || war ;; Military_operation || operation ;; Military_operation || launched ;; Hold
conducted ;; Action || fired ;; Use_firearm || broke out ;; Process_start || kidnapping ;; Kidnapping || murder ;; Killing || arrest ;; Arrest || conflict ;; Hostile_encounter
airstrikes ;; Military_operation || bombardment ;; Military_operation || attacks ;; Attack || resulted in ;; Causation || death ;; Death || fighting ;; Hostile_encounter
stop ;; Preventing_or_letting || operation ;; Military_operation || kidnapping ;; Kidnapping || murder ;; Killing || fire ;; Use_firearm || increased ;; Cause_change_of_position_on_a_scale || crackdown ;; Attack || launched ;; Process_start
obtain ;; Getting || monitor ;; Scrutiny || guarantee ;; Commitment || release ;; Releasing || bring ;; Bringing || pressure ;; Influence || blockade ;; Besieging || end ;; Process_end || offensive ;; Military_operation || ceasefire ;; Process_end || isolation ;; Confronting_problem || lift ;; Cause_change_of_position_on_a_scale || overcome ;; Resolve_problem
response ;; Response || fire ;; Use_firearm || launched ;; Military_operation || raids ;; Attack
died ;; Death || caused ;; Causation || explosion ;; Attack || explosion ;; Attack || fired ;; Use_firearm || launched ;; Military_operation || airstrike ;; Attack || assumed ;; Protest
began ;; Process_start || operation ;; Military_operation || operation ;; Military_operation || invasion ;; Military_operation || destroying ;; Destroying || expanded ;; Expansion || withdrew ;; Escaping
announced ;; Expressing_publicly || ceasefire ;; Process_end
fired ;; Use_firearm || reported ;; Reporting || flight ;; Motion || shot down ;; Use_firearm || intercepted ;; Defending
hit ;; Attack
fell ;; Motion_directional || struck ;; Attack
killed ;; Killing
attacked ;; Attack || destroyed ;; Destroying || used up ;; Expend_resource || destroyed ;; Destroying
killed ;; Killing || left ;; GiveUp || disabled ;; Body_movement || wounded ;; Bodily_harm
damaged ;; Damaging || reports ;; Reporting || estimated ;; Scrutiny || estimates ;; Statement || razed ;; Destroying || affected ;; Influence || bombing ;; Use_firearm
costs ;; Cost || calculated ;; Check || run ;; Process_start
killed ;; Killing || injured ;; Bodily_harm
operation ;; Military_operation || impact ;; Influence || estimated ;; Statement
began ;; Process_start || hostage ;; Kidnapping || affiliated ;; Connect || took ;; Conquering
killed ;; Killing || attack ;; Attack || led ;; Bringing
raided ;; Attack || forces ;; Hostile_encounter
killed ;; Killing
freed ;; Releasing
captured ;; Arrest
attacks ;; Attack || carried out ;; Action
increasing ;; Cause_change_of_position_on_a_scale || threat ;; Warning
threat ;; Warning
occurred ;; Presence
kidnapped ;; Kidnapping
war ;; Hostile_encounter || response ;; Response
murdered ;; Killing || negotiations ;; Communication || release ;; Releasing || led to ;; Causation || kidnappers ;; Kidnapping
limited ;; Limiting || supported ;; Supporting
enabled ;; Preventing_or_letting || released ;; Releasing || detained ;; Arrest || arrested ;; Arrest
requested ;; Request || aid ;; Assistance || deployed ;; Arranging || support ;; Supporting
showed ;; Presence
criticized ;; Judgment_communication || response ;; Response
violence ;; Violence || formed ;; Coming_to_be || events ;; Social_event || galvanized ;; Influence || use ;; Using || support ;; Supply || accelerated ;; Cause_change_of_position_on_a_scale || attaining ;; Getting
began ;; Process_start || hijacking ;; Kidnapping
ended ;; Process_end || rescue ;; Rescuing
registered ;; Writing || referenced ;; Adducing || occurred ;; Coming_to_be
attacked ;; Attack || extorted ;; Kidnapping
reported ;; Reporting
hijacking ;; Kidnapping
defense ;; Defending || carried out ;; Action
hijacked ;; Kidnapping || broken ;; Bodily_harm || orders ;; Arranging
exchange ;; Exchange || free ;; Releasing || affiliated ;; Collaboration
diverted ;; Removing || originated ;; Coming_to_be
supported ;; Supporting || beginning ;; Process_start || hijacking ;; Kidnapping || informed ;; Telling
separated ;; Forming_relationships || moving ;; Motion
released ;; Releasing || flown ;; Self_motion
remained ;; Presence || death ;; Death || threatened ;; Warning
provided ;; Supply
kill ;; Killing || release ;; Releasing || threatened ;; Warning
planning ;; Arranging || rescue ;; Rescuing || led to ;; Causation
took place ;; Process_start
rescue ;; Rescuing || carried ;; Bringing
planning ;; Arranging
rescued ;; Rescuing || remaining ;; Presence || killed ;; Killing
killed ;; Killing
wounded ;; Bodily_harm || killed ;; Killing
become ;; Becoming
killed ;; Killing || destroyed ;; Destroying
present ;; Presence || supported ;; Supporting || issued ;; Expressing_publicly || retaliate ;; Revenge
fled ;; Escaping || killed ;; Killing
referred ;; Adducing
reported ;; Reporting || made ;; Manufacturing || said ;; Telling || seen ;; Perception_active
telling ;; Telling || said ;; Telling || varied ;; Change || seen ;; Perception_active
telling ;; Telling || kidnapped ;; Kidnapping || lied ;; Placing
charged ;; Judgment_communication || pleaded ;; Request || murder ;; Killing
found ;; Know
reports ;; Reporting || varied ;; Change || found ;; Know
ruled ;; Legal_rulings || death ;; Death
applying ;; Using || death ;; Death || administering ;; Control || murdered ;; Committing_crime
countered ;; Communication || swimming ;; Self_motion || drowned ;; Killing
contended ;; Statement || said ;; Communication || issues ;; Creating || upbringing ;; Education_teaching || lied ;; Placing || abuse ;; Committing_crime
defense ;; Defending || died ;; Death || challenged ;; Competition || calling ;; Name_conferral
murder ;; Killing || aggravated ;; Cause_change_of_strength || aggravated ;; Cause_change_of_strength || providing ;; Supply || found ;; Know
released ;; Releasing || served ;; Supply
attacked ;; Attack || defended ;; Defending || verdict ;; Legal_rulings
died ;; Death || complained ;; Judgment_communication || said ;; Statement
described ;; Statement
attacks ;; Attack || conflict ;; Hostile_encounter
began ;; Process_start || start ;; Process_start || overthrow ;; Change_of_leadership
weakened ;; Cause_change_of_strength || attacks ;; Attack || consisted ;; Cause_to_be_included
proclaimed ;; Statement || pledged ;; Commitment
blamed ;; Judgment_communication || increase ;; Cause_change_of_position_on_a_scale || established ;; Creating
restore ;; Recovering
launched ;; Process_start
violence ;; Violence || abduction ;; Kidnapping
occurred ;; Coming_to_be || resulted in ;; Causation
suffered ;; Bodily_harm || demolishing ;; Destroying || evacuating ;; Removing || pressed ;; Self_motion || build ;; Building || smuggling ;; Committing_crime
stated ;; Statement
killed ;; Killing || kidnapped ;; Kidnapping || beheaded ;; Killing || conflict ;; Hostile_encounter || start ;; Process_start
killed ;; Killing || injured ;; Bodily_harm || attack ;; Attack
use ;; Using || pressed ;; Self_motion
spoke ;; Telling || shared ;; Communication || said ;; Communication || takes ;; Action
saw ;; Self_motion || visit ;; Traveling || spoke ;; Telling || said ;; Communication
defense ;; Defending || defense ;; Defending || advise ;; Convincing || use ;; Using || fight ;; Hostile_encounter
taken ;; Conquering
crime ;; Committing_crime || organized ;; Arranging
turned ;; Becoming || travel ;; Traveling || commemorate ;; Social_event || reports ;; Reporting
use ;; Using || firing ;; Use_firearm || intercept ;; Preventing_or_letting
handed over ;; Giving || killed ;; Killing || concluded ;; Coming_to_believe || assault ;; Committing_crime || taken ;; Conquering
disputed ;; Quarreling
killed ;; Killing || stated ;; Statement || investigation ;; Criminal_investigation || claim ;; Statement || mistaken ;; Judgment_communication || given ;; Giving
claimed ;; Statement || abduction ;; Kidnapping || put on trial ;; Legality || masterminded ;; Committing_crime || convicted ;; Criminal_investigation
reports ;; Reporting || omitted ;; Removing
kidnapping ;; Kidnapping || state ;; Statement || murder ;; Committing_crime
fled ;; Escaping || committed ;; Commitment || arrested ;; Arrest || incident ;; Kidnapping
arrested ;; Arrest
caused ;; Causation || kidnapping ;; Kidnapping || protests ;; Protest
resignation ;; Change_of_leadership || protests ;; Protest || led to ;; Causation
became ;; Becoming || faced ;; Confronting_problem || disappearance ;; Kidnapping
protests ;; Protest || led to ;; Causation
gave ;; Giving || found ;; Know || announced ;; Expressing_publicly
arrested ;; Arrest
confirmed ;; Check || identified ;; Check || dead ;; Death
called ;; Name_conferral || named ;; Name_conferral || hijacked ;; Kidnapping
release ;; Releasing || imprisoned ;; Arrest || secure ;; Getting || hijacking ;; Kidnapping
rescued ;; Rescuing || stormed ;; Attack
rescue ;; Rescuing || operation ;; Military_operation
hijacking ;; Kidnapping
purchased ;; Commerce_buy || transport ;; Sending || involved ;; Cause_to_be_included || operation ;; Arranging || set ;; Placing
flew ;; Motion
refurbished ;; Recovering || restored ;; Recovering || cleaned ;; Emptying
completed ;; Process_end
hijacked ;; Kidnapping
reached ;; Arriving || followed ;; Motion
hijackers ;; Kidnapping || apprehended ;; Arrest
arrested ;; Arrest || captured ;; Conquering
sentenced ;; Legal_rulings || pleaded ;; Request
surrendered ;; Surrendering || returned ;; Arriving
pleaded ;; Request || hijacking ;; Kidnapping
sentenced ;; Legal_rulings
recapture ;; Kidnapping || seized ;; Control
shelling ;; Attack || fighting ;; Hostile_encounter || taken ;; Conquering
marked ;; Recording || fighting ;; Hostile_encounter
masked ;; Hiding_objects || grew ;; Cause_to_make_progress || captured ;; Conquering
began ;; Process_start || gained ;; Getting || control ;; Control || fortify ;; Cause_change_of_strength || claimed ;; Statement
created ;; Creating || standoff ;; Hostile_encounter || launched ;; Process_start
began ;; Process_start || increased ;; Cause_change_of_position_on_a_scale || take ;; Bringing
said ;; Communication
leading to ;; Causation || sabotage ;; Damaging || ordered ;; Request
fled ;; Escaping
retreated ;; Escaping
flown ;; Motion || landing ;; Motion || hijacked ;; Attack
accused ;; Judgment_communication || hijacking ;; Attack || support ;; Supporting || assistance ;; Assistance
gunmen ;; Bearing_arms || entered ;; Arriving || hijacked ;; Attack
flown ;; Motion || ordered ;; Arranging
hijackers ;; Attack || controlled ;; Control || land ;; Motion || touching down ;; Arriving
released ;; Releasing || wounded ;; Bodily_harm || hijackers ;; Attack || stabbed ;; Attack
control ;; Control || landed ;; Motion
became ;; Becoming || thought ;; Know
surrounded ;; Surrounding || found ;; Know || found ;; Know || prevent ;; Preventing_or_letting || joined ;; Becoming_a_member || landed ;; Motion
resolved ;; Resolve_problem || hijackers ;; Attack || support ;; Supporting || said ;; Communication
appears ;; Change_sentiment || hijacking ;; Attack || release ;; Releasing
ended ;; Process_end || agreed ;; Agree_or_refuse_to_act || release ;; Releasing || lasted ;; Process_end
terrorist ;; Terrorism || kidnapping ;; Kidnapping || murder ;; Killing || terror ;; Terrorism || attacks ;; Attack
hijacking ;; Attack || attack ;; Attack || seen ;; Perception_active
ended ;; Process_end || affair ;; Criminal_investigation || affair ;; Criminal_investigation || executed ;; Action || captured ;; Conquering
gave ;; Giving || involvement ;; Participation
demand ;; Request || deaths ;; Death || investigated ;; Criminal_investigation || led ;; Causation || led ;; Causation || surrounding ;; Surrounding || hijacking ;; Kidnapping || publication ;; Publishing || uproar ;; Quarreling
deaths ;; Death || captured ;; Conquering || charges ;; Judgment_communication || related ;; Know || acquitted ;; Legal_rulings
gave ;; Giving || killed ;; Killing || service ;; Assistance || order ;; Request || implicated ;; Convincing || emerged ;; Coming_to_be || concealing ;; Hiding_objects
refused ;; Agree_or_refuse_to_act || investigation ;; Criminal_investigation || affair ;; Criminal_investigation || call ;; Preventing_or_letting || forced ;; Causation || resign ;; Departing
given ;; Giving || granted ;; Giving || charges ;; Judgment_communication || pardons ;; Releasing || resigned ;; Change_of_leadership
investigate ;; Criminal_investigation || set up ;; Placing
stationed ;; Placing
fired ;; Use_firearm || hitting ;; Attack
caused ;; Causation || killing ;; Killing || injuring ;; Bodily_harm || damage ;; Damaging || shelling ;; Attack || widespread ;; Dispersal
retaliated ;; Revenge || shelling ;; Attack
responded ;; Response || stated ;; Statement || fired ;; Use_firearm
caused ;; Causation || widespread ;; Dispersal || prompted ;; Cause_change_of_position_on_a_scale
declared ;; Expressing_publicly || incidents ;; Catastrophe || ended ;; Process_end || become ;; Becoming || said ;; Statement || escalated ;; Expansion
attack ;; Attack || War ;; Hostile_encounter || occurred ;; Coming_to_be
constructed ;; Building
built ;; Building || support ;; Supporting || operations ;; Military_operation
became ;; Becoming || attacked ;; Attack || reported ;; Reporting || Incident ;; Attack
perceived ;; Know || response ;; Response || deploy ;; Arranging || prevent ;; Preventing_or_letting || attacks ;; Attack || aggression ;; Attack || forces ;; Causation || passed ;; Motion_directional || enabled ;; Preventing_or_letting
bombing ;; Attack || ordered ;; Arranging || Resolution ;; Statement || retaliation ;; Revenge || passed ;; Process_end || reported ;; Reporting || attacks ;; Attack
retaliation ;; Revenge || forces ;; Causation
retaliations ;; Revenge || attacks ;; Attack || start ;; Process_start || upsetting ;; Change_sentiment || Election ;; Change_of_leadership || launched ;; Process_start || avoid ;; Preventing_or_letting
changes ;; Change || removed ;; Removing || experiencing ;; Perception_active
begun ;; Process_start || reducing ;; Cause_change_of_position_on_a_scale || aid ;; Assistance
compete ;; Competition || aftermath ;; Catastrophe || downfall ;; Change_of_leadership || redefine ;; Change || growing ;; Cause_change_of_position_on_a_scale
ties ;; Connect || formation ;; Coming_to_be || travelled ;; Traveling || rebuild ;; Recovering
chaos ;; Violence || report ;; Reporting
attack ;; Attack || staged ;; Hold
election ;; Change_of_leadership || decided ;; Deciding || Operation ;; Military_operation || victory ;; Earnings_and_losses || launch ;; Process_start || strikes ;; Attack || targets ;; Aiming || entailed ;; Causation
bombing ;; Attack || decided ;; Deciding || signalling ;; Communication || aid ;; Assistance || reversal ;; Change
incidents ;; Hostile_encounter || skirmishes ;; Hostile_encounter
Starting ;; Process_start || described ;; Statement
attacked ;; Attack || killing ;; Killing || began ;; Process_start || reports ;; Reporting
retaliation ;; Revenge || crossed ;; Motion_directional || denied ;; Agree_or_refuse_to_act || claimed ;; Statement
crossed ;; Motion_directional || said ;; Statement || killing ;; Killing || skirmish ;; Hostile_encounter
beheaded ;; Killing || reactions ;; Response || sparked ;; Causation
reports ;; Reporting || denied ;; Agree_or_refuse_to_act
death ;; Death || skirmish ;; Hostile_encounter || led to ;; Causation
talks ;; Communication || reached ;; Achieve || understanding ;; Communication
skirmishes ;; Hostile_encounter || resulting in ;; Causation || continued ;; Hold || casualties ;; Bodily_harm || casualties ;; Bodily_harm
skirmishes ;; Hostile_encounter || affected ;; Influence
killed ;; Killing || claimed ;; Statement
killed ;; Killing || skirmish ;; Hostile_encounter || retreated ;; Escaping
killed ;; Killing || decapitated ;; Killing || found ;; Know
recovered ;; Cure || airlifted ;; Motion || admitted ;; Receiving
hired ;; Employment || claimed ;; Statement || carried out ;; Action
disaster ;; Catastrophe || disaster ;; Catastrophe || disaster ;; Catastrophe
occurred ;; Presence
decision ;; Deciding
invade ;; Attack || decided ;; Deciding
causing ;; Causation || retaliated ;; Revenge
occurred ;; Presence || suffering ;; Bodily_harm || crushing ;; Attack
operation ;; Military_operation
retreat ;; Escaping || uprising ;; Change_of_leadership
enabling ;; Preventing_or_letting || approaching ;; Arriving || defeat ;; Conquering || halted ;; Process_end || regroup ;; Cause_to_amalgamate
fought ;; Hostile_encounter || support ;; Supporting
war ;; Hostile_encounter || taken ;; Creating
began ;; Process_start || launched ;; Process_start
defeat ;; Conquering || drive ;; Self_motion || helping ;; Assistance
control ;; Control || liberate ;; Conquering || assert ;; Statement
revenge ;; Revenge || service ;; Supply || included ;; Cause_to_be_included
contact ;; Connect || established ;; Building
fighting ;; Quarreling
fighting ;; Hostile_encounter || reinforced ;; Cause_change_of_strength || command ;; Control || taken over ;; Military_operation
halted ;; Process_end || support ;; Supporting || flying ;; Self_motion || allow ;; Preventing_or_letting || allegations ;; Statement
called ;; Name_conferral
pleaded ;; Request
sent ;; Sending || supply ;; Supply || command ;; Control
gaining ;; Getting || sent ;; Sending
casualties ;; Catastrophe || wounded ;; Bodily_harm || killed ;; Killing
executions ;; Rewards_and_punishments || died ;; Death
exposed ;; Reveal_secret
killed ;; Killing
destroyed ;; Destroying || buildings ;; Building
abandon ;; GiveUp || destroyed ;; Destroying || suffered ;; Bodily_harm || invasion ;; Attack
armed ;; Bearing_arms || called ;; Name_conferral
named ;; Name_conferral || adopted ;; Choosing
signing ;; Sign_agreement
maintained ;; Preserving
became ;; Becoming || death ;; Death
maintain ;; Preserving
increased ;; Cause_change_of_position_on_a_scale || include ;; Cause_to_be_included || murder ;; Killing
attacked ;; Attack || retaliated ;; Revenge || raiding ;; Attack
participated ;; Participation || remained ;; Presence || deemed ;; Coming_to_believe
attack ;; Attack
attacked ;; Attack || attack ;; Attack || burned ;; Attack || culminating ;; Self_motion
killed ;; Killing || taken over ;; Conquering
went ;; Motion || pushed ;; Motion || burning ;; Bodily_harm
overwhelmed ;; Destroying || destroyed ;; Destroying || end ;; Process_end
fled ;; Escaping || killed ;; Killing
destroyed ;; Destroying || damaged ;; Damaging || ruined ;; Destroying || decimated ;; Destroying || losing ;; Earnings_and_losses
attacked ;; Attack
began ;; Process_start
support ;; Supporting || began ;; Process_start || faced ;; Perception_active
began ;; Process_start || unidentified ;; Check || entered ;; Arriving || firing ;; Use_firearm
violence ;; Violence
violence ;; Violence || killed ;; Killing || shooting ;; Use_firearm
revenge ;; Revenge
crimes ;; Committing_crime || unprecedented ;; Catastrophe || described ;; Statement || shootings ;; Use_firearm
attacks ;; Attack || conflict ;; Hostile_encounter || surge ;; Cause_change_of_position_on_a_scale || characterized ;; Statement
violence ;; Violence
split ;; Cause_to_amalgamate
destroyed ;; Destroying || shooting ;; Use_firearm
beginning ;; Process_start || shootings ;; Use_firearm || committed ;; Committing_crime
wounded ;; Bodily_harm || killed ;; Killing
ended ;; Process_end || died ;; Death || standoff ;; Hostile_encounter || shootout ;; Use_firearm
declared ;; Expressing_publicly || warfare ;; Hostile_encounter || associates ;; Collaboration || retaliation ;; Revenge || reporting ;; Reporting || posted ;; Reporting
incidents ;; Catastrophe || driven ;; Motion
hit ;; Attack || wounded ;; Bodily_harm || injured ;; Bodily_harm || rammed ;; Motion || opened ;; Action
involved ;; Cause_to_be_included || charged ;; Judgment_communication
took place ;; Process_start
besieged ;; Besieging || retaliation ;; Revenge
attacks ;; Attack || managed ;; Control || defeated ;; Conquering || lifted ;; Body_movement
perished ;; Death || managed ;; Control
caused ;; Causation || enabling ;; Preventing_or_letting || leave ;; Departing || capture ;; Conquering
events ;; Social_event
retaliation ;; Revenge || launched ;; Motion || led ;; Causation
targeted ;; Aiming
gathered ;; Come_together || invaded ;; Attack
allowing ;; Preventing_or_letting || found ;; Know || abandoned ;; GiveUp || defeated ;; Conquering
sacking ;; Hold || turned ;; Change || arrived ;; Arriving
aid ;; Assistance
revealed ;; Reveal_secret || attack ;; Attack || fortified ;; Cause_change_of_strength || garrisoned ;; Defending
break ;; Bodily_harm || negotiate ;; Communication
allowed ;; Preventing_or_letting || take ;; Bringing || concluded ;; Coming_to_believe || left ;; Departing
destroyed ;; Destroying || recover ;; Recovering
slaughtered ;; Killing || driven ;; Motion
taken ;; Conquering || refusing ;; Agree_or_refuse_to_act || convert ;; Change || executed ;; Rewards_and_punishments || becoming ;; Becoming
conquest ;; Attack || disaster ;; Catastrophe || event ;; Social_event
alter ;; Change || shifting ;; Change || supported ;; Supporting || discredited ;; Judgment_communication
contributed ;; Giving || relied ;; Supply
bombings ;; Terrorism || bombings ;; Terrorism || killed ;; Killing || injured ;; Bodily_harm
bomb ;; Terrorism || explosions ;; Damaging || took place ;; Process_start
bomb ;; Terrorism || went off ;; Process_end
planted ;; Placing || bombs ;; Terrorism || exploded ;; Attack
blamed ;; Judgment_communication || attack ;; Terrorism || claimed ;; Statement
arrested ;; Arrest
convicted ;; Legal_rulings || death ;; Death || sentenced ;; Legal_rulings
sentence ;; Legal_rulings || death ;; Death || upheld ;; Action
recruited ;; Becoming_a_member || avenge ;; Revenge || riots ;; Violence
assisted ;; Assistance || planted ;; Placing || chose ;; Choosing
died ;; Death
Riots ;; Violence || Battle ;; Attack || Final ;; Competition || riots ;; Violence
stabbed ;; Bodily_harm || killing ;; Killing || viewed ;; Statement || retaliation ;; Revenge
stabbed ;; Bodily_harm || started ;; Process_start || led to ;; Causation || broke out ;; Process_start
occupied ;; Control
attacked ;; Attack || revenge ;; Revenge
warning ;; Warning || deployed ;; Arranging || control ;; Control || riot ;; Violence || fired ;; Use_firearm
injuries ;; Bodily_harm || riots ;; Violence || arrests ;; Arrest
condemned ;; Judgment_communication || riots ;; Violence || expel ;; Removing || competition ;; Competition || threatened ;; Warning
criticized ;; Judgment_communication || riots ;; Violence
Battle ;; Hostile_encounter || confrontation ;; Hostile_encounter || fought ;; Hostile_encounter || War ;; Military_operation
torpedoed ;; Attack || bombardment ;; Attack || prompting ;; Causation
salvaged ;; Rescuing || drifting ;; Motion || beached ;; Motion
deployed ;; Arranging || bombarded ;; Attack || retaliation ;; Revenge || organised ;; Arranging || loss ;; Earnings_and_losses
engaged ;; Hostile_encounter
deployed ;; Arranging || organised ;; Arranging || draw ;; Motion || engaged ;; Hostile_encounter
retreated ;; Departing || joined ;; Becoming_a_member
sailed ;; Self_motion || reach ;; Arriving || combined ;; Cause_to_amalgamate
deployed ;; Arranging || losing ;; Earnings_and_losses || engaged ;; Hostile_encounter
deployment ;; Arranging || battle ;; Hostile_encounter || blockade ;; Besieging || imposed ;; Action || operation ;; Military_operation || marked ;; Recording || loosening ;; Cause_change_of_strength || war ;; Military_operation
repaired ;; Recovering || towed ;; Self_motion || service ;; Supply
battle ;; Hostile_encounter || killed ;; Killing
bombardment ;; Attack || charged ;; Legality
warfare ;; Hostile_encounter || started ;; Process_start || cut off ;; Having_or_lacking_access
fighting ;; Hostile_encounter || capturing ;; Conquering || claimed ;; Statement || control ;; Control
battles ;; Hostile_encounter || took place ;; Process_start
conflict ;; Hostile_encounter || captured ;; Conquering || deaths ;; Death
reduced ;; Cause_change_of_position_on_a_scale || swelling ;; Expansion || flee ;; Escaping || casualties ;; Death
capture ;; Conquering || claims ;; Statement || occurred ;; Presence || attack ;; Attack || retaliate ;; Revenge || vowed ;; Commitment || progress ;; Cause_to_make_progress || resettling ;; Hold
stated ;; Statement
statement ;; Statement || associated ;; Collaboration || press ;; Sending
fire ;; Use_firearm
wounded ;; Bodily_harm || gunshot ;; Attack || killed ;; Killing || taking ;; Conquering
finding ;; Know || premeditated ;; Committing_crime || indicating ;; Statement || ruled ;; Legal_rulings || attack ;; Attack
ruin ;; Destroying || stated ;; Statement || shooting ;; Use_firearm
revealed ;; Reveal_secret || shooting ;; Use_firearm || hiding ;; Hiding_objects
supporting ;; Supporting || account ;; Statement || revenge ;; Revenge
planning ;; Arranging || terrorist ;; Terrorism || terrorism ;; Terrorism || involved ;; Cause_to_be_included || attacks ;; Attack || joint ;; Collaboration || preparing ;; GetReady || running ;; Hold
providing ;; Supply || terrorists ;; Terrorism || joint ;; Collaboration || involved ;; Cause_to_be_included || support ;; Supporting || state ;; Statement || operating ;; Military_operation
joint ;; Collaboration || joint ;; Collaboration
supplied ;; Supply
accused ;; Judgment_communication || reinvigorating ;; Recovering || terrorism ;; Terrorism || support ;; Supporting || revenge ;; Revenge || help ;; Assistance || destabilize ;; Damaging
report ;; Reporting || indicated ;; Statement || revive ;; Recovering
supplying ;; Supply || printing ;; Publishing
Battle ;; Hostile_encounter || battle ;; Hostile_encounter
defeat ;; Conquering || avenge ;; Revenge
defeated ;; Conquering || battle ;; Hostile_encounter || command ;; Control
defeat ;; Conquering || join ;; Becoming_a_member || forces ;; Motion
received ;; Receiving || began ;; Process_start || readying ;; GetReady
believed ;; Know || locate ;; Placing || appeared ;; Presence
built ;; Building || fight ;; Defending || constructed ;; Building || response ;; Response
using ;; Using || attack ;; Attack || bloodshed ;; Killing || produced ;; Creating
used ;; Using || distract ;; Preventing_or_letting || assault ;; Attack || envelop ;; Besieging
worked ;; Being_in_operation || cut ;; Removing || took ;; Bringing
retreated ;; Escaping
attacked ;; Attack || killed ;; Killing || observing ;; Perception_active
ended ;; Process_end || battle ;; Hostile_encounter || saving ;; Rescuing
mount ;; Motion_directional
threat ;; Warning || end ;; Process_end || free ;; Releasing
Insurrection ;; Hostile_encounter || rebellion ;; Hostile_encounter || took place ;; Process_start || led ;; Causation
killed ;; Killing
hiding ;; Hiding_objects || rebellion ;; Hostile_encounter
rebellion ;; Hostile_encounter || suppressed ;; Hindering
organized ;; Arranging || fear ;; Change_sentiment || retaliation ;; Revenge
accused ;; Judgment_communication || punished ;; Rewards_and_punishments || rebellion ;; Hostile_encounter
murdered ;; Killing
present ;; Presence || prohibiting ;; Preventing_or_letting || restricting ;; Limiting || requiring ;; Preventing_or_letting || services ;; Supply
forces ;; Military_operation || command ;; Control || raided ;; Attack
occupied ;; Control || removed ;; Removing
cleared ;; Emptying || raids ;; Attack || initiatives ;; Statement || end ;; Process_end || sit-ins ;; Social_event || raided ;; Attack
killings ;; Killing || described ;; Statement || raids ;; Attack
died ;; Death || dispersal ;; Dispersal
killed ;; Killing || injured ;; Bodily_harm
deaths ;; Death || stated ;; Statement || sit-in ;; Social_event
toppled ;; Change_of_leadership || made ;; Action || revolution ;; Change_of_leadership
violence ;; Violence || denounced ;; Judgment_communication || dispersals ;; Emptying
followed ;; Motion || retaliation ;; Revenge
declared ;; Expressing_publicly || instituted ;; Action || curfews ;; Action || response ;; Response
bombings ;; Attack || occurred ;; Presence
killed ;; Killing || attack ;; Attack
injured ;; Bodily_harm
sentenced ;; Legal_rulings || bombings ;; Attack || convicted ;; Legal_rulings
causing ;; Causation || attack ;; Attack || involved ;; Cause_to_be_included || carried ;; Bringing || detonated ;; Attack || detonated ;; Attack
stated ;; Statement || retaliation ;; Revenge || support ;; Supporting || bombings ;; Attack || carrying ;; Bringing
executed ;; Achieve
killed ;; Killing
killed ;; Killing || took place ;; Process_start || massacre ;; Killing
assault ;; Attack || occurred ;; Presence || preceded ;; Process_start || blockade ;; Besieging || war ;; Hostile_encounter || end ;; Process_end || rule ;; Control || relieve ;; Confronting_problem
opened ;; Openness || entering ;; Arriving || fire ;; Use_firearm || felt ;; Perception_active
fighting ;; Hostile_encounter || fell ;; Motion_directional
killed ;; Killing || battle ;; Hostile_encounter || shot ;; Use_firearm || died ;; Death || thrown ;; Motion
killed ;; Killing || combat ;; Hostile_encounter || boast ;; Statement
conducted ;; Action || left ;; Departing || lying ;; Placing || found ;; Know || decapitated ;; Killing || disemboweled ;; Bodily_harm
mutilation ;; Bodily_harm || rape ;; Committing_crime || wrote ;; Writing
taken ;; Conquering || killed ;; Killing || prisoner ;; Arrest || paraded ;; Self_motion
attackers ;; Attack || killed ;; Killing || injured ;; Bodily_harm
condemned ;; Judgment_communication || killings ;; Killing
sent ;; Sending || rebuffed ;; Agree_or_refuse_to_act
warned ;; Warning || held ;; Defending || incidents ;; Catastrophe || occurred ;; Presence || massacre ;; Killing
became ;; Becoming || deaths ;; Death || conflict ;; Hostile_encounter
used ;; Using || attack ;; Attack || accuse ;; Judgment_communication || play ;; Competition || left ;; Departing || affair ;; Killing || embellished ;; Statement || blackening ;; Damaging || violating ;; Preventing_or_letting
encouraging ;; Influence || killings ;; Killing || flee ;; Escaping || strengthened ;; Cause_change_of_strength || sparked ;; Causation
ended ;; Process_end || revenge ;; Revenge || attack ;; Attack || killing ;; Killing || convoy ;; Patrolling || massacre ;; Killing || massacre ;; Killing || staff ;; Employment
occurred ;; Coming_to_be || killing ;; Killing || carrying ;; Carry_goods || sank ;; Motion
cause ;; Causation || dispute ;; Quarreling || sinking ;; Catastrophe
presented ;; Presence || investigation ;; Criminal_investigation || sunk ;; Motion || investigation ;; Criminal_investigation || carried out ;; Action
conclusions ;; Coming_to_believe || report ;; Reporting || resulted in ;; Causation
sanctions ;; Revenge || imposed ;; Action || sinking ;; Catastrophe
sinking ;; Catastrophe
offer ;; Supply || aid ;; Assistance || investigation ;; Criminal_investigation
presented ;; Presence
investigation ;; Criminal_investigation || concur ;; Agree_or_refuse_to_act || report ;; Reporting
condemning ;; Judgment_communication || identifying ;; Check || attack ;; Attack
took place ;; Process_start
killing ;; Killing || wounding ;; Bodily_harm || fired ;; Use_firearm
play ;; Competition || targeted ;; Aiming || watching ;; Perception_active || frequented ;; Traveling || crowded ;; Come_together
called ;; Name_conferral
killing ;; Killing || retaliation ;; Revenge || claimed ;; Statement
linked ;; Connect || protected ;; Preventing_or_letting || destroying ;; Destroying || investigation ;; Criminal_investigation
request ;; Request || investigated ;; Research
investigation ;; Criminal_investigation || concluded ;; Coming_to_believe || colluded ;; Collaboration
report ;; Reporting || investigate ;; Criminal_investigation
demanded ;; Request
replaced ;; Change_of_leadership || ordered ;; Arranging || report ;; Reporting
found ;; Know || investigation ;; Criminal_investigation || attack ;; Attack || concluded ;; Coming_to_believe
released ;; Publishing
named ;; Name_conferral || claimed ;; Statement
bombing ;; Attack || took place ;; Process_start
destroyed ;; Destroying || carrying ;; Carry_goods || repairing ;; Self_motion
killed ;; Killing || wounded ;; Bodily_harm
killed ;; Killing || collaborating ;; Collaboration || claimed ;; Statement || saying ;; Statement
killed ;; Killing || interpreted ;; Statement
shooting ;; Use_firearm || retaliated ;; Revenge
conducted ;; Action || used ;; Using || strike ;; Attack || attacks ;; Attack || attacks ;; Attack
killed ;; Killing || wounded ;; Bodily_harm || retaliation ;; Revenge || carried out ;; Attack
targeted ;; Aiming || attacks ;; Attack
attack ;; Attack || hit ;; Arriving
used ;; Using
convicted ;; Legal_rulings || ordering ;; Arranging || attack ;; Attack || attack ;; Attack || characterized ;; Statement
killed ;; Killing || attacks ;; Attack || attacks ;; Attack
killed ;; Killing || suicide bomber ;; Killing || explosions ;; Use_firearm || occurred ;; Presence || attacks ;; Attack || massacre ;; Killing
suggested ;; Convincing || violence ;; Terrorism
estimated ;; Statement || exploded ;; Attack
killed ;; Killing
race ;; Competition || supported ;; Supporting
wounded ;; Bodily_harm || died ;; Death || explosions ;; Use_firearm
died ;; Death
blasts ;; Attack || blasts ;; Attack
injured ;; Bodily_harm || casualties ;; Death || took ;; Bringing
bombings ;; Attack || said ;; Statement || killed ;; Killing || revenge ;; Revenge || insults ;; Judgment_communication || saying ;; Statement || threatening ;; Warning || attacks ;; Attack
statement ;; Statement || attacks ;; Attack || avenge ;; Revenge || insults ;; Judgment_communication || said ;; Statement
violence ;; Violence || War ;; Military_operation
attacked ;; Attack || party ;; Participation || raiding ;; Attack || retaliation ;; Revenge || ambush ;; Attack || burning ;; Destroying
battles ;; Hostile_encounter || clashes ;; Hostile_encounter
battles ;; Hostile_encounter || claimed ;; Statement || patrols ;; Patrolling || fired ;; Use_firearm
killed ;; Killing || killed ;; Killing || wounded ;; Bodily_harm
injured ;; Bodily_harm
damaged ;; Damaging || destroyed ;; Destroying || leaving ;; Departing
violence ;; Violence || came into effect ;; Change || truce ;; Process_end || took place ;; Process_start || ended ;; Process_end
invasion ;; Attack || War ;; Military_operation
defeating ;; Conquering || battle ;; Military_operation || burned ;; Destroying
destruction ;; Destroying || attack ;; Attack || retaliation ;; Revenge
captured ;; Conquering || War ;; Military_operation || marks ;; Recording || Burning ;; Destroying || occupied ;; Control
fled ;; Escaping || Battle ;; Military_operation || victory ;; Getting
refuge ;; Preserving || found ;; Know
spent ;; Expend_resource
stands ;; Placing || known ;; Statement
began ;; Process_start || attack ;; Attack || fires ;; Catastrophe
dropping ;; Motion || spun ;; Motion || passed ;; Motion_directional || killing ;; Killing || setting down ;; Motion_directional || lifting ;; Motion
returned ;; Arriving || damaged ;; Damaging || storm ;; Catastrophe
debated ;; Quarreling || raze ;; Destroying || occupation ;; Control || lasted ;; Process_end
fought ;; Hostile_encounter || Revolt ;; Hostile_encounter || Revolt ;; Hostile_encounter || War ;; Hostile_encounter || war ;; Hostile_encounter
includes ;; Cause_to_be_included || occurred ;; Presence || Rebellion ;; Hostile_encounter
uprising ;; Change_of_leadership
recorded ;; Recording || death ;; Death || due ;; Causation || conflict ;; Hostile_encounter || reduction ;; Cause_change_of_position_on_a_scale || led to ;; Causation || occurred ;; Presence
research ;; Research || killed ;; Killing || forces ;; Hostile_encounter || massacres ;; Killing || reprisals ;; Revenge || revolt ;; Change_of_leadership || deported ;; Removing
massacred ;; Killing || remained ;; Temporary_stay || deportations ;; Removing || deported ;; Removing || carrying out ;; Military_operation
died ;; Death || thirst ;; Catastrophe || starvation ;; Catastrophe || journey ;; Self_motion
reduction ;; Cause_change_of_position_on_a_scale || reported ;; Reporting
deaths ;; Death || caused ;; Causation || due ;; Causation || war ;; Hostile_encounter || famine ;; Catastrophe
war ;; Hostile_encounter || relocated ;; Removing
war ;; Hostile_encounter || rebellion ;; Change_of_leadership || casualties ;; Death
occurred ;; Presence || excluded ;; Preventing_or_letting || uprising ;; Change_of_leadership
involved ;; Cause_to_be_included || warring ;; Hostile_encounter || cause ;; Causation
attack ;; Attack || revenge ;; Revenge || directed ;; Aiming || rebels ;; Hostile_encounter || overthrow ;; Change_of_leadership || revolt ;; Attack
emigration ;; Departing || revolt ;; Attack || ensued ;; Coming_to_be
held ;; Hold || festivals ;; Social_event
organised ;; Arranging
held ;; Hold || event ;; Social_event
festival ;; Social_event || suffered ;; Catastrophe || withdrew ;; GiveUp
added ;; Cause_to_be_included || sales ;; Commerce_sell || slow ;; Hindering
included ;; Cause_to_be_included || line-up ;; Come_together
performed ;; Hold || relegated ;; Change
played ;; Participation
eat ;; Expend_resource
included ;; Cause_to_be_included
transformed ;; Change || rain ;; Bodily_harm || marked ;; Recording || pelted ;; Connect
retaliated ;; Revenge || throwing ;; Body_movement
performed ;; Hold || enhanced ;; Cause_change_of_position_on_a_scale || cleared ;; Emptying || rain ;; Bodily_harm
leading to ;; Causation || playing ;; Participation || kept ;; Temporary_stay || lived ;; Perception_active || kept ;; Bringing
sales ;; Commerce_sell || sales ;; Commerce_sell || tempered ;; Hindering || weather ;; Causation || held ;; Hold
festival ;; Social_event
visitors ;; Traveling || competitions ;; Competition || parade ;; Motion || bills ;; Statement || attracts ;; Motion
traced ;; Check || held ;; Hold
organized ;; Arranging
became ;; Becoming || organize ;; Arranging || merged ;; Cause_to_amalgamate || festival ;; Social_event || returned ;; Motion || create ;; Creating
Organized ;; Arranging || supported ;; Supporting || grown ;; Expansion
racing ;; Competition || events ;; Social_event || televised ;; Communication
target ;; Aiming || increasing ;; Cause_change_of_position_on_a_scale
tied ;; Connect || identity ;; Name_conferral
nickname ;; Name_conferral || team ;; Participation || carries ;; Bringing || called ;; Name_conferral
party ;; Social_event || painted ;; Create_artwork || held ;; Creating
explosion ;; Attack || explosion ;; Attack
killed ;; Killing || explosion ;; Attack
election ;; Change_of_leadership
blew up ;; Destroying || passing ;; Motion_directional
sent ;; Sending || died ;; Death || injuries ;; Bodily_harm
explosion ;; Attack || killed ;; Killing || said ;; Statement || injured ;; Bodily_harm
killed ;; Killing || report ;; Reporting || said ;; Statement
explosion ;; Attack || described ;; Statement || assassination ;; Attack || said ;; Statement || happened ;; Know || draw ;; Create_artwork || conclusions ;; Process_end
tour ;; Traveling || concert ;; Social_event
commenced ;; Process_start || concluded ;; Process_end
named ;; Name_conferral || tour ;; Traveling
left ;; Departing || start ;; Process_start || tour ;; Traveling
replaced ;; Change
tour ;; Traveling || held ;; Hold
return ;; Arriving || filmed ;; Create_artwork
final ;; Process_end || tour ;; Traveling || embarked ;; Process_start || Tour ;; Traveling
held ;; Defending || fair ;; Social_event || Fair ;; Social_event
fair ;; Social_event || expositions ;; Social_event || exhibitions ;; Social_event
used ;; Using || used ;; Using || races ;; Competition || used ;; Using || sporting ;; Competition || events ;; Social_event
sanctioned ;; Preventing_or_letting || state ;; Statement || fair ;; Social_event || held ;; Hold
Fair ;; Social_event || event ;; Social_event
increased ;; Cause_change_of_position_on_a_scale
record ;; Recording || set ;; Placing || attendance ;; Participation || fair ;; Social_event
increased ;; Cause_change_of_position_on_a_scale || record ;; Recording || fair ;; Social_event
increased ;; Cause_change_of_position_on_a_scale || record ;; Recording || increased ;; Cause_change_of_position_on_a_scale || Fair ;; Social_event || fair ;; Social_event
announced ;; Expressing_publicly || cancelled ;; Change_event_time || flood ;; Motion || fair ;; Social_event || left ;; Causation
announcement ;; Expressing_publicly || competitive ;; Competition || planning ;; Arranging || came ;; Self_motion || hold ;; Defending || fair ;; Social_event
sport ;; Competition || opening ;; Openness || opening ;; Openness
sport ;; Competition || hosting ;; Come_together
played ;; Competition
participate ;; Participation || allowed ;; Preventing_or_letting || affiliated ;; Connect || invited ;; Request
competed ;; Competition
competed ;; Competition
draw ;; Create_artwork || took place ;; Hold
concert ;; Social_event || tour ;; Traveling || Tour ;; Traveling
performed ;; Hold || support ;; Supporting || Launched ;; Motion
set ;; Placing || animated ;; Change_sentiment || tour ;; Traveling || tour ;; Traveling
sport ;; Motion || tour ;; Traveling
tour ;; Traveling || legs ;; Social_event || shows ;; Social_event
tour ;; Traveling
included ;; Cause_to_be_included || set ;; Placing
concert ;; Social_event || included ;; Cause_to_be_included || filmed ;; Create_artwork || released ;; Publishing
festival ;; Social_event || performing ;; Hold || takes place ;; Process_start
festival ;; Social_event
headlined ;; Publishing || appearing ;; Presence
festival ;; Social_event || recorded ;; Recording || receives ;; Receiving || released ;; Publishing || coverage ;; Communication
attended ;; Participation
festival ;; Social_event || helping ;; Assistance || raise ;; Bringing
festival ;; Social_event || inspired ;; Influence
retains ;; Preserving
festival ;; Social_event || grew ;; Expansion || attendees ;; Participation || swollen ;; Expansion || took place ;; Process_start
concert ;; Social_event || seeing ;; Perception_active
performed ;; Hold || festival ;; Social_event || record ;; Recording || set ;; Placing
taken ;; Conquering || held ;; Hold || held ;; Hold
festival ;; Social_event || took place ;; Process_start
recording ;; Recording || concert ;; Social_event || beginning ;; Process_start || completed ;; Process_end || tours ;; Traveling || Tour ;; Traveling
followed ;; Self_motion || Tour ;; Traveling || Tour ;; Traveling
completed ;; Process_end || tour ;; Traveling || Tour ;; Arriving
concert ;; Social_event || tours ;; Traveling
see ;; Perception_active || tours ;; Traveling || tours ;; Traveling
Tour ;; Traveling
debut ;; Process_start || concert ;; Social_event || recording ;; Recording || tour ;; Traveling || Tour ;; Traveling
Beginning ;; Process_start || debut ;; Process_start || supported ;; Supporting || tour ;; Traveling
played ;; Action
concert ;; Social_event || set ;; Placing || chronicled ;; Recording
concert ;; Social_event || Filmed ;; Create_artwork
died ;; Death
completed ;; Process_end || filming ;; Create_artwork
crashed ;; Catastrophe || piloted ;; Motion || takeoff ;; Motion
crash ;; Catastrophe || killed ;; Killing
looked ;; Change_sentiment || overloaded ;; Filling || investigation ;; Scrutiny
crash ;; Catastrophe || returned ;; Arriving
held ;; Hold
death ;; Death || release ;; Publishing
became ;; Becoming
known ;; Know || concert ;; Social_event
founded ;; Creating || concert ;; Social_event || tour ;; Traveling
spraying ;; Create_artwork || joined ;; Becoming_a_member || tour ;; Traveling
acquired ;; Getting || remaining ;; Presence || changed ;; Change
began ;; Process_start || Beginning ;; Process_start || produce ;; Creating || Festival ;; Self_motion || festival ;; Social_event
festival ;; Social_event
concert ;; Social_event || recording ;; Recording || artist ;; Create_artwork
promote ;; Cause_change_of_position_on_a_scale || extended ;; Change_event_time
began ;; Process_start || became ;; Becoming || concluded ;; Process_end
became ;; Becoming || let ;; Preventing_or_letting || performed ;; Hold || incorporate ;; Cause_to_amalgamate
opening ;; Openness || served ;; Assistance
promoted ;; Cause_change_of_position_on_a_scale || sponsored ;; Supply
prevent ;; Preventing_or_letting || using ;; Using || ticketing ;; Self_motion || sold ;; Commerce_sell || occurred ;; Presence
donated ;; Giving || sold ;; Commerce_sell || devoted ;; Giving
described ;; Statement
subject ;; Research
opened ;; Openness || performing ;; Hold
suspended ;; Process_end
elevated ;; Cause_change_of_position_on_a_scale || mounted ;; Motion_directional
performed ;; Hold || deceased ;; Death || rendered ;; Causation
received ;; Receiving || mixed ;; Cause_to_amalgamate
praised ;; Judgment_communication || deemed ;; Coming_to_believe || believed ;; Know
present ;; Presence
marks ;; Recording
tour ;; Traveling || overturned ;; Request
death ;; Death || resulted in ;; Causation
cause ;; Causation || specified ;; Statement
received ;; Receiving
released ;; Publishing || concert ;; Social_event || limited ;; Limiting
left ;; Departing
took over ;; Change_of_leadership || remarked ;; Statement
enjoyed ;; Perception_active || handle ;; Resolve_problem
began ;; Process_start || ended ;; Process_end
filmed ;; Create_artwork
recognised ;; Know || players ;; Participation
entered ;; Arriving
playing ;; Competition || tournament ;; Competition || pitted ;; Hostile_encounter
event ;; Competition || beating ;; Competition
reviving ;; Recovering || drew ;; Create_artwork
works ;; Action || collection ;; Create_artwork || work ;; Action
works ;; Action || project ;; Creating || concert ;; Social_event || showcases ;; Social_event
developed ;; Coming_to_be || project ;; Creating || conceived ;; Creating
presenting ;; Presence || designed ;; Creating
project ;; Creating || recorded ;; Recording || puts out ;; Placing
works ;; Action || selected ;; Choosing
works ;; Action || create ;; Creating || ordered ;; Request
beginning ;; Process_start || beginning ;; Process_start || brings ;; Bringing || work ;; Action
presented ;; Presence
collaborates ;; Collaboration || arts ;; Create_artwork
held ;; Defending
signature ;; Sign_agreement || gather ;; Come_together || led ;; Causation || singing ;; Self_motion
draws ;; Create_artwork || singing ;; Self_motion
attendance ;; Participation
prevented ;; Preventing_or_letting || held ;; Defending || attending ;; Participation
grown ;; Expansion
produced ;; Manufacturing
took place ;; Hold
took place ;; Process_start
defeated ;; Conquering
wrestling ;; Competition || produced ;; Manufacturing
took place ;; Process_start
contested ;; Competition
replaced ;; Change_of_leadership || match ;; Competition || match ;; Competition || win ;; Getting || lost ;; Earnings_and_losses || defeated ;; Conquering || defeated ;; Conquering
included ;; Cause_to_be_included || match ;; Competition || debut ;; Process_start
records ;; Recording || record ;; Recording
disputed ;; Quarreling || claiming ;; Statement || record ;; Recording
paid ;; Commerce_pay || record ;; Recording || included ;; Cause_to_be_included || admitted ;; Statement
gave ;; Giving || held ;; Defending
elected ;; Choosing
drawn ;; Create_artwork
Election ;; Choosing
formed ;; Coming_to_be
collect ;; Come_together || uprising ;; Change_of_leadership || took place ;; Process_start
joined ;; Participation
marched ;; Self_motion
found ;; Know || put ;; Self_motion || put ;; Self_motion
reformed ;; Reforming_a_system || held ;; Defending
drew ;; Create_artwork || leading to ;; Causation
debate ;; Communication
Festival ;; Social_event || held ;; Hold
began ;; Process_start || festival ;; Social_event
expanded ;; Expansion || festival ;; Social_event || attracts ;; Influence
festival ;; Social_event
present ;; Presence || provide ;; Supply || create ;; Creating || promote ;; Cause_to_make_progress || festival ;; Social_event || support ;; Supporting || held ;; Hold
festival ;; Social_event
Festival ;; Social_event || present ;; Presence
exhibitions ;; Social_event
performing ;; Hold || includes ;; Cause_to_be_included || spoken ;; Telling
performing ;; Hold || produced ;; Creating
murder ;; Killing
killed ;; Killing
included ;; Cause_to_be_included
charged ;; Judgment_communication || convicted ;; Legal_rulings
killing ;; Killing || given ;; Giving || arrest ;; Arrest || served ;; Assistance
took place ;; Process_start || called ;; Name_conferral || shocking ;; Body_movement
marked ;; Recording
referred ;; Adducing
started ;; Process_start || changed ;; Change
referred ;; Adducing || called ;; Name_conferral
prompted ;; Causation || became ;; Becoming
exposed ;; Reveal_secret || killing ;; Killing || increased ;; Cause_change_of_position_on_a_scale
denounced ;; Judgment_communication || rescue ;; Rescuing
armed ;; Bearing_arms
recognized ;; Know || decorated ;; Create_artwork
took place ;; Process_start
attacked ;; Attack
learned ;; Education_teaching
began ;; Process_start
began ;; Process_start || rescued ;; Rescuing || caught ;; Body_movement
killed ;; Killing || wounded ;; Bodily_harm
caused ;; Causation || filmed ;; Create_artwork
drove ;; Motion
shot ;; Use_firearm || dragged ;; Body_movement
support ;; Supporting || concert ;; Social_event
separate ;; Hold || promoting ;; Cause_to_make_progress || release ;; Publishing || release ;; Publishing
debuted ;; Process_start || Playing ;; Action || shows ;; Presence || release ;; Publishing
debut ;; Process_start || name ;; Name_conferral || made ;; Causation
changed ;; Change || reverting ;; Change || released ;; Publishing
form ;; Coming_to_be || performance ;; Hold || released ;; Publishing
include ;; Cause_to_be_included || remain ;; Temporary_stay || travel ;; Traveling
played ;; Action || sung ;; Expressing_publicly
lying ;; Placing || sung ;; Expressing_publicly || release ;; Publishing || released ;; Publishing
consisted ;; Cause_to_be_included || debut ;; Process_start || title ;; Name_conferral || preaching ;; Statement || reading ;; Statement
Starting ;; Process_start || performed ;; Action
contained ;; Containing || recording ;; Recording || performed ;; Action
began ;; Process_start || played ;; Action
found ;; Know
debut ;; Process_start || damage ;; Damaging || devolved ;; Change
found ;; Know || debuted ;; Process_start
found ;; Know || done ;; Causation
filmed ;; Create_artwork
Concert ;; Social_event || appear ;; Coming_to_be
created ;; Creating || produced ;; Creating || run ;; Being_in_operation || using ;; Using
set ;; Placing
Set ;; Placing
siege ;; Besieging || invasion ;; Attack
named ;; Name_conferral || identified with ;; Check
entered ;; Arriving || proceeded ;; Self_motion
retreated ;; Escaping || assembled ;; Come_together || defeated ;; Conquering || response ;; Response
reached ;; Arriving
plundering ;; Robbery || occupying ;; Control || paid ;; Commerce_pay || withdrew ;; Escaping
deployment ;; Arranging || deaths ;; Death || riots ;; Violence || rioted ;; Violence || looting ;; Robbery || arson ;; Violence
started ;; Process_start || dead ;; Killing || Protests ;; Violence || death ;; Death || shot ;; Use_firearm
ensued ;; Coming_to_be || clashes ;; Violence || destruction ;; Destroying
looting ;; Robbery || took place ;; Process_start
rioting ;; Violence || taking place ;; Process_start
violence ;; Violence
detected ;; Know || proven ;; Convincing || disturbances ;; Attack || disturbances ;; Attack || incidents ;; Attack
riot ;; Violence || proven ;; Coming_to_believe || witnessed ;; Know || disturbances ;; Attack
arrests ;; Arrest || made ;; Action || offences ;; Judgment_communication || charges ;; Judgment_communication
sat ;; Hold
linked ;; Connect || crimes ;; Committing_crime
violent ;; Violence
incurred ;; Causation || damage ;; Damaging || struggling ;; Hostile_encounter
debate ;; Communication || riots ;; Violence || generated ;; Causation
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || fought ;; Hostile_encounter
ended ;; Process_end || battle ;; Hostile_encounter || took place ;; Process_start
decided ;; Deciding || invaded ;; Attack || ascension ;; Change_of_leadership || reassert ;; Recovering || control ;; Control
conflict ;; Hostile_encounter || forcing ;; Causation || take ;; Conquering || stop ;; Preventing_or_letting || advance ;; Self_motion
battle ;; Hostile_encounter || ambushed ;; Attack || defeated ;; Conquering || plundered ;; Robbery || routed ;; Destroying
withdraw ;; Escaping || battle ;; Hostile_encounter || forced ;; Causation || control ;; Control
agreed ;; Agree_or_refuse_to_act || rule ;; Control || treaty ;; Sign_agreement || recognized ;; Agree_or_refuse_to_act
Battle ;; Hostile_encounter
command ;; Control || defeated ;; Conquering || supported ;; Supporting
first ;; Process_end || battle ;; Hostile_encounter || regarded ;; Statement || resistance ;; Defending || victory ;; Earnings_and_losses || invasion ;; Military_operation
opened ;; Openness || reached ;; Arriving
convened ;; Come_together || pledged ;; Commitment
looted ;; Robbery || plundered ;; Robbery || murdered ;; Killing || burned ;; Destroying || saw ;; Communication
Organized ;; Arranging || renounced ;; Statement || attacked ;; Attack || pledge ;; Commitment
battle ;; Hostile_encounter || lost ;; Earnings_and_losses || recaptured ;; Conquering
headed ;; Self_motion || hanged ;; Rewards_and_punishments
refers ;; Adducing || referred ;; Adducing
started ;; Process_start || claimed ;; Statement
damaging ;; Damaging || occurred ;; Coming_to_be
violence ;; Violence || broke out ;; Process_start
massacres ;; Killing
fired ;; Use_firearm || seems ;; Change_sentiment
organized ;; Arranging || destroyed ;; Destroying || suffering ;; Confronting_problem || stemmed ;; Preventing_or_letting || citing ;; Adducing || pillaged ;; Robbery || lost ;; Earnings_and_losses
formed ;; Coming_to_be || consisted ;; Forming_relationships
issued ;; Statement
carry out ;; Military_operation
revolted ;; Change_of_leadership
spread ;; Dispersal
captured ;; Conquering || plundered ;; Robbery
defeated ;; Conquering
captured ;; Conquering || pursued ;; Aiming
asserted ;; Statement || reemerged ;; Cause_to_amalgamate
reinstate ;; Recovering || force ;; Request
disrupted ;; Hindering
Battle ;; Hostile_encounter || called ;; Name_conferral
began ;; Process_start || search ;; Scrutiny || operation ;; Military_operation
ended ;; Process_end || responded ;; Response || attacked ;; Attack || search ;; Scrutiny
developed ;; Coming_to_be || battles ;; Hostile_encounter
imposed ;; Action || clashes ;; Hostile_encounter || comprised ;; Containing
moved ;; Motion || carried out ;; Action || searches ;; Scrutiny || coming ;; Motion || attack ;; Attack
searches ;; Scrutiny || caused ;; Causation || destruction ;; Destroying || fired ;; Use_firearm
complained ;; Judgment_communication || suffering ;; Catastrophe || abuse ;; Committing_crime
supplies ;; Supply || brought ;; Bringing || marched ;; Self_motion
killed ;; Killing || wounded ;; Bodily_harm || arrested ;; Arrest
wounded ;; Bodily_harm
captured ;; Conquering
looting ;; Robbery || involved ;; Cause_to_be_included
turning ;; Change
support ;; Supporting || seen ;; Perception_active || turned ;; Becoming || boosted ;; Cause_change_of_strength
ended ;; Process_end || Battle ;; Hostile_encounter || took place ;; Process_start
expansion ;; Expansion || expansion ;; Expansion || halt ;; Process_end
ended ;; Process_end || battle ;; Hostile_encounter || beginning ;; Process_start || destructive ;; Destroying || war ;; Military_operation || treaty ;; Sign_agreement
reconquered ;; Conquering || lost ;; Earnings_and_losses || treaty ;; Sign_agreement
equipped ;; Supply
battle ;; Hostile_encounter || wounded ;; Bodily_harm || captured ;; Conquering
captured ;; Conquering || married ;; Forming_relationships
participated ;; Participation || withdrew ;; Escaping || campaign ;; Military_operation || retired ;; Escaping
looting ;; Robbery || forces ;; Causation || marched ;; Self_motion || occupying ;; Control
battle ;; Hostile_encounter || switch ;; Change || assert ;; Statement || led ;; Causation
occurred ;; Presence || Burning ;; Bodily_harm
began ;; Process_start || shelling ;; Attack || landing ;; Motion || came ;; Arriving || burn ;; Bodily_harm
fled ;; Escaping || occupied ;; Control
began ;; Process_start || looting ;; Robbery || worked ;; Being_in_operation || drive ;; Motion || impede ;; Hindering || burning ;; Bodily_harm
destroyed ;; Destroying
destruction ;; Destroying || use ;; Using || completed ;; Process_end || deny ;; Agree_or_refuse_to_act
raiding ;; Attack || left ;; Departing
began ;; Process_start || overthrow ;; Change_of_leadership || support ;; Supporting
lasted ;; Process_end || enveloped ;; Filling || left ;; Departing
took ;; Conquering
response ;; Response || precipitated ;; Change
recovered ;; Recovering || end ;; Process_end || pushing ;; Motion
repel ;; Defending || hired ;; Employment
elected ;; Choosing || retreating ;; Escaping || installed ;; Change_of_leadership || signed ;; Sign_agreement
implemented ;; Action || terminated ;; Process_end || recommenced ;; Process_start
established ;; Creating
joined ;; Becoming_a_member || capture ;; Conquering
declared ;; Expressing_publicly
announcement ;; Expressing_publicly || looting ;; Robbery || murder ;; Committing_crime || followed ;; Self_motion
retook ;; Conquering || pacify ;; Influence || intervened ;; Hindering || found ;; Know
negotiations ;; Communication || intervened ;; Hindering || promote ;; Cause_change_of_position_on_a_scale
signed on ;; Sign_agreement
gave ;; Giving || deployment ;; Arranging || control ;; Control || fighting ;; Hostile_encounter || monitor ;; Scrutiny
rebels ;; Hostile_encounter || advancing ;; Cause_to_make_progress
began ;; Process_start || declared ;; Expressing_publicly || support ;; Supporting || intervene ;; Response
help ;; Assistance || renewed ;; Recovering || support ;; Supporting || defeated ;; Conquering
declared ;; Expressing_publicly
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
retreated ;; Escaping || moved ;; Motion
driven ;; Motion
leaving ;; Departing || surrendered ;; Surrendering
organized ;; Arranging || expedition ;; Traveling
sailed ;; Self_motion || landing ;; Arriving
landing ;; Arriving || settled ;; Statement || driven ;; Motion
command ;; Control || ambush ;; Attack || appeared ;; Presence || laid ;; Giving
abandoned ;; GiveUp || concealed ;; Hiding_objects || beached ;; Placing
attack ;; Attack || appeared ;; Presence || started ;; Process_start || looted ;; Robbery
inflict ;; Causation || claim ;; Statement
born ;; Bringing
raid ;; Military_operation
captured ;; Conquering || took ;; Conquering || occupation ;; Control || remained ;; Presence
ordered ;; Request || regained ;; Getting || control ;; Control || march ;; Motion_directional
took ;; Conquering || assistance ;; Assistance || assist ;; Assistance || rebelled ;; Hostile_encounter || formed ;; Coming_to_be
sent ;; Sending
took ;; Conquering || perform ;; Action || massacre ;; Killing
celebrating ;; Social_event || killed ;; Killing
plundered ;; Robbery || destroyed ;; Destroying
captured ;; Conquering || taken ;; Conquering
occupation ;; Control || rushed ;; Motion || ordered ;; Request || march ;; Motion_directional
killed ;; Killing || escape ;; Escaping || ordered ;; Request
ordered ;; Request || construction ;; Building
fled ;; Escaping
massacres ;; Killing || perpetrated ;; Committing_crime || looting ;; Robbery
affected ;; Influence
started ;; Process_start
killed ;; Killing || converted ;; Change
remained ;; Presence
obtain ;; Getting || travel ;; Traveling || permits ;; Preventing_or_letting
declarations ;; Expressing_publicly || give ;; Giving || converted ;; Change || converted ;; Change || coerced ;; Convincing
confined ;; Hindering || allowed ;; Preventing_or_letting || came ;; Motion
paid ;; Commerce_pay || pay ;; Commerce_pay || forced ;; Hostile_encounter
described ;; Statement || incidents ;; Catastrophe || organized ;; Arranging || assembly ;; Building
incidents ;; Catastrophe
described ;; Statement || attack ;; Attack
toured ;; Traveling || restore ;; Recovering || camped ;; Hostile_encounter
rehabilitated ;; Recovering || restore ;; Recovering
abandoned ;; GiveUp || accepted ;; Receiving
migrated ;; Self_motion
riots ;; Violence || occurred ;; Coming_to_be
Unrest ;; Violence || arrest ;; Arrest || began ;; Process_start || beating ;; Bodily_harm || videotaped ;; Recording || viewed ;; Perception_active
rioted ;; Violence || spread ;; Dispersal
assault ;; Attack || murder ;; Killing || damage ;; Damaging || looting ;; Robbery || arson ;; Committing_crime || occurred ;; Coming_to_be
deployed ;; Arranging || sent ;; Sending || controlling ;; Control
restored ;; Recovering || arrests ;; Arrest || killed ;; Killing || injured ;; Bodily_harm
announced ;; Expressing_publicly || attributed ;; Judgment_communication || resignation ;; Change_of_leadership
record ;; Recording
brought ;; Bringing || flooding ;; Motion || hit ;; Attack
named ;; Name_conferral || originated ;; Coming_to_be
intensified ;; Cause_change_of_strength || tracked ;; Self_motion
strengthened ;; Cause_change_of_strength || curved ;; Motion_directional || making ;; Manufacturing || landfall ;; Self_motion
storm ;; Catastrophe || dissipated ;; Dispersal
reorganized ;; Reforming_a_system
began ;; Process_start || regained ;; Recovering
attained ;; Getting || landfall ;; Self_motion
interaction ;; Connect || dissipated ;; Dispersal
alerts ;; Warning || threat ;; Warning || put ;; Placing
caused ;; Causation || landfall ;; Self_motion || flooding ;; Motion
damage ;; Damaging || widespread ;; Dispersal || affected ;; Influence
perished ;; Death
died ;; Death || mudslide ;; Catastrophe
damaged ;; Damaging || overflowed ;; Filling
evacuated ;; Removing
damage ;; Damaging || storm ;; Catastrophe
impacts ;; Influence || spread ;; Dispersal
destroyed ;; Destroying || floods ;; Motion
impacts ;; Influence
occurred ;; Presence || died ;; Death || landfall ;; Self_motion || rendered ;; Causation
declared ;; Expressing_publicly || impacts ;; Influence
Damage ;; Damaging
dispatched ;; Sending || aid ;; Assistance || relief ;; Cure
became ;; Becoming || dispatched ;; Sending || looting ;; Robbery || impacted ;; Influence || looting ;; Robbery || prevent ;; Preventing_or_letting
lost ;; Earnings_and_losses || damage ;; Damaging || exceeded ;; Cause_change_of_position_on_a_scale
fought ;; Hostile_encounter || Wars ;; Hostile_encounter
Battle ;; Hostile_encounter || destroyed ;; Destroying || engaged ;; Hostile_encounter
survive ;; Preserving || battle ;; Hostile_encounter || detached ;; Connect
carrying ;; Carry_goods || sailed ;; Self_motion || charged ;; Judgment_communication
passing ;; Motion_directional || attacked ;; Attack || intercepted ;; Preventing_or_letting
became ;; Becoming || escape ;; Escaping
exchanged ;; Exchange || engagement ;; Hostile_encounter || board ;; Temporary_stay || suffering ;; Bodily_harm || casualties ;; Death
battle ;; Hostile_encounter || outfought ;; Self_motion
surrendered ;; Surrendering || ship ;; Sending || wounded ;; Bodily_harm || ordering ;; Request || wave ;; Body_movement
looting ;; Robbery || operation ;; Military_operation || took ;; Getting || encouraged ;; Influence
warfare ;; Hostile_encounter || captured ;; Conquering || bringing ;; Bringing || established ;; Building || assist ;; Assistance || treatment ;; Cure || co-operated ;; Collaboration || forced ;; Request || denied ;; Agree_or_refuse_to_act
published ;; Publishing || conduct ;; Action || praised ;; Judgment_communication || account ;; Statement || castigated ;; Judgment_communication || exaggerated ;; Expansion
exchanged ;; Exchange || acquitted ;; Judgment_communication || recaptured ;; Conquering || control ;; Control || order ;; Request || returned ;; Arriving || seized ;; Conquering
captured ;; Conquering
raids ;; Attack || raids ;; Attack || conducted ;; Action
damaged ;; Damaging || attacked ;; Attack || resisted ;; Defending
destroyed ;; Destroying
destroyed ;; Destroying
destroyed ;; Destroying || landed ;; Arriving || plundered ;; Robbery
stopping ;; Preventing_or_letting
damage ;; Damaging || prevented ;; Preventing_or_letting || crossing ;; Motion_directional || descended on ;; Attack
War ;; Military_operation || waged ;; Hold || Battle ;; Hostile_encounter || battle ;; Hostile_encounter
battle ;; Hostile_encounter || fought ;; Hostile_encounter || called ;; Name_conferral
battle ;; Hostile_encounter || took place ;; Process_start
deployed ;; Arranging || guard ;; Patrolling || advance ;; Cause_to_make_progress
discovered ;; Know || caused ;; Causation || invading ;; Attack || attacked ;; Attack || advance ;; Cause_to_make_progress || advanced ;; Cause_to_make_progress || recoil ;; Escaping || marching ;; Motion_directional
deployed ;; Arranging || battle ;; Hostile_encounter || attack ;; Attack || alerted ;; Warning || set up ;; Placing
arrived in ;; Arriving || deployed ;; Arranging || battle ;; Hostile_encounter || attack ;; Attack || benefit ;; Receiving || deny ;; Preventing_or_letting
called ;; Name_conferral || called ;; Name_conferral || composed ;; Come_together
deployed ;; Arranging
firing ;; Use_firearm || called ;; Name_conferral || skirmish ;; Hostile_encounter
fought ;; Hostile_encounter || known ;; Know || foot ;; Connect
attacked ;; Attack || defensive ;; Defending || attack ;; Attack || fire ;; Use_firearm || propelled ;; Cause_change_of_strength || assault ;; Attack || staggered ;; Motion
forces ;; Causation || Colliding ;; Connect || threw ;; Hold || disorder ;; Action || rout ;; Earnings_and_losses
withdrawal ;; Escaping || massacred ;; Killing || fleeing ;; Escaping || mount ;; Process_start
ensued ;; Process_start
fled ;; Escaping || overturned ;; Process_end || sank ;; Motion_directional || drowning ;; Death
hiding ;; Hiding_objects || sought ;; Hold || died ;; Death
pillaging ;; Robbery || battlefield ;; Hostile_encounter || pursuit ;; Aiming || focusing ;; Aiming || pursuit ;; Aiming || end ;; Process_end || performed ;; Hold || buried ;; Placing
raided ;; Attack || Battle ;; Hostile_encounter || skirmish ;; Hostile_encounter || War ;; Hostile_encounter
advanced ;; Cause_to_make_progress
raid ;; Attack || free ;; Releasing || war ;; Hostile_encounter
destroy ;; Destroying || orders ;; Arranging
opposition ;; Hostile_encounter
stationed ;; Placing
raided ;; Attack || forces ;; Causation || Confederate ;; Collaboration || fire ;; Use_firearm || commanded ;; Control
capturing ;; Conquering || set fire ;; Damaging || looted ;; Robbery
became ;; Becoming || raid ;; Attack || explosion ;; Destroying || fire ;; Attack || exploded ;; Destroying || reopening ;; Process_start
fled ;; Escaping || fired ;; Use_firearm
regrouped ;; Cause_to_amalgamate || chased ;; Self_motion
surrendered ;; Surrendering || occupied ;; Control || returned ;; Arriving
engagement ;; Hostile_encounter || Wars ;; Hostile_encounter
cruising ;; Traveling || encountered ;; Hostile_encounter
damage ;; Damaging || combat ;; Hostile_encounter || suffering ;; Catastrophe || engaged ;; Hostile_encounter || separated ;; Forming_relationships
battle ;; Hostile_encounter || repairs ;; Recovering || resume ;; Process_start || ordered ;; Arranging || dead ;; Death || slipped ;; Motion_directional
arrived ;; Arriving || repairs ;; Recovering || approached ;; Arriving || firing ;; Use_firearm
surrendered ;; Surrendering || resistance ;; Defending || commended ;; Judgment_communication
looted ;; Robbery || brought ;; Bringing
imprisoned ;; Arrest
renamed ;; Name_conferral || deceased ;; Death
captured ;; Conquering || served ;; Supply || recaptured ;; Conquering
took place ;; Process_start || battle ;; Military_operation || led ;; Bringing
resulted in ;; Causation || victory ;; Earnings_and_losses || annihilated ;; Destroying || battle ;; Military_operation || refuge ;; Preserving || headed ;; Self_motion || seek ;; Getting
supported ;; Supporting
arrival ;; Arriving
began ;; Process_start || resist ;; Defending || flee ;; Escaping || battle ;; Hostile_encounter
drowned ;; Death || escape ;; Escaping || overloaded ;; Filling || sank ;; Motion_directional
captured ;; Conquering || looted ;; Robbery || continued ;; Hold || march ;; Conquering
Battle ;; Hostile_encounter || Uprising ;; Violence
became ;; Becoming || uprising ;; Violence || erupted ;; Process_start
departed ;; Departing || assault ;; Attack || hold ;; Hold
entered ;; Arriving || outmanoeuvre ;; Self_motion
attack ;; Attack || waited ;; Temporary_stay || came ;; Arriving
battle ;; Hostile_encounter || left ;; Departing || command ;; Control || forces ;; Military_operation || uprising ;; Violence || withdrew ;; Escaping
bought ;; Commerce_buy || plundered ;; Robbery || left ;; Departing || plundered ;; Robbery || left ;; Departing || demanding ;; Request || robbed ;; Robbery || returned ;; Giving
mounted ;; Process_start
held ;; Hold || seized ;; Conquering
attack ;; Attack || ordered ;; Arranging
took ;; Giving
depleted ;; Expend_resource || spent ;; Expend_resource || pay ;; Commerce_pay || looted ;; Robbery
surrendered ;; Surrendering || reduced ;; Cause_change_of_position_on_a_scale
took place ;; Process_start
killed ;; Killing || revolutionary ;; Change_of_leadership
destroyed ;; Destroying || looted ;; Robbery
revolution ;; Change_of_leadership || taken ;; Creating
began ;; Process_start || withdrew ;; Escaping || entered ;; Arriving || massacre ;; Killing
demanding ;; Request
dispatched ;; Sending
investigation ;; Criminal_investigation || concluded ;; Coming_to_believe
began ;; Process_start || ended ;; Process_end || riot ;; Violence || conflict ;; Violence || violent ;; Violence || provoked ;; Causation
died ;; Death || riot ;; Violence
injuries ;; Bodily_harm || injured ;; Bodily_harm || lost ;; Earnings_and_losses || attributed ;; Causation || confrontations ;; Hostile_encounter
riots ;; Violence || violence ;; Violence || named ;; Name_conferral || fatalities ;; Death
arson ;; Committing_crime || riots ;; Violence || murder ;; Killing || made it ;; Causation || combination ;; Cause_to_amalgamate || looting ;; Robbery
competition ;; Competition || changes ;; Change || caused ;; Causation || War ;; Hostile_encounter || growing ;; Cause_to_make_progress
Migration ;; Motion || settled ;; Statement
defended ;; Defending || established ;; Creating
caused ;; Causation || War ;; Hostile_encounter || housing ;; Building || frictions ;; Hostile_encounter
increased ;; Cause_change_of_position_on_a_scale || war ;; Hostile_encounter || frictions ;; Hostile_encounter || resistance ;; Defending || contributed ;; Influence
combination ;; Cause_to_amalgamate || strained ;; Causation
death ;; Death || drifted ;; Motion || came to ;; Causation || turmoil ;; Violence
arose ;; Coming_to_be || unrest ;; Violence
attacked ;; Attack || attacked ;; Attack || seeking ;; Hold || get to ;; Motion || went ;; Self_motion || employment ;; Employment
organized ;; Arranging || resist ;; Defending || protect ;; Preventing_or_letting || aid ;; Assistance || turned ;; Change
organized ;; Arranging || refused ;; Agree_or_refuse_to_act || ready ;; GetReady || riot ;; Violence || riot ;; Violence || send ;; Sending || game ;; Competition || ask ;; Request || ensured ;; Preventing_or_letting || made ;; Action || exacerbated ;; Cause_change_of_position_on_a_scale
causes ;; Causation || convened ;; Come_together || investigate ;; Criminal_investigation || issued ;; Creating || report ;; Reporting || end ;; Process_end || urged ;; Convincing
decrease ;; Cause_change_of_position_on_a_scale || discord ;; Violence || promote ;; Cause_to_make_progress
request ;; Request || riot ;; Violence || took ;; Action || quell ;; Preventing_or_letting || promote ;; Cause_to_make_progress
riots ;; Violence || plants ;; Placing || closed ;; Being_in_operation || avoid ;; Preventing_or_letting || shut down ;; Being_in_operation
elections ;; Change_of_leadership || riot ;; Violence || drew ;; Getting || influence ;; Influence
decisions ;; Deciding || effects ;; Influence || separation ;; Preventing_or_letting
fight ;; Hostile_encounter || known ;; Know || regarded ;; Know || passed ;; Motion_directional
remain ;; Presence
control ;; Control || diverted ;; Removing
pay ;; Commerce_pay || afford ;; Commerce_pay || resented ;; Judgment_communication || hire ;; Employment
express ;; Expressing_publicly || turned ;; Change
listed ;; Cause_to_be_included
proclaimed ;; Statement || enforce ;; Action || said ;; Telling
reach ;; Arriving || ransacked ;; Robbery || destroyed ;; Destroying || burned ;; Bodily_harm
changed ;; Change
left ;; Departing
Capture ;; Conquering || expedition ;; Scrutiny
forces ;; Military_operation
landed ;; Motion || occurred ;; Presence || assault ;; Attack || forces ;; Military_operation
assaulted ;; Attack || forces ;; Military_operation || overlooking ;; Perception_active
captured ;; Conquering || turned ;; Motion
opened ;; Openness || negotiations ;; Communication
adopting ;; Choosing || Admiral ;; Patrolling || rejected ;; Agree_or_refuse_to_act || insisting ;; Statement
choosing ;; Choosing || surrender ;; Surrendering || rejected ;; Agree_or_refuse_to_act
sent ;; Sending || forces ;; Military_operation || permitted ;; Preventing_or_letting || loot ;; Robbery
forces ;; Causation || approaching ;; Arriving || arrived ;; Arriving || word ;; Writing
battled ;; Hostile_encounter || fleets ;; Patrolling
damaged ;; Damaging || fleets ;; Patrolling || returned ;; Arriving
control ;; Control || returned ;; Arriving
called ;; Name_conferral || uprising ;; Change_of_leadership
died ;; Death || suggest ;; Convincing
took up arms ;; Bearing_arms || demonstrating ;; Protest || killed ;; Killing || beaten ;; Bodily_harm || raped ;; Bodily_harm || robbing ;; Robbery
ended ;; Process_end
began ;; Process_start || called ;; Name_conferral || reference ;; Adducing
test ;; Research || Support ;; Supporting
uprising ;; Change_of_leadership
took ;; Conquering || become ;; Becoming || occurred ;; Coming_to_be || protest ;; Attack || started ;; Process_start || point to ;; Reporting || contend ;; Statement || coming ;; Self_motion
defined ;; Statement
restore ;; Recovering || established ;; Building
Uprising ;; Change_of_leadership || inscribed ;; Writing
revolution ;; Change_of_leadership || establishment ;; Creating || conflict ;; Hostile_encounter || led to ;; Causation
erupted ;; Process_start || looted ;; Robbery
joined ;; Becoming_a_member || watched ;; Perception_active
Uprisings ;; Hostile_encounter
destroyed ;; Destroying || occupied ;; Control
restored ;; Recovering || taken up ;; Change_of_leadership || started ;; Process_start || committed ;; Commitment || rioting ;; Attack || talking ;; Statement
pulled out ;; GiveUp
declared ;; Expressing_publicly || voted ;; Choosing
assembled ;; Come_together
appealed ;; Request
powers ;; Influence || resulting ;; Causation
restore ;; Recovering || reconquer ;; Conquering || made ;; Action
failed ;; Earnings_and_losses
signing ;; Sign_agreement || accept ;; Agree_or_refuse_to_act
murdered ;; Committing_crime || deported ;; Removing || held ;; Defending || arrested ;; Arrest
deportation ;; Removing || killing ;; Killing || carried out ;; Action || implemented ;; Action
robbery ;; Robbery || rape ;; Committing_crime || massacre ;; Killing
came ;; Bringing
targeted ;; Aiming
moved ;; Motion || legal ;; Legality
organized ;; Arranging || acknowledged ;; Statement || genocides ;; Killing || carried out ;; Action
recognized ;; Know || genocide ;; Killing
robbery ;; Robbery
bring ;; Bringing || attacked ;; Attack
included ;; Cause_to_be_included
present ;; Presence
escaped ;; Escaping || planning ;; Arranging
recovered ;; Recovering
beaten ;; Bodily_harm || use ;; Using
injuries ;; Bodily_harm || end ;; Process_end
robbery ;; Robbery || hid ;; Hiding_objects
found ;; Know || incriminating ;; Judgment_communication || led to ;; Causation
sentenced ;; Legal_rulings
occurred ;; Coming_to_be || campaign ;; Military_operation || Battle ;; Hostile_encounter
campaigns ;; Military_operation || come ;; Arriving
send ;; Sending || weaken ;; Cause_change_of_strength || force ;; Military_operation
sent ;; Sending || complete ;; Process_end
moved ;; Motion || prepared ;; GetReady
came ;; Arriving || positioned ;; Placing || dominating ;; Control
ambushed ;; Attack || got ;; Getting || routed ;; Destroying
raided ;; Attack || loot ;; Robbery
began ;; Process_start || occupied ;; Control || seized ;; Self_motion
accused ;; Judgment_communication || abuse ;; Committing_crime || followed ;; Self_motion || impeach ;; Change_of_leadership
criticized ;; Judgment_communication || demanded ;; Request || reopening ;; Openness
controlled ;; Control || service ;; Supply || cordoned ;; Having_or_lacking_access
chose ;; Choosing
shot ;; Use_firearm || paralyzed ;; Bodily_harm
killed ;; Killing
murdered ;; Committing_crime || joined ;; Becoming_a_member || disappeared ;; Departing
damage ;; Damaging
attracted ;; Influence
inspired ;; Influence || standing ;; Placing
traveled ;; Traveling || join ;; Becoming_a_member
becoming ;; Becoming || issues ;; Creating
indicted ;; Criminal_investigation || misconduct ;; Action
stayed ;; Temporary_stay
opened ;; Openness || following ;; Self_motion || climbed ;; Self_motion || accused ;; Judgment_communication
died ;; Death
event ;; Competition || took place ;; Process_start
event ;; Competition
forming ;; Coming_to_be || commemorated ;; Social_event
match ;; Competition
defended ;; Defending
match ;; Competition
defended ;; Defending
retained ;; Preserving
defeated ;; Conquering
promoted ;; Cause_to_make_progress
defeated ;; Conquering
marked ;; Recording
became ;; Becoming || stream ;; Having_or_lacking_access
campaigns ;; Military_operation || conducted ;; Action || refer ;; Name_conferral
captured ;; Conquering || constructed ;; Building || supplemented ;; Supply || conquest ;; Conquering
regained ;; Recovering || counter-attack ;; Attack
threat ;; Warning || create ;; Creating || attempted ;; Hold
recovered ;; Recovering || defeated ;; Conquering || defeat ;; Conquering || retook ;; Control
recovered ;; Recovering || access ;; Having_or_lacking_access || ending ;; Process_end
campaigns ;; Military_operation || control ;; Control || drove ;; Motion || lost ;; Earnings_and_losses
capture ;; Conquering || demise ;; Destroying
operated ;; Being_in_operation || charter ;; Renting
entered ;; Arriving || permission ;; Having_or_lacking_access || run ;; Motion || take-off ;; Self_motion
injuring ;; Bodily_harm || killing ;; Killing || collision ;; Catastrophe || avoiding ;; Preventing_or_letting || take ;; Action || occurred ;; Presence
evacuated ;; Emptying || damaged ;; Damaging || using ;; Using || aborted ;; Process_end || take-off ;; Self_motion
written ;; Writing || service ;; Supply || repaired ;; Recovering || returned ;; Arriving
accident ;; Catastrophe || made ;; Creating
named ;; Name_conferral
explored ;; Scrutiny
determine ;; Deciding
remains ;; Presence || monitoring ;; Scrutiny
left ;; Causation
became ;; Becoming || reduced ;; Cause_change_of_position_on_a_scale || containing ;; Cause_to_be_included
began ;; Process_start || completed ;; Process_end || cleanup ;; Removing
exists ;; Presence || put ;; Placing
spreading ;; Dispersal || stated ;; Statement
putting ;; Placing
received ;; Receiving || dropped ;; Motion_directional || revealed ;; Reveal_secret
erected ;; Building || marks ;; Recording || took place ;; Process_start
accessible ;; Having_or_lacking_access
brought ;; Bringing || flooding ;; Motion
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength || moved ;; Motion || storm ;; Catastrophe
hurricane ;; Catastrophe || reached ;; Arriving
approaching ;; Arriving || storm ;; Catastrophe || intensified ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || dissipated ;; Expend_resource
brought ;; Bringing || rainfall ;; Catastrophe
caused ;; Causation || fell ;; Motion_directional || killed ;; Killing || damage ;; Damaging || remaining ;; Temporary_stay || flooding ;; Motion
hurricane ;; Catastrophe || struck ;; Attack || causing ;; Causation || damage ;; Damaging || War ;; Military_operation
Damage ;; Damaging || hurricane ;; Catastrophe || damaged ;; Damaging || destroyed ;; Destroying
caused ;; Causation || damage ;; Damaging || flooding ;; Motion || isolated ;; Having_or_lacking_access
reaching ;; Arriving || flooded ;; Catastrophe
lost ;; Earnings_and_losses || damage ;; Damaging || resulted in ;; Causation
ranked ;; Placing || cyclone ;; Catastrophe
damage ;; Damaging || deaths ;; Death || estimated ;; Scrutiny
access ;; Having_or_lacking_access || plans ;; Arranging || broke out ;; Process_start || effort ;; Influence || elected ;; Choosing || protect ;; Defending
received ;; Receiving || financing ;; Supply || armed ;; Bearing_arms || occupy ;; Control
supported ;; Supporting
decided ;; Deciding || using ;; Using || lasted ;; Process_end
control ;; Control || consult ;; Communication || signed ;; Sign_agreement || signed ;; Sign_agreement || rendered ;; Supply
adopted ;; Choosing || reaffirmed ;; Statement || control ;; Control || end ;; Process_end || called ;; Name_conferral
Battle ;; Hostile_encounter || fought ;; Hostile_encounter
battle ;; Hostile_encounter || Battle ;; Hostile_encounter
recommenced ;; Process_start || consisting ;; Cause_to_be_included
training ;; Education_teaching
starting ;; Process_start || lasting ;; Being_in_operation
forces ;; Causation || linked ;; Having_or_lacking_access
set ;; Process_end
broke out ;; Process_start
attending ;; Participation || took place ;; Process_start
killed ;; Killing || dead ;; Death || reporting ;; Reporting
interred ;; Placing
marks ;; Recording
identified ;; Check || interred ;; Placing
occurring ;; Coming_to_be || buildings ;; Building
offering ;; Supply || arose ;; Coming_to_be
conflagration ;; Catastrophe
cut off ;; Hindering || jammed ;; Having_or_lacking_access || escape ;; Escaping
flight ;; Motion || flight ;; Motion
ripped ;; Destroying || destroying ;; Destroying || flight ;; Motion || operating ;; Being_in_operation || fly ;; Motion || explosion ;; Attack
killed ;; Killing || carried ;; Bringing || injured ;; Bodily_harm
damaged ;; Damaging || loading ;; Placing || flight ;; Motion || due ;; Causation || delayed ;; Change_event_time
hidden ;; Hiding_objects || exploded ;; Attack || bomb ;; Use_firearm || boarding ;; Having_or_lacking_access
saved ;; Rescuing || delay ;; Change_event_time || bomb ;; Use_firearm
planted ;; Placing || bomb ;; Use_firearm || talks ;; Communication || concluded ;; Coming_to_believe
reported ;; Reporting || uncovered ;; Hiding_objects || suicide ;; Killing
battle ;; Hostile_encounter || fought ;; Hostile_encounter
capture ;; Conquering || cutting off ;; Having_or_lacking_access || access ;; Having_or_lacking_access || hardened ;; Cause_change_of_strength
recaptured ;; Conquering
defending ;; Defending || assault ;; Attack || connects ;; Connect || launched ;; Motion
capturing ;; Conquering || defeated ;; Conquering
attacks ;; Attack || reached ;; Achieve
increased ;; Cause_change_of_position_on_a_scale || support ;; Supporting || held ;; Hold
began ;; Process_start || damaged ;; Damaging || battle ;; Hostile_encounter || defenses ;; Defending
ended ;; Process_end || recaptured ;; Conquering || battle ;; Hostile_encounter
attacks ;; Attack || War ;; Hostile_encounter
block ;; Hiding_objects || attacked ;; Attack || conduct ;; Action || raiding ;; Attack || shipping ;; Self_motion
allowed ;; Preventing_or_letting || access ;; Having_or_lacking_access
attack ;; Attack || preventing ;; Preventing_or_letting || raiding ;; Attack || raiding ;; Attack || leaving ;; Departing || plan ;; Arranging
narrower ;; Expansion || giving ;; Giving || target ;; Aiming
provided ;; Supply || assaulting ;; Attack
assault ;; Attack || attack ;; Attack || limited ;; Limiting || garnered ;; Getting
defended ;; Defending || operation ;; Military_operation || taken ;; Action || drove ;; Self_motion || forcing ;; Hostile_encounter
attack ;; Attack || sinking ;; Motion || entrance ;; Arriving || launched ;; Motion || proved ;; Statement || close ;; Process_end
attack ;; Attack || war ;; Hostile_encounter || came to ;; Self_motion || stopped ;; Process_end
held ;; Hold
started ;; Process_start
held ;; Hold
grown ;; Expansion || include ;; Cause_to_be_included
moved ;; Motion || accessible ;; Having_or_lacking_access
preceded ;; Process_start
began ;; Process_start || ended ;; Process_end || conflict ;; Hostile_encounter
conflict ;; Hostile_encounter || resulted in ;; Causation
stated ;; Statement || stated ;; Statement || response ;; Response
attacked ;; Attack || striking ;; Attack
broke out ;; Process_start || response ;; Response
began ;; Process_start || invasion ;; Attack
given ;; Giving || access ;; Having_or_lacking_access
began ;; Process_start || operating ;; Being_in_operation
struck ;; Attack
intensified ;; Cause_change_of_strength || attacks ;; Attack
decided ;; Deciding || striking ;; Attack
ended ;; Process_end || declared ;; Expressing_publicly || announcing ;; Expressing_publicly || conflict ;; Hostile_encounter
withdrawal ;; Escaping || completed ;; Process_end
conflict ;; Hostile_encounter || decrease ;; Cause_change_of_position_on_a_scale
produced ;; Creating || recommended ;; Convincing
released ;; Publishing
targeted ;; Aiming || wrote ;; Writing
rejected ;; Agree_or_refuse_to_act
ordered ;; Request || conduct ;; Action
concluded ;; Coming_to_believe
concert ;; Social_event || held ;; Hold
concert ;; Social_event || selected ;; Choosing || paid ;; Commerce_pay
selected ;; Choosing
provided ;; Supply || directed ;; Action || produced ;; Creating || concert ;; Social_event
performed ;; Hold || joining ;; Becoming_a_member || concert ;; Social_event
recording ;; Recording || streamed ;; Having_or_lacking_access
selling ;; Commerce_sell || added ;; Cause_change_of_position_on_a_scale || concert ;; Social_event || scheduled ;; Process_start || concert ;; Social_event
sold ;; Commerce_sell || resulting in ;; Causation
held ;; Hold
published ;; Publishing || recording ;; Recording || record ;; Recording
received ;; Receiving || choice ;; Choosing || concerts ;; Social_event || praise ;; Judgment_communication || regards ;; Know || giving ;; Giving || disliked ;; Judgment_communication
Championships ;; Competition || sanctioned ;; Deciding
participated ;; Participation || competition ;; Competition || representing ;; Presence
competition ;; Competition || competition ;; Competition
played ;; Competition || games ;; Competition || tournament ;; Competition || took place ;; Process_start
final ;; Process_end || tournament ;; Competition || defeating ;; Conquering
scored ;; Getting || scored ;; Getting
written ;; Writing
finals ;; Process_end || became ;; Becoming
created ;; Creating
injuries ;; Bodily_harm || cancelled ;; Change_event_time || available ;; Having_or_lacking_access
began ;; Process_start || became ;; Becoming || created ;; Causation
found ;; Know || teams ;; Participation || hit ;; Attack
struggled ;; Confronting_problem || beat ;; Competition || took ;; Bringing
ended up ;; Process_end || playing ;; Competition
attack ;; Attack || carried out ;; Action || built ;; Building || Operation ;; Military_operation
follow-up ;; Motion || taken place ;; Process_start
called ;; Name_conferral || aim ;; Aiming || shipping ;; Self_motion || paddling ;; Self_motion || placing ;; Placing || sink ;; Removing
used ;; Using || gain ;; Getting || access ;; Having_or_lacking_access
killed ;; Killing || captured ;; Conquering || died ;; Death || raid ;; Attack || battles ;; Hostile_encounter || party ;; Participation
capture ;; Conquering || espionage ;; Committing_crime || transported ;; Sending
conquered ;; Conquering || conquer ;; Conquering || appointed ;; Change_of_leadership
conquered ;; Conquering
providing ;; Giving || access ;; Having_or_lacking_access || reconstructed ;; Recovering
joined ;; Becoming_a_member
joined ;; Becoming_a_member
given ;; Giving
capture ;; Conquering
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
commanded ;; Control || defeated ;; Conquering || forces ;; Causation
defended ;; Defending || besieging ;; Besieging
marched ;; Self_motion || gathering ;; Come_together || gathered ;; Come_together
forces ;; Causation || wars ;; Hostile_encounter || battle ;; Hostile_encounter || made ;; Causation
relieve ;; Conquering
gathered ;; Come_together || dissuaded ;; Convincing
launched ;; Military_operation || attack ;; Hostile_encounter
fight ;; Hostile_encounter || annihilated ;; Destroying || routed ;; Scrutiny || lasting ;; Process_end
abandoned ;; Escaping || losing ;; Earnings_and_losses || defeat ;; Hostile_encounter || losing ;; Earnings_and_losses
retrieved ;; Getting || loss ;; Earnings_and_losses || tried ;; Coming_to_believe || link ;; Having_or_lacking_access
conflict ;; Hostile_encounter
withdrawal ;; Escaping || conducted ;; Action || targeting ;; Aiming
violence ;; Violence || raising ;; Cause_change_of_position_on_a_scale || suppressing ;; Hindering
accused ;; Judgment_communication || conducting ;; Action
violence ;; Violence || tortured ;; Bodily_harm || killed ;; Killing || imprisoned ;; Arrest || suppressing ;; Hindering
access ;; Having_or_lacking_access || restrict ;; Hindering
confrontations ;; Violence
ended ;; Process_end || disputes ;; Hostile_encounter || victory ;; Getting
withdraw ;; Escaping || concluded ;; Process_end || War ;; Hostile_encounter || agreement ;; Sign_agreement || warfare ;; Hostile_encounter || conceded ;; Agree_or_refuse_to_act || threats ;; Warning || forfeited ;; Preventing_or_letting
devastated ;; Destroying || lost ;; Earnings_and_losses || lost ;; Earnings_and_losses || access ;; Having_or_lacking_access
coined ;; Giving
disputes ;; Quarreling
repairing ;; Self_motion || killed ;; Killing || death ;; Death || War ;; Hostile_encounter || collision ;; Hostile_encounter
collided ;; Hostile_encounter
dispute ;; Quarreling || studies ;; Research || contributed ;; Causation
applied ;; Using
died ;; Death || collided ;; Catastrophe
extends ;; Expansion
operate ;; Being_in_operation || avoid ;; Preventing_or_letting || see ;; Perception_active
operate ;; Being_in_operation || control ;; Control || required ;; Preventing_or_letting
grant ;; Commitment || transit ;; Having_or_lacking_access || going ;; Self_motion
used ;; Using || take ;; Causation
face ;; Confronting_problem
surrendered ;; Surrendering || Isolated ;; Having_or_lacking_access || command ;; Control
occurred ;; Presence
escaping ;; Escaping || capture ;; Conquering || envelopment ;; Besieging
withdrew ;; Escaping || escape ;; Escaping || open ;; Openness
moved ;; Motion || cut off ;; Removing
trek ;; Self_motion || set off ;; Departing || evaded ;; Escaping
moved ;; Motion
surrender ;; Surrendering || decided ;; Deciding || occupied ;; Control || fought ;; Quarreling
began ;; Process_start || hurricane ;; Catastrophe || landfall ;; Arriving
hurricane ;; Catastrophe || linked ;; Connect
hurricane ;; Catastrophe || hurricane ;; Catastrophe || hurricanes ;; Catastrophe || receive ;; Receiving || becoming ;; Becoming || formed ;; Coming_to_be
developed ;; Coming_to_be || formed ;; Coming_to_be || undetected ;; Having_or_lacking_access
hurricane ;; Catastrophe || becoming ;; Becoming || strengthened ;; Cause_change_of_strength || Moving ;; Self_motion || neared ;; Self_motion
hurricane ;; Catastrophe || moving ;; Self_motion || reached ;; Achieve
caused ;; Causation || landfall ;; Arriving || struck ;; Attack
moved ;; Self_motion || resulting in ;; Causation
produce ;; Creating || joined ;; Becoming_a_member
destroyed ;; Destroying || reached ;; Achieve || flooded ;; Destroying
destroying ;; Destroying || overflowed ;; Motion || breached ;; Damaging
reported ;; Reporting
caused ;; Causation || killed ;; Killing
fell ;; Motion_directional
killed ;; Killing || exceeded ;; Cause_change_of_position_on_a_scale
called ;; Name_conferral
witnessed ;; Know || duels ;; Hostile_encounter
enforced ;; Action || set ;; Placing
records ;; Recording || documented ;; Recording || participants ;; Participation || illegal ;; Legality || known ;; Know || dueling ;; Hostile_encounter || duels ;; Hostile_encounter || obscure ;; Having_or_lacking_access || fought ;; Hostile_encounter
dueling ;; Hostile_encounter || duel ;; Hostile_encounter || fought ;; Hostile_encounter || fell ;; Motion_directional || settling ;; Resolve_problem || War ;; Military_operation
held ;; Hold || championship ;; Competition
playoff ;; Competition || playoff ;; Competition
halt ;; Process_end || bid ;; Hold || came to ;; Causation
finished ;; Process_end || rebounded ;; Motion || took ;; Bringing
win ;; Getting
ended ;; Process_end || bid ;; Hold
win ;; Getting
Entering ;; Having_or_lacking_access
support ;; Supporting || tour ;; Traveling
included ;; Cause_to_be_included || tour ;; Traveling
focused ;; Aiming || tour ;; Traveling
spend ;; Expend_resource || expressed ;; Expressing_publicly || gained ;; Getting || cancelled ;; Change_event_time || regret ;; Reveal_secret || tour ;; Traveling || tour ;; Traveling
took place ;; Process_start
appear ;; Presence || cancelled ;; Change_event_time || replaced ;; Change
Festival ;; Social_event || tour ;; Traveling
purchase ;; Commerce_buy || tour ;; Traveling || tour ;; Traveling
Tour ;; Traveling || support ;; Supporting || final ;; Process_end || concert ;; Social_event || rock ;; Social_event || tour ;; Traveling
tour ;; Traveling || started ;; Process_start || planned ;; Arranging || play ;; Competition || tour ;; Traveling || Festival ;; Social_event || scheduled ;; Arranging
begin ;; Process_start || announced ;; Expressing_publicly || statement ;; Statement || fight ;; Hostile_encounter || concert ;; Social_event || festival ;; Social_event || cancellation ;; Change_event_time || tour ;; Traveling || exist ;; Presence || coming ;; Motion
statement ;; Statement || read ;; Statement || relief ;; Cure
write ;; Writing || say ;; Statement || working ;; Motion
bought ;; Commerce_buy || tickets ;; Preventing_or_letting || Apologies ;; Statement
conference ;; Social_event || fair ;; Social_event
conference ;; Social_event || Presentations ;; Expressing_publicly
organised ;; Arranging || E-Commerce ;; Commerce_sell
E-Commerce ;; Commerce_sell
merchants ;; Commerce_buy || attending ;; Participation
conference ;; Social_event || organizes ;; Arranging
member ;; Participation
capture ;; Conquering || Wars ;; Hostile_encounter || War ;; Hostile_encounter
taken ;; Conquering || fled ;; Escaping || resistance ;; Defending || leaving ;; Departing
given ;; Giving || capture ;; Conquering || achievement ;; Achieve
opening ;; Openness || procurement ;; Commerce_buy || fall ;; Conquering || victory ;; Getting
reconstruction ;; Building || overseeing ;; Scrutiny
returned ;; Arriving || leaving ;; Departing
control ;; Control || recaptured ;; Conquering || solidified ;; Cause_change_of_strength || incorporating ;; Cause_to_amalgamate
released ;; Publishing
handled ;; Hold
supported ;; Supporting
purchase ;; Commerce_buy
conflicts ;; Hostile_encounter
withdrawal ;; Escaping || eliminated ;; Removing
increased ;; Cause_change_of_position_on_a_scale
pledged ;; Commitment
arose ;; Coming_to_be || resulted in ;; Causation || occupation ;; Control
conflict ;; Hostile_encounter || brought ;; Causation
opposed ;; Agree_or_refuse_to_act || objected ;; Agree_or_refuse_to_act
determined ;; Deciding || attacked ;; Attack || contain ;; Containing || prevent ;; Preventing_or_letting || launched ;; Military_operation
acquire ;; Getting || dislodge ;; Removing || retreated ;; Escaping
escalated ;; Cause_change_of_position_on_a_scale
resulted ;; Causation
purchased ;; Commerce_buy || built ;; Building || increased ;; Cause_change_of_position_on_a_scale
withdrew ;; Escaping
ended ;; Process_end
operated ;; Being_in_operation || occurred ;; Coming_to_be || crashed ;; Catastrophe
died ;; Death
returning ;; Arriving || took off ;; Departing
received ;; Receiving
crashed ;; Catastrophe || burned ;; Bodily_harm
died ;; Death || stated ;; Statement || configured ;; Hold || lowered ;; Cause_change_of_position_on_a_scale
stated ;; Statement || crash ;; Catastrophe
landing ;; Self_motion
causes ;; Causation || attributed ;; Causation || change ;; Change
recommended ;; Convincing
allowed ;; Preventing_or_letting
bought ;; Commerce_buy
wrestling ;; Competition
inspired ;; Causation || held ;; Defending || held ;; Defending
bought ;; Commerce_buy
holds ;; Defending
made ;; Manufacturing
ended ;; Process_end || extend ;; Change_event_time
started ;; Process_start || attack ;; Attack
combined ;; Cause_to_amalgamate || fought ;; Hostile_encounter || linked ;; Connect || attack ;; Attack
suffered ;; Bodily_harm
provided ;; Supply || attempts ;; Creating || command ;; Control
capture ;; Conquering || gathered ;; Come_together
finding ;; Know || suffered ;; Bodily_harm || afford ;; Commerce_buy || stretched ;; Expansion
provide ;; Supply || Realising ;; Coming_to_believe
constructed ;; Building || assented ;; Agree_or_refuse_to_act || sensing ;; Know
taken ;; Conquering || called ;; Request || weakened ;; Cause_change_of_strength || seen ;; Perception_active
renewed ;; Recovering
detonated ;; Attack
purchased ;; Commerce_buy || abandoned ;; GiveUp || placed ;; Placing
carry out ;; Action || explosion ;; Use_firearm || controlled ;; Control
received ;; Receiving || warning ;; Warning || given ;; Giving || explosion ;; Use_firearm || evacuated ;; Emptying
caused ;; Causation || fatalities ;; Death || cuts ;; Bodily_harm
explosion ;; Use_firearm || included ;; Cause_to_be_included || broke ;; Destroying || seen ;; Know
reports ;; Reporting || happened ;; Presence || happened on ;; Presence || explosion ;; Use_firearm
commercial ;; Commerce_sell
services ;; Supply || hit ;; Attack
moved ;; Motion
employing ;; Employment
leaks ;; Self_motion || plagued ;; Bodily_harm
crashing ;; Catastrophe || underwent ;; Imposing_obligation || collapsed ;; Catastrophe
received ;; Receiving || died ;; Death || injuries ;; Bodily_harm
ongoing ;; Action || demolished ;; Destroying || lawsuits ;; Legality
purchased ;; Commerce_buy
consists ;; Cause_to_be_included || spread ;; Dispersal || broke out ;; Process_start
erupted ;; Process_start
expanded ;; Expansion
fled ;; Escaping
seen ;; Perception_active
says ;; Statement
note ;; Know
preferred ;; Choosing
respond ;; Response
fueled ;; Supply
identified ;; Check
entered ;; Arriving
established ;; Building
became ;; Becoming || displaced ;; Change
dominated ;; Control || election ;; Choosing
asserted ;; Statement || penetrated ;; Attack || supported ;; Supporting || elected ;; Choosing
elections ;; Choosing
started ;; Process_start
appeared ;; Coming_to_be || encouraged ;; Influence
upped ;; Cause_change_of_position_on_a_scale || military ;; Military_operation
effecting ;; Influence
identified ;; Check
affected ;; Influence
paid ;; Commerce_pay || sold ;; Commerce_sell
handled ;; Hold
provoked ;; Causation
involved ;; Cause_to_be_included
given ;; Giving
engage ;; Participation
attributed ;; Causation
bought ;; Commerce_buy || converted ;; Change
work ;; Employment
faced ;; Confronting_problem
caused ;; Causation
contributed ;; Giving
begun ;; Process_start || provided ;; Supply
operated ;; Being_in_operation || scheduled ;; Hold
approach ;; Arriving || flight ;; Motion || landing ;; Arriving
visibility ;; Limiting
reported ;; Reporting
approach ;; Arriving || initiated ;; Process_start
overlooks ;; Perception_active
go ;; Motion || land ;; Self_motion || end ;; Process_end
prevented ;; Preventing_or_letting || installation ;; Building || landing ;; Arriving
landing ;; Self_motion
landed ;; Self_motion
slow ;; Change || thrusting ;; Motion || braking ;; Motion || initiated ;; Process_start
began ;; Process_start || ran off ;; Motion || veer ;; Motion || end ;; Process_end || left ;; Self_motion
rolled ;; Motion || blast ;; Attack
investigation ;; Criminal_investigation || cause ;; Causation || accident ;; Catastrophe || determined ;; Deciding || precluded ;; Hindering
accident ;; Catastrophe || highlighted ;; Expressing_publicly || fatalities ;; Death
surrounded ;; Surrounding || expanded ;; Expansion
moved ;; Motion
use ;; Using
used ;; Using || kept ;; Temporary_stay || acquisition ;; Commerce_buy
surrounded ;; Surrounding || defensive ;; Defending || constructed ;; Building
dominated ;; Control || used ;; Using || observation ;; Perception_active
attacks ;; Attack || Battle ;; Hostile_encounter || attacks ;; Attack || failed ;; Earnings_and_losses
made ;; Action || retirements ;; Departing || occupied ;; Hold || retreat ;; Escaping
recaptured ;; Conquering || offensive ;; Attack || retreat ;; Escaping
recaptured ;; Conquering
purchased ;; Commerce_buy || help ;; Assistance || donations ;; Giving || preservation ;; Preserving
announced ;; Expressing_publicly || sale ;; Commerce_sell
sell ;; Commerce_sell || offered ;; Giving
strike ;; Attack || carried out ;; Military_operation || destroyed ;; Destroying
caused ;; Causation || damage ;; Damaging || damage ;; Damaging || came ;; Motion || repaired ;; Reforming_a_system
stated ;; Statement || established ;; Creating || added ;; Cause_to_be_included
purchased ;; Commerce_buy
named ;; Name_conferral || make ;; Manufacturing || viewed ;; Perception_active || designed ;; Creating
bombed ;; Attack || damaged ;; Damaging
become ;; Becoming || called ;; Name_conferral || said ;; Statement
killed ;; Killing
elections ;; Change_of_leadership || took place ;; Process_start
rebuked ;; Judgment_communication || met with ;; Response
terrorism ;; Terrorism || wrote ;; Writing || called ;; Name_conferral
destruction ;; Destroying || cited ;; Writing
debated ;; Communication || cemented ;; Connect || brought ;; Bringing || acknowledge ;; Statement || drove ;; Bringing
earthquake ;; Catastrophe || struck ;; Attack
struck ;; Attack
killed ;; Killing || injured ;; Bodily_harm || destroyed ;; Destroying || damaged ;; Damaging || damaged ;; Damaging
caused ;; Causation || deaths ;; Death
earthquakes ;; Catastrophe || damage ;; Damaging || wreaked ;; Bodily_harm
reported ;; Reporting || arrived at ;; Arriving
occurred ;; Coming_to_be || injuries ;; Bodily_harm || Damage ;; Damaging
killed ;; Killing
felt ;; Change_sentiment
reported ;; Reporting || felt ;; Change_sentiment
hit ;; Attack || damage ;; Damaging || leading to ;; Causation || terrorizing ;; Bodily_harm
became ;; Becoming || destruction ;; Destroying
warned ;; Warning || began ;; Process_start || spread ;; Dispersal || purchase ;; Commerce_buy || looking ;; Scrutiny
conflicts ;; Hostile_encounter || took place ;; Process_start
used ;; Using
became ;; Becoming || escalated ;; Cause_change_of_position_on_a_scale || disputed ;; Quarreling || purchases ;; Commerce_buy || wars ;; Hostile_encounter || conflicts ;; Hostile_encounter || triggered ;; Causation || convinced ;; Coming_to_believe || facing ;; Confronting_problem || acknowledge ;; Judgment_communication
campaigns ;; Military_operation || summoned ;; Request || mount ;; Process_start || overpower ;; Hindering
aimed ;; Aiming || opposed ;; Agree_or_refuse_to_act || campaigns ;; Military_operation || quashing ;; Removing || strengthen ;; Cause_change_of_strength
became ;; Becoming || battled ;; Hostile_encounter
allowed ;; Preventing_or_letting || abandon ;; GiveUp || use ;; Using || block ;; Hiding_objects || losses ;; Earnings_and_losses || inflict ;; Causation || withstand ;; Defending || fortified ;; Defending
used ;; Using || campaigns ;; Military_operation || fought ;; Hostile_encounter
lost ;; Earnings_and_losses || campaigns ;; Military_operation || wars ;; Hostile_encounter || exceeded ;; Cause_change_of_position_on_a_scale
broke out ;; Process_start || led to ;; Causation
spread ;; Dispersal || concluded ;; Process_end || war ;; Hostile_encounter || invasion ;; Attack || campaign ;; Social_event
fought ;; Hostile_encounter || developed ;; Cause_to_make_progress
responded ;; Response || imprison ;; Arrest || used ;; Using || confiscate ;; Rewards_and_punishments || provoked ;; Causation
fought ;; Hostile_encounter || control ;; Control || varying ;; Change || War ;; Hostile_encounter || coalition ;; Collaboration || faced ;; Confronting_problem
dominated ;; Control || successes ;; Achieve
conflict ;; Hostile_encounter || dissolution ;; Dispersal || brought ;; Causation
bought ;; Commerce_buy || invasion ;; Attack || placed ;; Placing || control ;; Control || established ;; Creating
expand ;; Expansion || existed ;; Presence || defection ;; Escaping || establish ;; Creating
became ;; Becoming || joint ;; Collaboration || Battle ;; Hostile_encounter || war ;; Hostile_encounter || successes ;; Achieve || offensive ;; Attack || turned ;; Change
extended ;; Change_event_time || culminating ;; Process_end || campaign ;; Military_operation || siege ;; Besieging
lost ;; Earnings_and_losses || war ;; Hostile_encounter || ended ;; Process_end
signed ;; Sign_agreement || gaining ;; Getting || retaining ;; Preserving
become ;; Becoming || conquest ;; Conquering || took place ;; Process_start || undermined ;; Destroying
conducted ;; Action || changing ;; Change || raids ;; Attack || establishing ;; Creating
reforming ;; Reforming_a_system || attempted ;; Hold
Conquest ;; Conquering
began ;; Process_start || invaded ;; Attack || conquest ;; Conquering
purchased ;; Commerce_buy
traversed ;; Motion_directional || subdued ;; Conquering
captured ;; Conquering
raids ;; Attack || advance ;; Cause_to_make_progress || take ;; Creating || Following ;; Motion_directional || prepared ;; GetReady
embarked ;; Participation
battle ;; Hostile_encounter || combined ;; Cause_to_amalgamate
attacked ;; Attack || repelled ;; Defending
defense ;; Defending || gathered ;; Come_together
defend ;; Defending || mount ;; Self_motion || invading ;; Attack || massed ;; Destroying
mount ;; Motion_directional || storm ;; Attack || held off ;; Defending
mount ;; Motion_directional || storm ;; Attack || counterattacked ;; Attack
killed ;; Killing || allowed ;; Preventing_or_letting || retreat ;; Escaping || capture ;; Conquering
retreated ;; Escaping || entered ;; Arriving
retreated ;; Escaping || regrouped ;; Cause_to_amalgamate
attacked ;; Attack || dead ;; Death || defeated ;; Hostile_encounter
disputed ;; Quarreling || swimming ;; Self_motion || claiming ;; Statement || wounded ;; Bodily_harm || escape ;; Escaping || drowned ;; Killing
retreated ;; Escaping || destroying ;; Destroying || remains ;; Presence || command ;; Control || left ;; Departing
use ;; Using || ruins ;; Destroying || returned ;; Motion || subduing ;; Conquering || established ;; Building
attacks ;; Attack || suffered ;; Bodily_harm
Battle ;; Hostile_encounter || defeated ;; Conquering
fight ;; Hostile_encounter || captured ;; Conquering
escaped ;; Escaping
remained ;; Presence || took ;; Getting
became ;; Becoming || known ;; Know
ended ;; Process_end || retreated ;; Escaping || escape ;; Escaping || capture ;; Conquering
requested ;; Request || contact ;; Connect || granted ;; Giving
service ;; Supply || proposed ;; Creating || come to ;; Self_motion || rejected ;; Agree_or_refuse_to_act
suffer ;; Bodily_harm || preferred ;; Choosing || staying ;; Temporary_stay
became ;; Becoming || went to ;; Self_motion
Festival ;; Social_event || festival ;; Social_event
start ;; Process_start || runs ;; Being_in_operation || date ;; Process_start || moved ;; Motion
set up ;; Building
sponsors ;; Supply || changed ;; Change || began ;; Process_start || festival ;; Social_event
included ;; Cause_to_be_included || purchase ;; Commerce_buy || festival ;; Social_event || Admission ;; Becoming_a_member
included ;; Cause_to_be_included || concerts ;; Social_event || signings ;; Sign_agreement
require ;; Imposing_obligation
occurred ;; Coming_to_be || bombing ;; Terrorism || attack ;; Terrorism
parked ;; Placing || killing ;; Killing || social ;; Social_event || exploded ;; Attack || wounding ;; Bodily_harm
explosion ;; Attack
attack ;; Terrorism
claimed ;; Statement || bombing ;; Terrorism
condemning ;; Judgment_communication || attack ;; Terrorism || adopted ;; Action
blamed ;; Judgment_communication || saying ;; Statement
inspected ;; Scrutiny || aid ;; Assistance || car bomb ;; Terrorism
linked ;; Connect || bombing ;; Attack || bombing ;; Attack || participation ;; Participation || died ;; Death
acquired ;; Getting || drove ;; Motion || bought ;; Commerce_buy || using ;; Using
bomb ;; Use_firearm || claimed ;; Statement || employed ;; Employment || knowing ;; Know || detonated ;; Committing_crime
described ;; Statement || claiming ;; Statement || denied ;; Statement || terrorism ;; Terrorism || attack ;; Attack || bomb ;; Use_firearm || planted ;; Placing
released ;; Publishing || found ;; Know || said ;; Statement || attack ;; Attack || called ;; Name_conferral
conflict ;; Hostile_encounter
captured ;; Conquering || criss-crossed ;; Motion_directional || engaged ;; Participation
prevent ;; Preventing_or_letting
clashed ;; Hostile_encounter
defied ;; Agree_or_refuse_to_act || turn ;; Motion
understood ;; Know
defeat ;; Conquering || inflicted ;; Causation
shattered ;; Destroying || returned ;; Motion
bought ;; Commerce_buy || reach ;; Arriving || losing ;; Earnings_and_losses
conduct ;; Action || withdraw ;; Removing
seized ;; Hold || claimed ;; Statement
demonstrated ;; Protest
control ;; Control || faced ;; Confronting_problem || disobedience ;; Hindering
festival ;; Social_event || Festival ;; Social_event || held ;; Defending
recognised ;; Know
recorded ;; Recording
produced ;; Manufacturing || recognised ;; Know
Festival ;; Social_event
policy ;; Legality
purchased ;; Commerce_buy || assistance ;; Assistance
built ;; Building
festival ;; Social_event
re-named ;; Name_conferral || facilitated ;; Cause_to_make_progress
called ;; Name_conferral || called ;; Name_conferral
held ;; Hold
take place ;; Process_start
burned ;; Bodily_harm || killing ;; Killing
carrying ;; Carry_goods
carrying ;; Carry_goods
arrive at ;; Arriving || carrying ;; Carry_goods
bought ;; Commerce_buy
converted ;; Change
Battle ;; Hostile_encounter || Shop ;; Commerce_buy || Battle ;; Hostile_encounter || Battles ;; Hostile_encounter || War ;; Hostile_encounter || took place ;; Process_start
converged ;; Come_together || retreating ;; Escaping
defense ;; Defending || attacks ;; Attack
saved ;; Rescuing || retreat ;; Escaping
advance ;; Motion || stopped ;; Preventing_or_letting
led ;; Causation
delayed ;; Change_event_time
driven ;; Motion || attack ;; Attack || left ;; Departing
cut off ;; Removing
established ;; Creating
produced ;; Manufacturing || event ;; Social_event
took place ;; Process_start
event ;; Social_event || took place ;; Process_start
retained ;; Earnings_and_losses || defeating ;; Hostile_encounter
retained ;; Preserving || retained ;; Preserving || retained ;; Preserving || retained ;; Preserving
received ;; Receiving || buys ;; Commerce_buy
produced ;; Creating || event ;; Social_event
held ;; Hold
show ;; Social_event || decorated ;; Filling
show ;; Social_event || seemed ;; Change_sentiment || event ;; Social_event
chose ;; Choosing || reflected ;; Change_sentiment || event ;; Social_event || shows ;; Social_event || hold ;; Hold
response ;; Response
Bash ;; Social_event || show ;; Social_event || held ;; Hold
elected ;; Choosing || show ;; Social_event
Bash ;; Social_event
turning ;; Change || marked ;; Recording
defeating ;; Conquering || made ;; Getting || debut ;; Process_start
event ;; Social_event || turned ;; Becoming || revealed ;; Check
appearance ;; Presence || final ;; Process_end || event ;; Social_event
promote ;; Cause_to_make_progress || event ;; Social_event || titled ;; Name_conferral || starred ;; Participation
purchased ;; Commerce_buy || owned ;; Earnings_and_losses
produced ;; Manufacturing || event ;; Social_event
took place ;; Hold
event ;; Social_event
matches ;; Competition || contested ;; Competition
decide ;; Deciding || match ;; Competition || contender ;; Competition || defeated ;; Conquering || retaining ;; Preserving || retaining ;; Preserving
received ;; Receiving || received ;; Receiving || buys ;; Commerce_buy || buys ;; Commerce_buy
Fair ;; Social_event
starts ;; Process_start || runs through ;; Self_motion
established ;; Building
renamed ;; Name_conferral || organized ;; Arranging || operate ;; Being_in_operation || selected ;; Choosing
purchased ;; Commerce_buy
expanded ;; Expansion || includes ;; Cause_to_be_included
starts ;; Process_start
named ;; Name_conferral || called ;; Name_conferral || headed ;; Self_motion
begun ;; Process_start
built ;; Building
called ;; Name_conferral || grown ;; Expansion
selected ;; Choosing
changed ;; Change || felt ;; Perception_active
bought ;; Commerce_buy || renamed ;; Name_conferral
grew ;; Expansion
fleeing ;; Escaping || departed ;; Departing
headed ;; Self_motion
moved ;; Motion
Battle ;; Hostile_encounter || occurred ;; Coming_to_be || control ;; Control || fought ;; Hostile_encounter
battle ;; Hostile_encounter || Wars ;; Hostile_encounter || Wars ;; Hostile_encounter || campaign ;; Military_operation || military ;; Military_operation
battle ;; Hostile_encounter || met ;; Come_together || commanded ;; Control
forced ;; Causation || withdraw ;; Escaping
creating ;; Creating || strengthen ;; Cause_change_of_strength || fighting ;; Hostile_encounter || forces ;; Military_operation || extended ;; Expansion || concentrated ;; Aiming || pulled ;; Placing
commanded ;; Control || buying ;; Commerce_buy || arrive ;; Arriving || flanked ;; Attack || intervention ;; Assistance || reinforcements ;; Cause_change_of_strength
rally ;; Military_operation || death ;; Death || trampled ;; Body_movement
driven ;; Motion
event ;; Competition || produced ;; Manufacturing
took place ;; Process_start
event ;; Competition || name ;; Name_conferral || starred ;; Participation || produced ;; Manufacturing
event ;; Competition || beginning ;; Process_start || marked ;; Recording
surrounded ;; Surrounding || event ;; Competition || match ;; Competition || fighting ;; Hostile_encounter
match ;; Competition || defeated ;; Conquering || retain ;; Preserving
defeated ;; Conquering || defeated ;; Conquering || matches ;; Competition
event ;; Competition || used ;; Using || event ;; Competition || marked ;; Recording
received ;; Receiving || event ;; Competition || grossed ;; Earnings_and_losses || sales ;; Commerce_sell || buys ;; Commerce_buy || event ;; Competition
event ;; Competition || increase ;; Cause_change_of_position_on_a_scale || helped ;; Assistance
event ;; Competition || released ;; Publishing || reached ;; Achieve
held ;; Hold
held ;; Hold
attracts ;; Influence || promoting ;; Cause_change_of_position_on_a_scale || certified ;; Check
performances ;; Social_event
includes ;; Cause_to_be_included
include ;; Cause_to_be_included || performing ;; Hold
showcases ;; Placing
shopping ;; Commerce_buy || include ;; Cause_to_be_included
operated ;; Being_in_operation || production ;; Manufacturing || operate ;; Being_in_operation || hires ;; Employment
fought ;; Hostile_encounter
sent ;; Sending
met ;; Come_together
scattered ;; Dispersal || destroyed ;; Destroying || led ;; Conquering
fled ;; Escaping || wounded ;; Bodily_harm
chose ;; Choosing || withdraw ;; Escaping
died ;; Death || reversed ;; Change
forged ;; Creating || converted ;; Change
runs ;; Hold
founded ;; Creating || presenting ;; Presence || offer ;; Supply
encouraged ;; Influence
starting ;; Process_start || funded ;; Supply || held ;; Hold
providing ;; Supply
providing ;; Supply
helped ;; Assistance || followed ;; Motion
gained ;; Getting || sought ;; Motion
Funding ;; Supply || established ;; Creating
began ;; Process_start
started ;; Process_start
started ;; Process_start
organised ;; Arranging || festivals ;; Social_event
Festival ;; Social_event || contributed ;; Giving
founded ;; Creating || handed over ;; Giving || contributing ;; Giving || run ;; Self_motion
celebrated ;; Social_event || included ;; Cause_to_be_included
included ;; Cause_to_be_included || set ;; Placing || led ;; Causation
presented ;; Presence || presenting ;; Presence || performed ;; Hold
presented ;; Presence
crashed ;; Catastrophe || operated ;; Control || flying ;; Self_motion
due ;; Causation || disaster ;; Catastrophe || blizzard ;; Catastrophe || deviated ;; Change
crashed ;; Catastrophe || carrying ;; Bringing
killed ;; Killing || crash ;; Catastrophe
disaster ;; Catastrophe || succumbed ;; Death || injuries ;; Bodily_harm
crash ;; Catastrophe || disaster ;; Catastrophe
cause ;; Causation || accident ;; Catastrophe || investigation ;; Criminal_investigation || established ;; Creating
tour ;; Traveling || referred ;; Name_conferral
created ;; Creating || Conceived ;; Creating || produced ;; Creating || promoted ;; Cause_to_make_progress || run ;; Hold
toured ;; Traveling || tour ;; Traveling
collected ;; Come_together || suggested ;; Convincing || donation ;; Giving || required ;; Imposing_obligation || raised ;; Cause_change_of_position_on_a_scale
tour ;; Traveling || tour ;; Traveling
decisions ;; Deciding || reported ;; Reporting
outpaced ;; Cause_change_of_position_on_a_scale
fought ;; Hostile_encounter || ended ;; Process_end || state ;; Statement || War ;; Hostile_encounter
included ;; Cause_to_be_included || War ;; Hostile_encounter
run ;; Control
fought ;; Hostile_encounter || war ;; Hostile_encounter
began ;; Process_start || marks ;; Recording || building ;; Building || close ;; Process_end
said ;; Communication || adopt ;; Choosing || war ;; Hostile_encounter || prompted ;; Causation
Games ;; Competition || Games ;; Competition || games ;; Competition || Games ;; Competition || event ;; Social_event || took place ;; Process_start || hosted ;; Social_event
games ;; Competition || hosted ;; Social_event
edition ;; Arranging || include ;; Cause_to_be_included || sports ;; Competition
commenced ;; Process_start || games ;; Competition || events ;; Social_event || held ;; Hold
participated ;; Participation
opened ;; Process_start
final ;; Process_end || Hosts ;; Social_event || followed ;; Motion || led ;; Getting
games ;; Competition || games ;; Competition || records ;; Recording || broken ;; Conquering
cost ;; Cost || games ;; Competition || competition ;; Competition || games ;; Competition || rising ;; Cause_change_of_position_on_a_scale || promotion ;; Achieve || commitment ;; Commitment || hosting ;; Social_event || deemed ;; Coming_to_believe
election ;; Change_of_leadership || staged ;; Hold
started ;; Process_start || personnel ;; Participation || followed ;; Action || making ;; Arriving || taken ;; Bringing
arrested ;; Arrest
reform ;; Reforming_a_system || help ;; Assistance || said ;; Statement || ran ;; Control || established ;; Creating
signed ;; Sign_agreement || selected ;; Choosing || election ;; Change_of_leadership || condemnation ;; Judgment_communication || sanctions ;; Rewards_and_punishments || led to ;; Causation
postponed ;; Change_event_time || election ;; Change_of_leadership || aborted ;; Process_end
tasked ;; Request
held ;; Hold || called ;; Name_conferral
held ;; Hold || run ;; Control
held ;; Hold
held ;; Hold
ran out of ;; Expend_resource
ended up ;; Process_end
competed ;; Competition
open ;; Openness || competition ;; Competition
Participants ;; Participation
participants ;; Participation
compete ;; Competition
aired ;; Expressing_publicly || aired ;; Expressing_publicly || televised ;; Communication
given ;; Giving || armed ;; Bearing_arms || War ;; Hostile_encounter || conflict ;; Hostile_encounter || name ;; Name_conferral
cease-fire ;; Process_end || hostilities ;; Hostile_encounter || broke out ;; Process_start
planted ;; Placing || explosives ;; Attack || blew ;; Attack
used ;; Using || shoulder-launched ;; Motion || conflict ;; Hostile_encounter
used ;; Using || flying ;; Motion
took over ;; Getting || rising ;; Cause_change_of_position_on_a_scale || capture ;; Conquering || marked ;; Recording || War ;; Hostile_encounter || success ;; Achieve
control ;; Control || War ;; Hostile_encounter || end ;; Process_end
recurring ;; Coming_to_be || originating ;; Coming_to_be
held ;; Hold
moving ;; Motion || keeping ;; Temporary_stay || festival ;; Social_event || inception ;; Arranging
returned ;; Motion
attending ;; Participation || attending ;; Participation || festivals ;; Social_event
acquired ;; Getting
stated ;; Statement || integrate ;; Cause_to_amalgamate
convention ;; Social_event || convention ;; Social_event || organized ;; Arranging
convention ;; Social_event
used ;; Using
created ;; Creating || convention ;; Social_event
called ;; Name_conferral || convention ;; Social_event || run ;; Hold
accident ;; Catastrophe || occurred ;; Coming_to_be
operations ;; Control || supervising ;; Scrutiny
winds ;; Motion || run-off ;; Self_motion || fading ;; Departing
blown out ;; Damaging || meet ;; Come_together || run ;; Motion
occurred ;; Coming_to_be || approached ;; Arriving || drawn ;; Motion || fouled ;; Hindering || collision ;; Attack
waving ;; Motion || slacken ;; Cause_change_of_position_on_a_scale || signalling ;; Warning || ran ;; Motion
ran ;; Motion || fell ;; Motion || derailed ;; Causation
demolished ;; Destroying || remained ;; Temporary_stay
killed ;; Killing || injured ;; Bodily_harm
accident ;; Catastrophe || comment ;; Statement || advised ;; Request || seemed ;; Change_sentiment || pinned ;; Placing
accident ;; Catastrophe || damaged ;; Damaging || shows ;; Statement
Festival ;; Social_event || event ;; Social_event || held over ;; Process_start
competitions ;; Competition || event ;; Social_event
festival ;; Social_event
upgraded ;; Cause_to_make_progress || festival ;; Social_event
capacity ;; Building
take place ;; Process_start
competition ;; Competition || built ;; Building || competitions ;; Competition || takes place ;; Process_start
events ;; Competition || open ;; Openness || sporting ;; Competition || include ;; Cause_to_be_included
event ;; Social_event
event ;; Social_event || program ;; Arranging
crashed ;; Catastrophe || operated ;; Control || land ;; Arriving
crashed ;; Damaging
crash ;; Catastrophe || perished ;; Death || fire ;; Catastrophe
died ;; Death
indicated ;; Statement || provide ;; Supply || run ;; Hold || reports ;; Reporting
held ;; Defending
marked ;; Recording || inauguration ;; Social_event
administered ;; Control
held ;; Defending || open ;; Openness
remains ;; Temporary_stay
inauguration ;; Social_event || take place ;; Process_start || established ;; Building || take place ;; Hold
become ;; Becoming || resignation ;; Change_of_leadership
became ;; Becoming || held ;; Defending || elected ;; Choosing
engagement ;; Hostile_encounter || took place ;; Process_start || War ;; Military_operation || Battle ;; Hostile_encounter
ended ;; Process_end || battle ;; Hostile_encounter || occurred ;; Coming_to_be
consisted ;; Cause_to_be_included || belligerents ;; Hostile_encounter || command ;; Control
capturing ;; Conquering || blown up ;; Destroying || dismasted ;; Destroying || made ;; Change || driving ;; Hold || ran ;; Control
use ;; Using || failed ;; Earnings_and_losses || deliver ;; Bringing || blow ;; Attack
merged ;; Cause_to_amalgamate || battle ;; Hostile_encounter || War ;; Military_operation || War ;; Military_operation
Battle ;; Hostile_encounter || forces ;; Military_operation || fought ;; Hostile_encounter
invaded ;; Attack || aid ;; Assistance || ruler ;; Control || called ;; Request
arrived ;; Arriving || cross ;; Motion_directional || engaged ;; Participation || managing ;; Control || rearguard ;; Collaboration || retreated ;; Escaping
attacked ;; Attack || sent ;; Sending || captured ;; Conquering || crossed ;; Motion_directional
rescue ;; Rescuing || casualties ;; Catastrophe || came to ;; Motion || suffered ;; Earnings_and_losses
opened ;; Openness || campaign ;; Military_operation || control ;; Control || failure ;; Earnings_and_losses || meant ;; Statement
withdrawal ;; Escaping || resulted in ;; Causation
began ;; Process_start || intensified ;; Cause_change_of_strength
conducted ;; Action || fired ;; Use_firearm || armed ;; Bearing_arms
quell ;; Hindering
clashes ;; Hostile_encounter || played ;; Competition
defended ;; Defending || defected ;; Escaping || ambushed ;; Attack || built ;; Building
sent ;; Sending || following ;; Self_motion
supply ;; Supply || launched ;; Process_start || shelling ;; Attack || blocking ;; Hiding_objects
withdraw ;; Escaping || forcing ;; Hostile_encounter || launched ;; Process_start
following ;; Self_motion || fighting ;; Hostile_encounter || shelling ;; Attack || occurred ;; Coming_to_be
control ;; Control
fighting ;; Hostile_encounter || control ;; Control || held ;; Defending
captured ;; Conquering || control ;; Control || leaving ;; Departing
reinforced ;; Cause_change_of_strength || arrived ;; Arriving || launched ;; Process_start
forced ;; Hostile_encounter
moved ;; Motion || fighting ;; Hostile_encounter
captured ;; Conquering
allowed ;; Preventing_or_letting || following ;; Self_motion || control ;; Control || reached ;; Arriving || leaving ;; Departing
conflict ;; Hostile_encounter || conflict ;; Hostile_encounter
fought ;; Hostile_encounter
present ;; Presence
increased ;; Cause_change_of_position_on_a_scale || led to ;; Causation
include ;; Cause_to_be_included
claimed ;; Statement
violent ;; Violence
perpetrated ;; Committing_crime
reported ;; Reporting || died ;; Death
led to ;; Causation || said ;; Statement
becoming ;; Becoming
began ;; Process_start || sending ;; Sending
control ;; Control
decreased ;; Cause_change_of_position_on_a_scale
razed ;; Destroying || casualties ;; Catastrophe
fled ;; Escaping
ceased ;; Process_end
attacked ;; Attack
departed ;; Departing || flight ;; Self_motion || chartered ;; Renting
crashed ;; Catastrophe || resulting in ;; Causation || deaths ;; Death
flight ;; Self_motion || climbed ;; Motion_directional
contacted ;; Communication || become ;; Becoming || respond ;; Response || impaired ;; Confronting_problem || instructions ;; Communication || speech ;; Expressing_publicly
contact ;; Communication || intercepted ;; Preventing_or_letting
flying ;; Self_motion || crashing ;; Catastrophe || heading ;; Motion || running out of ;; Expend_resource
crash ;; Catastrophe || became ;; Becoming || known ;; Know
become ;; Becoming || hypoxia ;; Hindering || suffering ;; Bodily_harm || meaning ;; Causation || investigation ;; Criminal_investigation || concluded ;; Coming_to_believe || operate ;; Control
began ;; Process_start || flight ;; Self_motion || crashed ;; Catastrophe || end ;; Process_end || starved ;; Expend_resource
said ;; Statement || report ;; Reporting || damage ;; Damaging || due ;; Causation || investigation ;; Criminal_investigation || investigators ;; Criminal_investigation || used ;; Using || conclude ;; Coming_to_believe
final ;; Process_end || report ;; Reporting || determine ;; Deciding || incapacitated ;; Hindering
crash ;; Catastrophe || recommendations ;; Convincing || made ;; Action
founded ;; Creating || encourage ;; Influence
applied ;; Using || promoting ;; Cause_change_of_position_on_a_scale || look ;; Perception_active
linked ;; Connect || involved ;; Participation || arranged ;; Arranging || appeared ;; Coming_to_be || festival ;; Social_event
associated ;; Collaboration || run ;; Hold
provide ;; Supply || provides ;; Supply
formed ;; Coming_to_be || networked ;; Connect || festival ;; Social_event
attend ;; Participation
beaten ;; Bodily_harm || chased ;; Self_motion || found ;; Know
achieved ;; Achieve || reported ;; Reporting
stood ;; Placing
revived ;; Cure
released ;; Releasing
reported ;; Reporting || marked ;; Recording
observed ;; Perception_active || said ;; Communication
determined ;; Deciding || referred ;; Adducing
called ;; Telling
told ;; Telling
arrested ;; Arrest
expressed ;; Expressing_publicly
took place ;; Process_start || suffered ;; Bodily_harm
condemned ;; Judgment_communication
festival ;; Social_event
took place ;; Process_start
founded ;; Creating || managing ;; Control
included ;; Cause_to_be_included
gave ;; Giving
included ;; Cause_to_be_included
start ;; Process_start || race ;; Competition || run ;; Control || race ;; Competition
win ;; Getting || giving ;; Giving || race ;; Competition
reported ;; Reporting || record ;; Recording || attendance ;; Participation
increase ;; Cause_change_of_position_on_a_scale || winning ;; Earnings_and_losses || payout ;; Earnings_and_losses
held ;; Hold
ran ;; Control || relaunched ;; Recovering
took place ;; Process_start
took place ;; Process_start
Festival ;; Social_event || known ;; Know || presenting ;; Presence
continue ;; Process_start
conjunction ;; Collaboration || run ;; Control
Festival ;; Social_event || supports ;; Supporting || culminate ;; Process_end
festival ;; Social_event || promote ;; Cause_to_make_progress
skirmish ;; Hostile_encounter || War ;; Military_operation || fought ;; Hostile_encounter
shot ;; Use_firearm || ambush ;; Attack || set up ;; Attack
captured ;; Conquering || established ;; Building || forces ;; Causation || campaign ;; Military_operation
killed ;; Killing || ambush ;; Attack || operating ;; Control
concealed ;; Hiding_objects || preventing ;; Preventing_or_letting || recovery ;; Rescuing || buried ;; Filling
confirmation ;; Coming_to_believe || establishment ;; Creating || led to ;; Causation
reconnaissance ;; Scrutiny || reduced ;; Cause_change_of_position_on_a_scale || crossing ;; Motion_directional || Making ;; Manufacturing || casualty ;; Death || success ;; Earnings_and_losses || success ;; Earnings_and_losses || resulted in ;; Causation || contributing ;; Causation
operated ;; Being_in_operation
broke ;; Destroying || open ;; Openness || accident ;; Catastrophe || operating ;; Being_in_operation || referred ;; Adducing
caused ;; Causation || control ;; Control || control ;; Control || led to ;; Causation || jammed ;; Motion || restricted ;; Hindering
caused ;; Causation || control ;; Control || deflect ;; Motion
causing ;; Causation || control ;; Control || shut down ;; Being_in_operation || severed ;; Destroying
control ;; Control || rupture ;; Destroying
restricted ;; Hindering || landing ;; Arriving || apply ;; Using || pressure ;; Causation
approach ;; Arriving || prevent ;; Preventing_or_letting || becoming ;; Becoming || landing ;; Motion
combined ;; Cause_to_amalgamate || using ;; Using || left ;; Departing || thrust ;; Motion
controls ;; Control || land ;; Arriving || return ;; Arriving || injuries ;; Bodily_harm
cause ;; Causation || latch ;; Motion || latching ;; Motion
changes ;; Change || avoid ;; Preventing_or_letting || instituted ;; Creating || repeat ;; Causation
causing ;; Causation || open ;; Openness || control ;; Control || go out ;; Self_motion || crash ;; Catastrophe
crash ;; Catastrophe || crash ;; Catastrophe || killed ;; Killing || crash ;; Catastrophe || disaster ;; Catastrophe
conducted ;; Action
named ;; Name_conferral
drive ;; Control
ensure ;; Preventing_or_letting || conduct ;; Action || elections ;; Choosing
included ;; Cause_to_be_included
hosted ;; Social_event
held ;; Hold
Begun ;; Process_start || inspired ;; Convincing
ranks ;; Placing
organized ;; Arranging
compete ;; Competition
viewed ;; Scrutiny
established ;; Creating
Concert ;; Social_event || concert ;; Social_event || recording ;; Recording
performed ;; Hold || driven ;; Motion || concert ;; Social_event
taken ;; Conquering
performed ;; Hold || concert ;; Social_event
included ;; Cause_to_be_included
added ;; Cause_to_be_included || concert ;; Social_event || demands ;; Request || tourism ;; Traveling || concert ;; Social_event || scheduled ;; Arranging
received ;; Receiving || concert ;; Social_event || plagued ;; Bodily_harm || maintain ;; Preserving || control ;; Control || performing ;; Hold
revealed ;; Reveal_secret || held ;; Defending || made ;; Manufacturing || concert ;; Social_event || concert ;; Social_event || collect ;; Come_together || record ;; Recording
resulting in ;; Causation || War ;; Military_operation || war ;; Military_operation
remain ;; Presence || displaced ;; Escaping
war ;; Military_operation
war ;; Military_operation
run ;; Control
reinforced ;; Cause_change_of_strength || link ;; Connect || mandate ;; Giving || structure ;; Building
opposed ;; Agree_or_refuse_to_act
shifting ;; Change || contributed ;; Influence
linked ;; Connect || War ;; Hostile_encounter || preceded ;; Process_start || sided ;; Communication || sided ;; Communication
began ;; Process_start || formed ;; Coming_to_be || Fighting ;; Hostile_encounter
shifted ;; Change || fighting ;; Hostile_encounter
became ;; Becoming || fought ;; Hostile_encounter || involved ;; Cause_to_be_included || war ;; Hostile_encounter
stationed ;; Placing || keeping ;; Temporary_stay
beginning ;; Process_start || end ;; Process_end || fighting ;; Hostile_encounter || marked ;; Recording
began ;; Process_start || conflict ;; Hostile_encounter || formulate ;; Creating || appointed ;; Deciding
crimes ;; Committing_crime || pardoned ;; Rewards_and_punishments
began ;; Process_start || dissolved ;; Dispersal || rebuild ;; Building
remained ;; Presence || war ;; Military_operation
intensified ;; Cause_change_of_strength
draw ;; Connect
conceived ;; Creating
commenced ;; Process_start
conducted ;; Action
engaged ;; Forming_relationships
withdrawn ;; Escaping
replaced ;; Change_of_leadership || remained ;; Temporary_stay
lost ;; Earnings_and_losses
tied ;; Connect
support ;; Supporting || concert ;; Social_event
Beginning ;; Process_start || visited ;; Traveling
began ;; Process_start || command ;; Control || concerts ;; Social_event || offered ;; Giving
designed ;; Creating || marked ;; Recording || engage ;; Collaboration
cancelled ;; Change_event_time
become ;; Becoming || support ;; Supporting
fought ;; Hostile_encounter
normalized ;; Change
invaded ;; Attack || blocked ;; Hiding_objects
forced ;; Request || guaranteed ;; Commitment
deployed ;; Arranging
became ;; Becoming
reiterated ;; Statement
announced ;; Expressing_publicly || be closed to ;; Having_or_lacking_access || mobilized ;; Influence
debate ;; Communication || caused ;; Causation
caught ;; Self_motion || destroyed ;; Destroying
caught ;; Self_motion
conquered ;; Conquering || inflicted ;; Causation || rushed ;; Self_motion
entered ;; Arriving || defense ;; Defending || began ;; Process_start
envisioned ;; Creating || prevent ;; Preventing_or_letting
attacks ;; Judgment_communication || repelled ;; Defending || ordered ;; Request
agreed ;; Agree_or_refuse_to_act || signed ;; Writing || agreed ;; Agree_or_refuse_to_act
killed ;; Killing || losing ;; Earnings_and_losses || crippled ;; Destroying
seized ;; Hold
improved ;; Cause_to_make_progress || standing ;; Self_motion
humiliated ;; Bodily_harm || reinstated ;; Recovering || leading ;; Causation
led to ;; Causation || contributing ;; Giving || defense ;; Defending
fled ;; Escaping
fled ;; Escaping || expelled ;; Removing || going ;; Motion
festival ;; Social_event || held ;; Hold
include ;; Cause_to_be_included
conceived ;; Creating || held ;; Hold || featuring ;; Cause_to_be_included
expanded ;; Expansion
contains ;; Containing || covering ;; Filling
built ;; Building
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
origin ;; Coming_to_be || battle ;; Hostile_encounter || cited ;; Adducing
died ;; Death || making ;; Manufacturing || battles ;; Hostile_encounter || argue ;; Protest || fought ;; Hostile_encounter || forged ;; Causation
became ;; Becoming || invasion ;; Military_operation || violated ;; Legality || defeated ;; Hostile_encounter || launched ;; Process_start
join ;; Participation || sailed ;; Motion
killed ;; Killing || Battle ;; Hostile_encounter || came up ;; Motion || recounts ;; Statement
preserved ;; Preserving
wrote ;; Writing || consolidated ;; Cause_change_of_strength
battle ;; Hostile_encounter || battle ;; Military_operation || called ;; Name_conferral
proposed ;; Creating || battle ;; Hostile_encounter
called ;; Name_conferral || named ;; Name_conferral || joint ;; Collaboration || Operation ;; Military_operation
disrupt ;; Hindering || elections ;; Choosing
carried out ;; Action
targets ;; Aiming
conceived ;; Creating || operational ;; Being_in_operation || deployment ;; Arranging
utilized ;; Using || opening ;; Process_start
used ;; Using || surveillance ;; Scrutiny || reconnaissance ;; Scrutiny || tasked ;; Request || known ;; Know || fell ;; Motion_directional
sent ;; Sending || fired ;; Use_firearm || killed ;; Killing || killing ;; Killing || shot down ;; Use_firearm
became ;; Becoming || rescued ;; Rescuing || lasted ;; Process_end || recovered ;; Recovering
achieved ;; Achieve || aided ;; Assistance || gained ;; Getting || regrouped ;; Cause_to_amalgamate || returned ;; Arriving
wounded ;; Bodily_harm
killed ;; Killing
engagement ;; Hostile_encounter || Wars ;; Hostile_encounter || defeated ;; Conquering || captured ;; Conquering
damaging ;; Damaging || prevent ;; Preventing_or_letting || entering ;; Arriving || leaving ;; Departing || eliminating ;; Removing || war ;; Hostile_encounter || blockade ;; Besieging || trade ;; Exchange || designed ;; Creating || formed ;; Becoming || cutting ;; Removing
reinforce ;; Cause_change_of_strength || supplies ;; Supply
discovered ;; Know || enter ;; Arriving || leave ;; Departing || tasked ;; Confronting_problem || cleared ;; Emptying || watching ;; Scrutiny || intercepting ;; Preventing_or_letting
battle ;; Hostile_encounter || chased ;; Motion || brought ;; Bringing || preventing ;; Preventing_or_letting || using ;; Using || gunfire ;; Use_firearm || closing ;; Openness || boarding ;; Temporary_stay || overwhelm ;; Destroying
killed ;; Killing || lasted ;; Process_end || storm ;; Attack || battle ;; Hostile_encounter || suffered ;; Bodily_harm || capture ;; Conquering
helped ;; Assistance || arrived ;; Arriving || attracted ;; Influence || gunfire ;; Use_firearm || journey ;; Motion || secure ;; Getting
battle ;; Hostile_encounter || shortages ;; Cause_change_of_position_on_a_scale || supplies ;; Supply || isolated ;; Hindering || suffering ;; Bodily_harm
surrounded ;; Surrounding || invaded ;; Attack || captured ;; Conquering || arrived in ;; Arriving || blockade ;; Besieging || broke ;; Destroying
blockade ;; Besieging || communications ;; Communication || seized ;; Conquering || cut off ;; Hindering
campaign ;; Military_operation || capturing ;; Conquering
devised ;; Creating
described ;; Statement
claimed ;; Statement
condemned ;; Judgment_communication || managed ;; Preventing_or_letting
published ;; Publishing
rejected ;; Agree_or_refuse_to_act
gave ;; Giving
studied ;; Education_teaching
judged ;; Deciding
destroyed ;; Destroying
destroyed ;; Destroying
turned ;; Becoming || records ;; Recording
discovered ;; Know
found ;; Know
engaged ;; Participation
proposed ;; Creating
conducted ;; Action || aimed ;; Aiming || create ;; Creating || ending ;; Process_end
emerged ;; Coming_to_be || defend ;; Defending || failure ;; Earnings_and_losses || attack ;; Attack
gained ;; Getting || defend ;; Defending
conducted ;; Action || took ;; Conquering
characterised ;; Judgment_communication
declared ;; Expressing_publicly || debate ;; Quarreling
penetrate ;; Attack || reorganised ;; Reforming_a_system || designed ;; Creating
weakening ;; Cause_change_of_strength || remain ;; Presence || resolve ;; Resolve_problem || carry out ;; Military_operation || characterised ;; Judgment_communication
aid ;; Assistance || conflict ;; Hostile_encounter || made ;; Manufacturing || escalate ;; Cause_change_of_position_on_a_scale
began ;; Process_start || bombing ;; Attack
admitted ;; Agree_or_refuse_to_act
ended ;; Process_end || declared ;; Expressing_publicly
agreement ;; Statement || conflict ;; Hostile_encounter || accepted ;; Receiving
declared ;; Expressing_publicly || end ;; Process_end
proposed ;; Creating || invasion ;; Attack || plan ;; Arranging
surrendered ;; Surrendering || canceled ;; Change_event_time || planned ;; Arranging || operation ;; Military_operation || bombings ;; Attack || invasion ;; Attack || war ;; Hostile_encounter
begin ;; Process_start || capture ;; Conquering || used ;; Using || captured ;; Conquering || Set ;; GetReady
invasion ;; Attack || planned ;; Arranging
support ;; Supporting || captured ;; Conquering || allow ;; Preventing_or_letting
taken place ;; Process_start
defensive ;; Defending || invasion ;; Attack || invasion ;; Attack || plans ;; Arranging || plan ;; Arranging || adjust ;; Change
defense ;; Defending || planned ;; Arranging || defense ;; Defending
varied ;; Change
resisted ;; Defending || invasion ;; Attack || ran ;; Self_motion || casualties ;; Death
experiment ;; Research || held ;; Hold || promote ;; Cause_to_make_progress
held ;; Hold
passing ;; Motion_directional || offer ;; Giving
asked ;; Request || pass ;; Giving
held ;; Hold
held ;; Hold
planned ;; Arranging || give ;; Giving
given ;; Giving || increase ;; Cause_change_of_position_on_a_scale || make ;; Causation || think ;; Coming_to_believe
invented ;; Creating || give ;; Giving
asked ;; Request || give ;; Giving || services ;; Supply || charge ;; Commerce_pay
visit ;; Traveling || tweeted ;; Connect || left ;; Placing
event ;; Social_event
quoted ;; Statement || describing ;; Statement || growing ;; Expansion || remains ;; Temporary_stay || saying ;; Statement || promote ;; Cause_to_make_progress || growing ;; Cause_change_of_position_on_a_scale
killing ;; Killing || fire ;; Use_firearm || took place ;; Process_start || ordered ;; Request
injured ;; Bodily_harm
walled ;; Surrounding
fleeing ;; Escaping || fire ;; Use_firearm || blocked ;; Hindering || exhausted ;; Expend_resource
declared ;; Expressing_publicly || punish ;; Rewards_and_punishments || dispel ;; Dispersal
stay ;; Temporary_stay || die ;; Death || dead ;; Death || aid ;; Assistance || wounded ;; Bodily_harm || condemned ;; Judgment_communication || lay ;; Placing
convinced ;; Convincing || banned ;; Preventing_or_letting || meetings ;; Social_event || take place ;; Process_start
disseminated ;; Dispersal || celebrate ;; Social_event || festival ;; Social_event || arrest ;; Arrest || gathered ;; Come_together || protest ;; Judgment_communication
exhausted ;; Expend_resource || directing ;; Aiming || flee ;; Escaping || warning ;; Warning || raised ;; Cause_change_of_position_on_a_scale || supply ;; Supply || entered ;; Arriving || blocking ;; Hindering || took up ;; Conquering
stated ;; Statement || killed ;; Killing || report ;; Reporting || hear ;; Perception_active
fired ;; Use_firearm
published ;; Publishing || offered ;; Giving || criticised ;; Judgment_communication || identified ;; Check || wounded ;; Bodily_harm || report ;; Reporting || dead ;; Death || injured ;; Bodily_harm || compile ;; Scrutiny || quoted ;; Adducing
injured ;; Bodily_harm || dead ;; Death
became ;; Becoming || lauded ;; Judgment_communication || benefiting ;; Earnings_and_losses
criticised ;; Judgment_communication || censured ;; Judgment_communication || investigation ;; Criminal_investigation
orders ;; Arranging || murder ;; Killing || acting ;; Action
bring ;; Bringing || removed ;; Removing || proposed ;; Creating || chose ;; Choosing || turned down ;; Becoming
died ;; Death || retired ;; Process_end || returned ;; Arriving
Responses ;; Response
declared ;; Expressing_publicly
giving ;; Giving || murderers ;; Killing || shocked ;; Change_sentiment || stated ;; Statement
caused ;; Causation || led ;; Causation || note ;; Know || carried out ;; Military_operation
control ;; Control || retrained ;; Recovering || developed ;; Coming_to_be
loss ;; Earnings_and_losses || resulting in ;; Causation
inquiry ;; Criminal_investigation || widespread ;; Dispersal
end ;; Process_end || consider ;; Coming_to_believe || rule ;; Control
Tour ;; Traveling || tour ;; Traveling || concert ;; Social_event
decorated ;; Filling || resemble ;; Change_sentiment
covering ;; Filling || saw ;; Self_motion
became ;; Becoming || tour ;; Traveling || designed ;; Creating
start ;; Process_start || lifted ;; Motion || shows ;; Social_event || lift ;; Body_movement
played ;; Action || effects ;; Influence || coming up ;; Arriving
tour ;; Traveling || play ;; Motion || replacing ;; Change
named ;; Name_conferral || improve ;; Cause_to_make_progress
opening ;; Openness
selling ;; Commerce_sell
revolt ;; Change_of_leadership || sovereign ;; Change_of_leadership || War ;; Hostile_encounter || War ;; Hostile_encounter
regained ;; Recovering || control ;; Control || deployed ;; Arranging || rebelling ;; Violence
resistance ;; Defending
oust ;; Change_of_leadership || established ;; Creating
included ;; Cause_to_be_included || beginnings ;; Process_start || conceived ;; Creating || war ;; Hostile_encounter || war ;; Hostile_encounter || carrying ;; Bringing
start ;; Process_start || Truce ;; Process_end || recognized ;; Know
Hostilities ;; Hostile_encounter || War ;; Hostile_encounter || broke out ;; Process_start
end ;; Process_end || reached ;; Arriving || recognised ;; Know
beginning ;; Process_start
death ;; Death || conquest ;; Conquering || conquests ;; Conquering
commenced ;; Process_start || losing ;; Earnings_and_losses || remaining ;; Presence || conquest ;; Conquering || concluded ;; Process_end
invaded ;; Attack || warring ;; Hostile_encounter || concluding ;; Process_end || laid ;; Placing || control ;; Control || conquest ;; Conquering || defeat ;; Conquering || Battle ;; Hostile_encounter || split ;; Motion
spread ;; Dispersal || expeditions ;; Motion_directional || furthering ;; Cause_change_of_position_on_a_scale
added ;; Cause_to_be_included || raids ;; Attack || growing ;; Expansion || receive ;; Receiving || range ;; Cause_to_be_included || succeeded ;; Change_of_leadership || invasions ;; Attack
defeated ;; Conquering
invasion ;; Attack
invasions ;; Attack || come ;; Motion || accounts ;; Statement || written in ;; Writing
documenting ;; Recording || describes ;; Statement || view ;; Perception_active || shown ;; Presence || written ;; Writing || illustrating ;; Statement
began ;; Process_start || Beginning ;; Process_start || conquest ;; Conquering || construct ;; Creating || finalised ;; Creating
reaching ;; Arriving || copied ;; Writing || interpolated ;; Creating
differs ;; Change || giving ;; Giving || conflicting ;; Hostile_encounter
given ;; Giving
supports ;; Supporting || cited ;; Adducing
occurred ;; Coming_to_be || bombings ;; Terrorism
explosions ;; Use_firearm || explosions ;; Use_firearm || went off ;; Motion
occurred ;; Coming_to_be || explosions ;; Use_firearm
explosions ;; Use_firearm || reported ;; Reporting
dead ;; Death || confirmed ;; Check || injured ;; Bodily_harm
unreported ;; Reveal_secret || exploded ;; Use_firearm
exploded ;; Use_firearm
ordered ;; Request || cancelled ;; Preventing_or_letting
confessed ;; Reveal_secret || arrested ;; Arrest || claimed ;; Statement
bombings ;; Terrorism || claimed ;; Statement
blamed ;; Judgment_communication || coup ;; Change_of_leadership
denied ;; Agree_or_refuse_to_act
admitted ;; Statement || claim ;; Statement
found ;; Know
terrorism ;; Terrorism
terrorists ;; Terrorism || shrugged ;; Judgment_communication || suggestions ;; Judgment_communication
identify ;; Check || attacks ;; Attack || meeting ;; Communication
announced ;; Expressing_publicly || designs ;; Creating || saying ;; Communication || used ;; Using || inspection ;; Check || indicated ;; Coming_to_believe
arrested ;; Arrest
claimed ;; Statement
bombings ;; Terrorism || determined ;; Deciding
used ;; Using || used ;; Using
hired ;; Employment || connected ;; Connect || claiming ;; Statement || downplayed ;; Judgment_communication
given ;; Giving || uprising ;; Change_of_leadership || supported ;; Supporting || revolution ;; Change_of_leadership || war ;; Military_operation || war ;; Military_operation || assumed ;; Choosing
movement ;; Change_of_leadership || unbound ;; Sign_agreement || ruled ;; Control
overthrowing ;; Change_of_leadership || preventing ;; Preventing_or_letting || coup ;; Change_of_leadership || eroding ;; Cause_change_of_strength || inauguration ;; Change_of_leadership || affected ;; Influence || enjoyed ;; Getting
events ;; Social_event || end ;; Process_end || marked ;; Recording
appointed ;; Deciding
headed ;; Motion || elected ;; Choosing || revolution ;; Change_of_leadership || press ;; Request || abide by ;; Imposing_obligation || kept from ;; Hindering || adopt ;; Action || taking ;; Conquering
speculated ;; Coming_to_believe || movement ;; Change_of_leadership || revolutionary ;; Change_of_leadership || grew ;; Cause_to_make_progress || came to ;; Motion || overthrow ;; Change_of_leadership || secession ;; Removing || developed ;; Coming_to_be || advocate ;; Convincing
used ;; Using || rest ;; Placing || alleged ;; Statement || turn ;; Change || noted ;; Statement || broadcasting ;; Statement
commenced ;; Process_start || uprising ;; Change_of_leadership || killed ;; Killing
started ;; Process_start || killed ;; Killing || called ;; Request || movement ;; Change_of_leadership || wake ;; Response
shot ;; Use_firearm || died ;; Death || victim ;; Catastrophe
rebelled ;; Change_of_leadership
war ;; Military_operation
revolt ;; Change_of_leadership || support ;; Supporting || crushed ;; Destroying
fighting ;; Hostile_encounter || dead ;; Death || damage ;; Damaging || fighting ;; Hostile_encounter || surrender ;; Surrendering || report ;; Reporting || deaths ;; Death || suffered ;; Bodily_harm
assembly ;; Building || election ;; Change_of_leadership || enactment ;; Creating || defeat ;; Conquering || granted ;; Giving
established ;; Building || growing ;; Expansion || left ;; Departing || called ;; Name_conferral || closed ;; Process_end || enacted ;; Creating
beginning ;; Process_start || revolution ;; Change_of_leadership || marks ;; Recording
known ;; Know || Revolution ;; Change_of_leadership || consider ;; Deciding
conflict ;; Change_of_leadership || revolt ;; Change_of_leadership || occurring ;; Presence
proposed ;; Creating || Operation ;; Military_operation || operation ;; Military_operation
proposals ;; Statement || put together ;; Placing
target ;; Aiming || plan ;; Arranging || war ;; Hostile_encounter || investigation ;; Criminal_investigation || created ;; Creating || party ;; Participation || party ;; Participation || inspired ;; Influence
help ;; Assistance || planned ;; Arranging
surveillance ;; Scrutiny || illegal ;; Legality || included ;; Cause_to_be_included || carried out ;; Action
abandoned ;; GiveUp || break ;; Destroying || control ;; Control || devising ;; Creating || plan ;; Arranging || party ;; Participation || passed ;; Giving
avoided ;; Preventing_or_letting || plan ;; Arranging || led to ;; Causation
fought ;; Hostile_encounter || battle ;; Hostile_encounter
occurred ;; Coming_to_be
designed ;; Creating || advancing ;; Cause_to_make_progress
attacked ;; Attack || achieving ;; Achieve
forcing ;; Hostile_encounter || held ;; Defending
attacked ;; Attack
stalled ;; Hindering
advance ;; Cause_to_make_progress || prevented ;; Preventing_or_letting || capture ;; Conquering || captured ;; Arrest
became ;; Becoming || set ;; Placing || called ;; Name_conferral || made ;; Manufacturing
festival ;; Social_event || held ;; Hold || claims ;; Statement
begins ;; Process_start || involving ;; Cause_to_be_included || ends ;; Process_end
make ;; Manufacturing || participate ;; Participation || weaves ;; Creating || dates ;; Presence
festival ;; Social_event
gain ;; Getting || require ;; Using || select ;; Choosing
foundation ;; Coming_to_be || ends ;; Process_end
includes ;; Cause_to_be_included
represent ;; Presence
presented ;; Presence || founded ;; Creating
proposed ;; Statement || protest ;; Response || demonstration ;; Protest || grant ;; Supply || increasing ;; Cause_change_of_position_on_a_scale || unemployment ;; Employment || caused ;; Causation || cuts ;; Removing || took place ;; Process_start
protest ;; Response || Organized ;; Arranging || protesters ;; Dispersal || described ;; Communication
protestors ;; Dispersal || travelling ;; Self_motion
walk ;; Self_motion || protestors ;; Dispersal || took ;; Conquering
injuries ;; Bodily_harm || made ;; Creating || wounded ;; Bodily_harm || engaged ;; Participation || arrests ;; Arrest || protestors ;; Dispersal || sustaining ;; Preserving
arrested ;; Arrest || damage ;; Damaging || breach ;; Committing_crime || charged ;; Imposing_obligation
made ;; Creating || occupied ;; Control || protestors ;; Dispersal || Department ;; Collaboration || complaints ;; Statement || led to ;; Causation || march ;; Self_motion || students ;; Becoming_a_member || end ;; Process_end
marched ;; Self_motion || wondered ;; Coming_to_believe
said ;; Communication || protest ;; Response || taken ;; Bringing
occurred ;; Coming_to_be
held ;; Defending || abandon ;; GiveUp || obligated ;; Imposing_obligation
positioned ;; Placing
move ;; Motion || allowed ;; Preventing_or_letting
began ;; Process_start || constructing ;; Building || built ;; Building || expanding ;; Expansion
reached ;; Arriving || invested ;; Besieging
stopped ;; Process_end || completing ;; Process_end
assembled ;; Come_together || relieve ;; Cause_to_make_progress
retreated ;; Escaping || attacked ;; Attack || repelled ;; Defending
siege ;; Besieging || launched ;; Process_start || resumed ;; Process_start || grew ;; Expansion || repulsed ;; Defending
signed ;; Sign_agreement
sent ;; Sending || surrender ;; Surrendering || ordering ;; Request
marched ;; Self_motion
handed over ;; Giving || recognition ;; Know
consent ;; Commitment || detonated ;; Attack || required ;; Imposing_obligation
killed ;; Killing || armed ;; Bearing_arms || use ;; Using || bombings ;; Use_firearm || remain ;; Presence
prepared ;; GetReady
preceded ;; Process_start || firebombing ;; Attack || undertaking ;; Action || devastated ;; Destroying
signed ;; Sign_agreement || war ;; Hostile_encounter || concluded ;; Coming_to_believe || turned ;; Change
called ;; Request || declaration ;; Expressing_publicly || destruction ;; Destroying || alternative ;; Choosing || prompt ;; Preventing_or_letting
war ;; Hostile_encounter || continued ;; Motion
deliver ;; Military_operation || equipped ;; Supply || specialized ;; Education_teaching || group ;; Come_together || produced ;; Manufacturing
used ;; Using || issued ;; Statement
modified ;; Change || dropped ;; Motion_directional
dropped ;; Motion_directional
devastated ;; Destroying || bombs ;; Use_firearm
killed ;; Killing || deaths ;; Death || occurred ;; Coming_to_be || effects ;; Influence
injuries ;; Bodily_harm || compounded ;; Cause_to_amalgamate || die ;; Death || burns ;; Bodily_harm || continued ;; Motion || effects ;; Influence
dead ;; Death
surrendered ;; Surrendering || declaration ;; Expressing_publicly || bombing ;; Attack
ended ;; Process_end || signed ;; Sign_agreement
debate ;; Communication || social ;; Social_event || legal ;; Legality || effects ;; Influence || studied ;; Research
drowned ;; Killing || crossing ;; Motion_directional || training ;; Education_teaching
occur ;; Coming_to_be || expeditions ;; Traveling || made ;; Causation || death ;; Death
trekking ;; Traveling || training ;; Education_teaching || got ;; Getting || crossing ;; Motion_directional
washed ;; Motion || slipped ;; Self_motion
search ;; Know || rescue ;; Rescuing || died ;; Death || airlifted ;; Bringing
death ;; Death || investigation ;; Criminal_investigation || concluded ;; Coming_to_believe
recommended ;; Convincing || criticised ;; Judgment_communication || adjourned ;; Change_event_time || reconsider ;; Deciding || charges ;; Imposing_obligation || investigation ;; Criminal_investigation || inquest ;; Criminal_investigation
reported ;; Reporting || inquest ;; Criminal_investigation || charges ;; Imposing_obligation || press ;; Request || resumed ;; Process_start
verdict ;; Legal_rulings || inquest ;; Criminal_investigation || hearing ;; Perception_active || concluded ;; Statement
gave ;; Giving || verdict ;; Legal_rulings || recommendations ;; Convincing
training ;; Education_teaching || suggestions ;; Convincing || receive ;; Receiving
died ;; Death || airlifted ;; Removing
stated ;; Statement || event ;; Social_event || organises ;; Arranging || proceed ;; GetReady
abandoned ;; GiveUp || death ;; Death
modified ;; Change || carry ;; Bringing || event ;; Social_event || use ;; Using || allow ;; Preventing_or_letting
genocide ;; Killing || War ;; Hostile_encounter || genocides ;; Killing
conducted ;; Action || massacred ;; Killing || allied ;; Collaboration || unarmed ;; Bearing_arms || fleeing ;; Escaping || relocated ;; Departing || attacks ;; Attack
genocide ;; Killing || genocides ;; Killing || took place ;; Process_start
genocide ;; Killing || classified ;; Check || campaign ;; Military_operation || genocide ;; Killing || took place ;; Process_start
killings ;; Killing || differing ;; Change || asserting ;; Statement || regards ;; Judgment_communication
deport ;; Escaping || orders ;; Request
slain ;; Killing || attacks ;; Attack || incorporated ;; Cause_to_amalgamate
massacres ;; Killing || carried out ;; Action
increased ;; Cause_change_of_position_on_a_scale || starvation ;; Death || death ;; Death || disease ;; Bodily_harm || abuse ;; Bodily_harm
death ;; Death || varied ;; Change
accepts ;; Receiving || providing ;; Supply || deaths ;; Death || death ;; Death || treaty ;; Imposing_obligation || reported ;; Reporting
killing ;; Killing || deaths ;; Death || adds ;; Cause_to_be_included
reached ;; Arriving || genocide ;; Killing || campaign ;; Military_operation
passing ;; Motion_directional || referred ;; Cause_to_be_included
genocides ;; Killing || endorsed ;; Agree_or_refuse_to_act || repudiation ;; Statement || leading ;; Causation
loss ;; Earnings_and_losses || resulted in ;; Causation
occurred ;; Coming_to_be || heading ;; Motion || rounded ;; Motion || carrying ;; Carry_goods
using ;; Using
caused ;; Causation || rise ;; Cause_change_of_position_on_a_scale
conducted ;; Action
opened ;; Openness
heard ;; Perception_active || took place ;; Process_start
censured ;; Judgment_communication || alerting ;; Warning || required ;; Imposing_obligation
given ;; Giving || concluded ;; Coming_to_believe || forgotten ;; GiveUp
criticised ;; Judgment_communication || stop ;; Process_end || reduced ;; Cause_change_of_position_on_a_scale || aware ;; Perception_active || observe ;; Perception_active
use ;; Using
offered ;; Giving || used ;; Using || showing ;; Presence
Open ;; Openness || Championship ;; Competition || Championship ;; Competition || held ;; Hold
Championship ;; Competition || stroke ;; Motion
competitors ;; Competition || reduce ;; Conquering || players ;; Competition || final ;; Process_end || entering ;; Becoming_a_member || introduced ;; Choosing
final ;; Process_end || making ;; Action || playing ;; Competition || played ;; Competition || provision ;; Imposing_obligation
change ;; Change || distribution ;; Dispersal
given ;; Giving || unchanged ;; Temporary_stay
scored ;; Getting
players ;; Competition || made ;; Action
failed ;; Agree_or_refuse_to_act
scored ;; Getting
scored ;; Getting || maintain ;; Preserving
shared ;; Getting
reached ;; Arriving || finished ;; Process_end
reached ;; Arriving
dropped ;; Motion || Taking ;; Bringing || reached ;; Arriving
putted ;; Hold
reached ;; Arriving || came back ;; Competition || finished ;; Process_end
drop ;; Motion_directional || took ;; Conquering
captured ;; Conquering || engagement ;; Hostile_encounter || Wars ;; Hostile_encounter || chased ;; Self_motion
sent ;; Sending || break ;; Destroying || destroying ;; Destroying || mission ;; Arranging || blockade ;; Besieging || carry ;; Carry_goods
supplies ;; Supply || return ;; Arriving || improve ;; Cause_to_make_progress
patrolling ;; Patrolling || sighted ;; Know
action ;; Response || chased ;; Self_motion || bring ;; Bringing
surrendering ;; Surrendering || engagement ;; Hostile_encounter || damaged ;; Damaging || casualties ;; Bodily_harm
commissioned ;; Imposing_obligation || remained ;; Temporary_stay
recaptured ;; Conquering || capture ;; Conquering || destroyed ;; Destroying || intercepted ;; Hold || defeated ;; Conquering
associated ;; Collaboration
fined ;; Judgment_communication || investigations ;; Criminal_investigation || convicted ;; Legality
dismissed ;; Agree_or_refuse_to_act
appeal ;; Request || using ;; Using || sentenced ;; Legal_rulings
released ;; Releasing || overturned ;; Change
pleaded ;; Request
received ;; Receiving || associated ;; Collaboration || commissioned ;; Imposing_obligation || detailed ;; Telling
sentenced ;; Legal_rulings
arrested ;; Arrest || acquitted ;; Response
finding ;; Know || detained ;; Arrest || suspected ;; Judgment_communication
confirmed ;; Check || found ;; Know || charged ;; Legality
Released ;; Releasing || required ;; Imposing_obligation || return ;; Arriving || hearing ;; Criminal_investigation
performance ;; Hold || joking ;; Communication || displayed ;; Action
faced ;; Confronting_problem || trial ;; Criminal_investigation || judge ;; Deciding || set ;; Placing
drugs ;; Bodily_harm || show ;; Presence || prove ;; Convincing
cross-examination ;; Scrutiny || defense ;; Defending || raised ;; Cause_change_of_position_on_a_scale
trial ;; Criminal_investigation || acquitting ;; Legal_rulings || charges ;; Judgment_communication || deliberated ;; Judgment_communication || returning ;; Giving
trial ;; Criminal_investigation || incident ;; Catastrophe || proved ;; Convincing
told ;; Telling || arrest ;; Arrest || using ;; Using || use ;; Using || convinced ;; Coming_to_believe
stated ;; Statement || warned ;; Warning || planted ;; Placing || believed ;; Know || drugs ;; Bodily_harm || planned ;; Arranging || flying ;; Motion
carried ;; Bringing
revealed ;; Reveal_secret || prevent ;; Preventing_or_letting || going out ;; Self_motion || bribed ;; Hold
crashed ;; Catastrophe
operating ;; Being_in_operation || involved ;; Cause_to_be_included
crashed ;; Catastrophe || approach ;; Arriving
perished ;; Death
crash ;; Catastrophe || crash ;; Catastrophe
published ;; Publishing || found ;; Know
allowed ;; Preventing_or_letting || initiating ;; Process_start || lost ;; Earnings_and_losses || descend ;; Motion_directional || concluded ;; Coming_to_believe
published ;; Publishing || conducted ;; Action
Hurricane ;; Catastrophe || reached ;; Arriving
storm ;; Catastrophe || hurricane ;; Catastrophe
storm ;; Catastrophe || upgraded ;; Cause_to_make_progress
formed ;; Coming_to_be
weakened ;; Cause_change_of_strength
moved ;; Motion || strengthened ;; Cause_change_of_strength
became ;; Becoming || weakened ;; Cause_change_of_strength || moved ;; Motion
brought ;; Bringing || responsible ;; Imposing_obligation
caused ;; Causation || damage ;; Damaging || spawned ;; Motion
caused ;; Causation
began ;; Process_start
differing ;; Change || views ;; Perception_active
argue ;; Quarreling || change ;; Change || reform ;; Reforming_a_system
organized ;; Arranging || engage ;; Participation
requiring ;; Imposing_obligation
register ;; Recording
requests ;; Request || meet ;; Come_together
gathered ;; Come_together || present ;; Presence
turned ;; Becoming || clash ;; Violence || demonstration ;; Social_event
injured ;; Bodily_harm || arrested ;; Arrest
demonstrations ;; Social_event || demonstration ;; Social_event || proved ;; Convincing
died ;; Death || routed ;; Motion
travelling ;; Traveling
passed ;; Motion_directional
formed ;; Coming_to_be
occurred ;; Coming_to_be
travelling ;; Traveling || noticed ;; Know
applied ;; Using || reduce ;; Cause_change_of_position_on_a_scale
struck ;; Attack
derailed ;; Self_motion
died ;; Death
found ;; Know || forgotten ;; GiveUp
braked ;; Self_motion || realised ;; Know
found ;; Know
requested ;; Request
sent ;; Sending || slowed ;; Change_event_time
determined ;; Deciding
permitted ;; Preventing_or_letting
formed ;; Coming_to_be || taken out ;; Removing || replaced ;; Change_of_leadership
required ;; Imposing_obligation
left ;; GiveUp
began ;; Process_start
fighting ;; Hostile_encounter || led ;; Causation || led ;; Causation
spread ;; Dispersal
conduct ;; Action
accused ;; Judgment_communication
heads ;; Communication
reports ;; Reporting || responsible ;; Imposing_obligation || bomb ;; Attack
organized ;; Arranging
recruited ;; Becoming_a_member || included ;; Cause_to_be_included
signed up ;; Sign_agreement || died ;; Death
arrested ;; Arrest || crossed ;; Motion_directional
used ;; Using
started ;; Process_start || raised ;; Cause_change_of_position_on_a_scale
brought ;; Bringing || killed ;; Killing
placed ;; Placing || lost ;; Death || expressing ;; Expressing_publicly
illegal ;; Legality || raised ;; Cause_change_of_position_on_a_scale || proclaiming ;; Statement || supported ;; Supporting
attended ;; Participation
confirmed ;; Check || give ;; Giving
reported ;; Reporting
obtained ;; Getting || projected ;; Coming_to_be
measured ;; Imposing_obligation || commissioned ;; Request
met with ;; Response
caused ;; Causation
began ;; Process_start || ended ;; Process_end
raise ;; Earnings_and_losses || levied ;; Imposing_obligation || wedding ;; Forming_relationships
sent ;; Sending || negotiate ;; Communication
asked ;; Request || added ;; Cause_to_be_included || provide ;; Supply || granted ;; Giving || pay ;; Cost
refused ;; Agree_or_refuse_to_act
threaten ;; Preventing_or_letting
join ;; Participation
built ;; Building || decided ;; Deciding || ceased ;; Process_end || dedicated ;; Giving || free ;; Releasing || borne ;; Bringing || claimed ;; Statement || lift ;; Process_end
persuaded ;; Convincing || attack ;; Attack || seizing ;; Hold
raided ;; Attack || assumed ;; Receiving
started ;; Process_start
fled ;; Escaping || attacked ;; Attack || making ;; Manufacturing
regaining ;; Recovering || entered ;; Arriving || took ;; Conquering
celebrate ;; Social_event || returned ;; Arriving
found ;; Know || reinforced ;; Cause_change_of_strength || returned ;; Arriving || regained ;; Getting
gained ;; Achieve || entrusted ;; Giving || rebelled ;; Attack
replaced ;; Change_of_leadership || used ;; Using
ambushed ;; Attack || suffering ;; Bodily_harm
fighting ;; Hostile_encounter || rebelled ;; Attack || turned on ;; Becoming
send ;; Sending || defeated ;; Earnings_and_losses
obtained ;; Getting || helped ;; Assistance || employing ;; Employment || held ;; Defending
attacked ;; Attack || failed ;; Earnings_and_losses || capture ;; Conquering
lost ;; Earnings_and_losses || recognising ;; Know || leading to ;; Causation || leading to ;; Causation
hold ;; Defending
organised ;; Arranging || contest ;; Competition
used ;; Using || beginning ;; Process_start || singing ;; Perception_active
selected ;; Choosing
applied ;; Using || voting ;; Choosing
voting ;; Choosing || carried ;; Imposing_obligation
used ;; Using || select ;; Choosing || contest ;; Competition
targeted ;; Aiming || moving ;; Motion || support ;; Supporting
initiated ;; Process_start || created ;; Creating
impede ;; Hindering || flow ;; Motion
begun ;; Process_start || Bombing ;; Attack
bombing ;; Attack || decided ;; Deciding || stepped up ;; Cause_change_of_strength || increasing ;; Cause_change_of_position_on_a_scale || increased ;; Cause_change_of_position_on_a_scale || operating ;; Being_in_operation || presence ;; Presence
combined ;; Cause_to_amalgamate || renamed ;; Name_conferral
infiltrated ;; Self_motion || analysts ;; Scrutiny
launched ;; Motion
end ;; Process_end || flown ;; Self_motion
strikes ;; Attack || strikes ;; Attack || supplemented ;; Supply
shot down ;; Use_firearm
implemented ;; Action || flying ;; Self_motion
strikes ;; Attack || accounted ;; Statement
flow ;; Motion || close ;; Self_motion || war ;; Hostile_encounter || supplies ;; Supply || slowed ;; Change_event_time || required ;; Imposing_obligation
occurred ;; Coming_to_be || shooting ;; Use_firearm
shot ;; Use_firearm || killed ;; Killing
fleeing ;; Escaping || shooting ;; Use_firearm || report ;; Reporting || surfaced ;; Coming_to_be || charged ;; Imposing_obligation
shooting ;; Use_firearm || generating ;; Creating || led ;; Causation || motivated ;; Influence
investigated ;; Research
conducted ;; Action || investigations ;; Scrutiny
indicted ;; Criminal_investigation
released ;; Releasing
ended ;; Process_end || due ;; Causation
pleaded ;; Request || returned ;; Arriving
murder ;; Killing || dropped ;; Motion_directional
determining ;; Deciding || murder ;; Committing_crime || sentenced ;; Legal_rulings
took place ;; Process_start || Battle ;; Hostile_encounter
aided ;; Assistance || fought ;; Hostile_encounter
battle ;; Hostile_encounter
defeated ;; Conquering || marking ;; Recording
sent ;; Sending || overthrow ;; Removing || response ;; Response
burning ;; Bodily_harm || capturing ;; Conquering || forced ;; Motion || retreat ;; Escaping
swore ;; Commitment || response ;; Response
saying ;; Communication || shot ;; Use_firearm
charged ;; Imposing_obligation
battle ;; Hostile_encounter
began ;; Process_start || crushed ;; Bodily_harm || revolt ;; Hostile_encounter
sent ;; Sending || attacks ;; Attack
besiege ;; Besieging || capture ;; Conquering
sailed ;; Motion
joined ;; Cause_to_amalgamate || blocking ;; Hindering
sent ;; Sending || asking ;; Request
gave ;; Giving || arrived in ;; Arriving
chose ;; Choosing || prevented ;; Preventing_or_letting
ordered ;; Request || forces ;; Causation
reinforced ;; Cause_change_of_strength
broke ;; Bodily_harm || slaughtered ;; Killing
marked ;; Recording || retreated ;; Escaping
began ;; Process_start || revolted ;; Change_of_leadership || postponing ;; Change_event_time || raising ;; Causation
began ;; Process_start || died ;; Death || restarted ;; Process_start
wars ;; Hostile_encounter || beaten ;; Bodily_harm || wars ;; Hostile_encounter || begun ;; Process_start || seen ;; Traveling
raised ;; Building
influential ;; Influence || seen ;; Traveling
occurred ;; Presence || demanding ;; Request || triggering ;; Causation || demolition ;; Destroying || constructed ;; Building || March ;; Social_event
demanding ;; Request || movement ;; Social_event
observed ;; Perception_active || march ;; Self_motion
supported ;; Supporting
providing ;; Supply || charge ;; Imposing_obligation
gathered ;; Come_together || participate ;; Participation
addressed ;; Expressing_publicly || commenced ;; Process_start || gathering ;; Come_together
walked ;; Self_motion
continued ;; Motion
fought ;; Hostile_encounter || campaign ;; Hostile_encounter
imposed ;; Imposing_obligation
sent ;; Sending || seize ;; Conquering
occupied ;; Control || capture ;; Conquering
finding ;; Know || besieged ;; Besieging || reached ;; Arriving || regained ;; Getting || fought ;; Hostile_encounter
surrendered ;; Surrendering || returned ;; Arriving || help ;; Assistance || fought ;; Hostile_encounter || defeated ;; Conquering
sent ;; Sending || surrendered ;; Surrendering
sent ;; Sending || recaptured ;; Conquering || left ;; GiveUp
survive ;; Preserving || defeat ;; Conquering
sent ;; Sending || recapture ;; Conquering || capitulation ;; Surrendering || surrender ;; Surrendering
became ;; Becoming || overran ;; Conquering
sent ;; Sending || surrender ;; Surrendering || landed ;; Arriving
reinforced ;; Cause_change_of_strength || stationed ;; Placing || protect ;; Defending
send ;; Sending || decided ;; Deciding || capture ;; Conquering || recapture ;; Conquering || loss ;; Earnings_and_losses || aware ;; Perception_active || nationalized ;; Reforming_a_system
comprised ;; Forming_relationships || command ;; Control
knew ;; Know || sailed ;; Self_motion
warned ;; Warning || reinforced ;; Cause_change_of_strength
suffering ;; Bodily_harm || leading to ;; Causation || took ;; Conquering
deciding ;; Deciding || arrival ;; Arriving || abandon ;; GiveUp || sail ;; Self_motion
discovered ;; Know
surrendered ;; Surrendering || revolt ;; Hostile_encounter
joining ;; Becoming_a_member || taken ;; Conquering
force ;; Request || cancellation ;; Removing
died ;; Death || faced ;; Confronting_problem || return ;; Arriving
dereliction ;; GiveUp || laid ;; Placing || conviction ;; Imposing_obligation
attacked ;; Attack || returned ;; Arriving
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || fought ;; Hostile_encounter
battle ;; Hostile_encounter || taking place ;; Process_start || Battles ;; Hostile_encounter || War ;; Military_operation
took part ;; Conquering || using ;; Using
Battles ;; Hostile_encounter || struck ;; Attack || Campaign ;; Military_operation || sailed ;; Self_motion || landed ;; Self_motion
fended ;; Defending || slowing ;; Hindering || take ;; Motion
called ;; Name_conferral || Battles ;; Hostile_encounter || counterattacks ;; Attack || wounded ;; Bodily_harm || took ;; Getting || launched ;; Process_start
attacks ;; Attack || culminated ;; Process_end
commanded ;; Control
present ;; Presence || exchanges ;; Exchange || battle ;; Hostile_encounter || boarded ;; Self_motion || sailed ;; Self_motion || locate ;; Placing
hindered ;; Hindering
caused ;; Causation || battle ;; Hostile_encounter || delayed ;; Change_event_time
battle ;; Hostile_encounter || exchange ;; Exchange || occurred ;; Presence
triggered ;; Causation || charges ;; Attack
repulsing ;; Defending || effectiveness ;; Influence || factor ;; Causation || resulting in ;; Causation
withdrew ;; Escaping || forces ;; Motion || battle ;; Hostile_encounter || remained ;; Temporary_stay
plan ;; Arranging || capture ;; Conquering
caused ;; Causation || assaults ;; Attack || launch ;; Process_start || charging ;; Imposing_obligation
provided ;; Supply
heralded ;; Statement || battle ;; Hostile_encounter
accused ;; Judgment_communication || haunted ;; Influence || criticism ;; Judgment_communication
began ;; Process_start || ended ;; Process_end || sit-ins ;; Social_event || opposed ;; Agree_or_refuse_to_act || led ;; Causation || following ;; Causation || agreement ;; Resolve_problem
made up ;; Creating || involved ;; Participation
coalition ;; Participation
supporting ;; Supporting || supporting ;; Supporting || divided ;; Creating
split ;; Destroying || claiming ;; Statement || elections ;; Change_of_leadership
demanded ;; Request || seeking ;; Preventing_or_letting || create ;; Creating
collapse ;; Destroying || give ;; Giving
ruled ;; Control
refused ;; Agree_or_refuse_to_act
remove ;; Removing || seeking ;; Preventing_or_letting || elections ;; Change_of_leadership || hold ;; Hold || held ;; Hold
seeking ;; Preventing_or_letting || elections ;; Change_of_leadership || replace ;; Change_of_leadership || hold ;; Hold
elects ;; Change_of_leadership
receive ;; Receiving || session ;; Social_event || sessions ;; Social_event
declared ;; Expressing_publicly || expressed ;; Expressing_publicly || situation ;; Confronting_problem || deadlocked ;; Process_end || war ;; Military_operation || escalate ;; Cause_change_of_strength
continue ;; Process_start || elections ;; Change_of_leadership || suggested ;; Convincing
violence ;; Violence || started ;; Process_start
paralyzing ;; Hindering || blocked ;; Hindering || crackdown ;; Attack
erupted ;; Process_start || battles ;; Hostile_encounter || called ;; Name_conferral || decision ;; Deciding || declaration ;; Expressing_publicly || war ;; Military_operation
defusing ;; Resolve_problem || agreement ;; Sign_agreement || signed ;; Sign_agreement || clashes ;; Violence
election ;; Change_of_leadership || formation ;; Creating || entailed ;; Imposing_obligation
operated ;; Being_in_operation
crashed ;; Damaging
killed ;; Killing
crash ;; Catastrophe
treated ;; Cure || declared ;; Expressing_publicly || caused ;; Causation || determined ;; Deciding
kept ;; Temporary_stay || reported ;; Reporting
reaching ;; Arriving || locked ;; Hold || initiated ;; Process_start || impacted ;; Influence
implemented ;; Action || response ;; Response || authorized ;; Preventing_or_letting || require ;; Imposing_obligation
ensure ;; Action || issued ;; Statement
announced ;; Expressing_publicly || adopted ;; Choosing
massacre ;; Killing || massacre ;; Killing
massacred ;; Committing_crime
victims ;; Catastrophe || crushed ;; Destroying
open ;; Openness
described ;; Statement || massacre ;; Killing
arrived ;; Arriving || massacre ;; Catastrophe
conducted ;; Action || massacre ;; Killing
conducted ;; Action || killed ;; Killing
escape ;; Escaping || attack ;; Attack || shelling ;; Attack || preceded ;; Process_start || circling ;; Surrounding
transported ;; Sending || killed ;; Killing || close ;; Self_motion
used ;; Using || destroy ;; Destroying || massacre ;; Killing || buried ;; Filling
states ;; Statement || assault ;; Attack || remembered ;; Change_sentiment
became ;; Becoming || remaining ;; Presence || guarding ;; Patrolling || departure ;; Departing
armed ;; Bearing_arms || designation ;; Name_conferral || earn ;; Getting
founded ;; Creating
War ;; Hostile_encounter || conflict ;; Hostile_encounter
accused ;; Judgment_communication
fled ;; Escaping || start ;; Process_start || denied ;; Statement
igniting ;; Causation || broke out ;; Process_start
deployed ;; Arranging || fight ;; Hostile_encounter
reached ;; Achieve || agreement ;; Sign_agreement
mediated ;; Resolve_problem || called ;; Name_conferral
signed ;; Sign_agreement
returned ;; Arriving || appointed ;; Choosing
fled ;; Escaping
replaced ;; Change_of_leadership || become ;; Becoming || splitting ;; Destroying || conflict ;; Hostile_encounter
fighting ;; Hostile_encounter || led ;; Causation || led to ;; Causation
came into effect ;; Change
killed ;; Killing || estimated ;; Scrutiny
undertones ;; Communication || fighting ;; Hostile_encounter
accused ;; Judgment_communication || accused ;; Judgment_communication || attacking ;; Attack || attacking ;; Attack
fled ;; Escaping
breaking out ;; Process_start || facing ;; Confronting_problem || soared ;; Causation
devastated ;; Destroying
halved ;; Cause_change_of_position_on_a_scale
hurricane ;; Catastrophe || hurricane ;; Catastrophe || causing ;; Causation
drifted ;; Motion || Originating ;; Creating || nearing ;; Self_motion
existence ;; Presence || hurricane ;; Catastrophe || destroyed ;; Destroying || considered ;; Statement || damaged ;; Damaging || capsized ;; Destroying
turned ;; Becoming || arrived at ;; Arriving || approaching ;; Arriving
producing ;; Creating || came ;; Arriving
noted ;; Know || passed ;; Process_end
produced ;; Manufacturing || hurricane ;; Catastrophe
damaged ;; Damaging || destroyed ;; Destroying
damage ;; Damaging || ruined ;; Destroying
toppled ;; Destroying || inundated ;; Filling || cracked ;; Destroying || rainfall ;; Motion
endured ;; Confronting_problem || struck ;; Attack
destroyed ;; Destroying || rendered ;; Destroying
effects ;; Influence || returning ;; Arriving || flee ;; Escaping || visited ;; Traveling || giving ;; Communication
Damage ;; Damaging
invasion ;; Attack
began ;; Process_start || ended ;; Process_end || invasion ;; Attack || crossing ;; Motion_directional || defeat ;; Conquering
invasion ;; Attack || defeats ;; Conquering || inflicted ;; Bodily_harm || enthroned ;; Change_of_leadership
consolidated ;; Cause_change_of_strength || turned ;; Motion
crossing ;; Motion_directional || entered ;; Arriving
moving ;; Motion
abandoned ;; GiveUp || reaching ;; Arriving || gave ;; Giving || campaign ;; Military_operation
resisted ;; Defending || invasion ;; Attack || invade ;; Attack || following ;; Self_motion
chose ;; Choosing || invasion ;; Attack
ambushed ;; Attack || destroyed ;; Destroying || reinforcing ;; Cause_change_of_strength || chased ;; Self_motion
formulated ;; Communication || invasion ;; Attack
retreated ;; Escaping || burning ;; Bodily_harm || armies ;; Hostile_encounter || dispersing ;; Dispersal || passed ;; Motion_directional || leaving ;; Causation || hiding ;; Hiding_objects
devastated ;; Destroying || shrunk ;; Cause_change_of_position_on_a_scale
armies ;; Bearing_arms || clashed ;; Hostile_encounter
defeated ;; Conquering || surrender ;; Surrendering || forced ;; Request
fled ;; Escaping
persuade ;; Convincing || declare ;; Expressing_publicly
led ;; Conquering
ended ;; Process_end || battle ;; Hostile_encounter
added ;; Cause_to_be_included || lost ;; Earnings_and_losses
led ;; Conquering || reentered ;; Action
develop ;; Coming_to_be || conquered ;; Conquering || hold over ;; Control || links ;; Connect || maintained ;; Preserving || consolidate ;; Cause_change_of_strength
gained ;; Getting
produced ;; Manufacturing || took place ;; Process_start
produced ;; Manufacturing
used ;; Using
presented ;; Presence || using ;; Using
saw ;; Self_motion
match ;; Competition || match ;; Competition
protests ;; Agree_or_refuse_to_act
took place ;; Process_start
suppressed ;; Hindering || attended ;; Participation
arrest ;; Arrest || deaths ;; Death
struggle ;; Hostile_encounter || blew up ;; Destroying
demonstrated ;; Protest
began ;; Process_start
created ;; Causation || ending ;; Process_end
sent ;; Sending
reforms ;; Reforming_a_system || hampered ;; Hindering
erupted ;; Process_start
destruction ;; Destroying
taken ;; Conquering
Final ;; Competition || took place ;; Hold
sent ;; Sending || match ;; Competition || game ;; Competition || play ;; Participation || contested ;; Competition
criticized ;; Judgment_communication || game ;; Competition || resign ;; Change_of_leadership || match ;; Competition || refereeing ;; Rewards_and_punishments || coupled ;; Cause_to_amalgamate || saw ;; Communication || forced ;; Request
prevailed ;; Conquering || win ;; Earnings_and_losses || deficit ;; Earnings_and_losses || Final ;; Process_end || joint ;; Collaboration
match ;; Competition || attendance ;; Participation || set ;; Bringing
match ;; Competition || hat-trick ;; Hold || scored ;; Getting
performance ;; Hold || earned ;; Rewards_and_punishments
siege ;; Besieging || command ;; Control
hold ;; Communication || relieved ;; Process_end
fired ;; Use_firearm || defence ;; Defending
compounded ;; Cause_to_amalgamate || take ;; Conquering
helped ;; Assistance || contribute ;; Giving
begin ;; Process_start
begin ;; Process_start
scheduled ;; Hold
approach ;; Arriving || crashed ;; Catastrophe
attributed ;; Causation || descent ;; Motion_directional || accident ;; Catastrophe || believing ;; Protest || making ;; Action
approach ;; Arriving
approach ;; Arriving || thinking ;; Coming_to_believe || descent ;; Motion_directional || stopped ;; Preventing_or_letting || locate ;; Placing
final ;; Process_end || flew ;; Motion || lost ;; Departing || impacted ;; Influence
rescued ;; Rescuing || survived ;; Rescuing
fatalities ;; Death
organized ;; Arranging || sports ;; Competition || called ;; Name_conferral || called ;; Name_conferral || host ;; Social_event
competition ;; Competition || took place ;; Hold
Games ;; Competition || slogan ;; Communication
participating ;; Participation || Games ;; Competition || played ;; Participation
added ;; Cause_change_of_position_on_a_scale || concerts ;; Social_event
provided ;; Supply || Games ;; Competition
planning ;; Arranging || Games ;; Competition
created ;; Creating
offered ;; Supply || event ;; Social_event
besieged ;; Besieging
surrendered ;; Surrendering || wounded ;; Bodily_harm || granted ;; Giving
provoked ;; Causation || joined ;; Becoming_a_member
held ;; Defending || overrun ;; Conquering
put up ;; Military_operation || tied up ;; Hindering
prevented ;; Preventing_or_letting || sending ;; Sending || started ;; Process_start || allowing ;; Preventing_or_letting
captured ;; Conquering || failed ;; Earnings_and_losses || relieve ;; Cure || smashed ;; Destroying
revolution ;; Change_of_leadership
began ;; Process_start || control ;; Control || threat ;; Influence || drove ;; Motion
began ;; Process_start || using ;; Using || revolt ;; Change_of_leadership || attracted ;; Influence || marched ;; Motion_directional || calling ;; Request
detained ;; Arrest || demands ;; Request || broadcast ;; Expressing_publicly || entering ;; Arriving
demanded ;; Request || fired ;; Use_firearm || release ;; Releasing
died ;; Death || wrapped ;; Placing
start ;; Process_start || revolution ;; Change_of_leadership
erupted ;; Process_start || spread ;; Dispersal || violence ;; Violence
spread ;; Dispersal || revolt ;; Change_of_leadership || collapsed ;; Process_end
battling ;; Hostile_encounter || organised ;; Arranging
released ;; Releasing || imprisoned ;; Arrest || executed ;; Killing || armed ;; Bearing_arms
demanded ;; Request || changes ;; Change || wrested ;; Hold
declared ;; Expressing_publicly || withdraw ;; Escaping || pledged ;; Commitment || elections ;; Change_of_leadership
began ;; Process_start || fighting ;; Hostile_encounter || stopped ;; Process_end || return ;; Arriving
negotiating ;; Communication || changed ;; Change || open ;; Openness || revolution ;; Change_of_leadership || withdrawal ;; Escaping || crush ;; Attack
invaded ;; Attack
resistance ;; Hostile_encounter
fled ;; Escaping || killed ;; Killing || conflict ;; Hostile_encounter
arrests ;; Arrest || denunciations ;; Judgment_communication
suppressed ;; Hindering
strengthening ;; Cause_change_of_strength || losses ;; Earnings_and_losses || alienated ;; Influence || leading to ;; Causation
discussion ;; Communication || revolution ;; Change_of_leadership || suppressed ;; Hindering
debate ;; Communication || thaw ;; Change || study ;; Research
declared ;; Expressing_publicly || inauguration ;; Social_event
called ;; Request || crusades ;; Military_operation || recapture ;; Recovering
called ;; Request || lost ;; Earnings_and_losses || aid ;; Assistance || expedition ;; Conquering
conquer ;; Conquering || expedition ;; Conquering || establishment ;; Creating || resulting ;; Causation || re-captured ;; Attack || fallen ;; Motion_directional || expansion ;; Expansion || culminated ;; Process_end || re-conquest ;; Recovering
expedition ;; Conquering || reaction ;; Response || appeal ;; Statement
phrased ;; Communication || convocation ;; Come_together || re-conquest ;; Recovering || warfare ;; Hostile_encounter || speech ;; Social_event || writing ;; Writing
preceded ;; Process_start || movement ;; Social_event
attacking ;; Attack || travelled ;; Traveling || came up ;; Motion
defeated ;; Conquering
starting ;; Process_start || arriving at ;; Arriving || campaign ;; Military_operation
capturing ;; Conquering || marched ;; Self_motion
took ;; Conquering || arrived at ;; Arriving || assault ;; Attack || massacring ;; Killing
recapture ;; Recovering || repulsed ;; Defending
established ;; Building
took ;; Conquering || returned ;; Arriving || occupied ;; Control
retaking ;; Recovering || returned ;; Arriving
reconquest ;; Recovering || left ;; Causation
died ;; Death || visit ;; Traveling || struck ;; Attack
became ;; Becoming || death ;; Death || dispute ;; Quarreling || organised ;; Arranging || attending ;; Participation
staying ;; Temporary_stay || concluded ;; Coming_to_believe || committed ;; Killing || running ;; Self_motion || jumping ;; Body_movement
verdict ;; Legal_rulings || hearing ;; Perception_active || rejected ;; Agree_or_refuse_to_act || describe ;; Communication
telephoned ;; Communication || died ;; Death || involvement ;; Participation || distressed ;; Confronting_problem
died ;; Death || investigated ;; Criminal_investigation || accident ;; Killing || commissioned ;; Criminal_investigation || reports ;; Reporting || staged ;; Hold
ordered ;; Arranging || inquest ;; Criminal_investigation || ordered ;; Arranging || reopen ;; Openness || investigation ;; Criminal_investigation || litigation ;; Legality
injuries ;; Bodily_harm || verdict ;; Legal_rulings || killed ;; Killing || altercation ;; Quarreling || upheld ;; Statement || rejected ;; Agree_or_refuse_to_act
discredit ;; Judgment_communication || criticism ;; Judgment_communication || criticism ;; Judgment_communication || involvement ;; Participation || attributed ;; Causation || say ;; Statement
disaster ;; Catastrophe || occurred ;; Presence || called ;; Name_conferral || resort ;; Motion
cut ;; Bodily_harm || died ;; Death || supporting ;; Supporting || flying ;; Self_motion
confessed ;; Reveal_secret || burned ;; Bodily_harm || incriminating ;; Judgment_communication || returning ;; Arriving
put on trial ;; Legality || manslaughter ;; Killing || homicide ;; Killing || found ;; Coming_to_believe
conduct ;; Action || destroyed ;; Destroying || recorded ;; Recording || guilty ;; Committing_crime || obstruction ;; Hindering || found ;; Coming_to_believe
disaster ;; Catastrophe
Battle ;; Hostile_encounter || fought ;; Hostile_encounter || War ;; Military_operation
ended ;; Process_end
sailed ;; Self_motion
encountered ;; Hostile_encounter
formed ;; Coming_to_be || sailed ;; Self_motion || battle ;; Hostile_encounter || forming ;; Coming_to_be
ended up ;; Process_end || came back ;; Motion || heading ;; Self_motion || seeing ;; Traveling || paralleled ;; Motion || threatened ;; Warning || turned ;; Self_motion
ordered ;; Arranging || guard ;; Defending || ships ;; Sending || ships ;; Sending || doubling ;; Cause_change_of_position_on_a_scale || leaving ;; Departing || completed ;; Process_end
began ;; Process_start || firing ;; Attack || turned ;; Self_motion
began ;; Process_start || retreat ;; Escaping || bear ;; Change
damage ;; Damaging || attacked ;; Attack || followed ;; Self_motion || inflicting ;; Causation
allowed ;; Preventing_or_letting || ceased ;; Process_end || anchored ;; Temporary_stay || get away ;; Departing || firing ;; Attack
attacked ;; Attack || damaged ;; Damaging || seen ;; Perception_active
surrendered ;; Surrendering || put up ;; Military_operation || resistance ;; Defending || resistance ;; Defending
attacked ;; Attack
brought ;; Bringing
surrounded ;; Surrounding || fought ;; Hostile_encounter
surrendered ;; Surrendering || shot ;; Use_firearm || placed ;; Placing
seen ;; Perception_active || taken off ;; Departing
saved ;; Rescuing
losing ;; Earnings_and_losses || recalled ;; Statement || chasing ;; Self_motion
brought in ;; Bringing
killed ;; Killing || wounded ;; Bodily_harm || captured ;; Conquering
damage ;; Damaging
allowed ;; Preventing_or_letting || control ;; Control
took place ;; Process_start
proclaimed ;; Statement
decided ;; Deciding || recover ;; Recovering
took place ;; Process_start || joined ;; Participation
expelled ;; Removing || demolished ;; Destroying || entered ;; Arriving
held ;; Hold
framed ;; Communication || took ;; Conquering
sent ;; Sending || attack ;; Attack
retreated ;; Escaping
placed ;; Placing || made ;; Manufacturing
attacked ;; Attack || left ;; Departing || retook ;; Conquering
landed ;; Arriving || rebelled ;; Attack
switched ;; Change || opposed ;; Agree_or_refuse_to_act
uprisings ;; Change_of_leadership || rebellions ;; Violence || spread ;; Dispersal
began ;; Process_start || response ;; Response || starting ;; Process_start
response ;; Response || spread ;; Dispersal || revolution ;; Change_of_leadership || appear ;; Coming_to_be || heralded ;; Judgment_communication || shared ;; Communication || taking place ;; Process_start
organizing ;; Arranging || recognized ;; Know || service ;; Supply || rally ;; Social_event || blocked ;; Hindering
accusing ;; Judgment_communication || scrutinized ;; Scrutiny || suppressed ;; Hindering
violence ;; Violence || spread ;; Dispersal || social ;; Social_event || toppled ;; Change_of_leadership || uprisings ;; Change_of_leadership || occurred ;; Coming_to_be || riots ;; Violence || insurgencies ;; Change_of_leadership || effects ;; Influence || wars ;; Military_operation
demonstrations ;; Social_event || took place ;; Process_start
occurred ;; Coming_to_be
bring ;; Removing
responses ;; Response || revolutions ;; Change_of_leadership || demonstrations ;; Social_event || met with ;; Come_together
violence ;; Violence || attacks ;; Attack || answered ;; Response
conflicts ;; Violence || War ;; Military_operation || insurgency ;; Change_of_leadership || war ;; Military_operation || Crisis ;; Influence || coup ;; Change_of_leadership || unrest ;; Violence || insurgency ;; Change_of_leadership || War ;; Military_operation || Crisis ;; Influence || war ;; Military_operation || resulted ;; Causation
response ;; Response || continued ;; Hold
held ;; Hold || changed ;; Change || opened ;; Process_start
resulted in ;; Causation || battle ;; Hostile_encounter
increase ;; Cause_change_of_position_on_a_scale || destructive ;; Destroying || end ;; Process_end || bring about ;; Causation || interventions ;; Response || collapsed ;; Destroying || wars ;; Military_operation
conflicts ;; Violence || referred ;; Name_conferral
uprising ;; Change_of_leadership || resulted in ;; Causation || transition ;; Change
started ;; Process_start || uprisings ;; Change_of_leadership || show ;; Communication || going away ;; Departing || occurring ;; Coming_to_be
began ;; Process_start || invaded ;; Attack || ended ;; Process_end || accepted ;; Receiving || War ;; Military_operation
replace ;; Change_of_leadership || rebel ;; Bearing_arms
planned ;; Arranging || annex ;; Becoming_a_member || war ;; Military_operation
repelled ;; Defending || regained ;; Recovering || take ;; Getting || made ;; Creating
end ;; Process_end || offensive ;; Attack
provided ;; Supply || support ;; Supporting || isolated ;; GiveUp
ceasefire ;; Process_end || led to ;; Causation
conflict ;; Hostile_encounter
war ;; Military_operation || seen ;; Perception_active || developed ;; Coming_to_be
formulated ;; Communication || impact ;; Influence || war ;; Military_operation || lead to ;; Causation
died ;; Death
war ;; Military_operation || resulted in ;; Causation
campaigns ;; Hostile_encounter || waged ;; Hold
expansion ;; Expansion || lasted ;; Process_end || culminated ;; Process_end || resulted in ;; Causation
ease ;; Cause_change_of_position_on_a_scale || unite ;; Cause_to_amalgamate
wars ;; Hostile_encounter || become ;; Becoming || paved ;; Influence
wars ;; Hostile_encounter || agree ;; Agree_or_refuse_to_act || pay ;; Commerce_pay || fought ;; Hostile_encounter || boost ;; Cause_change_of_position_on_a_scale
attacked ;; Attack
allowed ;; Preventing_or_letting || Conquering ;; Conquering
Wars ;; Hostile_encounter || described ;; Communication
produced ;; Manufacturing || event ;; Competition
took place ;; Process_start
event ;; Competition
surrounded ;; Surrounding || event ;; Competition || match ;; Competition || defeated ;; Conquering
bouts ;; Hostile_encounter
matches ;; Competition || defeated ;; Earnings_and_losses || defeated ;; Conquering || retain ;; Getting || retain ;; Getting
used ;; Using || event ;; Competition || marked ;; Communication || event ;; Competition || event ;; Competition
sales ;; Commerce_sell || received ;; Receiving || helping ;; Assistance || grossed ;; Earnings_and_losses || attendance ;; Participation || increase ;; Cause_change_of_position_on_a_scale || compared ;; Check
event ;; Competition || peaked ;; Motion_directional || released ;; Publishing
resulted in ;; Causation
precipitated ;; Causation
install ;; Change_of_leadership
marked ;; Recording
invaded ;; Attack
control ;; Control || achieve ;; Achieve || remained ;; Presence
comprising ;; Cause_to_be_included
captured ;; Conquering || causing ;; Causation
captured ;; Conquering || agreed ;; Agree_or_refuse_to_act || retreat ;; Escaping || swap ;; Exchange || forced ;; Request
escaped ;; Escaping
dragged on ;; Motion
conquered ;; Conquering || achieve ;; Achieve
began ;; Process_start || acquire ;; Getting
leaving ;; Giving || returned ;; Arriving
recognised ;; Know
fight ;; Hostile_encounter
agreed ;; Agree_or_refuse_to_act || recognised ;; Know
died ;; Death || ascended ;; Change_of_leadership || beginning ;; Process_start
called ;; Name_conferral || questioned ;; Communication || described ;; Statement
held ;; Hold
agreed ;; Agree_or_refuse_to_act || held ;; Hold || switched ;; Exchange || granted ;; Preventing_or_letting || free ;; Releasing
lost ;; Earnings_and_losses || get ;; Getting
lost ;; Earnings_and_losses || beat ;; Competition || bowled ;; Competition
named ;; Name_conferral
said ;; Communication
shown ;; Statement || qualified ;; Becoming_a_member
claimed ;; Statement || saying ;; Statement
echoed ;; Change_sentiment
totals ;; Destroying || involving ;; Cause_to_be_included || recorded ;; Recording
began ;; Process_start || change ;; Change || hacking ;; Committing_crime || controversy ;; Communication || summit ;; Social_event || research ;; Research || research ;; Research || copying ;; Writing
change ;; Change || controversy ;; Communication || describe ;; Statement || broken ;; Bodily_harm
manipulated ;; Hold || suppress ;; Hindering || warming ;; Cause_change_of_position_on_a_scale || conspiracy ;; Committing_crime || argued ;; Protest
rejected ;; Agree_or_refuse_to_act || saying ;; Statement || exchange ;; Exchange || reflected ;; Change_sentiment || taken out ;; Removing
change ;; Change || negotiations ;; Communication || began ;; Process_start || mitigation ;; Cause_change_of_position_on_a_scale || picked up ;; Choosing
said ;; Statement || release ;; Publishing || campaign ;; Social_event || undermine ;; Destroying
change ;; Change || statements ;; Statement || response ;; Response || supporting ;; Supporting || rising ;; Cause_change_of_position_on_a_scale || controversy ;; Communication || concluding ;; Coming_to_believe || caused ;; Causation || growing ;; Expansion || released ;; Statement
published ;; Publishing || finding ;; Know || fraud ;; Committing_crime || allegations ;; Statement || misconduct ;; Committing_crime || investigated ;; Scrutiny
consensus ;; Agree_or_refuse_to_act || occurring ;; Presence || investigations ;; Scrutiny
called ;; Name_conferral || engagement ;; Participation || engagement ;; Participation || invasion ;; Hostile_encounter
widened ;; Expansion || war ;; Hostile_encounter
expansion ;; Expansion || influence ;; Influence || elimination ;; Removing
declared ;; Expressing_publicly || war ;; Hostile_encounter || take effect ;; Change
raids ;; Attack || defensive ;; Defending || war ;; Hostile_encounter || strategies ;; Arranging || exchanged ;; Exchange || transpired ;; Coming_to_believe
skirmishing ;; Hostile_encounter || patrols ;; Patrolling || exchanged ;; Exchange
announced ;; Expressing_publicly
signed ;; Sign_agreement || attack ;; Attack || launched ;; Military_operation || coming ;; Arriving
conquest ;; Conquering || penetrated ;; Attack || stalled ;; Hindering
signed ;; Sign_agreement
signed ;; Sign_agreement || came into effect ;; Change
allowed ;; Preventing_or_letting || captured ;; Conquering || obtained ;; Getting || extended ;; Expansion || fighting ;; Hostile_encounter || control ;; Control || occupy ;; Hold || created ;; Creating
set up ;; Reforming_a_system || control ;; Control || oversee ;; Control
forces ;; Hostile_encounter || faced ;; Confronting_problem
lost ;; Earnings_and_losses || reoccupy ;; Conquering
defeated ;; Conquering || engagement ;; Hostile_encounter || recapture ;; Conquering
arrival ;; Arriving
served ;; Assistance || command ;; Control
suicide ;; Killing || murdered ;; Killing || abandoned ;; GiveUp || said ;; Communication || refused ;; Agree_or_refuse_to_act || claims ;; Statement || committed ;; Committing_crime || load ;; Placing || remained ;; Temporary_stay || command ;; Control || provoked ;; Causation
determined ;; Deciding || discussed ;; Expressing_publicly || lawsuit ;; Legality
destroyed ;; Destroying || attack ;; Attack || came ;; Motion
gave ;; Giving || captured ;; Conquering
despatched ;; Sending || blockading ;; Besieging || arriving ;; Arriving
reinforce ;; Cause_change_of_strength || engaged ;; Hostile_encounter || drive ;; Motion
wounded ;; Bodily_harm || died ;; Death || exchanges ;; Exchange || opening ;; Openness
recapturing ;; Control || overwhelmed ;; Destroying || forced ;; Control || drive ;; Motion
captured ;; Conquering
operate ;; Being_in_operation || planned ;; Arranging || built ;; Building
served ;; Supply
captured ;; Conquering || commanded ;; Control || route ;; Self_motion
escape ;; Escaping || taking ;; Conquering || detect ;; Know
avoid ;; Preventing_or_letting || sailing ;; Self_motion
alleged ;; Statement || arrested ;; Arrest
offered ;; Giving || refused ;; Agree_or_refuse_to_act || imprisoned ;; Arrest || exchange ;; Exchange
added ;; Cause_to_be_included
burned ;; Bodily_harm || retreat ;; Escaping || scuttled ;; Destroying
injuring ;; Bodily_harm || occurred ;; Coming_to_be || terrorist ;; Terrorism || attack ;; Attack || killing ;; Killing || attacked ;; Attack
killed ;; Killing || captured ;; Conquering || wounded ;; Bodily_harm
dead ;; Death || comprised ;; Cause_to_be_included
election ;; Choosing || candidate ;; Participation || host ;; Come_together
elected ;; Change_of_leadership
focused ;; Aiming || took ;; Conquering || attack ;; Attack
asserted ;; Statement || described ;; Statement || suicide ;; Killing || attack ;; Attack || went ;; Self_motion
trained ;; Education_teaching || planning ;; Arranging || handled ;; Resolve_problem
speculated ;; Coming_to_believe || paid ;; Commerce_pay || provided ;; Supply || exchange ;; Exchange || hijacked ;; Terrorism
found ;; Know || arrested ;; Arrest
suicide ;; Killing || death ;; Death || ruled ;; Statement
disputing ;; Quarreling || violence ;; Violence || cause ;; Causation || protests ;; Protest || alleging ;; Statement
pulled ;; Motion
arrest ;; Arrest || charge ;; Judgment_communication || assaulting ;; Attack || exchange ;; Exchange || escalated ;; Cause_change_of_position_on_a_scale || resulting in ;; Causation
arrest ;; Arrest || recorded ;; Recording
placed ;; Placing || leave ;; Departing || reviewed ;; Criminal_investigation
determined ;; Deciding || conducted ;; Action || investigation ;; Criminal_investigation || training ;; Education_teaching || follow ;; Motion
declined ;; Agree_or_refuse_to_act
statements ;; Statement || making ;; Action
settled ;; Resolve_problem || changes ;; Change || lawsuit ;; Legality
end ;; Process_end || agreement ;; Agree_or_refuse_to_act || dropped ;; GiveUp
reconnaissance ;; Scrutiny || shot down ;; Use_firearm || performing ;; Action
flown ;; Motion || hit ;; Attack || crashed ;; Catastrophe
captured ;; Arrest || parachuted ;; Self_motion
acknowledged ;; Statement || forced ;; Causation || admit ;; Reveal_secret || taken ;; Creating || mission ;; Scrutiny || loss ;; Earnings_and_losses || produced ;; Manufacturing
summit ;; Social_event || incident ;; Scrutiny || opening ;; Social_event || occurred ;; Coming_to_be
caused ;; Causation || prompted ;; Causation || deterioration ;; Damaging || War ;; Hostile_encounter
released ;; Releasing || exchange ;; Exchange || convicted ;; Rewards_and_punishments || sentenced ;; Rewards_and_punishments
Wars ;; Hostile_encounter || encounters ;; Hostile_encounter
occurred ;; Coming_to_be || encountered ;; Hostile_encounter || chased ;; Motion
raiding ;; Attack
response ;; Response || ordered ;; Request || intercept ;; Preventing_or_letting || fought ;; Hostile_encounter || engagement ;; Hostile_encounter || defeat ;; Conquering
cruising ;; Patrolling || appeared ;; Presence
identified ;; Check || fleeing ;; Escaping || turning ;; Becoming || began ;; Process_start || chase ;; Motion || gave ;; Giving || investigate ;; Scrutiny || fight ;; Hostile_encounter || overtake ;; Motion
exchanged ;; Exchange || cheering ;; Body_movement || lasting ;; Process_end
board ;; Temporary_stay
lost ;; Earnings_and_losses || engagement ;; Hostile_encounter || wounded ;; Bodily_harm || casualties ;; Death
captured ;; Conquering || returned ;; Arriving || conflict ;; Hostile_encounter || proclaimed ;; Statement
engagement ;; Hostile_encounter || manned ;; Action
fought ;; Hostile_encounter || battle ;; Military_operation || collapse ;; Process_end || Revolution ;; Change_of_leadership
expelled ;; Removing || ruled ;; Control || murdered ;; Killing || replaced ;; Change_of_leadership
followed ;; Bringing || appeared ;; Coming_to_be
defected ;; Escaping || named ;; Name_conferral
Stationed ;; Placing || observing ;; Know || conflict ;; Hostile_encounter || tasked ;; Confronting_problem || interfere ;; Hindering
renamed ;; Name_conferral || include ;; Cause_to_be_included || reasoned ;; Protest || reported ;; Reporting || presented ;; Presence
discovered ;; Know || ordered ;; Request || Sailing ;; Self_motion || intercept ;; Scrutiny || surrender ;; Surrendering
refused ;; Agree_or_refuse_to_act || exchanged ;; Exchange
prevented ;; Preventing_or_letting || boarding ;; Temporary_stay || dismasted ;; Destroying || battered ;; Damaging
surrendered ;; Surrendering || depositing ;; Placing || bringing ;; Bringing
renamed ;; Name_conferral || actions ;; Response || approved ;; Agree_or_refuse_to_act || returned ;; Arriving
produced ;; Manufacturing || event ;; Social_event || wrestling ;; Competition
pre-existing ;; Presence || involved ;; Cause_to_be_included
build ;; Building || culminate ;; Process_end || wrestling ;; Competition || match ;; Competition
exchange ;; Exchange || work ;; Being_in_operation
prompted ;; Causation || aiding ;; Assistance || prompted ;; Causation || competing ;; Competition || came to ;; Motion || give ;; Giving || interrupted ;; Hindering || come to ;; Motion || saw ;; Self_motion || match ;; Competition || match ;; Competition || match ;; Competition || interfere ;; Hindering
open ;; Openness || announced ;; Expressing_publicly || match ;; Competition || deemed ;; Coming_to_believe
signing ;; Sign_agreement
released ;; Releasing || made ;; Getting
ended ;; Process_end || Tour ;; Traveling || tour ;; Traveling || Tour ;; Traveling
tour ;; Traveling || Tour ;; Traveling
Tour ;; Traveling || record ;; Recording || breaking ;; Process_end || record ;; Recording || perform ;; Action || breaking ;; Destroying
played ;; Participation
tour ;; Traveling || opening ;; Process_start
Tour ;; Traveling || changes ;; Exchange || carried ;; Bringing
match ;; Competition
match ;; Competition || competition ;; Competition || final ;; Process_end
appearing ;; Presence || finished ;; Process_end || beating ;; Competition
appearing ;; Presence
received ;; Receiving || progressed ;; Achieve || reach ;; Becoming_a_member
matches ;; Competition || quarter-finals ;; Competition || semi-final ;; Competition || beat ;; Competition
matches ;; Competition
semi-final ;; Competition || beat ;; Competition || beat ;; Competition
match ;; Competition || went ;; Self_motion
Watched ;; Perception_active || match ;; Competition || exchanges ;; Exchange || turned down ;; Becoming
injuries ;; Bodily_harm || suffered ;; Bodily_harm
disallowed ;; Preventing_or_letting || adjudged ;; Judgment_communication
extended ;; Change_event_time
finishing ;; Process_end || failed ;; Earnings_and_losses || retain ;; Preserving
led to ;; Causation || banning ;; Preventing_or_letting || reduced ;; Cause_change_of_position_on_a_scale || competition ;; Competition || match ;; Competition || Riots ;; Violence
Tour ;; Traveling
toured ;; Traveling || provided ;; Supply || marked ;; Recording || support ;; Supporting || first ;; Process_start
toured ;; Traveling || supporting ;; Supporting
confirmed ;; Check || opening ;; Openness || tour ;; Traveling
opening ;; Openness || tour ;; Traveling
announced ;; Expressing_publicly || replacement ;; Exchange
announced ;; Expressing_publicly || concert ;; Social_event || concert ;; Social_event || cancelled ;; Change_event_time || got ;; Getting || moved ;; Motion
due ;; Causation || cut short ;; Removing
filled ;; Filling || left ;; Departing || tour ;; Traveling || play ;; Participation
joined ;; Participation || finished ;; Process_end
wars ;; Hostile_encounter || started ;; Process_start || wars ;; Hostile_encounter
resulted in ;; Causation || emergence ;; Coming_to_be || loss ;; Earnings_and_losses
besiege ;; Besieging || conquer ;; Conquering || launch ;; Military_operation || raids ;; Attack
became ;; Becoming || established ;; Creating || stabilize ;; Cause_change_of_strength
became ;; Becoming || raids ;; Attack || exchanged ;; Exchange || conflict ;; Hostile_encounter || sponsored ;; Supply
avoided ;; Preventing_or_letting || retreat ;; Escaping || open ;; Openness || fortified ;; Cause_change_of_strength || preferring ;; Choosing || defensive ;; Defending
begin ;; Process_start || invasions ;; Attack || destructive ;; Destroying || take ;; Conquering || launch ;; Military_operation || retaliate ;; Defending || combat ;; Military_operation || raids ;; Attack
turned ;; Becoming || decline ;; Cause_change_of_position_on_a_scale || strengthening ;; Cause_change_of_strength
restored ;; Recovering || broke ;; Bodily_harm
wars ;; Hostile_encounter || appearance ;; Presence || dominated ;; Control
became ;; Becoming || launched ;; Military_operation || raids ;; Attack
reached ;; Arriving || reaching ;; Arriving || raids ;; Attack
storm ;; Catastrophe || impacted ;; Influence
fell ;; Motion_directional || snowfall ;; Self_motion
left ;; Departing || storm ;; Catastrophe
storm ;; Catastrophe || weak ;; Cause_change_of_strength || formed ;; Cause_to_amalgamate || originated ;; Coming_to_be
supported ;; Supporting || merger ;; Cause_to_amalgamate || developed ;; Coming_to_be
deepened ;; Cause_change_of_strength || become ;; Becoming || full-fledged ;; Becoming
began ;; Process_start || weaken ;; Cause_change_of_strength
clogged ;; Hindering || Widespread ;; Dispersal || blizzard ;; Attack || abandoned ;; GiveUp || close ;; Process_end || wrought ;; Causation
departed ;; Departing || storm ;; Catastrophe || closed ;; Process_end
opened ;; Openness || delayed ;; Change_event_time || Exchange ;; Exchange || marking ;; Recording || opening ;; Openness
deliveries ;; Sending || drifting ;; Motion || left ;; Self_motion || receive ;; Receiving || isolated ;; Preventing_or_letting
blamed ;; Judgment_communication || deaths ;; Death || storm ;; Catastrophe
fires ;; Catastrophe || tragedies ;; Catastrophe || accidents ;; Catastrophe || resulted ;; Causation || poisoning ;; Bodily_harm || exposure ;; Catastrophe
captured ;; Conquering || besieged ;; Besieging || took place ;; Process_start
commanded ;; Control
captured ;; Conquering
invaded ;; Attack || threatening ;; Warning || crossed ;; Motion_directional || crossing ;; Motion_directional || preceded ;; Process_start
captured ;; Conquering || supplies ;; Supply || arrival ;; Arriving || stalled ;; Hindering
remained ;; Presence || close ;; Self_motion || garrisoned ;; Hostile_encounter
exchange ;; Exchange || returned ;; Arriving
killing ;; Killing || obtained ;; Getting || massacre ;; Killing || occurred ;; Coming_to_be || end ;; Process_end || trading ;; Exchange
employing ;; Employment || established ;; Creating
gave out ;; Giving || employees ;; Employment
found ;; Know || massacre ;; Killing || speculation ;; Coming_to_believe || end ;; Process_end || happened ;; Presence
found ;; Know || passed ;; Motion_directional || trading ;; Body_movement
found ;; Know
hide ;; Hiding_objects || diving ;; Self_motion || shot ;; Use_firearm || came up ;; Arriving
Capture ;; Conquering || Battle ;; Hostile_encounter || fought ;; Hostile_encounter
captured ;; Conquering || killed ;; Killing
meet ;; Come_together || waiting ;; Temporary_stay || sailing ;; Self_motion || command ;; Control || commanded ;; Control || put ;; Placing
rendering ;; Causation || exchange ;; Exchange || shot ;; Use_firearm || suffered ;; Confronting_problem
wounded ;; Bodily_harm || carried ;; Carry_goods
struggled ;; Hostile_encounter || give up ;; Process_end || carry out ;; Action
party ;; Social_event || overwhelmed ;; Destroying
battle ;; Hostile_encounter || killed ;; Killing || wounded ;; Bodily_harm
injured ;; Bodily_harm || fighting ;; Hostile_encounter
imprisoned ;; Arrest || repaired ;; Recovering || taken ;; Conquering || taken ;; Conquering
broken up ;; Destroying || sold ;; Commerce_sell
build ;; Building || visited ;; Traveling || used ;; Using || seen ;; Perception_active
broken up ;; Destroying || survived ;; Preserving
rebellion ;; Change_of_leadership || putting up ;; Military_operation
included ;; Cause_to_be_included || uprising ;; Change_of_leadership || believed ;; Know || opposed ;; Agree_or_refuse_to_act
declaring ;; Expressing_publicly || fighting ;; Hostile_encounter || deemed ;; Coming_to_believe || fighting ;; Hostile_encounter || recognized ;; Know || passed ;; Motion_directional
establishing ;; Building
began ;; Process_start || clashes ;; Hostile_encounter
become ;; Becoming || become ;; Becoming || curtailed ;; Hindering || regarding ;; Know
disagreed ;; Agree_or_refuse_to_act
debated ;; Communication || defeated ;; Conquering || flood ;; Motion
declined ;; Cause_change_of_position_on_a_scale || led to ;; Causation
invade ;; Attack
declared ;; Expressing_publicly
vowed ;; Commitment || Determined ;; Deciding || retake ;; Conquering
entered ;; Arriving || found ;; Know
defeating ;; Conquering || surrendered ;; Surrendering
defeated ;; Conquering || killing ;; Killing
fled ;; Escaping || created ;; Creating || command ;; Control || move ;; Motion
received ;; Receiving || paused ;; Process_end
Becoming ;; Becoming || strength ;; Cause_change_of_strength
assault ;; Attack || staged ;; Self_motion
retreat ;; Escaping || exchange ;; Exchange || taken ;; Creating || ordered ;; Arranging
refused ;; Agree_or_refuse_to_act || conflicts ;; Hostile_encounter
led ;; Causation
held ;; Hold
help ;; Assistance || organised ;; Arranging || joined ;; Participation || appeals ;; Request || marched ;; Social_event
drew ;; Getting
ended ;; Process_end || march ;; Social_event
end ;; Process_end || demanded ;; Request
broke out ;; Process_start || killed ;; Killing || gunshot ;; Use_firearm || fired ;; Use_firearm
arrested ;; Arrest || convicted ;; Legality
released ;; Releasing || exchange ;; Exchange
caused ;; Causation || increase ;; Cause_change_of_position_on_a_scale || secure ;; Preventing_or_letting || withdraw ;; Escaping
lifted ;; Process_end || blockade ;; Besieging || negotiations ;; Communication
wrestling ;; Competition || held ;; Defending
held ;; Defending
became ;; Becoming
held ;; Defending
moved ;; Motion || switching ;; Exchange || announced ;; Expressing_publicly
canceled ;; Change_event_time || brought ;; Bringing
fall ;; Motion_directional || threat ;; Warning
became ;; Becoming
established ;; Building
provided ;; Supply
exports ;; Sending || traded ;; Exchange || supplemented ;; Supply || caused ;; Causation
starting ;; Process_start || ended ;; Process_end || Tour ;; Traveling || scheduled ;; Arranging
placed ;; Placing || tour ;; Traveling || concert ;; Social_event
tour ;; Traveling
shared ;; Exchange || tour ;; Traveling || came ;; Arriving
shows ;; Social_event || playing ;; Participation
increase ;; Cause_change_of_position_on_a_scale || move ;; Removing || expenses ;; Cost || called ;; Name_conferral || result ;; Causation || war ;; Military_operation || war ;; Military_operation || fought ;; Competition
aid ;; Assistance || inhabited ;; Placing || sought ;; Getting || defeat ;; Conquering || war ;; Military_operation
invaded ;; Attack || Aided ;; Assistance || defeated ;; Conquering
negotiations ;; Communication || called ;; Request || securing ;; Getting || assistance ;; Assistance || truce ;; Process_end || protracted ;; Change_event_time
suffered ;; Confronting_problem || migrate ;; Escaping || settling ;; Response
agree ;; Agree_or_refuse_to_act || eliminated ;; Removing || battle ;; Hostile_encounter || routed ;; Attack || forced ;; Causation
ended ;; Process_end || restored ;; Recovering || treaty ;; Sign_agreement || war ;; Military_operation || confirmed ;; Check
pay ;; Commerce_pay || obliged ;; Request || exchange ;; Exchange || return ;; Arriving
treaty ;; Sign_agreement || ceded ;; Surrendering
treaty ;; Sign_agreement || death ;; Death || lasted ;; Process_end
began ;; Process_start || crossed ;; Motion_directional || known ;; Know || invasion ;; Military_operation || engage ;; Participation || defeat ;; Conquering
cease ;; Process_end || trading ;; Exchange || compel ;; Action || sue ;; Request
liberate ;; Conquering
named ;; Name_conferral || provide ;; Supply || gain ;; Getting
known ;; Know || assembled ;; Come_together
destroy ;; Destroying || winning ;; Getting || pushed ;; Motion
retreat ;; Escaping || leaving ;; Departing || burn ;; Bodily_harm || slipped ;; Motion
employed ;; Employment || destroying ;; Destroying || feeding ;; Supply || fell ;; Motion_directional || forcing ;; Request
dug ;; Hiding_objects || caught ;; Motion
casualties ;; Catastrophe || resulted in ;; Causation
withdrew ;; Escaping || leaving ;; Departing
abandoned ;; GiveUp || burned ;; Bodily_harm || entered ;; Arriving
stayed ;; Temporary_stay || compel ;; Action || sue ;; Request
left ;; Departing || marched ;; Self_motion
began ;; Process_start || retreat ;; Escaping
suffered ;; Bodily_harm
Lack ;; Earnings_and_losses || led to ;; Causation
fighting ;; Hostile_encounter || resulted in ;; Causation
captured ;; Conquering || crossed ;; Motion_directional || lost ;; Death || remained ;; Presence
left ;; Departing || urging ;; Convincing
raise ;; Cause_change_of_position_on_a_scale || resist ;; Defending || returned ;; Arriving || protect ;; Defending
ended ;; Process_end || leaving ;; Departing
turning ;; Becoming
involving ;; Cause_to_be_included || campaigns ;; Military_operation
weakened ;; Cause_change_of_strength || suffered ;; Bodily_harm
made up ;; Becoming_a_member || reduced ;; Cause_change_of_position_on_a_scale
triggered ;; Causation
broke ;; Destroying || switched ;; Exchange
triggered ;; Causation
killed ;; Killing || crash ;; Catastrophe
became ;; Becoming || referred ;; Name_conferral
consisting ;; Cause_to_be_included || party ;; Social_event
joined ;; Becoming_a_member || rising ;; Cause_change_of_position_on_a_scale || tour ;; Traveling
affected ;; Influence
chose ;; Choosing || stopping ;; Process_end || perform ;; Hold || reach ;; Arriving || charter ;; Renting
swapped ;; Exchange || taking ;; Creating
takeoff ;; Self_motion || crashed ;; Catastrophe || lost ;; Earnings_and_losses
killed ;; Killing
mentioned ;; Statement
erected ;; Building || hosted ;; Come_together
forced ;; Motion
killed ;; Killing || injured ;; Bodily_harm
identified ;; Check || attack ;; Attack
attacks ;; Attack || murdered ;; Killing
raised ;; Cause_change_of_position_on_a_scale || deployed ;; Arranging
exchanged ;; Exchange || led to ;; Causation || discovery ;; Criminal_investigation
emerged ;; Coming_to_be || shot ;; Use_firearm || took ;; Bringing
met ;; Come_together || demonstrations ;; Social_event || joined ;; Participation
became ;; Becoming || support ;; Supporting
broken out ;; Process_start || took place ;; Process_start
encountered ;; Hostile_encounter || engaged ;; Participation
reduced ;; Cause_change_of_position_on_a_scale || exchange ;; Exchange
removed ;; Removing || set ;; Placing || on fire ;; Damaging || returning ;; Arriving
exodus ;; Departing || expulsion ;; Escaping || captured ;; Conquering
occurred ;; Coming_to_be || War ;; Military_operation
transformed ;; Change || designated ;; Choosing
improve ;; Cause_to_make_progress || exodus ;; Departing || control ;; Control || resumed ;; Process_start || expulsion ;; Escaping || took place ;; Process_start || fighting ;; Hostile_encounter || prompting ;; Causation
averted ;; Preventing_or_letting || helped ;; Assistance || forcing ;; Causation || demoralize ;; Influence
defending ;; Defending || ordered ;; Request
died ;; Death || deaths ;; Death || surrendered ;; Surrendering || conquest ;; Conquering || took ;; Conquering || led to ;; Causation
died ;; Death || suggests ;; Statement
defense ;; Defending || expulsion ;; Escaping || issued ;; Expressing_publicly
forced ;; Motion || supplies ;; Supply || bussed ;; Escaping || provide ;; Supply || expelled ;; Escaping
died ;; Death
events ;; Social_event
noted ;; Know || laid ;; Giving
killed ;; Killing || Hurricane ;; Catastrophe || hurricane ;; Catastrophe || brushing ;; Motion
intensified ;; Cause_change_of_strength || Originating ;; Coming_to_be || disturbed ;; Change_sentiment
became ;; Becoming || reached ;; Arriving || Gaining ;; Getting || hurricane ;; Catastrophe || organized ;; Arranging
passed ;; Motion_directional || attained ;; Getting
hurricane ;; Catastrophe || Due ;; Causation || weakened ;; Cause_change_of_strength
open ;; Openness || dissipated ;; Dispersal
warnings ;; Warning || arrival ;; Arriving || hurricane ;; Catastrophe || issued ;; Expressing_publicly
opened ;; Openness
triggered ;; Causation || storm ;; Catastrophe || killing ;; Killing || flooding ;; Catastrophe
effects ;; Influence || limited ;; Limiting || mitigating ;; Cure
uprising ;; Protest || protests ;; Protest
began ;; Process_start || announced ;; Expressing_publicly || protesting ;; Protest || declaration ;; Expressing_publicly || granted ;; Preventing_or_letting
protect ;; Defending || elected ;; Choosing
organized ;; Arranging || demonstrations ;; Social_event
injuries ;; Bodily_harm || resulted in ;; Causation || deaths ;; Death || demonstrations ;; Social_event || clashes ;; Violence
gathered ;; Come_together || surrounded ;; Surrounding
gathered ;; Come_together || support ;; Supporting || show ;; Giving
protest ;; Protest || resigned ;; Change_of_leadership || spoke ;; Expressing_publicly
resignations ;; Change_of_leadership || tendered ;; Statement
resigned ;; Change_of_leadership
added ;; Cause_to_be_included || removed ;; Removing || declaration ;; Expressing_publicly
approved ;; Agree_or_refuse_to_act || supported ;; Supporting
claimed ;; Statement || called ;; Request || inquiry ;; Criminal_investigation
gathered ;; Come_together || celebrate ;; Social_event || protests ;; Protest || anniversary ;; Social_event
election ;; Choosing || demanding ;; Request
reported ;; Reporting || Demonstrations ;; Social_event
formed ;; Coming_to_be || supported ;; Supporting || collected ;; Come_together || signatures ;; Sign_agreement || demonstrations ;; Protest || resignation ;; Change_of_leadership
statement ;; Statement || responds ;; Response || announcing ;; Expressing_publicly || end ;; Process_end || released ;; Statement
statement ;; Statement || announced ;; Expressing_publicly || suspended ;; Process_end || elections ;; Change_of_leadership || held ;; Defending
became ;; Becoming
protest ;; Protest || demonstrations ;; Protest || overthrow ;; Change_of_leadership
killed ;; Killing || clashes ;; Hostile_encounter
demonstrators ;; Protest || denied ;; Statement || shooting ;; Use_firearm || claims ;; Statement
revolution ;; Change_of_leadership || revolution ;; Change_of_leadership || revolution ;; Change_of_leadership || led to ;; Causation || foundation ;; Coming_to_be || war ;; Military_operation
revolution ;; Change_of_leadership || received ;; Receiving || began ;; Process_start || placing ;; Placing || support ;; Supporting || social ;; Social_event || demobilized ;; Employment
helped ;; Assistance || demanded ;; Request || establish ;; Creating || secession ;; Change_of_leadership
helped ;; Assistance || support ;; Supporting || seize ;; Conquering
murdered ;; Killing || resigned ;; Change_of_leadership
became ;; Becoming || end ;; Process_end || coup ;; Change_of_leadership || accept ;; Receiving
terminated ;; Process_end || dissolving ;; Destroying
proclamation ;; Statement || issued ;; Expressing_publicly || withdrawing ;; Escaping
proclaimed ;; Statement || named ;; Name_conferral
established ;; Creating
overthrown ;; Change_of_leadership || revival ;; Recovering || invasion ;; Attack || restored ;; Recovering
threatened ;; Warning
formed ;; Coming_to_be
became ;; Becoming
intensified ;; Cause_change_of_strength || peaked ;; Arriving
weakened ;; Cause_change_of_strength || leveled ;; Cause_change_of_position_on_a_scale
passing ;; Motion_directional || downgraded ;; Cause_change_of_position_on_a_scale
weaken ;; Cause_change_of_strength || dissipated ;; Dispersal
weakened ;; Cause_change_of_strength || issued ;; Expressing_publicly || dropped ;; Motion_directional
brought ;; Bringing
closed ;; Openness
Convention ;; Social_event || convention ;; Social_event
organised ;; Arranging
growth ;; Expansion || stretch ;; Expansion || movement ;; Social_event || gathered ;; Come_together
founded ;; Creating || brought about ;; Causation || transformation ;; Change
called ;; Name_conferral
went to ;; Motion || came to ;; Arriving || despised ;; Judgment_communication
death ;; Death || came to ;; Motion
received ;; Receiving || permitted ;; Preventing_or_letting || preach ;; Expressing_publicly
converted ;; Change || varying ;; Change
ceased ;; Process_end || exist ;; Presence
dispel ;; Dispersal || composed ;; Writing || honors ;; Achieve
cross ;; Motion_directional || replenished ;; Supply || flooded ;; Motion
sent ;; Sending || praised ;; Judgment_communication
work ;; Employment || causing ;; Causation || mentions ;; Statement || came to ;; Arriving || preached ;; Expressing_publicly
came ;; Motion
died ;; Death || suffered ;; Bodily_harm
occurred ;; Presence
gathered ;; Come_together
made ;; Manufacturing
occurred ;; Presence || led to ;; Causation
began ;; Process_start || reports ;; Reporting
seen ;; Traveling || violence ;; Violence || spread ;; Dispersal || fear ;; Change_sentiment
union ;; Cause_to_amalgamate
merger ;; Cause_to_amalgamate || marked ;; Recording
merger ;; Cause_to_amalgamate || rejected ;; Agree_or_refuse_to_act
merger ;; Cause_to_amalgamate || changed ;; Change || call ;; Name_conferral
spread ;; Dispersal
addressed ;; Expressing_publicly
wrestling ;; Competition || promoted ;; Cause_to_make_progress
used ;; Using || highlighted ;; Expressing_publicly
added ;; Cause_to_be_included || used ;; Using
held ;; Defending
changed ;; Change || starting ;; Process_start || held ;; Defending
matches ;; Competition
televised ;; Communication
televised ;; Communication
conflict ;; Hostile_encounter
originated ;; Coming_to_be
conflict ;; Hostile_encounter || dominated ;; Control || gain ;; Getting
withdrawal ;; Escaping || escalated ;; Cause_change_of_strength
supported ;; Supporting || Beginning ;; Process_start
declared ;; Expressing_publicly
addressed ;; Expressing_publicly || conflict ;; Hostile_encounter
reached ;; Arriving || intervened ;; Hindering
withdrew ;; Escaping
reached ;; Arriving
controlled ;; Control || remained ;; Presence
unchanged ;; Presence
broke out ;; Process_start || reemerged ;; Cause_to_amalgamate
marked ;; Recording || resulting in ;; Causation
began ;; Process_start || spread ;; Dispersal || prevent ;; Preventing_or_letting
subsided ;; Departing
controlled ;; Control || controlled ;; Control
conflict ;; Hostile_encounter || displaced ;; Destroying
built ;; Building || annihilated ;; Destroying
caused ;; Causation
extended ;; Expansion
transported ;; Sending
started ;; Process_start
extended ;; Expansion
came to ;; Arriving
offered ;; Giving || ordered ;; Arranging
hampered ;; Hindering || reached ;; Arriving
stay ;; Temporary_stay || evacuate ;; Rescuing || forced ;; Motion
sat ;; Placing
found ;; Know || reached ;; Arriving
save ;; Rescuing || burnt ;; Bodily_harm
escaped ;; Escaping || died ;; Death || hired ;; Employment
highlighted ;; Expressing_publicly
restored ;; Recovering
dismantled ;; Destroying
lies ;; Placing
died ;; Death
conference ;; Social_event || initiated ;; Creating
celebration ;; Social_event || came ;; Getting
conference ;; Social_event || expressed ;; Expressing_publicly
confirmed ;; Check
announced ;; Expressing_publicly
sent ;; Sending || Invitation ;; Request
unrest ;; Hostile_encounter || intention ;; Aiming || show ;; Publishing || commitment ;; Commitment
finding ;; Know || segment ;; Communication || underlined ;; Statement || joint ;; Collaboration
presented ;; Presence || prepared ;; GetReady || join ;; Becoming_a_member || based ;; Causation
conference ;; Social_event || announced ;; Expressing_publicly
meeting ;; Social_event || meeting ;; Social_event
cast ;; Giving || visit ;; Traveling
accused ;; Judgment_communication || visited ;; Traveling || said ;; Communication || meet ;; Come_together
stated ;; Statement || interview ;; Expressing_publicly || conference ;; Social_event || increasing ;; Expansion
occurred ;; Presence
triggered ;; Causation
killed ;; Killing || injured ;; Bodily_harm
investigations ;; Scrutiny || determine ;; Deciding
said ;; Communication || associated ;; Collaboration || addressing ;; Expressing_publicly || involving ;; Participation || occurred ;; Coming_to_be
effects ;; Influence
occurred ;; Coming_to_be || felt ;; Change_sentiment || said ;; Statement
issued ;; Statement
flew ;; Motion || approach ;; Arriving || operating ;; Being_in_operation
killed ;; Killing
crash ;; Catastrophe || involving ;; Cause_to_be_included || crash ;; Catastrophe || crash ;; Catastrophe
loss ;; Earnings_and_losses
crash ;; Catastrophe || due ;; Causation || Investigation ;; Criminal_investigation || cause ;; Causation || carried out ;; Military_operation || concluded ;; Statement
lost ;; Earnings_and_losses || approach ;; Arriving
flew ;; Motion
warnings ;; Warning || organized ;; Arranging || call ;; Request || investigation ;; Criminal_investigation || issue ;; Communication || showed ;; Statement || highlighted ;; Expressing_publicly
change ;; Change || orchestrated ;; Arranging || event ;; Social_event || advocate ;; Convincing || took place ;; Process_start
participants ;; Participation || change ;; Change || event ;; Social_event
change ;; Change || called in ;; Process_start || founded ;; Creating || endorsed ;; Agree_or_refuse_to_act
conceived ;; Coming_to_believe || response ;; Response
take place ;; Hold
event ;; Social_event || worldwide ;; Dispersal
organized ;; Arranging || event ;; Social_event
organized ;; Arranging || event ;; Social_event || events ;; Social_event || presentations ;; Expressing_publicly
used ;; Using || battle ;; Hostile_encounter || name ;; Name_conferral || used ;; Using || name ;; Name_conferral || victory ;; Earnings_and_losses
battle ;; Hostile_encounter || fought ;; Hostile_encounter
allowing ;; Preventing_or_letting || arrive ;; Arriving
trained ;; Education_teaching || battle ;; Hostile_encounter
retreat ;; Escaping || victory ;; Earnings_and_losses
start ;; Process_start || war ;; Hostile_encounter || end ;; Process_end || clamored ;; Quarreling || march ;; Self_motion || bring ;; Bringing
Yielding ;; Surrendering
plan ;; Arranging || attack ;; Attack || planning ;; Arranging || attack ;; Attack || found ;; Know || left ;; Departing || left ;; Departing
reinforcements ;; Cause_change_of_strength
arrived ;; Arriving || battle ;; Hostile_encounter || changed ;; Change
nickname ;; Name_conferral || receiving ;; Receiving || stood ;; Placing || resulted in ;; Causation
began ;; Process_start || withdrawing ;; Escaping || retreat ;; Escaping || turned ;; Becoming || counterattack ;; Attack || fire ;; Use_firearm || rout ;; Earnings_and_losses || launched ;; Motion
order ;; Arranging || ran ;; Self_motion
realized ;; Coming_to_believe || fighting ;; Hostile_encounter || going to ;; Motion || war ;; Hostile_encounter || casualties ;; Death
highlighted ;; Expressing_publicly || war ;; Hostile_encounter || problems ;; Confronting_problem || deficiencies ;; Resolve_problem
employ ;; Employment || attacks ;; Attack || exposed ;; Reveal_secret || committed ;; Commitment || protect ;; Defending
combined ;; Cause_to_amalgamate || Confederate ;; Collaboration || commit ;; Commitment || committed ;; Commitment
earthquake ;; Catastrophe || occurred ;; Coming_to_be
shock ;; Catastrophe || occurred ;; Coming_to_be
damage ;; Damaging || indicated ;; Statement || ravaged ;; Destroying
conflicts ;; Hostile_encounter || displaced ;; Causation || plagued ;; Dispersal
collapsed ;; Destroying
said ;; Expressing_publicly || casualties ;; Death
damage ;; Damaging || cause ;; Causation || earthquakes ;; Catastrophe
wreak ;; Damaging
quake ;; Catastrophe || damage ;; Damaging || centred ;; Surrounding || reported ;; Reporting || felt ;; Change_sentiment
killing ;; Killing || set ;; Destroying
injured ;; Bodily_harm || left ;; Causation || arrived at ;; Arriving || trapped ;; Preventing_or_letting
said ;; Expressing_publicly || killed ;; Killing
became ;; Becoming || due ;; Causation
arrived at ;; Arriving || show ;; Presence
doused ;; Filling || started ;; Action || descended ;; Departing || stormed ;; Attack || opened ;; Action || trapped ;; Bodily_harm
attack ;; Attack || classified ;; Check
festival ;; Social_event || Festival ;; Social_event
organized ;; Arranging || benefit ;; Earnings_and_losses || work ;; Employment
donations ;; Giving || festival ;; Social_event || make ;; Manufacturing || broadcast ;; Expressing_publicly || broadcast ;; Expressing_publicly || call ;; Request
festival ;; Social_event || performances ;; Hold || funds ;; Supply || released ;; Publishing || raise ;; Cause_change_of_position_on_a_scale
festival ;; Social_event
festival ;; Social_event || reading ;; Perception_active || consisted ;; Cause_to_be_included || speaking ;; Expressing_publicly
festival ;; Social_event || festival ;; Social_event || foundation ;; Creating
expressed ;; Expressing_publicly || festival ;; Social_event || reviving ;; Recovering
earthquake ;; Catastrophe || occurred ;; Coming_to_be
fatalities ;; Killing || shock ;; Response
earthquakes ;; Catastrophe
originated ;; Coming_to_be || called ;; Name_conferral
pose ;; Causation || reactivated ;; Recovering
performing ;; Action
implement ;; Action || highlighting ;; Expressing_publicly
fought ;; Hostile_encounter || war ;; Military_operation || war ;; Military_operation || lasting ;; Process_end
paid ;; Giving || started ;; Process_start || war ;; Military_operation || fighting ;; Hostile_encounter || expedition ;; Escaping || collapsed ;; Destroying
took place ;; Process_start || conflict ;; Hostile_encounter || War ;; Military_operation || interlude ;; Hostile_encounter || united ;; Collaboration
retreat ;; Escaping || resulted in ;; Causation || revolution ;; Change_of_leadership || gaining ;; Getting || War ;; Military_operation || control ;; Control || forcing ;; Request || establishing ;; Creating
legitimate ;; Legality || standoff ;; Hostile_encounter || claiming ;; Statement
ended ;; Process_end || signed ;; Sign_agreement || war ;; Military_operation || continues ;; Process_start || debate ;; Quarreling
opposed ;; Agree_or_refuse_to_act || agreement ;; Resolve_problem || called ;; Expressing_publicly
intensified ;; Cause_change_of_strength || brushed ;; Removing
crossed ;; Motion_directional
issued ;; Expressing_publicly
resulted in ;; Causation || produced ;; Creating
killed ;; Killing
caused ;; Causation
killed ;; Killing
took place ;; Process_start || claimed ;; Statement || marred ;; Damaging || election ;; Choosing
organized ;; Arranging || highlighted ;; Expressing_publicly
election ;; Choosing
ordered ;; Arranging
declared ;; Expressing_publicly
received ;; Receiving || showed ;; Presence
declared ;; Expressing_publicly || ended ;; Process_end
became ;; Becoming || declared ;; Expressing_publicly || conducted ;; Action || election ;; Choosing || election ;; Choosing || election ;; Choosing
ousted ;; Change_of_leadership
resulted in ;; Causation
contested ;; Competition || reigning ;; Change_of_leadership
win ;; Conquering || played ;; Participation || beat ;; Competition
match ;; Competition
appearance ;; Presence
illicit ;; Legality || match ;; Competition || given ;; Giving || sign ;; Writing
took ;; Conquering || scored ;; Getting
scored ;; Getting || replied ;; Response
interview ;; Expressing_publicly || praised ;; Judgment_communication || looked ;; Know
played ;; Competition || presented ;; Presence || likened ;; Judgment_communication
affected ;; Influence || floods ;; Catastrophe
beginning ;; Process_start || droughts ;; Catastrophe || experiencing ;; Perception_active || wave ;; Motion
steered ;; Hold || flooding ;; Catastrophe || brought ;; Bringing
flooding ;; Catastrophe || occurring ;; Presence || beginning ;; Process_start || leading to ;; Causation
gathered ;; Come_together || occurred ;; Presence || rain events ;; Catastrophe || travelled ;; Self_motion
caused ;; Causation || warnings ;; Warning || flood ;; Catastrophe || alerts ;; Warning || floods ;; Catastrophe || overwhelmed ;; Destroying || issued ;; Expressing_publicly || hit ;; Arriving
killed ;; Killing || halted ;; Process_end || caught ;; Getting || services ;; Supply
product ;; Creating || fronts ;; Self_motion || thunderstorms ;; Motion || travelling ;; Self_motion
caused ;; Causation || flooding ;; Catastrophe || forcing ;; Causation || alert ;; Warning || seen ;; Traveling || floods ;; Catastrophe || caused ;; Causation || travelling ;; Self_motion || floods ;; Catastrophe || struck ;; Attack || issue ;; Communication || rainfall ;; Catastrophe
flooding ;; Catastrophe || damage ;; Damaging || brought ;; Bringing
flooding ;; Catastrophe || occurred ;; Presence || overwhelmed ;; Destroying || rains ;; Motion
began ;; Process_start
scheduled ;; Arranging
given in ;; Presence
marked ;; Recording
appeared ;; Presence
cancelling ;; Change_event_time || diagnosed ;; Check || undergo ;; Cure || announced ;; Expressing_publicly
stated ;; Statement
announced ;; Expressing_publicly || playing ;; Motion
said ;; Statement
performed ;; Hold || played ;; Motion
performed ;; Hold
defended ;; Defending || attacks ;; Attack || battle ;; Hostile_encounter || battle ;; Hostile_encounter || campaign ;; Military_operation || war ;; Military_operation
fought ;; Hostile_encounter || described ;; Statement || campaign ;; Military_operation
battle ;; Hostile_encounter || attacks ;; Attack || recognise ;; Know
battle ;; Hostile_encounter || campaign ;; Military_operation
agree ;; Agree_or_refuse_to_act || forces ;; Motion || compel ;; Convincing
began ;; Process_start || targeting ;; Aiming || shipping ;; Sending || blockade ;; Besieging
shifted ;; Change || attacks ;; Attack || incapacitating ;; Destroying || directed ;; Aiming || Command ;; Request || achieve ;; Achieve
battle ;; Hostile_encounter || progressed ;; Cause_to_make_progress || targeted ;; Aiming || involved ;; Cause_to_be_included || production ;; Manufacturing
employed ;; Employment || bombing ;; Use_firearm
leaving ;; Departing || threat ;; Terrorism || invasion ;; Attack || overwhelmed ;; Destroying || face ;; Confronting_problem
attack ;; Attack || controlled ;; Control || command ;; Control || knew ;; Know
ordered ;; Arranging || assault ;; Attack
raids ;; Attack || disrupted ;; Hindering || converted ;; Change || postpone ;; Hindering || cancel ;; Process_end || forced ;; Causation
raids ;; Attack || became ;; Becoming || operations ;; Military_operation
destroy ;; Destroying || armistice ;; Sign_agreement || War ;; Military_operation || conflict ;; Hostile_encounter || cited ;; Statement || defeat ;; Conquering
given ;; Giving || battle ;; Hostile_encounter || takes ;; Conquering || speech ;; Expressing_publicly || called ;; Name_conferral
begin ;; Process_start || Battle ;; Hostile_encounter
conducted ;; Action
Conceived ;; Creating
lost ;; Earnings_and_losses || start ;; Process_start || throwing ;; Self_motion || called ;; Name_conferral || got ;; Getting
invaded ;; Attack || advanced ;; Cause_to_make_progress || destroying ;; Destroying
shifted ;; Change
reinforced ;; Cause_change_of_strength || supported ;; Supporting || diverted ;; Removing || arrival ;; Arriving
driven ;; Self_motion
resisted ;; Defending || threat ;; Warning
committed ;; Commitment || held ;; Defending || besieging ;; Besieging || leaving ;; Departing
defined ;; Statement || planned ;; Arranging
attacked ;; Attack
taken ;; Conquering || captured ;; Conquering || casualties ;; Catastrophe || counter-attacks ;; Attack || regained ;; Getting || causing ;; Causation
withdrawal ;; Escaping || caught ;; Self_motion || closed down ;; Openness
recaptured ;; Conquering || highlighted ;; Expressing_publicly
operating ;; Being_in_operation
accident ;; Catastrophe
terrorist ;; Terrorism || caused ;; Causation || diverted ;; Removing || involved ;; Participation
became ;; Becoming || parked ;; Placing || blocking ;; Hiding_objects || forcing ;; Hostile_encounter
control ;; Control || see ;; Perception_active || drifting ;; Motion
initiated ;; Process_start || shrouded ;; Hiding_objects || turn off ;; Motion || occurred ;; Coming_to_be || takeoff ;; Self_motion
killed ;; Killing || impact ;; Influence
investigation ;; Criminal_investigation || cause ;; Causation || takeoff ;; Self_motion || control ;; Control || concluded ;; Coming_to_believe || take off ;; Departing || issued ;; Creating
agreed ;; Agree_or_refuse_to_act || placed ;; Placing || compensate ;; Rewards_and_punishments
using ;; Using || highlighting ;; Expressing_publicly
contributing ;; Giving || training ;; Education_teaching
violence ;; Violence
wounded ;; Bodily_harm || killed ;; Killing
began ;; Process_start || organised ;; Arranging || working ;; Employment || assassinate ;; Attack || living ;; Placing
killed ;; Killing || wounded ;; Bodily_harm || went to ;; Motion || addresses ;; Expressing_publicly
killing ;; Killing || wounding ;; Bodily_harm || wounding ;; Bodily_harm || match ;; Competition || opened ;; Action
beaten ;; Bodily_harm || escape ;; Escaping || held ;; Hold || killed ;; Killing || claimed ;; Statement
damaged ;; Damaging || support ;; Supporting || increased ;; Cause_change_of_position_on_a_scale || considered ;; Statement
shot ;; Use_firearm || returning ;; Arriving || speaking ;; Expressing_publicly || engagement ;; Participation || wounded ;; Bodily_harm
attack ;; Use_firearm || played ;; Action || impress ;; Change_sentiment
seeing ;; Perception_active || developed ;; Coming_to_be
breaking ;; Destroying || wounded ;; Bodily_harm || causing ;; Causation || ricocheted ;; Motion || puncturing ;; Bodily_harm || bleeding ;; Bodily_harm || hit ;; Attack
stabilized ;; Recovering || underwent ;; Placing || death ;; Death || arrival ;; Arriving
released ;; Releasing || shot ;; Use_firearm || recovered ;; Recovering || becoming ;; Becoming || assassination ;; Attack
returned ;; Arriving || stated ;; Statement || control ;; Control || took place ;; Process_start
wounded ;; Bodily_harm
caused ;; Causation || damage ;; Damaging || death ;; Death || injury ;; Bodily_harm || suffered ;; Bodily_harm
subpoenaed ;; Criminal_investigation || testify ;; Protest || trial ;; Criminal_investigation || found ;; Know || assassinate ;; Attack
announced ;; Expressing_publicly || charge ;; Judgment_communication || death ;; Death || death ;; Death || homicide ;; Killing
killing ;; Killing || attack ;; Terrorism || suicide bombers ;; Terrorism || occurred ;; Coming_to_be || detonated ;; Committing_crime
injured ;; Bodily_harm || blasts ;; Catastrophe
transporting ;; Bringing || parade ;; Social_event || speculated ;; Coming_to_believe
injured ;; Bodily_harm || dead ;; Death || death ;; Death || put ;; Placing || put ;; Placing
statement ;; Statement || attack ;; Terrorism || death ;; Death || involved ;; Cause_to_be_included || issued ;; Statement
change ;; Change || attack ;; Terrorism || injured ;; Bodily_harm || explanation ;; Statement || said ;; Statement || mentioned ;; Expressing_publicly || provided ;; Giving
unite ;; Cause_to_amalgamate || assembly ;; Come_together || claim ;; Statement || rebellion ;; Change_of_leadership || uprising ;; Change_of_leadership || uprising ;; Change_of_leadership || response ;; Response
uprising ;; Change_of_leadership
unification ;; Cause_to_amalgamate || decision ;; Deciding
party ;; Participation || selection ;; Choosing || favouring ;; Deciding || unification ;; Cause_to_amalgamate
erupted ;; Process_start || named ;; Name_conferral || uprising ;; Change_of_leadership
support ;; Supporting || defeated ;; Conquering
destroyed ;; Destroying || participants ;; Participation || imprisoned ;; Arrest
fled ;; Escaping || retreated ;; Escaping || continuing ;; Hold || resistance ;; Defending || lasted ;; Process_end
occurred ;; Coming_to_be || issue ;; Deciding || call ;; Expressing_publicly || continued ;; Process_start || forced ;; Request || resist ;; Defending
shot down ;; Use_firearm || fired ;; Use_firearm
killed ;; Killing || destroyed ;; Destroying
hit ;; Attack || flying ;; Motion || departing ;; Departing
drew ;; Motion || entered ;; Arriving || warning ;; Warning || operating ;; Being_in_operation || limits ;; Limiting
disputed ;; Quarreling
identified ;; Check || attacking ;; Attack
supplied ;; Supply || equipped ;; Supply || briefed ;; Know
received ;; Receiving || response ;; Response || made ;; Action || contact ;; Communication
said ;; Telling
shot down ;; Use_firearm || identified ;; Check || used ;; Using
generated ;; Creating || deal ;; Resolve_problem || criticism ;; Judgment_communication
blamed ;; Judgment_communication
issued ;; Expressing_publicly || expressing ;; Expressing_publicly
agreed ;; Agree_or_refuse_to_act || recognized ;; Know || statement ;; Statement || legal ;; Legality || included ;; Cause_to_be_included || caused ;; Causation || apologize ;; Judgment_communication || reached ;; Achieve || expressed ;; Expressing_publicly || pay ;; Commerce_pay
disaster ;; Catastrophe || involving ;; Cause_to_be_included || occur ;; Coming_to_be
killed ;; Killing || trip ;; Traveling
found ;; Know
reported ;; Reporting || taken ;; Bringing
murder ;; Killing || spent ;; Expend_resource
released ;; Releasing || opened ;; Openness || found ;; Know
death ;; Death || supported ;; Supporting
supported ;; Supporting
murder ;; Killing || sentenced ;; Legal_rulings
given ;; Giving || convicted ;; Legality
criticised ;; Judgment_communication || aroused ;; Causation
made ;; Action || appeals ;; Request
release ;; Releasing || exhausted ;; Expend_resource || led to ;; Causation
overturned ;; Change
conducted ;; Action || resulted in ;; Causation
killed ;; Killing || delivered ;; Sending || taken ;; Bringing
released ;; Releasing || paid ;; Commerce_pay || issued ;; Expressing_publicly
written ;; Writing
released ;; Publishing || made ;; Creating
death ;; Death || reported ;; Reporting
announced ;; Expressing_publicly || attack ;; Attack || died ;; Death
received ;; Receiving || treatment ;; Cure
said ;; Statement || trip ;; Traveling
died ;; Death || reported ;; Reporting
announced ;; Expressing_publicly
funeral ;; Social_event
occurred ;; Presence || flood ;; Catastrophe
flood ;; Catastrophe || ranges ;; Cause_change_of_position_on_a_scale || occurred ;; Coming_to_be
escape ;; Escaping || floods ;; Catastrophe
wounded ;; Bodily_harm || flood ;; Catastrophe || killed ;; Killing
casualties ;; Death || died ;; Death || receiving ;; Receiving || medical care ;; Institutionalization
affected ;; Influence || flood ;; Catastrophe
damaged ;; Damaging
rescue ;; Rescuing || operations ;; Rescuing || made ;; Causation || blocked ;; Hindering
devastating ;; Destroying || affected ;; Influence
impacted ;; Influence || flood ;; Catastrophe
stop ;; Process_end || flight ;; Self_motion || scheduled ;; Arranging
crashed ;; Catastrophe || killing ;; Killing || operating ;; Being_in_operation
occurred ;; Presence || investigated ;; Criminal_investigation || crash ;; Catastrophe
asked ;; Request || investigation ;; Criminal_investigation || handle ;; Hold
investigation ;; Criminal_investigation || crash ;; Catastrophe || taken place ;; Process_start || Investigation ;; Criminal_investigation || accidental ;; Catastrophe || crash ;; Catastrophe || criminal ;; Committing_crime || proposed ;; Statement || handing ;; Giving || suggested ;; Statement
investigation ;; Criminal_investigation || proposal ;; Convincing
found ;; Know || cause ;; Causation || accident ;; Catastrophe || departure ;; Departing || impact ;; Influence || relief ;; Cure || control ;; Control
determined ;; Deciding || reason ;; Protest || relief ;; Cure
caused ;; Causation || control ;; Control || incident ;; Catastrophe || concluded ;; Coming_to_believe
causes ;; Causation || control ;; Control || focusing ;; Aiming || control ;; Control || crash ;; Catastrophe || report ;; Reporting || suggested ;; Statement
stated ;; Statement || impact ;; Influence || report ;; Reporting || relief ;; Cure || control ;; Control || reason ;; Protest || relief ;; Cure || report ;; Reporting || determine ;; Deciding
support ;; Supporting || concert ;; Social_event || hits ;; Attack
began ;; Process_start || concluded ;; Process_end || consisting ;; Containing
postponed ;; Change_event_time || tour ;; Traveling || diagnosed ;; Cure
renamed ;; Name_conferral || revised ;; Change || resumed ;; Recovering
placed ;; Placing || grossed ;; Earnings_and_losses
sold ;; Commerce_sell
gain ;; Getting
organized ;; Arranging || carried out ;; Military_operation
demanded ;; Request
set up ;; Building || stayed ;; Temporary_stay || organized ;; Arranging || presenting ;; Presence
motivated ;; Influence
observing ;; Know || improved ;; Cause_to_make_progress || gains ;; Getting || shifted ;; Change
alleviating ;; Cure
declared ;; Expressing_publicly || identified ;; Check
changing ;; Change || grow ;; Expansion
became ;; Becoming || suffered ;; Bodily_harm || recognized ;; Know || amplified ;; Expansion
focus ;; Aiming || helping ;; Assistance || saw ;; Self_motion
address ;; Change
help ;; Assistance || uniting ;; Cause_to_amalgamate || presenting ;; Presence || start ;; Process_start || races ;; Competition || plan ;; Arranging || dramatizing ;; Expansion
asked ;; Request || helping ;; Assistance
said ;; Communication || demands ;; Request || ending ;; Process_end || war ;; Hostile_encounter || opening ;; Openness || war ;; Hostile_encounter
bring ;; Bringing || see ;; Perception_active || come ;; Self_motion || forcing ;; Request || get ;; Getting
sit down ;; Temporary_stay || come to ;; Self_motion || stay ;; Temporary_stay || say ;; Communication || come to ;; Self_motion
earthquake ;; Catastrophe || earthquake ;; Catastrophe || occurred ;; Coming_to_be
caused ;; Causation || earthquake ;; Catastrophe || damage ;; Damaging
shaking ;; Motion
damage ;; Damaging || recorded ;; Recording || resulting ;; Causation
treated ;; Cure || injuries ;; Bodily_harm || killed ;; Killing || earthquake ;; Catastrophe || resulting ;; Causation
resulted in ;; Causation || closure ;; Process_end
cyclone ;; Catastrophe
tying ;; Connect
formed ;; Coming_to_be || hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || reaching ;; Arriving || passing ;; Motion_directional
emerged ;; Coming_to_be || crossed ;; Motion_directional || weakened ;; Cause_change_of_strength || remained ;; Presence || hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength
drifted ;; Motion || dissipated ;; Dispersal || weakening ;; Cause_change_of_strength
tied ;; Connect || cyclone ;; Catastrophe
caused ;; Causation || damages ;; Damaging || hurricane ;; Catastrophe || storm ;; Catastrophe || deaths ;; Death
devastated ;; Destroying || impacting ;; Influence
caused ;; Causation || storm ;; Catastrophe || reaching ;; Arriving || deaths ;; Death || damage ;; Damaging || storm ;; Catastrophe || passed ;; Motion_directional
took ;; Conquering || affected ;; Influence || repair ;; Cure
aid ;; Assistance || rebuilding ;; Building || destroyed ;; Destroying || joined ;; Becoming_a_member
production ;; Manufacturing || damage ;; Damaging || return ;; Motion
took ;; Conquering || rebuild ;; Building || damaged ;; Damaging
hurricane ;; Catastrophe
damaging ;; Damaging || damage ;; Damaging || resulted in ;; Causation || naming ;; Name_conferral || populated ;; Surrounding
battle ;; Hostile_encounter || attack ;; Attack || force ;; Causation || battle ;; Hostile_encounter || war ;; Military_operation
battle ;; Hostile_encounter || attacks ;; Attack || created ;; Creating || dissolved ;; Dispersal || fought ;; Hostile_encounter
achieved ;; Achieve || supplies ;; Supply || devastated ;; Destroying || attacks ;; Attack || turned ;; Change || increased ;; Cause_change_of_position_on_a_scale || battlefield ;; Hostile_encounter || transporting ;; Military_operation
diverted ;; Removing || managed ;; Control || recovery ;; Cause_change_of_position_on_a_scale || battle ;; Hostile_encounter || stripped ;; Removing
command ;; Control || challenge ;; Competition || centralised ;; Cause_to_amalgamate
patrols ;; Patrolling || reduced ;; Cause_change_of_position_on_a_scale || contact ;; Connect || support ;; Supporting
lost ;; Earnings_and_losses || increased ;; Cause_change_of_position_on_a_scale || battle ;; Hostile_encounter
loss ;; Earnings_and_losses || equipment ;; Supply || reinforcement ;; Cause_change_of_strength || Battle ;; Hostile_encounter || contributed ;; Causation || defeat ;; Conquering
supplies ;; Supply || carry ;; Sending || forced ;; Causation
reduced ;; Cause_change_of_position_on_a_scale || established ;; Building || increased ;; Cause_change_of_position_on_a_scale || attacks ;; Attack
caused ;; Causation || offensive ;; Attack
enabled ;; Preventing_or_letting || relieve ;; Cure || benefited ;; Influence
attacks ;; Attack || attacks ;; Attack || supported ;; Supporting || criticism ;; Judgment_communication || war ;; Military_operation || offensive ;; Attack || put ;; Placing || established ;; Creating
concert ;; Social_event || concert ;; Social_event
treatments ;; Cure || research ;; Research
founded ;; Creating
diagnosed ;; Cure || cancer ;; Cure || funded ;; Supply || set up ;; Placing || response ;; Response
succumbed ;; Defending
concert ;; Social_event || left ;; Departing
departure ;; Departing || work ;; Employment || working ;; Employment || creating ;; Creating
moved ;; Motion
reported ;; Reporting || prevention ;; Preventing_or_letting || joint ;; Collaboration || working ;; Employment || aimed ;; Aiming
moved ;; Motion || called ;; Name_conferral || quit ;; Process_end
developed ;; Coming_to_be
fire ;; Catastrophe
injuring ;; Bodily_harm || fire ;; Catastrophe || claiming ;; Statement || lives ;; Death
shocked ;; Change_sentiment
changes ;; Change || reform ;; Reforming_a_system || treatment ;; Cure || rehabilitation ;; Cure || led to ;; Causation
death ;; Death || fire ;; Catastrophe || fire ;; Catastrophe
killed ;; Killing || fire ;; Catastrophe
battle ;; Hostile_encounter || Battle ;; Hostile_encounter
supervision ;; Scrutiny || took place ;; Process_start || commanded ;; Control || commanded ;; Control
exploded ;; Attack || forces ;; Causation || blowing ;; Attack
beginning ;; Process_start || attackers ;; Attack || deteriorated ;; Change
charged ;; Attack
assault ;; Attack || witnessed ;; Know
launched ;; Motion || counterattacks ;; Attack || recovered ;; Recovering
casualties ;; Catastrophe || repulsed ;; Defending || sealed ;; Filling
mauled ;; Bodily_harm
end ;; Process_end
warfare ;; Hostile_encounter || settled ;; Resolve_problem
relieved ;; Cure || debacle ;; Catastrophe || returned ;; Arriving || command ;; Control || command ;; Control
died ;; Death
described ;; Statement
caused ;; Causation
contributed ;; Causation
spent ;; Expend_resource || disputed ;; Quarreling
found ;; Know
taken ;; Conquering || called ;; Request || made ;; Action || resuscitate ;; Cure
pronounced ;; Statement || dead ;; Death
concluded ;; Process_end || died ;; Death || examination ;; Scrutiny
recorded ;; Recording || finding ;; Know || suicide ;; Killing || verdict ;; Legal_rulings || inquest ;; Scrutiny
stated ;; Statement || taken ;; Motion
interred ;; Placing
asked ;; Request || death ;; Death || reopen ;; Process_start || investigation ;; Criminal_investigation
decided ;; Deciding || proved ;; Statement || proceeding ;; Process_start || inquiry ;; Criminal_investigation || investigation ;; Criminal_investigation
wrestling ;; Competition || produced ;; Manufacturing
established ;; Building || died ;; Death || battle ;; Hostile_encounter
presented ;; Presence || rising ;; Cause_change_of_position_on_a_scale || death ;; Death || held ;; Defending
held ;; Defending
participated ;; Participation
hosted ;; Come_together
match ;; Competition || came ;; Motion
claimed ;; Statement || used ;; Using
competed ;; Competition || death ;; Death
opening ;; Openness || interview ;; Communication || eliminated ;; Removing || praised ;; Judgment_communication
used ;; Using || beginning ;; Process_start
form ;; Coming_to_be || diagnosed ;; Cure || went to ;; Self_motion
assisted ;; Assistance
called ;; Name_conferral || capture ;; Conquering
siege ;; Besieging
suffered ;; Bodily_harm || prevented ;; Preventing_or_letting
sent ;; Sending || relieve ;; Cure || defeated ;; Conquering
surrendered ;; Surrendering || incorporated ;; Cause_to_amalgamate
used ;; Using
defense ;; Defending || began ;; Process_start || killing ;; Killing
attacks ;; Attack || preceded ;; Process_start
began ;; Process_start || attack ;; Attack || caused ;; Causation || response ;; Response
stated ;; Statement || attacks ;; Attack || aims ;; Aiming || disrupt ;; Hindering || strip ;; Emptying
violence ;; Violence || accusing ;; Judgment_communication || blamed ;; Judgment_communication || attacks ;; Attack
cited ;; Adducing || attacks ;; Attack || reason ;; Protest
blocks ;; Hiding_objects || claimed ;; Statement || struck ;; Attack || strip ;; Emptying
killed ;; Killing || wounded ;; Bodily_harm || report ;; Reporting
displaced ;; Destroying
killed ;; Killing
caused ;; Causation || landing ;; Self_motion || strip ;; Emptying || casualties ;; Killing
alleged ;; Statement
attacks ;; Attack || firing ;; Use_firearm || fell ;; Motion_directional || intensified ;; Cause_change_of_strength
used ;; Using
fired ;; Use_firearm
fired ;; Use_firearm || hit ;; Attack
killed ;; Killing || hit ;; Attack
killed ;; Killing || treated ;; Cure || injured ;; Bodily_harm
defense ;; Defending || hit ;; Attack || fell ;; Motion_directional || fell ;; Motion_directional || intercepted ;; Preventing_or_letting
injuring ;; Bodily_harm || bombed ;; Attack
defend ;; Defending || condemned ;; Judgment_communication || attacks ;; Attack || expressed ;; Sending
condemned ;; Judgment_communication
held ;; Defending || decision ;; Deciding || reach ;; Arriving
negotiations ;; Communication || announced ;; Expressing_publicly
claimed ;; Statement
achieved ;; Achieve || ended ;; Process_end || stated ;; Statement || invading ;; Attack || said ;; Communication
fighting ;; Hostile_encounter
known ;; Know || Typhoon ;; Catastrophe || contribute ;; Creating
developed ;; Coming_to_be
tracked ;; Motion || attained ;; Achieve
turned ;; Becoming || passing ;; Motion_directional
dropped ;; Motion_directional || alleviated ;; Cure
occurred ;; Coming_to_be || damage ;; Damaging
began ;; Process_start || weakening ;; Cause_change_of_strength || affecting ;; Influence
passed ;; Motion_directional || produced ;; Creating
lost ;; Earnings_and_losses || killed ;; Killing
damage ;; Damaging || injury ;; Bodily_harm
passed ;; Motion_directional || weakening ;; Cause_change_of_strength
crossed ;; Motion_directional || storm ;; Catastrophe
affected ;; Influence
took place ;; Process_start
conducted ;; Action || fought ;; Hostile_encounter || constructed ;; Building
repelled ;; Defending || delayed ;; Change_event_time || reach ;; Arriving || relieve ;; Cure
ended ;; Process_end || put ;; Placing
operate ;; Being_in_operation || denied ;; Agree_or_refuse_to_act
enabled ;; Preventing_or_letting || lost ;; Earnings_and_losses || declare ;; Expressing_publicly
operated ;; Being_in_operation
began ;; Process_start || converted ;; Change
killing ;; Killing || escaped ;; Escaping || destroyed ;; Destroying
recovered ;; Cure
dedicated ;; Giving
revolt ;; Change_of_leadership
revolt ;; Change_of_leadership || caused ;; Causation || spread ;; Dispersal
demanded ;; Request || relief ;; Cure || ruling ;; Legal_rulings
organized ;; Arranging || threatened ;; Warning || demands ;; Request || dismissed ;; Response
united ;; Cause_to_amalgamate || forming ;; Coming_to_be
became ;; Becoming || going ;; Self_motion
assumed ;; Choosing
siege ;; Besieging || negotiated ;; Communication || laid ;; Placing
retreated ;; Escaping
sent ;; Sending || end ;; Process_end || disbanded ;; Emptying
broken ;; Destroying || end ;; Process_end
took ;; Bringing
declared ;; Expressing_publicly
tortured ;; Bodily_harm || received ;; Receiving || captured ;; Conquering
beheaded ;; Killing || quartered ;; Temporary_stay
complete ;; Process_end
reforms ;; Reforming_a_system || lowered ;; Cause_change_of_position_on_a_scale || instituted ;; Action
prevented ;; Preventing_or_letting || occurred ;; Coming_to_be
fought ;; Hostile_encounter
brought ;; Bringing || reversed ;; Change
started ;; Process_start
struck ;; Attack || armed ;; Bearing_arms
fought ;; Quarreling || brought ;; Bringing
replaced ;; Change_of_leadership
invaded ;; Attack || relieved ;; Cure
seized ;; Hold || went ;; Self_motion
dubbed ;; Name_conferral || give ;; Giving || capitalize ;; Hold || election ;; Choosing || election ;; Choosing
aided ;; Assistance || supported ;; Supporting
refused ;; Agree_or_refuse_to_act || surrender ;; Surrendering
reverted ;; Change
followed ;; Motion
provided ;; Supply || blended ;; Cause_to_amalgamate
set up ;; Reforming_a_system || conquered ;; Conquering
destroyed ;; Destroying
forced ;; Motion
died ;; Death
mounted ;; Connect || tracked ;; Recording
died ;; Death
ended ;; Process_end || surrendered ;; Surrendering || accepted ;; Receiving
incorporated ;; Cause_to_amalgamate
took place ;; Process_start || attacked ;; Attack || Capture ;; Conquering
fought ;; Quarreling || attacked ;; Attack
took ;; Conquering || Reinforced ;; Cause_change_of_strength || attacked ;; Attack
began ;; Process_start || battle ;; Hostile_encounter || dig ;; Self_motion || stabilised ;; Cause_change_of_strength
conducted ;; Action || attack ;; Attack || repulsed ;; Defending
took place ;; Process_start || began ;; Process_start || became ;; Becoming || established ;; Creating
began ;; Process_start || began ;; Process_start || improved ;; Cause_to_make_progress || defend ;; Defending || marked ;; Recording
destroyed ;; Destroying || cut ;; Removing
taken ;; Conquering || caused ;; Causation || overrun ;; Destroying || advanced ;; Cause_to_make_progress || suffer ;; Bodily_harm
attack ;; Attack || occupied ;; Control
began ;; Process_start || reached ;; Arriving || ready ;; GetReady || receive ;; Receiving || collapsed ;; Destroying
attacks ;; Attack || pushed ;; Motion || relieved ;; Cure
recaptured ;; Conquering || retire ;; Escaping || Dismounted ;; Departing
retaken ;; Conquering
fought ;; Hostile_encounter || War ;; Military_operation
capture ;; Conquering || lost it ;; Earnings_and_losses
besieged ;; Besieging || defense ;; Defending || beg ;; Convincing || retreat ;; Escaping || arrival ;; Arriving || relieving ;; Cure || forced ;; Request
gather ;; Come_together || war ;; Hostile_encounter || conclusion ;; Process_end || ordered ;; Request
began ;; Process_start || siege ;; Besieging || celebrating ;; Social_event || joined ;; Becoming_a_member || marched ;; Self_motion || arrived ;; Arriving
hold ;; Defending || respond ;; Response || slow ;; Change_event_time || information ;; Getting || reinforcements ;; Cause_change_of_strength
asked ;; Request || evaded ;; Escaping || lose ;; Earnings_and_losses
siege ;; Besieging || relief ;; Assistance || siege ;; Besieging || followed ;; Self_motion || assemble ;; Come_together || proved ;; Convincing || raise ;; Creating || fell ;; Conquering
fortifying ;; Cause_change_of_strength || appointing ;; Change_of_leadership || returned ;; Arriving || troubling ;; Preventing_or_letting
accepting ;; Receiving || arrived at ;; Arriving || loss ;; Earnings_and_losses || conflict ;; Hostile_encounter || putting an end to ;; Process_end || rebellion ;; Attack || absorbed ;; Response
occurred ;; Presence || causing ;; Causation || exploded ;; Attack || bombing ;; Terrorism
occurred ;; Presence || derailment ;; Motion
killed ;; Killing || retracted ;; Agree_or_refuse_to_act || stated ;; Statement || injured ;; Bodily_harm
deaths ;; Death || reported ;; Reporting
exploded ;; Attack || injuring ;; Bodily_harm || bomb ;; Terrorism
reported ;; Reporting || triggered ;; Causation
treat ;; Cure || set up ;; Reforming_a_system
affected ;; Influence || carrying ;; Carry_goods
blamed ;; Judgment_communication || crater ;; Destroying || caused ;; Causation || showed ;; Statement || found ;; Know || terrorism ;; Terrorism
confirmed ;; Check || caused ;; Causation
caused ;; Causation || damage ;; Damaging || hit ;; Bodily_harm || cyclone ;; Catastrophe
originated ;; Coming_to_be
intensification ;; Expansion || reaching ;; Arriving
weakening ;; Cause_change_of_strength
moving ;; Motion || became ;; Becoming || degenerated ;; Change || weakened ;; Cause_change_of_strength
strike ;; Attack || cyclone ;; Catastrophe
produced ;; Creating
caused ;; Causation || dropped ;; Motion_directional || rainfall ;; Motion || cyclone ;; Catastrophe || flooding ;; Catastrophe
destroyed ;; Destroying
damaged ;; Damaging || relief ;; Cure || cyclone ;; Catastrophe || disrupted ;; Hindering
killed ;; Killing || flooding ;; Catastrophe
destroyed ;; Destroying || leaving ;; Causation
killed ;; Killing || caused ;; Causation || damage ;; Damaging
deployed ;; Arranging || hampered ;; Hindering || help ;; Assistance || storm ;; Catastrophe || storm ;; Catastrophe
causing ;; Causation || rise ;; Cause_change_of_position_on_a_scale
donated ;; Giving || supplies ;; Supply
took place ;; Process_start
providing ;; Supply || landing ;; Self_motion || capture ;; Conquering
defended ;; Defending || said ;; Statement
advancing ;; Cause_to_make_progress || captured ;; Conquering || held ;; Defending || relieved ;; Cure
prevent ;; Preventing_or_letting || capture ;; Conquering || leave ;; GiveUp || cut off ;; Dispersal
retained ;; Preserving || used ;; Using
fell ;; Motion_directional
attached ;; Connect || comprised ;; Cause_to_be_included
flew ;; Motion
delivered ;; Sending || flying ;; Self_motion || described ;; Statement
defended ;; Defending || arrived ;; Arriving || captured ;; Conquering
known ;; Statement || race ;; Competition
took place ;; Process_start || steeplechase ;; Competition || concluded ;; Process_end || meeting ;; Social_event || held ;; Hold
ran ;; Self_motion || competing ;; Competition || making it ;; Causation
changes ;; Change || made ;; Change || deaths ;; Death
changes ;; Change || reducing ;; Cause_change_of_position_on_a_scale || participating ;; Participation || placed ;; Placing || raising ;; Cause_change_of_position_on_a_scale
beating ;; Competition || ridden ;; Self_motion || race ;; Competition || finish ;; Process_end || National ;; Competition
became ;; Becoming || win ;; Getting
finished ;; Process_end || scoring ;; Cause_to_make_progress || resulted in ;; Causation
euthanised ;; Killing
treated ;; Cure || broke ;; Bodily_harm || omitted ;; Removing
race ;; Competition || televised ;; Communication || broadcast ;; Expressing_publicly
broadcast ;; Expressing_publicly
aired ;; Expressing_publicly || held ;; Hold
unsold ;; Commerce_sell || burial ;; Placing || burial ;; Placing || undertaken ;; Action
buried ;; Placing
released ;; Publishing || maligned ;; Judgment_communication
burial ;; Placing || reported ;; Reporting || leading to ;; Causation
become ;; Becoming || event ;; Placing || crash ;; Catastrophe || sold ;; Commerce_sell
believed ;; Know
verified ;; Check || disposed ;; Removing
called ;; Name_conferral || validate ;; Change || excavate ;; Emptying
revealed ;; Reveal_secret || discarded ;; GiveUp || excavation ;; Emptying
given ;; Giving || auctioned ;; Commerce_sell || recovered ;; Cure || burial ;; Placing || excavation ;; Emptying || curation ;; Cure || raise ;; Cause_change_of_position_on_a_scale
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || start ;; Process_start || entry ;; Process_start || took place ;; Process_start
occurred ;; Presence || offensive ;; Attack || reversed ;; Change
battle ;; Hostile_encounter || fought ;; Hostile_encounter || support ;; Supporting
capturing ;; Conquering || consolidate ;; Cause_change_of_strength || ordered ;; Arranging || pull ;; Motion
attacked ;; Attack || success ;; Achieve || cut off ;; Hindering || withdrew ;; Escaping
ordered ;; Arranging || defend ;; Defending || halt ;; Preventing_or_letting
pushing ;; Motion
force ;; Control || turned ;; Change || advancing ;; Cause_to_make_progress || threatening ;; Warning || cutting ;; Bodily_harm
attacked ;; Attack || guarding ;; Patrolling
counter-attacked ;; Attack || forces ;; Causation || counter-attacked ;; Attack || pushed ;; Motion
captured ;; Conquering || withdrawal ;; Escaping || decisions ;; Deciding || battle ;; Hostile_encounter || defended ;; Defending || limited ;; Limiting || confused ;; Perception_active || command ;; Control || resulted in ;; Causation || contact ;; Communication || held ;; Defending
withdrawal ;; Escaping || open ;; Openness || ordered ;; Arranging || left ;; Departing || regain ;; Getting || threatened ;; Warning
withdraw ;; Escaping || observed ;; Perception_active || fighting ;; Hostile_encounter || ceased ;; Process_end
checked ;; Preventing_or_letting || redeployed ;; Placing
fighting ;; Hostile_encounter
casualties ;; Catastrophe || losses ;; Earnings_and_losses || halted ;; Preventing_or_letting || suffered ;; Bodily_harm
relieved ;; Cure || aftermath ;; Catastrophe || death ;; Death || taken over ;; Conquering
preventing ;; Preventing_or_letting || withdrawal ;; Escaping || left ;; Departing || keeping ;; Temporary_stay || securing ;; Getting
casualties ;; Catastrophe || Suffering ;; Bodily_harm || due ;; Causation || halted ;; Process_end
forced ;; Motion || holding ;; Defending || withdraw ;; Escaping || reinforced ;; Cause_change_of_strength || positions ;; Placing
forced ;; Motion || began ;; Process_start || retreat ;; Escaping || withdraw ;; Escaping || starting ;; Process_start
withdrew ;; Escaping || defensive ;; Defending || positions ;; Placing || forces ;; Causation
held ;; Defending
hosting ;; Come_together || Built ;; Building
used ;; Using || help ;; Assistance || test ;; Research || roped ;; Connect
restores ;; Recovering || dropped ;; Motion_directional
given ;; Giving || deliver ;; Sending || swimming ;; Self_motion || record ;; Recording
include ;; Cause_to_be_included
contest ;; Competition || Training ;; Education_teaching || contested ;; Competition
given ;; Giving
describe ;; Statement || used ;; Using
emerged ;; Coming_to_be || Roaring ;; Motion || attracting ;; Influence
repressive ;; Hindering || embodied ;; Judgment_communication || perceived ;; Know
involved ;; Cause_to_be_included
enabled ;; Preventing_or_letting
investigating ;; Research || told ;; Telling || raided ;; Attack
represented ;; Change_sentiment
hide ;; Hiding_objects || open ;; Openness || made ;; Change
helped ;; Assistance || devastating ;; Destroying || spawned ;; Coming_to_be || decline ;; Agree_or_refuse_to_act || shipping ;; Sending || offset ;; Motion
made ;; Causation
ended ;; Process_end || end ;; Process_end
bombings ;; Terrorism || elections ;; Choosing
injured ;; Bodily_harm || killed ;; Killing
terrorist ;; Terrorism || carried out ;; Action || attack ;; Attack || bombing ;; Terrorism
found ;; Know || directed ;; Action || investigation ;; Criminal_investigation || attacks ;; Terrorism
arrested ;; Arrest
arose ;; Coming_to_be || party ;; Participation || reasons ;; Causation
elections ;; Choosing
bombing ;; Use_firearm
seen ;; Causation || taking ;; Conquering
attacks ;; Attack || demonstrations ;; Social_event
terrorist ;; Terrorism || attacks ;; Attack || analysts ;; Research || elections ;; Change_of_leadership || lost ;; Earnings_and_losses
attack ;; Terrorism || investigation ;; Criminal_investigation
sentence ;; Legal_rulings
investigate ;; Research || established ;; Creating
investigated ;; Research
giving rise to ;; Causation
investigated ;; Research || investigations ;; Scrutiny || collect ;; Come_together
ran ;; Being_in_operation || reach ;; Earnings_and_losses
began ;; Process_start
published ;; Publishing || published ;; Publishing
resigned ;; Change_of_leadership
conducted ;; Action || determined ;; Deciding || Census ;; Research || increase ;; Cause_change_of_position_on_a_scale
record ;; Recording || census ;; Research
achieved ;; Achieve || asked ;; Request
asked ;; Request || census ;; Research || census ;; Research || introduced ;; Statement
test ;; Research || census ;; Research
census ;; Research
forces ;; Military_operation || conflict ;; Violence || clashes ;; Violence
rise ;; Change_of_leadership || beginning ;; Process_start || extension ;; Expansion || involved ;; Participation || War ;; Hostile_encounter
marked ;; Recording || conflict ;; Violence || taking place ;; Process_start
found ;; Know || participating ;; Participation || support ;; Supporting || support ;; Supporting || save ;; Assistance || intervened ;; Hold || war ;; Hostile_encounter
support ;; Supporting || scouting ;; Scrutiny || fighting ;; Hostile_encounter || delineated ;; Know || providing ;; Supply || war ;; Hostile_encounter || assumed ;; Response
changed ;; Change || occupation ;; Attack || forces ;; Military_operation || war ;; Hostile_encounter || seen ;; Know || united ;; Cause_to_amalgamate || opposing ;; Defending
found ;; Know || deprived ;; Rewards_and_punishments || provided ;; Supply || forces ;; Military_operation || confronting ;; Confronting_problem || cancelling ;; Removing
expelled ;; Removing || putting an end to ;; Process_end || forces ;; Military_operation || conflict ;; Violence || War ;; Hostile_encounter || followed ;; Motion
claimed ;; Statement || annex ;; Becoming_a_member
became ;; Becoming || control ;; Control || expulsion ;; Removing || use ;; Using || expand ;; Expansion || evaluation ;; Research || creation ;; Creating || ties ;; Connect || secure ;; Assistance || maintain ;; Preserving
involved ;; Cause_to_be_included
test ;; Research || resulted in ;; Causation
sucked ;; Motion || ignited ;; Process_start
crashed ;; Attack
occurred ;; Coming_to_be
caused ;; Causation || flooding ;; Catastrophe
developed ;; Coming_to_be || moved ;; Motion || struck ;; Bodily_harm
turned ;; Motion || strengthened ;; Cause_change_of_strength || crossing ;; Motion_directional || accelerated ;; Influence
improve ;; Cause_to_make_progress || produced ;; Causation
spread ;; Dispersal || changes ;; Change || blamed ;; Judgment_communication || weaken ;; Cause_change_of_strength || experiment ;; Research || made history ;; Creating || targeted ;; Aiming || storm ;; Catastrophe
turned ;; Motion || making ;; Creating || slowed ;; Cause_change_of_position_on_a_scale
damage ;; Damaging || limited ;; Limiting || produced ;; Causation
losses ;; Earnings_and_losses || caused ;; Causation || dissipated ;; Removing
violence ;; Violence
violence ;; Violence || outbreaks ;; Process_start || outbreaks ;; Process_start || violence ;; Violence
violence ;; Violence || caused ;; Causation || instigated ;; Causation || burning ;; Use_firearm
ended ;; Process_end || dead ;; Death || injured ;; Bodily_harm
killed ;; Killing
destruction ;; Destroying || killings ;; Killing || rapes ;; Violence || looting ;; Committing_crime
violence ;; Violence || accused ;; Judgment_communication || gave ;; Giving || initiating ;; Process_start || directed ;; Aiming
rejected ;; Response
reacted ;; Response || reported ;; Reporting
suppressed ;; Hindering || allegations ;; Criminal_investigation
rejected ;; Response || prosecution ;; Legality
expressed ;; Expressing_publicly || rejected ;; Response
alleging ;; Statement || attacks ;; Attack
stated ;; Statement
violence ;; Violence || killed ;; Death || incidents ;; Attack
state ;; Statement || studying ;; Research
participated ;; Participation || change ;; Change || protests ;; Request || immigration ;; Escaping
noted ;; Know || mobilizations ;; Removing || influence ;; Influence || participation ;; Participation
response ;; Communication || began ;; Process_start || proposed ;; Statement
illegal ;; Legality || helped ;; Assistance || raise ;; Cause_change_of_position_on_a_scale || immigration ;; Escaping || penalties ;; Rewards_and_punishments || classify ;; Check
reform ;; Reforming_a_system || debate ;; Communication || protests ;; Preventing_or_letting || rejection ;; Agree_or_refuse_to_act
began ;; Process_start || protests ;; Preventing_or_letting || demonstrations ;; Protest
participants ;; Participation || demonstration ;; Protest || held ;; Hold
impetus ;; Motion
occurred ;; Presence || marching ;; Self_motion || protest ;; Agree_or_refuse_to_act
participants ;; Participation || state ;; Statement || revised ;; Change || estimated ;; Cause_change_of_position_on_a_scale || analysis ;; Research
occurred ;; Presence || protest ;; Agree_or_refuse_to_act
protests ;; Agree_or_refuse_to_act || attracted ;; Influence
protests ;; Agree_or_refuse_to_act || took place ;; Process_start
enabling ;; Preventing_or_letting || researched ;; Research
documented ;; Recording || led to ;; Causation
allowing ;; Preventing_or_letting || utilized ;; Using || improve ;; Cause_to_make_progress
causes ;; Causation
involved ;; Cause_to_be_included
collected ;; Come_together || led to ;; Causation
concluded ;; Process_end
reputed ;; Judgment_communication
reported ;; Reporting || fires ;; Catastrophe
fires ;; Catastrophe || fires ;; Catastrophe || reached ;; Cause_change_of_position_on_a_scale || burning ;; Destroying
blown ;; Motion || leading ;; Causation || declare ;; Expressing_publicly || generated ;; Creating || risk ;; Catastrophe || advisory ;; Warning || fires ;; Catastrophe || created ;; Creating
reached ;; Arriving
drifting ;; Motion || observed ;; Know
fires ;; Catastrophe || fire ;; Catastrophe || fire ;; Catastrophe
consumed ;; Expend_resource
burnt ;; Destroying
suggested ;; Statement || fires ;; Catastrophe || study ;; Research || drove ;; Bringing
emitted ;; Creating
produced ;; Manufacturing || took place ;; Process_start
event ;; Social_event
scheduled ;; Arranging
match ;; Competition || defeat ;; Conquering || retain ;; Preserving
defeated ;; Conquering || test ;; Research
defending ;; Defending || retain ;; Preserving
debut ;; Process_start || returns ;; Arriving
departed ;; Departing
crashed ;; Catastrophe
killed ;; Killing || accident ;; Catastrophe
loss ;; Death || crashed ;; Catastrophe || permitted ;; Agree_or_refuse_to_act || fly ;; Self_motion
caused ;; Causation || caused ;; Causation || accident ;; Catastrophe || separate ;; Forming_relationships
investigating ;; Research || accident ;; Catastrophe
found ;; Know || investigated ;; Research || accident ;; Catastrophe
made ;; Action || fixed ;; Resolve_problem
called ;; Name_conferral
fought ;; Hostile_encounter
employed ;; Employment || supported ;; Supporting
achieving ;; Achieve || planned ;; Arranging || supported ;; Supporting || met ;; Come_together
commenced ;; Process_start || deployed ;; Arranging || evacuated ;; Removing || established ;; Creating
reconnaissance ;; Scrutiny || criticised ;; Judgment_communication || training ;; Education_teaching || control ;; Research || command ;; Control
investigated ;; Research || found ;; Know
led ;; Causation
investigation ;; Research || charge ;; Judgment_communication
carrying ;; Carry_goods || derailed ;; Self_motion
caused ;; Causation || damage ;; Damaging || injured ;; Bodily_harm || took ;; Creating || clean ;; Emptying
restore ;; Recovering
investigating ;; Research || found ;; Know || caused ;; Causation || stretching ;; Expansion
surrounding ;; Surrounding || went out ;; Self_motion || criticizing ;; Judgment_communication
derailment ;; Catastrophe || cited ;; Adducing
research ;; Research || held ;; Defending
organized ;; Arranging
held ;; Defending
renamed ;; Name_conferral || held ;; Defending
event ;; Social_event
became ;; Becoming || started ;; Process_start || export ;; Sending || make ;; Manufacturing
occurred ;; Presence || died ;; Death || state ;; Statement || incident ;; Catastrophe || fire ;; Catastrophe || Burning ;; Bodily_harm
disputed ;; Quarreling || returning ;; Self_motion
spent ;; Expend_resource || going ;; Motion || fire ;; Catastrophe || case ;; Criminal_investigation || mob ;; Violence || committed ;; Commitment || concluded ;; Coming_to_believe || burning ;; Bodily_harm || investigate ;; Research || set up ;; Reforming_a_system
stated ;; Statement || held ;; Defending || fire ;; Catastrophe || accident ;; Catastrophe || appointed ;; Choosing
incident ;; Catastrophe || conspiracy ;; Committing_crime || crime ;; Committing_crime || convicted ;; Legal_rulings
conviction ;; Legal_rulings
destruction ;; Destroying || loss ;; Earnings_and_losses || perceived ;; Perception_active || trigger ;; Causation || riots ;; Violence || resulted in ;; Causation
casualties ;; Death || casualties ;; Death
gone ;; Self_motion || ceremony ;; Social_event || called ;; Name_conferral
mix ;; Cause_to_amalgamate || boarded ;; Self_motion
stop ;; Preventing_or_letting
stopped ;; Process_end || started ;; Process_start || leaving ;; Departing || brake ;; Motion || pulled ;; Motion
stated ;; Statement || pulled ;; Motion
attacked ;; Attack || mob ;; Violence
set alight ;; Damaging
death ;; Death || burnt ;; Bodily_harm || injured ;; Bodily_harm
ready ;; GetReady || arrived at ;; Arriving || kept ;; Temporary_stay || use ;; Using
inquiries ;; Criminal_investigation || found ;; Know || stating ;; Statement || conflagration ;; Catastrophe || accident ;; Catastrophe || conspiracy ;; Committing_crime
blamed ;; Judgment_communication || known ;; Know || accepted ;; Agree_or_refuse_to_act
airstrike ;; Attack || strike ;; Attack
used ;; Using || attack ;; Attack || defended ;; Defending
target ;; Aiming || hit ;; Attack || consisted ;; Cause_to_be_included
attacked ;; Attack || research ;; Research || strike ;; Attack || target ;; Aiming
shot down ;; Use_firearm || becoming ;; Becoming
damaged ;; Damaging || hit ;; Attack
causing ;; Causation || damage ;; Damaging
strike ;; Attack || attack ;; Attack || defenses ;; Defending
contribute ;; Giving || convinced ;; Convincing || call ;; Name_conferral || illustrated ;; Statement
earthquake ;; Catastrophe || occurred ;; Coming_to_be
affected ;; Influence
caused ;; Causation || earthquake ;; Catastrophe || damage ;; Damaging
deaths ;; Death || affected ;; Influence
earthquake ;; Catastrophe || earthquake ;; Catastrophe
earthquake ;; Catastrophe || perceived ;; Know
damage ;; Damaging || judged ;; Deciding
responded ;; Response || studies ;; Research || earthquake ;; Catastrophe || conducted ;; Action
damaged ;; Damaging
surrounded ;; Surrounding || earthquakes ;; Catastrophe || earthquake ;; Catastrophe || swarms ;; Motion || interconnected ;; Forming_relationships
equipped ;; Supply || earthquakes ;; Catastrophe
earthquake ;; Catastrophe || studies ;; Research
broke ;; Destroying || earthquake ;; Catastrophe || known ;; Know || discovered ;; Know
capture ;; Conquering || scout ;; Scrutiny
transformed ;; Change
threat ;; Warning
planned ;; Arranging || called ;; Name_conferral
sent ;; Sending || escorted ;; Patrolling || ensure ;; Preventing_or_letting || gathering ;; Come_together || commanded ;; Control
decided ;; Deciding || investigate ;; Research || completing ;; Process_end
carrying ;; Carry_goods || due ;; Causation || make ;; Creating
reached ;; Arriving || Sailing ;; Self_motion
taking ;; Conquering || Anchored ;; Connect || lured ;; Convincing
sent ;; Sending || determined ;; Deciding || undergoing ;; Process_start
released ;; Releasing || Capturing ;; Conquering || assaulting ;; Attack || sailed ;; Self_motion || returning ;; Arriving
conference ;; Social_event || organised ;; Arranging
focus ;; Aiming || advanced ;; Cause_to_make_progress || development ;; Coming_to_be || application ;; Using
conference ;; Social_event || grown ;; Expansion || become ;; Becoming || hosting ;; Come_together || meetings ;; Social_event
meetings ;; Social_event || meeting ;; Social_event
meetings ;; Social_event || conference ;; Social_event
conference ;; Social_event || consists ;; Cause_to_be_included
given in ;; Giving || presented ;; Presence || chosen ;; Choosing || research ;; Research || presentations ;; Communication || talks ;; Communication
included ;; Cause_to_be_included
give ;; Giving || recipients ;; Rewards_and_punishments || invited ;; Request
published ;; Publishing || conference ;; Social_event
lost ;; Earnings_and_losses || crashed ;; Catastrophe || control ;; Control || maneuvered ;; Hold
killing ;; Killing || fell ;; Motion_directional || stalled ;; Hindering
suffered ;; Bodily_harm
captured ;; Conquering
delayed ;; Change_event_time || flight ;; Self_motion || concluded ;; Coming_to_believe || involving ;; Cause_to_be_included
teaching ;; Education_teaching || study ;; Research
used ;; Using || training ;; Education_teaching || complying ;; Action || correcting ;; Resolve_problem || violates ;; Causation
competition ;; Competition
held ;; Defending || held ;; Defending
created ;; Creating
added ;; Cause_to_be_included || education ;; Education_teaching || rebranded ;; Name_conferral
added ;; Cause_to_be_included || science ;; Research
supported ;; Supporting || sponsored ;; Supply
changed ;; Change || held ;; Defending
Festival ;; Social_event || festival ;; Social_event || created ;; Creating
Festival ;; Social_event || nominated ;; Choosing
conducted ;; Action || came ;; Self_motion || survey ;; Research || feeling ;; Change_sentiment || felt ;; Change_sentiment || revealed ;; Communication
survey ;; Research || aim ;; Aiming || resulted in ;; Causation || Festival ;; Social_event || festival ;; Social_event
festival ;; Social_event
announced ;; Expressing_publicly || cancelled ;; Change_event_time || festival ;; Social_event
hosted ;; Come_together || festival ;; Social_event
Festival ;; Social_event || run ;; Self_motion
build ;; Building || festival ;; Social_event
Typhoon ;; Catastrophe || Hurricane ;; Catastrophe || cyclone ;; Catastrophe || effects ;; Influence
identified ;; Check || Originating ;; Coming_to_be
becoming ;; Becoming || hurricane ;; Catastrophe || storm ;; Catastrophe || ensued ;; Coming_to_be || moved ;; Self_motion
hurricane ;; Catastrophe || storm ;; Catastrophe || took place ;; Process_start || intensification ;; Cause_change_of_position_on_a_scale || reached ;; Arriving
found ;; Know || cyclone ;; Catastrophe || investigating ;; Research
stalled ;; Temporary_stay || resulting in ;; Causation
killed ;; Killing
caused ;; Causation || hurricane ;; Catastrophe || weakening ;; Cause_change_of_strength
became ;; Becoming || acquired ;; Getting
typhoon ;; Catastrophe || hurricane ;; Catastrophe || crossed ;; Motion_directional
storm ;; Catastrophe || Remaining ;; Presence || cyclone ;; Catastrophe || weakened ;; Cause_change_of_strength
cyclone ;; Catastrophe || cyclone ;; Catastrophe || turning ;; Change || degrading ;; Cause_change_of_strength || transitioned ;; Change || dissipated ;; Dispersal
conflict ;; Hostile_encounter || supported ;; Supporting
began ;; Process_start || conflict ;; Hostile_encounter || war ;; Hostile_encounter
broke down ;; Destroying || accused ;; Judgment_communication || demands ;; Request || release ;; Releasing || talks ;; Communication || addressing ;; Resolve_problem
conflict ;; Hostile_encounter || killed ;; Killing || taken place ;; Process_start
confined ;; Hindering
called ;; Name_conferral || armed ;; Bearing_arms
surveyed ;; Scrutiny || study ;; Research || perception ;; Change_sentiment
targeted ;; Aiming || improved ;; Cause_to_make_progress || say ;; Statement
war ;; Hostile_encounter || claim ;; Statement || rebellion ;; Hostile_encounter
announced ;; Expressing_publicly || combatting ;; Hostile_encounter || called ;; Name_conferral || aimed ;; Aiming || support ;; Supporting
included ;; Cause_to_be_included || increased ;; Cause_change_of_position_on_a_scale || reduction ;; Cause_change_of_position_on_a_scale
removed ;; Removing
reduced ;; Cause_change_of_position_on_a_scale
injuries ;; Bodily_harm || deaths ;; Death || reported ;; Reporting
gained ;; Getting || attack ;; Attack || deaths ;; Death || resulted in ;; Causation
toured ;; Traveling
played ;; Competition
sending ;; Sending || developed ;; Coming_to_be || tours ;; Traveling
sailed ;; Self_motion
arranged ;; Arranging || known ;; Know || touring ;; Traveling
helped ;; Assistance || losing ;; Earnings_and_losses || suffered ;; Bodily_harm
became ;; Becoming || improving ;; Cause_to_make_progress || end ;; Process_end || combination ;; Cause_to_amalgamate
experiment ;; Research
limited ;; Limiting || tour ;; Traveling
losing ;; Earnings_and_losses || played ;; Competition
commented ;; Statement || running ;; Self_motion
conflicts ;; Violence
began ;; Process_start || political ;; Social_event || supported ;; Supporting || confrontations ;; Hostile_encounter
served ;; Supply
established ;; Creating
emerged ;; Coming_to_be || advocated ;; Convincing || reforms ;; Reforming_a_system
demanded ;; Request || command ;; Control
refused ;; Agree_or_refuse_to_act || permitted ;; Preventing_or_letting || marched ;; Self_motion
fled ;; Escaping || organized ;; Arranging || meet ;; Motion
struggle ;; Hostile_encounter || fought ;; Hostile_encounter
Battle ;; Hostile_encounter || defeated ;; Conquering || defeated ;; Conquering || Battle ;; Hostile_encounter
surrendered ;; Surrendering || battle ;; Hostile_encounter || fought ;; Hostile_encounter
fled ;; Escaping || killed ;; Killing || arrival ;; Arriving
Battle ;; Hostile_encounter || defeated ;; Conquering
committed ;; Killing || battle ;; Hostile_encounter
became ;; Becoming || Battle ;; Hostile_encounter || defeated ;; Conquering
changes ;; Change || political ;; Social_event || eliminated ;; Removing || led to ;; Causation
unrest ;; Hostile_encounter || become ;; Becoming || repressed ;; Hindering || strikes ;; Attack
wars ;; Hostile_encounter || ended ;; Process_end || downturn ;; Cause_change_of_position_on_a_scale
reform ;; Reforming_a_system
deployed ;; Arranging || recruited ;; Employment
put ;; Placing || calling ;; Request
stopped ;; Process_end || marched ;; Self_motion || stopped ;; Process_end
dispersed ;; Dispersal || marched ;; Self_motion || warned ;; Warning
released ;; Releasing || attacked ;; Attack
decapitated ;; Killing
seized ;; Hold
sentenced ;; Legal_rulings
became ;; Becoming || bring ;; Bringing || fomented ;; Convincing
forgotten ;; GiveUp || focussed ;; Aiming || insurrection ;; Hostile_encounter
boosted ;; Cause_change_of_strength
Games ;; Competition || ceremony ;; Social_event || took place ;; Process_start
combined ;; Cause_to_amalgamate || opening ;; Social_event || event ;; Social_event || parade ;; Motion
Games ;; Competition || opened ;; Process_start
entitled ;; Name_conferral
ceremony ;; Social_event || Games ;; Competition || reach ;; Achieve || set ;; Placing
noted ;; Know || cost ;; Cost || ceremony ;; Social_event || hailed ;; Judgment_communication
spent ;; Expend_resource || ceremonies ;; Social_event || budget ;; Cost || budgeted ;; Cost
ceremony ;; Social_event || success ;; Achieve || seen ;; Perception_active || praised ;; Judgment_communication
began ;; Process_start || ceremony ;; Social_event
ceremony ;; Social_event || watched ;; Perception_active || becoming ;; Becoming
content ;; Containing || kept ;; Preventing_or_letting || involving ;; Cause_to_be_included
noted ;; Know || industrial ;; Manufacturing || represented ;; Statement || service ;; Supply || heritage ;; Preserving || music ;; Expressing_publicly || culture ;; Influence || storytelling ;; Communication || use ;; Using || music ;; Communication || revolution ;; Reforming_a_system
appearance ;; Presence || comment ;; Response || involving ;; Cause_to_be_included || joined ;; Participation
ascribed ;; Coming_to_believe || sense ;; Perception_active
ceremony ;; Social_event || Games ;; Competition || reflecting ;; Influence
complete ;; Process_end || ceremony ;; Social_event || ceremony ;; Social_event || edited ;; Writing || released ;; Publishing
revolution ;; Social_event || events ;; Social_event || violent ;; Violence || culminated ;; Come_together || took place ;; Process_start || elected ;; Choosing
relationship ;; Forming_relationships || sought ;; Getting
achieve ;; Achieve || provided ;; Supply || reforms ;; Reforming_a_system || agreement ;; Agree_or_refuse_to_act
complicated ;; Cause_change_of_position_on_a_scale
postpone ;; Change_event_time || enter ;; Becoming_a_member
signing ;; Sign_agreement || led to ;; Causation || interpreted ;; Statement
became ;; Becoming || resulted in ;; Causation || Clashes ;; Violence || deaths ;; Death
became ;; Becoming || fled ;; Escaping || rose ;; Cause_change_of_position_on_a_scale
control ;; Control || proceeded ;; Motion
declared ;; Expressing_publicly || relieved ;; Process_end
asked ;; Request || illegal ;; Legality || assistance ;; Assistance || said ;; Statement
viewed ;; Perception_active || recognize ;; Know || illegal ;; Legality || replaced ;; Change_of_leadership
received ;; Receiving || occurred ;; Presence || election ;; Change_of_leadership || support ;; Supporting
escalated ;; Cause_change_of_position_on_a_scale
proceeded ;; Motion || agreement ;; Sign_agreement || sign ;; Sign_agreement
became ;; Becoming || elections ;; Choosing
restored ;; Recovering || initiated ;; Process_start
found ;; Know || change ;; Change || illegal ;; Legality || regarded ;; Statement
produced ;; Creating
reformed ;; Reforming_a_system || crossed ;; Motion_directional
intensified ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength
produced ;; Creating
caused ;; Causation || flooded ;; Motion
spawned ;; Causation || led to ;; Causation
extended ;; Change_event_time
emerged ;; Coming_to_be || re-intensified ;; Cause_change_of_strength
sent ;; Sending || invasion ;; Attack
reformed ;; Reforming_a_system || state ;; Statement
ordered ;; Arranging
attacked ;; Attack || focused ;; Aiming || trading ;; Commerce_sell
achieve ;; Achieve || use ;; Using || invade ;; Attack || capture ;; Conquering || instructed ;; Causation || held ;; Defending
given ;; Giving || campaign ;; Military_operation || supported ;; Supporting
taken ;; Conquering || surrender ;; Surrendering || trading ;; Commerce_sell || called ;; Name_conferral || opposed ;; Agree_or_refuse_to_act
sieges ;; Besieging || control ;; Control || remain ;; Presence
began ;; Process_start
forces ;; Hostile_encounter || Fighting ;; Violence
supported ;; Supporting
took place ;; Process_start || extended ;; Expansion
decided ;; Deciding || included ;; Cause_to_be_included || combined ;; Cause_to_amalgamate || command ;; Control
surrendered ;; Surrendering || surrendered ;; Surrendering
entered ;; Arriving || landed ;; Self_motion
accepted ;; Receiving
refused ;; Agree_or_refuse_to_act || deferred ;; Change_event_time || support ;; Supporting
proclaimed ;; Statement
ruled ;; Control || governed ;; Control || abdication ;; Change_of_leadership
overthrew ;; Change_of_leadership || declared ;; Expressing_publicly || restored ;; Recovering
began ;; Process_start || control ;; Control
agreed ;; Agree_or_refuse_to_act || talks ;; Telling || held ;; Defending
broke out ;; Process_start || went ;; Self_motion
reorganizing ;; Reforming_a_system
established ;; Building || put ;; Placing
involved ;; Cause_to_be_included
supplied ;; Supply || equipped ;; Supply || turned ;; Change
included ;; Cause_to_be_included
use ;; Using || becoming ;; Becoming || forbidden ;; Preventing_or_letting || prevent ;; Preventing_or_letting
called ;; Name_conferral || sale ;; Commerce_sell
attacking ;; Attack || validated ;; Check || pushing ;; Self_motion
lack ;; Confronting_problem || weak ;; Cause_change_of_strength
limited ;; Limiting || use ;; Using || made ;; Manufacturing || bombing ;; Attack
provided ;; Supply || used ;; Using || use ;; Using || developed ;; Coming_to_be || convoy ;; Patrolling || supply ;; Supply || facilitated ;; Cause_to_make_progress || support ;; Supporting
combination ;; Cause_to_amalgamate || defeat ;; Conquering || culminating ;; Self_motion
gave ;; Giving || control ;; Control || conference ;; Social_event
denounced ;; Judgment_communication
deposed ;; Change_of_leadership || creating ;; Creating
developed ;; Coming_to_be
escalated ;; Cause_change_of_strength
fought ;; Hostile_encounter || called ;; Name_conferral
fought ;; Hostile_encounter || included ;; Cause_to_be_included
progress ;; Cause_change_of_position_on_a_scale || invasion ;; Attack
began ;; Process_start || invaded ;; Attack || attacked ;; Attack || accepted ;; Receiving
aided ;; Assistance || saw ;; Self_motion || pitted ;; Hostile_encounter
announcement ;; Expressing_publicly || surrender ;; Surrendering || attacks ;; Attack || resulting in ;; Causation || invasion ;; Attack || declaration ;; Expressing_publicly || culminated ;; Process_end || accompanied ;; GetReady
surrender ;; Surrendering || took place ;; Process_start
determined ;; Deciding || lost ;; Earnings_and_losses || limited ;; Limiting
reforms ;; Reforming_a_system || relinquished ;; GiveUp || pave ;; Filling
shot down ;; Use_firearm
flew ;; Motion || deviated ;; Change
proceeded ;; Self_motion
crashed ;; Catastrophe
killed ;; Killing
found ;; Know || found ;; Know
denied ;; Agree_or_refuse_to_act
said ;; Telling
accused ;; Judgment_communication
suppressed ;; Hindering
resulted in ;; Causation
altered ;; Change
redesigned ;; Reforming_a_system
took place ;; Process_start || Battle ;; Hostile_encounter
arrived ;; Arriving || assigned ;; Choosing
given ;; Giving || devastate ;; Destroying || surrender ;; Surrendering || led ;; Causation || began ;; Process_start || battle ;; Hostile_encounter || reaching ;; Arriving
march ;; Self_motion || collecting ;; Come_together || intimidated ;; Influence || irritated ;; Influence
instructed ;; Request || inform ;; Telling || bring ;; Bringing || disband ;; Dispersal || reinforcements ;; Cause_change_of_strength || reorganise ;; Reforming_a_system || incorporate ;; Cause_to_be_included
informed ;; Telling || arrival ;; Arriving
withdraw ;; Escaping || abandon ;; GiveUp || operations ;; Military_operation
gathering ;; Come_together || invested ;; Expend_resource
entered ;; Arriving || rushed ;; Self_motion
stand ;; Placing || tumult ;; Hostile_encounter || get ;; Getting || allayed ;; Cause_change_of_position_on_a_scale || order ;; Request
piled ;; Placing || got ;; Getting || fighting ;; Hostile_encounter
reminded ;; Telling || made ;; Bringing || deal ;; Resolve_problem || submission ;; Surrendering
told ;; Telling || returned ;; Arriving || given ;; Giving || homecoming ;; Self_motion
carry ;; Carry_goods || slaughter ;; Killing
begun ;; Process_start || allow ;; Preventing_or_letting || fighting ;; Hostile_encounter || say ;; Telling
closed ;; Process_end
War ;; Military_operation || war ;; Military_operation || fought ;; Hostile_encounter
began ;; Process_start || result ;; Causation
attacked ;; Attack || forces ;; Causation || War ;; Military_operation || broke out ;; Process_start || inaugurated ;; Change_of_leadership
proclaimed ;; Statement || support ;; Supporting || included ;; Cause_to_be_included
faced ;; Confronting_problem || uphold ;; Defending || advocated ;; Convincing
declared ;; Expressing_publicly || organized ;; Arranging || rebellion ;; Change_of_leadership || seceded ;; Escaping
fleeing ;; Escaping || given ;; Giving || War ;; Military_operation || grew ;; Cause_to_make_progress || control ;; Control || claimed ;; Statement
join ;; Becoming_a_member || developed ;; Cause_to_make_progress || due ;; Causation || intervention ;; Preventing_or_letting || invited ;; Request
recognized ;; Coming_to_believe
remained ;; Presence
raised ;; Cause_change_of_position_on_a_scale || fought ;; Hostile_encounter
dead ;; Death || combined ;; Cause_to_amalgamate || combat ;; Hostile_encounter || left ;; Causation || wars ;; Military_operation
ended ;; Process_end || surrendered ;; Surrendering || war ;; Military_operation || battle ;; Military_operation
occurring ;; Coming_to_be || surrender ;; Surrendering
destroyed ;; Destroying
freed ;; Releasing || collapsed ;; Destroying || abolished ;; Process_end
restored ;; Recovering || expanded ;; Cause_change_of_strength || granted ;; Giving || war ;; Military_operation || Reconstruction ;; Cause_to_make_progress || amendments ;; Reforming_a_system
studied ;; Education_teaching || war ;; Military_operation
winds ;; Motion || flooding ;; Motion || produced ;; Creating
storm ;; Catastrophe || intensifying ;; Cause_change_of_strength || striking ;; Attack
moved ;; Motion || storm ;; Catastrophe || reached ;; Arriving
moved ;; Motion || reformed ;; Reforming_a_system || weakened ;; Cause_change_of_strength || dissipated ;; Dispersal || redeveloped ;; Recovering
storm ;; Catastrophe || flooded ;; Attack
struck ;; Attack || producing ;; Creating || resulted in ;; Causation
reported ;; Reporting
cost ;; Cost || lost ;; Earnings_and_losses
storm ;; Catastrophe || destroyed ;; Destroying || surge ;; Motion
winds ;; Motion || moved ;; Motion || storm ;; Catastrophe || damaged ;; Damaging || produced ;; Creating || flooding ;; Catastrophe
record ;; Recording
affected ;; Influence || storm ;; Catastrophe || flooding ;; Catastrophe || flooding ;; Catastrophe
Damage ;; Damaging
winds ;; Motion || injured ;; Bodily_harm || reached ;; Arriving || fallen ;; Motion_directional || driving ;; Self_motion
associated ;; Collaboration || caused ;; Causation || storm ;; Catastrophe || fatality ;; Death
began ;; Process_start || growing ;; Expansion
response ;; Response || called ;; Name_conferral
end ;; Process_end || called ;; Name_conferral
occurred ;; Coming_to_be
marched ;; Self_motion
reform ;; Reforming_a_system || granted ;; Giving
marked ;; Recording
took place ;; Process_start
took place ;; Process_start
stressed ;; Statement || used ;; Using
held ;; Hold
administered ;; Control
proclaimed ;; Statement
reform ;; Reforming_a_system || called ;; Request || control ;; Control
stated ;; Statement
vowed ;; Commitment || promote ;; Cause_change_of_position_on_a_scale || stated ;; Statement
vowed ;; Commitment || stated ;; Statement || wars ;; Hostile_encounter
linked ;; Connect
attended ;; Participation || watched ;; Perception_active
joint ;; Collaboration
attacked ;; Attack || refusing ;; Agree_or_refuse_to_act
participate ;; Participation || cross ;; Motion_directional || invasion ;; Attack || ordered ;; Arranging
killed ;; Killing || wounded ;; Bodily_harm
stopped ;; Process_end || strengthened ;; Cause_change_of_strength || reforms ;; Reforming_a_system || party ;; Social_event || invasion ;; Attack
ended ;; Process_end || fought ;; Hostile_encounter || came ;; Self_motion
broke ;; Destroying || defended ;; Defending || occurred ;; Coming_to_be || attacks ;; Attack
held ;; Defending || evacuate ;; Escaping
held ;; Defending || captured ;; Conquering
developed ;; Coming_to_be || commanding ;; Control || adopted ;; Choosing
held ;; Defending || strengthened ;; Cause_change_of_strength || undertook ;; Action
replaced ;; Change_of_leadership
restructured ;; Reforming_a_system
began ;; Process_start || replace ;; Change_of_leadership || suffered ;; Bodily_harm || arrived ;; Arriving || arrive ;; Arriving
reinforced ;; Cause_change_of_strength || carried out ;; Action
arrived ;; Arriving || commence ;; Process_start
developed ;; Coming_to_be
extended ;; Expansion || supported ;; Supporting
began ;; Process_start || Beginning ;; Process_start
determined ;; Deciding || holding ;; Defending
due ;; Causation || strength ;; Cause_change_of_strength
intensified ;; Cause_change_of_strength || attacks ;; Attack
found ;; Know || evacuated ;; Escaping
retreat ;; Escaping || forced ;; Causation || collapsed ;; Destroying
captured ;; Conquering
opposed ;; Agree_or_refuse_to_act
Fighting ;; Hostile_encounter
organized ;; Arranging
controlled ;; Control
equipped ;; Supply || reorganized ;; Reforming_a_system || Select ;; Choosing
fought ;; Hostile_encounter || granted ;; Giving
excluded ;; Preventing_or_letting || made ;; Causation
fought ;; Hostile_encounter || recognized ;; Know
Counter-offensive ;; Defending || conflict ;; Hostile_encounter || known ;; Know
began ;; Process_start || clashes ;; Hostile_encounter || involving ;; Cause_to_be_included
removed ;; Removing || invasion ;; Military_operation || occupied ;; Control
formed ;; Coming_to_be || fight ;; Hostile_encounter
cooperation ;; Collaboration || form ;; Coming_to_be || scheming ;; Arranging
began ;; Process_start || purging ;; Removing || capitulated ;; Surrendering
began ;; Process_start || dominated ;; Control || attacking ;; Attack || beginning ;; Process_start || attack ;; Attack || formed ;; Coming_to_be
exchanges ;; Communication || fighting ;; Hostile_encounter || reunified ;; Recovering
perceived ;; Coming_to_believe
attack ;; Attack || launched ;; Process_start
negotiate ;; Communication || assault ;; Attack || strike ;; Attack || launched ;; Process_start
achieved ;; Achieve || negotiate ;; Communication || remained ;; Temporary_stay || withdrew ;; Escaping
fighting ;; Hostile_encounter || mediate ;; Resolve_problem
agree ;; Agree_or_refuse_to_act
decided ;; Deciding || remove ;; Removing || perceiving ;; Coming_to_believe
invaded ;; Attack || overran ;; Destroying || deaths ;; Death || ending ;; Process_end
ending ;; Process_end || intervention ;; Participation || aid ;; Assistance || allowing ;; Preventing_or_letting || mitigate ;; Cause_change_of_position_on_a_scale || viewed ;; Perception_active
beginning ;; Process_start || occupation ;; Control || marking ;; Recording || established ;; Creating
formed ;; Coming_to_be || fight ;; Hostile_encounter || occupation ;; Control || legitimate ;; Legality || recognised ;; Agree_or_refuse_to_act
begin ;; Process_start || approached ;; Arriving
implemented ;; Action || reforms ;; Reforming_a_system || withdrawal ;; Escaping
agreed ;; Agree_or_refuse_to_act || meeting ;; Communication || arrangement ;; Arranging || forming ;; Cause_to_amalgamate
elected ;; Change_of_leadership || supervising ;; Scrutiny || tasked ;; Request
decided ;; Deciding || chose ;; Choosing || participate ;; Participation || attacks ;; Attack || killing ;; Killing || elections ;; Change_of_leadership || disrupt ;; Hindering || launching ;; Motion
elections ;; Change_of_leadership || party ;; Social_event || party ;; Social_event || defeated ;; Conquering || win ;; Conquering
refused ;; Agree_or_refuse_to_act || announced ;; Expressing_publicly || secede ;; Removing || accept ;; Receiving || defeat ;; Conquering || drawn ;; Motion
agreed ;; Agree_or_refuse_to_act || avoid ;; Preventing_or_letting || form ;; Coming_to_be
restored ;; Recovering || formed ;; Coming_to_be
accident ;; Catastrophe
caused ;; Causation || injuring ;; Bodily_harm || killing ;; Killing
exposed ;; Reveal_secret || deaths ;; Death || result in ;; Causation
found ;; Know || Reports ;; Reporting
went ;; Self_motion || administration ;; Reforming_a_system
limit ;; Limiting || tightening ;; Cause_change_of_strength
found ;; Coming_to_believe
demonstration ;; Social_event || rally ;; Social_event || rally ;; Social_event || called ;; Name_conferral || held ;; Hold
deemed ;; Legal_rulings || organised ;; Arranging || supported ;; Supporting || rally ;; Social_event || opposition ;; Judgment_communication || illegal ;; Statement || Elections ;; Choosing
ensure ;; Preventing_or_letting || chaired ;; Hold || pushing ;; Body_movement || elections ;; Choosing || election ;; Choosing
demanded ;; Request || reform ;; Reforming_a_system || allow ;; Preventing_or_letting || clean ;; Removing || use ;; Using || introduce ;; Choosing || put an end to ;; Process_end
stop ;; Preventing_or_letting || rallies ;; Social_event || gatherings ;; Come_together || illegal ;; Legality || planned ;; Arranging || taking place ;; Process_start
decided ;; Deciding || consultations ;; Communication || planned ;; Arranging || march ;; Self_motion || hold ;; Hold
claim ;; Statement || demands ;; Request || fell ;; Motion_directional || reform ;; Reforming_a_system || made ;; Action
planned ;; Arranging || dismissing ;; Agree_or_refuse_to_act || secure ;; Getting || permit ;; Preventing_or_letting || demands ;; Request || reform ;; Reforming_a_system || due ;; Causation || called ;; Request
deployed ;; Arranging || congregate ;; Come_together || forced ;; Causation || disperse ;; Dispersal
arrested ;; Arrest
raid ;; Attack || took place ;; Process_start
employed ;; Employment || raise ;; Cause_change_of_position_on_a_scale || took over ;; Conquering
due ;; Causation || state ;; Statement || massacres ;; Killing
saw ;; Self_motion || bring ;; Bringing
served ;; Assistance
lasted ;; Process_end || bombs ;; Attack || deaths ;; Death || resulting in ;; Causation
saw ;; Self_motion || building ;; Building || massacres ;; Killing
managed ;; Control || give way ;; Giving || persuade ;; Convincing || assigning ;; Choosing
wrought ;; Causation || reported ;; Reporting || violence ;; Violence || praising ;; Judgment_communication
enforce ;; Action || reforms ;; Reforming_a_system || issuing ;; Creating || condemning ;; Judgment_communication || massacres ;; Killing || take place ;; Process_start
took place ;; Hold || contested ;; Competition
held ;; Hold || held ;; Hold
conference ;; Communication || remain ;; Presence || promoted ;; Cause_to_make_progress
attended ;; Participation || match ;; Competition
match ;; Competition || televised ;; Communication
final ;; Process_end || exist ;; Coming_to_be || contested ;; Competition
reformed ;; Reforming_a_system || folded ;; Cause_to_amalgamate
reformed ;; Reforming_a_system || folded ;; Cause_to_amalgamate
conflict ;; Violence
support ;; Supporting
conflict ;; Violence || insurgents ;; Terrorism || identifying with ;; Check || supported ;; Supporting
conflict ;; Violence || tied ;; Connect || dispute ;; Quarreling
reforms ;; Reforming_a_system || provided ;; Supply || expressing ;; Expressing_publicly || limited ;; Limiting || reversed ;; Change || limited ;; Limiting || caused ;; Causation || increase ;; Cause_change_of_position_on_a_scale || support ;; Supporting
armed ;; Bearing_arms || disputed ;; Quarreling || created ;; Creating || resulted in ;; Causation || election ;; Choosing
began ;; Process_start || attacks ;; Attack || escalated ;; Cause_change_of_strength
armed ;; Bearing_arms || died ;; Death || fighting ;; Hostile_encounter || died ;; Death || targeted ;; Aiming
accused ;; Judgment_communication || supporting ;; Supporting || training ;; Education_teaching
fight ;; Hostile_encounter
supported ;; Supporting || trained ;; Education_teaching
released ;; Releasing || conflict ;; Violence || dead ;; Death
deaths ;; Death || fallen ;; Motion_directional || start ;; Process_start
deaths ;; Death || claim ;; Statement
involved ;; Cause_to_be_included
became ;; Becoming
nominate ;; Choosing || sparked ;; Causation
killed ;; Killing || overthrow ;; Change_of_leadership || invited ;; Request
assaulted ;; Attack || besieged ;; Besieging || established ;; Creating
abandoned ;; GiveUp || collapsed ;; Destroying || died ;; Death
emerged ;; Coming_to_be
taken over ;; Conquering
slain ;; Killing || routed ;; Attack
reasserted ;; Recovering
reforms ;; Reforming_a_system || restructuring ;; Reforming_a_system
intensified ;; Cause_change_of_strength || developed ;; Coming_to_be
involved ;; Participation || surveillance ;; Scrutiny || surveillance ;; Scrutiny || protest ;; Protest
participating ;; Participation || form ;; Coming_to_be || complaining ;; Judgment_communication || took place ;; Process_start || read ;; Perception_active
surveillance ;; Scrutiny
fight ;; Quarreling
reforms ;; Reforming_a_system || going to ;; Self_motion || made ;; Causation || instituted ;; Creating
announced ;; Expressing_publicly
included ;; Cause_to_be_included || social ;; Social_event || fight ;; Hostile_encounter || press ;; Self_motion || collaborative ;; Collaboration
asked ;; Request || oppose ;; Agree_or_refuse_to_act || support ;; Supporting || freedom ;; Change_of_leadership || enact ;; Action
said ;; Communication || sent ;; Sending || placed ;; Placing
participated ;; Participation || uprising ;; Change_of_leadership
filled ;; Filling || Named ;; Name_conferral || killed ;; Killing || victims ;; Catastrophe || forces ;; Hostile_encounter || protests ;; Agree_or_refuse_to_act
demanded ;; Request || reforms ;; Reforming_a_system || fall ;; Motion_directional || demanding ;; Request || end ;; Process_end || implementation ;; Action || carried ;; Bringing
named ;; Name_conferral || participated ;; Participation || gave ;; Giving
joined ;; Becoming_a_member
present ;; Presence || ended ;; Process_end || forces ;; Hostile_encounter
occurrence ;; Presence || Hurricane ;; Catastrophe || cyclones ;; Catastrophe || Hurricane ;; Catastrophe || strike ;; Attack
named ;; Name_conferral || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
formed ;; Coming_to_be || spawned ;; Causation
turned ;; Becoming || moving ;; Motion
allowed ;; Preventing_or_letting || hurricane ;; Catastrophe || attain ;; Getting || attained ;; Getting
weakened ;; Cause_change_of_strength || increased ;; Cause_change_of_position_on_a_scale || storm ;; Catastrophe || turned ;; Becoming
storm ;; Catastrophe || dissipated ;; Dispersal
hurricane ;; Catastrophe || Hurricane ;; Catastrophe || form ;; Coming_to_be
combined ;; Cause_to_amalgamate || killing ;; Killing || causing ;; Causation || damage ;; Damaging || hurricanes ;; Catastrophe || impacts ;; Influence || affected ;; Influence
effects ;; Influence || deaths ;; Death || damage ;; Damaging || due ;; Causation
storms ;; Catastrophe || precipitated ;; Change || filling ;; Filling
storm ;; Catastrophe || peaked ;; Cause_change_of_position_on_a_scale
caused ;; Causation || damaging ;; Damaging || flooding ;; Catastrophe
storm ;; Catastrophe || damaged ;; Damaging || made ;; Manufacturing || landfall ;; Arriving || flooded ;; Destroying
storm ;; Catastrophe || effects ;; Influence || spread ;; Dispersal || causing ;; Causation || flooding ;; Catastrophe
declared ;; Expressing_publicly || storm ;; Catastrophe || states ;; Statement
aid ;; Assistance || hit ;; Attack || assistance ;; Assistance || distributed ;; Supply
due ;; Causation || names ;; Name_conferral || retired ;; GiveUp || impacts ;; Influence
assisted ;; Assistance || attacked ;; Attack || guarded ;; Patrolling || Battle ;; Hostile_encounter || Confederate ;; Collaboration || engagement ;; Hostile_encounter || War ;; Hostile_encounter || fleet ;; Military_operation || fleet ;; Military_operation || led ;; Causation || commanded ;; Control
order ;; Request
became ;; Becoming
order ;; Request
go ;; Motion
monitors ;; Scrutiny || enabling ;; Preventing_or_letting || battle ;; Hostile_encounter || marked ;; Recording || run through ;; Motion || claimed ;; Statement || fleet ;; Military_operation || get ;; Getting
retire ;; Departing || engaged ;; Participation
enabled ;; Preventing_or_letting || received ;; Receiving || overcome ;; Conquering || inflict ;; Causation || imbalance ;; Expansion || injury ;; Bodily_harm
surrendered ;; Surrendering || battle ;; Hostile_encounter || reduced ;; Cause_change_of_position_on_a_scale || ending ;; Process_end
surrendered ;; Surrendering || support ;; Supporting
control ;; Control || passed ;; Motion_directional
completing ;; Process_end || closure ;; Change
battle ;; Hostile_encounter || capture ;; Conquering || victory ;; Getting
fought ;; Hostile_encounter || present ;; Presence || war ;; Military_operation
overthrew ;; Change_of_leadership || establishing ;; Creating
fighting ;; Hostile_encounter || resistance ;; Defending || supported ;; Supporting
used ;; Using || repress ;; Hindering
increasing ;; Cause_change_of_position_on_a_scale || support ;; Supporting || ravaged ;; Destroying
dissolved ;; Change || establishing ;; Creating
ended ;; Process_end || support ;; Supporting || overwhelmed ;; Destroying
fled ;; Escaping || defeated ;; Conquering
overthrew ;; Change_of_leadership || ended ;; Process_end || entered ;; Arriving
replaced ;; Change_of_leadership || dissolved ;; Change
left ;; Causation
played ;; Participation
match ;; Competition || contested ;; Competition || Cup ;; Competition
match ;; Competition || closure ;; Change || reconstruction ;; Building
competition ;; Competition || met ;; Come_together || appearance ;; Presence
occur ;; Coming_to_be || signing ;; Sign_agreement || meeting ;; Come_together
omission ;; Change || left ;; Departing
making ;; Getting || made ;; Getting || signings ;; Sign_agreement || appearance ;; Presence || appearance ;; Presence || debut ;; Participation
scored ;; Getting || Watched ;; Perception_active
scored ;; Getting || extended ;; Expansion
scored ;; Getting || find ;; Know
match ;; Competition || Shield ;; Competition || win ;; Getting
League ;; Competition || victory ;; Earnings_and_losses
match ;; Competition || appeals ;; Request || turned down ;; Agree_or_refuse_to_act
competition ;; Competition || marked ;; Recording || match ;; Competition || loss ;; Earnings_and_losses
eruption ;; Catastrophe || occurred ;; Presence
eruption ;; Catastrophe || eruption ;; Catastrophe || occur ;; Presence
declared ;; Expressing_publicly
created ;; Creating || eruption ;; Catastrophe || preceded ;; Process_start || earthquakes ;; Catastrophe || caused ;; Causation || injection ;; Motion
caused ;; Causation || creating ;; Creating || earthquake ;; Catastrophe || weakened ;; Cause_change_of_strength || slide ;; Motion
pulverized ;; Destroying || overtaking ;; Motion
deposited ;; Come_together || eruption ;; Catastrophe || rose ;; Motion_directional
melted ;; Change || reached ;; Arriving || forming ;; Creating
killed ;; Killing
killed ;; Killing || causing ;; Causation || reduced ;; Cause_change_of_position_on_a_scale || left ;; GiveUp
preserved ;; Preserving
hurricane ;; Catastrophe || storms ;; Catastrophe
formed ;; Coming_to_be || maintained ;; Preserving
storm ;; Catastrophe || hurricane ;; Catastrophe
hurricane ;; Catastrophe
weakened ;; Cause_change_of_strength
hurricane ;; Catastrophe || killed ;; Killing
Damage ;; Damaging || destroyed ;; Destroying
Damage ;; Damaging
hurricane ;; Catastrophe || entered ;; Arriving || exiting ;; Departing
hurricane ;; Catastrophe || posed ;; Influence || advised ;; Convincing
evacuations ;; Escaping || declared ;; Expressing_publicly
turned ;; Becoming || hurricane ;; Catastrophe || struck ;; Attack
causing ;; Causation || dissipated ;; Change
caused ;; Causation
hurricane ;; Catastrophe || damage ;; Damaging || deaths ;; Death
cyclone ;; Catastrophe || cyclone ;; Catastrophe || storm ;; Catastrophe || affected ;; Influence
developed ;; Coming_to_be || cyclone ;; Catastrophe || storm ;; Catastrophe
storm ;; Catastrophe || tracking ;; Change || strengthened ;; Cause_change_of_strength || exceeded ;; Cause_change_of_position_on_a_scale
intensified ;; Cause_change_of_strength || storm ;; Catastrophe
storm ;; Catastrophe
moving ;; Motion || weakened ;; Cause_change_of_strength || dissipated ;; Change
caused ;; Causation || collide ;; Bodily_harm || resulting in ;; Causation
dropped ;; Motion || storm ;; Catastrophe || uprooted ;; Removing
deaths ;; Death || confirmed ;; Check
rainfall ;; Motion || impacted ;; Influence || evacuate ;; Escaping
reaching ;; Arriving || sank ;; Motion
missing ;; Departing
death ;; Death || cross ;; Motion_directional
fatalities ;; Death
storm ;; Catastrophe || fatalities ;; Death || attributed ;; Causation
killed ;; Killing || injured ;; Bodily_harm || bombing ;; Attack
occurred ;; Presence || election ;; Change_of_leadership || attack ;; Attack || rammed ;; Self_motion || election ;; Choosing
targeted ;; Aiming || attack ;; Attack
struck ;; Attack
died ;; Death || injured ;; Bodily_harm
destroyed ;; Destroying || blast ;; Catastrophe || blast ;; Catastrophe
damaged ;; Damaging || suspended ;; Change
blast ;; Catastrophe || carried out ;; Attack
banned ;; Preventing_or_letting || election ;; Change_of_leadership || attack ;; Attack || came ;; Motion || launched ;; Process_start
bombing ;; Attack || motivated ;; Causation
caused ;; Causation || hurricane ;; Catastrophe
hurricane ;; Catastrophe || hurricane ;; Catastrophe
developed ;; Coming_to_be
strengthened ;; Cause_change_of_strength || became ;; Becoming
intensify ;; Cause_change_of_strength || hurricane ;; Catastrophe || upgraded ;; Cause_to_make_progress || tracked ;; Self_motion || continued ;; Process_start
began ;; Process_start || hurricane ;; Catastrophe || peaked ;; Cause_change_of_position_on_a_scale || deepen ;; Cause_change_of_strength
began ;; Process_start || weakened ;; Cause_change_of_strength
hurricane ;; Catastrophe
weakened ;; Cause_change_of_strength
moving ;; Motion || weakened ;; Cause_change_of_strength || downgraded ;; Cause_change_of_position_on_a_scale || emerging ;; Coming_to_be
began ;; Process_start || hurricane ;; Catastrophe || upgraded ;; Cause_to_make_progress
hurricane ;; Catastrophe
weakened ;; Cause_change_of_strength || dissipated ;; Change
brought ;; Bringing || resulted in ;; Causation
caused ;; Causation || flooded ;; Motion
affected ;; Influence || occurred ;; Coming_to_be
reported ;; Reporting
occurred ;; Coming_to_be || presumed ;; Coming_to_believe || disappeared ;; Change
brought ;; Bringing || reporting ;; Reporting || reported ;; Reporting
reported ;; Reporting || destroyed ;; Destroying || damaged ;; Damaging || suffered ;; Bodily_harm
killed ;; Killing
occurred ;; Coming_to_be
caused ;; Causation || reached ;; Achieve
destroyed ;; Destroying || damaged ;; Damaging || occurred ;; Coming_to_be
drowned ;; Killing
associated ;; Collaboration
hurricane ;; Catastrophe || tied ;; Connect || attaining ;; Achieve
emerged ;; Coming_to_be
took place ;; Process_start
trekked ;; Motion
consolidated ;; Cause_change_of_strength
increased ;; Cause_change_of_position_on_a_scale || acquired ;; Getting
acquired ;; Getting
remained ;; Presence || stalled ;; Temporary_stay
developed ;; Coming_to_be || intensified ;; Cause_change_of_strength
hurricane ;; Catastrophe || spanned ;; Expansion
prompted ;; Causation || weakening ;; Cause_change_of_strength
moved ;; Motion || degraded ;; Cause_change_of_position_on_a_scale || hurricane ;; Catastrophe || strength ;; Cause_change_of_strength
lost ;; Earnings_and_losses || degenerated ;; Change
wound ;; Motion || subsiding ;; Change
lost ;; Earnings_and_losses || dissipated ;; Dispersal
existence ;; Presence || hurricane ;; Catastrophe || remained ;; Presence || brought ;; Bringing
drowned ;; Death || capsized ;; Catastrophe
caused ;; Causation || flooding ;; Catastrophe
effects ;; Influence || felt ;; Perception_active
brought ;; Bringing
damage ;; Damaging || occurring ;; Coming_to_be
damage ;; Damaging
drowned ;; Death
rescues ;; Rescuing || losses ;; Earnings_and_losses || attributed ;; Causation || reached ;; Achieve
hurricane ;; Catastrophe || regarded ;; Perception_active || strike ;; Attack
storm ;; Catastrophe || Developing ;; Coming_to_be || organized ;; Come_together
developed ;; Coming_to_be || attained ;; Getting || hurricane ;; Catastrophe
storm ;; Catastrophe || landfall ;; Arriving
dissipated ;; Change || noted ;; Statement
mudslides ;; Catastrophe || produced ;; Manufacturing || flooding ;; Motion
damage ;; Damaging
killed ;; Killing || injured ;; Bodily_harm || storm ;; Catastrophe
affected ;; Influence
devastating ;; Destroying || deepened ;; Cause_change_of_strength || cyclone ;; Catastrophe
developed ;; Coming_to_be
cyclone ;; Catastrophe || intensify ;; Cause_change_of_strength || moved ;; Self_motion
reached ;; Achieve
began ;; Process_start || weakened ;; Cause_change_of_strength || landfall ;; Arriving
resulted in ;; Causation || dissipated ;; Change
damage ;; Damaging || affected ;; Influence
caused ;; Causation
caused ;; Causation
destroyed ;; Destroying
caused ;; Causation || brought ;; Bringing || flooding ;; Catastrophe || flooded ;; Catastrophe
damage ;; Damaging || deaths ;; Death
retired ;; Process_end
cyclones ;; Catastrophe
brought ;; Bringing || rainfall ;; Catastrophe || formed ;; Coming_to_be
moving ;; Motion || developed ;; Coming_to_be
passing ;; Motion_directional || allowed ;; Preventing_or_letting || intensify ;; Cause_change_of_strength || forming ;; Coming_to_be
attained ;; Getting
due ;; Causation || weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
remain ;; Temporary_stay || made ;; Manufacturing
cyclone ;; Catastrophe || dissipated ;; Dispersal
moving ;; Motion || dissipating ;; Change || redeveloping ;; Recovering || turned ;; Becoming
destroyed ;; Destroying
killed ;; Killing || due ;; Causation || collapsing ;; Destroying
killed ;; Killing || causing ;; Causation || due ;; Causation || passed ;; Motion_directional
forced ;; Motion || destroyed ;; Destroying || damaged ;; Damaging || due ;; Causation || evacuate ;; Emptying
sweeping ;; Removing
destroyed ;; Destroying || damaged ;; Damaging
killed ;; Killing
devastated ;; Destroying || hurricane ;; Catastrophe
developed ;; Coming_to_be || cyclone ;; Catastrophe || hurricane ;; Catastrophe || storm ;; Catastrophe || identified ;; Check
moved ;; Motion || existence ;; Presence || paralleled ;; Motion || intensifying ;; Cause_change_of_strength
storm ;; Catastrophe || strengthened ;; Cause_change_of_strength || positioned ;; Placing
hurricane ;; Catastrophe || attained ;; Getting || intensify ;; Cause_change_of_strength || reaching ;; Cause_change_of_position_on_a_scale
hurricane ;; Catastrophe || storm ;; Catastrophe || landfall ;; Arriving
moved ;; Motion || storm ;; Catastrophe || moved ;; Motion || Weakening ;; Cause_change_of_position_on_a_scale || recurving ;; Self_motion
storm ;; Catastrophe || landfall ;; Arriving
weaken ;; Cause_change_of_position_on_a_scale || transitioning ;; Cause_change_of_position_on_a_scale || degenerating ;; Change
progressed ;; Cause_change_of_position_on_a_scale || dissipating ;; Change
caused ;; Causation || storm ;; Catastrophe || generated ;; Creating
caused ;; Causation || hurricane ;; Catastrophe || landfall ;; Arriving || unroofed ;; Damaging
hampered ;; Hindering
rose ;; Motion_directional || inundating ;; Destroying
Damage ;; Damaging || caused ;; Causation
aided ;; Assistance || caused ;; Causation || hurricane ;; Catastrophe || damage ;; Damaging || landfall ;; Arriving || lift ;; Motion
caused ;; Causation || hurricane ;; Catastrophe
tied ;; Connect || storm ;; Catastrophe || cyclone ;; Catastrophe
became ;; Becoming || Forming ;; Coming_to_be || tracked ;; Motion
developed ;; Coming_to_be || named ;; Name_conferral || cyclone ;; Catastrophe
began ;; Process_start || ending ;; Process_end || storm ;; Self_motion || intensification ;; Expansion || increased ;; Cause_change_of_position_on_a_scale || decreased ;; Cause_change_of_position_on_a_scale
storm ;; Self_motion || reported ;; Reporting || peaked ;; Arriving
caused ;; Causation || Increasing ;; Cause_change_of_position_on_a_scale || acceleration ;; Cause_change_of_position_on_a_scale
storm ;; Self_motion
moving ;; Motion || dissipating ;; Change
caused ;; Causation || damage ;; Damaging || prevented ;; Preventing_or_letting || cyclone ;; Self_motion
peaked ;; Arriving || storm ;; Catastrophe
damage ;; Damaging || reported ;; Reporting || flooding ;; Catastrophe || recorded ;; Recording
retired ;; GiveUp
Championship ;; Competition || Championships ;; Competition
tournament ;; Competition || co-hosted ;; Social_event
championship ;; Competition || hosted ;; Social_event
hosted ;; Social_event
tournament ;; Competition
hosted ;; Social_event
hosted ;; Social_event
tournament ;; Competition || anniversary ;; Social_event || anniversary ;; Social_event || anniversary ;; Social_event || anniversary ;; Social_event || anniversary ;; Social_event || confederation ;; Cause_to_amalgamate || anniversary ;; Social_event || initiated ;; Process_start
planned ;; Arranging || make ;; Action || celebrations ;; Social_event
organized ;; Arranging || event ;; Competition
provided ;; Supply
event ;; Competition || relegation ;; Motion || compete ;; Competition
relegated ;; Change
killing ;; Killing || cyclone ;; Catastrophe || impacted ;; Influence
storm ;; Catastrophe || proceeded ;; Self_motion || strike ;; Bodily_harm || Developing ;; Cause_to_make_progress
heading ;; Self_motion
Weakened ;; Cause_change_of_strength || storm ;; Catastrophe || landfall ;; Catastrophe
weakened ;; Cause_change_of_strength || moving ;; Self_motion || cyclone ;; Catastrophe || dissipated ;; Change
Flooding ;; Catastrophe || losses ;; Earnings_and_losses || took ;; Bringing
went ;; Self_motion
buffeted ;; Bodily_harm
hurricane ;; Catastrophe || demolished ;; Destroying
died ;; Death
took ;; Conquering || storm ;; Catastrophe || flooding ;; Catastrophe || damage ;; Damaging || destroyed ;; Destroying || razed ;; Destroying
destruction ;; Destroying || killed ;; Killing || took place ;; Process_start
isolated ;; Preventing_or_letting
hurricane ;; Catastrophe || floods ;; Catastrophe
flowed ;; Motion || disaster ;; Catastrophe || Reported ;; Statement || decimated ;; Cause_change_of_position_on_a_scale || flood ;; Catastrophe
trapped ;; Preventing_or_letting
losses ;; Earnings_and_losses || storm ;; Catastrophe || dead ;; Death || left ;; Causation
reached ;; Arriving || damage ;; Damaging
beginning ;; Process_start || ending ;; Process_end || command ;; Control
formed ;; Coming_to_be || imposed ;; Action
became ;; Becoming || generate ;; Creating || incurred ;; Causation
became ;; Becoming || applied ;; Using
fermented ;; Change || make ;; Causation
resisted ;; Defending
served ;; Assistance
fighting ;; Hostile_encounter || believed ;; Know
used ;; Using || prevent ;; Preventing_or_letting
paid ;; Commerce_pay || serve ;; Assistance || arrived in ;; Arriving || came to ;; Motion
attacked ;; Attack || raised ;; Cause_change_of_position_on_a_scale
responded ;; Response || send ;; Sending || sending ;; Sending || negotiate ;; Communication || enforce ;; Action
provided ;; Supply || rode ;; Self_motion || suppress ;; Hindering
rebels ;; Hostile_encounter || arrival ;; Arriving
arrested ;; Arrest
found ;; Know || convicted ;; Legal_rulings
recorded ;; Recording
demonstrated ;; Protest || collect ;; Come_together || suppress ;; Hindering || remained ;; Presence
contributed ;; Giving
repealed ;; Removing
handled ;; Hold || strengthened ;; Cause_change_of_strength || argues ;; Protest
storm ;; Catastrophe || struck ;; Attack || Cyclone ;; Catastrophe
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe || intensified ;; Cause_change_of_strength || moving ;; Motion
storm ;; Catastrophe || intensified ;; Cause_change_of_strength || passing ;; Motion_directional || cyclone ;; Catastrophe || rainfall ;; Catastrophe || produced ;; Creating
strengthened ;; Cause_change_of_strength || cyclone ;; Catastrophe || reach ;; Arriving
weakened ;; Cause_change_of_strength || crossing ;; Motion_directional || cyclone ;; Catastrophe
caused ;; Causation || bringing ;; Bringing || damage ;; Damaging || meandered ;; Self_motion
approaching ;; Arriving || trough ;; Motion || cyclone ;; Catastrophe || turned ;; Becoming || dissipated ;; Change
Damage ;; Damaging || made ;; Manufacturing || Cyclone ;; Catastrophe || landfall ;; Arriving
destroyed ;; Destroying || leaving ;; Departing
reached ;; Arriving || knocked ;; Destroying || wrecked ;; Destroying
rainfall ;; Self_motion || flooded ;; Catastrophe
damage ;; Damaging || deaths ;; Death
storm ;; Catastrophe || provide ;; Supply
hurricane ;; Catastrophe || entered ;; Arriving
situated ;; Placing || developed ;; Cause_change_of_position_on_a_scale
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || tracking ;; Motion || reached ;; Achieve
hurricane ;; Catastrophe || intensifying ;; Cause_change_of_strength || curved ;; Motion_directional
became ;; Becoming
landfall ;; Arriving || maintained ;; Cause_to_make_progress
storm ;; Catastrophe || moving ;; Motion || weakened ;; Cause_change_of_strength || remained ;; Presence || dissipating ;; Change
storm ;; Catastrophe || occurred ;; Coming_to_be
cleared ;; Removing || resulting in ;; Causation
storm ;; Catastrophe || delayed ;; Change_event_time
storm ;; Catastrophe || damage ;; Damaging || occurred ;; Coming_to_be || produced ;; Creating
storm ;; Catastrophe || considered ;; Influence
reported ;; Reporting
destroyed ;; Destroying || damaged ;; Damaging
storm ;; Catastrophe || sunk ;; Motion || occurred ;; Coming_to_be || caused ;; Causation
damage ;; Damaging || reached ;; Achieve
cyclone ;; Catastrophe
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || cyclone ;; Catastrophe
became ;; Becoming || Looping ;; Self_motion || transitioned ;; Change
tracked ;; Self_motion || oscillating ;; Motion || fluctuations ;; Cause_change_of_position_on_a_scale
landfalls ;; Arriving
cyclone ;; Catastrophe || dissipated ;; Change
damage ;; Damaging || brought ;; Causation || reported ;; Reporting
causing ;; Causation || landfalls ;; Arriving
evacuation ;; Removing || forced ;; Causation
damaged ;; Damaging || destroyed ;; Destroying || causing ;; Causation || spawned ;; Causation || struck ;; Connect
damage ;; Damaging || deaths ;; Death || reported ;; Reporting
contributed ;; Causation
hurricane ;; Catastrophe
formed ;; Coming_to_be || hurricane ;; Catastrophe
intensified ;; Cause_change_of_strength || headed ;; Self_motion
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe
curved ;; Self_motion
weakened ;; Cause_change_of_strength || hurricane ;; Catastrophe || resumed ;; Recovering || moving ;; Self_motion
weakened ;; Cause_change_of_strength || dissipated ;; Change
bypassed ;; Self_motion || observed ;; Know
hurricane ;; Catastrophe || associated ;; Collaboration || observed ;; Know || surpassed ;; Motion_directional
causing ;; Causation || flee ;; Escaping
flooded ;; Destroying
reported ;; Reporting
suffered ;; Bodily_harm
occurred ;; Presence || contact ;; Connect || came ;; Self_motion || downed ;; Motion
hurricane ;; Catastrophe || reaching ;; Arriving
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || hurricane ;; Catastrophe || originated ;; Coming_to_be
became ;; Becoming || hurricane ;; Catastrophe || hurricane ;; Catastrophe || Moving ;; Motion
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || turned ;; Motion
landfall ;; Arriving || weakened ;; Cause_change_of_strength
turned ;; Motion || weakened ;; Cause_change_of_strength || dissipating ;; Change
caused ;; Causation || affected ;; Influence || produced ;; Causation
prompting ;; Causation || continue ;; Process_start || strike ;; Attack || evacuations ;; Removing
turning ;; Motion || dropped ;; Motion_directional
hurricane ;; Catastrophe
buffeted ;; Motion || flooded ;; Motion
damaged ;; Damaging || estimated ;; Statement || leaving ;; Causation
prompted ;; Causation
caused ;; Causation
killed ;; Killing || hurricane ;; Catastrophe
killed ;; Killing
hurricane ;; Catastrophe || struck ;; Attack
hurricane ;; Catastrophe || hurricanes ;; Catastrophe || hurricanes ;; Catastrophe || remains ;; Presence
hurricanes ;; Catastrophe || spent ;; Expend_resource
hurricane ;; Catastrophe || hurricane ;; Catastrophe || achieve ;; Achieve
moved ;; Motion
passed ;; Motion_directional || causing ;; Causation
struck ;; Attack || causing ;; Causation || caused ;; Causation || crossing ;; Motion_directional || weakened ;; Cause_change_of_strength
killed ;; Killing || left ;; Causation
retired ;; GiveUp
retired ;; GiveUp
recorded ;; Recording || precipitated ;; Change
fought ;; Hostile_encounter
aided ;; Assistance
commanded ;; Control
fought ;; Hostile_encounter
leading ;; Causation
hold ;; Defending || defeat ;; Conquering || occupied ;; Control || retaining ;; Preserving
regain ;; Recovering || fallen ;; Conquering
invaded ;; Attack || found ;; Know || supported ;; Supporting
outnumbered ;; Cause_change_of_position_on_a_scale || acquired ;; Getting || made it ;; Causation || defend ;; Defending
fortified ;; Cause_change_of_strength || remained ;; Presence
advance ;; Arriving || prepared ;; GetReady || halted ;; Process_end
march ;; Motion_directional || gathered ;; Come_together
break ;; Destroying || siege ;; Besieging || fought ;; Hostile_encounter
taken ;; Arriving
controlled ;; Control || camp ;; Placing || forced ;; Request
began ;; Process_start || arrived in ;; Arriving || freeze ;; Change
reported ;; Reporting || hold ;; Defending
attack ;; Attack || assumed ;; Deciding
record ;; Recording || cyclone ;; Catastrophe
named ;; Name_conferral || storm ;; Catastrophe || formed ;; Coming_to_be
moved ;; Motion || passed ;; Motion_directional || intensifying ;; Expansion
storm ;; Catastrophe || cyclone ;; Catastrophe || estimated ;; Statement || intensified ;; Expansion
looped ;; Motion || weakened ;; Cause_change_of_strength || turning ;; Removing
storm ;; Catastrophe || passing ;; Motion_directional
became ;; Becoming || dissipating ;; Change || turning ;; Removing
received ;; Receiving || dropped ;; Motion
recorded ;; Recording
lift ;; Motion || occurred ;; Coming_to_be || called ;; Name_conferral || leading to ;; Causation
damaged ;; Damaging || floods ;; Catastrophe
caused ;; Causation || destroyed ;; Destroying || damage ;; Damaging || damaged ;; Damaging
storm ;; Catastrophe || Losses ;; Earnings_and_losses || killed ;; Killing
devastated ;; Destroying || hurricane ;; Catastrophe || cyclone ;; Motion
hurricane ;; Catastrophe || hurricane ;; Catastrophe || formed ;; Coming_to_be || storm ;; Catastrophe
began ;; Process_start || existence ;; Presence || nearing ;; Arriving || intensify ;; Cause_change_of_position_on_a_scale
Curving ;; Motion || reached ;; Arriving
moved ;; Motion || approached ;; Arriving || Intensification ;; Cause_change_of_position_on_a_scale
reached ;; Arriving || intensified ;; Cause_change_of_position_on_a_scale
began ;; Process_start || weaken ;; Cause_change_of_position_on_a_scale || dissipated ;; Change
damages ;; Damaging || caused ;; Causation || deaths ;; Death || hurricane ;; Catastrophe
caused ;; Causation || incidents ;; Catastrophe || damage ;; Damaging || inundated ;; Damaging || storm ;; Catastrophe || mitigated ;; Cause_change_of_position_on_a_scale
destroyed ;; Destroying || downed ;; Motion || forced ;; Causation
peaked ;; Cause_change_of_position_on_a_scale
fatal ;; Killing || approach ;; Arriving || involved ;; Cause_to_be_included || crashed ;; Catastrophe || accident ;; Catastrophe
extended ;; Expansion || Due ;; Causation || maintain ;; Preserving
affected ;; Influence || descended ;; Motion_directional || freezing ;; Change || approached ;; Self_motion
began ;; Process_start || passed ;; Motion_directional || crashed ;; Catastrophe || hit ;; Connect || roll ;; Motion
collided ;; Catastrophe || demolished ;; Destroying
impact ;; Influence || died ;; Death || killed ;; Killing || destroyed ;; Destroying
injuries ;; Bodily_harm
hurricane ;; Catastrophe || record ;; Recording
cyclone ;; Catastrophe || observed ;; Know || tracked ;; Motion
intensified ;; Expansion || reached ;; Achieve
hurricane ;; Catastrophe || landfall ;; Arriving || turning ;; Self_motion
dissipating ;; Change
hurricane ;; Catastrophe || Impact ;; Influence
disasters ;; Catastrophe || becoming ;; Becoming || curved ;; Motion_directional || moved ;; Motion || forecast ;; Statement
submerged ;; Destroying
destroyed ;; Destroying || flooding ;; Catastrophe || damaged ;; Damaging
leaving ;; Causation || storm ;; Catastrophe || recover ;; Recovering
caused ;; Causation || rainfall ;; Catastrophe || mudslides ;; Catastrophe || contributed ;; Giving || fatalities ;; Death
delivering ;; Sending || destruction ;; Destroying || cyclone ;; Catastrophe || hindered ;; Hindering
disease ;; Catastrophe || prevent ;; Preventing_or_letting
hurricane ;; Catastrophe || inflicted ;; Causation || damage ;; Damaging
storm ;; Catastrophe
developed ;; Coming_to_be || storm ;; Catastrophe
caused ;; Causation || moving ;; Motion || dissipate ;; Change
storm ;; Catastrophe || regenerated ;; Recovering
storm ;; Catastrophe || hit ;; Arriving || attained ;; Getting || turned ;; Becoming
became ;; Becoming || storm ;; Catastrophe || accelerated ;; Cause_change_of_strength
storm ;; Catastrophe || reach ;; Arriving || landfall ;; Self_motion
impact ;; Influence
caused ;; Causation || consisted ;; Cause_to_be_included
caused ;; Causation || storm ;; Catastrophe || killed ;; Killing || inflicted ;; Causation
struck ;; Attack || landfall ;; Self_motion
hurricane ;; Catastrophe || landfall ;; Arriving
developed ;; Coming_to_be || storms ;; Catastrophe || hurricanes ;; Catastrophe
becoming ;; Becoming || storm ;; Catastrophe || crossed ;; Motion_directional || moved ;; Self_motion
caused ;; Causation || approaching ;; Arriving || turn ;; Change || attained ;; Getting
damage ;; Damaging || moved ;; Motion || spawning ;; Causation || leaving ;; Departing
killed ;; Killing || produced ;; Causation
caused ;; Causation || weakening ;; Cause_change_of_strength || maintained ;; Preserving
passed ;; Motion_directional || becoming ;; Becoming || dissipating ;; Change
approach ;; Arriving || crash ;; Catastrophe
hit ;; Attack || perished ;; Death
operated ;; Being_in_operation || service ;; Supply || services ;; Supply || scheduled ;; Arranging
icing ;; Change || caused ;; Causation
causing ;; Causation || caused ;; Causation || reduced ;; Cause_change_of_position_on_a_scale
caused ;; Causation || entered ;; Becoming_a_member || icing ;; Motion || loss ;; Earnings_and_losses || control ;; Control || dive ;; Self_motion
deceased ;; Death
conflict ;; Hostile_encounter || War ;; Hostile_encounter || took place ;; Process_start
warfare ;; Hostile_encounter || protracted ;; Change_event_time || curb ;; Hindering || progressed ;; Cause_change_of_position_on_a_scale || entangled ;; Participation
voted ;; Choosing || uniting ;; Collaboration || voted ;; Choosing || boycotted ;; Agree_or_refuse_to_act || held ;; Defending
began ;; Process_start || conflict ;; Hostile_encounter || demand ;; Request || disintegrated ;; Change || grew ;; Expansion || resulting in ;; Causation || claims ;; Statement
broke out ;; Process_start || voted ;; Choosing || clashes ;; Hostile_encounter || unify ;; Cause_to_amalgamate
conflict ;; Hostile_encounter || declaration ;; Expressing_publicly
declared ;; Expressing_publicly || removed ;; Removing || proclaimed ;; Statement || voted ;; Choosing || unrecognized ;; Know || secede ;; Escaping || held ;; Defending
erupted ;; Process_start || fighting ;; Hostile_encounter || bring ;; Bringing || end ;; Process_end || work ;; Employment
captured ;; Conquering
control ;; Control || end ;; Process_end || war ;; Hostile_encounter || surrounding ;; Surrounding
signed ;; Sign_agreement || result in ;; Causation || talks ;; Communication
legal ;; Legality || left ;; Departing || remaining ;; Presence || unrecognized ;; Know || control ;; Control
conflict ;; Hostile_encounter
occurred ;; Coming_to_be || explosion ;; Catastrophe
liquefied ;; Change
killed ;; Killing || burned ;; Bodily_harm
victims ;; Catastrophe
renovated ;; Recovering
disaster ;; Catastrophe || disaster ;; Catastrophe || hurricane ;; Catastrophe
occurred ;; Coming_to_be || hurricane ;; Catastrophe
storm ;; Catastrophe || hurricane ;; Catastrophe || wave ;; Motion || developed ;; Coming_to_be
headed ;; Motion || struck ;; Attack
entering ;; Arriving || strengthened ;; Cause_change_of_strength || storm ;; Catastrophe
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe
transitioned ;; Change
struck ;; Attack || dissipating ;; Change
storm ;; Catastrophe || rainfall ;; Catastrophe || caused ;; Causation || dropped ;; Motion || damage ;; Damaging
tides ;; Catastrophe || tornado ;; Catastrophe || resulted in ;; Causation || damage ;; Damaging
losses ;; Earnings_and_losses
storm ;; Catastrophe || effects ;; Influence || caused ;; Causation || becoming ;; Becoming
approaching ;; Arriving || receive ;; Getting || storm ;; Catastrophe
destroyed ;; Destroying || damaged ;; Damaging
capsized ;; Destroying || fatalities ;; Death
damaged ;; Damaging || disrupted ;; Hindering || costing ;; Cost
became ;; Becoming || disaster ;; Catastrophe
storm ;; Catastrophe || affected ;; Influence
storm ;; Catastrophe || included ;; Cause_to_be_included || damaging ;; Damaging || storm ;; Catastrophe || flooding ;; Catastrophe || record ;; Recording || tornado ;; Catastrophe || outbreak ;; Catastrophe
developed ;; Coming_to_be || storm ;; Catastrophe || tornadoes ;; Catastrophe || advanced ;; Cause_to_make_progress || spawning ;; Causation
turned ;; Becoming || reaching ;; Arriving || intensified ;; Cause_change_of_strength || cyclone ;; Catastrophe || nor'easter ;; Catastrophe
storm ;; Catastrophe || causing ;; Causation || drift ;; Self_motion || forward ;; Self_motion || slowed ;; Cause_change_of_position_on_a_scale
caused ;; Causation || allowed ;; Preventing_or_letting || storm ;; Catastrophe || change ;; Change
crippling ;; Destroying || remained ;; Temporary_stay || accumulations ;; Cause_change_of_position_on_a_scale || reached ;; Arriving || travel ;; Traveling
Drifts ;; Self_motion || blocked ;; Hindering || isolating ;; Hindering || forcing ;; Causation
attributed ;; Causation || storm ;; Catastrophe || fatalities ;; Death
occurred ;; Presence || freezing ;; Change
caused ;; Causation || damage ;; Damaging || aftermath ;; Catastrophe || hurricane ;; Catastrophe
received ;; Receiving || record ;; Recording || blamed ;; Judgment_communication || storm ;; Catastrophe || snowstorm ;; Catastrophe || deaths ;; Death
caused ;; Causation || drainage ;; Emptying || flooding ;; Catastrophe || forced ;; Causation || submerged ;; Destroying
battered ;; Bodily_harm
unrest ;; Violence || strikes ;; Attack || events ;; Social_event
halt ;; Process_end || brought ;; Causation
fled ;; Escaping || ceased ;; Process_end || protests ;; Attack || reached ;; Arriving || feared ;; Change_sentiment || war ;; Hostile_encounter || revolution ;; Change_of_leadership
spurred ;; Causation
began ;; Process_start || unrest ;; Violence || order ;; Arranging
spread ;; Dispersal || involving ;; Cause_to_be_included || strikes ;; Attack
decentralized ;; Dispersal || created ;; Creating || conflict ;; Violence
strike ;; Attack || strike ;; Attack
strikes ;; Attack || initiated ;; Process_start || met with ;; Come_together
strikes ;; Attack || strikes ;; Attack || leading to ;; Causation || spread ;; Dispersal || quell ;; Preventing_or_letting || inflamed ;; Expansion || battles ;; Attack
fled ;; Escaping || assembly ;; Building || returning ;; Arriving || dissolved ;; Dispersal || called ;; Request || elections ;; Choosing
evaporated ;; Change || arose ;; Coming_to_be
emerged ;; Coming_to_be || went back ;; Recovering || held ;; Hold || elections ;; Choosing
affected ;; Influence
turning ;; Change
revolution ;; Change_of_leadership
named ;; Name_conferral || hurricane ;; Catastrophe || known ;; Know
developed ;; Coming_to_be || hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || moved ;; Self_motion || reach ;; Achieve
reached ;; Achieve || passing ;; Motion_directional || encountering ;; Hostile_encounter || turning ;; Motion
dissipated ;; Change
hurricane ;; Catastrophe
prevented ;; Preventing_or_letting
declaring ;; Expressing_publicly || hurricane ;; Catastrophe
received ;; Receiving || extending ;; Change_event_time || supported ;; Supporting || resulting in ;; Causation
hurricane ;; Catastrophe || hurricane ;; Catastrophe || strike ;; Bodily_harm
storm ;; Catastrophe || observed ;; Know || cyclone ;; Catastrophe || storm ;; Catastrophe || hurricane ;; Catastrophe
moving ;; Motion || storm ;; Catastrophe || strengthened ;; Cause_change_of_strength
became ;; Becoming || storm ;; Catastrophe || hurricane ;; Catastrophe
landfall ;; Motion
winds ;; Motion
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || emerging ;; Arriving
storm ;; Catastrophe || re-strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe
hurricane ;; Catastrophe || curved ;; Motion_directional || intensified ;; Cause_change_of_strength
storm ;; Catastrophe || struck ;; Attack
storm ;; Catastrophe || landfall ;; Motion || weakening ;; Cause_change_of_strength
emerged ;; Coming_to_be
storm ;; Catastrophe || meandered ;; Motion || dissipating ;; Change || striking ;; Attack
observed ;; Perception_active
damage ;; Damaging
destroyed ;; Destroying || obliterated ;; Destroying || establish ;; Building
losses ;; Earnings_and_losses || suffered ;; Bodily_harm
deaths ;; Death || damage ;; Damaging || reported ;; Reporting
brought ;; Bringing || record ;; Recording || travel ;; Traveling || event ;; Social_event
began ;; Process_start || included ;; Cause_to_be_included || record ;; Recording || breaking ;; Destroying
began ;; Process_start || falling ;; Motion_directional || record ;; Recording
falling ;; Motion_directional
recorded ;; Recording
affected ;; Influence
causing ;; Causation || thaw ;; Change || recovered ;; Recovering
reintroduced ;; Recovering
brought ;; Bringing
closed ;; Self_motion || led to ;; Causation
records ;; Recording || recorded ;; Recording || record ;; Recording || broken ;; Destroying
begun ;; Process_start || thaw ;; Change
end ;; Process_end || brought ;; Bringing
ended ;; Process_end || returned ;; Arriving
affected ;; Influence
associated ;; Collaboration
issues ;; Creating || focus ;; Aiming
give ;; Giving || support ;; Supporting || distrust ;; Influence
began ;; Process_start || collaborated ;; Collaboration || oppose ;; Agree_or_refuse_to_act || deal ;; Resolve_problem || led ;; Causation || energized ;; Cause_change_of_strength
gained ;; Getting
election ;; Choosing || election ;; Choosing
affected ;; Influence || reversed ;; Change || election ;; Choosing || undertaken ;; Action || elections ;; Choosing
party ;; Social_event || split ;; Destroying
deserted ;; Escaping || party ;; Social_event || felt ;; Know
state ;; Statement || voted ;; Change_of_leadership
solidified ;; Change || increased ;; Cause_change_of_position_on_a_scale || defense ;; Defending || rolling ;; Motion
led ;; Causation || call ;; Name_conferral
remains ;; Presence
become ;; Becoming || issues ;; Creating || marriage ;; Forming_relationships
party ;; Social_event || made ;; Manufacturing || election ;; Choosing
occurred ;; Coming_to_be || crash ;; Catastrophe
resulted in ;; Causation || deaths ;; Death
controlled ;; Control
occupied ;; Control || standing ;; Placing || accident ;; Catastrophe
occurred ;; Coming_to_be || collided ;; Hostile_encounter
struck ;; Attack
starting ;; Process_start || engulfed ;; Destroying
killed ;; Killing
established ;; Building || destroyed ;; Destroying || recovered ;; Recovering || consumed ;; Expend_resource
reduced ;; Cause_change_of_position_on_a_scale
identified ;; Check || claimed ;; Statement
buried ;; Placing
completed ;; Process_end || found ;; Know || neglect ;; GiveUp
occupied ;; Control || allow ;; Preventing_or_letting || reversed ;; Change
presence ;; Presence || cleared ;; Removing
convicted ;; Legal_rulings || charged ;; Judgment_communication
released ;; Releasing || re-employed ;; Employment
tour ;; Traveling
festivals ;; Social_event || lead ;; Causation || included ;; Cause_to_be_included || perform ;; Hold
planned ;; Arranging || announcing ;; Expressing_publicly || spend ;; Expend_resource
announced ;; Expressing_publicly || announced ;; Expressing_publicly || concert ;; Social_event || Festival ;; Social_event || playing ;; Participation || playing ;; Participation
success ;; Achieve
earned ;; Earnings_and_losses || announced ;; Expressing_publicly || sold ;; Commerce_sell || tickets ;; Rewards_and_punishments
ranked ;; Placing
debut ;; Process_start || released ;; Publishing
released ;; Publishing
selling ;; Commerce_sell || certified ;; Check || debuted ;; Process_start
spent ;; Expend_resource || remained ;; Temporary_stay
best-performing ;; Judgment_communication || certified ;; Check || ranked ;; Placing
selling ;; Commerce_sell
attack ;; Attack
prompted ;; Causation || reigned ;; Control || fighting ;; Hostile_encounter
aid ;; Assistance || attack ;; Attack || involved ;; Participation
hold ;; Defending || spent ;; Expend_resource || build ;; Building
conducted ;; Action || granted ;; Giving || occurred ;; Coming_to_be || form ;; Coming_to_be || considers ;; Coming_to_believe
written ;; Writing
organized ;; Arranging
spent ;; Expend_resource
started ;; Process_start || opening ;; Openness
reminded ;; Telling
close ;; Self_motion || discuss ;; Communication
signed ;; Sign_agreement
notify ;; Telling || taking up ;; Conquering || abrogated ;; Process_end || discuss ;; Communication
noted ;; Know || forming ;; Coming_to_be
turned ;; Becoming || notify ;; Telling || relocated ;; Removing
released ;; Publishing
preceded ;; Process_start || release ;; Publishing
change ;; Change || hit ;; Attack || marked ;; Recording || featuring ;; Cause_to_be_included
sales ;; Commerce_sell || spent ;; Expend_resource || certified ;; Check
sold ;; Commerce_sell || spent ;; Expend_resource
best-selling ;; Commerce_sell || ranked ;; Placing
crashed ;; Catastrophe || killing ;; Killing
take-off ;; Self_motion || reported ;; Reporting
lost ;; Earnings_and_losses
crashed ;; Catastrophe
found ;; Know || loss ;; Earnings_and_losses || victim ;; Catastrophe || investigation ;; Criminal_investigation || accident ;; Catastrophe
exhausted ;; Expend_resource || due ;; Causation || using ;; Using
occurred ;; Presence || denoted ;; Writing || conflict ;; Hostile_encounter
fought ;; Hostile_encounter
resulted in ;; Causation || intertwined ;; Cause_to_amalgamate
assistance ;; Assistance || petitioning ;; Request || formed ;; Creating
broke out ;; Process_start
eliminate ;; Removing || raids ;; Attack || staged ;; Hold
deployed ;; Arranging || reconnaissance ;; Scrutiny || carry out ;; Action || track ;; Self_motion
became ;; Becoming || progressed ;; Cause_to_make_progress
produced ;; Manufacturing || resulted in ;; Causation || casualties ;; Death
disrupt ;; Hindering || arming ;; Hold || raids ;; Attack || stop ;; Process_end
confront ;; Confronting_problem || Beginning ;; Process_start || command ;; Control
bolstered ;; Supporting
ended ;; Process_end || intensify ;; Cause_change_of_strength || leading to ;; Causation || resumed ;; Process_start || took ;; Getting
ended ;; Process_end || committed ;; Commitment || withdrawal ;; Removing || mediated ;; Assistance
launched ;; Military_operation
received ;; Receiving
impact ;; Influence || fought ;; Hostile_encounter
presenting ;; Presence || used ;; Using || devoted ;; Expend_resource || stoke ;; Convincing
remains ;; Presence || given rise to ;; Cause_change_of_position_on_a_scale
terrorist ;; Terrorism || ended ;; Process_end || occurred ;; Coming_to_be || involved ;; Cause_to_be_included || hostage-taking ;; Terrorism || massacre ;; Killing
began ;; Process_start || entered ;; Arriving
killed ;; Killing || attacked ;; Attack || killing ;; Killing || building ;; Building || injuring ;; Bodily_harm || entered ;; Arriving
took ;; Conquering || headed ;; Self_motion
spending ;; Expend_resource
release ;; Releasing || kill ;; Killing || issued ;; Creating
stormed ;; Catastrophe
killed ;; Killing
killed ;; Killing || injured ;; Bodily_harm
fought ;; Hostile_encounter
siege ;; Besieging || depleted ;; Expend_resource || supplies ;; Supply
obtain ;; Getting || arrived in ;; Arriving || began ;; Process_start || help ;; Assistance || maintained ;; Preserving || put ;; Placing || reduce ;; Cause_change_of_position_on_a_scale || ordered ;; Request
march ;; Self_motion || sail ;; Sending || carried ;; Bringing
beginning ;; Process_start || led ;; Conquering
gave ;; Giving || notified ;; Telling || departure ;; Departing || patrolling ;; Patrolling || absence ;; Presence || command ;; Control
delaying ;; Change_event_time || causing ;; Causation || attacked ;; Attack || bring ;; Bringing || manoeuvered ;; Hold
attacks ;; Attack || continued ;; Giving || combined ;; Cause_to_amalgamate || fight ;; Defending || fighting ;; Defending || resist ;; Defending
damage ;; Damaging || celebrated ;; Social_event || Casualties ;; Catastrophe
affected ;; Influence
formed ;; Coming_to_be || named ;; Name_conferral
moved ;; Motion || steered ;; Motion
affected ;; Influence
weakened ;; Cause_change_of_strength || re-intensified ;; Cause_change_of_strength || approached ;; Arriving
reached ;; Arriving || developed ;; Coming_to_be
stalled ;; Temporary_stay || weakening ;; Cause_change_of_strength || turning ;; Motion
became ;; Becoming || dissipated ;; Expend_resource
Damage ;; Damaging || destroyed ;; Destroying || killed ;; Killing
destroyed ;; Destroying || leaving ;; Departing
damaging ;; Damaging || destroyed ;; Destroying || disrupted ;; Hindering
injured ;; Bodily_harm || killed ;; Killing
conducted ;; Action || raiding ;; Attack
raid ;; Attack || expedition ;; Self_motion
reached ;; Arriving || raiding ;; Attack
spent ;; Expend_resource || lost ;; Earnings_and_losses || gained ;; Getting || control ;; Control || destroying ;; Destroying || destroy ;; Destroying
production ;; Creating || left ;; Departing || flooded ;; Filling || returned ;; Arriving || repaired ;; Recovering
raiding ;; Attack || striking ;; Attack || returning ;; Arriving || continued ;; Self_motion
campaigns ;; Military_operation || engagement ;; Hostile_encounter || conducted ;; Action || Battle ;; Hostile_encounter || invasions ;; Attack
campaigns ;; Military_operation || made ;; Causation
campaigns ;; Military_operation || halting ;; Preventing_or_letting || invasion ;; Attack
change ;; Change || Battle ;; Hostile_encounter || following ;; Motion || sighted ;; Know || spend ;; Expend_resource || forced ;; Causation || heading ;; Self_motion
ordered ;; Arranging || destroyed ;; Destroying || skirmish ;; Hostile_encounter || pursue ;; Hostile_encounter
support ;; Supporting || concert ;; Social_event || Tour ;; Traveling
left ;; Departing || record ;; Recording || tour ;; Traveling || tour ;; Traveling
obtained ;; Getting || record ;; Recording
known ;; Know || work ;; Employment || held ;; Hold || replaced ;; Change
becoming ;; Becoming || match ;; Competition
named ;; Name_conferral
got to ;; Getting || knew ;; Know || clicked ;; Self_motion
released ;; Publishing || spent ;; Expend_resource || support ;; Supporting || spend ;; Expend_resource || debut ;; Process_start || debut ;; Process_start || saw ;; Self_motion || tour ;; Traveling || touring ;; Traveling || tour ;; Traveling || tour ;; Traveling
tour ;; Traveling || played ;; Action
left ;; Departing || swearing ;; Statement || tour ;; Traveling
end ;; Process_end || decided ;; Deciding || working out ;; Know || tour ;; Traveling || returned ;; Arriving
agreed ;; Agree_or_refuse_to_act || conflicts ;; Hostile_encounter || took place ;; Process_start || War ;; Hostile_encounter || final ;; Process_end || confrontation ;; Hostile_encounter
spent ;; Expend_resource || conflict ;; Hostile_encounter || war ;; Hostile_encounter || suffered ;; Catastrophe || casualties ;; Death
changes ;; Change || resulted ;; Causation
triggered ;; Causation || ruling ;; Legal_rulings || invading ;; Attack || war ;; Hostile_encounter || broke ;; Bodily_harm
end ;; Process_end || disputed ;; Quarreling || war ;; Hostile_encounter || held ;; Conquering || advanced ;; Cause_to_make_progress
conflict ;; Hostile_encounter || ended ;; Process_end || disputed ;; Quarreling || war ;; Hostile_encounter || established ;; Creating || founded ;; Creating
occupies ;; Control
agreed ;; Agree_or_refuse_to_act || declared ;; Expressing_publicly || ruling ;; Legal_rulings || implement ;; Action || signed ;; Sign_agreement || coalition ;; Collaboration || headed ;; Motion
referred ;; Name_conferral
fell ;; Motion_directional
defended ;; Defending
attacks ;; Attack || launched ;; Motion
took ;; Conquering || defending ;; Defending
occurred ;; Presence || spent ;; Expend_resource || wounded ;; Bodily_harm || defending ;; Defending || sending up ;; Sending || forces ;; Military_operation || evacuating ;; Emptying
increased ;; Cause_change_of_position_on_a_scale
adapted ;; Change || called ;; Name_conferral
Battle ;; Hostile_encounter || Counteroffensive ;; Attack || campaign ;; Military_operation || War ;; Military_operation || took place ;; Process_start
war ;; Military_operation || launched ;; Process_start
destroy ;; Destroying || negotiate ;; Communication || offensive ;; Attack || stop ;; Preventing_or_letting || use ;; Using || split ;; Change || encircle ;; Besieging || force ;; Causation || allowing ;; Preventing_or_letting
achieved ;; Achieve || attack ;; Attack || reconnaissance ;; Scrutiny || due ;; Causation
attack ;; Attack || incurred ;; Causation || war ;; Military_operation || forces ;; Causation
depleted ;; Expend_resource || battle ;; Hostile_encounter || forces ;; Causation
losses ;; Earnings_and_losses
attacked ;; Attack || grounded ;; Placing || defended ;; Defending || taking ;; Conquering
resistance ;; Defending || offensive ;; Attack || blocked ;; Hindering
advance ;; Self_motion || found ;; Know
allowed ;; Preventing_or_letting || threw ;; Causation || reinforce ;; Cause_change_of_strength || placed ;; Placing || favored ;; Causation
stopped ;; Process_end || offensive ;; Attack
permitted ;; Preventing_or_letting || attacks ;; Attack || offensive ;; Attack || improved ;; Cause_to_make_progress || forces ;; Motion || supply ;; Supply || sealed ;; Causation
siege ;; Besieging || reached ;; Arriving || ending ;; Process_end
attack ;; Attack || battle ;; Hostile_encounter || offensive ;; Attack || broken ;; Process_start || restored ;; Recovering
depleted ;; Expend_resource || retreated ;; Escaping || defeat ;; Conquering
attack ;; Attack || fighting ;; Hostile_encounter || involved ;; Cause_to_be_included
reinforced ;; Cause_change_of_strength || bringing ;; Bringing
killed ;; Killing || wounded ;; Bodily_harm || captured ;; Conquering
became ;; Becoming || killed ;; Killing
battle ;; Hostile_encounter || War ;; Military_operation || campaign ;; Military_operation
offensive ;; Attack || took place ;; Process_start
encircle ;; Besieging || seize ;; Conquering
begin ;; Process_start
amended ;; Change || invasion ;; Attack
expanded ;; Expansion
captured ;; Conquering || pushed ;; Motion || occupied ;; Control
began ;; Process_start
delayed ;; Change_event_time
sign ;; Sign_agreement || suspended ;; Change_event_time
diverted ;; Removing
withdraw ;; Escaping || ordered ;; Request
reinforced ;; Cause_change_of_strength || delayed ;; Change_event_time || made ;; Manufacturing
capture ;; Conquering || squandered ;; Expend_resource || achieving ;; Achieve || concluded ;; Process_end
committed ;; Commitment || resist ;; Defending || deprived ;; Destroying || forfeited ;; Earnings_and_losses
held ;; Hold || exhibition ;; Social_event
exhibition ;; Social_event
support ;; Supporting
set ;; Placing
exhibit ;; Social_event
celebration ;; Social_event
offers ;; Supply
cost ;; Cost || spent ;; Expend_resource
pleas ;; Request || fell ;; Cause_change_of_position_on_a_scale
meeting ;; Social_event
grew ;; Expansion || increase ;; Cause_change_of_position_on_a_scale
visited ;; Traveling
caused ;; Causation || storm ;; Catastrophe || cyclone ;; Catastrophe
storm ;; Catastrophe || formed ;; Coming_to_be
storm ;; Catastrophe || came ;; Arriving
crossing ;; Motion_directional || becoming ;; Becoming || produced ;; Creating
intensified ;; Cause_change_of_strength
hitting ;; Attack || dissipated ;; Expend_resource
became ;; Becoming || storm ;; Catastrophe || storm ;; Catastrophe || affected ;; Influence || damaging ;; Damaging
incidents ;; Catastrophe || caused ;; Causation || storm ;; Catastrophe || damage ;; Damaging
killing ;; Killing || capsized ;; Destroying
driven ;; Motion || put ;; Placing
cyclone ;; Catastrophe || Cyclone ;; Catastrophe || hurricane ;; Catastrophe || make ;; Creating
named ;; Name_conferral || formed ;; Coming_to_be || storm ;; Catastrophe
storms ;; Catastrophe || result ;; Causation || storm ;; Catastrophe || formation ;; Coming_to_be || increase ;; Cause_change_of_position_on_a_scale || hit ;; Attack || contributed ;; Causation
moved ;; Motion || strengthening ;; Cause_change_of_strength || turned ;; Motion
cyclone ;; Catastrophe || expected ;; Communication || intensified ;; Expansion || weakened ;; Cause_change_of_strength
developed ;; Coming_to_be || cyclone ;; Catastrophe || reaching ;; Achieve || striking ;; Attack || re-intensified ;; Cause_change_of_strength
struck ;; Attack || crossing ;; Motion_directional || dissipating ;; Expend_resource || weakened ;; Cause_change_of_strength || turning ;; Motion
rainfall ;; Self_motion || Cyclone ;; Catastrophe || dropped ;; Motion || reaching ;; Arriving
caused ;; Causation || flooding ;; Catastrophe || mudslides ;; Catastrophe || wrecking ;; Destroying || leaving ;; Causation
destroyed ;; Destroying || damaged ;; Damaging
killed ;; Killing || injured ;; Bodily_harm
election ;; Change_of_leadership || declared ;; Expressing_publicly || declared ;; Expressing_publicly || cyclone ;; Catastrophe || struck ;; Attack || disrupted ;; Hindering || lost ;; Earnings_and_losses
known ;; Know || flooding ;; Catastrophe || produced ;; Manufacturing
declared ;; Expressing_publicly
entering ;; Arriving || intensified ;; Cause_change_of_strength
intensified ;; Cause_change_of_strength || attaining ;; Achieve
weakening ;; Cause_change_of_strength || landfall ;; Self_motion
weakened ;; Cause_change_of_strength || dissipated ;; Expend_resource
killed ;; Killing || wrought ;; Causation
killed ;; Killing
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || control ;; Control || fought ;; Hostile_encounter
Battle ;; Hostile_encounter || capture ;; Conquering
reached ;; Arriving || engage ;; Hostile_encounter
reinforced ;; Cause_change_of_strength
relieved ;; Process_end || depleted ;; Expend_resource
massacred ;; Killing || counter-attack ;; Attack || relieved ;; Cure || driven ;; Motion
break ;; Destroying || become ;; Becoming || Campaign ;; Military_operation
capture ;; Arrest || campaign ;; Military_operation
recaptured ;; Conquering
organised ;; Arranging || relief ;; Assistance || raise ;; Earnings_and_losses
attended ;; Perception_active || attended ;; Participation
inspired ;; Influence || happened ;; Presence
watched ;; Perception_active
impact ;; Influence || debated ;; Communication
stated ;; Statement || generated ;; Creating
took ;; Conquering || states ;; Statement
adds ;; Cause_to_be_included || created ;; Creating || asked ;; Request
run ;; Self_motion || aid ;; Assistance
spent ;; Expend_resource || went to ;; Motion
event ;; Competition
games ;; Competition || hosted ;; Social_event
participated ;; Participation || Games ;; Competition
opened ;; Process_start
followed ;; Motion
broken ;; Destroying || records ;; Recording
cost ;; Cost || arrangement ;; Arranging || commitment ;; Commitment || conservation ;; Preserving || games ;; Competition || deemed ;; Coming_to_believe || spent ;; Expend_resource || host ;; Social_event
killed ;; Killing || record ;; Recording
hurricane ;; Catastrophe || Originating ;; Coming_to_be || tracked ;; Self_motion
storm ;; Catastrophe || intensified ;; Cause_change_of_position_on_a_scale
storm ;; Catastrophe || hurricane ;; Catastrophe || intensified ;; Cause_change_of_position_on_a_scale || attained ;; Getting
hurricane ;; Catastrophe || brushed ;; Damaging || making ;; Manufacturing
becoming ;; Becoming || weakened ;; Cause_change_of_position_on_a_scale
began ;; Process_start || hurricane ;; Catastrophe || interact ;; Connect
storm ;; Catastrophe || storm ;; Catastrophe || regenerating ;; Recovering
traveled ;; Traveling || intensifying ;; Cause_change_of_position_on_a_scale
weakened ;; Cause_change_of_strength || dissipated ;; Expend_resource
damages ;; Damaging || leaving ;; Departing || impacted ;; Damaging
triggered ;; Causation || occurred ;; Coming_to_be || loss ;; Earnings_and_losses || damage ;; Damaging || hurricane ;; Catastrophe || flooding ;; Motion
killed ;; Killing || flood ;; Motion || inundated ;; Destroying
brought ;; Bringing || hampering ;; Hindering || disaster ;; Catastrophe
killed ;; Killing || flooding ;; Motion || making ;; Manufacturing || hurricane ;; Catastrophe || impact ;; Influence
flew ;; Motion || supplies ;; Supply || damage ;; Damaging || storm ;; Catastrophe || assist ;; Assistance
send ;; Sending || supplies ;; Supply || set up ;; Placing
allowed ;; Preventing_or_letting || prevent ;; Preventing_or_letting || spread ;; Dispersal || burning ;; Bodily_harm || recovered ;; Recovering
burned ;; Bodily_harm
Due ;; Causation || damage ;; Damaging || loss ;; Earnings_and_losses || used ;; Using || retired ;; Removing
occurred ;; Presence || flying ;; Motion || crashed ;; Catastrophe
arrived at ;; Arriving
spent ;; Expend_resource || departed ;; Departing || lost ;; Earnings_and_losses || crashed ;; Catastrophe || refuelling ;; Self_motion
killed ;; Killing || survived ;; Rescuing || rescued ;; Rescuing
take-off ;; Motion || said ;; Statement
plunged ;; Motion_directional || pushing ;; Motion
remains ;; Temporary_stay || determined ;; Deciding
attacks ;; Attack || affiliated ;; Connect || hijacked ;; Committing_crime
killing ;; Killing || building ;; Building || crashed ;; Damaging
stormed ;; Self_motion || forced ;; Causation
control ;; Control || assumed ;; Choosing
made ;; Manufacturing || relayed ;; Communication || calls ;; Communication || hijacking ;; Committing_crime
crashed ;; Damaging
began ;; Process_start || crash ;; Catastrophe || incident ;; Catastrophe || witnessed ;; Know || reporting ;; Reporting
caused ;; Causation || damaged ;; Damaging
spent ;; Expend_resource || building ;; Building || working ;; Being_in_operation || collapsed ;; Destroying || extinguish ;; Emptying
damaged ;; Damaging || rebuilt ;; Building || moving back ;; Self_motion
attack ;; Attack || crash ;; Catastrophe || Memorial ;; Social_event
contains ;; Containing
began ;; Process_start || campaign ;; Military_operation
conquering ;; Conquering || formed ;; Coming_to_be || launched ;; Military_operation || following ;; Motion
ended ;; Process_end || said ;; Telling || conquer ;; Conquering
carried ;; Carry_goods || control ;; Control || advanced ;; Cause_to_make_progress || impressed ;; Change_sentiment || governing ;; Control || gaining ;; Getting || battle ;; Hostile_encounter || Battle ;; Hostile_encounter || allowed ;; Preventing_or_letting || defeated ;; Conquering || continue ;; Motion
fought ;; Hostile_encounter || battle ;; Hostile_encounter || battle ;; Hostile_encounter
march ;; Being_in_operation || confrontation ;; Hostile_encounter || put ;; Causation
refused ;; Agree_or_refuse_to_act || mutinied ;; Change_of_leadership || exhausted ;; Expend_resource || march ;; Self_motion
meeting ;; Social_event || hearing ;; Perception_active || relented ;; Agree_or_refuse_to_act || convinced ;; Coming_to_believe || return ;; Motion
caused ;; Causation || conquering ;; Conquering || advancing ;; Cause_to_make_progress || turn ;; Motion || turning ;; Motion
died ;; Death
founded ;; Creating || death ;; Death
caused ;; Causation || flooding ;; Catastrophe || remaining ;; Presence
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe
moved ;; Motion || strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || becoming ;; Becoming
strengthening ;; Cause_change_of_strength || hurricane ;; Catastrophe || curved ;; Motion_directional || resumed ;; Recovering
approach ;; Arriving || reached ;; Achieve
passed ;; Motion_directional || remained ;; Presence
swerved ;; Motion || weakening ;; Cause_change_of_strength
stalled ;; Cause_change_of_position_on_a_scale || dissipated ;; Expend_resource
prompted ;; Causation || warnings ;; Warning
loss ;; Earnings_and_losses || tourism ;; Traveling || evacuation ;; Escaping || displaced ;; Hold
stayed ;; Temporary_stay || storm ;; Catastrophe
stayed ;; Temporary_stay || causing ;; Causation || increase ;; Cause_change_of_position_on_a_scale || went to ;; Self_motion || effects ;; Influence
evacuations ;; Escaping || forecasting ;; Scrutiny
causing ;; Causation || produced ;; Manufacturing || flooding ;; Motion
marks ;; Recording || left ;; GiveUp || inundated ;; Filling
wrecked ;; Destroying || downed ;; Self_motion
damage ;; Damaging
drowned ;; Death
hurricane ;; Catastrophe
hurricane ;; Catastrophe
moving ;; Motion || hurricane ;; Catastrophe || passed ;; Motion_directional
caused ;; Causation || hurricane ;; Catastrophe || attributed ;; Causation || produced ;; Creating
spent ;; Expend_resource || named ;; Name_conferral
given names ;; Name_conferral
hurricane ;; Catastrophe
took place ;; Process_start || surrender ;; Surrendering || resulted in ;; Causation
help ;; Assistance || capture ;; Arrest || guarding ;; Defending
began ;; Process_start || reached ;; Arriving || bombard ;; Attack
began ;; Process_start || destroy ;; Destroying || surrender ;; Surrendering
rained ;; Motion || reduced ;; Cause_change_of_position_on_a_scale
developed ;; Cause_to_make_progress
siege ;; Besieging || switch ;; Change || forced ;; Motion || lower ;; Cause_change_of_position_on_a_scale
destroyed ;; Destroying || dug ;; Motion
progressed ;; Cause_to_make_progress || attempts ;; Hold || defeated ;; Conquering
lowered ;; Cause_change_of_position_on_a_scale
began ;; Process_start || captured ;; Conquering
surrendered ;; Surrendering || assault ;; Attack
allowed ;; Preventing_or_letting || disperse ;; Dispersal
lost ;; Earnings_and_losses
expended ;; Expend_resource
died ;; Death || wounded ;; Bodily_harm
wounded ;; Bodily_harm || killed ;; Killing
destroyed ;; Destroying
freed ;; Releasing || clearing up ;; Emptying
wars ;; Hostile_encounter || targeting ;; Aiming
Festival ;; Social_event || festival ;; Social_event || held ;; Giving
events ;; Social_event
festival ;; Social_event || takes place ;; Process_start
Competition ;; Competition || races ;; Competition || triathlon ;; Competition
triathlon ;; Competition
festival ;; Social_event || attend ;; Participation
event ;; Social_event
swim ;; Self_motion || ride ;; Self_motion || run ;; Motion
triathlon ;; Competition
event ;; Social_event || competed ;; Competition
event ;; Social_event || receives ;; Receiving || funding ;; Supply
spend ;; Expend_resource || sponsorship ;; Supply || funding ;; Supply
event ;; Social_event || usage ;; Using || designed ;; Creating
began ;; Process_start || Wars ;; Hostile_encounter || incidents ;; Hostile_encounter || established ;; Creating
settlements ;; Statement || established ;; Creating
raided ;; Attack || murdered ;; Killing || dispatched ;; Sending
raid ;; Attack || patrolled ;; Patrolling || conflict ;; Hostile_encounter || killing ;; Killing || ended ;; Process_end
conducted ;; Action || raids ;; Attack
attacks ;; Attack || order ;; Arranging || shoot ;; Use_firearm || led ;; Causation || issue ;; Creating || authorised ;; Preventing_or_letting
allied ;; Collaboration || conquer ;; Conquering || war ;; Hostile_encounter || returned ;; Arriving
fought ;; Hostile_encounter || using ;; Using || battles ;; Hostile_encounter || took place ;; Process_start
engagement ;; Hostile_encounter || resulted in ;; Causation || defeat ;; Conquering
destruction ;; Destroying || expansion ;; Expansion || farming ;; Manufacturing || settlement ;; Statement || cleared ;; Emptying || resulted in ;; Causation
caused ;; Causation || violent ;; Violence || confrontations ;; Hostile_encounter || combined ;; Cause_to_be_included || resulted in ;; Causation
engagement ;; Hostile_encounter || Wars ;; Hostile_encounter
fought ;; Hostile_encounter || engagement ;; Hostile_encounter || engaged ;; Hostile_encounter || protecting ;; Defending
began ;; Process_start || War ;; Military_operation || conquest ;; Conquering || ordered ;; Request || intercept ;; Attack
patrolling ;; Patrolling || command ;; Control || spotted ;; Know
discovered ;; Know || investigate ;; Scrutiny
approaching ;; Motion_directional || turned ;; Motion || sailing ;; Self_motion || cut ;; Removing
caught ;; Connect || ship ;; Sending || ship ;; Sending || engaged ;; Hostile_encounter
pursuit ;; Aiming
compelled ;; Action || surrender ;; Surrendering || held ;; Defending
escaped ;; Escaping || reaching ;; Arriving
Wars ;; Hostile_encounter || engagement ;; Hostile_encounter
patrols ;; Patrolling || battle ;; Hostile_encounter || sail ;; Self_motion || conducting ;; Action || attempting ;; Hold || encountered ;; Hostile_encounter
battle ;; Hostile_encounter || attack ;; Attack || Closing ;; Motion_directional || attempted ;; Hold || bring ;; Bringing || turn ;; Motion
escape ;; Escaping || engaged ;; Hostile_encounter || chased ;; Self_motion
attack ;; Attack || fire ;; Use_firearm || ran ;; Self_motion
damaged ;; Damaging || burn ;; Destroying || board ;; Temporary_stay
attack ;; Attack || bring ;; Bringing || suffered ;; Bodily_harm || protect ;; Preserving
repaired ;; Resolve_problem || returned ;; Self_motion
Battle ;; Hostile_encounter || attack ;; Attack || beginning ;; Process_start || War ;; Hostile_encounter
battle ;; Hostile_encounter || fought ;; Hostile_encounter
begun ;; Process_start || marked ;; Recording || victory ;; Getting || force ;; Military_operation || marched ;; Self_motion || end ;; Process_end || joint ;; Collaboration
ended ;; Process_end || attacks ;; Attack || control ;; Control || gaining ;; Getting || victory ;; Getting || ensured ;; Preventing_or_letting || plans ;; Arranging
ended ;; Process_end || began ;; Process_start || Campaign ;; Military_operation || pursuit ;; Motion
supported ;; Supporting || reversing ;; Change || pursuing ;; Self_motion || defeat ;; Earnings_and_losses || suffered ;; Bodily_harm
attacked ;; Attack || support ;; Supporting || force ;; Military_operation
enabled ;; Preventing_or_letting || mounted ;; Connect || join ;; Participation
mounted ;; Connect || patrols ;; Patrolling || reconnaissance ;; Scrutiny || defensive ;; Defending || constructed ;; Building || carried out ;; Action
force ;; Military_operation || reported ;; Reporting
began ;; Process_start || pushing ;; Motion || battle ;; Hostile_encounter || battle ;; Hostile_encounter || advancing ;; Cause_to_make_progress || took ;; Action
launched ;; Motion || attack ;; Attack || advancing ;; Cause_to_make_progress || force ;; Military_operation
became ;; Becoming || established ;; Building
retire ;; Departing || fighting ;; Hostile_encounter
battle ;; Hostile_encounter || reinforced ;; Cause_change_of_strength || joined ;; Participation
determined ;; Deciding || direct ;; Action || managed ;; Arriving
defending ;; Defending || came ;; Arriving || entrenched ;; Cause_change_of_strength
checked ;; Scrutiny || heat ;; Motion || prevailed ;; Conquering
pushed ;; Motion || attacking ;; Attack || positions ;; Placing || starting ;; Process_start || force ;; Military_operation || maintain ;; Preserving
advancing ;; Cause_to_make_progress || force ;; Military_operation || retiring ;; Departing || pursued ;; Aiming || fought ;; Hostile_encounter
ended ;; Process_end || abandoned ;; GiveUp || force ;; Military_operation || pursuit ;; Motion || retreated ;; Motion
killings ;; Killing || took place ;; Process_start
shot ;; Use_firearm
shot ;; Use_firearm || murdered ;; Killing || relieving ;; Cure || driven ;; Motion
killed ;; Killing || killed ;; Killing || rioting ;; Violence || duty ;; Patrolling
deaths ;; Death || led to ;; Causation || mourning ;; Social_event
precipitated ;; Causation || led to ;; Causation || resignation ;; Change_of_leadership
incident ;; Killing || raised ;; Cause_change_of_position_on_a_scale
killed ;; Killing || memorial ;; Social_event
begun ;; Process_start || Operation ;; Military_operation
participating ;; Participation
provided ;; Supply || included ;; Cause_to_be_included
provided ;; Supply || guard ;; Patrolling
provided ;; Supply || built ;; Building || modified ;; Change || made ;; Manufacturing
on station ;; Being_in_operation
Deployments ;; Arranging
stops ;; Temporary_stay
begun ;; Process_start
engagement ;; Hostile_encounter || fought ;; Hostile_encounter || blockade ;; Besieging || Wars ;; Hostile_encounter
blockade ;; Besieging || dominance ;; Control || enforced ;; Action || made it ;; Causation || operate ;; Military_operation
patrol ;; Patrolling || assigned ;; Request
reach ;; Arriving || consisting ;; Cause_to_be_included || united ;; Collaboration
spotted ;; Know || chase ;; Self_motion || gave ;; Giving || patrolling ;; Patrolling
sailing ;; Self_motion || anchorage ;; Placing || took ;; Getting
blockade ;; Besieging || arrived ;; Arriving || watch ;; Scrutiny
attacked ;; Attack || defended ;; Defending || anchored ;; Connect
withdrew ;; Escaping || fire ;; Use_firearm
sent ;; Sending || attack ;; Attack
change ;; Change || recorded ;; Recording || landing ;; Arriving || becoming ;; Becoming || grounded ;; Connect || forced ;; Causation
escaped ;; Escaping || blockade ;; Besieging || refloated ;; Recovering
damaged ;; Damaging || attack ;; Attack || move ;; Self_motion || remained ;; Temporary_stay || becoming ;; Becoming || grounded ;; Connect || forced ;; Causation
entered ;; Arriving || destroying ;; Destroying || remained ;; Temporary_stay || set ;; Bodily_harm || on fire ;; Damaging
prevented ;; Preventing_or_letting || blockade ;; Besieging || launching ;; Process_start || operations ;; Military_operation
defeated ;; Conquering || Battle ;; Hostile_encounter || operating ;; Being_in_operation || attempt ;; Hold || sail ;; Self_motion || operations ;; Military_operation || undertaken ;; Action || carried out ;; Military_operation || operating ;; Being_in_operation
based ;; Presence || built ;; Building || commanded ;; Control
blockade ;; Besieging || assigned ;; Request || patrol ;; Patrolling || watch ;; Scrutiny
achieved ;; Achieve || captured ;; Conquering || seized ;; Conquering || ran ;; Self_motion || blockade ;; Besieging
airshow ;; Social_event
set ;; Placing
sets ;; Placing || fact ;; Protest
takes place ;; Process_start
produced ;; Manufacturing || joint ;; Collaboration
battle ;; Hostile_encounter || fought ;; Hostile_encounter || Wars ;; Hostile_encounter
engagement ;; Hostile_encounter || formed ;; Coming_to_be || fought ;; Hostile_encounter || prevent ;; Preventing_or_letting || resupplying ;; Supply || trapped ;; Placing || defeat ;; Conquering || Battle ;; Hostile_encounter
promised ;; Commitment || returned ;; Arriving || aid ;; Assistance || prompting ;; Influence || carrying ;; Bringing
sent ;; Sending || reached ;; Arriving || pass ;; Motion_directional || patrolling ;; Patrolling || seeking ;; Hold || avoid ;; Preventing_or_letting
discovered ;; Know || chased ;; Motion || brought ;; Causation
surrendered ;; Surrendering || dead ;; Death || wreck ;; Damaging || tore ;; Removing || packed ;; Placing || engagement ;; Hostile_encounter || lasting ;; Process_end || reduced ;; Cause_change_of_position_on_a_scale || wounded ;; Bodily_harm
brought ;; Bringing || served ;; Assistance
engagement ;; Hostile_encounter
fought ;; Hostile_encounter || force ;; Military_operation
deployed ;; Arranging || convoy ;; Patrolling || retreating ;; Escaping
destroy ;; Destroying || managed ;; Control
caused ;; Causation || death ;; Death
rescued ;; Rescuing || rescued ;; Rescuing || sent ;; Sending || attacking ;; Attack || force ;; Military_operation
escorted ;; Patrolling || reached ;; Arriving || convoy ;; Patrolling || sunk ;; Removing || landing ;; Arriving
fought ;; Hostile_encounter
surrendered ;; Surrendering || fight ;; Defending
sighted ;; Traveling || cruising ;; Patrolling
captured ;; Conquering
closed ;; Process_end
took effect ;; Change || refused ;; Agree_or_refuse_to_act || fought ;; Defending || fight ;; Defending
seemed ;; Change_sentiment
killed ;; Killing || wounded ;; Bodily_harm || wounded ;; Bodily_harm || suffered ;; Catastrophe || lost ;; Earnings_and_losses
service ;; Supply || took ;; Conquering
received ;; Receiving || command ;; Control
attacked ;; Attack || engagement ;; Hostile_encounter || Wars ;; Hostile_encounter || defeated ;; Conquering
resisted ;; Defending || engagement ;; Hostile_encounter || suffered ;; Causation || casualties ;; Bodily_harm
allow ;; Preventing_or_letting || escape ;; Escaping || held off ;; Hold
convoy ;; Patrolling || operating ;; Being_in_operation || engagement ;; Hostile_encounter || attack ;; Attack
began ;; Process_start || planned ;; Arranging || orders ;; Arranging || eliminate ;; Removing
Combined ;; Cause_to_amalgamate || attack ;; Attack || limited ;; Limiting || allowed ;; Preventing_or_letting || freedom ;; Releasing
attack ;; Attack || seize ;; Conquering || capture ;; Conquering || raiding ;; Attack || limited ;; Limiting || defence ;; Defending || required ;; Imposing_obligation
attack ;; Attack || convoy ;; Patrolling || Wars ;; Hostile_encounter || result ;; Causation
battle ;; Hostile_encounter || fought ;; Hostile_encounter || Wars ;; Hostile_encounter
supplies ;; Supply || damage ;; Damaging || Wars ;; Hostile_encounter || restrict ;; Hindering
achieve ;; Achieve || attacking ;; Attack || enter ;; Arriving || leave ;; Departing || maintained ;; Preserving
communication ;; Communication || patrolling ;; Patrolling || war ;; Hostile_encounter || faced ;; Confronting_problem || made ;; Action
deployed ;; Arranging || disrupt ;; Commitment || significant ;; Causation
required ;; Imposing_obligation || supply ;; Supply || reach ;; Arriving || made ;; Action
supplies ;; Supply || carrying ;; Carry_goods
leave ;; Departing || watched ;; Perception_active || avoid ;; Preventing_or_letting || contact ;; Connect
supported ;; Supporting || patrolling ;; Patrolling || capture ;; Conquering || ships ;; Motion || tasked ;; Request || passed ;; Motion_directional
chased ;; Motion || spotted ;; Know || emerged ;; Arriving
brought ;; Bringing || retained ;; Preserving || contact ;; Connect
casualties ;; Catastrophe || battle ;; Hostile_encounter || damage ;; Damaging || suffered ;; Catastrophe || contested ;; Competition
battle ;; Hostile_encounter
surrendered ;; Surrendering || alternative ;; Choosing
claimed ;; Statement || arrival ;; Arriving || victory ;; Getting || developed ;; Coming_to_be
reach ;; Arriving || failure ;; Earnings_and_losses
supplies ;; Supply || hampered ;; Hindering || limited ;; Limiting || operate ;; Control
massacre ;; Killing || occurred ;; Presence
riot ;; Violence || perceived ;; Perception_active || taking ;; Getting || resulting ;; Causation || massacre ;; Killing
found ;; Know || give ;; Giving || hiring ;; Employment || work ;; Employment || angering ;; Hostile_encounter
ended ;; Process_end || injured ;; Bodily_harm || dead ;; Death || rioting ;; Violence
burned ;; Bodily_harm || damage ;; Damaging || resulting in ;; Causation
violence ;; Violence
violence ;; Violence || culminating ;; Self_motion || massacre ;; Killing
suspended ;; Process_end || came to ;; Motion
ended up ;; Process_end || employed ;; Employment || took ;; Getting
increased ;; Cause_change_of_position_on_a_scale
formed ;; Coming_to_be
connection ;; Connect || linking ;; Connect || established ;; Creating
deployed ;; Arranging || rock ;; Motion || riot ;; Violence
fled ;; Escaping || escorted ;; Patrolling || riot ;; Violence
came ;; Motion
causes ;; Causation || limited ;; Limiting || support ;; Supporting || endorsed ;; Response || riot ;; Violence
violence ;; Violence || massacre ;; Killing || touched ;; Connect
resulted in ;; Causation || Battle ;; Hostile_encounter || death ;; Death || took place ;; Process_start
took place ;; Process_start
beheaded ;; Killing || patrol ;; Patrolling || pursuing ;; Aiming
killing ;; Killing || tracked ;; Recording || opened ;; Process_start
battle ;; Hostile_encounter || went ;; Motion
lost ;; Earnings_and_losses || left ;; Departing
death ;; Death || end ;; Process_end || meant ;; Statement
expedition ;; Attack || expedition ;; Attack || Wars ;; Hostile_encounter || Campaign ;; Military_operation || formed ;; Coming_to_be
allow ;; Preventing_or_letting || unite ;; Collaboration || displace ;; Destroying || invasion ;; Military_operation || cross ;; Motion_directional || land ;; Self_motion
support ;; Supporting || join ;; Participation || put ;; Motion
blockaded ;; Besieging || forces ;; Causation || put ;; Motion || extended ;; Expansion || left ;; Departing || defended ;; Defending || combining ;; Creating || cruise ;; Attack || intercept ;; Attack
achieved ;; Achieve || waiting ;; Motion || convoy ;; Patrolling || encountered ;; Hostile_encounter
convoy ;; Patrolling || interpose ;; Self_motion || captured ;; Conquering
escaped ;; Escaping || saved ;; Rescuing || attack ;; Attack || convoy ;; Patrolling || convoy ;; Patrolling || drawing ;; Causation
battle ;; Hostile_encounter || destroyed ;; Destroying || repairs ;; Recovering || forced ;; Motion || sailing ;; Motion
began ;; Process_start || capturing ;; Conquering || planned ;; Arranging || raiding ;; Attack || blockade ;; Besieging || disrupting ;; Hindering
resulting ;; Causation || campaign ;; Military_operation || Battle ;; Hostile_encounter
lost ;; Earnings_and_losses || expedition ;; Attack || returned ;; Arriving
festival ;; Social_event || take place ;; Hold
took place ;; Process_start
confirmed ;; Check || festival ;; Social_event || festival ;; Social_event
confirm ;; Check || announcement ;; Expressing_publicly || playing ;; Competition
Confirmed ;; Check
announced ;; Expressing_publicly
present ;; Presence || patrolling ;; Patrolling
conflict ;; Hostile_encounter || campaign ;; Military_operation || War ;; Military_operation || Battle ;; Hostile_encounter
directed ;; Aiming || campaign ;; Military_operation
include ;; Cause_to_be_included || extended ;; Expansion
loss ;; Earnings_and_losses || campaign ;; Military_operation || war ;; Military_operation || destructive ;; Destroying || resulted in ;; Causation
patrols ;; Patrolling || counter ;; Attack || blockades ;; Besieging || moved ;; Motion || monitored ;; Scrutiny || guarded ;; Defending
abandoned ;; GiveUp || cut off ;; Removing || campaign ;; Military_operation || supplies ;; Supply
Campaign ;; Military_operation || war ;; Military_operation || used ;; Using
losses ;; Earnings_and_losses || attack ;; Attack || escorting ;; Patrolling || called ;; Name_conferral || suffered ;; Bodily_harm
defensive ;; Defending || attack ;; Attack || sheltering ;; Placing || forcing ;; Causation
intercepted ;; Preventing_or_letting
damaged ;; Damaging || loss ;; Earnings_and_losses || shot down ;; Use_firearm
shot down ;; Use_firearm || shot down ;; Use_firearm
instructions ;; Education_teaching || attack ;; Attack || followed ;; Motion
losses ;; Earnings_and_losses || made ;; Manufacturing || ships ;; Sending || led to ;; Causation
operating ;; Being_in_operation || protect ;; Preventing_or_letting || assigned ;; Choosing
engagement ;; Hostile_encounter
raided ;; Attack || achieved ;; Achieve || capturing ;; Conquering || ships ;; Sending
ships ;; Sending || encountered ;; Hostile_encounter || carrying ;; Bringing || administered ;; Control
withdraw ;; Escaping || captured ;; Conquering || support ;; Supporting || forced ;; Control
raiding ;; Attack || disaster ;; Catastrophe || wrecked ;; Destroying
brought ;; Bringing
scattered ;; Dispersal || storm ;; Catastrophe || destroyed ;; Destroying || allowing ;; Preventing_or_letting || recapture ;; Conquering || captured ;; Conquering || patrolling ;; Patrolling || brought ;; Causation
encounter ;; Hostile_encounter
took place ;; Process_start || due ;; Causation
declined ;; Agree_or_refuse_to_act
dispersed ;; Dispersal || deployed ;; Arranging || remaining ;; Presence
united ;; Cause_to_amalgamate || sailed ;; Self_motion || convoy ;; Patrolling
sent ;; Sending || convoy ;; Patrolling || support ;; Supporting || escort ;; Patrolling
arrived ;; Arriving || arrived ;; Arriving
sailed ;; Self_motion || lost ;; Earnings_and_losses || ensued ;; Self_motion || contact ;; Connect
withdrew ;; Escaping || claimed ;; Statement || refused ;; Agree_or_refuse_to_act || escorting ;; Patrolling || convoy ;; Patrolling
conflict ;; Hostile_encounter || supporting ;; Supporting
originated ;; Coming_to_be || election ;; Choosing || lost ;; Earnings_and_losses || escalated ;; Cause_change_of_position_on_a_scale || election ;; Choosing
attacks ;; Attack || airstrikes ;; Attack
attacks ;; Attack
retained ;; Preserving || retained ;; Preserving || patrol ;; Patrolling || supervised ;; Scrutiny || blockade ;; Besieging
provides ;; Supply || controls ;; Control
remains ;; Presence
stated ;; Statement || regards ;; Participation
recognize ;; Know || recognized ;; Know
began ;; Process_start
began ;; Process_start || patrolling ;; Patrolling || Capture ;; Conquering
attack ;; Attack || end ;; Process_end || march ;; Self_motion || battle ;; Hostile_encounter || attack ;; Attack || end ;; Process_end
occupation ;; Control || established ;; Creating
ended ;; Process_end || Battle ;; Hostile_encounter
attack ;; Attack || Battle ;; Hostile_encounter || fought ;; Hostile_encounter || Battle ;; Hostile_encounter
decided ;; Deciding || strength ;; Cause_change_of_strength || stretched ;; Expansion || protect ;; Assistance || extend ;; Change_event_time
held ;; Defending || providing ;; Supply || launch ;; Military_operation || attacks ;; Attack
held ;; Defending
placed ;; Placing || shelled ;; Attack || bombed ;; Attack || strafed ;; Attack
consolidated ;; Cause_to_amalgamate || battle ;; Hostile_encounter
took place ;; Process_start || concerts ;; Social_event
raise ;; Cause_change_of_position_on_a_scale || increase ;; Cause_to_make_progress
police ;; Patrolling
conferences ;; Communication || engaged ;; Participation
became ;; Becoming || concerts ;; Social_event
took place ;; Process_start || fought ;; Hostile_encounter
village ;; Placing
formed ;; Creating
flowed ;; Motion || rose ;; Cause_change_of_position_on_a_scale
created ;; Creating
began ;; Process_start || attacks ;; Attack
began ;; Process_start || attack ;; Attack || detonated ;; Attack
enveloped ;; Besieging || withdrew ;; Escaping
patrols ;; Patrolling || captured ;; Conquering || reported ;; Reporting || occupied ;; Hold
destruction ;; Destroying || held ;; Defending || saved ;; Rescuing
began ;; Process_start || attacks ;; Attack || hurried ;; Self_motion || hold ;; Defending
suffered ;; Confronting_problem || took ;; Conquering
fought ;; Hostile_encounter || besieged ;; Besieging || Battle ;; Hostile_encounter || engagement ;; Participation || led ;; Getting || break ;; Destroying
command ;; Control
engaged ;; Participation || managed ;; Control
abandoned ;; GiveUp || fled ;; Escaping
abandoned ;; GiveUp || escorted ;; Patrolling || set fire ;; Damaging
caused ;; Causation || order ;; Arranging || relief ;; Assistance || entered ;; Arriving
gathered ;; Come_together || joining ;; Becoming_a_member || commanded ;; Control
offered ;; Giving || defeated ;; Conquering || lifted ;; Cause_change_of_position_on_a_scale
abandon ;; GiveUp || forced ;; Hostile_encounter || retreating ;; Escaping || relief ;; Assistance || face ;; Confronting_problem
claim ;; Statement
Battle ;; Hostile_encounter || fought ;; Hostile_encounter || forces ;; Military_operation
campaign ;; Military_operation || took place ;; Process_start
determined ;; Deciding || response ;; Response || counterattack ;; Attack || landings ;; Arriving || launch ;; Process_start
dispatched ;; Sending || forces ;; Military_operation || forces ;; Military_operation || landing ;; Arriving || execute ;; Action || landing ;; Arriving || drive ;; Motion
command ;; Control || landed ;; Self_motion || engaged ;; Hostile_encounter
attacks ;; Attack || losses ;; Earnings_and_losses || attackers ;; Attack || defeated ;; Conquering
attack ;; Attack || battle ;; Hostile_encounter || expand ;; Expansion || forces ;; Military_operation || forces ;; Military_operation
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || upgraded ;; Cause_to_make_progress
upgraded ;; Cause_to_make_progress || hurricane ;; Catastrophe
approaching ;; Arriving || storm ;; Catastrophe || curved ;; Motion_directional
passing ;; Motion_directional || entered ;; Arriving
became ;; Becoming || storm ;; Catastrophe || hurricane ;; Catastrophe || reaching ;; Arriving
upgraded ;; Cause_to_make_progress || hurricane ;; Catastrophe
made ;; Manufacturing || hurricane ;; Catastrophe || weakened ;; Cause_change_of_strength
storm ;; Catastrophe || weakened ;; Cause_change_of_strength || reduced ;; Cause_change_of_position_on_a_scale
centered ;; Placing || transitioned ;; Cause_change_of_position_on_a_scale
moving ;; Motion || reached ;; Arriving || dissipated ;; Dispersal
crossing ;; Motion_directional || brought ;; Bringing || damage ;; Damaging || reported ;; Reporting || flooding ;; Motion
brought ;; Bringing || storm ;; Catastrophe || reaching ;; Arriving
observed ;; Perception_active
spawned ;; Coming_to_be || damaged ;; Damaging || caused ;; Causation || impacts ;; Influence || destructive ;; Destroying || injuries ;; Bodily_harm || destroyed ;; Destroying || resulting in ;; Causation
recorded ;; Recording || remains ;; Temporary_stay
buildings ;; Building || buildings ;; Building || destroyed ;; Destroying
damage ;; Damaging || suffered ;; Bodily_harm || buildings ;; Building
losses ;; Earnings_and_losses
damage ;; Damaging || touched down ;; Arriving || causing ;; Causation || destruction ;; Destroying
damage ;; Damaging || reported ;; Reporting
attributed ;; Causation || losses ;; Earnings_and_losses || deaths ;; Death
damaged ;; Damaging || occurred ;; Coming_to_be || drowned ;; Death || flooding ;; Motion
losses ;; Earnings_and_losses || resulted in ;; Causation
took place ;; Process_start || ended ;; Process_end
entered ;; Arriving
landed ;; Arriving
occupied ;; Control || set up ;; Placing
gave way ;; Process_end
signed on ;; Sign_agreement || led to ;; Causation
departed ;; Departing || commanded ;; Control || commemorated ;; Social_event || marked ;; Recording || entered ;; Arriving
changed ;; Change
occupation ;; Control
arrival ;; Arriving
preventing ;; Preventing_or_letting || tasked ;; Request || supervising ;; Scrutiny
delivered ;; Sending || stationed ;; Placing || landed ;; Arriving
crashed ;; Catastrophe || landed ;; Arriving
encountered ;; Hostile_encounter
arresting ;; Arrest || supervising ;; Scrutiny
confirm ;; Check || deaths ;; Death || taken part ;; Dispersal
end ;; Process_end || returned ;; Arriving || disbanded ;; Removing
operated ;; Being_in_operation || crashed ;; Catastrophe
operating ;; Being_in_operation || renamed ;; Name_conferral || stopover ;; Temporary_stay
operated ;; Being_in_operation
touched down ;; Arriving || flipped ;; Motion || typhoon ;; Catastrophe || landing ;; Self_motion
killed ;; Killing || survived ;; Preserving
operate ;; Being_in_operation || originating ;; Coming_to_be
ended ;; Process_end || service ;; Supply
flown ;; Being_in_operation
losses ;; Earnings_and_losses || crashed ;; Catastrophe
losses ;; Earnings_and_losses || serving ;; Supply
engagement ;; Hostile_encounter
conducted ;; Action
became ;; Becoming || landed ;; Arriving
attacks ;; Attack || recapture ;; Recovering
conduct ;; Action || gathered ;; Come_together
attack ;; Attack || indication ;; Communication || reported ;; Reporting
began ;; Process_start || directed ;; Aiming
caused ;; Causation || casualties ;; Catastrophe || failed ;; Confronting_problem || caught ;; Body_movement
arrived ;; Arriving || attack ;; Attack || Expecting ;; Coming_to_believe || reinforce ;; Cause_change_of_strength || materialised ;; Coming_to_be
declared ;; Expressing_publicly || collect ;; Come_together || dead ;; Death || bury ;; Filling
capturing ;; Conquering || end ;; Process_end || evacuated ;; Escaping
dispute ;; Quarreling || led to ;; Causation || confrontation ;; Hostile_encounter || issue ;; Creating
existed ;; Presence || beginning ;; Process_start
surrender ;; Surrendering || led to ;; Causation
feared ;; Change_sentiment
end ;; Process_end
took place ;; Process_start
surrender ;; Surrendering || lost ;; Earnings_and_losses
end ;; Process_end || support ;; Supporting || led to ;; Causation
beginning ;; Process_start || established ;; Building
began ;; Process_start || lost ;; Earnings_and_losses
sparked ;; Causation
reached ;; Arriving
abandon ;; GiveUp
landed ;; Arriving
organized ;; Arranging || armed ;; Bearing_arms || reached ;; Arriving || end ;; Process_end || took place ;; Process_start
evacuated ;; Emptying || began ;; Process_start || targeting ;; Aiming
became ;; Becoming || occurred ;; Coming_to_be
become ;; Becoming || drove ;; Self_motion || bringing ;; Bringing
determined ;; Deciding || supported ;; Supporting || blockade ;; Besieging || established ;; Building
unified ;; Cause_to_amalgamate || resignation ;; Change_of_leadership
joined ;; Becoming_a_member
provided ;; Supply
defeated ;; Conquering || followed ;; Self_motion
land ;; Arriving || flying ;; Self_motion || crashed ;; Catastrophe
died ;; Death
injured ;; Bodily_harm || survived ;; Rescuing
published ;; Publishing || report ;; Reporting
stated ;; Statement || loss ;; Earnings_and_losses || cause ;; Causation
checking ;; Scrutiny || report ;; Reporting || command ;; Control || operation ;; Arranging || pairing ;; Cause_to_amalgamate || charter ;; Renting
end ;; Process_end || war ;; Hostile_encounter
invaded ;; Attack || beginning ;; Process_start
surrender ;; Surrendering
losses ;; Earnings_and_losses || resulted in ;; Causation || defeat ;; Conquering
campaigns ;; Military_operation || claimed ;; Statement
contact ;; Connect || came ;; Self_motion || cut off ;; Removing
causes ;; Causation || besieged ;; Besieging || deaths ;; Death || claim ;; Statement || denied ;; Agree_or_refuse_to_act
fought ;; Hostile_encounter
support ;; Supporting
commenced ;; Process_start || concluded ;; Process_end
selected ;; Choosing || create ;; Creating || intimate ;; Connect || took place ;; Process_start
support ;; Supporting || served ;; Assistance
sale ;; Commerce_sell
added ;; Cause_to_be_included || shows ;; Social_event
operating ;; Being_in_operation || stop ;; Arriving
developed ;; Coming_to_be || filling ;; Filling || operating ;; Being_in_operation || spread ;; Dispersal
forcing ;; Causation || burned ;; Damaging || disabled ;; Destroying || divert ;; Motion
opened ;; Openness || became ;; Becoming || created ;; Creating || engulfed ;; Surrounding || killing ;; Killing || evacuate ;; Escaping || landed ;; Arriving
became ;; Becoming || accident ;; Catastrophe || accident ;; Catastrophe || make ;; Causation || changed ;; Change
became ;; Becoming || equipment ;; Supply || strip ;; Emptying || marking ;; Recording || increased ;; Cause_change_of_position_on_a_scale || training ;; Education_teaching || evacuation ;; Escaping || updated ;; Cause_to_make_progress
become ;; Becoming || assist ;; Assistance || manufacturers ;; Manufacturing || accident ;; Catastrophe || evacuation ;; Escaping || prove ;; Convincing || evacuated ;; Escaping || instructed ;; Request
wrestling ;; Bodily_harm || took place ;; Process_start || produced ;; Manufacturing
given ;; Giving || vote ;; Choosing
started ;; Process_start || ended ;; Process_end || voting ;; Choosing
wrestling ;; Competition
planned ;; Arranging || took place ;; Process_start
assigned ;; Choosing || expansion ;; Expansion
match ;; Competition || involving ;; Participation
retain ;; Preserving || match ;; Competition || defeated ;; Conquering
fought ;; Hostile_encounter || escaping ;; Escaping || match ;; Competition || wrestling ;; Competition || touch ;; Connect || defeated ;; Conquering
match ;; Competition
defeated ;; Conquering
received ;; Receiving
wrestling ;; Competition
invasion ;; Attack || capture ;; Conquering || took place ;; Process_start || landed ;; Arriving
due ;; Causation || fire ;; Use_firearm || landing ;; Arriving
siege ;; Besieging || capitulation ;; Surrendering || led to ;; Causation
decided ;; Deciding || attacking ;; Attack
decided ;; Deciding || attack ;; Attack || bombardment ;; Attack || defences ;; Defending
surrendered ;; Surrendering || expedition ;; Attack
captured ;; Conquering
crashed ;; Catastrophe
approach ;; Arriving || hit ;; Motion || occurred ;; Coming_to_be
due ;; Causation || fire ;; Catastrophe || landing ;; Arriving
killed ;; Killing
remains ;; Presence || Flight ;; Motion || Flight ;; Motion || crashes ;; Catastrophe || took place ;; Process_start
determined ;; Deciding || report ;; Reporting || crash ;; Catastrophe
crash ;; Catastrophe || occurred ;; Presence || flight ;; Self_motion || approach ;; Self_motion || landing ;; Arriving || struck ;; Connect
departed ;; Departing || cleared ;; Emptying
asked ;; Request || passing ;; Motion_directional || report ;; Reporting
maintaining ;; Preserving || reported ;; Reporting
asked ;; Request || passed ;; Motion_directional
reported ;; Reporting
struck ;; Connect
campaign ;; Military_operation
death ;; Death || defeat ;; Earnings_and_losses || landing ;; Arriving
conflict ;; Hostile_encounter
decided ;; Deciding || expand ;; Expansion || sending ;; Sending || invade ;; Attack
send ;; Sending || crown ;; Change_of_leadership || return ;; Arriving || invited ;; Request || drive ;; Motion
fled ;; Escaping || fighting ;; Hostile_encounter || expedition ;; Attack
murder ;; Killing || capturing ;; Conquering || set off ;; Causation || finish ;; Process_end
campaign ;; Military_operation || death ;; Death || ended ;; Process_end || defeat ;; Earnings_and_losses
recorded ;; Recording || death ;; Death
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe
organized ;; Arranging || remaining ;; Temporary_stay || attain ;; Getting || storm ;; Catastrophe
moving ;; Motion || reach ;; Arriving || strengthen ;; Cause_change_of_strength || passing ;; Motion_directional || reached ;; Arriving
struck ;; Connect || turned ;; Becoming || drifted ;; Self_motion || turning ;; Becoming
becoming ;; Becoming || passed ;; Motion_directional || accelerated ;; Cause_change_of_position_on_a_scale
Due ;; Causation || movement ;; Causation
casualties ;; Catastrophe
took place ;; Process_start || battle ;; Hostile_encounter
name ;; Name_conferral
damaged ;; Damaging
reach ;; Arriving
turned on ;; Military_operation || cover ;; Filling
searched ;; Scrutiny || lost ;; Earnings_and_losses || contact ;; Connect || returning ;; Arriving || headed ;; Self_motion
send ;; Sending || lost ;; Earnings_and_losses || broke ;; Destroying
allowed ;; Preventing_or_letting || dispatched ;; Sending || hunt ;; Scrutiny
rediscovered ;; Know
consisted ;; Cause_to_be_included
preventing ;; Preventing_or_letting || consisted ;; Cause_to_be_included || disabled ;; Destroying
damage ;; Damaging
attack ;; Attack || supported ;; Supporting
sunk ;; Motion_directional
damaged ;; Damaging
rescued ;; Rescuing || leaving ;; Departing || withdraw ;; Escaping
rescued ;; Rescuing
attacked ;; Attack || loss ;; Earnings_and_losses || resulting in ;; Causation
incident ;; Catastrophe || stops in ;; Arriving
flown ;; Motion
thrown up ;; Motion || flew ;; Motion || eruption ;; Catastrophe || resulting in ;; Causation
diverted ;; Motion || allow ;; Preventing_or_letting || hope ;; Coming_to_believe || land ;; Arriving || restarted ;; Resolve_problem
glided ;; Motion || allowing ;; Preventing_or_letting || failed ;; Confronting_problem || restarted ;; Resolve_problem || land ;; Self_motion
began ;; Process_start || accident ;; Catastrophe || boarded ;; Connect
battle ;; Hostile_encounter || battle ;; Hostile_encounter || unification ;; Cause_to_amalgamate
died ;; Death
take ;; Bringing || surrounding ;; Surrounding || perceived ;; Know
gathering ;; Come_together || forces ;; Causation || strike ;; Attack
attack ;; Attack || advance ;; Self_motion || cross ;; Self_motion || split ;; Destroying
approaching ;; Self_motion || raised ;; Cause_change_of_position_on_a_scale || marched ;; Self_motion || advancing ;; Cause_to_make_progress
destroyed ;; Destroying || fell ;; Motion_directional
fought ;; Hostile_encounter || taken ;; Conquering
forces ;; Causation || battle ;; Hostile_encounter || face ;; Confronting_problem || turned ;; Change || landed ;; Arriving
retreat ;; Escaping || destroyed ;; Destroying
accident ;; Catastrophe || took place ;; Process_start
dead ;; Death || derailed ;; Motion_directional || leaving ;; Causation || injured ;; Bodily_harm
crash ;; Catastrophe || visited ;; Forming_relationships || account ;; Resolve_problem || pledging ;; Commitment
parked ;; Placing || jumped ;; Motion_directional || said ;; Statement || crashed ;; Damaging
reports ;; Reporting || fix ;; Resolve_problem || carrying ;; Sending || stop ;; Process_end
killed ;; Killing || collision ;; Catastrophe
diving ;; Self_motion || involving ;; Cause_to_be_included
falling ;; Motion_directional || parachuting ;; Self_motion || flew ;; Motion
leaving ;; Departing || jump ;; Self_motion || landing ;; Arriving
deployed ;; Arranging
becoming ;; Becoming || Reaching ;; Arriving || form ;; Coming_to_be || broke ;; Destroying
records ;; Recording || broke ;; Destroying
record ;; Recording || broke ;; Destroying || set ;; Placing
record ;; Recording || set ;; Placing || control ;; Control || broke ;; Destroying
verified ;; Check || claims ;; Statement
crashed ;; Catastrophe
killed ;; Killing
began ;; Process_start || scheduled ;; Arranging || stop ;; Self_motion || travelling ;; Self_motion
cleared ;; Emptying || arrived at ;; Arriving || left ;; Departing
noticed ;; Know || reached ;; Arriving
began ;; Process_start || began ;; Process_start || drop ;; Motion_directional
increased ;; Cause_change_of_position_on_a_scale || picked up ;; Choosing || believed ;; Know
bank ;; Motion || climbed ;; Self_motion || continued ;; Process_start || reaching ;; Self_motion
running ;; Self_motion || struck ;; Connect || cartwheeled ;; Self_motion
falling ;; Motion_directional || slammed ;; Motion || broke ;; Destroying || pancaked ;; Self_motion || skidded ;; Self_motion || launched into ;; Motion
crash ;; Catastrophe
determined ;; Deciding || connecting ;; Connect
caused ;; Causation || prevented ;; Preventing_or_letting || put ;; Placing || correct ;; Resolve_problem
removed ;; Removing || attaching ;; Connect || hooked ;; Connect || replaced ;; Change
separated ;; Forming_relationships
crashed ;; Catastrophe || killing ;; Killing || landing ;; Arriving
broke ;; Destroying || struck ;; Attack
killed ;; Killing || survived ;; Rescuing
injuries ;; Bodily_harm || reported ;; Reporting
crashed ;; Catastrophe
accident ;; Catastrophe
Investigation ;; Scrutiny || ongoing ;; Criminal_investigation
performed ;; Action
captured ;; Conquering
fled ;; Escaping
retake ;; Conquering || return ;; Arriving
landed ;; Arriving
fled ;; Escaping || regrouped ;; Cause_to_amalgamate
fall ;; Motion || forced ;; Request || drove ;; Motion
fortified ;; Cause_change_of_strength
take off ;; Departing || crashed ;; Damaging
operated ;; Being_in_operation
killed ;; Killing || survived ;; Rescuing
slammed ;; Motion || surpassed ;; Cause_change_of_position_on_a_scale
took place ;; Process_start || crashed ;; Damaging
assisted ;; Assistance || concluded ;; Coming_to_believe
caused ;; Causation
cited ;; Judgment_communication
fog ;; Catastrophe || crash-landed ;; Self_motion || land ;; Arriving
flight ;; Motion || ferry ;; Motion
accident ;; Catastrophe || survived ;; Preserving
injuries ;; Bodily_harm || injured ;; Bodily_harm || taken ;; Bringing || suffered ;; Bodily_harm
accident ;; Catastrophe || loss ;; Earnings_and_losses || loss ;; Earnings_and_losses
accident ;; Catastrophe || cause ;; Causation || determined ;; Deciding
launched ;; Military_operation
began ;; Process_start || triggering ;; Causation || landed ;; Arriving
ended ;; Process_end || defeat ;; Conquering
altering ;; Change || pronounced ;; Statement || reaction ;; Response || conflict ;; Hostile_encounter || claimed ;; Statement
destruction ;; Destroying
fighting ;; Hostile_encounter || laid ;; Causation
killed ;; Killing
race ;; Competition
recorded ;; Recording
killed ;; Killing || crashed ;; Catastrophe || collision ;; Catastrophe || contact ;; Connect || retaining ;; Preserving
pronounced ;; Statement || death ;; Death || died ;; Death || impact ;; Influence
crash ;; Catastrophe
killed ;; Killing
publicized ;; Publishing || death ;; Death || improvements ;; Cause_to_make_progress || seen ;; Perception_active
began ;; Process_start || use ;; Using || escape ;; Escaping || led to ;; Causation || seen ;; Perception_active || set ;; Placing || develop ;; Coming_to_be
death ;; Death || died ;; Death
earthquake ;; Catastrophe || earthquake ;; Catastrophe || earthquake ;; Catastrophe || struck ;; Attack
earthquake ;; Catastrophe || centred ;; Placing
earthquake ;; Catastrophe || caused ;; Causation || increased ;; Cause_change_of_position_on_a_scale || earthquake ;; Catastrophe || felt ;; Perception_active || centered ;; Placing || arising ;; Causation
earthquake ;; Catastrophe || earthquake ;; Catastrophe || aftershock ;; Catastrophe || thought ;; Coming_to_believe || identified ;; Check
tsunamis ;; Catastrophe || pointed out ;; Reporting || waves ;; Motion || seen ;; Perception_active
dead ;; Death || reported ;; Reporting
causing ;; Causation || followed ;; Motion || towns ;; Placing || aftershocks ;; Catastrophe
destructive ;; Destroying || earthquake ;; Catastrophe
damaged ;; Damaging || earthquake ;; Catastrophe || city ;; Building || hosts ;; Connect
damaged ;; Damaging
damaged ;; Damaging || declare ;; Expressing_publicly || leading ;; Causation || catastrophe ;; Catastrophe
disaster ;; Catastrophe
devastating ;; Destroying || causing ;; Causation || collided ;; Motion || explosion ;; Catastrophe
killed ;; Killing || injured ;; Bodily_harm
explosion ;; Catastrophe || releasing ;; Causation
orders ;; Arranging
collided ;; Motion || chartered ;; Renting
damaged ;; Damaging || collision ;; Catastrophe || impact ;; Influence || leaking ;; Motion || ignited ;; Process_start || fire ;; Catastrophe
exploded ;; Attack
obliterated ;; Destroying
snapped ;; Destroying || demolished ;; Destroying || scattered ;; Dispersal || bent ;; Change || grounded ;; Connect
damage ;; Damaging
tsunami ;; Catastrophe || wiped ;; Destroying
began ;; Process_start || became ;; Becoming
began ;; Process_start || impeded ;; Hindering || arriving ;; Arriving
began ;; Process_start
found ;; Know || inquiry ;; Criminal_investigation || disaster ;; Catastrophe || blame ;; Judgment_communication || appeal ;; Protest || determined ;; Coming_to_believe
explosion ;; Catastrophe || memorials ;; Social_event
landing ;; Arriving || struck ;; Attack
destroying ;; Destroying || killing ;; Killing || ripped ;; Destroying
created ;; Creating || engulfed ;; Destroying
coming ;; Arriving || spun ;; Self_motion
initiated ;; Process_start
injured ;; Bodily_harm
controlled ;; Control || determined ;; Deciding
directed ;; Aiming
initiate ;; Process_start || handed ;; Motion
contacted ;; Communication || land ;; Arriving
handed ;; Giving || made ;; Creating
advised ;; Convincing
present ;; Presence || advised ;; Convincing
observed ;; Perception_active
entered ;; Arriving || touchdown ;; Motion || struck ;; Attack
concluded ;; Coming_to_believe
destroyed ;; Destroying || returned ;; Arriving
acquired ;; Getting || retired ;; Removing
tournament ;; Competition || played ;; Competition
Tour ;; Traveling
compensation ;; Commerce_pay || took place ;; Process_start
events ;; Social_event || televised ;; Communication
performed ;; Hold
tournaments ;; Competition || tournament ;; Competition || participants ;; Participation
defeated ;; Conquering || grew ;; Expansion
win ;; Getting
win ;; Getting || join ;; Becoming_a_member
tournament ;; Competition || tournament ;; Competition || marked ;; Recording || held ;; Creating
began ;; Process_start
hit ;; Attack
began ;; Process_start || slashed ;; Removing
called ;; Causation
demand ;; Request || return ;; Giving
struck ;; Attack
work ;; Employment
restore ;; Recovering
refused ;; Agree_or_refuse_to_act || adjusted ;; Change
erupted ;; Process_start
wounding ;; Bodily_harm || attack ;; Attack || armed ;; Connect || held off ;; Hold || killing ;; Killing
erupted ;; Process_start
wounded ;; Bodily_harm || forced ;; Motion || ran out of ;; Expend_resource || flee ;; Escaping
remained ;; Presence || raised ;; Causation
armed ;; Connect || clashed ;; Hostile_encounter
protect ;; Defending || mobilized ;; Influence || joined ;; Cause_to_amalgamate
hold ;; Hold || deepened ;; Cause_change_of_strength
returned ;; Departing
shattered ;; Preventing_or_letting
wrote ;; Statement || exist ;; Presence || declaring ;; Expressing_publicly
paying ;; Commerce_pay || ceased ;; Process_end || suspended ;; Change_event_time
turn ;; Change
game ;; Competition
defeated ;; Conquering
played ;; Competition
suspending ;; Process_end
finished ;; Process_end || named ;; Name_conferral || record ;; Recording
began ;; Process_start || defeated ;; Conquering || advanced ;; Cause_to_make_progress
bolstered ;; Supporting || finished ;; Process_end
defeated ;; Conquering || made ;; Creating
advanced ;; Cause_to_make_progress || broken ;; Destroying
took ;; Getting
recorded ;; Recording
recording ;; Recording || kept ;; Temporary_stay || forcing ;; Causation
named ;; Name_conferral
announced ;; Expressing_publicly
charged ;; Commerce_pay
occurred ;; Coming_to_be || marched ;; Self_motion
support ;; Supporting
refused ;; Agree_or_refuse_to_act || pay ;; Commerce_pay || demanded ;; Request || shut ;; Motion
marched ;; Self_motion
escaped ;; Escaping || capitulated ;; Surrendering || siege ;; Besieging || established ;; Building
agreed ;; Agree_or_refuse_to_act || increase ;; Cause_change_of_position_on_a_scale || paid ;; Commerce_pay || threats ;; Warning || harm ;; Bodily_harm
allow ;; Preventing_or_letting || happen ;; Presence
saw ;; Motion
built ;; Building || commanded ;; Control
replaced ;; Change_of_leadership
sending ;; Sending || fleeing ;; Escaping || started ;; Process_start || evacuated ;; Escaping
advanced ;; Cause_to_make_progress
called ;; Name_conferral || delayed ;; Change_event_time || owing ;; Commerce_pay
killed ;; Killing || wounded ;; Bodily_harm || damaged ;; Damaging
attacked ;; Attack || apologized ;; Judgment_communication
conducted ;; Action
rejected ;; Agree_or_refuse_to_act || maintain ;; Preserving
paid ;; Commerce_pay
paid ;; Commerce_pay
agreed ;; Agree_or_refuse_to_act
engagement ;; Hostile_encounter || Wars ;; Hostile_encounter
Wars ;; Hostile_encounter || engagements ;; Hostile_encounter || deployed ;; Arranging || operating ;; Control || lure ;; Hold
began ;; Process_start || sighted ;; Hold || chasing ;; Motion
proved ;; Statement || sighted ;; Know
escaping ;; Escaping || open ;; Openness || prevented ;; Preventing_or_letting || winds ;; Motion
resisted ;; Defending || received ;; Receiving || attack ;; Attack || causing ;; Causation || damage ;; Damaging
paid ;; Commerce_pay || prompted ;; Cause_change_of_position_on_a_scale || action ;; Response || service ;; Supply || taken ;; Conquering
storm ;; Catastrophe || storm ;; Catastrophe || storm ;; Catastrophe || known ;; Know || affected ;; Influence
named ;; Name_conferral
storm ;; Catastrophe
brought ;; Bringing || triggering ;; Causation || knocking out ;; Hindering
canceled ;; Change_event_time || delayed ;; Change_event_time || diverted ;; Removing
leaving ;; Departing || surrounding ;; Surrounding || leaving ;; Departing || stopped ;; Process_end || stranded ;; Motion
destroyed ;; Destroying || damaged ;; Damaging || costing ;; Commerce_pay
killed ;; Killing
losses ;; Earnings_and_losses
began ;; Process_start
began ;; Process_start || inspired ;; Influence
drew ;; Motion
set up ;; Building
gave ;; Giving
given in ;; Giving || costs ;; Cost
pay ;; Commerce_pay || reported ;; Reporting
dismantled ;; Destroying
acts ;; Action
shut ;; Body_movement || led ;; Causation
engage ;; Participation
organize ;; Arranging || support ;; Supporting
Society ;; Social_event
began ;; Process_start || gathered ;; Come_together || beginning ;; Process_start || discuss ;; Communication
developed ;; Coming_to_be || morphed ;; Change || took place ;; Process_start
sponsored ;; Supply
criticized ;; Judgment_communication || pay ;; Commerce_pay || known ;; Know || gain ;; Getting
fair ;; Social_event
run ;; Being_in_operation
consists ;; Cause_to_be_included
provided ;; Supply
charged ;; Commerce_pay
Capture ;; Conquering || took place ;; Process_start
surrendered ;; Surrendering || composed ;; Come_together || commanded ;; Control
surrender ;; Surrendering || pay ;; Commerce_pay || arrive ;; Arriving || mutinied ;; Change_of_leadership
surrender ;; Surrendering || accused ;; Judgment_communication || treason ;; Committing_crime || charges ;; Judgment_communication
sent ;; Sending || besiege ;; Besieging
capitulated ;; Surrendering
recaptured ;; Conquering || command ;; Control
wildfire ;; Catastrophe || burned ;; Destroying
pushed ;; Motion || conflagration ;; Catastrophe || fire ;; Catastrophe || originated ;; Coming_to_be || designed ;; Creating
fire ;; Catastrophe || forced ;; Causation || evacuation ;; Escaping || displacement ;; Escaping
killed ;; Killing || crashed ;; Catastrophe || fires ;; Catastrophe || casualties ;; Bodily_harm || reported ;; Reporting || battling ;; Hostile_encounter
destroyed ;; Destroying || destroyed ;; Destroying || damaged ;; Damaging || destroyed ;; Destroying || damaged ;; Damaging || fire ;; Catastrophe
leaving ;; Causation
fire ;; Catastrophe || gutted ;; Destroying
disaster ;; Catastrophe || damage ;; Damaging || estimated ;; Statement || making it ;; Causation
fire ;; Catastrophe || concluded ;; Coming_to_believe || arson ;; Committing_crime || arrests ;; Arrest || made ;; Action || cause ;; Causation
prompted ;; Causation || disaster ;; Catastrophe || support ;; Supporting
donations ;; Giving || asked ;; Request || inundated ;; Filling
offered ;; Giving || offered ;; Giving || payments ;; Commerce_pay || defer ;; Change_event_time || affected ;; Influence || displaced ;; Motion
promised ;; Commitment || aid ;; Assistance
took place ;; Process_start
led ;; Conquering
guaranteed ;; Commitment
paid ;; Commerce_pay || raided ;; Attack
cut off ;; Killing || pulled ;; Motion
terrorist ;; Terrorism || attack ;; Terrorism || took place ;; Process_start
bombings ;; Terrorism || hit ;; Attack
killed ;; Killing
injured ;; Bodily_harm
paying ;; Commerce_pay || checked ;; Scrutiny
attack ;; Terrorism || suicide bombings ;; Terrorism || came ;; Motion
fought ;; Hostile_encounter
pay ;; Commerce_pay || demanding ;; Request || seizing ;; Committing_crime || holding ;; Hold
pay ;; Commerce_pay || refused ;; Agree_or_refuse_to_act
disaster ;; Catastrophe || collapse ;; Catastrophe
overlaid ;; Filling || created ;; Causation
caused ;; Causation || killing ;; Killing || engulfed ;; Destroying || slide ;; Motion_directional || led to ;; Causation
blame ;; Judgment_communication || named ;; Name_conferral || disaster ;; Catastrophe || inquiry ;; Scrutiny || placed ;; Placing
begun ;; Process_start || disaster ;; Catastrophe || slipped ;; Motion_directional
emerged ;; Coming_to_be
saturated ;; Filling || slipped ;; Motion_directional
begun ;; Process_start || killed ;; Killing || hit ;; Attack
inquiry ;; Scrutiny || chaired ;; Hold
blame ;; Judgment_communication || report ;; Reporting || placed ;; Placing
statements ;; Statement || criticised ;; Judgment_communication || presence ;; Presence || making ;; Creating || providing ;; Giving
prosecuted ;; Judgment_communication || fined ;; Judgment_communication
disaster ;; Catastrophe || set up ;; Reforming_a_system
received ;; Receiving
removed ;; Removing || cost ;; Cost || fight ;; Defending || resistance ;; Defending
paid ;; Commerce_pay
paid back ;; Commerce_pay || donated ;; Giving || recompense ;; Rewards_and_punishments
suffered ;; Bodily_harm || experienced ;; Perception_active
takes place ;; Process_start
gets ;; Getting
called ;; Name_conferral
organizing ;; Arranging || paying ;; Commerce_pay || visitors ;; Traveling || beginning ;; Process_start
took place ;; Process_start || sold ;; Commerce_sell
target ;; Aiming || festival ;; Social_event
undertaken ;; Action || incorporated ;; Cause_to_amalgamate
formed ;; Coming_to_be
referred ;; Name_conferral || inhabited ;; Placing
paid ;; Commerce_pay || centred ;; Placing || incorporated ;; Cause_to_amalgamate
arrived in ;; Arriving || overthrew ;; Change_of_leadership
crossed ;; Motion_directional || scouted ;; Scrutiny
established ;; Creating
extended ;; Expansion
established ;; Creating
existing ;; Presence || established ;; Creating
caused ;; Causation
abandoned ;; GiveUp || fled ;; Escaping || launched ;; Process_start
led to ;; Causation || allowed ;; Preventing_or_letting || gained ;; Getting || promoted ;; Cause_to_make_progress
Games ;; Competition || Games ;; Competition || Games ;; Competition || hosted ;; Social_event
hosted ;; Social_event || games ;; Competition
hosted ;; Social_event || games ;; Competition
host ;; Social_event || Games ;; Competition || due ;; Causation || construction ;; Building || delays ;; Change_event_time || awarded ;; Award
commenced ;; Process_start || games ;; Competition || held ;; Hold
participated ;; Participation
opened ;; Process_start
host ;; Social_event || led ;; Bringing || followed ;; Motion
Games ;; Competition || games ;; Competition || records ;; Recording || broken ;; Destroying
cost ;; Cost || competition ;; Competition || games ;; Competition || games ;; Competition || games ;; Competition || host ;; Social_event || rising ;; Cause_change_of_position_on_a_scale || spent ;; Commerce_pay
marketed ;; Commerce_sell || recording ;; Recording || Tour ;; Traveling || Tour ;; Traveling || tour ;; Traveling
began ;; Process_start || final ;; Process_end || show ;; Social_event || scheduled ;; Arranging
utilized ;; Using || tour ;; Traveling || campaign ;; Social_event
signing ;; Sign_agreement || tour ;; Traveling || tour ;; Traveling || tour ;; Traveling
support ;; Supporting || acts ;; Action || tour ;; Traveling || opening ;; Openness || extended ;; Change_event_time
performed ;; Hold
performed ;; Hold
scrapped ;; GiveUp || support ;; Supporting || tour ;; Traveling
highest-grossing ;; Commerce_pay || tour ;; Traveling || tours ;; Traveling || making it ;; Causation
took place ;; Process_start
marked ;; Recording || set ;; Placing
freedom ;; Change_of_leadership || served ;; Assistance || commemorate ;; Social_event
expressed ;; Expressing_publicly || renewal ;; Recovering
mentioned ;; Statement
starting ;; Process_start || paid ;; Commerce_pay
held ;; Defending || included ;; Cause_to_be_included
administered ;; Control || led to ;; Causation
increased ;; Cause_change_of_position_on_a_scale || encourage ;; Influence
opened ;; Openness || breaking ;; Destroying || viewing ;; Perception_active
participated ;; Participation || organized ;; Arranging || afford ;; Commerce_pay || attend ;; Participation
hosted ;; Social_event
occurred ;; Presence || destroying ;; Destroying
took place ;; Process_start
rescued ;; Rescuing || destroyed ;; Destroying || killed ;; Killing
wounded ;; Bodily_harm
complicated ;; Hindering
determined ;; Deciding || killed ;; Killing || found ;; Know
counted ;; Judgment_communication
cause ;; Causation
suspect ;; Statement || cause ;; Causation || initiated ;; Process_start
receive ;; Receiving || destroyed ;; Destroying || arranged ;; Arranging
received ;; Receiving || affected ;; Influence
received ;; Receiving || destroyed ;; Destroying || supported ;; Supporting
given ;; Giving || pay ;; Commerce_pay
Battle ;; Hostile_encounter || defeated ;; Conquering
disputed ;; Quarreling || battle ;; Hostile_encounter || took place ;; Process_start
campaigned ;; Military_operation || documented ;; Recording
alliance ;; Cause_to_amalgamate || payment ;; Commerce_pay || gains ;; Earnings_and_losses || settled ;; Resolve_problem
battle ;; Hostile_encounter || possession ;; Control || determine ;; Deciding
participated ;; Participation || battle ;; Hostile_encounter || battle ;; Hostile_encounter || described ;; Statement || Records ;; Recording || mentioned ;; Statement
called ;; Request || conflicts ;; Hostile_encounter || fought ;; Hostile_encounter
fought ;; Hostile_encounter
came ;; Motion || fought ;; Hostile_encounter || wars ;; Hostile_encounter || causes ;; Causation || outbreak ;; Attack || rise ;; Cause_change_of_position_on_a_scale || opposed ;; Agree_or_refuse_to_act || coupled ;; Cause_to_amalgamate || called ;; Name_conferral
arrived ;; Arriving
killing ;; Killing || sparked ;; Causation || violence ;; Violence || killers ;; Killing || hunt ;; Scrutiny || launched ;; Motion
wars ;; Hostile_encounter || Rising ;; Expansion || led to ;; Causation || armed ;; Bearing_arms || exterminate ;; Destroying
killed ;; Killing || included ;; Cause_to_be_included || conflicts ;; Hostile_encounter || siege ;; Besieging || attack ;; Attack
given ;; Giving || confiscated ;; Removing || break down ;; Destroying || sanctuary ;; Preserving || claiming ;; Statement || confiscated ;; Removing || party ;; Participation || deemed ;; Coming_to_believe || threat ;; Warning
paid ;; Commerce_pay || attacks ;; Attack || expressed ;; Expressing_publicly || apologised ;; Judgment_communication
party ;; Participation
paying ;; Commerce_pay || allowed ;; Preventing_or_letting || sell ;; Commerce_sell || Act ;; Action || imposed ;; Action || target ;; Aiming
Act ;; Action || violation ;; Legality || opposed ;; Agree_or_refuse_to_act
sent ;; Sending || destroyed ;; Destroying
boarded ;; Self_motion || threw ;; Self_motion
responded ;; Response || revolution ;; Change_of_leadership || escalated ;; Cause_change_of_strength
became ;; Becoming || referred ;; Name_conferral || party ;; Participation || party ;; Participation || protest ;; Judgment_communication || protests ;; Judgment_communication
party ;; Participation || resistance ;; Defending || act ;; Action || passed ;; Motion_directional
taxed ;; Commerce_pay || elected ;; Choosing || believed ;; Know || objected ;; Causation
granted ;; Getting || resented ;; Judgment_communication || move ;; Motion
refused ;; Agree_or_refuse_to_act || prevented ;; Preventing_or_letting || unloading ;; Placing || allow ;; Preventing_or_letting || returned ;; Arriving
revolution ;; Change_of_leadership || party ;; Participation
responded ;; Response || ended ;; Process_end || closed ;; Process_end
responded ;; Response || resistance ;; Defending || repeal ;; Process_end || acts ;; Action || acts ;; Action
began ;; Process_start || escalated ;; Cause_change_of_strength || crisis ;; Catastrophe || War ;; Military_operation
wars ;; Hostile_encounter || Wars ;; Hostile_encounter || conflicts ;; Violence || culminating ;; Process_end || fought ;; Hostile_encounter
war ;; Hostile_encounter || joined ;; Participation
demanded ;; Request
pay ;; Commerce_pay || attack ;; Attack || hold ;; Hold || enslave ;; Conquering || take ;; Removing
pay ;; Commerce_pay || became ;; Becoming || refused ;; Agree_or_refuse_to_act || sent ;; Sending
bombarded ;; Attack || fleet ;; Motion || extracting ;; Getting
war ;; Hostile_encounter || War ;; Hostile_encounter || conclusion ;; Process_end || directed ;; Action
collapsed ;; Motion || died ;; Death || struck ;; Attack || protests ;; Protest
killing ;; Killing || manslaughter ;; Killing || inquest ;; Criminal_investigation || verdict ;; Legal_rulings || prosecuted ;; Judgment_communication
misconduct ;; Action || found ;; Know || dismissed ;; Legal_rulings
paid ;; Commerce_pay || caused ;; Causation || death ;; Death || acknowledged ;; Statement
published ;; Publishing || pushing ;; Motion || suffered ;; Bodily_harm || attack ;; Attack || concluded ;; Coming_to_believe || striking ;; Attack
struck ;; Attack || make ;; Getting
collapsed ;; Motion || died ;; Death || walked ;; Self_motion
began ;; Process_start || died ;; Death || caused ;; Causation || inquiry ;; Criminal_investigation || autopsies ;; Scrutiny || trauma ;; Bodily_harm || cirrhosis ;; Bodily_harm || indicated ;; Coming_to_believe || bleeding ;; Bodily_harm
decided ;; Deciding || charge ;; Judgment_communication || disagreement ;; Quarreling || show ;; Statement
manslaughter ;; Killing || changed ;; Change || killing ;; Killing || verdict ;; Legal_rulings || charged ;; Judgment_communication
acquitted ;; Legal_rulings
death ;; Death || sparked ;; Causation || debate ;; Quarreling
published ;; Publishing || response ;; Response || aimed ;; Aiming || restore ;; Recovering || report ;; Reporting
took place ;; Process_start
decided ;; Deciding || called ;; Name_conferral || called ;; Name_conferral || break ;; Destroying
refused ;; Agree_or_refuse_to_act || ruled ;; Legal_rulings || join ;; Becoming_a_member
invasion ;; Attack || depose ;; Change_of_leadership
lost ;; Earnings_and_losses || defeated ;; Conquering
killed ;; Killing
taken away ;; Conquering
besieged ;; Besieging || failed ;; Earnings_and_losses || capture ;; Arrest
raiding ;; Attack || invasion ;; Attack || taking ;; Creating
asked ;; Request || help ;; Assistance
defended ;; Defending || brought ;; Bringing || conquering ;; Conquering
pay ;; Commerce_pay
built ;; Building || find ;; Know
invasion ;; Attack || operations ;; Military_operation || launched ;; Process_start
invasion ;; Attack || successes ;; Achieve || launched ;; Process_start
raided ;; Attack || reached ;; Arriving || Commanded ;; Control || defeated ;; Conquering || forces ;; Causation || forces ;; Causation
turned ;; Becoming || assault ;; Attack
converging ;; Come_together || forces ;; Causation || neutralized ;; Preventing_or_letting || secure ;; Getting || trap ;; Conquering
regain ;; Recovering || allowed ;; Preventing_or_letting || hand ;; Giving
sent ;; Sending || detained ;; Arrest || included ;; Cause_to_be_included
agree ;; Agree_or_refuse_to_act || pay ;; Commerce_pay || forced ;; Causation
warfare ;; Hostile_encounter || focused ;; Aiming || resumed ;; Process_start || led to ;; Causation
played ;; Competition
contested ;; Competition || played ;; Competition
cross ;; Body_movement || put ;; Giving || swept ;; Motion
came ;; Motion || allow ;; Preventing_or_letting || dropped ;; Body_movement || tip ;; Motion
scoring ;; Getting || failed ;; Earnings_and_losses || played ;; Competition || made up ;; Causation
occupied ;; Control || took ;; Conquering
sent ;; Sending || suspended ;; Change_event_time || replaced ;; Change || taken ;; Conquering
Wars ;; Hostile_encounter || conflicts ;; Hostile_encounter || took place ;; Process_start
defeated ;; Conquering
attack ;; Attack || facing ;; Confronting_problem || War ;; Military_operation || fought ;; Hostile_encounter
ended ;; Process_end || lost ;; Earnings_and_losses || conflicts ;; Hostile_encounter
became ;; Becoming || pushed ;; Motion || enlarged ;; Expansion
war ;; Military_operation || crisis ;; Catastrophe || war ;; Military_operation
achieved ;; Achieve
formed ;; Coming_to_be
causes ;; Causation || War ;; Military_operation || lost ;; Earnings_and_losses || Wars ;; Hostile_encounter
started ;; Process_start || lost ;; Earnings_and_losses || flee ;; Escaping
increase ;; Cause_change_of_position_on_a_scale
Wars ;; Hostile_encounter || disaster ;; Catastrophe
fall ;; Earnings_and_losses || relinquishing ;; Surrendering || collapse ;; Death || created ;; Causation
held ;; Defending || assassinated ;; Killing
began ;; Process_start || ended ;; Process_end || attacked ;; Attack || war ;; Military_operation || treaty ;; Sign_agreement
began ;; Process_start || war ;; Military_operation
prolonged ;; Change_event_time || repudiated ;; Preventing_or_letting || retained ;; Placing || conquered ;; Conquering || argument ;; Protest || occupation ;; Control
commenced ;; Process_start || made ;; Manufacturing || trampled ;; Motion
repelled ;; Defending || counter-attacked ;; Attack || combined ;; Cause_to_amalgamate
invaded ;; Attack || taken ;; Conquering || conflict ;; Hostile_encounter
attacked ;; Attack || regaining ;; Recovering || advanced ;; Cause_to_make_progress
lost ;; Earnings_and_losses || forced ;; Motion || resulting ;; Causation || gained ;; Getting || treaty ;; Sign_agreement || war ;; Military_operation || cede ;; Surrendering
Race ;; Competition || running ;; Motion || event ;; Social_event || held ;; Hold
participation ;; Participation || risen ;; Cause_to_make_progress || established ;; Creating
using ;; Using || races ;; Competition || event ;; Social_event || takes place ;; Process_start
open ;; Openness || comprises ;; Containing || race ;; Competition || run ;; Motion
run ;; Motion
races ;; Competition || starting ;; Process_start || emphasis ;; Cause_change_of_position_on_a_scale || promote ;; Cause_to_make_progress
race ;; Competition || competed ;; Competition
race ;; Competition || extended ;; Change_event_time
race ;; Competition || race ;; Competition
record ;; Recording || setting ;; Placing
race ;; Competition || assessment ;; Statement || describe ;; Statement
named ;; Name_conferral || held ;; Hold
moved ;; Motion || held ;; Hold
moved ;; Motion || festival ;; Social_event
held ;; Process_start || extended ;; Change_event_time
attracted ;; Influence
cancelled ;; Change_event_time
takes place ;; Process_start
followed ;; Motion
confirmed ;; Check || returning ;; Arriving
played ;; Competition
finals ;; Process_end || gain ;; Getting || win ;; Earnings_and_losses || knew ;; Know
extend ;; Change_event_time || banned ;; Preventing_or_letting || raised ;; Bringing || competition ;; Competition || charged ;; Judgment_communication || led ;; Giving || lead ;; Change || give ;; Giving || played ;; Competition
lost ;; Earnings_and_losses || went ;; Forming_relationships
achieved ;; Achieve || competition ;; Competition || eliminating ;; Removing
marred ;; Damaging
extending ;; Change_event_time || receives ;; Receiving
Founded ;; Creating || festival ;; Social_event || established ;; Creating
started ;; Process_start || regarded ;; Know
adopted ;; Choosing || grew ;; Expansion || help ;; Assistance || changed ;; Change
selected ;; Choosing
organised ;; Arranging
elected ;; Choosing
destructive ;; Destroying || record ;; Recording || hurricane ;; Catastrophe || Hurricane ;; Catastrophe || hurricane ;; Catastrophe || cyclone ;; Catastrophe || strike ;; Attack
moved ;; Motion || originated ;; Coming_to_be
developed ;; Coming_to_be || strengthened ;; Cause_change_of_strength || storm ;; Catastrophe
halted ;; Process_end || intensification ;; Cause_change_of_position_on_a_scale
took place ;; Process_start || explosive ;; Attack || intensification ;; Cause_change_of_position_on_a_scale || reached ;; Achieve
begun ;; Process_start || prolonged ;; Change_event_time || turn ;; Change
began ;; Process_start || strengthening ;; Cause_change_of_strength || hurricane ;; Catastrophe || cyclone ;; Catastrophe || enter ;; Arriving || weakened ;; Cause_change_of_strength
turning ;; Becoming || began ;; Process_start || transition ;; Change || completed ;; Process_end || striking ;; Attack
cyclone ;; Catastrophe
caused ;; Causation || hurricane ;; Catastrophe || deaths ;; Death || produced ;; Causation
passed ;; Motion_directional || hurricane ;; Catastrophe || damage ;; Damaging || limited ;; Limiting || losing ;; Earnings_and_losses
losses ;; Earnings_and_losses
damage ;; Damaging || seen ;; Perception_active || wrought ;; Causation || claimed ;; Statement
stretches ;; Expansion || washed ;; Removing || flooding ;; Catastrophe
killed ;; Killing || costs ;; Cost || damage ;; Damaging || record ;; Recording
deployed ;; Arranging || aid ;; Assistance || assist ;; Assistance || distribution ;; Supply || recovery ;; Rescuing
cyclone ;; Catastrophe
hurricane ;; Catastrophe || storm ;; Catastrophe || storm ;; Catastrophe
moved ;; Motion || hurricane ;; Catastrophe
hurricane ;; Catastrophe || slowed ;; Change_event_time
storm ;; Catastrophe || intensity ;; Cause_change_of_position_on_a_scale
executed ;; Self_motion || made ;; Causation
weakened ;; Cause_change_of_strength
hurricane ;; Catastrophe || impacts ;; Damaging
ordered ;; Request || evacuate ;; Departing
caused ;; Causation || landfall ;; Catastrophe || storm ;; Catastrophe
killed ;; Killing
produced ;; Manufacturing || storm ;; Catastrophe || swept ;; Removing
storm ;; Catastrophe
killed ;; Killing
aided ;; Assistance || storm ;; Catastrophe || rainfall ;; Catastrophe || produced ;; Causation
peaked ;; Cause_change_of_position_on_a_scale
caused ;; Causation || rains ;; Bodily_harm
deaths ;; Death || hurricane ;; Catastrophe
revolution ;; Change_of_leadership || beginning ;; Process_start
conquered ;; Conquering || revolution ;; Change_of_leadership || brought ;; Bringing || established ;; Building || culminated ;; Process_end
altered ;; Change || triggering ;; Causation || decline ;; Cause_change_of_position_on_a_scale || replacing ;; Change_of_leadership
extended ;; Change_event_time
regard ;; Perception_active
revolution ;; Change_of_leadership || debated ;; Communication
worsened ;; Cause_change_of_position_on_a_scale || enjoyed ;; Perception_active || established ;; Building
proclaimed ;; Statement || awakened ;; Recovering || hold ;; Defending
contributed ;; Giving || change ;; Change
took ;; Conquering || attacked ;; Attack || passed ;; Motion_directional
left ;; Departing
thwarting ;; Hindering
proclaimed ;; Statement
led to ;; Causation
revolution ;; Change_of_leadership
beginning ;; Process_start || facilitated ;; Cause_to_make_progress || eluded ;; Escaping
culminating ;; Hostile_encounter || rise ;; Cause_change_of_position_on_a_scale
expelled ;; Removing || created ;; Creating || imposed ;; Action || established ;; Building
assumed ;; Choosing
repudiated ;; Agree_or_refuse_to_act || persecuted ;; Attack || creating ;; Creating || suspended ;; Process_end || made ;; Manufacturing || elections ;; Choosing
collapsed ;; Destroying
became ;; Becoming || revolution ;; Change_of_leadership || established ;; Building || setting ;; Placing
revolution ;; Change_of_leadership
revolution ;; Change_of_leadership
became ;; Becoming || revolution ;; Change_of_leadership
revolution ;; Change_of_leadership
revolution ;; Change_of_leadership || landed ;; Self_motion
differed ;; Change
accelerated ;; Cause_change_of_position_on_a_scale
became ;; Becoming || leading to ;; Causation || spread ;; Dispersal
defense ;; Defending || organising ;; Arranging
affect ;; Influence
noted ;; Know
named ;; Name_conferral || developed ;; Coming_to_be
slowed ;; Change_event_time || move ;; Motion || starting ;; Process_start || undertook ;; Action || developing ;; Coming_to_be || intensify ;; Cause_change_of_strength
passed ;; Motion_directional || passing ;; Motion_directional || intensifying ;; Cause_change_of_strength
moved ;; Motion || reached ;; Achieve
moved ;; Motion || started ;; Process_start || increased ;; Cause_change_of_position_on_a_scale || weaken ;; Cause_change_of_strength
move ;; Motion || passed ;; Motion_directional || weakening ;; Cause_change_of_strength
noted ;; Know || crossed ;; Motion_directional || producing ;; Creating || degenerated ;; Destroying
affected ;; Influence || affected ;; Influence
destroyed ;; Destroying || left ;; Causation
Damage ;; Damaging || confined ;; Hindering
use ;; Using || retired ;; Removing || withdrawn ;; Removing
War ;; Hostile_encounter || war ;; Hostile_encounter
began ;; Process_start || ended ;; Process_end || given ;; Giving
war ;; Hostile_encounter || Return ;; Arriving || initiated ;; Process_start || rivalry ;; Competition
war ;; Hostile_encounter || utilized ;; Using || incident ;; Attack || rivalry ;; Competition
war ;; Hostile_encounter || prolonged ;; Change_event_time
victory ;; Earnings_and_losses
reduced ;; Cause_change_of_position_on_a_scale || emigrate ;; Escaping
defense ;; Defending
took place ;; Process_start || Knockout ;; Competition
defeated ;; Conquering || semi-final ;; Competition || semi-final ;; Competition || Defending ;; Defending
matches ;; Process_end || played ;; Competition || winning ;; Getting
lost ;; Earnings_and_losses || final ;; Competition || tournament ;; Competition || match ;; Competition
tournament ;; Competition
sent ;; Sending || posted ;; Sending
slowed ;; Change_event_time || scored ;; Getting || final ;; Competition
loss ;; Earnings_and_losses || reached ;; Arriving
match ;; Competition || playing ;; Competition
Match ;; Competition || Tournament ;; Competition || named ;; Name_conferral || named ;; Name_conferral
battles ;; Hostile_encounter || culminated ;; Process_end
surrender ;; Surrendering || led ;; Causation
marked ;; Recording
involved ;; Participation
sent ;; Sending || came ;; Motion || joined ;; Participation
sent ;; Sending || opposed ;; Agree_or_refuse_to_act || oppose ;; Agree_or_refuse_to_act
built ;; Building || moved ;; Motion || oppose ;; Agree_or_refuse_to_act
delivering ;; Sending || decisions ;; Deciding || combined ;; Cause_to_amalgamate || control ;; Control || weaker ;; Cause_change_of_strength || gained ;; Getting
dispute ;; Quarreling || defeated ;; Conquering
arrived ;; Arriving || began ;; Process_start || moving ;; Body_movement
send ;; Sending || delayed ;; Change_event_time
began ;; Process_start
decided ;; Deciding || shortened ;; Change_event_time || abandon ;; GiveUp || stormed ;; Attack
became ;; Becoming || negotiations ;; Communication || surrendered ;; Surrendering || opened ;; Openness
reached ;; Arriving || fell ;; Motion_directional || entered ;; Arriving
recognized ;; Know
defending ;; Defending || discussed ;; Communication || led to ;; Causation
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || War ;; Hostile_encounter
fought ;; Hostile_encounter
advanced ;; Self_motion || objective ;; Aiming || siege ;; Besieging
Battles ;; Hostile_encounter || Battle ;; Hostile_encounter || fought ;; Hostile_encounter || advance ;; Self_motion || halted ;; Process_end
reinforced ;; Cause_change_of_strength || offensive ;; Attack || command ;; Control
retreating ;; Escaping || cut ;; Removing || outflanked ;; Besieging
surrendered ;; Surrendering || prolonged ;; Change_event_time || siege ;; Besieging || fought ;; Hostile_encounter || assault ;; Attack || slow-moving ;; Self_motion || intercepted ;; Preventing_or_letting
defense ;; Defending || conflict ;; Hostile_encounter
used ;; Using
killed ;; Killing || destroyed ;; Destroying
speaking ;; Statement || conflict ;; Hostile_encounter || attack ;; Attack || upended ;; Self_motion
suffered ;; Bodily_harm
stated ;; Statement || released ;; Publishing || incidents ;; Catastrophe || attack ;; Attack
casualties ;; Catastrophe || services ;; Supply || reaching ;; Arriving || delayed ;; Change_event_time
turned ;; Becoming || believed ;; Know
refused ;; Agree_or_refuse_to_act || said ;; Communication || speaking ;; Statement || target ;; Aiming
added ;; Cause_to_be_included
said ;; Communication || named ;; Name_conferral || extended ;; Change_event_time || think ;; Perception_active || printed ;; Publishing
operating ;; Being_in_operation || began ;; Process_start || extended ;; Change_event_time || states ;; Statement
operated ;; Being_in_operation || deny ;; Statement
said ;; Telling
reported ;; Reporting || insisted ;; Statement || used ;; Using
impact ;; Influence || destructive ;; Destroying || Hurricane ;; Catastrophe
formed ;; Coming_to_be || accelerated ;; Cause_change_of_position_on_a_scale
made ;; Manufacturing || hurricane ;; Catastrophe || fast-moving ;; Self_motion || storm ;; Catastrophe
affected ;; Influence || headed ;; Self_motion || storm ;; Catastrophe
dissipated ;; Dispersal
Damage ;; Damaging
received ;; Receiving || record ;; Recording || falling ;; Motion_directional
caused ;; Causation || destruction ;; Destroying || drowned ;; Death
destroyed ;; Destroying || recorded ;; Recording || flooding ;; Motion || extended ;; Change_event_time
damage ;; Damaging || deaths ;; Death || attributed ;; Causation || storm ;; Catastrophe
hurricane ;; Catastrophe || cyclone ;; Catastrophe || observed ;; Perception_active || recorded ;; Recording
storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || included ;; Cause_to_be_included || hurricanes ;; Catastrophe || recorded ;; Recording
formed ;; Coming_to_be || developed ;; Coming_to_be
moved ;; Motion || passing ;; Motion_directional || entered ;; Arriving
intensified ;; Cause_change_of_strength || achieving ;; Achieve || reach ;; Achieve
weakened ;; Cause_change_of_strength
weakening ;; Cause_change_of_strength || degenerated ;; Change
inundated ;; Motion || caused ;; Causation
damage ;; Damaging || suffered ;; Bodily_harm || flooding ;; Catastrophe
destroyed ;; Destroying
received ;; Receiving || damage ;; Damaging || damage ;; Damaging
destruction ;; Destroying || damage ;; Damaging
declared ;; Expressing_publicly || storm ;; Catastrophe
disrupted ;; Hindering
hurricane ;; Catastrophe || reported ;; Reporting || deaths ;; Death || deaths ;; Death || reported ;; Reporting
damage ;; Damaging || reported ;; Reporting
extended ;; Change_event_time
peaked ;; Cause_change_of_position_on_a_scale
associated ;; Collaboration
damage ;; Damaging
hurricane ;; Catastrophe || deaths ;; Death
battle ;; Hostile_encounter
fought ;; Hostile_encounter
joint ;; Collaboration || forces ;; Causation || command ;; Control || collaboration ;; Collaboration || command ;; Control
force ;; Causation
dispatched ;; Sending || seize ;; Conquering
achieved ;; Achieve || deadly ;; Killing
became ;; Becoming || extending ;; Change_event_time || rescue ;; Rescuing
competition ;; Competition
began ;; Process_start || ended ;; Process_end || tournament ;; Competition
time-frame ;; Change_event_time || remained ;; Temporary_stay || ran ;; Action
broadcast ;; Expressing_publicly
hosted ;; Social_event || tournament ;; Competition || final ;; Competition
beat ;; Competition || final ;; Competition
named ;; Name_conferral || named ;; Name_conferral
defences ;; Defending || driving ;; Self_motion || intention ;; Aiming || break ;; Destroying || capture ;; Conquering
retreat ;; Escaping || fighting ;; Hostile_encounter || collapse ;; Destroying || cut off ;; Removing || forces ;; Military_operation
battle ;; Hostile_encounter
using ;; Using || attack ;; Attack || launched ;; Process_start
captured ;; Conquering || defences ;; Defending || broke ;; Destroying
slowed ;; Change_event_time || attack ;; Attack || counter-attacks ;; Attack
attack ;; Attack || advanced ;; Self_motion || halted ;; Process_end || prepare ;; GetReady
promoted ;; Cause_change_of_position_on_a_scale || concert ;; Social_event || tour ;; Traveling || released ;; Publishing
began ;; Process_start || ended ;; Process_end || show ;; Social_event
renamed ;; Name_conferral || tour ;; Traveling
confirmed ;; Check || prolonged ;; Change_event_time || happen ;; Presence || planned ;; Arranging || tour ;; Traveling || shows ;; Social_event
marking ;; Recording || includes ;; Cause_to_be_included || performs ;; Social_event
postponed ;; Change_event_time || shows ;; Social_event || production ;; Creating || issues ;; Creating
take place ;; Process_start
announced ;; Expressing_publicly || show ;; Social_event
match ;; Competition
given ;; Giving || match ;; Competition || played ;; Competition
competing ;; Participation
beat ;; Competition
reached ;; Arriving || appearance ;; Presence || losing ;; Earnings_and_losses
beat ;; Competition || resulted in ;; Causation
scored ;; Getting || Watched ;; Perception_active || took ;; Getting
extended ;; Change_event_time
scored ;; Getting
scored ;; Getting || scored ;; Getting || responded ;; Response
due ;; Causation || scored ;; Getting
progressed ;; Cause_to_make_progress
lose ;; Earnings_and_losses
reached ;; Arriving || marked ;; Recording || final ;; Process_end || emergence ;; Coming_to_be || lost ;; Earnings_and_losses
win ;; Getting
competition ;; Competition
hurricane ;; Catastrophe || formed ;; Coming_to_be || dissipated ;; Dispersal || Hurricane ;; Catastrophe
developed ;; Coming_to_be || hurricane ;; Catastrophe || hurricane ;; Catastrophe
moved ;; Motion || storm ;; Catastrophe || intensified ;; Cause_change_of_strength
entered ;; Arriving
turned ;; Becoming || intensified ;; Cause_change_of_strength
slowed ;; Change_event_time || storm ;; Catastrophe || weakened ;; Cause_change_of_strength || weakened ;; Cause_change_of_strength
dissipated ;; Dispersal
hurricanes ;; Catastrophe || attain ;; Getting || hurricane ;; Catastrophe || strength ;; Cause_change_of_strength
broke ;; Destroying || storm ;; Catastrophe || towing ;; Self_motion
produced ;; Manufacturing || hurricane ;; Catastrophe
flooding ;; Motion || impacted ;; Influence || occurred ;; Presence || affected ;; Influence
caused ;; Causation || deaths ;; Death
established ;; Creating
competition ;; Competition || started ;; Process_start || teams ;; Participation
competed ;; Competition || matches ;; Competition || final ;; Process_end
qualify ;; Becoming_a_member || tournament ;; Competition
defeating ;; Conquering || final ;; Competition || tournament ;; Competition
affected ;; Influence || tournament ;; Competition || conditions ;; Causation || weather ;; Causation
shortened ;; Change_event_time || abandoned ;; GiveUp || played ;; Competition || matches ;; Competition || matches ;; Competition || games ;; Competition
occurred ;; Coming_to_be || wave ;; Motion
caused ;; Causation || shifted ;; Change || creating ;; Creating || block ;; Hiding_objects || wave ;; Motion || bomb ;; Attack || associated ;; Collaboration
advance ;; Motion || allowed ;; Preventing_or_letting || bringing ;; Causation
wave ;; Motion
extending ;; Change_event_time || Thanksgiving ;; Social_event || last ;; Process_end || expected ;; Statement
broke down ;; Destroying || moving ;; Motion || due ;; Causation || storm ;; Catastrophe
brought ;; Bringing || end ;; Process_end || wave ;; Motion || forecast ;; Statement || return ;; Arriving
ended ;; Process_end || wave ;; Motion || brought ;; Causation
affecting ;; Influence || extended ;; Expansion
caused ;; Causation || occurred ;; Presence
extended ;; Change_event_time
associated ;; Collaboration || resulting in ;; Causation || tracked ;; Motion
broken ;; Destroying || fell ;; Motion_directional
affected ;; Influence || include ;; Cause_to_be_included || ranging ;; Cause_change_of_position_on_a_scale || extending ;; Expansion
hurricane ;; Catastrophe || Hurricane ;; Catastrophe || inflicting ;; Causation || damage ;; Damaging
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe || emerged ;; Coming_to_be || hurricane ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
developed ;; Coming_to_be
began ;; Process_start || became ;; Becoming || storm ;; Catastrophe || tracking ;; Self_motion
upgraded ;; Cause_to_make_progress || hurricane ;; Catastrophe
slowed ;; Change_event_time || storm ;; Catastrophe || hurricane ;; Catastrophe || Hurricane ;; Catastrophe || storm ;; Catastrophe || peaked ;; Motion || turned ;; Motion
moved ;; Motion || turned ;; Becoming
moved ;; Motion || storm ;; Catastrophe || regained ;; Recovering || losing ;; Earnings_and_losses || hurricane ;; Catastrophe || entering ;; Arriving || weakened ;; Cause_change_of_strength
performed ;; Hold || hurricane ;; Catastrophe || strike ;; Attack || storm ;; Catastrophe
warnings ;; Warning || evacuated ;; Removing || evacuated ;; Removing || relocated ;; Removing || watches ;; Motion
deployed ;; Arranging
Hurricane ;; Catastrophe || storm ;; Catastrophe
reported ;; Reporting || damage ;; Damaging
storm ;; Catastrophe || washed ;; Motion
storm ;; Catastrophe || damage ;; Damaging || feared ;; Change_sentiment
damage ;; Damaging
hosted ;; Social_event
began ;; Process_start || ended ;; Process_end || game ;; Competition || played ;; Competition
defeated ;; Conquering || win ;; Getting
named ;; Name_conferral
promoted ;; Cause_change_of_position_on_a_scale
lost ;; Earnings_and_losses || semifinal ;; Competition || missed ;; Earnings_and_losses || final ;; Competition
extended ;; Change_event_time || victory ;; Earnings_and_losses
finish ;; Process_end || result in ;; Causation
ended up ;; Process_end
storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
developed ;; Coming_to_be || tracked ;; Self_motion
crossing ;; Motion_directional || strengthened ;; Cause_change_of_strength || reach ;; Achieve
moved ;; Motion || hit ;; Arriving || hit ;; Attack
weakening ;; Cause_change_of_strength || turned ;; Becoming || dissipated ;; Dispersal
Hurricane ;; Catastrophe || entered ;; Arriving || weakened ;; Cause_change_of_strength || observed ;; Perception_active
hurricane ;; Catastrophe || strike ;; Attack
damage ;; Damaging || Due ;; Causation || hurricane ;; Catastrophe || extended ;; Change_event_time || remaining ;; Temporary_stay || produced ;; Creating || resulting in ;; Causation
affected ;; Influence || hurricanes ;; Catastrophe || strike ;; Attack
cyclone ;; Catastrophe || Cyclone ;; Catastrophe || cyclone ;; Catastrophe || considered ;; Deciding || affect ;; Influence
disaster ;; Catastrophe || cyclone ;; Catastrophe || designated ;; Statement || lasted ;; Process_end
damage ;; Damaging || suffered ;; Bodily_harm
devastated ;; Destroying || cyclone ;; Catastrophe || waves ;; Motion || winds ;; Motion
damages ;; Damaging || caused ;; Causation
destruction ;; Destroying || damage ;; Damaging || damages ;; Damaging || put ;; Placing
declared ;; Expressing_publicly
resigned ;; Process_end
joined ;; Connect
swung ;; Motion
cyclone ;; Catastrophe || develop ;; Cause_to_make_progress
attained ;; Achieve || Forming ;; Coming_to_be
caused ;; Causation || turn ;; Motion || took ;; Creating
reached ;; Arriving
existence ;; Presence || classified ;; Check
moved ;; Motion || becoming ;; Becoming
effects ;; Influence
resulting in ;; Causation || capsized ;; Motion
rescued ;; Rescuing || remained ;; Temporary_stay
Cyclone ;; Catastrophe || produced ;; Creating
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe
moved ;; Motion || Given ;; Giving
cyclone ;; Catastrophe || approaching ;; Arriving || attained ;; Achieve
passed ;; Motion_directional || producing ;; Creating || destroyed ;; Destroying
damage ;; Damaging || occurred ;; Coming_to_be || damage ;; Damaging || estimated ;; Statement
killed ;; Killing
passing ;; Motion_directional || struck ;; Attack
storm ;; Catastrophe || set ;; Placing || records ;; Recording || dropped ;; Motion
caused ;; Causation || flooding ;; Catastrophe || mudslides ;; Catastrophe || resulted in ;; Causation
flooding ;; Catastrophe || left ;; GiveUp
damaged ;; Damaging || destroyed ;; Destroying || leaving ;; Causation
deaths ;; Death || Damage ;; Damaging || estimated ;; Statement
loop ;; Motion || dissipated ;; Dispersal || weakened ;; Cause_change_of_strength
marked ;; Recording || mapped ;; Recording
intensified ;; Cause_change_of_strength || forming ;; Coming_to_be || formation ;; Coming_to_be || reaching ;; Achieve
hurricane ;; Catastrophe || peaked ;; Cause_change_of_position_on_a_scale
intensifying ;; Cause_change_of_strength || meandered ;; Self_motion
moved ;; Motion
weakened ;; Cause_change_of_strength || fluctuating ;; Motion
storm ;; Catastrophe || landfall ;; Arriving || turned ;; Motion
left ;; Causation
produced ;; Creating
strike ;; Attack || hurricane ;; Catastrophe
storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
forming ;; Creating || predicted ;; Statement
Moving ;; Motion || hurricane ;; Catastrophe || strengthened ;; Cause_change_of_strength || reach ;; Achieve
hurricane ;; Catastrophe || hurricane ;; Catastrophe || Weakening ;; Cause_change_of_strength || turned ;; Motion || dissipating ;; Dispersal
effects ;; Influence || included ;; Cause_to_be_included || damage ;; Damaging || deaths ;; Death || retired ;; Removing
occurred ;; Coming_to_be || effects ;; Influence || damaged ;; Damaging || destroyed ;; Destroying || injured ;; Bodily_harm
damaged ;; Damaging || destroyed ;; Destroying
killed ;; Killing || departing ;; Departing || flight ;; Motion || crashed ;; Damaging || turned ;; Motion
indicated ;; Statement || turned ;; Motion
refuelled ;; Getting || bound ;; Self_motion
crashed ;; Damaging
requested ;; Request
reported ;; Reporting || determined ;; Coming_to_believe
announced ;; Expressing_publicly || aircraft ;; Arranging || crashed ;; Damaging || carrying ;; Sending
sanctioned ;; Preventing_or_letting
participated ;; Participation || competition ;; Competition
played ;; Competition || took place ;; Process_start || tournament ;; Competition
split ;; Motion || took part ;; Dispersal
win ;; Getting || beat ;; Competition
defeating ;; Conquering
competed ;; Competition || marked ;; Recording
played ;; Competition || make it ;; Causation
relegated ;; Hindering || excluded ;; Preventing_or_letting
given ;; Giving || fill ;; Filling
affect ;; Influence || cyclone ;; Catastrophe
storm ;; Catastrophe || hurricane ;; Catastrophe || developed ;; Cause_change_of_position_on_a_scale
intensifying ;; Cause_change_of_strength || storm ;; Catastrophe || tracked ;; Self_motion
strengthening ;; Cause_change_of_strength || hurricane ;; Catastrophe || occurred ;; Presence || attained ;; Arriving
turn ;; Motion
storm ;; Catastrophe || Drifting ;; Motion || weakening ;; Cause_change_of_strength
dissipated ;; Dispersal
caused ;; Causation || flooding ;; Catastrophe
reported ;; Reporting
destroyed ;; Destroying || left ;; Departing
flooded ;; Motion || storm ;; Catastrophe || washed ;; Removing
damage ;; Damaging
reported ;; Reporting || fatalities ;; Death
impact ;; Influence
occurred ;; Coming_to_be
shook ;; Motion
felt ;; Know || Tremors ;; Catastrophe
earthquake ;; Catastrophe || assessed ;; Scrutiny
observed ;; Perception_active
identified ;; Check || occurred ;; Coming_to_be || mapped ;; Recording
recorded ;; Recording || Cyclone ;; Catastrophe || cyclone ;; Catastrophe || cyclone ;; Catastrophe || Cyclone ;; Catastrophe
destructive ;; Destroying || cyclone ;; Catastrophe
killed ;; Killing
caused ;; Causation || damages ;; Damaging || Cyclone ;; Catastrophe || devastating ;; Destroying || record ;; Recording || storms ;; Catastrophe || hit ;; Attack
intensified ;; Cause_change_of_strength || Forming ;; Coming_to_be || storm ;; Catastrophe
became ;; Becoming || intensified ;; Cause_change_of_strength || cyclone ;; Catastrophe || cyclone ;; Catastrophe || landfall ;; Arriving
emerged ;; Coming_to_be || crossing ;; Motion_directional || landfall ;; Arriving || made ;; Arriving
arrived at ;; Arriving || transitioned ;; Change || loop ;; Motion
became ;; Becoming || weakened ;; Cause_change_of_strength || dissipating ;; Departing
killing ;; Killing || working ;; Being_in_operation || occurred ;; Presence || crashed ;; Destroying || disaster ;; Catastrophe
crash ;; Catastrophe || crash ;; Catastrophe || crash ;; Catastrophe
taken off ;; Departing
inspected ;; Scrutiny || carrying ;; Bringing || load ;; Filling
accident ;; Catastrophe || air ;; Expressing_publicly || concluded ;; Statement
turn ;; Motion || avert ;; Preventing_or_letting || made ;; Bringing || climbing ;; Motion || allowed ;; Preventing_or_letting || sideslip ;; Change
exploded ;; Destroying || thrown up ;; Motion
dead ;; Death
attack ;; Attack || involved ;; Cause_to_be_included || Battle ;; Hostile_encounter
invasion ;; Attack || partitioned ;; Change_of_leadership
included ;; Cause_to_be_included
attacked ;; Attack || killed ;; Killing || assault ;; Attack
surrendered ;; Surrendering || captured ;; Conquering
attack ;; Attack || followed ;; Motion || battle ;; Hostile_encounter
destroyed ;; Destroying || occurred ;; Presence
tore ;; Destroying || travelling ;; Motion
preventing ;; Preventing_or_letting || halt ;; Process_end || derailing ;; Self_motion
aided ;; Assistance || wounded ;; Bodily_harm || fought ;; Hostile_encounter
killed ;; Killing || died ;; Death
underwent ;; Bodily_harm || required ;; Imposing_obligation
using ;; Using || detonated ;; Attack
reported ;; Reporting || boarded ;; Self_motion
ran away ;; Self_motion || leaped ;; Motion || moving ;; Motion
shook ;; Motion
conducted ;; Action
floods ;; Catastrophe || affected ;; Influence
described ;; Statement
reported ;; Reporting || killed ;; Killing
drowned ;; Death || believed ;; Know
swept ;; Motion || damaged ;; Damaging
allowing ;; Preventing_or_letting || rise ;; Cause_change_of_position_on_a_scale || death ;; Death || flood ;; Catastrophe || receded ;; Cause_change_of_position_on_a_scale || reach ;; Arriving
fell ;; Motion
came ;; Motion
estimated ;; Scrutiny
hit ;; Attack
preventing ;; Preventing_or_letting
caused ;; Causation || known ;; Know
Originating ;; Coming_to_be || classified ;; Check
strengthened ;; Cause_change_of_strength || moving ;; Motion
turned ;; Motion || attained ;; Achieve
turned ;; Motion
underwent ;; Self_motion || entered ;; Arriving
attained ;; Getting
weakened ;; Cause_change_of_strength
moving ;; Motion || landfall ;; Self_motion || dissipating ;; Dispersal
brought ;; Bringing || meandered ;; Motion
perished ;; Death || left ;; Departing
caused ;; Causation
triggered ;; Causation || associated ;; Collaboration || washed ;; Removing || inundated ;; Destroying
took place ;; Process_start
took place ;; Process_start || listed ;; Recording
forces ;; Causation || Battle ;; Hostile_encounter || assault ;; Attack
aided ;; Assistance || prevented ;; Preventing_or_letting
planned ;; Arranging || besieged ;; Besieging || led ;; Causation || raid ;; Military_operation || executed ;; Action
seized ;; Conquering || forces ;; Causation || landed ;; Motion || raided ;; Attack
raid ;; Attack || disputed ;; Quarreling
claimed ;; Statement || prevented ;; Preventing_or_letting || attack ;; Attack || delivered ;; Sending || claimed ;; Statement || capture ;; Conquering || destroying ;; Destroying || driven ;; Motion
cyclone ;; Catastrophe || hurricane ;; Catastrophe || landfall ;; Arriving || affect ;; Influence
developed ;; Coming_to_be || storm ;; Catastrophe || cyclone ;; Catastrophe
became ;; Becoming || intensified ;; Cause_change_of_strength || moved ;; Motion
looped ;; Motion || approaching ;; Arriving || approached ;; Arriving
strengthening ;; Cause_change_of_strength || turned ;; Change
became ;; Becoming || cyclone ;; Catastrophe || striking ;; Attack
impact ;; Influence || remained ;; Temporary_stay
droughts ;; Catastrophe || reported ;; Reporting || ending ;; Process_end
destroyed ;; Destroying
Damage ;; Damaging || damaged ;; Damaging || left ;; Departing
storm ;; Catastrophe || killed ;; Killing || resulted in ;; Causation
storm ;; Catastrophe || causing ;; Causation || damage ;; Damaging || produced ;; Creating || resulting in ;; Causation
cyclone ;; Catastrophe || affected ;; Influence
formed ;; Coming_to_be || hurricane ;; Catastrophe || becoming ;; Becoming
moving ;; Motion || beginning ;; Process_start || attained ;; Achieve || turned ;; Becoming
approaching ;; Arriving || reached ;; Achieve
storm ;; Catastrophe || turned ;; Motion || strike ;; Bodily_harm || strike ;; Bodily_harm || crossing ;; Motion_directional || predicted ;; Statement
damage ;; Damaging || storm ;; Catastrophe || storm ;; Catastrophe || produced ;; Causation || reported ;; Reporting
storm ;; Catastrophe || hurricane ;; Catastrophe || struck ;; Bodily_harm || strike ;; Bodily_harm || becoming ;; Becoming
warnings ;; Warning || damage ;; Damaging || damage ;; Damaging || flooding ;; Catastrophe || rainfall ;; Catastrophe || tides ;; Catastrophe || building ;; Building || reducing ;; Cause_change_of_position_on_a_scale || resulted ;; Causation
drowned ;; Death || storm ;; Catastrophe || submerged ;; Motion || exacerbated ;; Cause_change_of_position_on_a_scale || threatened ;; Warning
held ;; Defending || death ;; Death || minimizing ;; Cause_change_of_position_on_a_scale
damage ;; Damaging || caused ;; Causation || storm ;; Catastrophe || flooding ;; Catastrophe || loss ;; Earnings_and_losses
moved ;; Motion || hurricane ;; Catastrophe || leading to ;; Causation || entered ;; Arriving || threatened ;; Warning
storm ;; Catastrophe || storm ;; Catastrophe || flooding ;; Catastrophe || making ;; Causation || killed ;; Killing || destroying ;; Destroying || produced ;; Causation
storm ;; Catastrophe || hurricane ;; Catastrophe || flooding ;; Catastrophe || spurred ;; Influence || resulted ;; Causation || safeguard ;; Defending
damage ;; Damaging || caused ;; Causation || storm ;; Catastrophe || killed ;; Killing
organized ;; Arranging || decided ;; Deciding || Finals ;; Competition
Final ;; Competition || held ;; Hold || Final ;; Competition || held ;; Hold || Final ;; Competition || held ;; Hold
progressed ;; Achieve || progressed ;; Achieve || finishing ;; Process_end || finishing ;; Process_end
defending ;; Defending || beat ;; Competition || reach ;; Achieve || finals ;; Competition
final ;; Competition || took ;; Conquering || drove ;; Motion
fallen ;; Body_movement || finishing ;; Process_end || lobbed ;; Body_movement
scored ;; Getting || win ;; Earnings_and_losses || final ;; Competition
finished ;; Process_end || result ;; Causation || tournament ;; Competition
made it ;; Causation || final ;; Process_end || represented ;; Presence || entered ;; Getting || defeated ;; Conquering
playing ;; Competition
organized ;; Arranging || called ;; Name_conferral || support ;; Supporting
took place ;; Process_start
inspired ;; Influence || comments ;; Statement || march ;; Social_event || debate ;; Communication
promised ;; Commitment || stop ;; Process_end || saying ;; Communication || end ;; Process_end || debate ;; Communication
added ;; Cause_to_be_included
viewed ;; Perception_active || social ;; Social_event || issues ;; Creating || threat ;; Warning || issue ;; Creating || convey ;; Bringing
drew ;; Motion || received ;; Receiving || participants ;; Participation || Record ;; Recording || march ;; Self_motion
included ;; Cause_to_be_included || cast ;; Motion
support ;; Supporting
offensive ;; Attack || War ;; Military_operation || followed ;; Motion || start ;; Process_start || Battle ;; Hostile_encounter || took place ;; Process_start || invasion ;; Attack
fought ;; Hostile_encounter || battle ;; Hostile_encounter
detecting ;; Know || launched ;; Process_start || attacks ;; Attack || equipped ;; Supply || support ;; Supporting || advance ;; Self_motion
resistance ;; Defending || withdraw ;; Escaping || captured ;; Conquering || forced ;; Conquering || fighting ;; Hostile_encounter
attacking ;; Attack || reinforced ;; Cause_change_of_strength || manned ;; Giving || penetrating ;; Destroying
losses ;; Earnings_and_losses || fighting ;; Hostile_encounter || assault ;; Attack || repulsed ;; Defending || launched ;; Process_start || assault ;; Attack || failed ;; Earnings_and_losses
killing ;; Killing || advanced ;; Self_motion || capturing ;; Conquering
cleared ;; Emptying || opposition ;; Defending
losses ;; Earnings_and_losses || died ;; Death || battle ;; Military_operation || fighting ;; Hostile_encounter || included ;; Cause_to_be_included || casualties ;; Death || wounded ;; Bodily_harm
Hurricane ;; Catastrophe || landfall ;; Arriving
named ;; Name_conferral || evolved ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || noted ;; Know
developed ;; Cause_to_make_progress || moved ;; Motion
strengthening ;; Cause_change_of_strength || became ;; Becoming || turned ;; Becoming || storm ;; Catastrophe || hurricane ;; Catastrophe || cyclone ;; Catastrophe || ensued ;; Process_start
became ;; Becoming || hurricane ;; Catastrophe || reported ;; Reporting
grazed ;; Self_motion
weakened ;; Cause_change_of_strength || cyclone ;; Catastrophe || Moving ;; Motion
spread ;; Dispersal || initiating ;; Process_start || hurricane ;; Catastrophe
developing ;; Coming_to_be || caused ;; Causation || weakening ;; Cause_change_of_strength || storm ;; Catastrophe || turn ;; Motion
weakened ;; Cause_change_of_strength || passed ;; Motion_directional || cyclone ;; Catastrophe || cyclone ;; Catastrophe || turn ;; Motion || turning ;; Motion
made ;; Manufacturing || Hurricane ;; Catastrophe || landfall ;; Arriving
became ;; Becoming || cyclone ;; Catastrophe || landfall ;; Arriving || final ;; Process_end || issued ;; Statement
occurred ;; Coming_to_be
reported ;; Reporting
struck ;; Attack || earthquake ;; Catastrophe
recorded ;; Recording || suggesting ;; Statement
reported ;; Reporting || felt ;; Change_sentiment || shaking ;; Motion
reported ;; Reporting || damage ;; Damaging
requests ;; Request || due ;; Causation || help ;; Assistance || made ;; Action || cracked ;; Destroying || lost ;; Earnings_and_losses
recorded ;; Recording || felt ;; Change_sentiment || aftershocks ;; Catastrophe
aftershock ;; Catastrophe || occurred ;; Coming_to_be
felt ;; Change_sentiment || tremor ;; Judgment_communication
quake ;; Catastrophe || aftershock ;; Catastrophe || occurred ;; Coming_to_be
originated ;; Coming_to_be
classified ;; Check || drifted ;; Motion
dissipated ;; Dispersal || upgraded ;; Cause_to_make_progress
moved ;; Motion
dissipated ;; Dispersal
contributed ;; Giving
inundated ;; Destroying
damaged ;; Damaging
destroyed ;; Destroying
died ;; Death || killed ;; Killing
held ;; Hold
began ;; Process_start || got ;; Getting || celebration ;; Social_event || held ;; Hold
celebrated ;; Social_event
recognized ;; Know || become ;; Becoming || grown ;; Cause_change_of_position_on_a_scale
referring ;; Adducing
produced ;; Manufacturing || managed ;; Control
competition ;; Competition || includes ;; Cause_to_be_included || strolling ;; Self_motion
occurred ;; Coming_to_be || earthquake ;; Catastrophe
named ;; Name_conferral || shock ;; Catastrophe || centered ;; Placing
injuries ;; Bodily_harm || deaths ;; Death || shock ;; Catastrophe || responsible ;; Causation
occurred ;; Coming_to_be || earthquake ;; Catastrophe || designated ;; Coming_to_be || foreshocks ;; Attack
extended ;; Expansion || Damage ;; Damaging || effects ;; Influence
occurred ;; Coming_to_be || present ;; Presence || faulting ;; Bodily_harm || landslides ;; Catastrophe || failures ;; Catastrophe
issue ;; Catastrophe || damaged ;; Damaging || effects ;; Influence || seen ;; Perception_active || observed ;; Perception_active
referred ;; Adducing || happened ;; Presence
begin ;; Process_start || prevented ;; Preventing_or_letting || failures ;; Catastrophe || traffic ;; Motion || game ;; Competition || loss ;; Earnings_and_losses || played ;; Competition || suffered ;; Bodily_harm
casualties ;; Catastrophe || event ;; Earnings_and_losses || collapse ;; Destroying || collapse ;; Destroying || casualties ;; Killing || accidents ;; Catastrophe || occurring ;; Presence || contributed ;; Causation
proposed ;; Statement
took place ;; Hold
dumped ;; Placing
citing ;; Adducing || took ;; Motion
overturning ;; Request || forcing ;; Causation || stormed ;; Attack || wear ;; Wearing
ended ;; Process_end || suspend ;; Process_end || promised ;; Commitment
invasion ;; Attack
following ;; Self_motion || launched ;; Process_start
ordered ;; Arranging || staged ;; Hold
deposed ;; Change_of_leadership || installed ;; Placing
declared ;; Expressing_publicly
invaded ;; Attack || declared ;; Expressing_publicly || captured ;; Conquering
collapsed ;; Destroying || replaced ;; Change_of_leadership
capture ;; Conquering || resulted in ;; Causation
became ;; Becoming || referred ;; Adducing
expelled ;; Removing
displaced ;; Change
ended ;; Process_end
declared ;; Expressing_publicly || recognizes ;; Know
viewed ;; Perception_active || illegal ;; Legality || illegal ;; Legality || became ;; Becoming
invasion ;; Attack
took ;; Creating || referred ;; Adducing
began ;; Process_start
initiated ;; Process_start
raised ;; Cause_change_of_position_on_a_scale || services ;; Supply
refers ;; Adducing || income ;; Earnings_and_losses || wealth ;; Earnings_and_losses
decisions ;; Deciding || petitioning ;; Request || assemblies ;; Come_together
turned ;; Becoming || focus ;; Aiming || meetings ;; Social_event || occupying ;; Attack
offensive ;; Attack
took place ;; Process_start
referred ;; Adducing
began ;; Process_start || attack ;; Killing || included ;; Cause_to_be_included
withdrawn ;; Escaping || involved ;; Cause_to_be_included
held ;; Hold || tournament ;; Competition
held ;; Hold
held ;; Hold || contested ;; Competition || matches ;; Competition || played ;; Participation
tournament ;; Competition || qualified ;; Becoming_a_member || qualified ;; Becoming_a_member
contested ;; Competition || tournament ;; Competition || final ;; Competition || taken ;; Action
tournament ;; Competition || announced ;; Expressing_publicly || pulled out ;; Removing || citing ;; Adducing
invited ;; Request
tournament ;; Competition || Defending ;; Defending || champions ;; Competition || defeats ;; Earnings_and_losses
claiming ;; Statement || defeated ;; Conquering
named ;; Name_conferral || tournament ;; Competition || tournament ;; Competition
referred ;; Adducing || cyclone ;; Catastrophe || traveled ;; Traveling
emerged ;; Coming_to_be || Forming ;; Coming_to_be || passed ;; Motion_directional
made ;; Manufacturing || arrived at ;; Arriving
emerged ;; Coming_to_be || crossed ;; Motion_directional
struggling ;; Hostile_encounter || intensifying ;; Cause_change_of_strength || Being ;; Becoming
made ;; Manufacturing || weakened ;; Cause_change_of_strength || caused ;; Causation || entered ;; Arriving
fought ;; Hostile_encounter || control ;; Control || known ;; Know || thought ;; Know
fought ;; Hostile_encounter || referred ;; Adducing
wars ;; Hostile_encounter || fought ;; Hostile_encounter || lost ;; Earnings_and_losses
supplies ;; Supply || shipping ;; Sending || faced ;; Confronting_problem
communications ;; Communication || faced ;; Confronting_problem || coupled ;; Collaboration
turned ;; Becoming || disputed ;; Quarreling || control ;; Control || income ;; Earnings_and_losses || end ;; Process_end || better-equipped ;; Bearing_arms
disputed ;; Quarreling || granted ;; Preventing_or_letting
injuring ;; Bodily_harm || crashed ;; Catastrophe || killing ;; Killing || takeoff ;; Self_motion
take-off ;; Self_motion || attempted ;; Hold
control ;; Control || came back ;; Motion || maintain ;; Hold
impacted ;; Influence || killing ;; Killing || caught ;; Being_in_operation
found ;; Know
found ;; Know || cited ;; Adducing || cause ;; Causation || take off ;; Departing || contributing ;; Giving
told ;; Telling || refused ;; Agree_or_refuse_to_act || refuse ;; Agree_or_refuse_to_act || takeoff ;; Self_motion || due ;; Causation || take off ;; Departing || take off ;; Departing || due ;; Causation
taken ;; Conquering || hold ;; Defending || takeoff ;; Self_motion || preceded ;; Departing
controls ;; Control || took ;; Conquering || lost ;; Earnings_and_losses || control ;; Control || let ;; Preventing_or_letting || get ;; Getting || save ;; Rescuing
organized ;; Arranging || festival ;; Social_event
announced ;; Expressing_publicly || Starting ;; Process_start
focuses ;; Aiming || ranged ;; Adducing || festival ;; Social_event
influenced ;; Influence || started ;; Process_start || took on ;; Conquering || keeping ;; Temporary_stay
includes ;; Cause_to_be_included
organized ;; Arranging || managed ;; Control
citing ;; Adducing || started ;; Process_start || ran ;; Motion || cancelled ;; Change_event_time
cancel ;; Change_event_time || diagnosed ;; Check || prompting ;; Causation || show ;; Social_event
rescheduled ;; Arranging
received ;; Receiving || sold ;; Commerce_sell || shows ;; Social_event
concert ;; Social_event
utilized ;; Using || sets ;; Placing || tour ;; Traveling
serving ;; Assistance || show ;; Social_event || structured ;; Arranging
used ;; Using
used ;; Using || placed ;; Placing
used ;; Using || tour ;; Traveling
began ;; Process_start || ended ;; Process_end || announcement ;; Expressing_publicly || tour ;; Traveling || tour ;; Traveling || Including ;; Cause_to_be_included
sold ;; Commerce_sell
became ;; Becoming || died ;; Death
became ;; Becoming
died ;; Death
followed ;; Action
committed ;; Commitment
evolved ;; Change
called ;; Name_conferral
refer ;; Adducing
ordered ;; Request
resulted in ;; Causation
connected ;; Connect || operating ;; Being_in_operation || involves ;; Cause_to_be_included || Insurgency ;; Violence
autonomy ;; Change_of_leadership
independence ;; Change_of_leadership || demand ;; Request
consists ;; Cause_to_be_included
exist ;; Presence || Tensions ;; Hostile_encounter
raise ;; Cause_change_of_position_on_a_scale || making ;; Manufacturing || tensions ;; Hostile_encounter || eased ;; Cause_change_of_position_on_a_scale
rose ;; Cause_change_of_position_on_a_scale || attack ;; Attack || tensions ;; Hostile_encounter || offensive ;; Attack || launched ;; Process_start || led to ;; Causation
conducted ;; Action
election ;; Change_of_leadership
shows ;; Communication || claim ;; Statement
terrorist ;; Terrorism || listed ;; Adducing || insurgency ;; Violence || promote ;; Cause_to_make_progress
organised ;; Arranging
competition ;; Competition
watched ;; Perception_active
contest ;; Competition || produced ;; Manufacturing
chosen ;; Choosing
joined ;; Becoming_a_member || vote ;; Choosing
impact ;; Influence || makes ;; Manufacturing
raised ;; Cause_change_of_position_on_a_scale
began ;; Process_start
used ;; Using || referred ;; Adducing
expansion ;; Expansion || made ;; Manufacturing
tourism ;; Traveling || attracted ;; Influence
massacre ;; Killing || massacre ;; Killing || referred ;; Adducing || dead ;; Death || attack ;; Terrorism
assault ;; Attack || support ;; Supporting
revealed ;; Reveal_secret || attack ;; Terrorism
helped ;; Assistance || say ;; Statement || assault ;; Attack || direct ;; Action
attack ;; Attack || carried out ;; Attack
said ;; Statement || get ;; Removing || shooting ;; Attack
died ;; Death || killed ;; Killing
linked ;; Connect || led to ;; Causation
made ;; Causation
point to ;; Reporting || noted ;; Know
died ;; Death || injured ;; Bodily_harm
assault ;; Attack || assault ;; Attack || break up ;; Destroying
displacement ;; Change
assault ;; Attack || cited ;; Adducing
attack ;; Attack || read ;; Statement || enforcement ;; Action
found ;; Know || procedure ;; Statement || illegal ;; Legality || allowed ;; Preventing_or_letting || served ;; Assistance
hinder ;; Hindering || forbidden ;; Preventing_or_letting
given ;; Giving || informed ;; Telling
assault ;; Attack || identified ;; Check
began ;; Process_start || spread ;; Dispersal
claimed ;; Statement
evacuated ;; Removing
appeared ;; Coming_to_be
told ;; Telling || started ;; Process_start
argued ;; Quarreling || started ;; Process_start
extinguished ;; Killing
referred ;; Adducing
help ;; Assistance || mobilized ;; Arranging
defeated ;; Conquering
took place ;; Process_start || election ;; Choosing
took place ;; Process_start
took place ;; Process_start
took place ;; Process_start
cited ;; Adducing
gathered ;; Come_together
reported ;; Reporting
participated ;; Participation
wars ;; Hostile_encounter || War ;; Hostile_encounter || conquest ;; Conquering || battles ;; Hostile_encounter
starting ;; Process_start || began ;; Process_start || use ;; Using || used ;; Using || take ;; Creating || establishing ;; Building || conquest ;; Conquering || ending ;; Process_end || signing ;; Sign_agreement
used ;; Using || War ;; Hostile_encounter || refers ;; Adducing
offered ;; Giving || ended up ;; Process_end || end ;; Process_end || accept ;; Response || emigrated ;; Escaping
ended up ;; Process_end
agree ;; Agree_or_refuse_to_act
deported ;; Removing
died ;; Death || transit ;; Sending
remained ;; Presence || settled ;; Removing
refers ;; Adducing || known ;; Know
flowing ;; Motion
united ;; Cause_to_amalgamate
attacks ;; Attack || struck ;; Attack
begin ;; Process_start
constructed ;; Building || end ;; Process_end
left ;; Departing
triggered ;; Causation
caused ;; Causation
took ;; Action
arrested ;; Arrest || detained ;; Arrest
allowed ;; Preventing_or_letting
referred ;; Adducing
included ;; Cause_to_be_included
arrested ;; Arrest || detained ;; Arrest
retired ;; Process_end || remained ;; Presence
earthquake ;; Catastrophe || occurred ;; Coming_to_be
earthquake ;; Catastrophe || earthquake ;; Catastrophe || Earthquake ;; Catastrophe || Earthquake ;; Catastrophe || referred ;; Adducing
began ;; Process_start || earthquake ;; Catastrophe || earthquake ;; Catastrophe || recorded ;; Recording || powerful ;; Influence || record-keeping ;; Statement
earthquake ;; Catastrophe || reached ;; Arriving || triggered ;; Causation || traveled ;; Motion
killed ;; Killing || washed ;; Removing
moved ;; Motion || increased ;; Cause_change_of_position_on_a_scale || earthquake ;; Catastrophe || shifted ;; Motion || estimates ;; Scrutiny || generated ;; Creating || detected ;; Know
caused ;; Causation || earthquake ;; Catastrophe || sinking ;; Motion || exceed ;; Cause_change_of_position_on_a_scale || kept on rising ;; Self_motion
deaths ;; Death || killed ;; Killing || drowning ;; Bodily_harm || caused ;; Causation
due ;; Causation || housing ;; Building || indicated ;; Statement
buildings ;; Building || damaged ;; Damaging || buildings ;; Building || listed ;; Statement || collapsed ;; Motion_directional
caused ;; Causation || damage ;; Damaging || earthquake ;; Catastrophe || damage ;; Damaging || fires ;; Attack
said ;; Telling || toughest ;; Confronting_problem || crisis ;; Catastrophe
left ;; Departing
caused ;; Causation || affecting ;; Influence || associated ;; Motion
built ;; Building || suffered ;; Catastrophe || explosions ;; Destroying || resulting ;; Causation
evacuated ;; Emptying
losses ;; Earnings_and_losses
offered ;; Giving
disaster ;; Catastrophe || estimated ;; Statement
decide ;; Deciding || conference ;; Social_event || conference ;; Social_event
defeated ;; Conquering || tied ;; Connect || winning ;; Getting
conference ;; Social_event || played ;; Competition || held ;; Defending
played ;; Participation || played ;; Competition
defense ;; Defending || faced ;; Perception_active
referred ;; Adducing
became ;; Becoming
posting ;; Sending || made ;; Manufacturing
advanced ;; Cause_to_make_progress
due ;; Causation || referred ;; Adducing
came ;; Motion
defense ;; Defending
record ;; Recording || threw ;; Self_motion
scored ;; Getting || build ;; Building
named ;; Name_conferral
became ;; Becoming || defensive ;; Defending || named ;; Name_conferral
joint ;; Collaboration
striking ;; Attack || derailed ;; Self_motion
advised ;; Convincing || crossing ;; Motion_directional || reducing ;; Cause_change_of_position_on_a_scale || use ;; Using
found ;; Know || reaching ;; Arriving || stop ;; Process_end
brake ;; Self_motion
completed ;; Process_end || made ;; Manufacturing || pointed out ;; Reporting
attacked ;; Attack || victory ;; Getting
supported ;; Supporting
claimed ;; Statement || launched ;; Military_operation
commenced ;; Process_start || declared ;; Expressing_publicly
storm ;; Damaging || change ;; Change
gained ;; Getting || built ;; Building || made ;; Causation || created ;; Creating
settle ;; Resolve_problem || followed ;; Motion
led to ;; Causation || seizing ;; Hold || attack ;; Attack
defend ;; Defending || made it ;; Causation
thrust ;; Motion
defeated ;; Earnings_and_losses || pinned ;; Motion
relied ;; Supply || intervene ;; Hindering
signed ;; Writing || advise ;; Convincing || train ;; Preventing_or_letting || provide ;; Supply || played ;; Participation
suffered ;; Bodily_harm || killed ;; Killing || killed ;; Killing
killed ;; Killing
captured ;; Conquering
cite ;; Adducing || claims ;; Statement || killed ;; Killing
held ;; Hold || committed ;; Commitment
joint ;; Collaboration || designed ;; Creating
stated ;; Statement || committed ;; Commitment
adopted ;; Choosing || prevent ;; Preventing_or_letting
committed ;; Commitment || reported ;; Reporting || included ;; Cause_to_be_included
sued ;; Legal_rulings
fled ;; Escaping || found ;; Know || took place ;; Process_start || affirmed ;; Response || ruled ;; Legal_rulings
found ;; Know || committed ;; Commitment
convicted ;; Legal_rulings
conducted ;; Action || raids ;; Attack || destruction ;; Destroying || killing ;; Killing || causing ;; Causation || War ;; Military_operation
raids ;; Attack || attacks ;; Attack || positions ;; Placing || limited ;; Limiting || Raid ;; Attack || War ;; Military_operation
raids ;; Attack || began ;; Process_start || bombing ;; Attack || end ;; Process_end || war ;; Military_operation
attacked ;; Attack
began ;; Process_start || campaign ;; Military_operation || intensified ;; Cause_change_of_strength || war ;; Military_operation
ready ;; GetReady || attacks ;; Attack || begin ;; Process_start || plans ;; Arranging || prepared ;; GetReady || War ;; Military_operation
raids ;; Attack || make ;; Creating || staged ;; Motion_directional
became ;; Becoming || campaign ;; Military_operation || expanded ;; Expansion || bombing ;; Attack || result ;; Causation || Campaign ;; Military_operation
attacks ;; Attack || bombing ;; Attack || using ;; Using || target ;; Aiming
manufacturing ;; Manufacturing || switched ;; Change || damage ;; Damaging || resulted in ;; Causation || carried out ;; Action || approach ;; Arriving
struck ;; Attack || planned ;; Arranging || scheduled ;; Arranging || invasion ;; Attack
struck ;; Attack || destroyed ;; Destroying
attacks ;; Attack || military ;; Military_operation || stop ;; Preventing_or_letting
defensive ;; Defending || operated ;; Control || assigned ;; Choosing
coordination ;; Collaboration || constrained ;; Hindering || shortages ;; Causation
attacks ;; Attack || raid ;; Attack || services ;; Assistance || constructed ;; Building
losses ;; Earnings_and_losses || damage ;; Damaging || result ;; Causation || inflict ;; Causation || suffering ;; Confronting_problem
decision ;; Deciding || campaign ;; Military_operation || bombing ;; Attack || surrender ;; Surrendering || influenced ;; Influence
attacks ;; Attack || debate ;; Quarreling || use ;; Using
raids ;; Attack || killed ;; Killing || wounded ;; Bodily_harm || cited ;; Adducing || casualties ;; Catastrophe
fatalities ;; Catastrophe
raids ;; Attack || loss ;; Earnings_and_losses || decline ;; Cause_change_of_position_on_a_scale || production ;; Creating || contributed ;; Assistance
aimed ;; Aiming || insurrection ;; Change_of_leadership
began ;; Process_start || captured ;; Conquering
conflict ;; Hostile_encounter || shift ;; Change || provoked ;; Causation
uprising ;; Change_of_leadership || rendered ;; Causation
encouraged ;; Influence
encouraged ;; Influence || plan ;; Arranging || strike ;; Attack || weakened ;; Cause_change_of_strength
plans ;; Arranging || got ;; Getting
costs ;; Cost || coming to ;; Motion || avoid ;; Preventing_or_letting || aware ;; Know
brought ;; Bringing || derail ;; Hindering
avoid ;; Preventing_or_letting || decision ;; Deciding || Uprising ;; Change_of_leadership || triggered ;; Causation
joined ;; Becoming_a_member
support ;; Supporting || forced ;; Causation
abandon ;; GiveUp || struggle ;; Hostile_encounter || persuaded ;; Convincing
hit ;; Attack || decision ;; Deciding || abolish ;; Removing
turn ;; Change || work ;; Employment || break-up ;; Hold || convinced ;; Convincing
earthquake ;; Catastrophe || occurred ;; Coming_to_be
devastated ;; Destroying || earthquake ;; Catastrophe || centred ;; Placing
magnitude ;; Catastrophe || quake ;; Self_motion || preceded ;; Process_start || struck ;; Attack
reported ;; Reporting
produced ;; Manufacturing || registering ;; Recording || shaking ;; Body_movement
caused ;; Causation || destroyed ;; Destroying || damage ;; Damaging || affected ;; Influence
dismantling ;; Destroying || damaged ;; Damaging || completed ;; Process_end || work ;; Employment
cut off ;; Removing || downed ;; Self_motion
costs ;; Cost || following ;; Self_motion || earthquake ;; Catastrophe || damage ;; Damaging
suffered ;; Bodily_harm || died ;; Death || knocked ;; Body_movement
occurred ;; Coming_to_be || explosion ;; Attack
killed ;; Killing || injured ;; Bodily_harm
destroyed ;; Destroying || leaving ;; Departing || damage ;; Damaging
explosion ;; Attack || disabled ;; Destroying || effects ;; Influence || felt ;; Change_sentiment
damaged ;; Damaging || disrupting ;; Hindering
ruptured ;; Destroying || forced ;; Request
conducted ;; Action || initiated ;; Process_start || investigation ;; Criminal_investigation || operating ;; Being_in_operation || reaction ;; Response || explosion ;; Attack
conducted ;; Action
cost ;; Cost || loss ;; Earnings_and_losses || increase ;; Cause_change_of_position_on_a_scale || contributed ;; Supply
Hurricane ;; Catastrophe || hurricane ;; Catastrophe
moved ;; Motion || Forming ;; Coming_to_be
observed ;; Perception_active
attained ;; Getting || hurricane ;; Catastrophe || response ;; Response || turned ;; Becoming
moving ;; Motion || hurricane ;; Catastrophe || reached ;; Arriving
hurricane ;; Catastrophe || weakened ;; Cause_change_of_strength || maintained ;; Preserving || dissipating ;; Removing
hurricane ;; Catastrophe || prompted ;; Causation || resulting in ;; Causation
caused ;; Causation || produced ;; Creating
struck ;; Attack || hurricane ;; Catastrophe || damaged ;; Damaging
totaled ;; Cost
produced ;; Creating
operating ;; Being_in_operation
terrorists ;; Terrorism || planted ;; Placing || flight ;; Self_motion || explosion ;; Catastrophe || bomb ;; Terrorism || serving ;; Supply || disintegrated ;; Destroying
killing ;; Killing || fell ;; Motion_directional
killing ;; Killing || accident ;; Catastrophe || terrorism ;; Terrorism || act ;; Action || bombing ;; Terrorism || attacks ;; Terrorism
bombing ;; Terrorism || bombing ;; Terrorism
linked ;; Connect || aiming ;; Aiming || bombing ;; Terrorism || believe ;; Know
bomb ;; Terrorism || loaded ;; Filling || exploded ;; Attack
arrested ;; Arrest || bombing ;; Terrorism || convicted ;; Legal_rulings || pleaded ;; Request
investigation ;; Criminal_investigation || prosecution ;; Legal_rulings || lasted ;; Process_end
expensive ;; Cost || costing ;; Cost || trial ;; Criminal_investigation
conduct ;; Action || appointed ;; Deciding
allowed ;; Preventing_or_letting || terrorist ;; Terrorism || attack ;; Attack || take place ;; Process_start || report ;; Reporting || service ;; Supply || completed ;; Process_end || released ;; Publishing || concluded ;; Coming_to_believe
held ;; Defending
commences ;; Process_start
held ;; Defending
surrounding ;; Surrounding || comprise ;; Cause_to_be_included || census-designated ;; Social_event
competitive ;; Competition
cost ;; Cost || pass ;; Motion_directional
support ;; Supporting || tour ;; Traveling
support ;; Supporting || tour ;; Traveling
marked ;; Recording || documented ;; Recording || concert ;; Social_event || toured ;; Traveling || released ;; Publishing
lost ;; Earnings_and_losses || expenses ;; Cost || tour ;; Traveling || renting ;; Renting
got ;; Getting || asking ;; Request || working ;; Employment || come ;; Motion
due ;; Causation || loss ;; Earnings_and_losses || declined ;; Agree_or_refuse_to_act || show ;; Social_event || filled ;; Filling
interview ;; Communication || recalled ;; Statement || tour ;; Traveling
used ;; Using || tour ;; Traveling
married ;; Forming_relationships || tour ;; Traveling || missed ;; Earnings_and_losses
debuted ;; Process_start
races ;; Competition || running ;; Self_motion || bicycling ;; Self_motion
found ;; Know || cost ;; Cost
estimated ;; Statement
fought ;; Hostile_encounter
led to ;; Causation || pitted ;; Hostile_encounter
revolted ;; Change_of_leadership
fomenting ;; Causation || led to ;; Causation
ravaging ;; Destroying || convinced ;; Convincing
call ;; Request
sent ;; Sending || fight ;; Hostile_encounter
sent ;; Sending || cost ;; Cost
break ;; Destroying || defend ;; Defending
Battle ;; Hostile_encounter || raid ;; Attack || failed ;; Earnings_and_losses || forces ;; Military_operation || War ;; Hostile_encounter
recognized ;; Know || planned ;; Arranging || attack ;; Attack || sailed ;; Self_motion
request ;; Request || support ;; Supporting || assault ;; Attack || planned ;; Arranging || carried ;; Carry_goods || commanding ;; Control
raid ;; Attack || retreat ;; Escaping || costing ;; Cost || suffered ;; Catastrophe || effect ;; Influence
wounded ;; Bodily_harm || dead ;; Death || campaign ;; Military_operation || captured ;; Conquering
accused ;; Judgment_communication || raid ;; Attack || exonerated ;; Removing
affect ;; Influence || disaster ;; Catastrophe
noted ;; Know || located ;; Know
intensified ;; Cause_change_of_strength || started ;; Process_start || storm ;; Catastrophe || storm ;; Catastrophe || intensify ;; Cause_change_of_strength || becoming ;; Becoming
storm ;; Catastrophe || weakened ;; Cause_change_of_strength
dissipated ;; Dispersal
impact ;; Influence || killed ;; Killing || reported ;; Reporting
damages ;; Damaging || died ;; Death || cost ;; Cost
championship ;; Competition || involved ;; Participation || Cup ;; Competition
games ;; Competition || Tournament ;; Competition
win ;; Earnings_and_losses || game ;; Competition || result ;; Causation || defeated ;; Conquering || final ;; Competition
named ;; Name_conferral
tournament ;; Competition || scoring ;; Getting
postponed ;; Change_event_time || scheduled ;; Arranging || disputes ;; Quarreling || due ;; Causation
postponed ;; Change_event_time || invasion ;; Military_operation || boycott ;; Agree_or_refuse_to_act || events ;; Competition || result ;; Causation || following ;; Motion
speculated ;; Coming_to_believe || attendance ;; Participation || event ;; Competition || costs ;; Cost || tournament ;; Competition || due ;; Causation || rising ;; Cause_change_of_position_on_a_scale
refused ;; Agree_or_refuse_to_act || allow ;; Preventing_or_letting || controversy ;; Judgment_communication || generated ;; Creating
sparked ;; Creating || revolution ;; Social_event || conflict ;; Hostile_encounter
resulted in ;; Causation
began ;; Process_start || launched ;; Military_operation
elimination ;; Removing || armed ;; Bearing_arms || pursued ;; Aiming
arrested ;; Arrest || elections ;; Choosing
ended ;; Process_end || surrendered ;; Surrendering
suppress ;; Preventing_or_letting || election ;; Change_of_leadership || operations ;; Being_in_operation || strikes ;; Attack
raids ;; Attack || assist ;; Assistance || created ;; Creating
support ;; Supporting
murder ;; Killing || deportation ;; Removing || rape ;; Committing_crime
massacres ;; Killing
fled ;; Escaping || displaced ;; Cause_change_of_position_on_a_scale
violence ;; Violence || broke out ;; Process_start
committed ;; Commitment || prevails ;; Coming_to_believe
proclaimed ;; Statement || formed ;; Coming_to_be
played ;; Participation
waged ;; Hold || Led ;; Causation
liberated ;; Besieging
regained ;; Getting
carried out ;; Action
flew ;; Escaping
restricted ;; Preventing_or_letting
secured ;; Getting
formed ;; Coming_to_be || moved ;; Motion
defected ;; Escaping
escaped ;; Escaping || interned ;; Arrest
operated ;; Being_in_operation || free ;; Cost
caused ;; Causation
provided ;; Supply || led ;; Causation || support ;; Supporting
organised ;; Arranging || support ;; Supporting
led ;; Causation || dissented ;; Judgment_communication
joined ;; Participation
achieved ;; Achieve || surrendered ;; Surrendering
changed ;; Change || emergence ;; Process_start
recognised ;; Know
festival ;; Social_event
organized ;; Arranging
performed ;; Hold
cost ;; Cost
recorded ;; Recording || exceeded ;; Cause_change_of_position_on_a_scale
brought ;; Bringing
complained ;; Judgment_communication || noted ;; Know
carried out ;; Attack || attack ;; Attack || bombing ;; Attack
detonated ;; Attack
caused ;; Causation || targeted ;; Aiming || bombing ;; Attack || attacks ;; Attack
sent ;; Sending || warnings ;; Warning || detonated ;; Attack
defuse ;; Resolve_problem || evacuated ;; Emptying
evacuating ;; Emptying || injured ;; Bodily_harm
taken place ;; Process_start || hosting ;; Social_event
targeted ;; Aiming || attack ;; Attack
attack ;; Attack || ended ;; Process_end
condemned ;; Judgment_communication || bombing ;; Attack
causing ;; Causation || claimed ;; Statement || issued ;; Creating || statement ;; Statement
demolished ;; Destroying || damaged ;; Damaging || explosion ;; Attack || closed ;; Process_end
cost ;; Cost || completed ;; Process_end
charged ;; Arrest || conceded ;; Statement || bombing ;; Attack || attack ;; Attack
starting ;; Process_start || turning ;; Becoming || bombing ;; Attack || bombing ;; Attack
raid ;; Attack || took place ;; Process_start
conducted ;; Action || raid ;; Attack || reduce ;; Cause_change_of_position_on_a_scale
attack ;; Attack
cost ;; Cost || reduced ;; Cause_change_of_position_on_a_scale || claimed ;; Statement
postponed ;; Change_event_time || rebuild ;; Recovering
modified ;; Change || rebuilt ;; Building
added ;; Cause_to_be_included || sent ;; Sending || cover ;; Filling
combined ;; Cause_to_amalgamate || proved ;; Convincing
sent ;; Sending || lost ;; Earnings_and_losses || damaged ;; Damaging || scrapped ;; GiveUp
lost ;; Earnings_and_losses
lost ;; Earnings_and_losses || return ;; Arriving
provide ;; Supply
exposed ;; Reveal_secret || attacks ;; Attack
flawed ;; Hindering
questioned ;; Communication || claimed ;; Statement
refused ;; Agree_or_refuse_to_act || cooperate ;; Collaboration
provided ;; Supply
built ;; Building || receiving ;; Receiving
achieved ;; Achieve
assembled ;; Come_together
performing ;; Hold || crashed ;; Catastrophe
killed ;; Killing || making ;; Causation
cost ;; Cost || investigation ;; Criminal_investigation || rescue ;; Rescuing || response ;; Response || took ;; Creating || search ;; Scrutiny
carried out ;; Military_operation || investigation ;; Criminal_investigation || allowed ;; Preventing_or_letting || resulting in ;; Causation || crash ;; Catastrophe || spread ;; Dispersal
made ;; Manufacturing || incorporated ;; Cause_to_amalgamate
carried ;; Carry_goods
battles ;; Military_operation || War ;; Military_operation || Battle ;; Hostile_encounter
cost ;; Cost || caused ;; Causation || lose ;; Earnings_and_losses || battle ;; Military_operation || losses ;; Earnings_and_losses || victory ;; Earnings_and_losses
wounded ;; Bodily_harm
became ;; Becoming || open ;; Openness || loss ;; Earnings_and_losses || meant ;; Causation || entry ;; Process_start || war ;; Military_operation || leading to ;; Causation || founding ;; Creating
lay ;; Placing || battle ;; Hostile_encounter
used ;; Using || describe ;; Statement
causing ;; Causation || Cyclone ;; Catastrophe || cyclone ;; Catastrophe || affected ;; Influence
intensified ;; Cause_change_of_strength
deepened ;; Cause_change_of_strength || cyclone ;; Catastrophe || classified ;; Check
strengthened ;; Cause_change_of_strength || intensified ;; Cause_change_of_strength || cyclone ;; Catastrophe || cyclone ;; Catastrophe || Cyclone ;; Catastrophe
continued ;; Process_start || track ;; Self_motion || maintaining ;; Preserving
crossed ;; Motion_directional || spanning ;; Self_motion
passed ;; Motion_directional
crossed ;; Motion_directional || maintained ;; Preserving || dissipating ;; Dispersal
caused ;; Causation || cyclone ;; Catastrophe || hit ;; Attack || accounting ;; Causation
died ;; Death
moved ;; Motion || Cyclone ;; Catastrophe
passed ;; Motion_directional || damage ;; Damaging || suffered ;; Bodily_harm
cost ;; Cost || put ;; Causation
cause ;; Causation
destroy ;; Destroying
cost ;; Cost || destroyed ;; Destroying
left ;; Causation
beginning ;; Process_start || given ;; Giving
attacked ;; Attack
attack ;; Attack
took ;; Conquering || reached ;; Arriving || captured ;; Conquering
suffered ;; Bodily_harm || attack ;; Attack || making ;; Manufacturing
captured ;; Conquering || captured ;; Conquering || failed ;; Earnings_and_losses || success ;; Achieve
recaptured ;; Conquering || counter-attacks ;; Attack || lost ;; Earnings_and_losses || attacks ;; Attack
attack ;; Attack
advance ;; Cause_to_make_progress
began ;; Process_start || resisting ;; Defending || defeats ;; Conquering || defence ;; Defending || attack ;; Attack || abandoned ;; GiveUp
observed ;; Know || suffered ;; Bodily_harm || killed ;; Killing
suffered ;; Bodily_harm || destroyed ;; Destroying || issued ;; Creating || took ;; Creating
cost ;; Cost || writing ;; Writing
fought ;; Hostile_encounter
fought ;; Hostile_encounter
began ;; Process_start || developing ;; Coming_to_be || landing ;; Self_motion || strength ;; Cause_change_of_strength || break out ;; Destroying || lift ;; Body_movement
cut off ;; Process_end || resupply ;; Supply || faced ;; Perception_active
reached ;; Arriving || allowed ;; Preventing_or_letting || make ;; Manufacturing || exit ;; Departing
managed ;; Control || assassinated ;; Killing || cost ;; Cost || signing ;; Sign_agreement
fought ;; Hostile_encounter || wars ;; Hostile_encounter || led to ;; Causation
declared ;; Expressing_publicly || fueling ;; Supply || wars ;; Hostile_encounter
ended ;; Process_end || cost ;; Cost || damage ;; Damaging || wars ;; Hostile_encounter || involving ;; Cause_to_be_included
preserve ;; Preserving || preserve ;; Preserving || crushing ;; Destroying || use ;; Using || evoked ;; Causation
began ;; Process_start || became ;; Becoming || lose ;; Earnings_and_losses
aim ;; Aiming || create ;; Creating || report ;; Reporting || restore ;; Recovering
abandoned ;; GiveUp || ended ;; Process_end || brought ;; Bringing || wars ;; Hostile_encounter || following ;; Self_motion || agreement ;; Agree_or_refuse_to_act
described ;; Statement || marked ;; Recording || wars ;; Hostile_encounter || conflicts ;; Hostile_encounter || crimes ;; Committing_crime
crime ;; Committing_crime || judged ;; Legal_rulings
established ;; Building || prosecute ;; Criminal_investigation
death ;; Death
killed ;; Killing || conflicts ;; Hostile_encounter
start ;; Process_start || Tour ;; Traveling
announced ;; Expressing_publicly || Concert ;; Social_event || tour ;; Traveling
confirmed ;; Check || announced ;; Expressing_publicly || tour ;; Traveling
sale ;; Commerce_sell || started ;; Process_start || pre-sale ;; Cost || started ;; Process_start || tickets ;; Rewards_and_punishments
added ;; Cause_to_be_included
postponed ;; Change_event_time || concerts ;; Social_event
held ;; Hold
developed ;; Cause_to_make_progress || promoting ;; Cause_to_make_progress
organised ;; Arranging || prevent ;; Preventing_or_letting || free ;; Cost || used ;; Using
present ;; Presence || behaved ;; Action
selling ;; Commerce_sell
caused ;; Causation || occurred ;; Presence || collapse ;; Destroying || repaired ;; Building
received ;; Receiving || put on ;; Placing
Battle ;; Hostile_encounter
fought ;; Hostile_encounter
made ;; Manufacturing
began ;; Process_start || defence ;; Defending
command ;; Control || designed ;; Creating
giving ;; Giving || concentrated ;; Aiming
implemented ;; Action || launched ;; Process_start
repulsed ;; Defending || consisted ;; Forming_relationships
stationed ;; Placing
carried out ;; Action || resumed ;; Process_start
Supported ;; Supporting || crossed ;; Motion_directional
retreating ;; Escaping || kept ;; Temporary_stay || capturing ;; Conquering
cost ;; Cost || due ;; Causation
captured ;; Conquering
floods ;; Catastrophe || hit ;; Attack
floods ;; Catastrophe || evacuation ;; Emptying || forced ;; Causation
affected ;; Influence
Damage ;; Damaging || estimated ;; Earnings_and_losses || raised ;; Cost
reduction ;; Cause_change_of_position_on_a_scale
deaths ;; Death || floods ;; Catastrophe
declared ;; Expressing_publicly
flooding ;; Catastrophe || recorded ;; Recording || hit ;; Attack
flood ;; Catastrophe || caused ;; Causation
devastated ;; Destroying || storm ;; Catastrophe
inundated ;; Destroying
assistance ;; Assistance || expressed ;; Expressing_publicly
activated ;; Response || created ;; Creating
rebuilding ;; Building || formed ;; Coming_to_be || established ;; Creating || investigate ;; Scrutiny
hit ;; Attack
floods ;; Catastrophe || floods ;; Catastrophe || grapple ;; Confronting_problem || flooding ;; Catastrophe
attack ;; Attack || Bombing ;; Attack || Battle ;; Hostile_encounter
prevent ;; Preventing_or_letting || using ;; Using || invasion ;; Attack || War ;; Military_operation || raids ;; Attack || attacked ;; Attack || contest ;; Military_operation
defended ;; Defending || attack ;; Attack || losses ;; Earnings_and_losses || forces ;; Causation || inflicted ;; Bodily_harm || cost ;; Cost
raids ;; Attack || damage ;; Damaging || suffered ;; Confronting_problem
attack ;; Attack || left ;; Departing
raids ;; Attack || raids ;; Attack
defense ;; Defending || war ;; Hostile_encounter || battle ;; Hostile_encounter
became ;; Becoming || target ;; Aiming || fell ;; Change_of_leadership
attacking ;; Attack || consisting ;; Cause_to_be_included
junction ;; Connect || used ;; Using || Flying ;; Motion || bombing ;; Attack
taken ;; Conquering || costs ;; Cost || ordered ;; Request
received ;; Receiving || defense ;; Defending || attack ;; Attack || started ;; Process_start || siege ;; Besieging || take ;; Conquering || orders ;; Request
participation ;; Participation || codename ;; Name_conferral
aimed ;; Aiming || enforcing ;; Action || protect ;; Defending || coalition ;; Cause_to_amalgamate
designated ;; Choosing
participation ;; Participation
confirmed ;; Check || cost ;; Cost || met ;; Come_together
prevent ;; Preventing_or_letting || attacks ;; Attack
prepared ;; GetReady
designating ;; Choosing || spelling ;; Writing
found ;; Know
name ;; Name_conferral
provided ;; Supply || transport ;; Military_operation || undertaken ;; Action
secure ;; Assistance
supported ;; Supporting
prevent ;; Preventing_or_letting || capture ;; Conquering || reinforcing ;; Cause_change_of_strength
added ;; Cause_to_be_included || invasion ;; Attack
changes ;; Change || invasion ;; Attack || necessitated ;; Causation || delay ;; Change_event_time || landing ;; Self_motion || assembled ;; Come_together || doubled ;; Cause_change_of_position_on_a_scale
attacking ;; Attack || faced ;; Confronting_problem
equipped ;; Supply || beginning ;; Process_start || assigned ;; Choosing || undertaken ;; Action || defend ;; Defending
began ;; Process_start || arrived ;; Arriving || controlling ;; Control || advance ;; Cause_to_make_progress
fulfill ;; Achieve || met ;; Come_together || landed ;; Self_motion
casualties ;; Catastrophe || beginning ;; Process_start || secured ;; Assistance || landed ;; Self_motion
clearing ;; Bringing || arrive ;; Arriving || set ;; Self_motion
captured ;; Conquering || planned ;; Arranging || remained ;; Presence
cost ;; Cost || casualties ;; Catastrophe || landed ;; Motion
casualties ;; Catastrophe || arriving ;; Arriving || numbered ;; Cause_change_of_position_on_a_scale
lost ;; Death
losses ;; Earnings_and_losses
captured ;; Conquering || destroyed ;; Destroying || brought ;; Bringing
Hurricane ;; Catastrophe
killed ;; Killing || striking ;; Bodily_harm
raising ;; Cause_change_of_position_on_a_scale || storm ;; Catastrophe || causing ;; Causation || struck ;; Bodily_harm
death ;; Causation || damage ;; Causation
affecting ;; Influence || destroyed ;; Destroying
destroying ;; Destroying || made ;; Manufacturing || hurricane ;; Catastrophe || landfall ;; Motion
traveled ;; Motion
caused ;; Causation || affected ;; Influence || brought ;; Bringing || damage ;; Damaging
consolidated ;; Cause_to_amalgamate || turned ;; Motion
caused ;; Causation || storm ;; Catastrophe || hit ;; Attack || overflowed ;; Filling || flooding ;; Catastrophe
converted ;; Change
damage ;; Damaging || incurred ;; Cost
effects ;; Influence || storms ;; Catastrophe || traveling ;; Traveling || storm ;; Catastrophe || combination ;; Causation || dealing ;; Resolve_problem
storm ;; Catastrophe || hurricane ;; Catastrophe || stalled ;; Temporary_stay || remained ;; Temporary_stay
summoned ;; Request || distributed ;; Dispersal || Hurricane ;; Catastrophe || aid ;; Assistance || cleanup ;; Emptying || established ;; Building
festival ;; Social_event || sponsored ;; Supply
edition ;; Publishing
association ;; Collaboration || produced ;; Creating
festival ;; Social_event || Festival ;; Social_event || dedicated ;; Giving
festival ;; Social_event || competing ;; Competition || edition ;; Publishing || happen ;; Presence
decided ;; Deciding || moved ;; Motion || announcement ;; Expressing_publicly
broadcast ;; Being_in_operation
prices ;; Cost
War ;; Hostile_encounter || involving ;; Cause_to_be_included || conflict ;; Hostile_encounter || forces ;; Military_operation
involved ;; Cause_to_be_included || Beginning ;; Process_start || invading ;; Attack || conflict ;; Hostile_encounter
fought ;; Hostile_encounter || thought ;; Coming_to_believe
gain ;; Getting || cost ;; Cost || control ;; Control || conflict ;; Hostile_encounter || served ;; Supply
assisted ;; Assistance || rivals ;; Competition || become ;; Becoming || forces ;; Military_operation
nicknamed ;; Name_conferral || war ;; Hostile_encounter || conflict ;; Hostile_encounter
repelled ;; Defending || capturing ;; Conquering || control ;; Control || portugal ;; Motion
war ;; Hostile_encounter || benefited ;; Earnings_and_losses
final ;; Process_end || Battle ;; Hostile_encounter || movement ;; Hostile_encounter || took place ;; Process_start
conducted ;; Action || consisting ;; Cause_to_be_included
started ;; Process_start || advancing ;; Cause_to_make_progress || flooded ;; Motion
cost ;; Cost
delayed ;; Change_event_time || allowed ;; Preventing_or_letting || forces ;; Military_operation || concentrated ;; Aiming || advance ;; Motion
fighting ;; Hostile_encounter
linked ;; Connect || control ;; Control || renewed ;; Recovering
retain ;; Preserving || Fighting ;; Hostile_encounter || evacuate ;; Escaping
withdrawal ;; Escaping || ended ;; Process_end || destroyed ;; Destroying
fought ;; Hostile_encounter || War ;; Hostile_encounter || Campaign ;; Military_operation || Battle ;; Hostile_encounter
invaded ;; Attack || Confederate ;; Collaboration
surrounded ;; Surrounding || bombarded ;; Attack || captured ;; Conquering || cost ;; Cost || victory ;; Getting
planned ;; Arranging || capture ;; Conquering || supply ;; Supply || advanced ;; Motion
chose ;; Choosing || sent ;; Sending || converge ;; Come_together || attack ;; Attack || outnumbering ;; Expansion || dividing ;; Cause_to_amalgamate || pursued ;; Self_motion
taking up commanding positions ;; Temporary_stay || keeping ;; Temporary_stay || surrounding ;; Surrounding
ensued ;; Coming_to_be || approaching ;; Arriving || defenses ;; Defending || encountered ;; Hostile_encounter || skirmishing ;; Hostile_encounter
drove ;; Motion || attacks ;; Attack
defended ;; Defending || fighting ;; Hostile_encounter || Confederate ;; Collaboration || arrived ;; Arriving || see ;; Perception_active
positioned ;; Placing || ordered ;; Request
move ;; Motion || attack ;; Attack || left ;; Departing
positioned ;; Placing
began ;; Process_start || ordered ;; Request
agreed ;; Agree_or_refuse_to_act || raise ;; Cause_change_of_position_on_a_scale || realized ;; Know
died ;; Death || surrender ;; Surrendering || wounded ;; Bodily_harm
rushed ;; Motion || Battle ;; Hostile_encounter || rejoin ;; Becoming_a_member
included ;; Cause_to_be_included || flooding ;; Catastrophe || effects ;; Influence
formed ;; Creating || weakened ;; Cause_change_of_strength || becoming ;; Becoming || struck ;; Attack || hurricane ;; Catastrophe || storm ;; Catastrophe
destroyed ;; Destroying || struck ;; Attack
hurricane ;; Catastrophe || torrential ;; Motion || dropped ;; Motion || washed ;; Removing
caused ;; Causation || impact ;; Influence || widespread ;; Dispersal || flooding ;; Motion
set back ;; Hindering || development ;; Cause_to_make_progress
damaged ;; Damaging || storm ;; Catastrophe || wrecked ;; Destroying || leaving ;; Causation
caused ;; Causation || costs ;; Cost || damage ;; Damaging
exports ;; Sending || ruined ;; Destroying || damage ;; Damaging || due ;; Causation || cut ;; Cause_change_of_position_on_a_scale || storm ;; Catastrophe
damaged ;; Damaging || damaged ;; Damaging
lost ;; Earnings_and_losses || storm ;; Catastrophe
affected ;; Influence || formed ;; Coming_to_be
damaged ;; Damaging || buildings ;; Building || Floods ;; Attack
reported ;; Reporting
requested ;; Request || assistance ;; Assistance || storm ;; Catastrophe
began ;; Process_start || decreasing ;; Cause_change_of_position_on_a_scale || product ;; Manufacturing || end ;; Process_end
enacted ;; Action || widespread ;; Dispersal || order ;; Arranging || storm ;; Catastrophe || restricted ;; Hindering
outbreaks ;; Process_start || faced ;; Confronting_problem
damage ;; Damaging || Due ;; Causation || naming ;; Name_conferral || retired ;; Process_end
concert ;; Social_event || Tour ;; Traveling || Tour ;; Traveling || tour ;; Traveling
Supporting ;; Supporting || tour ;; Traveling || consisted ;; Cause_to_be_included
rejoined ;; Becoming_a_member || tour ;; Traveling || quintet ;; Social_event
become ;; Becoming || tours ;; Traveling
sales ;; Commerce_sell || tour ;; Traveling || tour ;; Traveling || grossing ;; Earnings_and_losses || gross ;; Earnings_and_losses
include ;; Cause_to_be_included || tour ;; Traveling
broke ;; Destroying || sold ;; Commerce_sell || tour ;; Traveling || record ;; Recording || tickets ;; Cost
began ;; Process_start || occupation ;; Control
creating ;; Creating || rule ;; Control || leaving ;; Causation
declared ;; Expressing_publicly || declared ;; Expressing_publicly
requested ;; Request || destroyed ;; Destroying || assistance ;; Assistance || invaded ;; Attack
declared ;; Expressing_publicly
withdrawal ;; Escaping || condemning ;; Judgment_communication || invasion ;; Attack || calling ;; Request
began ;; Process_start || negotiations ;; Communication || recognised ;; Agree_or_refuse_to_act
supported ;; Supporting
caused ;; Causation || invasion ;; Attack || suppression ;; Hindering || harm ;; Damaging
caused ;; Causation || killings ;; Killing || outrage ;; Violence
end ;; Process_end || remained ;; Presence || awarded ;; Award
became ;; Becoming || vote ;; Choosing || resulted in ;; Causation
deaths ;; Death
destroyed ;; Destroying || violence ;; Violence || vote ;; Choosing || undertook ;; Action
departure ;; Departing || establishing ;; Creating || administered ;; Control || prosecute ;; Legality
caused ;; Causation || call ;; Request || limited ;; Limiting
teaches ;; Education_teaching || held ;; Hold
festival ;; Social_event || event ;; Social_event || held ;; Hold
participants ;; Participation || scheduled ;; Arranging || held ;; Hold || establishes ;; Creating
event ;; Social_event || includes ;; Cause_to_be_included
begins ;; Process_start || assistance ;; Assistance || provides ;; Supply || create ;; Creating || Grants ;; Assistance
given out ;; Giving || grants ;; Assistance
added ;; Cause_to_be_included || allow ;; Preventing_or_letting || grant ;; Assistance
grants ;; Assistance
included ;; Cause_to_be_included
events ;; Social_event || includes ;; Cause_to_be_included || dancing ;; Self_motion
burns ;; Bodily_harm || event ;; Social_event
bans ;; Preventing_or_letting
limited ;; Limiting || ban ;; Preventing_or_letting
limited ;; Limiting || produce ;; Creating || moving ;; Motion
allowed ;; Preventing_or_letting
named ;; Name_conferral
formed ;; Coming_to_be
moved ;; Motion || response ;; Response || strengthened ;; Cause_change_of_strength || reaching ;; Arriving
established ;; Building || became ;; Becoming || increase ;; Cause_change_of_position_on_a_scale || encounter ;; Hostile_encounter || turned ;; Becoming
maintained ;; Preserving
discontinue ;; Process_end || prompting ;; Causation
remained ;; Presence || limited ;; Limiting || existence ;; Presence || effects ;; Influence
moving ;; Motion || produced ;; Manufacturing || widespread ;; Dispersal
injuring ;; Bodily_harm || generated ;; Creating
stayed ;; Temporary_stay || causing ;; Causation || enhanced ;; Cause_change_of_strength
brought ;; Bringing || damage ;; Damaging
developed ;; Coming_to_be
moved ;; Motion || decreased ;; Cause_change_of_position_on_a_scale || due ;; Causation || intensify ;; Cause_change_of_strength
reached ;; Arriving
weakening ;; Cause_change_of_strength || re-intensified ;; Cause_change_of_strength
transitioned ;; Change
dropped ;; Motion || passed ;; Motion_directional
affected ;; Influence || dropped ;; Motion
Damage ;; Damaging || limited ;; Limiting || damage ;; Damaging
rain ;; Motion || brought ;; Bringing
Battle ;; Hostile_encounter || fought ;; Hostile_encounter || War ;; Military_operation
attacked ;; Attack
attacked ;; Attack
battle ;; Hostile_encounter || losing ;; Earnings_and_losses
helped ;; Assistance
deserted ;; Escaping || command ;; Control
defeated ;; Conquering
escaped ;; Escaping
signed on ;; Sign_agreement || treaty ;; Sign_agreement
accepted ;; Agree_or_refuse_to_act || treaty ;; Sign_agreement || laid ;; Writing
lost ;; Earnings_and_losses || incorporated ;; Cause_to_amalgamate || conclusion ;; Process_end || war ;; Military_operation
destruction ;; Destroying || battle ;; Hostile_encounter || led to ;; Causation
surrendered ;; Surrendering || consolidate ;; Cause_change_of_position_on_a_scale
abolished ;; Removing || limited ;; Limiting || annexation ;; Change_of_leadership
impacted ;; Influence || Hurricane ;; Catastrophe
developed ;; Coming_to_be || hurricane ;; Catastrophe || hurricane ;; Catastrophe || cyclone ;; Catastrophe
storm ;; Catastrophe || moved ;; Motion
allowed ;; Preventing_or_letting || hurricane ;; Catastrophe || reach ;; Arriving
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || hurricane ;; Catastrophe || storm ;; Catastrophe
became ;; Becoming || hurricane ;; Catastrophe || Intensification ;; Cause_change_of_position_on_a_scale || reaching ;; Arriving
weakened ;; Cause_change_of_strength || brushing ;; Motion || hurricane ;; Catastrophe || attaining ;; Getting
weakening ;; Cause_change_of_strength || occurred ;; Presence || decreased ;; Cause_change_of_position_on_a_scale || made ;; Manufacturing || landfall ;; Arriving || landfall ;; Arriving
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
developing ;; Coming_to_be || damage ;; Damaging || limited ;; Limiting || cyclone ;; Catastrophe || produced ;; Creating
damage ;; Damaging
Passing ;; Motion_directional || hurricane ;; Catastrophe || produced ;; Creating
caused ;; Causation || damage ;; Damaging || widespread ;; Dispersal
destroyed ;; Destroying || damaged ;; Damaging
damage ;; Damaging || left ;; Causation
toppled ;; Destroying
caused ;; Causation || damage ;; Damaging || fatality ;; Death
known ;; Know || landfall ;; Self_motion || Typhoon ;; Catastrophe
formed ;; Coming_to_be || intensifying ;; Cause_change_of_strength
moving ;; Motion || intensified ;; Cause_change_of_strength
developing ;; Coming_to_be || attained ;; Achieve || passed ;; Motion_directional
moved ;; Motion || dissipated ;; Dispersal
Impact ;; Influence || limited ;; Limiting || reported ;; Reporting
damage ;; Damaging
destroyed ;; Destroying || damaged ;; Damaging
affecting ;; Influence || affected ;; Influence
caused ;; Causation || deaths ;; Death
brief ;; Telling || War ;; Hostile_encounter || took place ;; Process_start
limited ;; Limiting
sent ;; Sending || reacting ;; Response
ended ;; Process_end || decided ;; Deciding || responded ;; Response
surrendered ;; Surrendering || participated ;; Participation || believed ;; Know
released ;; Releasing || murder ;; Killing || pardoned ;; Rewards_and_punishments || died ;; Death || convicted ;; Legal_rulings
compelled ;; Request || result ;; Causation || cede ;; Surrendering
building ;; Building || increased ;; Cause_change_of_position_on_a_scale || abandoned ;; GiveUp || reoccupying ;; Attack
convinced ;; Convincing || compelled ;; Request || move ;; Motion
involved ;; Participation || preceded ;; Process_start || concerned ;; Participation
storm ;; Catastrophe || effects ;; Influence
moved ;; Motion
began ;; Process_start
formed ;; Coming_to_be || became ;; Becoming
reported ;; Reporting || reconnaissance ;; Scrutiny || storm ;; Catastrophe
named ;; Name_conferral || attained ;; Getting
made ;; Manufacturing
delayed ;; Change_event_time || ensued ;; Coming_to_be
warnings ;; Warning || issued ;; Creating || evacuated ;; Emptying
limited ;; Limiting || impacts ;; Influence
destroyed ;; Destroying || remaining ;; Presence || flooding ;; Motion
damage ;; Damaging || storm ;; Catastrophe
known ;; Know || affected ;; Influence || storm ;; Catastrophe
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe
moved ;; Motion || striking ;; Attack
weakened ;; Cause_change_of_strength || reorganized ;; Arranging
weakened ;; Cause_change_of_strength || turned ;; Becoming || strengthened ;; Cause_change_of_strength || curved ;; Motion_directional
made ;; Manufacturing || dissipating ;; Dispersal
causing ;; Causation || brought ;; Bringing || resulted in ;; Causation || Interacting ;; Communication
causing ;; Causation || damaged ;; Damaging || destroyed ;; Destroying
warnings ;; Warning || threatened ;; Warning || storm ;; Catastrophe || prompting ;; Cause_change_of_position_on_a_scale || restricted ;; Limiting
produced ;; Manufacturing || wrecked ;; Destroying
Damage ;; Damaging || death ;; Death
event ;; Social_event
airing ;; Expressing_publicly
produced ;; Manufacturing || aired ;; Expressing_publicly
began ;; Process_start || created ;; Creating || airing ;; Expressing_publicly
airs ;; Expressing_publicly
presenting ;; Presence
giving ;; Giving || vote ;; Choosing
used ;; Using || determining ;; Deciding
presented ;; Presence || replaced ;; Removing
pass ;; Motion_directional
determined ;; Deciding || performed ;; Hold || vote ;; Choosing
festival ;; Social_event || determining ;; Deciding || tasked ;; Request
limit ;; Limiting || criticism ;; Judgment_communication || use ;; Using || use ;; Using
allowed ;; Preventing_or_letting || use ;; Using || receive ;; Receiving
delivered ;; Sending
praised ;; Judgment_communication || festival ;; Social_event || brought ;; Bringing || providing ;; Supply
resulted in ;; Causation
divided ;; Change || takes place ;; Process_start
festival ;; Social_event || meet ;; Come_together
consolidated ;; Cause_to_amalgamate || verified ;; Check || celebrated ;; Social_event || festival ;; Social_event || celebrated ;; Social_event
limited ;; Limiting || festival ;; Social_event
organized ;; Arranging || invited ;; Request || selected ;; Choosing || tribute ;; Judgment_communication || play ;; Competition || dissolved ;; Dispersal
playing ;; Participation
organized ;; Arranging || festival ;; Social_event
organized ;; Arranging || competition ;; Competition || festival ;; Social_event || festival ;; Social_event
invasion ;; Attack || operation ;; Military_operation
invaded ;; Attack || invaded ;; Attack
ended ;; Process_end || lasted ;; Process_end
called ;; Name_conferral
existence ;; Presence || signed on ;; Sign_agreement || invasion ;; Attack || indicated ;; Statement
achieved ;; Achieve || encountering ;; Hostile_encounter || limited ;; Limiting
captured ;; Conquering
began ;; Process_start
control ;; Control || annexed ;; Destroying
conducted ;; Action || made ;; Manufacturing || used ;; Using || use ;; Using || elections ;; Change_of_leadership
began ;; Process_start || arrests ;; Arrest || executions ;; Rewards_and_punishments || targeting ;; Aiming || murders ;; Killing
sent ;; Sending
driven ;; Self_motion || occupied ;; Hold
reconquered ;; Scrutiny
compensating ;; Supply || permitted ;; Agree_or_refuse_to_act
appended ;; Connect
recognized ;; Know || signed ;; Sign_agreement
recognized ;; Know || returned ;; Arriving
concert ;; Social_event || played ;; Participation
support ;; Supporting || included ;; Cause_to_be_included || toured ;; Traveling || release ;; Publishing
entitled ;; Name_conferral || recorded ;; Recording || released ;; Publishing
limited ;; Limiting || releases ;; Publishing
recorded ;; Recording || released ;; Publishing
concert ;; Social_event || recreating ;; Creating || appeared ;; Presence
described ;; Statement || concert ;; Social_event
Marathon ;; Competition
Majors ;; Competition
race ;; Competition
Marathon ;; Competition
race ;; Competition
race ;; Competition || run ;; Motion
races ;; Competition || pursuit ;; Aiming || growing ;; Expansion || marathon ;; Competition
achieved ;; Achieve || record ;; Recording || provide ;; Supply || marathons ;; Competition || developing ;; Cause_to_make_progress || produced ;; Creating
organized ;; Arranging || marathon ;; Competition || race ;; Competition
limited ;; Limiting || finish ;; Process_end || race ;; Competition
selected ;; Choosing || participate ;; Participation
race ;; Competition || include ;; Cause_to_be_included || limited ;; Limiting
event ;; Competition || participation ;; Participation || encourage ;; Cause_change_of_position_on_a_scale
support ;; Supporting || contain ;; Containing
started ;; Process_start || started ;; Process_start
used ;; Using || limited ;; Limiting || reiterated ;; Statement
joined ;; Becoming_a_member
claimed ;; Statement || took place ;; Process_start
increased ;; Cause_change_of_position_on_a_scale
Limited ;; Limiting || crash ;; Catastrophe || occurred ;; Coming_to_be || collided ;; Catastrophe
killed ;; Killing || collision ;; Catastrophe || route ;; Sending
found ;; Know || given ;; Giving || investigation ;; Scrutiny || order ;; Arranging || differed ;; Change
causing ;; Causation || collision ;; Catastrophe || resulting in ;; Causation
alleged ;; Statement || passing ;; Motion_directional || charged ;; Judgment_communication
hired ;; Employment
obtained ;; Getting || joined ;; Participation || case ;; Criminal_investigation
crash ;; Catastrophe || installed ;; Placing || occurred ;; Coming_to_be
prevented ;; Preventing_or_letting || realigned ;; Change || eliminating ;; Removing || seeing ;; Perception_active
became ;; Becoming || defence ;; Defending || rise ;; Cause_change_of_position_on_a_scale
dead ;; Death
attack ;; Attack
attack ;; Attack || prelude ;; Process_start
named ;; Name_conferral
limited ;; Limiting
deployed ;; Arranging || forces ;; Hostile_encounter || use ;; Using
detect ;; Know || built ;; Coming_to_be
signed ;; Sign_agreement || attack ;; Attack
demanded ;; Request || invaded ;; Attack || plan ;; Arranging || use ;; Using || accept ;; Agree_or_refuse_to_act
surrender ;; Surrendering || forced ;; Hostile_encounter || battle ;; Hostile_encounter
attack ;; Attack || fear ;; Change_sentiment || invasion ;; Attack || bomb ;; Attack || stopped ;; Process_end
forces ;; Hostile_encounter || Due ;; Causation || stopped ;; Process_end || fight ;; Hostile_encounter
campaign ;; Military_operation
caused ;; Causation || Hurricane ;; Catastrophe || impact ;; Influence
named ;; Name_conferral || developed ;; Coming_to_be || hurricane ;; Catastrophe || hurricane ;; Catastrophe
becoming ;; Becoming || upgraded ;; Cause_to_make_progress || storm ;; Catastrophe
became ;; Becoming || strengthen ;; Cause_change_of_strength || hurricane ;; Catastrophe
peaked ;; Cause_change_of_position_on_a_scale
caused ;; Causation || become ;; Becoming || peak ;; Cause_change_of_position_on_a_scale || weaken ;; Cause_change_of_position_on_a_scale
storm ;; Catastrophe || centered ;; Placing || downgraded ;; Cause_change_of_position_on_a_scale
became ;; Becoming || crossing ;; Motion_directional || weaken ;; Cause_change_of_position_on_a_scale
degenerated ;; Change
developed ;; Coming_to_be || monitored ;; Scrutiny
impact ;; Influence || limited ;; Limiting || storm ;; Catastrophe
caused ;; Causation || effects ;; Influence || damage ;; Damaging || resulted in ;; Causation
organized ;; Arranging || Race ;; Competition
created ;; Creating
allowed ;; Preventing_or_letting || get to ;; Arriving
pushing ;; Motion || dragging ;; Motion
allowed ;; Preventing_or_letting
prepare ;; GetReady
races ;; Competition
allowed ;; Preventing_or_letting || race ;; Competition
forces ;; Hostile_encounter || forces ;; Hostile_encounter || Drive ;; Self_motion || offensive ;; Attack
capture ;; Conquering || forces ;; Hostile_encounter || guarding ;; Defending
limited ;; Limiting || combat ;; Hostile_encounter || defensive ;; Defending || gains ;; Earnings_and_losses || offensive ;; Attack || made ;; Getting
combat ;; Hostile_encounter || experienced ;; Earnings_and_losses
halted ;; Process_end || counterattack ;; Attack || forces ;; Hostile_encounter || drive ;; Motion || launched ;; Process_start
Battle ;; Hostile_encounter
festival ;; Social_event || held ;; Hold
picnics ;; Social_event || events ;; Social_event || inspired ;; Influence || driven ;; Influence || putting on ;; Placing
give ;; Giving
rewarded ;; Rewards_and_punishments || due ;; Causation || limitations ;; Limiting || search ;; Scrutiny || finding ;; Know || find ;; Know
festival ;; Social_event || increased ;; Cause_change_of_position_on_a_scale || room ;; Temporary_stay || afforded ;; Supply || camping ;; Social_event
providing ;; Supply || approach ;; Arriving || design ;; Creating
included ;; Cause_to_be_included || provide ;; Supply || situated ;; Placing || use ;; Using
fought ;; Hostile_encounter || campaign ;; Military_operation
conducted ;; Action || initiated ;; Process_start || aimed ;; Aiming || war ;; Hostile_encounter || neutralise ;; Destroying
fighting ;; Hostile_encounter || forces ;; Hostile_encounter || took place ;; Process_start || landing ;; Self_motion
landing ;; Self_motion || fighting ;; Hostile_encounter || took place ;; Process_start
took over ;; Military_operation || beginning ;; Process_start || contained ;; Containing || Landing ;; Self_motion || limited ;; Limiting || undertook ;; Action
kept ;; Temporary_stay || concentrated ;; Aiming || assault ;; Attack || came ;; Action
questioned ;; Communication
support ;; Supporting || support ;; Supporting || allocated ;; Dispersal
starting ;; Process_start || party ;; Social_event || held ;; Hold
location ;; Placing || retreat ;; Departing || held ;; Hold
retreat ;; Departing || held ;; Hold || returned ;; Arriving
playing ;; Action
limited ;; Limiting
started ;; Process_start || expanded ;; Expansion
decided ;; Deciding || leaving ;; Departing || inspirations ;; Influence || event ;; Social_event || event ;; Social_event || location ;; Placing
event ;; Social_event
Known ;; Name_conferral
Battle ;; Hostile_encounter || bombardment ;; Attack || took place ;; Process_start || War ;; Military_operation
diminished ;; Cause_change_of_position_on_a_scale || accepted ;; Agree_or_refuse_to_act || plan ;; Arranging
attacks ;; Attack || bombardment ;; Attack || caused ;; Causation || deaths ;; Death || resulted in ;; Causation || leaving ;; Departing || Airstrikes ;; Attack
fired ;; Use_firearm
captured ;; Conquering || campaign ;; Military_operation || target ;; Aiming
defensive ;; Defending || achieved ;; Achieve || capturing ;; Conquering || maintained ;; Preserving || limited ;; Limiting || subordinated ;; Forming_relationships || supported ;; Supporting || advances ;; Cause_to_make_progress || advances ;; Cause_to_make_progress
arrests ;; Arrest || Battle ;; Hostile_encounter || made ;; Action || aftermath ;; Catastrophe || crimes ;; Committing_crime || charged ;; Criminal_investigation
committed ;; Commitment || defence ;; Defending || crimes ;; Committing_crime || charged ;; Criminal_investigation
sentences ;; Legal_rulings || received ;; Receiving || convicted ;; Legal_rulings
began ;; Process_start || attack ;; Attack || invasion ;; Attack
order ;; Arranging || invasion ;; Attack || put ;; Placing
issued ;; Creating
commenced ;; Process_start || attacks ;; Attack || invasion ;; Attack || attack ;; Attack
attacks ;; Attack || thrusts ;; Self_motion
attacked ;; Attack || attacks ;; Attack || limited ;; Limiting
entered ;; Arriving || faced ;; Confronting_problem
attack ;; Attack || inconsequential ;; Change_sentiment || due ;; Causation || collapse ;; Destroying
proposed ;; Statement || training ;; Education_teaching
ended ;; Process_end || signed on ;; Sign_agreement || invasion ;; Attack || came into effect ;; Change
occupied ;; Hold
created ;; Creating || invasion ;; Attack || annexed ;; Attack || remained ;; Presence
invasion ;; Attack || invasion ;; Attack || invasion ;; Attack || invasion ;; Attack
elect ;; Choosing || election ;; Change_of_leadership || election ;; Change_of_leadership
resulted in ;; Causation || faced ;; Confronting_problem
limiting ;; Limiting || signatures ;; Sign_agreement || opposed ;; Agree_or_refuse_to_act || initiate ;; Process_start || collected ;; Come_together || election ;; Change_of_leadership
recall ;; Communication
making ;; Manufacturing || run ;; Self_motion || defeating ;; Conquering
took place ;; Process_start
held ;; Defending || defeating ;; Conquering || elections ;; Choosing
facing ;; Confronting_problem || election ;; Change_of_leadership
control ;; Control || took place ;; Process_start || resulting in ;; Causation || elections ;; Choosing || elections ;; Choosing
defeated ;; Conquering || election ;; Change_of_leadership || election ;; Change_of_leadership
election ;; Change_of_leadership
election ;; Change_of_leadership || election ;; Change_of_leadership
election ;; Change_of_leadership
involved ;; Participation
issues ;; Creating || resolve ;; Resolve_problem || conflict ;; Hostile_encounter
withdrawal ;; Escaping || resumed ;; Process_start || facilitate ;; Preventing_or_letting || believed ;; Know || compel ;; Convincing
prepared ;; GetReady || limited ;; Limiting || took ;; Creating
warfare ;; Hostile_encounter || raids ;; Attack || proclaimed ;; Statement || shelling ;; Attack || characterized ;; Statement
ended ;; Process_end || began ;; Process_start || negotiations ;; Communication || remaining ;; Presence || commitment ;; Commitment
damage ;; Damaging || brought ;; Bringing
named ;; Name_conferral || originated ;; Coming_to_be
approached ;; Arriving || strengthening ;; Cause_change_of_strength || attained ;; Getting || reached ;; Arriving
weakening ;; Cause_change_of_strength || made ;; Manufacturing
dissipated ;; Dispersal
warnings ;; Warning || arrival ;; Arriving || issued ;; Creating
damage ;; Damaging || limited ;; Limiting || made ;; Manufacturing || struck ;; Attack
damage ;; Damaging
usage ;; Using || Following ;; Self_motion || replaced ;; Change_of_leadership
mandating ;; Giving
passed ;; Motion_directional
created ;; Creating || organized ;; Arranging || passed ;; Motion_directional || guarantee ;; Commitment
referred ;; Name_conferral || granted ;; Giving
separate ;; Forming_relationships || became ;; Becoming || prohibited ;; Preventing_or_letting || passed ;; Motion_directional
helped ;; Assistance || argue ;; Quarreling
founded ;; Creating || protect ;; Defending
allowed ;; Preventing_or_letting || sentenced ;; Legal_rulings
revoked ;; Removing
regained ;; Recovering || repealed ;; Removing
influenced ;; Influence || writing ;; Writing
called ;; Name_conferral
defeated ;; Conquering
asked ;; Request || prevented ;; Preventing_or_letting
used ;; Using
listed ;; Writing
protests ;; Response || inspired ;; Influence
added ;; Cause_to_be_included || assemblies ;; Come_together || launched ;; GetReady
called ;; Name_conferral
assemblies ;; Come_together || endorsed ;; Agree_or_refuse_to_act
targeted ;; Aiming || influence ;; Influence || lack ;; Causation || participation ;; Participation
held ;; Hold || demonstrations ;; Participation
chosen ;; Choosing
coordinate ;; Arranging || assemblies ;; Come_together
numbered ;; Writing
support ;; Supporting || demonstration ;; Social_event || allied ;; Collaboration
took place ;; Process_start
organized ;; Arranging || involved ;; Cause_to_be_included
received ;; Receiving || avoided ;; Preventing_or_letting || influenced ;; Influence
claimed ;; Statement
left ;; Departing || left ;; Departing || reached ;; Arriving || attend ;; Participation
refused ;; Agree_or_refuse_to_act || stood ;; Placing || handing ;; Giving || read ;; Statement
focused ;; Aiming || fighting ;; Hostile_encounter || garnered ;; Getting
read ;; Writing
commented ;; Statement
Talking ;; Telling || held ;; Hold
concluded ;; Process_end || started ;; Process_start
conceived ;; Creating
released ;; Publishing || recorded ;; Recording || promote ;; Cause_change_of_position_on_a_scale
peaked ;; Arriving
peaked ;; Arriving
playing ;; Participation || died ;; Death || concert ;; Social_event || collapsed ;; Catastrophe
announced ;; Expressing_publicly || ensued ;; Coming_to_be
ended ;; Process_end || arguing ;; Quarreling
released ;; Publishing || contained ;; Containing || performing ;; Hold || directed ;; Creating
bob ;; Body_movement || authorised ;; Preventing_or_letting || made ;; Creating || wrote ;; Writing
drew ;; Getting
travelling ;; Traveling
meeting ;; Social_event
met ;; Come_together || hand over ;; Change_of_leadership
told ;; Telling
resulted in ;; Causation
covered ;; Hiding_objects
make ;; Creating
invited ;; Request || made ;; Creating
came to ;; Self_motion || election ;; Choosing
terminated ;; Process_end
deteriorated ;; Cause_change_of_strength
election ;; Choosing
collapsed ;; Destroying
rising ;; Cause_change_of_position_on_a_scale || battered ;; Bodily_harm
decided ;; Deciding
moved ;; Motion
weakened ;; Cause_change_of_strength || falling ;; Cause_change_of_position_on_a_scale
decided ;; Deciding
sworn ;; Commitment
delivered ;; Expressing_publicly
outlines ;; Recording
wrote ;; Writing || leaked ;; Reveal_secret
began ;; Process_start
began ;; Process_start
organized ;; Arranging || declared ;; Expressing_publicly
called ;; Request
began ;; Process_start || failed ;; Earnings_and_losses
led to ;; Causation
signed ;; Writing
exiled ;; Escaping
ceased ;; Process_end
launched ;; Process_start
defeated ;; Conquering
returned ;; Arriving
gained ;; Getting
recognized ;; Know
ended ;; Process_end
ceded ;; Removing
broke out ;; Process_start
ordered ;; Statement
declared ;; Expressing_publicly
become ;; Becoming
response ;; Response || violence ;; Violence || protests ;; Protest || took place ;; Process_start
write ;; Writing || used ;; Using || disposed ;; Emptying
burned ;; Destroying || disposal ;; Emptying
reported ;; Reporting || resulting in ;; Causation || besieging ;; Besieging || raining ;; Damaging
killed ;; Killing
wounded ;; Bodily_harm
used ;; Using
caused ;; Causation || deaths ;; Death || injuries ;; Bodily_harm || included ;; Cause_to_be_included || riots ;; Violence
crashed ;; Catastrophe || killing ;; Killing || registered ;; Writing
caused ;; Causation || involving ;; Cause_to_be_included
published ;; Publishing || allowing ;; Preventing_or_letting || selecting ;; Choosing || sink ;; Motion_directional || ruled ;; Legal_rulings
approached ;; Arriving || failed ;; Confronting_problem || pull ;; Self_motion
stated ;; Statement || facing ;; Confronting_problem || prompting ;; Causation || aware ;; Know
issued ;; Statement
change ;; Change || Included ;; Cause_to_be_included
urged ;; Request || prevent ;; Preventing_or_letting || occurring ;; Coming_to_be || evaluate ;; Check
drew ;; Motion || claimed ;; Statement
struggling ;; Hostile_encounter || avert ;; Preventing_or_letting || claimed ;; Statement
occurred ;; Coming_to_be || resulted in ;; Causation || re-designed ;; Hold
operation ;; Military_operation
took place ;; Hold
took place ;; Process_start || coined ;; Writing
arrested ;; Arrest || plan ;; Arranging || free ;; Releasing
executed ;; Action
taking ;; Conquering || release ;; Releasing || attack ;; Attack || led to ;; Causation
died ;; Death || injuries ;; Bodily_harm
assassinated ;; Killing
presented ;; Presence
present ;; Presence || ambush ;; Attack || invasion ;; Attack
destruction ;; Destroying || attack ;; Attack
cut off ;; Hindering || stood ;; Placing || retreated ;; Escaping || wiped ;; Emptying
killed ;; Killing || influencing ;; Influence || elevated ;; Cause_change_of_position_on_a_scale || becoming ;; Becoming
change ;; Change || exaggerate ;; Cause_change_of_position_on_a_scale
recounted ;; Statement || celebrated ;; Social_event
adaptations ;; Writing
force ;; Control
received ;; Receiving || controlled ;; Control || ensured ;; Cause_to_make_progress || reached ;; Arriving
began ;; Process_start || seeking ;; Motion || reinforced ;; Cause_change_of_strength
rendered ;; Causation || made ;; Action
lost ;; Earnings_and_losses || sent ;; Sending || allowed ;; Preventing_or_letting || maintain ;; Cause_to_make_progress
dragged on ;; Change_event_time || control ;; Control || signed ;; Writing || signed ;; Sign_agreement || granted ;; Preventing_or_letting
flying ;; Self_motion
operated ;; Being_in_operation
crashed ;; Catastrophe || dive ;; Self_motion || take off ;; Departing || registered ;; Writing || entered ;; Arriving
killed ;; Killing
revealed ;; Reveal_secret || reduced ;; Cause_change_of_position_on_a_scale || Investigation ;; Criminal_investigation
became ;; Becoming || leaving ;; Departing || respond ;; Response
crash ;; Catastrophe || crash ;; Catastrophe
operational ;; Being_in_operation
revolutionary ;; Change_of_leadership || War ;; Hostile_encounter
formed ;; Coming_to_be || sent ;; Sending || aimed ;; Aiming || terrorists ;; Terrorism
killed ;; Killing
quarters ;; Temporary_stay || leave ;; Departing || wrote ;; Writing || lay ;; Placing || establish ;; Creating
debate ;; Communication
derided ;; Judgment_communication || revolution ;; Change_of_leadership || claims ;; Statement || gone ;; Removing
become ;; Becoming || debate ;; Communication
occurred ;; Coming_to_be || outbreak ;; Process_start || attempted ;; Hold
handed over ;; Giving || reach ;; Arriving || evaded ;; Escaping || passed ;; Motion_directional
attack ;; Attack || bringing ;; Bringing || Renamed ;; Name_conferral || ordered ;; Request
battle ;; Hostile_encounter
ended ;; Process_end
brought ;; Bringing || ruin ;; Destroying || Writing ;; Writing || borne ;; Bringing || forcing ;; Hostile_encounter
invaded ;; Attack || Battle ;; Hostile_encounter || fought ;; Hostile_encounter
fought ;; Hostile_encounter
sacked ;; Destroying
given ;; Giving || battle ;; Hostile_encounter
battle ;; Hostile_encounter || dating ;; Social_event || account ;; Reporting
dating ;; Social_event || used ;; Using
battle ;; Hostile_encounter || foundation ;; Coming_to_be || wrote ;; Writing || took place ;; Process_start
battle ;; Hostile_encounter || said ;; Statement || took place ;; Process_start
found ;; Know
referred ;; Name_conferral || devoted ;; Writing
arrested ;; Arrest || believed ;; Statement || charged ;; Arrest
arraignment ;; Criminal_investigation || pleaded ;; Protest
requested ;; Request || began ;; Process_start || trial ;; Judgment_communication || delays ;; Change_event_time
found ;; Coming_to_believe || trial ;; Judgment_communication || deliberation ;; Legal_rulings
serving ;; Arrest || began ;; Process_start || sentenced ;; Legal_rulings
arrest ;; Arrest || ruling ;; Legality || controversy ;; Quarreling || existing ;; Presence || led to ;; Causation || criminals ;; Committing_crime || carried out ;; Military_operation
released ;; Releasing || completing ;; Process_end || immigrated ;; Becoming_a_member || ordered ;; Request || excludes ;; Preventing_or_letting || deported ;; Removing
suggested ;; Convincing || amending ;; Change || permit ;; Agree_or_refuse_to_act || detention ;; Arrest || criminals ;; Committing_crime || deny ;; Agree_or_refuse_to_act || failed ;; Agree_or_refuse_to_act || issues ;; Resolve_problem || formed ;; Creating
tour ;; Traveling || Tour ;; Traveling
formed ;; Creating
asked ;; Request || join ;; Becoming_a_member || show ;; Social_event
introduce ;; Statement
asked ;; Request
created ;; Creating
told ;; Telling || return ;; Arriving || impressed ;; Change_sentiment
wrote ;; Writing
change ;; Change
become ;; Becoming || played ;; Participation
decided ;; Deciding
consisted ;; Cause_to_be_included
recorded ;; Recording
signed ;; Writing || brief ;; Telling || record ;; Recording
signed ;; Writing
decided ;; Deciding || considered ;; Deciding
replaced ;; Removing || replaced ;; Change_of_leadership
fought ;; Hostile_encounter || battle ;; Hostile_encounter || Battle ;; Hostile_encounter
battle ;; Hostile_encounter || took place ;; Process_start
recorded ;; Recording || fought ;; Hostile_encounter || giving ;; Giving || considered ;; Deciding || battle ;; Hostile_encounter
deduced ;; Coming_to_believe || continued ;; Process_start || issue ;; Creating
name ;; Name_conferral || name ;; Name_conferral
changeover ;; Change
struggling ;; Hostile_encounter
resisting ;; Defending
battle ;; Hostile_encounter
restored ;; Recovering || argued ;; Quarreling
resumed ;; Process_start || absorbed ;; Conquering
wrote ;; Writing
sold ;; Commerce_sell || record ;; Recording || attending ;; Participation
decided ;; Deciding || started ;; Process_start || become ;; Becoming
held ;; Hold
gave ;; Giving
constructed ;; Building || festival ;; Social_event
picked up ;; Choosing || wrote ;; Writing
launched ;; Process_start
visited ;; Traveling
played ;; Competition
spanned ;; Expansion
commenced ;; Process_start || tour ;; Traveling || playing ;; Participation
seen ;; Perception_active
toured ;; Traveling || supported ;; Supporting
included ;; Cause_to_be_included
set ;; Placing || playing ;; Competition || played ;; Competition
end ;; Process_end || tour ;; Traveling || known ;; Know || concert ;; Social_event || held ;; Defending
said ;; Telling
feel ;; Change_sentiment
said ;; Communication || touring ;; Traveling
written ;; Writing || returning ;; Arriving
revealed ;; Reveal_secret || play ;; Competition
service ;; Supply
approach ;; Arriving || operating ;; Being_in_operation || crashed ;; Catastrophe
killed ;; Killing
cause ;; Causation || made ;; Manufacturing || disclosed ;; Reveal_secret
stating ;; Statement || wrote ;; Writing || produced ;; Manufacturing || report ;; Reporting
report ;; Reporting || reasons ;; Protest
carried ;; Carry_goods
existing ;; Presence || aired ;; Expressing_publicly || search ;; Scrutiny || rescue ;; Rescuing || recovered ;; Recovering
caused ;; Causation || brought ;; Bringing || storm ;; Catastrophe
named ;; Name_conferral || developed ;; Coming_to_be
strengthened ;; Cause_change_of_strength || forming ;; Coming_to_be || attaining ;; Getting
due ;; Causation || degenerated ;; Change || maintain ;; Preserving
become ;; Becoming || noted ;; Writing
reached ;; Arriving || impacting ;; Influence
weakened ;; Cause_change_of_strength || prevented ;; Preventing_or_letting || storm ;; Catastrophe
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
remained ;; Presence || produced ;; Manufacturing
rescues ;; Rescuing || made ;; Manufacturing
effects ;; Influence
storm ;; Catastrophe || fell ;; Motion_directional || remained ;; Presence
conflict ;; Hostile_encounter || took place ;; Process_start
arrived ;; Arriving || said ;; Communication || laid ;; Placing
followed ;; Motion || marched ;; Motion_directional || secured ;; Assistance || marriage ;; Forming_relationships
recalls ;; Statement || defended ;; Defending || defeated ;; Hostile_encounter || engaged ;; Participation
made ;; Creating || paints ;; Using
expressed ;; Expressing_publicly
wrote ;; Writing || say ;; Communication
disaster ;; Catastrophe || occurred ;; Coming_to_be || blizzard ;; Catastrophe || died ;; Death || descend ;; Motion_directional
reach ;; Arriving || earthquake ;; Catastrophe || making it ;; Causation || avalanche ;; Catastrophe || avalanches ;; Catastrophe || caused ;; Causation || died ;; Death || fatalities ;; Death || deaths ;; Death
disaster ;; Catastrophe || gained ;; Getting || raised ;; Causation || commercialization ;; Cause_to_make_progress
storm ;; Catastrophe
died ;; Killing || reported ;; Reporting
disaster ;; Catastrophe || perished ;; Killing
storm ;; Catastrophe || died ;; Death
disaster ;; Catastrophe || written ;; Writing
published ;; Publishing || assignment ;; Request
impugned ;; Judgment_communication
disaster ;; Catastrophe || wrote ;; Writing
published ;; Publishing || tragedy ;; Catastrophe
rescue ;; Rescuing || added ;; Cause_to_be_included || coordinated ;; Collaboration
climbed ;; Body_movement || documented ;; Recording || storm ;; Catastrophe || forecast ;; Statement || delivered ;; Sending
planned ;; Arranging || storm ;; Catastrophe || summits ;; Arriving || descend ;; Motion_directional
crisis ;; Catastrophe || took ;; Conquering || occurred ;; Presence
entered ;; Arriving || transporting ;; Sending
attached ;; Connect || threatened ;; Warning || move ;; Self_motion || ignite ;; Process_start
becoming ;; Becoming || standoff ;; Hostile_encounter || led ;; Bringing
decided ;; Deciding || make ;; Causation || agitated ;; Influence || leave ;; Departing
attached ;; Connect || leaving ;; Departing
became ;; Becoming || began ;; Process_start || begging ;; Request
injuring ;; Bodily_harm || lifted ;; Motion || went off ;; Process_end
Returning ;; Arriving || shot ;; Use_firearm
escaped ;; Escaping || hospitalized ;; Cure || injuries ;; Bodily_harm || burns ;; Bodily_harm
disaster ;; Catastrophe || occurred ;; Coming_to_be
carrying ;; Sending || derailed ;; Motion
died ;; Death || scene ;; Catastrophe || injured ;; Bodily_harm
escaped ;; Escaping || events ;; Confronting_problem || held ;; Placing
became ;; Becoming || escape ;; Escaping
crash ;; Catastrophe || investigation ;; Criminal_investigation || resulted in ;; Causation || appointment ;; Employment || led ;; Criminal_investigation
raid ;; Attack
conducted ;; Action
supported ;; Supporting
support ;; Supporting
used ;; Using || transport ;; Military_operation
support ;; Supporting
bombing ;; Attack || took place ;; Hold
attack ;; Attack
arrived at ;; Arriving || incident ;; Catastrophe
killed ;; Killing || wounded ;; Bodily_harm
taken ;; Bringing || guilty ;; Committing_crime || punishment ;; Rewards_and_punishments || receive ;; Receiving || found ;; Coming_to_believe
delivered ;; Sending || arrested ;; Arrest || claimed ;; Statement || admitted ;; Telling || drove ;; Motion
attacks ;; Attack || took place ;; Process_start
attacks ;; Attack || attack ;; Attack || released ;; Publishing
attacks ;; Attack
attack ;; Attack || beheading ;; Killing || killed ;; Killing || attack ;; Attack || killed ;; Killing || undertaken ;; Action || resulted in ;; Causation
took place ;; Process_start || competitions ;; Competition
competition ;; Competition || took place ;; Process_start
contested ;; Competition
maintain ;; Preserving || claimed ;; Statement
emerged ;; Coming_to_be || bring ;; Bringing || Brought ;; Bringing
mounted ;; Connect || striking ;; Attack || Battling ;; Hostile_encounter
ended ;; Process_end || followed ;; Motion
delivered ;; Sending || bring ;; Bringing || managed ;; Control
produced ;; Manufacturing || swimming ;; Self_motion || winning ;; Getting
set ;; Placing || competition ;; Competition || records ;; Recording || records ;; Recording
flood ;; Catastrophe || flood ;; Catastrophe || started ;; Process_start || described ;; Statement
flood ;; Catastrophe || believed ;; Statement
death ;; Death || marooned ;; Hiding_objects
flooding ;; Catastrophe || affected ;; Influence
delivered ;; Military_operation || established ;; Creating
reported ;; Reporting || increase ;; Cause_change_of_position_on_a_scale || deaths ;; Death || flood ;; Catastrophe || occurring ;; Coming_to_be
floods ;; Catastrophe || deaths ;; Death || recorded ;; Recording || deaths ;; Death || floods ;; Catastrophe
crashed ;; Motion
killed ;; Killing || accident ;; Catastrophe || crash ;; Catastrophe
left ;; Departing || storm ;; Catastrophe || flying ;; Motion || diverted ;; Motion || avoid ;; Preventing_or_letting
contacted ;; Communication || reached ;; Arriving || cruising ;; Self_motion || advise ;; Convincing
reported ;; Reporting || passed ;; Motion_directional || passed ;; Motion_directional || dropped ;; Motion_directional || reaches ;; Arriving || went ;; Self_motion || stall ;; Motion || spun ;; Motion || glided ;; Motion
burst ;; Destroying
died ;; Death
reported ;; Reporting || transport ;; Sending || carrying ;; Carry_goods || deaths ;; Death || carried ;; Carry_goods || crashed ;; Motion
said ;; Telling
said ;; Telling || charred ;; Bodily_harm
reported ;; Reporting
burned ;; Destroying
plunged ;; Motion_directional || said ;; Statement
cutting ;; Bodily_harm || burst ;; Destroying || said ;; Statement || hit ;; Attack
determined ;; Deciding
believed ;; Know || due ;; Causation || flutter ;; Motion
storm ;; Catastrophe || initiated ;; Process_start || existence ;; Presence || reports ;; Reporting || surrounding ;; Surrounding || led ;; Causation || flutter ;; Motion
obtained ;; Getting || governing ;; Action || accident ;; Catastrophe || ordered ;; Request || grounded ;; Motion || performed ;; Action
discovered ;; Know || given ;; Giving || used ;; Using || authorized ;; Agree_or_refuse_to_act
cause ;; Causation || flutter ;; Motion || break-up ;; Destroying || ordered ;; Request || modified ;; Change || eliminate ;; Removing
sold ;; Commerce_sell || remaining ;; Presence || called ;; Name_conferral || delivery ;; Sending || accidents ;; Catastrophe
delivered ;; Sending || crashed ;; Motion
takeoff ;; Self_motion || crashed ;; Motion || stalled ;; Motion
develop ;; Coming_to_be || manufacturing ;; Manufacturing || allowed ;; Preventing_or_letting || crashed ;; Motion
piloted ;; Control
erected ;; Building || victims ;; Catastrophe || crash ;; Catastrophe
organized ;; Arranging || championship ;; Competition
hosted ;; Social_event || tournament ;; Competition
competition ;; Competition || decision ;; Deciding || move ;; Cause_change_of_position_on_a_scale
expanded ;; Expansion
hosted ;; Social_event || tournament ;; Competition || scheduled ;; Arranging
competition ;; Competition || hosted ;; Social_event
winning ;; Getting
hosting ;; Social_event || delays ;; Change_event_time || delivery ;; Sending || stripped ;; Removing
chosen ;; Choosing || competition ;; Competition
tournament ;; Competition || moved ;; Cause_change_of_position_on_a_scale
eliminated ;; Removing
eliminated ;; Removing || losing ;; Earnings_and_losses
winning ;; Getting || final ;; Competition || match ;; Competition || defeated ;; Conquering || beating ;; Competition
occurred ;; Coming_to_be
picked up ;; Choosing || initiated ;; Process_start || decapitated ;; Killing || moving ;; Motion || tracked ;; Self_motion
delivered ;; Sending
named ;; Name_conferral
carried out ;; Action || led to ;; Causation || elimination ;; Removing
occurred ;; Coming_to_be || crashed ;; Catastrophe
killed ;; Killing
injured ;; Bodily_harm
operated ;; Being_in_operation || transporting ;; Sending
crashed ;; Catastrophe || travelling ;; Self_motion
accident ;; Catastrophe
investigation ;; Criminal_investigation || closed ;; Process_end || ruled out ;; Legal_rulings
crash ;; Catastrophe || attributed ;; Causation
skirmish ;; Hostile_encounter || Battle ;; Hostile_encounter
armed ;; Bearing_arms || sales ;; Commerce_sell || skirmish ;; Hostile_encounter || conflict ;; Hostile_encounter || War ;; Hostile_encounter || occurred ;; Presence || fire ;; Use_firearm || enforcing ;; Action || embargo ;; Process_end || opened ;; Process_start
ban ;; Preventing_or_letting || ordered ;; Arranging || died ;; Death || brawl ;; Hostile_encounter
supplies ;; Supply || punishment ;; Rewards_and_punishments || meted ;; Rewards_and_punishments || suspended ;; Process_end || force ;; Causation || turn ;; Change
sailed ;; Self_motion || supplies ;; Supply || embargo ;; Process_end
sent ;; Sending || allow ;; Preventing_or_letting || encountered ;; Hostile_encounter || supply ;; Supply || demands ;; Request
supplies ;; Supply || received ;; Receiving || delivered ;; Sending
expired ;; Process_end || returned ;; Giving || stated ;; Statement || fire ;; Use_firearm || fire ;; Use_firearm || support ;; Supporting || opened ;; Process_start
re-engaged ;; Hostile_encounter || clash ;; Hostile_encounter || replenishing ;; Supply || pursued ;; Self_motion || sailing ;; Departing || retreated ;; Escaping || ending ;; Process_end
operations ;; Military_operation
took place ;; Process_start
delivered ;; Military_operation || armored ;; Bearing_arms || sweep ;; Removing
reached ;; Arriving || landings ;; Motion || took place ;; Process_start
began ;; Process_start || operations ;; Military_operation
Reinforced ;; Cause_change_of_strength || delays ;; Change_event_time || tightened ;; Cause_change_of_strength
validated ;; Check || claim ;; Statement || operate ;; Military_operation
focused ;; Aiming || creating ;; Creating
withdrawn ;; Escaping || ambushes ;; Attack || resumed ;; Process_start
occupied ;; Control || operating ;; Control
festival ;; Social_event || tour ;; Traveling || Tour ;; Traveling || headlined ;; Motion_directional
included ;; Cause_to_be_included || Opening ;; Openness || Come ;; Motion
formed ;; Coming_to_be || Tour ;; Traveling || followed ;; Self_motion
tour ;; Traveling || impressive ;; Change_sentiment || proved ;; Convincing
set up ;; Being_in_operation || transported ;; Sending
Battle ;; Hostile_encounter || attack ;; Attack
defeated ;; Conquering || retired ;; Escaping || driven ;; Motion
revolution ;; Change_of_leadership || battle ;; Hostile_encounter || victories ;; Earnings_and_losses || war ;; Hostile_encounter || wars ;; Hostile_encounter
break ;; Destroying || Siege ;; Besieging
achieve ;; Achieve || planned ;; Arranging || converging ;; Come_together
siege ;; Besieging || lifted ;; Process_end
allowed ;; Preventing_or_letting || separated ;; Forming_relationships || defeat ;; Conquering
attack ;; Attack || resulted in ;; Causation || final ;; Process_end || delivered ;; Military_operation || victory ;; Earnings_and_losses
released ;; Publishing
released ;; Publishing || follows ;; Self_motion || produced ;; Manufacturing
releasing ;; Publishing || moving ;; Motion
forbidding ;; Preventing_or_letting || cover ;; Hiding_objects || realized ;; Know
delivery ;; Sending
festival ;; Social_event || held ;; Hold
held ;; Hold || attracting ;; Influence || featuring ;; Cause_to_be_included
collaboration ;; Collaboration
created ;; Creating
acquired ;; Getting
regarded ;; Change_sentiment
cancelled ;; Change_event_time || promise ;; Commitment
announcements ;; Expressing_publicly || return ;; Arriving || made ;; Creating
announced ;; Expressing_publicly
agreed ;; Agree_or_refuse_to_act || planned ;; Arranging || happen ;; Presence || changes ;; Change || proceed ;; Self_motion
deliver ;; Sending || meet ;; Come_together || set out ;; Process_start
follow ;; Self_motion || tuned ;; Motion
planned ;; Arranging
hurricane ;; Catastrophe || hit ;; Attack
hurricane ;; Catastrophe || impact ;; Influence
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
moved ;; Motion || strengthened ;; Cause_change_of_strength || influence ;; Influence
weakened ;; Cause_change_of_strength || attained ;; Getting || turned ;; Becoming
made ;; Manufacturing || hit ;; Attack
became ;; Becoming || passing ;; Motion_directional || turned ;; Becoming || dissipating ;; Dispersal
hurricane ;; Catastrophe || hit ;; Attack
hurricane ;; Catastrophe || cause ;; Causation || death ;; Death || damaging ;; Damaging
destroyed ;; Destroying || damage ;; Damaging || hurricane ;; Catastrophe || resulted in ;; Causation
killed ;; Killing || storm ;; Catastrophe || hurricane ;; Catastrophe || closing ;; Process_end
hurricane ;; Catastrophe || destroyed ;; Destroying || transported ;; Sending || threatened ;; Influence || work ;; Employment
caused ;; Causation || damage ;; Damaging || drown ;; Death || resulted in ;; Causation
caused ;; Causation || deaths ;; Death || damage ;; Damaging
cyclone ;; Catastrophe || passed ;; Motion_directional || strike ;; Attack
formed ;; Coming_to_be
moving ;; Motion || named ;; Name_conferral
storm ;; Catastrophe || upgraded ;; Cause_to_make_progress
turned ;; Motion
upgraded ;; Cause_to_make_progress || intensify ;; Cause_change_of_strength
began ;; Process_start || storm ;; Catastrophe || reached ;; Arriving
weakened ;; Cause_change_of_strength || cyclone ;; Catastrophe || making ;; Causation
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
brought ;; Bringing || strengthening ;; Cause_change_of_strength
cyclone ;; Catastrophe || Cyclone ;; Catastrophe || killed ;; Killing || injured ;; Bodily_harm || landfall ;; Arriving || damage ;; Damaging
destroyed ;; Destroying
killed ;; Killing || combined ;; Cause_to_amalgamate || storm ;; Catastrophe || storm ;; Catastrophe || injured ;; Bodily_harm || damage ;; Damaging
cyclone ;; Catastrophe || leveled ;; Destroying || suffered ;; Bodily_harm || damage ;; Damaging
left ;; Causation
Cyclone ;; Catastrophe || destroyed ;; Destroying
downed ;; Change
storm ;; Catastrophe || Cyclone ;; Catastrophe || assistance ;; Assistance
donated ;; Giving || transport ;; Sending
distributed ;; Giving
handled ;; Hold || praised ;; Judgment_communication
conducted ;; Action
reinforce ;; Cause_change_of_strength
landing ;; Self_motion || operation ;; Military_operation
captured ;; Conquering
dropping ;; Motion
towed ;; Self_motion || proved ;; Convincing
arriving ;; Arriving
delivered ;; Sending || combat ;; Hostile_encounter
establishment ;; Creating
comprised ;; Cause_to_be_included
train ;; Education_teaching || operate ;; Using || returned ;; Arriving || flew ;; Self_motion
delivered ;; Sending || flying ;; Self_motion || diverted ;; Removing
assembled ;; Come_together
readied ;; GetReady || flew ;; Self_motion
claimed ;; Statement
began ;; Process_start || began ;; Process_start || converting ;; Change
departed ;; Departing || arrived ;; Arriving || disbanded ;; Removing
gave ;; Giving || received ;; Receiving
oppose ;; Agree_or_refuse_to_act || transport ;; Sending
murdered ;; Killing || began ;; Process_start || build ;; Building || deported ;; Removing
formed ;; Coming_to_be || began ;; Process_start || train ;; Education_teaching
support ;; Supporting
refused ;; Agree_or_refuse_to_act || surrender ;; Surrendering || ordered ;; Request
died ;; Death
casualties ;; Catastrophe || reporting ;; Reporting
revolt ;; Change_of_leadership
uprising ;; Change_of_leadership || knew ;; Know
deaths ;; Death || pick ;; Choosing || said ;; Telling
engagement ;; Hostile_encounter
carrying ;; Carry_goods
Sighted ;; Perception_active || journey ;; Traveling
found ;; Know || flee ;; Escaping || surrounded ;; Surrounding
captured ;; Conquering || approaches ;; Arriving || escape ;; Escaping || engagement ;; Hostile_encounter || separated ;; Forming_relationships
captured ;; Conquering || engagement ;; Hostile_encounter
captured ;; Conquering || taken ;; Bringing || transported ;; Sending
called ;; Name_conferral
used ;; Using
operation ;; Military_operation || freedom ;; Change_of_leadership
included ;; Cause_to_be_included
eliminated ;; Removing
remains ;; Temporary_stay
visited ;; Traveling || stationed ;; Placing
commanded ;; Control
Planned ;; Arranging || open ;; Openness
selected ;; Choosing
allocated ;; Dispersal
transported ;; Sending
captured ;; Conquering
engaged ;; Participation
withdrawn ;; Escaping || allowed ;; Preventing_or_letting
sailed ;; Self_motion
attacked ;; Attack || crashed ;; Catastrophe || killing ;; Killing
attacked ;; Attack || targeted ;; Aiming || believed ;; Know || suggests ;; Protest || suggested ;; Protest
operated ;; Being_in_operation || watched ;; Perception_active
escaped ;; Escaping || carried ;; Sending
beginning ;; Process_start || flying ;; Motion || left ;; Departing || respected ;; Supporting
begun ;; Process_start || lost ;; Damaging || attack ;; Attack || attacks ;; Attack
defense ;; Defending || operation ;; Military_operation
War ;; Hostile_encounter || forces ;; Military_operation
gained ;; Getting || end ;; Process_end || control ;; Control || transport ;; Sending
captured ;; Conquering || offensives ;; Attack
fled ;; Escaping
provoked ;; Causation || accusations ;; Judgment_communication || committed ;; Committing_crime
monitor ;; Scrutiny || contested ;; Quarreling
operation ;; Military_operation || retreating ;; Escaping || crimes ;; Committing_crime || perpetrated ;; Committing_crime
Inauguration ;; Social_event || took place ;; Process_start
administered ;; Control
speech ;; Communication || delivered ;; Sending
inauguration ;; Social_event
organized ;; Arranging
revolution ;; Change_of_leadership || resigned ;; Change_of_leadership
assumed ;; Choosing
began ;; Process_start || armed ;; Bearing_arms || conflict ;; Hostile_encounter || conflict ;; Hostile_encounter
conflict ;; Hostile_encounter || consisted ;; Cause_to_be_included
Defence ;; Defending
became ;; Becoming || command ;; Control
conflict ;; Hostile_encounter || played ;; Action
conflict ;; Hostile_encounter || force ;; Hostile_encounter
died ;; Death || dying ;; Death || attack ;; Attack
conflict ;; Hostile_encounter || prevent ;; Preventing_or_letting || force ;; Hostile_encounter || use ;; Using || seeks ;; Preventing_or_letting || minimise ;; Cause_change_of_position_on_a_scale
opposed ;; Agree_or_refuse_to_act
translating ;; Communication || armed ;; Bearing_arms || clash ;; Hostile_encounter
fought ;; Hostile_encounter
fighting ;; Hostile_encounter || recapturing ;; Control || resulted in ;; Causation
killed ;; Killing || resulted in ;; Causation || contributed ;; Giving
order ;; Request || create ;; Creating || prompted ;; Cause_change_of_position_on_a_scale
presented ;; Presence || requesting ;; Request || withdraw ;; Escaping || arrived at ;; Arriving
captured ;; Conquering || established ;; Achieve || staffed ;; Employment || remained ;; Presence || pull out ;; Removing
captured ;; Conquering || blockaded ;; Besieging
demonstrations ;; Participation || movement ;; Participation || revolution ;; Change_of_leadership || took place ;; Process_start
criticized ;; Judgment_communication || intervention ;; Participation || annexed ;; Becoming_a_member
escalated ;; Cause_change_of_position_on_a_scale || Protests ;; Protest
targeted ;; Aiming || bombings ;; Catastrophe || struck ;; Attack
maintain ;; Statement || sending ;; Sending || operation ;; Military_operation || started ;; Process_start || control ;; Control
war ;; Hostile_encounter || armed ;; Bearing_arms || forces ;; Hostile_encounter
revolution ;; Change_of_leadership || armed ;; Bearing_arms || culminating ;; Process_end || coup ;; Change_of_leadership || coup ;; Change_of_leadership || coup ;; Change_of_leadership || movement ;; Social_event
revolution ;; Change_of_leadership || prevented ;; Preventing_or_letting || inauguration ;; Change_of_leadership || ousted ;; Change_of_leadership || ended ;; Process_end
broke ;; Destroying || alliance ;; Collaboration
indicated ;; Statement
response ;; Response || supported ;; Supporting
held ;; Hold || elections ;; Change_of_leadership
triggered ;; Process_start || coup ;; Change_of_leadership || take ;; Getting || exiled ;; Departing
end ;; Process_end || marks ;; Recording || assumed ;; Choosing
conquered ;; Conquering || armed ;; Bearing_arms
imprisoned ;; Arrest
returning ;; Arriving || declaring ;; Expressing_publicly || revolted ;; Change_of_leadership
defeat ;; Conquering || training ;; Education_teaching || exceeding ;; Cause_change_of_position_on_a_scale
retreated ;; Escaping || reached ;; Arriving || return ;; Arriving
conflict ;; Hostile_encounter
armed ;; Bearing_arms || Battle ;; Hostile_encounter || fought ;; Hostile_encounter || control ;; Control || war ;; Hostile_encounter || confrontation ;; Hostile_encounter
held ;; Hold || War ;; Hostile_encounter || final ;; Process_end
Fighting ;; Hostile_encounter || control ;; Control || took ;; Getting
engagement ;; Hostile_encounter || wounded ;; Bodily_harm || carried ;; Bringing || exploded ;; Attack || bomb ;; Use_firearm
sent ;; Sending || indicates ;; Coming_to_believe || suicide bomber ;; Terrorism
drugged ;; Bodily_harm || behaving ;; Action || describe ;; Statement || harassed ;; Bodily_harm
conflict ;; Hostile_encounter || attacked ;; Attack
fought ;; Quarreling || war ;; Hostile_encounter || conflict ;; Hostile_encounter || forces ;; Military_operation
conflict ;; Hostile_encounter || association ;; Collaboration || formed ;; Coming_to_be
spread ;; Dispersal || arose ;; Coming_to_be || support ;; Supporting || drawing ;; Motion
retreated ;; Escaping || captured ;; Conquering || established ;; Creating
charges ;; Legality || violating ;; Preventing_or_letting || sentenced ;; Legal_rulings
viewed ;; Perception_active || brought ;; Bringing || end ;; Process_end
armed ;; Bearing_arms || overthrow ;; Change_of_leadership
involved ;; Cause_to_be_included
conflict ;; Hostile_encounter || armed ;; Bearing_arms || associated ;; Collaboration || insurgency ;; Change_of_leadership
end ;; Process_end || war ;; Hostile_encounter
participated ;; Participation || conflict ;; Hostile_encounter || fighting ;; Hostile_encounter || attracted ;; Influence
conflict ;; Hostile_encounter || armed ;; Bearing_arms || used ;; Using
insurgency ;; Change_of_leadership
concentrated ;; Aiming || violence ;; Violence
surrounding ;; Surrounding || incidents ;; Hostile_encounter || happened ;; Presence
defense ;; Defending || occurred ;; Coming_to_be || clash ;; Hostile_encounter || cut ;; Removing
killed ;; Killing || observation ;; Perception_active || receiving ;; Receiving || wounded ;; Bodily_harm || opened ;; Action
responded ;; Response || killing ;; Killing || shelling ;; Attack || airstrikes ;; Attack
wounding ;; Bodily_harm
escalation ;; Hostile_encounter || War ;; Hostile_encounter
asserted ;; Statement || intrude ;; Causation || opened ;; Action
claimed ;; Statement || crossed ;; Motion_directional
confirmed ;; Check || informed ;; Telling
stated ;; Statement
condemned ;; Judgment_communication || response ;; Response || announced ;; Expressing_publicly || aid ;; Assistance
responded ;; Response || agreeing ;; Agree_or_refuse_to_act || saying ;; Statement || use ;; Using || assistance ;; Assistance || decline ;; Agree_or_refuse_to_act
Revolution ;; Change_of_leadership || armed ;; Bearing_arms || Uprising ;; Change_of_leadership
began ;; Process_start || revolted ;; Change_of_leadership || uprising ;; Change_of_leadership
uprising ;; Change_of_leadership || joined ;; Participation
uprising ;; Change_of_leadership || insurgents ;; Change_of_leadership || army ;; Hostile_encounter || crushed ;; Destroying
become ;; Becoming || decreed ;; Legal_rulings
garrison ;; Hostile_encounter || closed ;; Self_motion
armed ;; Bearing_arms || War ;; Military_operation || conflict ;; Hostile_encounter
killed ;; Killing
death ;; Death
killed ;; Killing || party ;; Social_event || entered ;; Arriving
sent ;; Sending
ended ;; Process_end || surrendered ;; Surrendering || conflict ;; Hostile_encounter || returned ;; Arriving
interned ;; Arrest || forced ;; Request
began ;; Process_start || known ;; Know || invaded ;; Attack
killed ;; Killing || starved ;; Killing || overthrowing ;; Change_of_leadership || death ;; Death || occupation ;; Attack || sparked ;; Causation
combined ;; Cause_to_amalgamate || held ;; Defending || deaths ;; Death || violent ;; Violence || killings ;; Killing || deaths ;; Death || documented ;; Recording
advanced ;; Cause_to_make_progress || destroy ;; Destroying || weaponry ;; Bearing_arms || faced ;; Confronting_problem || procured ;; Getting
voted ;; Choosing || remaining ;; Temporary_stay || saw ;; Motion || independence ;; Forming_relationships || clashes ;; Hostile_encounter
achieved ;; Achieve
nicknamed ;; Name_conferral || began ;; Process_start || began ;; Process_start || accused ;; Judgment_communication || conflict ;; Hostile_encounter || fighting ;; Hostile_encounter
responded ;; Response || attacks ;; Attack || carrying out ;; Attack
death ;; Death || resulted in ;; Causation
composed ;; Come_together || conflict ;; Hostile_encounter || remained ;; Presence
recruited ;; Becoming_a_member || made up ;; Creating
named ;; Name_conferral
provided ;; Supply || attacks ;; Attack || supported ;; Supporting || denies ;; Response || claims ;; Statement
casualties ;; Catastrophe || dead ;; Death
creating ;; Creating || forced ;; Request
described ;; Statement
signed ;; Sign_agreement
gain ;; Getting || talks ;; Telling || see ;; Perception_active
raids ;; Attack || talks ;; Telling || disrupted ;; Hindering || strikes ;; Attack
vowed ;; Commitment || negotiations ;; Communication || rebel ;; Hostile_encounter
revolution ;; Change_of_leadership || made ;; Manufacturing || armed ;; Bearing_arms || signed ;; Sign_agreement
conducted ;; Action
aimed ;; Aiming || conquest ;; Conquering
taking ;; Conquering || armed ;; Bearing_arms || forces ;; Hostile_encounter || forcing ;; Hostile_encounter || viewing ;; Perception_active
attacked ;; Attack || encircled ;; Besieging
gave ;; Giving || encircled ;; Besieging || forces ;; Hostile_encounter || command ;; Control || advance ;; Cause_to_make_progress || move ;; Motion || stop ;; Preventing_or_letting
reduced ;; Cause_change_of_position_on_a_scale || counter-attacked ;; Attack
fought ;; Hostile_encounter || civil ;; Hostile_encounter || War ;; Hostile_encounter || war ;; Hostile_encounter || negating ;; Preventing_or_letting || victory ;; Earnings_and_losses
began ;; Process_start || emerged ;; Coming_to_be || violence ;; Violence || crushed ;; Bodily_harm || fight ;; Hostile_encounter || appeared ;; Change_sentiment || appeared ;; Change_sentiment || reached ;; Arriving || withstand ;; Defending || war ;; Hostile_encounter
became ;; Becoming || violence ;; Violence || fighting ;; Hostile_encounter || lost ;; Earnings_and_losses || support ;; Supporting || predation ;; Killing
violence ;; Violence || used ;; Using || sale ;; Commerce_sell || saw ;; Self_motion || war ;; Hostile_encounter || war ;; Hostile_encounter || brutality ;; Violence
killed ;; Killing || killed ;; Killing || infiltrated ;; Attack || targeted ;; Aiming || thought ;; Coming_to_believe || forces ;; Military_operation || involved ;; Cause_to_be_included
used ;; Using
estimated ;; Cause_change_of_position_on_a_scale || fatalities ;; Death
began ;; Process_start || party ;; Participation || party ;; Social_event || conflict ;; Hostile_encounter || elections ;; Change_of_leadership || appeared ;; Coming_to_be || defeat ;; Conquering
canceled ;; Change_event_time || took ;; Getting || elections ;; Change_of_leadership || control ;; Control || forcing ;; Request
emerged ;; Coming_to_be || began ;; Process_start || banned ;; Preventing_or_letting || arrested ;; Arrest || campaign ;; Military_operation
armed ;; Bearing_arms || armed ;; Bearing_arms || movement ;; Hostile_encounter || formed ;; Coming_to_be
declared ;; Expressing_publicly || negotiations ;; Communication || motto ;; Statement || made ;; Getting || war ;; Hostile_encounter || progress ;; Cause_to_make_progress
regrouped ;; Cause_to_amalgamate || becoming ;; Becoming
held ;; Giving || talks ;; Communication || elections ;; Change_of_leadership || collapsed ;; Destroying
began ;; Process_start || suggests ;; Convincing || fought ;; Hostile_encounter || targeting ;; Aiming || massacres ;; Killing || involvement ;; Participation
caused ;; Causation || declared ;; Expressing_publicly || desertion ;; Escaping || splits ;; Destroying || attack ;; Attack || ceasefire ;; Process_end
party ;; Social_event || elections ;; Change_of_leadership || supporting ;; Supporting || created ;; Creating
violence ;; Violence || taking ;; Conquering || election ;; Change_of_leadership || declined ;; Cause_change_of_position_on_a_scale
announced ;; Expressing_publicly || fighting ;; Hostile_encounter || spread ;; Dispersal || called ;; Name_conferral || support ;; Supporting || hunted ;; Killing || disappeared ;; Departing
took place ;; Process_start || battle ;; Hostile_encounter
attack ;; Attack || mark ;; Recording || engagement ;; Participation || defence ;; Bearing_arms || resulted in ;; Causation || victory ;; Getting || embarked ;; Process_start
conquered ;; Conquering
rejected ;; Agree_or_refuse_to_act || destroy ;; Destroying || gain ;; Getting || launching ;; Motion || assault ;; Hostile_encounter || ordered ;; Request
started ;; Process_start || closing ;; Process_end
gained ;; Getting
turned ;; Becoming || bombing ;; Attack || aimed ;; Aiming
began ;; Process_start || attacks ;; Attack || reached ;; Arriving
attack ;; Attack || drawing ;; Conquering
lasted ;; Being_in_operation || took part ;; Dispersal
raids ;; Attack || defeated ;; Conquering
dispersed ;; Dispersal || damage ;; Damaging
postponed ;; Change_event_time
lasted ;; Being_in_operation || turned ;; Becoming || defeated ;; Conquering
takes place ;; Process_start
known ;; Know || Battle ;; Hostile_encounter || took place ;; Process_start
took place ;; Process_start || attack ;; Attack
retreat ;; Escaping || combined ;; Cause_to_amalgamate || Battle ;; Hostile_encounter
consolidated ;; Cause_change_of_strength || defeated ;; Conquering || captured ;; Conquering || stretching ;; Expansion || garrisoned ;; Defending || retired ;; Departing
destroying ;; Destroying || reconnaissances ;; Scrutiny || deny ;; Preventing_or_letting || carried out ;; Military_operation
advance ;; Cause_to_make_progress || progressed ;; Cause_to_make_progress
reached ;; Arriving || abandoned ;; GiveUp
defensive ;; Defending
attack ;; Attack || defending ;; Defending
assisted ;; Assistance || supported ;; Supporting || fighting ;; Bearing_arms || attack ;; Attack
captured ;; Conquering || surrendered ;; Surrendering
armed ;; Bearing_arms || fought ;; Hostile_encounter || conflicts ;; Hostile_encounter
inherited ;; Coming_to_be || conflicts ;; Hostile_encounter
came ;; Arriving || raise ;; Creating || conflicts ;; Hostile_encounter
established ;; Creating || control ;; Control
created ;; Creating
put over ;; Military_operation || accept ;; Receiving
end ;; Process_end || conflicts ;; Hostile_encounter
participated ;; Participation || wars ;; Hostile_encounter || diverted ;; Cause_change_of_position_on_a_scale
conducted ;; Action || forces ;; Causation || unarmed ;; Bearing_arms || massacre ;; Killing
gave ;; Giving || killed ;; Killing || massacre ;; Killing || monument ;; Building || reported ;; Reporting || said ;; Statement
operational ;; Using
victims ;; Death
victims ;; Death
killed ;; Killing || burnt ;; Destroying
fought ;; Hostile_encounter || massacre ;; Killing || joined ;; Becoming_a_member
accused ;; Judgment_communication || conducting ;; Action || forces ;; Causation
massacre ;; Killing || crimes ;; Committing_crime
discussed ;; Communication || visited ;; Traveling || disclosed ;; Reveal_secret || massacre ;; Killing || massacre ;; Killing || report ;; Reporting
conflict ;; Hostile_encounter || conflict ;; Hostile_encounter || armed ;; Bearing_arms || party ;; Social_event || revolutionary ;; Change_of_leadership
began ;; Process_start || conflict ;; Hostile_encounter
deaths ;; Death || war ;; Hostile_encounter || colonization ;; Control || making it ;; Causation
casualties ;; Catastrophe || death ;; Death || due ;; Causation || targeting ;; Aiming || includes ;; Cause_to_be_included
conflict ;; Hostile_encounter || deaths ;; Death || become ;; Becoming || dormant ;; GiveUp || dropped ;; Cause_change_of_position_on_a_scale
violence ;; Violence || conflict ;; Hostile_encounter || resurgences ;; Recovering || erupted ;; Process_start
conflict ;; Hostile_encounter || conflict ;; Hostile_encounter || conflict ;; Hostile_encounter || armed ;; Bearing_arms || lasted ;; Process_end || making it ;; Causation
shoot down ;; Use_firearm || War ;; Military_operation || occurred ;; Presence
shot down ;; Use_firearm || operated ;; Using || flying ;; Motion || crashed ;; Catastrophe
conducted ;; Action || received ;; Receiving || support ;; Supporting || armed ;; Bearing_arms || independence ;; Change_of_leadership || incident ;; Catastrophe
transport ;; Bringing || carrying ;; Bringing
According ;; Reporting || shot down ;; Use_firearm || reports ;; Reporting
died ;; Death || crash ;; Catastrophe
assassinate ;; Killing
associated ;; Collaboration
armed ;; Bearing_arms || party ;; Participation || assassination ;; Killing || control ;; Control || forces ;; Military_operation || make ;; Bringing || wrest ;; Change_of_leadership
demands ;; Request || included ;; Cause_to_be_included
resistance ;; Preventing_or_letting || plot ;; Arranging || efforts ;; Supporting || overthrow ;; Change_of_leadership
arrest ;; Arrest || assassination ;; Killing || intended ;; Arranging || led ;; Causation || military ;; Military_operation || failure ;; Earnings_and_losses || executed ;; Killing
armed ;; Bearing_arms || conflict ;; Hostile_encounter
left ;; Departing
defensive ;; Defending || resisted ;; Defending || sent ;; Sending || reinforced ;; Cause_change_of_strength
killed ;; Killing || meeting ;; Social_event
leave ;; Departing || wounded ;; Bodily_harm
fled ;; Escaping
surrendered ;; Surrendering || captured ;; Conquering || reinforced ;; Cause_change_of_strength
executed ;; Rewards_and_punishments || sentenced ;; Legal_rulings
sent ;; Sending || settled on ;; Resolve_problem || war ;; Hostile_encounter
allowed ;; Preventing_or_letting || return ;; Arriving
stayed ;; Temporary_stay || became ;; Becoming || state ;; Statement
achieved ;; Getting || granted ;; Giving || recognition ;; Know
recognized ;; Know
war ;; Hostile_encounter || leading ;; Causation
known ;; Know || go through ;; Self_motion
received ;; Receiving || war ;; Hostile_encounter || brought ;; Bringing
posting ;; Sending || neglected ;; GiveUp
apprehended ;; Arrest
awakened ;; Recovering
Jumping ;; Self_motion || sprawled ;; Expansion
attack ;; Attack || realized ;; Know
dead ;; Death || assailants ;; Killing || beaten ;; Bodily_harm
attacker ;; Attack
fell ;; Motion_directional
attack ;; Attack || attacked ;; Attack || named ;; Name_conferral
killed ;; Killing || agree ;; Agree_or_refuse_to_act || attack ;; Attack || destroyed ;; Destroying || reports ;; Reporting
killed ;; Killing || chose ;; Choosing || deepened ;; Cause_change_of_strength || agree ;; Agree_or_refuse_to_act || lash ;; Bodily_harm
discovered ;; Know || march ;; Self_motion || traveling ;; Traveling
sparked ;; Causation || expand ;; Expansion || discovery ;; Know
arrived in ;; Arriving || took ;; Bringing
lies ;; Placing
open ;; Openness || shelled ;; Attack
Battle ;; Hostile_encounter || operations ;; Military_operation || advance ;; Self_motion || operations ;; Military_operation || passed ;; Motion_directional
became ;; Becoming || defence ;; Defending || receive ;; Receiving || prepared ;; GetReady
Battle ;; Hostile_encounter || defence ;; Defending
battle ;; Hostile_encounter || attack ;; Attack || stopped ;; Preventing_or_letting
surrender ;; Surrendering || consolidated ;; Cause_change_of_strength || combined ;; Cause_to_amalgamate || operation ;; Military_operation || inflicted ;; Bodily_harm || casualties ;; Death || forced ;; Causation || occupied ;; Control
repulsed ;; Defending || defences ;; Defending || machine-gun ;; Use_firearm || artillery-fire ;; Bearing_arms || exploit ;; Hold || went ;; Self_motion
held ;; Hold || battle ;; Hostile_encounter || operations ;; Military_operation || used ;; Using
became ;; Becoming || Battle ;; Hostile_encounter || recaptured ;; Conquering || lost ;; Earnings_and_losses
known ;; Know
took place ;; Process_start
sent ;; Sending || support ;; Supporting || establish ;; Building || establish ;; Building || ascertain ;; Check
attacked ;; Attack || receive ;; Receiving || commanding ;; Control || launched ;; Process_start
changed ;; Change || armed ;; Bearing_arms || led to ;; Causation
killing ;; Killing || loss ;; Earnings_and_losses || dead ;; Death || landed ;; Self_motion || captured ;; Conquering
refuse ;; Agree_or_refuse_to_act || negotiate ;; Communication
offensive ;; Attack || armed ;; Bearing_arms || revolution ;; Change_of_leadership || started ;; Process_start || war ;; Hostile_encounter || re-emerged ;; Coming_to_be || control ;; Control || alliance ;; Collaboration || forces ;; Military_operation || seize ;; Conquering
ended ;; Process_end || resulting ;; Causation || led to ;; Causation
create ;; Creating || stated ;; Statement
fought ;; Quarreling || conflict ;; Hostile_encounter || forces ;; Military_operation
conducted ;; Action
forces ;; Military_operation
opposed ;; Agree_or_refuse_to_act || claimed ;; Statement
captured ;; Conquering || reversed ;; Change
fought ;; Hostile_encounter || control ;; Control || war ;; Hostile_encounter
caused ;; Causation || took place ;; Process_start
conducted ;; Action || fought ;; Hostile_encounter || party ;; Social_event
controlled ;; Control
social ;; Social_event || controlled ;; Control
growth ;; Cause_change_of_position_on_a_scale || rise ;; Cause_change_of_position_on_a_scale || experienced ;; Perception_active
improved ;; Cause_to_make_progress || awakened ;; Know
causing ;; Causation || led to ;; Causation || militarisation ;; Bearing_arms || led to ;; Causation
carried out ;; Action || supplied ;; Supply
began ;; Process_start || reinforced ;; Cause_change_of_strength
leading to ;; Causation || engagements ;; Hostile_encounter
violence ;; Violence || became ;; Becoming || warfare ;; Hostile_encounter
died ;; Death || prisoners ;; Arrest || disease ;; Catastrophe
perished ;; Death
establish ;; Creating || plan ;; Arranging || passed ;; Motion_directional
cancelled ;; Change_event_time || emerged ;; Coming_to_be || defeat ;; Conquering
social ;; Social_event || reunited ;; Cause_to_amalgamate
means ;; Protest || dictate ;; Statement
freedom ;; Releasing
ended ;; Process_end || agreed ;; Agree_or_refuse_to_act || accept ;; Receiving || armed ;; Bearing_arms
implemented ;; Action
addressed ;; Statement || apply ;; Giving || separate ;; Forming_relationships
agreement ;; Agree_or_refuse_to_act || organized ;; Arranging || marked ;; Recording
emerging ;; Coming_to_be || address ;; Statement || developing ;; Cause_to_make_progress
gave ;; Giving || leaving ;; GiveUp
passed ;; Giving
named ;; Name_conferral || death ;; Death || forbidden ;; Agree_or_refuse_to_act
permitted ;; Preventing_or_letting || allow ;; Preventing_or_letting || subscribe ;; Agree_or_refuse_to_act || leave ;; Departing || freedom ;; Releasing
given ;; Giving || freedom ;; Releasing || retain ;; Defending
helped ;; Assistance
restored ;; Recovering || overturned ;; Change
emigrated ;; Escaping || lived ;; Placing
emigration ;; Escaping
began ;; Process_start || end ;; Process_end || conflict ;; Hostile_encounter || campaign ;; Motion
ended ;; Process_end || armed ;; Bearing_arms || called ;; Request || opposed ;; Agree_or_refuse_to_act
involved ;; Cause_to_be_included
targeted ;; Aiming || attacks ;; Attack || bomb ;; Attack
bombings ;; Attack || cause ;; Causation || disruption ;; Damaging || carried out ;; Action
campaign ;; Motion
declared ;; Expressing_publicly || deployment ;; Arranging || ending ;; Process_end || end ;; Process_end
attacks ;; Attack || service ;; Supply || target ;; Aiming
killed ;; Killing || service ;; Supply || campaign ;; Motion
carried out ;; Action || died ;; Death || bombing ;; Attack || killed ;; Killing
armed ;; Bearing_arms || rescue ;; Rescuing || expedition ;; Scrutiny || Expedition ;; Traveling || carried out ;; Action
decided ;; Deciding || imprisoned ;; Arrest || get ;; Getting || requests ;; Request || assistance ;; Assistance
response ;; Response || required ;; Imposing_obligation || expedition ;; Traveling || launched ;; GetReady
overcome ;; Conquering || captured ;; Conquering || rescued ;; Rescuing || expedition ;; Scrutiny || battle ;; Hostile_encounter || obstacles ;; Hindering || victorious ;; Statement
hailed ;; Judgment_communication || achieving ;; Achieve || return ;; Arriving || expedition ;; Traveling
described ;; Statement
armed ;; Bearing_arms || conflict ;; Hostile_encounter
began ;; Process_start || leading to ;; Causation
resulted in ;; Causation || led to ;; Causation
led to ;; Causation || involvement ;; Participation
issue ;; Creating
occurring ;; Presence
backed ;; Self_motion || entering ;; Participation
reports ;; Reporting || agitated ;; Influence
recovered ;; Recovering || feared ;; Perception_active || reverse ;; Change
lobbied ;; Convincing || going to ;; Self_motion
ignored ;; Response
sank ;; Removing || party ;; Social_event || avoid ;; Preventing_or_letting || exploded ;; Use_firearm || pushed ;; Motion
withdrawal ;; Escaping || help ;; Assistance || authorizing ;; Preventing_or_letting || gain ;; Getting || joint ;; Collaboration || demanding ;; Request || use ;; Using || signed ;; Sign_agreement
response ;; Response || severed ;; Process_end
began ;; Process_start
declared ;; Expressing_publicly
fought ;; Hostile_encounter
allowing ;; Preventing_or_letting || disembark ;; Arriving || attacks ;; Attack || well knew ;; Know || facing ;; Perception_active || wasted ;; Expend_resource
obtained ;; Getting || invaders ;; Attack || surrender ;; Surrendering || fighting ;; Hostile_encounter
protect ;; Defending || sued ;; Request || sunk ;; Motion || recalled ;; Request
negotiated ;; Communication || allowed ;; Preventing_or_letting || control ;; Control || ceded ;; Surrendering
involved ;; Participation || cover ;; Filling
defeat ;; Conquering || loss ;; Earnings_and_losses || provoked ;; Causation
gained ;; Getting || spanning ;; Self_motion || debate ;; Quarreling
engagement ;; Hostile_encounter
issued ;; Creating || armed ;; Bearing_arms || held off ;; Defending || inflicted ;; Attack
took place ;; Process_start || received ;; Receiving
defensive ;; Defending || holding off ;; Defending
construction ;; Building
brought ;; Bringing || end ;; Process_end
abandonment ;; GiveUp
revolution ;; Change_of_leadership || revolution ;; Change_of_leadership || war ;; Hostile_encounter || conflict ;; Hostile_encounter || struggle ;; Hostile_encounter
end ;; Process_end || declaration ;; Expressing_publicly || independence ;; Change_of_leadership || recognition ;; Judgment_communication || took place ;; Process_start
involved ;; Cause_to_be_included || armed ;; Bearing_arms || upheavals ;; Social_event || interventions ;; Participation || conflict ;; Hostile_encounter || struggle ;; Hostile_encounter
control ;; Control || control ;; Control || forces ;; Military_operation
became ;; Becoming || independence ;; Change_of_leadership || stalemate ;; Hostile_encounter || recognised ;; Know
revolution ;; Change_of_leadership || marked ;; Recording || end ;; Process_end || administration ;; Control
changed ;; Change || reducing ;; Cause_change_of_position_on_a_scale
gain ;; Getting || improve ;; Cause_to_make_progress
took place ;; Process_start || armed ;; Bearing_arms || conflict ;; Hostile_encounter || War ;; Military_operation
spread ;; Dispersal || sale ;; Commerce_sell || dispute ;; Quarreling || war ;; Military_operation || sparked ;; Process_start
brought in ;; Bringing || fought ;; Hostile_encounter || fluctuated ;; Cause_change_of_position_on_a_scale
ended ;; Process_end || accepting ;; Receiving || ceasefire ;; Process_end
suffered ;; Bodily_harm || claims ;; Statement || war ;; Military_operation
claimed ;; Statement || impose ;; Action
invasion ;; Attack || said ;; Telling || leading to ;; Causation
begun ;; Process_start || unlawful ;; Legality || armed ;; Bearing_arms || report ;; Reporting || claims ;; Statement || launching ;; Motion || attack ;; Attack || war ;; Military_operation || observed ;; Perception_active
stated ;; Statement || criminal ;; Committing_crime
opposed ;; Agree_or_refuse_to_act || suggestions ;; Convincing || conflict ;; Hostile_encounter || War ;; Military_operation || war ;; Military_operation || rejecting ;; Agree_or_refuse_to_act
referred ;; Statement
supply ;; Supply || Operation ;; Military_operation || mission ;; Military_operation || took place ;; Process_start
undertaken ;; Action
invasion ;; Attack
known ;; Know || armed ;; Bearing_arms || called ;; Name_conferral
involved ;; Cause_to_be_included || bombers ;; Attack || towing ;; Self_motion
planned ;; Arranging || complement ;; Supply
shot down ;; Use_firearm
lost ;; Earnings_and_losses || arrived in ;; Arriving || participate ;; Participation || invasion ;; Attack
supply ;; Supply || landing ;; Self_motion || becoming ;; Becoming || casualties ;; Death
retreat ;; Escaping || occupied ;; Control || forced ;; Request || re-establish ;; Recovering || Campaign ;; Military_operation || Operation ;; Hostile_encounter || battle ;; Hostile_encounter || Battle ;; Hostile_encounter || War ;; Military_operation || led ;; Bringing
joined ;; Becoming_a_member || defensive ;; Defending || battle ;; Hostile_encounter
stop ;; Preventing_or_letting || counterattack ;; Attack || led ;; Bringing
counterattack ;; Attack || attacking ;; Attack || armed ;; Bearing_arms
fought ;; Hostile_encounter || operations ;; Hostile_encounter || involved ;; Cause_to_be_included || make it ;; Causation || battles ;; Hostile_encounter || war ;; Military_operation
battle ;; Hostile_encounter || took place ;; Process_start
battle ;; Hostile_encounter || took place ;; Process_start
reported ;; Reporting || victory ;; Earnings_and_losses || led ;; Bringing
retreat ;; Escaping || occupied ;; Control || battles ;; Hostile_encounter
Battle ;; Hostile_encounter || fought ;; Hostile_encounter || Rebellion ;; Violence
sent ;; Sending || quell ;; Preventing_or_letting || conjunction ;; Collaboration || force ;; Control || uprising ;; Violence
arrive at ;; Arriving
organized ;; Arranging || defence ;; Defending || rebels ;; Attack
fire ;; Use_firearm || take ;; Conquering || providing ;; Supply
attempt ;; Influence || flank ;; Attack || well-armed ;; Bearing_arms || encountered ;; Hostile_encounter
retreated ;; Escaping
battle ;; Hostile_encounter || marked ;; Recording || followed ;; Causation || victory ;; Earnings_and_losses
war ;; Hostile_encounter || taking place ;; Process_start
grew ;; Expansion || resistance ;; Defending
began ;; Process_start || engaging ;; Forming_relationships || rebel ;; Change_of_leadership
armed ;; Bearing_arms || managed ;; Control || overthrow ;; Change_of_leadership
competing ;; Competition || began ;; Process_start || followed ;; Self_motion
fighting ;; Hostile_encounter || collapsed ;; Destroying
arrival ;; Arriving || followed ;; Self_motion || forces ;; Military_operation
fighting ;; Hostile_encounter
became ;; Becoming || failed ;; Earnings_and_losses
casualties ;; Catastrophe || reestablished ;; Recovering || withdrew ;; Escaping
return ;; Arriving || collapse ;; Destroying || law ;; Legality
established ;; Creating
decrease ;; Cause_change_of_position_on_a_scale || removing ;; Removing || led to ;; Causation || fighting ;; Hostile_encounter || conflicts ;; Hostile_encounter
established ;; Creating || followed ;; Self_motion
reduced ;; Cause_change_of_position_on_a_scale || conflict ;; Hostile_encounter || halted ;; Process_end || took place ;; Process_start || conflict ;; Hostile_encounter
fighting ;; Hostile_encounter || lower ;; Cause_change_of_position_on_a_scale
formed ;; Coming_to_be
fighting ;; Hostile_encounter || control ;; Control || splintered ;; Destroying
topped ;; Placing
establish ;; Creating || following ;; Self_motion || meetings ;; Social_event || entered ;; Arriving || fight ;; Hostile_encounter
integrated ;; Cause_to_amalgamate
established ;; Creating
describe ;; Statement || begun ;; Process_start || progress ;; Achieve
carried out ;; Action
conducted ;; Action || armed ;; Bearing_arms || supporting ;; Supporting || differed ;; Change
purpose ;; Aiming
carried out ;; Action
rendered ;; Causation
halted ;; Process_end || carried out ;; Action || become ;; Becoming || concentrated ;; Aiming
revolution ;; Change_of_leadership || took place ;; Process_start
included ;; Cause_to_be_included
removal ;; Removing
revolution ;; Change_of_leadership
revolution ;; Change_of_leadership || reaction ;; Response
abdicated ;; Change_of_leadership || granted ;; Giving
loss ;; Earnings_and_losses || suffered ;; Bodily_harm
took ;; Conquering || gained ;; Getting
retreated ;; Escaping || dissolved ;; Dispersal || replaced ;; Change_of_leadership
arrived in ;; Arriving || brought ;; Bringing
organized ;; Arranging || decide ;; Deciding
govern ;; Control || denied ;; Agree_or_refuse_to_act || established ;; Building
caused ;; Causation || unrest ;; Hostile_encounter || resigned ;; Change_of_leadership
send ;; Sending || included ;; Cause_to_be_included || join ;; Becoming_a_member || formed ;; Coming_to_be || invited ;; Request
outbreak ;; Attack
began ;; Process_start || issued ;; Creating
revolution ;; Change_of_leadership || occurred ;; Coming_to_be
debate ;; Communication || achieve ;; Achieve || change ;; Change || ready ;; GetReady || declaration ;; Expressing_publicly
issued ;; Creating || declaration ;; Expressing_publicly
destroy ;; Destroying || posed ;; Causation
reported ;; Reporting || consist ;; Cause_to_be_included
discovered ;; Know || sailed ;; Self_motion || driven ;; Motion
burnt ;; Use_firearm || returned ;; Arriving
sent ;; Sending || eliminate ;; Removing || attacking ;; Attack || capturing ;; Conquering
reduced ;; Cause_change_of_position_on_a_scale || led ;; Bringing
prepare ;; GetReady || ended ;; Process_end
took ;; Conquering || Capture ;; Conquering || occurred ;; Presence
fortification ;; Defending || occupied ;; Control
captured ;; Conquering || consisting ;; Containing || requisitioned ;; Removing
sent ;; Sending || help ;; Assistance || leave ;; Departing || prepared ;; GetReady
allowed ;; Preventing_or_letting || choice ;; Choosing || leave ;; Departing || defeated ;; Conquering
criticized ;; Judgment_communication || return ;; Arriving || beheaded ;; Killing
capture ;; Conquering || permitted ;; Preventing_or_letting || encircle ;; Besieging
carried out ;; Action
sailed ;; Self_motion || composed ;; Come_together || commanded ;; Control
appeared ;; Presence
occasioned ;; Causation || landed ;; Self_motion
defended ;; Defending
aided ;; Assistance || established ;; Building
constructed ;; Building || prepared ;; GetReady
chose ;; Choosing || decided ;; Deciding
ravaged ;; Destroying || entered ;; Arriving
sailed ;; Self_motion || remained ;; Presence || held ;; Hold
burned ;; Bodily_harm || destroyed ;; Destroying
weakened ;; Cause_change_of_strength || Cyclone ;; Catastrophe || Cyclone ;; Catastrophe || cyclones ;; Catastrophe || landfall ;; Arriving || moved ;; Motion
began ;; Process_start
strengthened ;; Cause_change_of_strength || emerging ;; Coming_to_be || storm ;; Catastrophe || drifted ;; Motion
intensified ;; Cause_change_of_strength || record ;; Recording || Aided ;; Assistance || cyclones ;; Catastrophe || reach ;; Arriving
moved ;; Motion || degenerated ;; Change
damage ;; Damaging || causing ;; Causation || record ;; Recording || rainfall ;; Catastrophe || produced ;; Causation || flooding ;; Catastrophe
final ;; Process_end || damage ;; Damaging || landfall ;; Arriving || reported ;; Reporting
injuries ;; Bodily_harm || cyclone ;; Catastrophe || preparations ;; GetReady || deaths ;; Death
name ;; Name_conferral
took place ;; Process_start
used ;; Using || tasked ;; Request
tasked ;; Request
departed ;; Departing
prepared ;; GetReady
damaged ;; Damaging
began ;; Process_start
engage ;; Participation
wounded ;; Bodily_harm
hauled ;; Motion
Rebellion ;; Change_of_leadership
allied ;; Collaboration || went ;; Change || signed ;; Sign_agreement
found ;; Know
held ;; Defending || gained ;; Getting || elevated ;; Cause_change_of_position_on_a_scale
handed over ;; Giving
raising ;; Cause_change_of_position_on_a_scale || angered ;; Judgment_communication
mingled ;; Action
disestablished ;; Destroying
given ;; Giving || replaced ;; Change_of_leadership
uprising ;; Change_of_leadership || called ;; Name_conferral
contact ;; Communication
declared ;; Expressing_publicly
legitimate ;; Legality || stated ;; Statement || belonged ;; Presence
marched ;; Self_motion || occupied ;; Attack || defeated ;; Conquering
gathered ;; Come_together || marched ;; Motion_directional
clashed ;; Hostile_encounter
celebrated ;; Social_event || defeat ;; Conquering
withdraw ;; Escaping || gained ;; Getting || signed ;; Sign_agreement
fighting ;; Hostile_encounter
took over ;; Change_of_leadership
gave up ;; Giving || ending ;; Process_end
ordered ;; Arranging
ready ;; GetReady || decreasing ;; Cause_change_of_position_on_a_scale || fight ;; Hostile_encounter
defeated ;; Conquering || crushed ;; Destroying
fled ;; Escaping || hid ;; Hiding_objects
arrested ;; Arrest || brought ;; Bringing
issued ;; Creating
buried ;; Placing
cede ;; Surrendering
re-united ;; Cause_to_amalgamate
became ;; Becoming || rise ;; Cause_change_of_position_on_a_scale
deals ;; Resolve_problem
became ;; Becoming
operation ;; Military_operation || combined ;; Cause_to_amalgamate
support ;; Supporting || sought ;; Getting || risen ;; Cause_change_of_position_on_a_scale
delayed ;; Change_event_time || besieged ;; Besieging || supplied ;; Supply || defeated ;; Conquering
remaining ;; Presence || led ;; Conquering || turned ;; Change
defended ;; Defending
siege ;; Besieging || approaches ;; Arriving || opening ;; Openness || prepared ;; GetReady
took ;; Creating || wounded ;; Bodily_harm
damaged ;; Damaging || driving ;; Self_motion || launched ;; Process_start
extended ;; Expansion || engaged ;; Participation
surrender ;; Surrendering
became ;; Becoming || siege ;; Besieging || remained ;; Presence
conducted ;; Action
siege ;; Besieging
starts ;; Process_start || siege ;; Besieging || established ;; Building
broken ;; Destroying || defenders ;; Defending || defence ;; Defending || supplies ;; Supply || forces ;; Hostile_encounter || siege ;; Besieging || batteries ;; Use_firearm || established ;; Building
fell ;; Motion_directional || thwarted ;; Hindering
combined ;; Cause_to_amalgamate || surrendering ;; Surrendering || opening ;; Openness || capitulated ;; Surrendering
opened ;; Openness || fell ;; Motion || march ;; Self_motion
escaped ;; Escaping || made ;; Manufacturing || defences ;; Defending || attack ;; Attack || prepare ;; GetReady
took place ;; Process_start
tournament ;; Competition
matches ;; Competition || competing ;; Competition
included ;; Cause_to_be_included || Qualifiers ;; Participation
moved ;; Motion || found ;; Know || scheduled ;; Arranging || complete ;; Process_end || preparations ;; GetReady || take place ;; Hold || inspection ;; Scrutiny
tournament ;; Competition || beating ;; Competition
scored ;; Getting || tournament ;; Competition || claimed ;; Statement
held ;; Defending || end ;; Process_end || rescuing ;; Rescuing
damaged ;; Damaging || failure ;; Earnings_and_losses || ensued ;; Coming_to_be
blamed ;; Judgment_communication || election ;; Change_of_leadership || loss ;; Earnings_and_losses
encountered ;; Hostile_encounter || aborted ;; Process_end
sent ;; Sending || arrived in ;; Arriving
storm ;; Attack || showed ;; Presence || caught ;; Hold
decided ;; Deciding || aborted ;; Process_end
advised ;; Convincing || discussed ;; Communication
crashed ;; Catastrophe || withdraw ;; Removing || contained ;; Containing || prepared ;; GetReady
killed ;; Killing || destroyed ;; Destroying
used ;; Using
claimed ;; Statement || protect ;; Defending || stopped ;; Process_end
attacked ;; Attack || guns ;; Use_firearm || bombs ;; Use_firearm
killed ;; Killing || describing ;; Statement || working ;; Employment || injured ;; Bodily_harm
victims ;; Bodily_harm
letting ;; Preventing_or_letting || slitting ;; Bodily_harm || attackers ;; Attack || asked ;; Request
tied ;; Connect || victim ;; Bodily_harm || dragged ;; Body_movement
criticized ;; Judgment_communication || prepared ;; GetReady || attack ;; Attack || escape ;; Escaping || assailants ;; Attack || forces ;; Military_operation || allowing ;; Preventing_or_letting
occurred ;; Presence || Combat ;; Hostile_encounter || skirmish ;; Hostile_encounter || War ;; Hostile_encounter || Wars ;; Hostile_encounter
battle ;; Hostile_encounter || controlled ;; Control || took place ;; Process_start || invasion ;; Attack || prepared ;; GetReady
sent ;; Sending || affair ;; Hostile_encounter || saying ;; Statement || engaging ;; Participation
surprised ;; Change_sentiment
retreat ;; Escaping || chose ;; Choosing || cross ;; Motion_directional || ordered ;; Arranging || avoiding ;; Preventing_or_letting || disaster ;; Catastrophe || engage ;; Participation
besiege ;; Besieging || objective ;; Aiming
fighting ;; Hostile_encounter || hard ;; Confronting_problem || assault ;; Attack || launched ;; Process_start || suffering ;; Bodily_harm || casualties ;; Death
revolution ;; Social_event || revolution ;; Social_event || revolutions ;; Social_event
ended ;; Process_end || led to ;; Causation || creation ;; Creating
elected ;; Choosing || overthrow ;; Change_of_leadership || ruled ;; Control
became ;; Becoming || steered ;; Self_motion
became ;; Becoming || rose ;; Cause_change_of_position_on_a_scale || turn ;; Change || rebellion ;; Hostile_encounter || insurrection ;; Violence
elected ;; Choosing || support ;; Supporting
suspended ;; Process_end || elected ;; Choosing || establishing ;; Creating
become ;; Becoming
work ;; Employment || revolution ;; Social_event || established ;; Creating || established ;; Creating || created ;; Creating
organization ;; Arranging || scheme ;; Arranging || established ;; Creating || preparing ;; GetReady
Uprising ;; Change_of_leadership || tensions ;; Hostile_encounter || led to ;; Causation
occurred ;; Coming_to_be
falling ;; Motion_directional || resulted in ;; Causation
siege ;; Besieging
contrary ;; Response
siege ;; Besieging || hold ;; Defending || prepared ;; GetReady || fortified ;; Cause_change_of_strength || defended ;; Defending
sieges ;; Besieging || confronting ;; Confronting_problem || supplies ;; Supply || defended ;; Defending || cut off ;; Hindering || launched ;; Motion || blocking ;; Hiding_objects || driving ;; Motion
consisting ;; Cause_to_be_included || driven ;; Motion
surrendered ;; Surrendering || siege ;; Besieging
hindered ;; Hindering
held ;; Defending || captured ;; Conquering || regaining ;; Getting || played ;; Motion
lost ;; Earnings_and_losses || wars ;; Hostile_encounter || combined ;; Cause_to_amalgamate
ended ;; Process_end
fighting ;; Hostile_encounter
took place ;; Process_start || involved ;; Participation
drawn ;; Motion
crippled ;; Hindering || preparing ;; GetReady || overcome ;; Resolve_problem || invade ;; Attack
became ;; Becoming || forestalled ;; Preventing_or_letting
achieved ;; Achieve || attack ;; Attack || gains ;; Getting || end ;; Process_end || defeated ;; Earnings_and_losses
handicapped ;; Hindering || defend ;; Defending
battle ;; Hostile_encounter || victory ;; Earnings_and_losses || war ;; Military_operation
war ;; Military_operation
influence ;; Influence || control ;; Control || referred ;; Name_conferral
captured ;; Conquering || war ;; Military_operation
expedition ;; Self_motion || mounted ;; Process_start || recover ;; Conquering
presence ;; Presence || attack ;; Attack || attempting ;; Hold || prepared ;; GetReady || landed ;; Self_motion || meet ;; Come_together
advanced ;; Self_motion || ambushed ;; Attack || casualties ;; Death || forced ;; Causation
landing ;; Self_motion || landing ;; Self_motion
began ;; Process_start || combined ;; Cause_to_amalgamate
carried out ;; Military_operation || circling ;; Motion
began ;; Process_start || reached ;; Arriving || preparing ;; GetReady
landed ;; Self_motion || delayed ;; Change_event_time
arrived ;; Arriving
caused ;; Causation || casualties ;; Catastrophe || crash ;; Catastrophe
assembled ;; Come_together || commenced ;; Process_start || advance ;; Motion
defeating ;; Conquering || defeated ;; Conquering
withdraw ;; Escaping || elected ;; Choosing || fight ;; Hostile_encounter
withdraw ;; Escaping || losses ;; Earnings_and_losses || retreat ;; Escaping || struggle ;; Confronting_problem || proved ;; Coming_to_believe
reached ;; Arriving || advancing ;; Motion
delayed ;; Change_event_time || upgrade ;; Cause_to_make_progress
opened ;; Openness
became ;; Becoming
storm ;; Catastrophe || recorded ;; Recording || cyclone ;; Catastrophe
moved ;; Motion || Developing ;; Coming_to_be || weak ;; Cause_change_of_strength
presence ;; Presence || storm ;; Catastrophe || strengthening ;; Cause_change_of_strength || falling ;; Motion_directional || led to ;; Causation
warnings ;; Warning || developed ;; Coming_to_be || strength ;; Cause_change_of_strength || predicted ;; Statement || issued ;; Statement
covered ;; Hiding_objects || storm ;; Catastrophe || confined ;; Hindering || due ;; Causation
moved ;; Motion || reaching ;; Arriving || weakened ;; Cause_change_of_strength || dissipating ;; Dispersal
caused ;; Causation || storm ;; Catastrophe
storm ;; Catastrophe || stranded ;; Motion || breakup ;; Destroying || storm ;; Catastrophe || beforehand ;; GetReady || led to ;; Causation || derived ;; Giving
race ;; Competition || covering ;; Hiding_objects || run ;; Self_motion
race ;; Competition
competition ;; Competition || race ;; Competition || running ;; Self_motion || experienced ;; Change_sentiment
beginning ;; Process_start || race ;; Competition || ending ;; Process_end || takes place ;; Process_start
organized ;; Arranging
race ;; Competition || run ;; Self_motion
race ;; Competition || form ;; Coming_to_be
race ;; Competition || known ;; Know || fill ;; Filling
ready ;; GetReady || given out ;; Giving || race ;; Competition || use ;; Using || set up ;; Building || awards ;; Judgment_communication
held ;; Defending
compete ;; Competition || called ;; Name_conferral
take ;; Creating || prepare ;; GetReady
called ;; Name_conferral || serve ;; Assistance || gathering ;; Come_together
streamed ;; Motion || event ;; Social_event || parade ;; Social_event || broadcast ;; Expressing_publicly
picked up ;; Choosing || televised ;; Communication || debuted ;; Process_start || get ;; Getting || parade ;; Social_event || parade ;; Social_event || broadcast ;; Expressing_publicly || broadcast ;; Expressing_publicly
critiqued ;; Judgment_communication || parade ;; Social_event || include ;; Cause_to_be_included
battle ;; Hostile_encounter || battle ;; Hostile_encounter || called ;; Name_conferral || led ;; Causation || took place ;; Process_start
attacked ;; Attack || campaign ;; Military_operation
defensive ;; Defending || prepared ;; GetReady || advance ;; Self_motion || forewarned ;; Warning
captured ;; Conquering || battle ;; Hostile_encounter || victory ;; Getting || military ;; Military_operation || occupied ;; Hold
formed ;; Coming_to_be || attack ;; Attack || opened ;; Openness || fire ;; Use_firearm || retreated ;; Escaping
flee ;; Escaping || failed ;; Earnings_and_losses || moved ;; Self_motion || forcing ;; Causation || occupy ;; Control
began ;; Process_start || victory ;; Getting || celebrate ;; Social_event
began ;; Process_start || opened ;; Openness || waver ;; Cause_change_of_position_on_a_scale || fire ;; Use_firearm
attack ;; Attack || caught ;; Hold || distracted ;; Causation || paused ;; Process_end || retreat ;; Escaping || turned ;; Change || launched ;; Process_start
killed ;; Killing || scattered ;; Dispersal || loss ;; Earnings_and_losses || military ;; Military_operation
engagement ;; Hostile_encounter || fought ;; Hostile_encounter || Action ;; Response || Wars ;; Hostile_encounter
supplies ;; Supply || fought ;; Hostile_encounter || action ;; Response || cut ;; Removing || shipping ;; Motion || preventing ;; Preventing_or_letting || contact ;; Communication
invasions ;; Attack || planned ;; Arranging || preparation ;; GetReady
supply ;; Supply || use ;; Using || response ;; Response || losing ;; Earnings_and_losses || bring ;; Bringing || military ;; Military_operation || made ;; Action
made ;; Action || despatched ;; Sending
captured ;; Conquering || War ;; Hostile_encounter || took ;; Giving
attacked ;; Attack || Discovered ;; Know || led ;; Causation
engagement ;; Hostile_encounter || fire ;; Catastrophe || overwhelmed ;; Destroying || capturing ;; Conquering
captured ;; Conquering || drawn ;; Motion || ships ;; Motion || launched ;; Motion || campaign ;; Military_operation || completed ;; Process_end || lasting ;; Achieve || gathered ;; Come_together
Championship ;; Competition || tournament ;; Competition || run ;; GetReady
contested ;; Competition || tournament ;; Competition
tournament ;; Competition || tournament ;; Competition || Participants ;; Participation || separate ;; Hold || separated ;; Forming_relationships
contested ;; Competition || tournament ;; Competition
played ;; Competition
finished ;; Process_end || Championship ;; Competition || gaining ;; Getting
finished ;; Process_end
damage ;; Damaging || cyclone ;; Catastrophe || brought ;; Bringing
storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || originated ;; Coming_to_be
traversing ;; Motion_directional || crossed ;; Motion_directional || entered ;; Arriving
become ;; Becoming || storm ;; Catastrophe || allowed ;; Preventing_or_letting
entered ;; Arriving
became ;; Becoming || reaching ;; Arriving || hurricane ;; Catastrophe
weakened ;; Cause_change_of_strength || deteriorating ;; Cause_change_of_position_on_a_scale
became ;; Becoming || storm ;; Catastrophe || indicating ;; Causation
storm ;; Catastrophe || hurricane ;; Catastrophe || storm ;; Catastrophe || prompted ;; Response
warned ;; Warning || storm ;; Catastrophe || prepare ;; GetReady
reached ;; Arriving || impact ;; Influence || remained ;; Temporary_stay
killing ;; Killing || wounding ;; Bodily_harm || died ;; Death || fire ;; Use_firearm || opened ;; Openness
restrained ;; Hindering || shooting ;; Use_firearm || prepare ;; GetReady
beaten ;; Bodily_harm || restrained ;; Hindering || recorded ;; Recording || death ;; Death
found ;; Know || attached ;; Connect || shooting ;; Use_firearm || inferred ;; Coming_to_believe
denounced ;; Judgment_communication || taken ;; Action
leave ;; Departing || hiding ;; Hiding_objects || shooting ;; Use_firearm
become ;; Becoming || involved ;; Cause_to_be_included
carried out ;; Action || raid ;; Attack
struck ;; Attack || locate ;; Placing || destroy ;; Destroying
commanded ;; Control || forewarned ;; Warning || sailed ;; Self_motion || attack ;; Attack
attack ;; Attack || offensive ;; Attack
ready ;; GetReady || reinforced ;; Cause_change_of_strength || deal ;; Resolve_problem || command ;; Control || relocated ;; Departing
made it ;; Causation || follow ;; Self_motion || success ;; Earnings_and_losses
Championship ;; Competition || match ;; Competition || Championship ;; Competition || played ;; Competition
appearing ;; Coming_to_be || Championship ;; Competition
game ;; Competition || ending ;; Process_end
declared ;; Expressing_publicly
blew ;; Motion
commenced ;; Process_start || broadcast ;; Expressing_publicly || television ;; Communication || stormed ;; Attack || led ;; Bringing
violence ;; Violence || stand ;; Placing || striking ;; Attack
violence ;; Violence || condemned ;; Judgment_communication || debate ;; Communication || game ;; Competition || led to ;; Causation
end ;; Process_end || compressed ;; Expansion || getting ready ;; GetReady || match ;; Competition || display ;; Action || said ;; Statement
remarked ;; Statement || throw up ;; Causation || said ;; Statement || talking ;; Communication || commemorated ;; Social_event || divided ;; Change
Championships ;; Competition || Championships ;; Competition
started ;; Process_start || ended ;; Process_end
Tournaments ;; Competition || held ;; Giving
get ;; Getting || tournament ;; Competition
fought ;; Competition
defending ;; Defending || champions ;; Competition || tournament ;; Competition || fights ;; Competition || help ;; Assistance || prepare ;; GetReady
competition ;; Competition
deemed ;; Coming_to_believe
gains ;; Getting
taken place ;; Process_start
sanctioned ;; Rewards_and_punishments
given ;; Giving || contributed ;; Assistance
celebrates ;; Social_event
Festival ;; Social_event
festival ;; Social_event
competition ;; Competition || rises ;; Cause_change_of_position_on_a_scale
prize ;; Rewards_and_punishments
festival ;; Social_event || go to ;; Motion
Festival ;; Social_event || festivals ;; Social_event || surpassed ;; Cause_change_of_position_on_a_scale
overthrow ;; Change_of_leadership
become ;; Becoming || death ;; Death
opposed ;; Agree_or_refuse_to_act
displace ;; Change || claimed ;; Statement
discussed ;; Communication || following ;; Self_motion || overthrow ;; Change_of_leadership || assassinate ;; Killing
landed ;; Action || coordinated ;; Collaboration
planned ;; Arranging || marching ;; Self_motion || control ;; Control || recruit ;; Employment || take ;; Conquering
landed ;; Action
became ;; Becoming || growing ;; Expansion || fought ;; Quarreling || commanded ;; Control
compete ;; Competition || capture ;; Conquering || failed ;; Earnings_and_losses
ended ;; Process_end
death ;; Death || condemned ;; Rewards_and_punishments
reigned ;; Control || overthrown ;; Change_of_leadership || consolidate ;; Cause_change_of_strength
match ;; Competition || played ;; Competition || Final ;; Process_end
competition ;; Competition || final ;; Competition
playing ;; Competition || competition ;; Competition || final ;; Process_end || lost ;; Earnings_and_losses
winning ;; Getting || bowl ;; Body_movement || opted ;; Choosing
scored ;; Getting || top-scored ;; Rewards_and_punishments
response ;; Response || lost ;; Earnings_and_losses || partnership ;; Collaboration || game ;; Competition || kept ;; Causation
helped ;; Assistance || bowl ;; Body_movement || earning ;; Earnings_and_losses
occurred ;; Coming_to_be
disputed ;; Quarreling || killed ;; Killing || led to ;; Causation || admitting ;; Coming_to_believe
resist ;; Defending || claimed ;; Statement
ceased ;; Process_end || took place ;; Process_start || maintains ;; Statement
proclaimed ;; Statement || held ;; Hold
killed ;; Killing || executions ;; Rewards_and_punishments || taken ;; Conquering || died ;; Death || took place ;; Process_start
killed ;; Killing
name ;; Name_conferral || Battle ;; Hostile_encounter || occurred ;; Presence
proclaimed ;; Statement || uprising ;; Change_of_leadership
occupied ;; Control
met ;; Come_together || command ;; Control
battle ;; Hostile_encounter || help ;; Assistance || defeated ;; Conquering
sent ;; Sending || captured ;; Conquering
negotiated ;; Communication || rewarded ;; Rewards_and_punishments || deal ;; Arranging
battle ;; Hostile_encounter || leaving ;; Departing || rode ;; Self_motion || crushed ;; Attack
repulsed ;; Defending || result ;; Causation || entered ;; Arriving
Battle ;; Hostile_encounter || commanded ;; Control || Battle ;; Hostile_encounter || battle ;; Hostile_encounter || War ;; Hostile_encounter
started ;; Process_start || ended ;; Process_end || battle ;; Hostile_encounter || routed ;; Destroying
Battle ;; Hostile_encounter || defeat ;; Conquering || fallen ;; Arriving
reinforced ;; Cause_change_of_strength || left ;; Departing || defend ;; Defending || war ;; Hostile_encounter
killed ;; Killing || wounded ;; Bodily_harm || deserted ;; Removing
marched ;; Self_motion || arrived in ;; Arriving
marched ;; Self_motion || engagement ;; Hostile_encounter
moved ;; Motion || receiving ;; Receiving || retreat ;; Escaping
moving ;; Motion
decided ;; Deciding || drove ;; Motion || attacking ;; Attack || counterattack ;; Attack
moving ;; Motion || command ;; Control || surround ;; Surrounding
casualties ;; Catastrophe || pulled ;; Motion || formed ;; Coming_to_be || defensive ;; Defending || attack ;; Attack || beginning ;; Process_start || faltering ;; Motion
approached ;; Arriving || retreated ;; Escaping
took up a position ;; Temporary_stay || fell ;; Motion || regrouped ;; Cause_to_amalgamate
supported ;; Supporting || assaulted ;; Attack || routed ;; Destroying
started ;; Process_start || fled ;; Escaping || taking over ;; Getting || battle ;; Hostile_encounter
chose ;; Choosing || treaty ;; Sign_agreement || sued ;; Rewards_and_punishments || resulting in ;; Causation
fought ;; Hostile_encounter
allowed ;; Preventing_or_letting || recapture ;; Conquering || lost ;; Earnings_and_losses || failing ;; Rewards_and_punishments
taken ;; Conquering || expanding ;; Expansion || integrated ;; Cause_to_amalgamate
received ;; Receiving || recapturing ;; Conquering || making up ;; Becoming_a_member || inflicted ;; Bodily_harm || defeat ;; Earnings_and_losses || hold ;; Defending
reverse ;; Change || remained ;; Temporary_stay
took ;; Conquering || recaptured ;; Conquering
ended ;; Process_end || conquest ;; Conquering || stem ;; Preventing_or_letting
took ;; Conquering || cease ;; Process_end || exist ;; Presence
established ;; Creating || took ;; Conquering
conquered ;; Conquering || terminated ;; Process_end || engaged ;; Hostile_encounter
massacre ;; Killing || crime ;; Committing_crime || occurred ;; Presence
death ;; Death
estimate ;; Statement
blamed ;; Judgment_communication || massacre ;; Killing
concealed ;; Hiding_objects || massacre ;; Killing
revealing ;; Reveal_secret || forbidden ;; Preventing_or_letting
death ;; Death || carried ;; Carry_goods || torture ;; Rewards_and_punishments
massacre ;; Killing || resulting in ;; Causation
violent ;; Violence || unrest ;; Violence || riots ;; Violence || Protests ;; Communication || initiated ;; Creating
erupted ;; Process_start || unrest ;; Violence
killed ;; Killing || stated ;; Statement || said ;; Statement || casualties ;; Death
blamed ;; Judgment_communication || unrest ;; Violence
killing ;; Killing || unrest ;; Violence || Following ;; Motion_directional || bombings ;; Terrorism || carried out ;; Action
executed ;; Rewards_and_punishments || bombings ;; Terrorism || carrying out ;; Action || convicted ;; Legal_rulings
held ;; Hold || Championship ;; Competition
played ;; Competition || taken place ;; Process_start
selected ;; Choosing || defending ;; Defending || represent ;; Change_sentiment
defeated ;; Conquering
ended ;; Process_end || Match ;; Competition
penalty ;; Rewards_and_punishments || miss ;; Earnings_and_losses || Championship ;; Competition || crowned ;; Getting
carried out ;; Military_operation || offensive ;; Military_operation
conducted ;; Action || killings ;; Killing
arranged ;; Arranging || operation ;; Military_operation
included ;; Cause_to_be_included || carried out ;; Military_operation
retreated ;; Departing || withdrew ;; Departing || fighting ;; Hostile_encounter
massacred ;; Killing || destroyed ;; Destroying || command ;; Control
protests ;; Response || relocated ;; Removing
capturing ;; Conquering || focused ;; Aiming || surrounding ;; Surrounding
escaped ;; Escaping || avoided ;; Preventing_or_letting || killed ;; Killing || war ;; Hostile_encounter || prosecution ;; Legality
captured ;; Conquering || found ;; Know || war ;; Hostile_encounter || death ;; Death || charges ;; Judgment_communication || sentenced ;; Rewards_and_punishments || executed ;; Killing
arose ;; Coming_to_be
received ;; Receiving || requested ;; Request
provoked ;; Causation || covered ;; Filling
resigned ;; Process_end
requested ;; Request
investigated ;; Criminal_investigation
exonerated ;; Rewards_and_punishments
issued ;; Statement
dismissed ;; Response || lawsuit ;; Legality
lasted ;; Process_end || Siege ;; Besieging
crossed ;; Motion_directional || placed ;; Placing || siege ;; Besieging || receiving ;; Receiving || sacked ;; Process_end
siege ;; Besieging
demolished ;; Destroying || forces ;; Hostile_encounter || held ;; Defending || Assisted ;; Assistance
killed ;; Killing || sold ;; Commerce_sell
included ;; Cause_to_be_included
destruction ;; Destroying || state ;; Statement || Following ;; Self_motion || executed ;; Rewards_and_punishments
taken ;; Creating || returned ;; Arriving
game ;; Competition
game ;; Competition || played ;; Participation
score ;; Getting
game ;; Competition
game ;; Competition || game ;; Competition || last ;; Process_end
referee ;; Rewards_and_punishments
collided ;; Motion
headed ;; Self_motion || take place ;; Process_start || occurred ;; Coming_to_be
killed ;; Killing || injured ;; Bodily_harm || maritime ;; Catastrophe
acquitted ;; Judgment_communication
endangering ;; Bodily_harm
sentenced ;; Rewards_and_punishments
expel ;; Removing || retreat ;; Escaping || assistance ;; Assistance || prevent ;; Preventing_or_letting || Operation ;; Military_operation || operations ;; Military_operation || push ;; Influence || War ;; Military_operation || launched ;; Process_start || commanded ;; Control
withdrawing ;; Escaping || escape ;; Escaping || closed ;; Process_end
involved ;; Participation || undertaking ;; Military_operation || supported ;; Supporting
assisted ;; Assistance || retreated ;; Escaping
comprised ;; Cause_to_be_included || operation ;; Military_operation || order ;; Request || battle ;; Hostile_encounter
supported ;; Supporting
retreat ;; Escaping || withdraw ;; Escaping || helping ;; Assistance || served ;; Assistance
utilised ;; Using || cover ;; Preventing_or_letting || engaging ;; Action || advancing ;; Cause_to_make_progress
increase ;; Cause_change_of_position_on_a_scale || swing ;; Self_motion || forces ;; Hostile_encounter || orders ;; Request || offensive ;; Attack || drove ;; Bringing
shot ;; Use_firearm || keeping ;; Preventing_or_letting || orders ;; Request || hanged ;; Rewards_and_punishments
capture ;; Conquering || break out ;; Process_start || set up ;; Reforming_a_system || forces ;; Hostile_encounter || combined ;; Cause_to_amalgamate || forces ;; Hostile_encounter || operation ;; Military_operation || withdraw ;; Escaping || resulted in ;; Causation
reached ;; Arriving || defensive ;; Defending || retreating ;; Escaping || end ;; Process_end || pursued ;; Aiming
identified ;; Check || murdered ;; Committing_crime || wounded ;; Bodily_harm || occurred ;; Coming_to_be
burned ;; Bodily_harm
told ;; Telling || taken ;; Conquering
sentenced ;; Rewards_and_punishments
apologized ;; Judgment_communication
condemned ;; Judgment_communication || describing ;; Statement
said ;; Communication || demanding ;; Request || passed ;; Motion_directional
pleaded ;; Request
committed ;; Commitment || stated ;; Statement || know ;; Know
concluded ;; Coming_to_believe
speculated ;; Coming_to_believe || involved ;; Cause_to_be_included
confirm ;; Check || said ;; Statement
published ;; Publishing || revealed ;; Reveal_secret || believed ;; Know
organized ;; Arranging || sponsorship ;; Supply || tournament ;; Competition
Tournament ;; Competition || played ;; Participation || final ;; Competition
competed ;; Competition || host ;; Social_event
prize ;; Rewards_and_punishments
received ;; Receiving || got ;; Getting || prize ;; Rewards_and_punishments || took away ;; Getting
received ;; Receiving || received ;; Receiving
games ;; Competition || broadcast ;; Expressing_publicly
fought ;; Hostile_encounter || offensive ;; Attack
began ;; Process_start || offensive ;; Attack
amassed ;; Come_together || begin ;; Process_start || offensive ;; Attack || drive ;; Motion
marched ;; Self_motion
provided ;; Supply || support ;; Supporting || consisted ;; Cause_to_be_included
reached ;; Arriving || recapture ;; Conquering
ended ;; Process_end || liberation ;; Change_of_leadership
caused ;; Causation || defeat ;; Conquering
retreat ;; Escaping || destroyed ;; Destroying || lost ;; Earnings_and_losses || organize ;; Arranging || encircled ;; Besieging
explosion ;; Attack || occurred ;; Coming_to_be
detonated ;; Destroying || loaded ;; Filling
refuse ;; Agree_or_refuse_to_act || inspired ;; Causation
sentenced ;; Legal_rulings || convicted ;; Legal_rulings
released ;; Releasing || served ;; Assistance
raised ;; Bringing
reconvened ;; Come_together || affirmed ;; Commitment
change ;; Change || led ;; Causation || turned ;; Becoming
dedicated ;; Giving
agreed ;; Agree_or_refuse_to_act || sponsored ;; Supply
recognized ;; Check || exonerated ;; Legality
took place ;; Process_start || invasion ;; Attack
raid ;; Attack || providing ;; Supply || maintained ;; Preserving || disrupt ;; Attack
decided ;; Deciding || order ;; Arranging || focus ;; Aiming || beginning ;; Process_start || neutralise ;; Removing || provided ;; Supply
collected ;; Come_together || forces ;; Hostile_encounter || garrisoning ;; Hostile_encounter
defeating ;; Conquering || Landing ;; Arriving || pushed ;; Motion || routing ;; Attack
protect ;; Defending || bypassed ;; Self_motion
bombarded ;; Attack || surrendering ;; Surrendering || casualties ;; Catastrophe || suffering ;; Bodily_harm
capture ;; Conquering || shipping ;; Self_motion || eliminating ;; Destroying
sent ;; Sending || losing ;; Earnings_and_losses || battle ;; Hostile_encounter || invasion ;; Attack || reinforce ;; Cause_change_of_strength
affect ;; Influence || scattered ;; Dispersal || Arriving ;; Arriving || return ;; Arriving
turned ;; Becoming || captured ;; Conquering || defeated ;; Conquering
form ;; Coming_to_be || organised ;; Arranging || event ;; Social_event
organised ;; Arranging
form ;; Coming_to_be || gathered ;; Come_together
break ;; Destroying || record ;; Recording
made ;; Creating || gathering ;; Come_together
presented ;; Presence || validating ;; Check
took place ;; Process_start
host ;; Social_event || host ;; Social_event
participants ;; Participation
composed ;; Come_together
participate ;; Participation
crash ;; Catastrophe || crash ;; Catastrophe
express ;; Expressing_publicly || service ;; Supply || traveling ;; Traveling || entered ;; Arriving || throwing ;; Self_motion || derailed ;; Self_motion
killed ;; Killing || died ;; Death || help ;; Assistance || flocked ;; Come_together || injured ;; Bodily_harm
services ;; Supply || respond ;; Response || fire ;; Use_firearm || claimed ;; Statement
dead ;; Death || injured ;; Bodily_harm
allow ;; Preventing_or_letting || service ;; Supply || cleared ;; Removing
discovered ;; Know || caused ;; Causation || severed ;; Destroying
claimed ;; Statement || terrorist ;; Terrorism || known ;; Know || target ;; Aiming || disaster ;; Catastrophe
cover ;; Hiding_objects || lead to ;; Causation || suggested ;; Convincing
announcement ;; Expressing_publicly || criticizing ;; Judgment_communication || cause ;; Causation
used ;; Using || investigations ;; Scrutiny || discovered ;; Know || used ;; Using || broken ;; Destroying
took ;; Conquering || see ;; Perception_active || work ;; Employment
agreed ;; Agree_or_refuse_to_act || reports ;; Reporting || crash ;; Catastrophe || caused ;; Causation
terrorist ;; Terrorism || took ;; Conquering || crash ;; Catastrophe || causing ;; Causation || established ;; Building
arrested ;; Arrest || attack ;; Attack || named ;; Name_conferral || fingerprinting ;; Check || questioned ;; Communication
involved ;; Cause_to_be_included || bombing ;; Attack || killed ;; Killing || charged ;; Committing_crime
fought ;; Hostile_encounter || supported ;; Supporting || wars ;; Hostile_encounter
dominated ;; Control || wars ;; Hostile_encounter || campaigns ;; Military_operation
become ;; Becoming
served ;; Assistance
incorporated ;; Cause_to_amalgamate || contend ;; Quarreling || defeated ;; Getting
made ;; Getting
assemble ;; Come_together || allowing ;; Preventing_or_letting || combat ;; Hostile_encounter || regain ;; Getting
ended ;; Process_end
came to ;; Motion || fought ;; Hostile_encounter || wars ;; Hostile_encounter
found ;; Know || fought ;; Hostile_encounter
saved ;; Rescuing || began ;; Process_start || devoted ;; Giving || depicting ;; Filling
resisted ;; Defending || battling ;; Hostile_encounter
cooperated ;; Collaboration || supported ;; Supporting
maintain ;; Preserving
built ;; Building || produced ;; Creating || protected ;; Defending
Championship ;; Competition || Championship ;; Competition || held ;; Hold
Championship ;; Competition
championship ;; Competition
Play ;; Competition || started ;; Process_start
followed ;; Motion
started ;; Process_start
scored ;; Hold
scores ;; Hold
played ;; Competition || scored ;; Hold
ascertain ;; Coming_to_believe || gathered ;; Come_together || attended ;; Participation
founded ;; Creating || festival ;; Social_event
takes place ;; Process_start
open ;; Openness
designed ;; Creating
assembled ;; Come_together || collection ;; Come_together
took place ;; Process_start || begun ;; Process_start
studied ;; Education_teaching || played ;; Competition
worked ;; Being_in_operation || record ;; Recording || changed ;; Change
relaunched ;; Recovering
offering ;; Supply
conference ;; Communication || conference ;; Social_event || convened ;; Come_together || concluded ;; Deciding
renaming ;; Name_conferral || meetings ;; Communication
decided ;; Deciding || cease ;; Process_end || conferred upon ;; Giving
conferences ;; Communication || statements ;; Statement || granted ;; Giving
given ;; Giving || unite ;; Cause_to_amalgamate || form ;; Coming_to_be
discussed ;; Communication
support ;; Supporting || raised ;; Cause_change_of_position_on_a_scale || rejected ;; Response
captured ;; Conquering || flowed ;; Motion || fortified ;; Cause_change_of_strength || led to ;; Causation
moved ;; Motion || converged ;; Come_together || joined ;; Becoming_a_member
failed ;; Earnings_and_losses
captured ;; Conquering || surrendered ;; Surrendering || defended ;; Defending
moved ;; Motion || besiege ;; Besieging || taking ;; Conquering
devastated ;; Destroying || caused ;; Causation || hurricane ;; Catastrophe || cyclone ;; Motion || accruing ;; Come_together
destruction ;; Destroying || hurricane ;; Catastrophe
hurricane ;; Catastrophe || cause ;; Causation
cyclone ;; Motion || formed ;; Coming_to_be
reached ;; Arriving || strengthening ;; Cause_change_of_strength || storm ;; Catastrophe || tracked ;; Self_motion
encountered ;; Hostile_encounter || reached ;; Arriving
storm ;; Catastrophe || reached ;; Arriving
storm ;; Catastrophe || tracked ;; Self_motion
caused ;; Causation || destruction ;; Destroying
storm ;; Catastrophe || destroyed ;; Destroying || leaving ;; Departing
deaths ;; Death || occurred ;; Coming_to_be
damage ;; Damaging || cyclone ;; Catastrophe || generated ;; Creating
storm ;; Catastrophe || deaths ;; Death || occurred ;; Coming_to_be || drowned ;; Death
hurricane ;; Catastrophe || traversed ;; Motion_directional
storm ;; Catastrophe
storm ;; Catastrophe || made ;; Manufacturing
caused ;; Causation || storm ;; Catastrophe || damage ;; Damaging
caused ;; Causation || interaction ;; Action || dissipate ;; Dispersal
damages ;; Damaging || effects ;; Influence || included ;; Cause_to_be_included || deaths ;; Death || Hurricane ;; Catastrophe
reached ;; Arriving || storm ;; Catastrophe
existing ;; Presence || reached ;; Arriving || merging ;; Cause_to_amalgamate || storm ;; Catastrophe || storm ;; Catastrophe
lost ;; Earnings_and_losses || stalled ;; Self_motion
flooding ;; Catastrophe || raced ;; Motion
received ;; Receiving || hold ;; Defending
killed ;; Killing || flooded ;; Catastrophe
killed ;; Killing || collected ;; Come_together || losses ;; Earnings_and_losses || flooded ;; Catastrophe
killed ;; Killing
took ;; Conquering || hit ;; Destroying || washed ;; Removing
exacerbated ;; Cause_change_of_position_on_a_scale
arrival ;; Arriving || storm ;; Catastrophe || proved ;; Coming_to_believe || came ;; Arriving
storm ;; Catastrophe
became ;; Becoming || prohibited ;; Preventing_or_letting || development ;; Cause_to_make_progress
summoned ;; Request || help ;; Assistance || cleanup ;; Removing
destruction ;; Destroying || used ;; Using || hurricane ;; Catastrophe || name ;; Name_conferral || retired ;; GiveUp
creation ;; Creating || retired ;; GiveUp || replaced ;; Change
crashed ;; Catastrophe || flying ;; Self_motion
gain ;; Getting || lifted off ;; Motion
coming to ;; Motion || hit ;; Attack || veered ;; Motion
killed ;; Killing
crashed ;; Catastrophe || accumulated ;; Come_together || happened ;; Presence
killed ;; Killing
accumulated ;; Come_together || revealed ;; Reveal_secret
disrupted ;; Hindering || prevented ;; Preventing_or_letting || increasing ;; Cause_change_of_position_on_a_scale || lifting off ;; Motion_directional
built ;; Building || delayed ;; Change_event_time || concluded ;; Coming_to_believe
begun ;; Process_start
found ;; Know
found ;; Know || used ;; Using
led to ;; Causation
occurred ;; Presence || protests ;; Response
protests ;; Response || followed ;; Motion
arising ;; Coming_to_be || provide ;; Supply || responses ;; Response || contained ;; Containing
argued ;; Quarreling || protect ;; Preventing_or_letting
initiated ;; Process_start || protest ;; Response || closing ;; Body_movement || opposing ;; Agree_or_refuse_to_act
protests ;; Response
kept ;; Temporary_stay || shut down ;; Having_or_lacking_access
protest ;; Response || joined ;; Participation
protests ;; Response || reported ;; Reporting
planned ;; Arranging || protest ;; Response || drew ;; Getting
issued ;; Statement || statement ;; Statement || support ;; Supporting || reduces ;; Cause_change_of_position_on_a_scale || increases ;; Cause_change_of_position_on_a_scale
sent ;; Sending || contacted ;; Communication || recorded ;; Recording || recorded ;; Recording || express ;; Statement || collected ;; Come_together || protest ;; Response || looked up ;; Scouring || emailed ;; Communication
supported ;; Supporting || withdrew ;; Removing || support ;; Supporting || protest ;; Response || expressed ;; Expressing_publicly
voiced ;; Expressing_publicly
described ;; Statement || used ;; Using
stated ;; Statement || given ;; Giving
protests ;; Response
shifted ;; Change
removed ;; Removing || raised ;; Cause_change_of_position_on_a_scale
noted ;; Know || postponed ;; Change_event_time || cautioned ;; Statement
happened ;; Presence
became ;; Becoming
ruled ;; Control
given ;; Giving
crowned ;; Change_of_leadership || triggered ;; Causation
felt ;; Change_sentiment
began ;; Process_start
gathered ;; Come_together
launched ;; Process_start
sent ;; Sending || responded ;; Response
sent ;; Sending
contained ;; Containing
conquered ;; Conquering || gained ;; Getting
blizzard ;; Catastrophe || impact ;; Influence || referred ;; Name_conferral
tracked ;; Motion || heading ;; Motion
brought ;; Bringing
accumulated ;; Come_together || halt ;; Process_end || bringing ;; Bringing
service ;; Supply || suspended ;; Change_event_time
reported ;; Reporting || occurred ;; Presence
recognized ;; Know || brought ;; Bringing
combined ;; Cause_to_amalgamate || bring ;; Bringing
occurred ;; Presence || focused ;; Aiming || dropped ;; Motion_directional
bombing ;; Terrorism || blast ;; Attack || occurred ;; Coming_to_be
commenced ;; Process_start || gathered ;; Come_together || explosion ;; Attack || ritual ;; Social_event || occurred ;; Coming_to_be
killed ;; Killing || injured ;; Bodily_harm || injured ;; Bodily_harm || sitting ;; Placing
injuries ;; Bodily_harm || blast ;; Attack || stampede ;; Escaping || increase ;; Expansion || broke ;; Destroying
hidden ;; Hiding_objects
riots ;; Violence || killing ;; Killing || occurred ;; Presence || blast ;; Attack || anniversary ;; Social_event || demolition ;; Destroying || demolished ;; Destroying
claimed ;; Statement || responsibility ;; Causation || blast ;; Attack
killed ;; Killing || incident ;; Attack || blasts ;; Attack || explosion ;; Attack
battle ;; Hostile_encounter || command ;; Control || Battle ;; Hostile_encounter || Wars ;; Hostile_encounter || defeated ;; Conquering
supported ;; Supporting || Wars ;; Hostile_encounter || Feud ;; Quarreling || supported ;; Supporting || spilled ;; Filling
invaded ;; Attack || supported ;; Supporting
invade ;; Attack || lift ;; Process_end || gather ;; Come_together || support ;; Supporting || siege ;; Besieging || threatened ;; Warning
command ;; Control || ordered ;; Request || compose ;; Collaboration
sent ;; Sending || eliminate ;; Removing || occupy ;; Hold || allowed ;; Preventing_or_letting
battle ;; Hostile_encounter || defeated ;; Conquering
campaign ;; Military_operation
engaged ;; Participation || conducted ;; Action || ordered ;; Request || gathered ;; Come_together
consisted ;; Cause_to_be_included
killed ;; Killing || lost ;; Earnings_and_losses
became ;; Becoming
destroyed ;; Destroying
destroy ;; Destroying || prosecute ;; Legality
swell ;; Expansion
dropped ;; Motion || dropped in ;; Motion
initiated ;; Process_start
conducted ;; Action || attacked ;; Attack || mounted ;; Self_motion
joined ;; Becoming_a_member
reached ;; Arriving
captured ;; Conquering
created ;; Creating
became ;; Becoming
abandoned ;; GiveUp || entered ;; Arriving
quelled ;; Preventing_or_letting || gathered ;; Come_together
became ;; Becoming || engaged ;; Participation
establish ;; Building
flood ;; Catastrophe || flood ;; Catastrophe || take place ;; Process_start || record ;; Recording
caused ;; Causation || floods ;; Catastrophe || amounting ;; Cause_change_of_position_on_a_scale || damage ;; Damaging || Triggered ;; Causation || rainfall ;; Motion
began ;; Process_start || response ;; Response || exceeding ;; Cause_change_of_position_on_a_scale || rise ;; Cause_change_of_position_on_a_scale
reached ;; Arriving || levels ;; Cause_change_of_position_on_a_scale || cresting ;; Action || record ;; Recording
orders ;; Arranging || affected ;; Influence
impact ;; Influence || washed ;; Removing || felt ;; Perception_active
disaster ;; Catastrophe || restoring ;; Recovering || Following ;; Self_motion || supplies ;; Assistance || funds ;; Assistance || aid ;; Assistance || distributed ;; Giving || affected ;; Influence
died ;; Death || perished ;; Death || damage ;; Damaging || cattle ;; Come_together
Battle ;; Hostile_encounter || attack ;; Attack || commanded ;; Control || covering ;; Hiding_objects
encounter ;; Hostile_encounter || defeated ;; Conquering || wounded ;; Bodily_harm
occurred ;; Coming_to_be || War ;; Military_operation || conflict ;; Hostile_encounter || Wars ;; Hostile_encounter
battles ;; Hostile_encounter || allied ;; Collaboration
collected ;; Come_together || marched ;; Motion_directional || forces ;; Causation
mauled ;; Bodily_harm
fight ;; Hostile_encounter || forces ;; Causation || swung ;; Self_motion
Battle ;; Hostile_encounter
concluded ;; Process_end || Conference ;; Communication || convened ;; Come_together
Coronation ;; Change_of_leadership
discussed ;; Communication || proposals ;; Convincing || conference ;; Communication || advise ;; Convincing || made up ;; Come_together
declaration ;; Expressing_publicly || presided ;; Control || developed ;; Coming_to_be
agreed ;; Agree_or_refuse_to_act || rejected ;; Agree_or_refuse_to_act || proposals ;; Convincing || consult ;; Communication || making ;; Creating
agree ;; Agree_or_refuse_to_act || final ;; Process_end
negotiation ;; Communication || proposals ;; Statement || consult ;; Communication || circulated ;; Dispersal || signing ;; Sign_agreement || affect ;; Influence || preparing ;; GetReady
agreed ;; Agree_or_refuse_to_act || consult ;; Communication || negotiating ;; Communication || expressed ;; Expressing_publicly
agreed ;; Agree_or_refuse_to_act || considering ;; Influence || negotiations ;; Communication
abandon ;; GiveUp || persuade ;; Convincing || approach ;; Arriving || governing ;; Action || Demonstration ;; Protest || made ;; Action
revolution ;; Change_of_leadership || occurred ;; Presence || demonstration ;; Protest
convince ;; Convincing || enforce ;; Action || defend ;; Defending || invasion ;; Attack || preserve ;; Preserving || assembly ;; Building || objectives ;; Aiming || rulings ;; Legal_rulings
demonstrators ;; Protest || veto ;; Agree_or_refuse_to_act
establish ;; Creating || constitutional ;; Legality || Demonstration ;; Protest || last ;; Process_end
Insurrection ;; Change_of_leadership
disaster ;; Catastrophe || stalled ;; Temporary_stay || negotiate ;; Motion || complete ;; Process_end
decided ;; Deciding || take ;; Bringing || divide ;; Removing || leaving ;; Departing
ran ;; Self_motion || braked ;; Hold || colliding ;; Motion
killed ;; Killing || injured ;; Bodily_harm
disaster ;; Catastrophe || disaster ;; Catastrophe || remains ;; Presence
accident ;; Catastrophe
becoming ;; Becoming || legal ;; Legality || disaster ;; Catastrophe || led ;; Causation
introduced ;; Statement || intervention ;; Participation
killed ;; Killing
majoring ;; Participation || planning ;; Arranging || enter ;; Becoming_a_member
accused ;; Judgment_communication || killing ;; Killing || arrested ;; Arrest || attack ;; Attack || turned ;; Motion
motivated ;; Causation || stated ;; Statement || investigation ;; Criminal_investigation || dispute ;; Quarreling
cooperating ;; Collaboration
charged ;; Legality || murder ;; Committing_crime
murder ;; Committing_crime || discharging ;; Removing || indicted ;; Criminal_investigation
shooting ;; Killing || launched ;; Process_start || investigations ;; Criminal_investigation
trial ;; Legal_rulings || scheduled ;; Process_start || take place ;; Process_start
shooting ;; Killing || sentenced ;; Legal_rulings || pleaded ;; Committing_crime
occurred ;; Coming_to_be || warfare ;; Hostile_encounter || implemented ;; Action || sinking ;; Motion_directional
identified ;; Check || torpedoed ;; Destroying
killing ;; Killing || went ;; Self_motion || leaving ;; Departing
campaigns ;; Military_operation || turned ;; Change || became ;; Becoming || recruiting ;; Employment || entry ;; Becoming_a_member || contributed ;; Causation || fought ;; Hostile_encounter
fell ;; Motion_directional || evading ;; Escaping || implemented ;; Action
causes ;; Causation || obstructed ;; Hindering || investigations ;; Criminal_investigation
legitimate ;; Legality || target ;; Aiming || claims ;; Statement
carrying ;; Carry_goods || sunk ;; Motion
made ;; Manufacturing || sinking ;; Motion_directional
killed ;; Killing || collaborators ;; Collaboration || massacre ;; Killing || Murder ;; Killing || Tumult ;; Killing || pogrom ;; Killing
violence ;; Violence || skirmishes ;; Hostile_encounter
believed ;; Know || survived ;; Coming_to_be || estimated ;; Statement || massacred ;; Killing
declared ;; Expressing_publicly || spurred ;; Causation || unrest ;; Violence || rose ;; Motion_directional || repression ;; Control || declining ;; Cause_change_of_position_on_a_scale
killed ;; Killing || confiscate ;; Removing || leading ;; Causation || place ;; Placing
atrocities ;; Violence || led ;; Causation || fire ;; Use_firearm
violence ;; Violence || spread ;; Dispersal || killing ;; Killing
declared ;; Expressing_publicly || kill ;; Killing || called ;; Request || cessation ;; Process_end || amnesty ;; Legality
clashes ;; Hostile_encounter
skirmishes ;; Hostile_encounter || assaulted ;; Attack
attacks ;; Attack || forces ;; Military_operation || sparked ;; Causation || pitted ;; Hostile_encounter
massacre ;; Killing || charged ;; Judgment_communication
massacre ;; Killing || cited ;; Adducing
party ;; Participation || die ;; Death || failed ;; Earnings_and_losses || took place ;; Process_start || putsch ;; Change_of_leadership || putsch ;; Change_of_leadership || seize ;; Getting || also known as ;; Know
confronted ;; Confronting_problem || marching ;; Self_motion || resulted in ;; Causation || deaths ;; Death
escaped ;; Escaping || wounded ;; Bodily_harm || arrest ;; Arrest || spirited ;; Bringing || clash ;; Violence
charged ;; Judgment_communication || arrested ;; Arrest || treason ;; Committing_crime
putsch ;; Change_of_leadership || brought ;; Causation || generated ;; Creating
gave ;; Giving || arrest ;; Arrest || trial ;; Criminal_investigation || express ;; Expressing_publicly || publicised ;; Dispersal
guilty ;; Committing_crime || treason ;; Committing_crime || found ;; Know || sentenced ;; Legal_rulings
released ;; Releasing || served ;; Assistance
released ;; Releasing || legal ;; Legality || redirected ;; Change || obtaining ;; Getting || changed ;; Change || developing ;; Cause_to_make_progress || revolution ;; Change_of_leadership
commencing ;; Process_start || movement ;; Social_event || intended ;; Deciding || perceived ;; Know
named ;; Name_conferral || movement ;; Social_event
began ;; Process_start || movement ;; Social_event || gained ;; Getting
movement ;; Social_event || alleviate ;; Cause_to_make_progress
repatriation ;; Surrendering
focussed ;; Aiming || legal ;; Legality
acts ;; Action || movement ;; Social_event || civil ;; Hostile_encounter || civil ;; Hostile_encounter || marches ;; Social_event
strengthen ;; Cause_change_of_strength || protests ;; Self_motion || made ;; Action || use ;; Using || hostile ;; Defending
murdered ;; Killing || murders ;; Killing || occurred ;; Coming_to_be || ambush ;; Attack
murder ;; Killing || sparked ;; Causation || accused ;; Judgment_communication || incident ;; Killing || controversies ;; Quarreling || end ;; Process_end || planning ;; Arranging || covering ;; Hiding_objects || event ;; Social_event || found ;; Coming_to_believe
declared ;; Expressing_publicly || terrorists ;; Terrorism || witness ;; Reveal_secret || questioned ;; Communication
ordered ;; Request || launch ;; Process_start || investigations ;; Criminal_investigation || investigations ;; Criminal_investigation || Investigation ;; Criminal_investigation || asked ;; Communication || aid ;; Assistance || concluded ;; Process_end
murder ;; Killing || inquiries ;; Criminal_investigation || surfaced ;; Know || launched ;; Process_start
Trials ;; Legality || convicted ;; Legal_rulings || held ;; Creating
become ;; Becoming || called ;; Name_conferral
used ;; Using || event ;; Social_event
commented ;; Statement || banned ;; Preventing_or_letting || massacre ;; Killing || demonstrated ;; Protest
summit ;; Social_event || held ;; Defending
present ;; Presence
declared ;; Expressing_publicly || legitimate ;; Legality || passed ;; Motion_directional
resolved ;; Resolve_problem || provide ;; Supply
summit ;; Social_event || conflict ;; Hostile_encounter
relinquish ;; GiveUp || acknowledge ;; Statement
weakened ;; Cause_change_of_strength
agreed ;; Agree_or_refuse_to_act || negotiate ;; Communication
uncovered ;; Reveal_secret || arrival ;; Arriving
awards ;; Award
given out ;; Giving || awards ;; Award
voting ;; Choosing || extended ;; Expansion
award ;; Award
winning ;; Getting || award ;; Award
ended ;; Process_end || award ;; Award
announced ;; Expressing_publicly || recipient ;; Participation || claimed ;; Statement
hosted ;; Social_event
captured ;; Conquering || besieged ;; Besieging || command ;; Control
crossing ;; Motion_directional
battle ;; Hostile_encounter
cross ;; Motion_directional
submerged ;; Motion || created ;; Creating || exposed ;; Reveal_secret
lay ;; Placing
constructed ;; Building
crossed ;; Motion_directional || contested ;; Competition
cross ;; Motion_directional || success ;; Achieve
gave ;; Giving
chased ;; Motion
command ;; Control || pushed ;; Self_motion
withdrew ;; Escaping || conclusion ;; Statement
offered ;; Giving
send ;; Sending || allow ;; Preventing_or_letting
refused ;; Agree_or_refuse_to_act || forcing ;; Hostile_encounter
tied ;; Connect
retreat ;; Escaping || control ;; Control || joining ;; Becoming_a_member || forced ;; Motion || acquired ;; Getting
remained ;; Presence
moved ;; Motion || retreated ;; Escaping || begin ;; Process_start || cross ;; Motion_directional
captured ;; Conquering || ended ;; Process_end
connecting ;; Connect || expanded ;; Expansion
bombarded ;; Attack || called ;; Statement || connected ;; Connect || completed ;; Process_end || defenses ;; Defending || captured ;; Arrest
withdrew ;; Escaping || defenses ;; Defending || capitulated ;; Surrendering
shot down ;; Use_firearm || flying ;; Self_motion
killed ;; Killing
lost ;; Earnings_and_losses || Contact ;; Communication || fell ;; Motion_directional
occurred ;; Coming_to_be || controlled ;; Control || War ;; Hostile_encounter || Battle ;; Hostile_encounter
loss ;; Earnings_and_losses || crash ;; Catastrophe
investigation ;; Criminal_investigation || joint ;; Collaboration || investigation ;; Criminal_investigation || concluded ;; Coming_to_believe || downed ;; Motion
fired ;; Use_firearm || used ;; Using || used ;; Using || shoot down ;; Use_firearm || originated ;; Coming_to_be || transported ;; Sending || returned ;; Arriving
hold ;; Defending || deployment ;; Arranging || taking ;; Action || hold ;; Defending
confirmed ;; Check || claims ;; Statement
supplied ;; Supply || shot down ;; Use_firearm || said ;; Telling
laid ;; Placing || blame ;; Judgment_communication
rejects ;; Response || crash ;; Catastrophe || claims ;; Statement
defense ;; Defending || said ;; Communication || deployed ;; Arranging
appeared ;; Coming_to_be || crash ;; Catastrophe
crash ;; Catastrophe || conflict ;; Hostile_encounter || asserting ;; Statement
shot down ;; Use_firearm
appeared ;; Coming_to_be || claiming ;; Statement || shooting down ;; Use_firearm || crash ;; Catastrophe || attributed ;; Causation
removed ;; Removing || shooting down ;; Use_firearm || denied ;; Agree_or_refuse_to_act
reported ;; Reporting || shot down ;; Use_firearm || claiming ;; Statement
discussing ;; Communication || claimed ;; Statement
recorded ;; Recording || obtained ;; Getting || arrive at ;; Arriving || crash ;; Catastrophe || crash ;; Catastrophe || rebels ;; Hostile_encounter || rebel ;; Hostile_encounter
started ;; Process_start || realise ;; Know
contesting ;; Competition || taking place ;; Process_start
competed ;; Competition
defeating ;; Conquering || hit ;; Motion
awarded ;; Giving || given ;; Giving || losing ;; Competition
finished ;; Process_end || played ;; Competition
claimed ;; Statement || ranking ;; Placing
claimed ;; Statement || taking ;; Getting
topped ;; Placing || lost ;; Earnings_and_losses || boosted ;; Cause_change_of_position_on_a_scale || returning ;; Arriving
floods ;; Catastrophe || mudslides ;; Catastrophe || took place ;; Process_start
occurred ;; Presence
caused ;; Causation || floods ;; Catastrophe || deaths ;; Death
became ;; Becoming || claims ;; Statement || contend ;; Statement || took place ;; Process_start || lost ;; Death || lives ;; Death
casualties ;; Catastrophe || reported ;; Reporting
exposed ;; Reveal_secret || hazards ;; Catastrophe
occurred ;; Presence
invaded ;; Attack || claimed ;; Statement
become ;; Becoming || end ;; Process_end || forced ;; Motion || adopt ;; Agree_or_refuse_to_act
agreement ;; Agree_or_refuse_to_act || withdrew ;; Escaping
gave ;; Giving || incorporate ;; Cause_to_amalgamate || occupied ;; Control
strengthened ;; Cause_change_of_strength || building ;; Building || communicate ;; Communication || gained ;; Getting
insurgency ;; Hostile_encounter || refers ;; Name_conferral
categorized ;; Know || strife ;; Violence
began ;; Process_start || declared ;; Expressing_publicly || reached ;; Arriving || open ;; Openness
battling ;; Hostile_encounter || contending ;; Statement
declaring ;; Expressing_publicly || Fighting ;; Hostile_encounter || escalated ;; Cause_change_of_strength || seizing ;; Hold
violence ;; Violence || occurred ;; Coming_to_be || claiming ;; Statement || forces ;; Military_operation
attackers ;; Attack || halting ;; Process_end || blew up ;; Destroying
represents ;; Presence
erupted ;; Process_start
rise ;; Motion_directional
tournament ;; Competition
contested ;; Competition || divided ;; Change
quarterfinals ;; Competition || advanced ;; Cause_to_make_progress || splits ;; Change
starts ;; Process_start || ends ;; Process_end || starts ;; Process_start || ends ;; Process_end
followed ;; Motion || Trophy ;; Competition || Trophy ;; Competition || Trophy ;; Competition || Trophy ;; Competition
final ;; Competition || contested ;; Competition
match ;; Competition || claim ;; Statement
occurred ;; Coming_to_be
attack ;; Attack
conducted ;; Action || postponed ;; Change_event_time || left ;; Departing
claimed ;; Statement
decided ;; Deciding || attack ;; Attack || avoid ;; Preventing_or_letting
survived ;; Preserving || wiped ;; Removing
occurring ;; Coming_to_be
carried out ;; Action
claimed ;; Statement
sanctioned ;; Arranging
participated ;; Participation
took place ;; Process_start || played ;; Competition
advancing ;; Cause_to_make_progress
beating ;; Competition || becoming ;; Becoming || retained ;; Preserving
drawn ;; Motion
earn ;; Earnings_and_losses
advance ;; Cause_to_make_progress || loss ;; Earnings_and_losses || faced ;; Hostile_encounter
prevailed ;; Conquering || making ;; Arriving || moved ;; Motion
moved ;; Motion
advanced ;; Cause_to_make_progress || come from ;; Motion
beat ;; Competition || beat ;; Competition || playing ;; Competition || settle ;; Statement
appeared ;; Change_sentiment
breakup ;; Dispersal || made ;; Creating
resumed ;; Recovering || appear ;; Presence
occurred ;; Coming_to_be
intercepted ;; Self_motion || escorted ;; Sending
surrounded ;; Surrounding || arrived ;; Arriving
supported ;; Supporting
went ;; Motion
claims ;; Statement
performing ;; Hold
began ;; Process_start || went off ;; Process_end
fought ;; Hostile_encounter || stripped ;; Emptying
killed ;; Killing || wounded ;; Bodily_harm || placed ;; Placing
died ;; Death || wounded ;; Bodily_harm
awarded ;; Giving
called ;; Name_conferral
designated ;; Choosing
Final ;; Competition || Championship ;; Competition || match ;; Competition || played ;; Competition
contested ;; Competition
finished ;; Process_end || final ;; Competition
claim ;; Statement || replay ;; Competition || captained ;; Control || managed ;; Control
game ;; Competition || game ;; Competition || dropping ;; Motion_directional || kicked ;; Motion
coming ;; Arriving || score ;; Getting || replay ;; Competition
accepting ;; Receiving || game ;; Competition || end ;; Process_end || gave ;; Giving
saying ;; Statement
finals ;; Process_end || get ;; Getting
games ;; Competition || shown ;; Statement
commentary ;; Statement || programmes ;; Creating || presented ;; Presence
fought ;; Hostile_encounter
claimed ;; Statement || liberated ;; Change_of_leadership
advocated ;; Convincing
decided ;; Deciding || became ;; Becoming
growing ;; Expansion || caused ;; Causation || dissolve ;; Dispersal || leading ;; Causation || declining ;; Cause_change_of_position_on_a_scale
bringing ;; Bringing || abolished ;; GiveUp || established ;; Creating
fighting ;; Hostile_encounter || supported ;; Supporting || enjoyed ;; Getting
became ;; Becoming || expelling ;; Removing || launch ;; Process_start
lost ;; Earnings_and_losses || end ;; Process_end || overwhelmed ;; Destroying || allowing ;; Preventing_or_letting || defeat ;; Conquering
help ;; Assistance || defeated ;; Conquering || took ;; Action
referendum ;; Change_of_leadership || recognition ;; Know || voted ;; Choosing
match ;; Competition || took place ;; Process_start || played ;; Competition
founded ;; Creating || organised ;; Arranging
held ;; Creating || selected ;; Choosing || matches ;; Competition
selected ;; Choosing || appointed ;; Change_of_leadership
dominated ;; Control || scored ;; Getting || claimed ;; Statement
met ;; Come_together || playing ;; Competition
played ;; Competition
achieved ;; Achieve || setting ;; Placing || record ;; Recording || debut ;; Process_start || race ;; Competition
race ;; Competition || competed ;; Competition || participation ;; Participation || trails ;; Expansion
retained ;; Preserving
begins ;; Process_start || race ;; Competition || visiting ;; Traveling || arriving in ;; Arriving || finishing ;; Process_end || progresses ;; Cause_to_make_progress
relay ;; Motion_directional || race ;; Competition || race ;; Competition || running ;; Self_motion || race ;; Competition || running ;; Self_motion || race ;; Competition || added ;; Cause_to_be_included
varying ;; Change || race ;; Competition || race ;; Competition
races ;; Competition || race ;; Competition || claiming ;; Statement
record ;; Recording || race ;; Competition
races ;; Competition
race ;; Competition
record ;; Recording || finished ;; Process_end
conducted ;; Action || offensive ;; Military_operation
attack ;; Attack || launched ;; Military_operation || claimed ;; Statement
displaced ;; Change
involved ;; Participation || flush ;; Emptying || hiding ;; Hiding_objects || described ;; Statement
received ;; Receiving
improved ;; Cause_to_make_progress || dropped ;; Cause_change_of_position_on_a_scale
began ;; Process_start
Championships ;; Competition || took place ;; Process_start
Championships ;; Competition || Olympic ;; Competition || leaving ;; Departing || contested ;; Participation
decided ;; Deciding
began ;; Process_start || respond ;; Response
accelerated ;; Cause_change_of_position_on_a_scale
resulted in ;; Causation || coming ;; Motion
hampered ;; Hindering || claim ;; Statement || ride ;; Motion || making it ;; Causation || lost ;; Earnings_and_losses
damaged ;; Damaging || passed ;; Motion_directional || walking ;; Motion
finished ;; Process_end
occurred ;; Presence || earthquake ;; Catastrophe
lasted ;; Process_end
earthquake ;; Catastrophe
lost ;; Earnings_and_losses
hit ;; Attack
earthquake ;; Catastrophe || claimed ;; Statement
occurred ;; Coming_to_be
wounded ;; Bodily_harm || took place ;; Process_start || killed ;; Killing
happened ;; Presence || held ;; Defending
carried out ;; Action || wrapped ;; Placing
declared ;; Expressing_publicly || injured ;; Bodily_harm
support ;; Supporting || carrying out ;; Action || claimed ;; Statement || said ;; Telling
criticised ;; Judgment_communication || condemning ;; Judgment_communication || support ;; Supporting || issued ;; Creating || affected ;; Influence
took place ;; Process_start
contested ;; Competition || occurring ;; Coming_to_be
organised ;; Arranging
changes ;; Change || scheduled ;; Arranging || take place ;; Hold
abandoned ;; GiveUp || compete ;; Competition || opting ;; Choosing
claim ;; Statement || defending ;; Defending || retained ;; Preserving || beating ;; Competition
became ;; Becoming || defend ;; Defending
claimed ;; Statement
competition ;; Competition || established ;; Creating || replacing ;; Change_of_leadership
began ;; Process_start || began ;; Process_start || launch ;; Process_start || suppress ;; Hindering
killed ;; Killing || raped ;; Bodily_harm || War ;; Hostile_encounter
cited ;; Protest || killed ;; Killing || claiming ;; Statement
declared ;; Expressing_publicly || supported ;; Supporting
fled ;; Escaping || conflict ;; Hostile_encounter
became ;; Becoming
violence ;; Violence || war ;; Hostile_encounter
killed ;; Killing || faced ;; Perception_active
claim ;; Statement
War ;; Hostile_encounter || took place ;; Process_start
genocide ;; Killing
battle ;; Hostile_encounter || battle ;; Hostile_encounter || ferry ;; Motion || war ;; Military_operation || marched ;; Motion_directional
battle ;; Hostile_encounter
making ;; Motion || marched ;; Motion_directional
arrived ;; Arriving
retreat ;; Escaping || causing ;; Causation || engaged ;; Hostile_encounter || casualties ;; Killing
taken part ;; Dispersal || moved ;; Self_motion
left ;; Departing || attacked ;; Attack || moved ;; Motion
battle ;; Hostile_encounter || open ;; Openness || called ;; Statement || took place ;; Process_start
resisted ;; Defending || attack ;; Attack || surprising ;; Change_sentiment
fled ;; Escaping || cut ;; Motion || throwing ;; Motion || chased ;; Motion
ended ;; Process_end || battle ;; Hostile_encounter
devastating ;; Destroying || damage ;; Damaging || cyclone ;; Catastrophe || carved ;; Bodily_harm
killed ;; Killing || damage ;; Damaging || record ;; Recording || made it ;; Causation
triggered ;; Causation || imposed ;; Action || evacuation ;; Escaping
storm ;; Catastrophe || effects ;; Influence || impacted ;; Influence || felt ;; Perception_active
developed ;; Coming_to_be
reached ;; Arriving || storm ;; Catastrophe
allowed ;; Preventing_or_letting || reach ;; Arriving || hurricane ;; Catastrophe
began ;; Process_start || weakening ;; Cause_change_of_strength || tracked ;; Self_motion || inhibit ;; Preventing_or_letting
dissipated ;; Dispersal || redeveloping ;; Cause_change_of_strength
moved ;; Motion || intensifying ;; Cause_change_of_strength
becoming ;; Becoming || dissipating ;; Dispersal || weakened ;; Cause_change_of_strength
inflicted ;; Causation || reporting ;; Reporting || claims ;; Statement || damage ;; Damaging || damage ;; Damaging || believed ;; Know
hit ;; Attack || aid ;; Assistance || established ;; Building || went ;; Motion
changed ;; Change || tilted ;; Motion
took place ;; Process_start || massacre ;; Killing
killed ;; Killing || wounding ;; Bodily_harm || command ;; Control
differ ;; Change
ended ;; Process_end || captured ;; Conquering || claimed ;; Statement || command ;; Control
visited ;; Traveling || resolve ;; Resolve_problem
organised ;; Arranging || proclaiming ;; Statement || incorporated ;; Cause_to_amalgamate
firing ;; Use_firearm || causing ;; Causation || interrupted ;; Hindering
expelled ;; Removing || response ;; Response || launched ;; Process_start
agreed ;; Agree_or_refuse_to_act
became ;; Becoming || signed on ;; Sign_agreement
charged ;; Judgment_communication
season ;; Competition
started ;; Process_start
broadcast ;; Expressing_publicly || privatised ;; Change
decided ;; Deciding || marked ;; Process_end || provide ;; Supply
competition ;; Competition || claimed ;; Statement || securing ;; Getting || losing ;; Earnings_and_losses
occurred ;; Presence || exploded ;; Attack || bombing ;; Attack
became ;; Becoming || attack ;; Attack || killed ;; Killing
send ;; Sending || warning ;; Warning || planted ;; Placing || took place ;; Process_start
warning ;; Warning || delayed ;; Change_event_time || attack ;; Attack || telephones ;; Communication || order ;; Arranging || due ;; Causation
found ;; Know || claimed ;; Statement || attack ;; Attack || carry out ;; Action || issued ;; Statement
published ;; Publishing || bombing ;; Attack || stated ;; Statement || investigation ;; Criminal_investigation || report ;; Reporting || believed in ;; Know
found ;; Know || report ;; Reporting || bombing ;; Attack || covered ;; Filling
bombing ;; Attack || described ;; Statement || inflicted ;; Bodily_harm
operated ;; Being_in_operation || scheduled ;; Process_start
crashed ;; Catastrophe || killing ;; Killing
published ;; Publishing || report ;; Reporting || concluded ;; Coming_to_believe
stated ;; Statement || flying ;; Motion || report ;; Reporting
approach ;; Arriving || conducted ;; Action || approach ;; Arriving || circling ;; Motion || revealed ;; Know
blamed ;; Judgment_communication || using ;; Using || report ;; Reporting || losing ;; Preventing_or_letting
fatal ;; Killing || recorded ;; Recording
Tour ;; Traveling || tour ;; Traveling || taking place ;; Process_start
organized ;; Arranging || support ;; Supporting || tour ;; Traveling || releases ;; Publishing
name ;; Name_conferral || used ;; Using || tour ;; Traveling || applied ;; Using
tour ;; Traveling
replacing ;; Change_of_leadership
recorded ;; Recording || tour ;; Traveling || concerts ;; Social_event || recordings ;; Recording || released ;; Publishing
released ;; Publishing || title ;; Name_conferral || filmed ;; Create_artwork
aired ;; Expressing_publicly
final ;; Process_end || recorded ;; Recording || concert ;; Social_event || show ;; Social_event
aired ;; Expressing_publicly || concert ;; Social_event
recording ;; Recording || released ;; Publishing
released ;; Publishing
concert ;; Social_event || released ;; Publishing
occurred ;; Coming_to_be
raided ;; Attack || established ;; Building || command ;; Control || learned ;; Education_teaching
injured ;; Bodily_harm || chose ;; Choosing
moving ;; Motion
ambushed ;; Attack || loss ;; Earnings_and_losses
killed ;; Killing || losses ;; Earnings_and_losses
fled ;; Escaping || reached ;; Arriving
published ;; Publishing || killed ;; Killing || killed ;; Killing || attacked ;; Attack || report ;; Reporting || went to ;; Motion || escaped ;; Escaping || followed ;; Self_motion
killed ;; Killing || dispersal ;; Dispersal || end ;; Process_end || led to ;; Causation || engagement ;; Hostile_encounter || raid ;; Attack
massacre ;; Killing || held ;; Defending
olympics ;; Competition || events ;; Competition || contested ;; Competition
participated ;; Participation
competition ;; Competition || competition ;; Competition || scheduled ;; Arranging
played ;; Competition
supplied ;; Supply || competitors ;; Competition
called ;; Request || Olympiad ;; Competition || Olympics ;; Competition
competitors ;; Competition || restricted ;; Hindering || open ;; Openness || ballooning ;; Expansion || events ;; Competition || open ;; Openness || events ;; Competition || handicapping ;; Hindering || selection ;; Choosing
published ;; Publishing || work ;; Employment
Tour ;; Traveling || tour ;; Traveling || celebrating ;; Social_event
award ;; Award || group ;; Cause_to_amalgamate || awarded ;; Giving
died ;; Death || founding ;; Creating || member ;; Becoming_a_member
filled ;; Filling || end ;; Process_end || tour ;; Traveling
group ;; Cause_to_amalgamate || released ;; Publishing
decided ;; Deciding || due ;; Causation || death ;; Death
joined ;; Becoming_a_member || group ;; Cause_to_amalgamate || played ;; Competition
met with ;; Come_together || band ;; Come_together
group ;; Cause_to_amalgamate || appeared ;; Presence
touring ;; Traveling || continues ;; Motion || rest ;; Placing
support ;; Supporting || concert ;; Social_event || Tour ;; Traveling || tour ;; Traveling || released ;; Publishing || released ;; Publishing
record ;; Recording || release ;; Publishing || embarked ;; Process_start || tour ;; Traveling || played ;; Competition
Awards ;; Social_event || played ;; Competition
equipment ;; Supply || set ;; Placing || played ;; Competition
took place ;; Process_start || tour ;; Traveling || show ;; Social_event
visited ;; Traveling || ending ;; Process_end || tour ;; Traveling
gave ;; Giving || took ;; Conquering || took ;; Conquering || tour ;; Traveling || break ;; Process_end || playing ;; Competition || schedule ;; Arranging
appearance ;; Presence || show ;; Social_event
began ;; Process_start || ended ;; Process_end || support ;; Supporting || tour ;; Traveling || Festival ;; Social_event || promotion ;; Cause_change_of_position_on_a_scale
held ;; Hold || concert ;; Social_event || tour ;; Traveling
seen ;; Perception_active
concert ;; Social_event || selected ;; Choosing || tour ;; Traveling
concert ;; Social_event || produced ;; Manufacturing || directed ;; Action || provided ;; Supply
composed ;; Creating || composed ;; Creating
performed ;; Hold || joining ;; Becoming_a_member || concert ;; Social_event
performed ;; Hold
performed ;; Hold
location ;; Placing || handled ;; Resolve_problem || concerts ;; Social_event || took place ;; Process_start
recorded ;; Recording || released ;; Publishing || released ;; Publishing
praised ;; Judgment_communication || concerts ;; Social_event || overlaid ;; Filling
began ;; Process_start || concert ;; Social_event || concert ;; Social_event || tour ;; Traveling
supported ;; Supporting || tour ;; Traveling
spread ;; Dispersal || spanning ;; Expansion || tour ;; Traveling
named ;; Name_conferral || opening ;; Openness || tour ;; Traveling
began ;; Process_start || published ;; Publishing || negotiations ;; Communication || Festival ;; Social_event || itinerary ;; Arranging || tour ;; Traveling
inclusion ;; Cause_to_be_included || materialize ;; Coming_to_be || failed ;; Agree_or_refuse_to_act
confirmed ;; Check || include ;; Cause_to_be_included || festival ;; Social_event || itinerary ;; Arranging || tour ;; Traveling
performances ;; Hold || tour ;; Traveling
told ;; Telling
tour ;; Traveling
playing ;; Self_motion
make ;; Manufacturing
love ;; Change_sentiment || play ;; Motion || playing ;; Motion
operated ;; Being_in_operation
crashed ;; Catastrophe || killing ;; Killing
ended ;; Process_end || search ;; Scrutiny || recovered ;; Recovering
perform ;; Hold || report ;; Reporting || control ;; Control || control ;; Control || released ;; Publishing || concluding ;; Protest || prompted ;; Cause_change_of_position_on_a_scale
Control ;; Control || lost ;; Earnings_and_losses || resulting in ;; Causation
contributing ;; Giving || cited ;; Statement
published ;; Publishing || album ;; Publishing
preceded ;; Presence || released ;; Publishing
reached ;; Arriving || came ;; Motion
published ;; Publishing || called ;; Name_conferral
debuted ;; Process_start || certified ;; Check
certified ;; Check
released ;; Publishing
certified ;; Check
crashed ;; Catastrophe
operated ;; Using || disappeared ;; Departing || take-off ;; Departing
published ;; Publishing || caused ;; Causation || pour ;; Motion || assisting ;; Assistance || cruising ;; Motion || lead to ;; Causation
crashed ;; Catastrophe
issued ;; Supply
attack ;; Attack || Battle ;; Hostile_encounter
battle ;; Hostile_encounter || occurred ;; Presence
revolution ;; Change_of_leadership || fought ;; Hostile_encounter || uprising ;; Violence || signaled ;; Statement
occurred ;; Coming_to_be || uprisings ;; Violence
achieved ;; Achieve || battle ;; Hostile_encounter || composed ;; Come_together
published ;; Publishing || said ;; Statement
occurred ;; Coming_to_be || disaster ;; Catastrophe || explosion ;; Destroying
run ;; Self_motion
working ;; Employment || explosion ;; Attack
recovered ;; Recovering
reports ;; Reporting
stated ;; Statement
released ;; Statement || statement ;; Statement || occurred ;; Coming_to_be || saying ;; Statement || explosion ;; Attack
saved ;; Rescuing || told ;; Telling || opening ;; Openness || complained ;; Judgment_communication
told ;; Telling || complained ;; Judgment_communication
beginning ;; Process_start || lose ;; Earnings_and_losses
allotted ;; Giving || guaranteed ;; Commitment
told ;; Telling || service ;; Supply || uses ;; Using
remarked ;; Statement || arriving ;; Arriving
reported ;; Reporting || strike ;; Attack
declaring ;; Expressing_publicly || signed ;; Sign_agreement || said ;; Statement
advanced ;; Cause_to_make_progress
found ;; Know || leading ;; Causation || explosion ;; Attack
found ;; Know || advanced ;; Cause_to_make_progress
found ;; Know
announced ;; Expressing_publicly || leaked ;; Motion || search ;; Scrutiny || search ;; Scrutiny || advancing ;; Cause_to_make_progress || suspended ;; Process_end
confirmed ;; Check || conference ;; Social_event || explosion ;; Attack || report ;; Reporting
announced ;; Expressing_publicly || closed ;; Body_movement || recovered ;; Recovering
published ;; Publishing || report ;; Reporting
stating ;; Statement || rescue ;; Rescuing || due ;; Causation
demanded ;; Request || reopened ;; Openness || rescue ;; Rescuing
conducted ;; Action || causes ;; Causation || investigation ;; Scrutiny || brought ;; Bringing || recovered ;; Recovering || compensated ;; Supply
hit ;; Attack || earthquake ;; Catastrophe
destroyed ;; Destroying || damage ;; Damaging
earthquake ;; Catastrophe
earthquake ;; Catastrophe || said ;; Statement
tsunami ;; Catastrophe
earthquake ;; Catastrophe || killed ;; Killing
earthquakes ;; Catastrophe
published ;; Publishing || predicted ;; Statement || occurred ;; Process_start
shooting ;; Killing || earthquake ;; Catastrophe || ordered ;; Arranging
formed ;; Coming_to_be || earthquake ;; Catastrophe
created ;; Causation
causing ;; Causation || Cyclone ;; Catastrophe || struck ;; Attack
began ;; Process_start || consolidated ;; Cause_to_amalgamate || developing ;; Coming_to_be
received ;; Receiving
cyclone ;; Catastrophe || bypassed ;; Self_motion || striking ;; Bodily_harm
affected ;; Influence || killed ;; Killing || flooded ;; Destroying
assisted ;; Assistance
emerged ;; Coming_to_be || regained ;; Recovering
cyclone ;; Catastrophe || landfall ;; Arriving || moved ;; Self_motion
killed ;; Killing || damaged ;; Damaging || destroyed ;; Destroying || triggered ;; Causation
submerged ;; Motion
traversed ;; Motion_directional || entered ;; Arriving
dropped ;; Motion
published ;; Publishing || asserted ;; Statement || confirmed ;; Check || tracked ;; Self_motion
published ;; Publishing
published ;; Publishing || consists ;; Cause_to_be_included || contains ;; Containing
published ;; Publishing || conjunction ;; Collaboration || participation ;; Participation
certified ;; Check
certified ;; Check
reached ;; Achieve
tour ;; Traveling
announcement ;; Expressing_publicly || breaking up ;; Destroying || saying ;; Communication || said ;; Telling
release ;; Publishing
statement ;; Statement || said ;; Communication || said ;; Communication || feel ;; Change_sentiment
satisfying ;; Change_sentiment || think ;; Coming_to_believe || believe ;; Protest
stated ;; Statement || saying ;; Communication || set ;; Placing || going to ;; Self_motion
performed ;; Hold
concert ;; Social_event
supported ;; Supporting || tour ;; Traveling
called ;; Name_conferral || saw ;; Self_motion
announced ;; Expressing_publicly || tour ;; Traveling
held ;; Hold || release ;; Publishing
filmed ;; Create_artwork || described ;; Statement
released ;; Publishing || confirmed ;; Check || present ;; Presence || recording ;; Recording || concert ;; Social_event || release ;; Publishing
placed ;; Placing || tours ;; Traveling || earning ;; Earnings_and_losses || appeared ;; Presence
Festival ;; Social_event || Festival ;; Social_event || festival ;; Social_event || referred ;; Name_conferral || took place ;; Process_start
held ;; Hold
festival ;; Social_event || attended ;; Participation
festival ;; Social_event || condemned ;; Judgment_communication
included ;; Cause_to_be_included
titled ;; Name_conferral || success ;; Earnings_and_losses || printed ;; Publishing || ask ;; Request
deaths ;; Death || murder ;; Killing || damaging ;; Damaging
avoided ;; Preventing_or_letting || festival ;; Social_event || issued ;; Creating || leaving ;; Departing || arrest ;; Arrest || arrest ;; Arrest
stated ;; Statement
promoted ;; Cause_change_of_position_on_a_scale
included ;; Cause_to_be_included
contained ;; Containing
collaborated ;; Collaboration
praised ;; Judgment_communication
received ;; Receiving
sold ;; Commerce_sell || announced ;; Expressing_publicly
used ;; Using || appeared ;; Publishing
support ;; Supporting || toured ;; Traveling
focused ;; Aiming || leg ;; Social_event || tour ;; Traveling
brought ;; Bringing || play ;; Self_motion || success ;; Achieve || crossed ;; Motion_directional || booked ;; Commerce_sell || proved ;; Coming_to_believe
attracted ;; Influence
visited ;; Traveling
introduced ;; Statement
starting ;; Process_start || ending ;; Process_end || tour ;; Traveling || shows ;; Social_event || embarked ;; Process_start
recorded ;; Recording || show ;; Social_event || released ;; Publishing
War ;; Hostile_encounter || concert ;; Social_event
inspired ;; Influence || war ;; Hostile_encounter || consists ;; Containing
performed ;; Hold
recorded ;; Recording || concerts ;; Social_event || release ;; Publishing
Festival ;; Social_event
serving ;; Assistance || performance ;; Hold
concerts ;; Social_event || debut ;; Process_start || marked ;; Recording
released ;; Publishing || recording ;; Recording
recording ;; Recording || debuted ;; Process_start
Recording ;; Recording || earned ;; Getting
received ;; Receiving || award ;; Award || War ;; Hostile_encounter || recordings ;; Recording
concert ;; Social_event || released ;; Publishing || support ;; Supporting
concluded ;; Process_end || started ;; Process_start
marked ;; Recording
performed ;; Participation
played ;; Participation
cut ;; Removing
entitled ;; Name_conferral
supporting ;; Supporting || released ;; Publishing
started ;; Process_start || ending ;; Process_end
sold ;; Commerce_sell
sold ;; Commerce_sell
included ;; Cause_to_be_included
filmed ;; Create_artwork
included ;; Cause_to_be_included || filmed ;; Create_artwork
included ;; Cause_to_be_included || filmed ;; Create_artwork
include ;; Cause_to_be_included || released ;; Publishing || circulated ;; Dispersal
followed ;; Motion
fought ;; Hostile_encounter || Capture ;; Conquering
lay ;; Placing
dominated ;; Control
formed ;; Creating
coming to ;; Motion || running ;; Motion
retarded ;; Hindering || brought ;; Bringing || strewn ;; Filling || keep ;; Preventing_or_letting || entangled ;; Hindering
begin ;; Process_start || reach ;; Arriving || attacks ;; Attack || attack ;; Attack
sent ;; Sending || fought ;; Hostile_encounter || counter-attack ;; Attack || give ;; Giving || regain ;; Recovering || arrive ;; Arriving || delay ;; Change_event_time
Beginning ;; Process_start || concert ;; Social_event || running ;; Motion || tandem ;; Cause_to_amalgamate || toured ;; Traveling || tours ;; Traveling
performed ;; Hold || shows ;; Social_event
sold ;; Commerce_sell || tour ;; Traveling || grossed ;; Earnings_and_losses
ended ;; Process_end || resumed ;; Process_start || tour ;; Traveling
leg ;; Social_event || tour ;; Traveling || canceled ;; Change_event_time || claiming ;; Statement
stated ;; Statement || restrains ;; Cause_change_of_position_on_a_scale || tour ;; Traveling
ending ;; Process_end || starting ;; Process_start || success ;; Achieve || tour ;; Traveling || proved ;; Coming_to_believe || playing ;; Competition
encounter ;; Hostile_encounter
conflict ;; Hostile_encounter || preceded ;; Process_start
seized ;; Hold || command ;; Control || dispatched ;; Arranging
surrounded ;; Surrounding || established ;; Building
conducted ;; Action || become ;; Becoming || siege ;; Besieging
withdrew ;; Escaping || killed ;; Killing || killed ;; Killing || lost ;; Earnings_and_losses || wounded ;; Bodily_harm || wounded ;; Bodily_harm
withdrew ;; Escaping || resulted in ;; Causation || arrest ;; Arrest
held ;; Defending
presided ;; Control
attended ;; Participation || making ;; Manufacturing
open ;; Openness || promote ;; Cause_to_make_progress
criticised ;; Judgment_communication || critiquing ;; Judgment_communication || sparked ;; Causation
performed ;; Hold
carried out ;; Military_operation
evacuated ;; Emptying
resulted in ;; Causation
existed ;; Presence
began ;; Process_start || beginning ;; Process_start
became ;; Becoming || assembled ;; Come_together || provide ;; Supply || support ;; Supporting
avoiding ;; Preventing_or_letting || allowing ;; Preventing_or_letting || collapse ;; Destroying || paused ;; Process_end
came ;; Getting || attack ;; Attack
commenced ;; Process_start || terminated ;; Process_end
beginning ;; Process_start || ending ;; Process_end || defense ;; Defending || compound ;; Cause_to_amalgamate || damage ;; Damaging || took place ;; Process_start
overwhelmed ;; Filling
completed ;; Process_end || left ;; GiveUp || left ;; GiveUp || chose ;; Choosing || stay ;; Temporary_stay
evacuated ;; Removing
sailed ;; Self_motion || flew ;; Self_motion
began ;; Process_start || allow ;; Preventing_or_letting || clog ;; Hindering || land ;; Body_movement || pushed ;; Motion
rescued ;; Rescuing || drop ;; Body_movement || ditch ;; Self_motion || told ;; Telling || take off ;; Departing
evacuated ;; Emptying
evacuated ;; Emptying
ending up ;; Process_end || processing ;; Self_motion || enter ;; Arriving || evacuated ;; Removing
deployment ;; Arranging
supporting ;; Supporting || tour ;; Traveling || Tour ;; Traveling
tour ;; Traveling || Tour ;; Traveling
became ;; Becoming || concerts ;; Social_event
said ;; Communication || grown ;; Cause_change_of_position_on_a_scale || noted ;; Know
renamed ;; Name_conferral || response ;; Response || extended ;; Expansion
filmed ;; Create_artwork
attacks ;; Attack
avoided ;; Preventing_or_letting || entered ;; Arriving || net ;; Earnings_and_losses || sink ;; Motion_directional
attacked ;; Attack || killed ;; Killing || detected ;; Know || scuttled ;; Destroying
recovered ;; Recovering
killing ;; Killing || torpedo ;; Attack || sank ;; Motion_directional
discovered ;; Know
disrupt ;; Hindering || raid ;; Military_operation || campaign ;; Military_operation || carried ;; Carry_goods || embarked ;; Process_start
attacked ;; Attack || killing ;; Killing || sinking ;; Motion_directional
bombarded ;; Attack
attacks ;; Attack || come ;; Arriving || bombardments ;; Attack || activity ;; Hostile_encounter || attack ;; Attack
destroy ;; Destroying || damage ;; Damaging || bombardments ;; Attack || effects ;; Influence || sank ;; Motion_directional
impact ;; Influence || upgrade ;; Cause_to_make_progress || protect ;; Defending || invasion ;; Military_operation || operations ;; Military_operation || creating ;; Creating || forcing ;; Request
Raid ;; Attack
planned ;; Arranging || force ;; Military_operation || reinforce ;; Cause_change_of_strength || withdraw ;; Removing
used ;; Using
allowing ;; Preventing_or_letting || organise ;; Arranging || defences ;; Defending || embarked ;; Self_motion || carrying ;; Bringing || call in ;; Request
negotiations ;; Communication || ended ;; Process_end || arrived in ;; Arriving || surrender ;; Surrendering
retreat ;; Escaping || ordered ;; Arranging
losses ;; Earnings_and_losses || forced ;; Causation || stop ;; Process_end
planned ;; Arranging || surrender ;; Surrendering || knowing ;; Know
received ;; Receiving || surrender ;; Surrendering || departure ;; Departing || made ;; Manufacturing
starting ;; Process_start || raid ;; Attack || forcing ;; Causation || giving rise to ;; Cause_change_of_position_on_a_scale || develop ;; Creating
Battle ;; Hostile_encounter || took place ;; Process_start
attack ;; Attack || Battle ;; Hostile_encounter || War ;; Hostile_encounter
decided ;; Deciding || planned ;; Arranging || capture ;; Conquering || conferences ;; Communication
began ;; Process_start || Battle ;; Hostile_encounter || effects ;; Influence
fired ;; Use_firearm || operated ;; Being_in_operation || found ;; Know || left ;; GiveUp || move ;; Motion
battle ;; Hostile_encounter || delays ;; Change_event_time || left ;; Causation || impression ;; Change_sentiment || advance ;; Cause_to_make_progress
lost ;; Earnings_and_losses || advance ;; Cause_to_make_progress
attacks ;; Attack || end ;; Process_end || changed ;; Change
began ;; Process_start || attack ;; Attack || capture ;; Conquering || advance ;; Cause_to_make_progress
discovered ;; Know || arrived ;; Arriving || make ;; Change || changes ;; Change
conducted ;; Action || attack ;; Attack || operation ;; Military_operation
attack ;; Attack || counter-attacks ;; Attack || control ;; Control || repulsed ;; Defending || retained ;; Preserving || forced ;; Request || retire ;; Escaping
Attacks ;; Attack || attack ;; Attack || gained ;; Getting || reached ;; Arriving
postponed ;; Change_event_time || restored ;; Recovering || improved ;; Cause_to_make_progress
losses ;; Earnings_and_losses || replace ;; Change || diverted ;; Removing
battle ;; Hostile_encounter || success ;; Earnings_and_losses
fought ;; Hostile_encounter || took place ;; Process_start || formed ;; Cause_to_amalgamate
occupied ;; Control
captured ;; Conquering || attacked ;; Attack
retreat ;; Escaping || stretching ;; Expansion || formed ;; Cause_to_amalgamate
advance ;; Self_motion || delay ;; Hindering || set up ;; Placing
retreat ;; Escaping || attack ;; Attack || battles ;; Hostile_encounter || fought ;; Hostile_encounter
captured ;; Conquering || supported ;; Supporting || fighting ;; Hostile_encounter
converged ;; Come_together || took ;; Conquering
retreated ;; Escaping || form ;; Cause_to_amalgamate
begun ;; Process_start
sealed ;; Placing
entombed ;; Placing
awarded ;; Giving
unearthed ;; Getting
breached ;; Damaging
made ;; Action
shipped ;; Sending || accepted ;; Receiving
fought ;; Hostile_encounter || War ;; Military_operation
changes ;; Change || shown ;; Presence
gave ;; Giving || decided ;; Deciding || defeat ;; Conquering || state ;; Statement || regain ;; Getting || ceded ;; Surrendering
hold ;; Defending || made ;; Causation
help ;; Assistance || supplies ;; Supply || cut off ;; Removing
allow ;; Preventing_or_letting || invasion ;; Attack || set up ;; Building
convinced ;; Convincing || resist ;; Defending
tournament ;; Competition
event ;; Competition || referred ;; Statement
participate ;; Participation || participating ;; Participation || field ;; Bringing
participate ;; Participation || field ;; Bringing
begin ;; Process_start || event ;; Competition || announced ;; Expressing_publicly || canceled ;; Removing
event ;; Competition || indicate ;; Statement || canceled ;; Removing
fought ;; Hostile_encounter
spanned ;; Expansion || involved ;; Participation || affecting ;; Influence
included ;; Cause_to_be_included || included ;; Cause_to_be_included || split ;; Causation
support ;; Supporting || conquer ;; Conquering || crush ;; Destroying
begun ;; Process_start || became ;; Becoming || conflict ;; Hostile_encounter || centered ;; Placing || drew ;; Motion
ever-growing ;; Cause_change_of_position_on_a_scale || bringing ;; Bringing || rivalry ;; Competition
revolution ;; Change_of_leadership || aligned ;; Cause_to_amalgamate
ended ;; Process_end || altering ;; Change || prevailed ;; Conquering || destroyed ;; Destroying
bombing ;; Terrorism
opposed ;; Agree_or_refuse_to_act || carried out ;; Military_operation
killed ;; Killing || injured ;; Bodily_harm || bombing ;; Terrorism || incident ;; Catastrophe
warnings ;; Warning || sent ;; Sending || moved ;; Motion
caused ;; Causation || dealt ;; Resolve_problem || bombing ;; Terrorism || spurred ;; Causation
declared ;; Expressing_publicly || kill ;; Killing || ceasefire ;; Process_end || apologised ;; Judgment_communication
included ;; Cause_to_be_included
killed ;; Killing || injured ;; Bodily_harm
bombing ;; Attack
alleged ;; Statement || given ;; Giving || prevented ;; Preventing_or_letting || bombing ;; Terrorism
revealed ;; Reveal_secret || monitoring ;; Scrutiny || driven ;; Bringing
warnings ;; Warning || criticised ;; Judgment_communication || bombing ;; Terrorism || said ;; Statement
obtained ;; Getting || bombing ;; Terrorism || convict ;; Legal_rulings
released ;; Releasing || revealed ;; Reveal_secret || convicted ;; Legal_rulings
found ;; Know
murder ;; Killing || charged ;; Judgment_communication || withdrawn ;; Removing
began ;; Process_start
defeated ;; Conquering
ordering ;; Arranging || rejected ;; Agree_or_refuse_to_act || issued ;; Statement
destroy ;; Destroying || protect ;; Defending
attacked ;; Attack
began ;; Process_start || fought ;; Hostile_encounter || acknowledge ;; Statement
received ;; Receiving
shot down ;; Use_firearm || protect ;; Defending
arranged ;; Arranging || forced ;; Request || suspend ;; Process_end || sailing ;; Self_motion || took ;; Conquering
began ;; Process_start || issued ;; Statement || extending ;; Expansion
drawn ;; Motion
suffered ;; Bodily_harm || failed ;; Earnings_and_losses || inflict ;; Causation || gain ;; Getting
Operation ;; Military_operation || took place ;; Process_start
operation ;; Military_operation || capture ;; Conquering
attack ;; Attack
taken ;; Conquering
began ;; Process_start || raid ;; Attack || destroyed ;; Destroying || attacking ;; Attack
Supported ;; Supporting
reached ;; Arriving || cleared ;; Removing
beginning ;; Process_start || withdrawal ;; Escaping || encountered ;; Hostile_encounter
fell ;; Motion_directional || linked ;; Connect
defended ;; Defending || ended ;; Process_end
success ;; Judgment_communication
stop ;; Process_end || sending ;; Sending || retired ;; Escaping
established ;; Creating
began ;; Process_start
takeoff ;; Motion || caught fire ;; Damaging || loss ;; Death
route ;; Self_motion
registered ;; Recording || named ;; Name_conferral || named ;; Name_conferral || accident ;; Catastrophe || flown ;; Motion
manifest ;; Presence
takeoff ;; Motion || aborted ;; Process_end || heard ;; Perception_active
ordered ;; Request || deaths ;; Death || fire ;; Catastrophe || failure ;; Causation || due ;; Causation || generated ;; Creating
changes ;; Change || covers ;; Hiding_objects || analyst ;; Scrutiny || said ;; Statement || accident ;; Catastrophe
competition ;; Competition || held ;; Hold
competition ;; Competition || festival ;; Social_event
festival ;; Social_event || held ;; Hold
covers ;; Hiding_objects || dancing ;; Social_event || incorporates ;; Cause_to_amalgamate
rising ;; Change_of_leadership || introduced ;; Choosing
Competition ;; Competition || events ;; Social_event || create ;; Creating
held ;; Defending || incorporates ;; Cause_to_amalgamate
held ;; Defending || connected ;; Connect
combined ;; Cause_to_amalgamate || festival ;; Social_event || shows ;; Presence
fought ;; Hostile_encounter
began ;; Process_start
changed ;; Change || met ;; Come_together
agreed ;; Agree_or_refuse_to_act || sell ;; Commerce_sell
hidden ;; Hiding_objects || hid ;; Hiding_objects || raid ;; Attack || bragged ;; Statement || consulted ;; Communication || replied ;; Response
advanced ;; Self_motion
delayed ;; Change_event_time
driven ;; Motion
turned ;; Becoming
casualties ;; Catastrophe || conflict ;; Hostile_encounter || consider ;; Statement
give ;; Giving
began ;; Process_start || occurred ;; Presence || took place ;; Process_start
occurred ;; Presence
captured ;; Conquering
dismounted ;; Motion || reinforced ;; Cause_change_of_strength || took place ;; Process_start
received ;; Receiving || forced ;; Motion || retire ;; Departing || garrisoned ;; Defending
began ;; Process_start || occupied ;; Control
consolidated ;; Cause_change_of_strength
adjusted ;; Change || secured ;; Preventing_or_letting
tell ;; Telling || advances ;; Cause_to_make_progress
began ;; Process_start
mounted ;; Connect || established ;; Building || got ;; Getting || cross ;; Motion_directional || constructed ;; Building
moving ;; Motion || captured ;; Conquering || crossed ;; Motion_directional || mounted ;; Connect || rising ;; Cause_change_of_position_on_a_scale || advanced ;; Self_motion
marching ;; Self_motion
destroying ;; Destroying || travelled ;; Traveling || cut ;; Removing
defended ;; Defending || replaced ;; Change_of_leadership || arrive ;; Arriving || blown up ;; Destroying || allow ;; Preventing_or_letting || strengthen ;; Cause_change_of_strength
sent ;; Sending || cover ;; Hiding_objects || took ;; Conquering
forced ;; Motion || retreat ;; Escaping || became ;; Becoming || achieve ;; Achieve || made ;; Creating
tour ;; Traveling || concert ;; Social_event
tour ;; Traveling || took place ;; Process_start
visited ;; Traveling
perform ;; Hold || cover ;; Hiding_objects
included ;; Cause_to_be_included || covers ;; Hiding_objects || set ;; Placing
killed ;; Killing || hidden ;; Hiding_objects || exploded ;; Attack || injured ;; Bodily_harm
death ;; Death || due ;; Causation || injuries ;; Bodily_harm || victims ;; Death || members ;; Becoming_a_member || expected ;; Statement
attack ;; Attack || group ;; Cause_to_amalgamate || claimed ;; Statement
arrested ;; Arrest || killed ;; Killing || accused ;; Judgment_communication || killing ;; Killing || forces ;; Causation || attack ;; Attack || suspects ;; Committing_crime || militants ;; Terrorism || taken ;; Conquering
operation ;; Military_operation || seized ;; Hold
extremists ;; Terrorism || brought ;; Bringing
moved ;; Motion || detaining ;; Arrest || finding ;; Know
included ;; Cause_to_be_included || Combat ;; Hostile_encounter || Combat ;; Hostile_encounter || operation ;; Military_operation
joined ;; Becoming_a_member || operation ;; Military_operation
discovered ;; Know || block ;; Hiding_objects || contained ;; Containing
revealed ;; Reveal_secret || fabricating ;; Manufacturing || search ;; Scrutiny || used ;; Using
found ;; Know || destroyed ;; Destroying
detained ;; Arrest || terrorists ;; Terrorism || questioning ;; Scrutiny
conducted ;; Action || led to ;; Causation || capture ;; Arrest
named ;; Name_conferral
combat ;; Hostile_encounter || joint ;; Collaboration || joint ;; Collaboration || force ;; Hostile_encounter || assigned ;; Choosing || commanded ;; Control || led ;; Conquering
searched ;; Scrutiny
found ;; Know || hiding ;; Hiding_objects
capture ;; Conquering || resist ;; Response
chartered ;; Preventing_or_letting
operating ;; Being_in_operation || crashed ;; Catastrophe || take-off ;; Motion
died ;; Death
blocked ;; Hiding_objects || built ;; Building || cause ;; Causation || receiving ;; Receiving || airspeed ;; Motion
sitting ;; Placing || covers ;; Hiding_objects
deadliest ;; Killing || involving ;; Cause_to_be_included || accident ;; Catastrophe
killed ;; Killing || suicide bombing ;; Terrorism || bombing ;; Attack
occurred ;; Presence || attack ;; Attack
injuries ;; Bodily_harm
reported ;; Reporting
choose ;; Choosing || attacked ;; Attack
constructed ;; Building || set up ;; Building || prevent ;; Preventing_or_letting
demonstration ;; Social_event
demonstration ;; Social_event || started ;; Process_start || went off ;; Process_end || mingled ;; Come_together
detonated ;; Motion
reports ;; Reporting
suggest ;; Statement || crashed ;; Attack || detonated ;; Motion
claim ;; Statement || worn ;; Hiding_objects
started ;; Process_start || leading to ;; Causation || explosion ;; Attack || explosions ;; Attack || killings ;; Killing || said ;; Statement || discharging ;; Use_firearm
used ;; Using || transport ;; Sending
stopped ;; Self_motion || sideswiped ;; Bodily_harm || crashed ;; Destroying
injured ;; Bodily_harm || killed ;; Killing || collision ;; Catastrophe
caused ;; Causation || collision ;; Catastrophe
installed ;; Placing || left ;; Departing
found ;; Know || told ;; Telling || inspected ;; Scrutiny || work ;; Being_in_operation
work ;; Being_in_operation || performed ;; Participation
inspected ;; Scrutiny || work ;; Employment || Hidden ;; Hiding_objects || made ;; Arriving || included ;; Cause_to_be_included
accident ;; Catastrophe || fined ;; Judgment_communication
Operation ;; Military_operation || evacuate ;; Emptying || advance ;; Self_motion || forced ;; Causation
learned ;; Know || retreating ;; Escaping
dispatched ;; Sending || pursued ;; Self_motion || intercept ;; Attack || moving ;; Self_motion
sent ;; Sending || hard ;; Confronting_problem || going ;; Motion || flying ;; Motion
arrived at ;; Arriving || set up ;; Building || blocks ;; Hiding_objects
arrived ;; Arriving || leading ;; Causation || ambush ;; Attack
attacked ;; Attack || attacks ;; Attack
surrendered ;; Surrendering || arriving ;; Arriving || pressing ;; Request
dispatched ;; Sending || captured ;; Conquering || attack ;; Attack || ordered ;; Request || took ;; Conquering || stop ;; Process_end || troops ;; Motion || advance ;; Self_motion || War ;; Military_operation
supply ;; Supply || breakdowns ;; Damaging || exhaustion ;; Bodily_harm || transport ;; Bringing
escaped ;; Escaping || disaster ;; Catastrophe
sent ;; Sending || reinforced ;; Cause_change_of_strength || brought ;; Bringing || form ;; Coming_to_be
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || battle ;; Hostile_encounter || Crossing ;; Motion_directional || defeated ;; Conquering || took ;; Conquering
advanced ;; Cause_to_make_progress || named ;; Name_conferral || crossing ;; Motion_directional || approaching ;; Arriving || defences ;; Defending || command ;; Control
defence ;; Defending
abandoned ;; GiveUp || retreat ;; Escaping
ended ;; Process_end || battle ;; Hostile_encounter || invasion ;; Attack
found ;; Know || retreat ;; Escaping || blocked ;; Hiding_objects || destroy ;; Destroying
escaped ;; Escaping || fleeing ;; Escaping || pursued ;; Aiming
Siege ;; Besieging
surrendered ;; Surrendering
turned ;; Becoming || killed ;; Killing
captured ;; Conquering || thrown ;; Motion || killed ;; Killing || hide ;; Hiding_objects
discovery ;; Coming_to_be || engaged ;; Participation
embittered ;; Bodily_harm || inspired ;; Influence
named ;; Name_conferral || record-breaking ;; Social_event
given ;; Giving || used ;; Using
formed ;; Coming_to_be
became ;; Becoming || weakened ;; Cause_change_of_strength || reached ;; Arriving || storm ;; Catastrophe || landfall ;; Self_motion
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
rain ;; Motion || making it ;; Causation || storm ;; Catastrophe
caused ;; Causation || caused ;; Causation || deaths ;; Death || floods ;; Motion
blocked ;; Hiding_objects || destroyed ;; Destroying
assassination ;; Killing || opposed ;; Agree_or_refuse_to_act || happened ;; Presence
fired ;; Use_firearm || killing ;; Killing || hit ;; Bodily_harm
died ;; Death || war ;; Military_operation || support ;; Supporting || appealing ;; Protest
attack ;; Attack
assassination ;; Killing
conduct ;; Action || condemned ;; Judgment_communication || murder ;; Killing || vowed ;; Request || investigation ;; Criminal_investigation
originating ;; Coming_to_be || involvement ;; Participation || charged ;; Criminal_investigation
murder ;; Killing || confessed ;; Reveal_secret
statement ;; Statement || torture ;; Bodily_harm || extracted ;; Containing || retracted ;; Agree_or_refuse_to_act
surrounded ;; Surrounding || arrested ;; Arrest || forces ;; Causation || block ;; Hiding_objects
took place ;; Process_start
alleged ;; Statement || ordered ;; Arranging
raises ;; Cause_change_of_position_on_a_scale
found ;; Know || hide ;; Hiding_objects
violence ;; Violence || concludes ;; Statement
agreed ;; Agree_or_refuse_to_act || offered ;; Supply || finds ;; Know
invasion ;; Attack || Operation ;; Military_operation
capture ;; Conquering || called ;; Request
provide ;; Supply || support ;; Supporting || provide ;; Supply || serve ;; Assistance || occupation ;; Control || cover ;; Hiding_objects || advancing ;; Self_motion
resist ;; Defending || defend ;; Defending || assigned ;; Choosing
damaging ;; Damaging || resist ;; Defending || destroying ;; Destroying || struck ;; Attack
began ;; Process_start || construction ;; Building || occupied ;; Control
began ;; Process_start || build ;; Building || established ;; Creating
observed ;; Perception_active
counter-attacked ;; Attack || initiating ;; Process_start || threatened ;; Warning
conflict ;; Hostile_encounter
covered ;; Hiding_objects || contains ;; Containing
crossed ;; Motion_directional || rises ;; Motion_directional
capture ;; Conquering || incorporated ;; Cause_to_be_included
defeat ;; Conquering
existed ;; Presence || conquest ;; Conquering
engaged ;; Participation
included ;; Cause_to_be_included
crossed ;; Motion_directional || penetrated ;; Attack
established ;; Creating
arrived at ;; Arriving || took place ;; Process_start || travelled ;; Traveling
disaster ;; Catastrophe || set out ;; Departing || massacred ;; Attack
placed ;; Placing
rebelled ;; Hostile_encounter || control ;; Control
reach ;; Arriving || incorporation ;; Dispersal || resulted in ;; Causation || defeat ;; Conquering
operation ;; Military_operation || took place ;; Conquering
attached ;; Connect || targeted ;; Aiming || concealed ;; Hiding_objects || command ;; Control || operation ;; Military_operation || believed ;; Statement
covering ;; Hiding_objects || uncover ;; Reveal_secret || resulted in ;; Causation || casualties ;; Bodily_harm
fought ;; Hostile_encounter || operation ;; Military_operation || war ;; Military_operation
war ;; Military_operation || clear ;; Emptying || remained ;; Presence
bombing ;; Attack || used ;; Using || attack ;; Attack || destroyed ;; Destroying || ending ;; Process_end || offensive ;; Attack
Tournament ;; Competition || tournament ;; Competition
tournament ;; Competition || held ;; Hold
tournament ;; Competition || held ;; Hold
tournament ;; Competition
tournament ;; Competition || tournament ;; Competition || tournament ;; Competition || match ;; Competition || elimination ;; Removing || facing ;; Confronting_problem || determine ;; Deciding
tournament ;; Competition || wrestle ;; Hostile_encounter || block ;; Hiding_objects || doubled ;; Cause_change_of_position_on_a_scale || take place ;; Process_start
matches ;; Competition || falls ;; Motion_directional
tournament ;; Competition || team ;; Cause_to_amalgamate || tournament ;; Competition || use ;; Using || help ;; Assistance
tournament ;; Competition
tournament ;; Competition
found ;; Know || participated ;; Participation || event ;; Competition || tournaments ;; Competition || documented ;; Recording || known ;; Know
cyclone ;; Catastrophe || affected ;; Influence
developed ;; Coming_to_be
developed ;; Coming_to_be
develop ;; Coming_to_be || storm ;; Catastrophe || intensified ;; Cause_change_of_strength
developed ;; Coming_to_be || reaching ;; Achieve
re-intensified ;; Cause_change_of_strength || weakened ;; Cause_change_of_strength
Increased ;; Expansion || weakened ;; Cause_change_of_strength || passed ;; Motion_directional
moved ;; Motion || storm ;; Catastrophe || turned ;; Removing || weakened ;; Cause_change_of_strength
became ;; Becoming || became ;; Becoming || meandered ;; Motion || cyclone ;; Catastrophe || weakened ;; Cause_change_of_strength
brought ;; Bringing || dropped ;; Motion_directional
Effects ;; Influence
storm ;; Catastrophe || dropped ;; Motion_directional
blocked ;; Hiding_objects || downed ;; Motion || left ;; Causation
supply ;; Supply || lost ;; Earnings_and_losses
losses ;; Earnings_and_losses || damage ;; Damaging
losses ;; Earnings_and_losses || damage ;; Damaging
injuries ;; Bodily_harm || killed ;; Killing
produced ;; Creating
fought ;; Hostile_encounter || engagement ;; Hostile_encounter || Wars ;; Hostile_encounter
captured ;; Conquering || attacked ;; Attack || defending ;; Defending || battle ;; Hostile_encounter || defeated ;; Conquering
command ;; Control
observation ;; Perception_active || revealed ;; Reveal_secret || surrender ;; Surrendering || engaged ;; Hostile_encounter || forced ;; Request
escaped ;; Escaping || engagement ;; Hostile_encounter || taking ;; Action
intention ;; Aiming || battle ;; Hostile_encounter || maintained ;; Preserving || eliminating ;; Destroying
sent ;; Sending || raid ;; Attack || reconnoitre ;; Scrutiny
captured ;; Conquering || followed ;; Motion || success ;; Earnings_and_losses
eliminated ;; Destroying
traveling ;; Traveling || toured ;; Traveling
traveling ;; Traveling || touring ;; Traveling
became ;; Becoming || became ;; Becoming || took place ;; Process_start || starting ;; Process_start
participated ;; Participation || named ;; Name_conferral || festival ;; Social_event || festival ;; Social_event || lended ;; Giving
began ;; Process_start || festival ;; Social_event || conceived ;; Creating
included ;; Cause_to_be_included
final ;; Process_end || stated ;; Statement
revealed ;; Reveal_secret || taking place ;; Process_start || tour ;; Traveling
detonated ;; Terrorism
destroy ;; Destroying || explosion ;; Catastrophe || passed ;; Motion_directional
accused ;; Judgment_communication || responded ;; Response || invasion ;; Attack || established ;; Creating || led to ;; Causation
exposed ;; Reveal_secret || withdrawal ;; Escaping || leading ;; Causation
bombing ;; Terrorism || known ;; Know
took place ;; Process_start || assassination ;; Killing
elections ;; Change_of_leadership || scheduled ;; Arranging || campaigning ;; Social_event
fired ;; Use_firearm || suicide ;; Killing || detonated ;; Committing_crime || shooting ;; Killing
declared ;; Expressing_publicly || dead ;; Death
killed ;; Killing || bombing ;; Catastrophe
killed ;; Killing || bombing ;; Attack || return ;; Arriving
caused ;; Causation || died ;; Death || stated ;; Statement || explosion ;; Attack || hit ;; Attack || force ;; Causation || reports ;; Reporting || indicated ;; Statement || strike ;; Attack
argued ;; Quarreling || rejected ;; Agree_or_refuse_to_act || suffered ;; Bodily_harm || bomb ;; Attack || detonation ;; Attack
claim ;; Statement
asked ;; Request
revealed ;; Reveal_secret || prevented ;; Preventing_or_letting || investigation ;; Scrutiny || assassination ;; Killing || taken ;; Action
rising ;; Motion_directional || led ;; Causation || govern ;; Action || war ;; Hostile_encounter || encompassed ;; Surrounding
marked ;; Recording || revolution ;; Change_of_leadership || revealed ;; Reveal_secret || war ;; Hostile_encounter || war ;; Hostile_encounter || rising ;; Cause_change_of_position_on_a_scale
debate ;; Communication || War ;; Hostile_encounter || overthrow ;; Change_of_leadership || took ;; Conquering
received ;; Receiving || War ;; Hostile_encounter || aid ;; Assistance
ended ;; Process_end || signing ;; Sign_agreement || War ;; Hostile_encounter || demobilization ;; Emptying
election ;; Choosing || election ;; Choosing || handing ;; Giving || resulted in ;; Causation
controlled ;; Control
approach ;; Arriving || crashed ;; Catastrophe
killed ;; Killing || remains ;; Presence
cause ;; Causation || descended ;; Motion_directional
found ;; Know || operating ;; Military_operation || control ;; Control || approach ;; Arriving
caused ;; Causation || electing ;; Choosing || follow ;; Self_motion || sitting ;; Motion
found ;; Know || training ;; Education_teaching
revealed ;; Reveal_secret || control ;; Control
terrorist ;; Terrorism || shooting ;; Use_firearm || took place ;; Process_start || carried out ;; Action || bombing ;; Attack || attacks ;; Attack || attacks ;; Attack
began ;; Process_start || drew ;; Getting || attacks ;; Attack
died ;; Death || wounded ;; Bodily_harm
attacks ;; Attack || occurred ;; Presence
explosion ;; Catastrophe
secured ;; Preventing_or_letting
conducted ;; Action || ended ;; Process_end || flush ;; Emptying || culminated ;; Process_end || death ;; Death
condemned ;; Judgment_communication
disclosed ;; Reveal_secret
said ;; Statement
confirmed ;; Check
released ;; Releasing || disappeared ;; Change_sentiment
revolution ;; Change_of_leadership || counter-revolution ;; Change_of_leadership
uprising ;; Change_of_leadership || acquired ;; Getting
uprising ;; Change_of_leadership || took place ;; Process_start || formed ;; Coming_to_be
uprising ;; Change_of_leadership || included ;; Cause_to_be_included || included ;; Cause_to_be_included || called ;; Name_conferral
became ;; Becoming
lost ;; Earnings_and_losses || defensive ;; Defending
taken ;; Creating
send ;; Sending || giving ;; Giving || stalled ;; Hindering || turned ;; Becoming
massacred ;; Killing
described ;; Statement || reject ;; Agree_or_refuse_to_act
uprising ;; Change_of_leadership || suppressed ;; Hindering
bestowed ;; Giving || destruction ;; Destroying || revealed ;; Reveal_secret || concludes ;; Process_end
revolutionary ;; Change_of_leadership || conflict ;; Hostile_encounter
cyberbullying ;; Damaging || hanging ;; Placing || suicide ;; Killing || committed ;; Committing_crime
exposing ;; Reveal_secret || assaulted ;; Attack || posted ;; Sending || used ;; Using || tell ;; Statement || blackmailed ;; Committing_crime || bullied ;; Hold
resulting in ;; Causation || went ;; Motion
views ;; Protest
investigations ;; Scrutiny || launched ;; Process_start
made ;; Creating || criminalizing ;; Preventing_or_letting || statement ;; Statement || discussion ;; Communication || response ;; Response || suggested ;; Convincing
funding ;; Supply || support ;; Supply || introduced ;; Creating || propose ;; Action
established ;; Creating || receiving ;; Receiving || support ;; Supply || donations ;; Giving
revised ;; Change || led to ;; Causation
launched ;; Process_start
planned ;; Arranging || exploit ;; Reveal_secret
reduced ;; Cause_change_of_position_on_a_scale
applied ;; Using || prevented ;; Preventing_or_letting
became ;; Becoming
became ;; Becoming
pitched ;; Motion || stalled ;; Motion || crashed ;; Destroying
caught fire ;; Damaging || burned ;; Damaging
injuries ;; Bodily_harm || died ;; Death
accident ;; Catastrophe || accident ;; Catastrophe
occurred ;; Coming_to_be
uncovered ;; Reveal_secret || Investigation ;; Scrutiny
taken off ;; Departing || became ;; Becoming
found ;; Know || performed ;; Hold || evidence ;; Statement || set ;; Being_in_operation
set ;; Being_in_operation || exacerbated ;; Cause_change_of_position_on_a_scale
suffered ;; Bodily_harm || cruising ;; Self_motion || leading to ;; Causation
suffered ;; Bodily_harm || injuries ;; Bodily_harm
service ;; Supply || operating ;; Being_in_operation
produced ;; Manufacturing || caused ;; Causation
determined ;; Deciding || revealed ;; Reveal_secret || manufacturing ;; Manufacturing || cause ;; Causation || joining ;; Becoming_a_member
increasing ;; Cause_change_of_position_on_a_scale || led to ;; Causation
documented ;; Recording || erupted ;; Process_start || eruptions ;; Catastrophe
pulverized ;; Destroying || spewed ;; Self_motion || ejecting ;; Motion
obliterated ;; Destroying || buried ;; Destroying
became ;; Becoming || revealed ;; Reveal_secret
found ;; Know
included ;; Cause_to_be_included || associated ;; Collaboration || eruptions ;; Catastrophe || eruptions ;; Catastrophe
elected ;; Choosing || election ;; Choosing
emerged ;; Coming_to_be || election ;; Choosing
adopted ;; Choosing
established ;; Building || election ;; Choosing
met ;; Come_together
imprisoned ;; Arrest || attended ;; Participation || elected ;; Choosing
lasted ;; Process_end
elected ;; Choosing
stated ;; Statement || acknowledge ;; Reveal_secret || elected ;; Choosing
called ;; Name_conferral
includes ;; Cause_to_be_included || elected ;; Choosing
listed ;; Cause_to_be_included
brought ;; Bringing || central ;; Placing
originated ;; Coming_to_be || developed ;; Cause_to_make_progress
began ;; Process_start || develop ;; Cause_to_make_progress
upgraded ;; Cause_to_make_progress
intensify ;; Cause_change_of_strength || crossing ;; Motion_directional || close ;; Body_movement
emerged ;; Coming_to_be || became ;; Becoming || organized ;; Arranging
revealed ;; Reveal_secret || intensified ;; Cause_change_of_strength
attained ;; Getting || landfall ;; Self_motion || named ;; Name_conferral
weakened ;; Cause_change_of_strength || degenerated ;; Change
flooding ;; Motion || experienced ;; Perception_active
killed ;; Killing || killed ;; Killing
killed ;; Killing || flooding ;; Catastrophe
visited ;; Traveling
began ;; Process_start || concluded ;; Process_end
performed ;; Hold
announced ;; Expressing_publicly || revealed ;; Reveal_secret
ended ;; Process_end || death ;; Death || involved ;; Cause_to_be_included
claimed ;; Statement
demanded ;; Request
fight ;; Hostile_encounter || attack ;; Attack || reach ;; Arriving || Due ;; Causation || held ;; Defending
center ;; Placing
began ;; Process_start || supported ;; Supporting || pumped ;; Self_motion || murder ;; Killing
killed ;; Killing || poisoning ;; Bodily_harm || died ;; Death || due ;; Causation
killed ;; Killing || died ;; Death || pumped ;; Self_motion
disclosed ;; Reveal_secret
hurricane ;; Catastrophe
developed ;; Coming_to_be
Moving ;; Motion || due ;; Causation || organized ;; Arranging
reaching ;; Arriving || intensified ;; Cause_change_of_strength
turned ;; Becoming || weakened ;; Cause_change_of_strength
passing ;; Motion_directional || downgraded ;; Cause_change_of_position_on_a_scale || became ;; Becoming || storm ;; Catastrophe || exposed ;; Reveal_secret
caused ;; Causation || impact ;; Influence
dissipated ;; Dispersal
occurred ;; Coming_to_be
killed ;; Killing || shooting ;; Killing || party ;; Social_event
included ;; Cause_to_be_included || dead ;; Death
massacre ;; Killing || accused ;; Judgment_communication || statements ;; Statement || revealed ;; Reveal_secret || inquiry ;; Scrutiny || reports ;; Reporting || consisting ;; Containing
shooting ;; Use_firearm || slipped ;; Self_motion
declared ;; Expressing_publicly || death ;; Death
massacre ;; Killing || died ;; Death || regaining ;; Recovering
became ;; Becoming || death ;; Death
ended ;; Process_end || battle ;; Hostile_encounter || repelled ;; Defending
moved ;; Motion
struck ;; Attack
split ;; Forming_relationships
prompted ;; Causation
force ;; Request
began ;; Process_start
operated ;; Being_in_operation
taking off ;; Departing || operating ;; Being_in_operation || crashed ;; Catastrophe
killed ;; Killing
occurred ;; Presence
found ;; Know || left ;; Departing || beginning ;; Process_start || landing ;; Arriving || separated ;; Forming_relationships || flipping ;; Self_motion
separated ;; Forming_relationships || damaged ;; Damaging || left ;; Departing || severed ;; Destroying
forces ;; Causation || resulted in ;; Causation
began ;; Process_start || produced ;; Manufacturing || deployed ;; Arranging || damaged ;; Damaging || left ;; Departing
caused ;; Causation || disrupted ;; Hindering || left ;; Departing || inverted ;; Motion || reaching ;; Arriving || crashing ;; Catastrophe || open ;; Openness || roll ;; Motion
separation ;; Forming_relationships || attributed ;; Judgment_communication || holding ;; Defending || caused ;; Causation || used ;; Using
party ;; Social_event || hosted ;; Social_event
event ;; Social_event || held ;; Hold
attracts ;; Influence
catering ;; Using
party ;; Social_event || attracts ;; Influence
lasts ;; Process_end
produced ;; Manufacturing || event ;; Social_event
tournament ;; Competition
matches ;; Competition || played ;; Competition
met ;; Come_together || played ;; Competition
consisted ;; Forming_relationships || matches ;; Competition || played ;; Participation || played ;; Competition
started ;; Process_start || played ;; Competition
held ;; Hold || siege ;; Besieging || siege ;; Besieging || crisis ;; Confronting_problem || occurred ;; Coming_to_be
debated ;; Communication || treated ;; Coming_to_believe || terrorist ;; Terrorism || event ;; Confronting_problem || attack ;; Attack
terrorist ;; Terrorism || event ;; Confronting_problem || deemed ;; Coming_to_believe || attack ;; Attack
siege ;; Besieging || led to ;; Causation || heard ;; Perception_active || standoff ;; Hostile_encounter || gunshot ;; Use_firearm || stormed ;; Attack
raid ;; Attack || killed ;; Killing || killed ;; Killing
killed ;; Killing
raid ;; Attack || injured ;; Bodily_harm
siege ;; Besieging || negotiation ;; Communication || taking ;; Action || siege ;; Besieging || criticised ;; Judgment_communication || deaths ;; Death || end ;; Process_end || handling ;; Hold
negotiated ;; Communication || siege ;; Besieging || called ;; Telling || said ;; Telling
die ;; Death || left ;; Departing
seen ;; Change_sentiment || holding ;; Hold
demanded ;; Request || used ;; Using || brought ;; Bringing || mistook ;; Coming_to_believe
demanded ;; Request || speak ;; Communication
indicated ;; Statement || described ;; Check || assessment ;; Judgment_communication
statement ;; Statement || issued ;; Creating || condemned ;; Judgment_communication || joint ;; Collaboration || siege ;; Besieging || incident ;; Attack || services ;; Communication || held ;; Hold
set up ;; Placing || turned ;; Becoming
damaged ;; Damaging || raid ;; Attack || reopening ;; Recovering || renovated ;; Recovering
tour ;; Traveling
began ;; Process_start || promote ;; Cause_to_make_progress
announced ;; Expressing_publicly || revealed ;; Statement
planned ;; Arranging || visit ;; Traveling
supported ;; Supporting || designed ;; Creating
planned ;; Arranging
announced ;; Expressing_publicly || concluded ;; Process_end
described ;; Statement
performed ;; Hold || performed ;; Hold || dance ;; Self_motion || divided ;; Forming_relationships
included ;; Cause_to_be_included || invited ;; Request
made ;; Manufacturing || changes ;; Change
generated ;; Creating
records ;; Recording || broke ;; Destroying
became ;; Becoming || earning ;; Earnings_and_losses || breaking ;; Destroying
performed ;; Hold || grossing ;; Earnings_and_losses || making it ;; Causation
acknowledge ;; Statement
faced ;; Confronting_problem || opposition ;; Judgment_communication
received ;; Receiving || performing ;; Hold || statement ;; Statement
killed ;; Killing || constructing ;; Building || collapsed ;; Catastrophe
released ;; Publishing || broadcast ;; Expressing_publicly
comprised ;; Cause_to_be_included
achieved ;; Achieve || overcame ;; Conquering
captured ;; Conquering || partitioned ;; Forming_relationships
led to ;; Causation || angered ;; Change_sentiment
provoked ;; Causation
besieged ;; Besieging || fortified ;; Cause_change_of_strength || invasion ;; Attack || engagement ;; Forming_relationships
besieged ;; Besieging
conducted ;; Action || send ;; Sending || interrupted ;; Hindering
began ;; Process_start
arrival ;; Arriving || beginning ;; Process_start || penetrated ;; Attack
began ;; Process_start || commenced ;; Process_start
surrendered ;; Surrendering || escaped ;; Escaping || remaining ;; Presence || occupied ;; Control || interned ;; Education_teaching
fought ;; Quarreling || begin ;; Process_start || withdrew ;; Escaping
participated ;; Participation || held ;; Defending
combining ;; Cause_to_amalgamate
began ;; Process_start
sold ;; Commerce_sell
become ;; Becoming || entered ;; Arriving
looks ;; Perception_active
allows ;; Preventing_or_letting
consisted ;; Cause_to_be_included || connected ;; Connect || towed ;; Motion
announced ;; Expressing_publicly || return ;; Arriving
cancelled ;; Change_event_time
said ;; Communication || takes place ;; Process_start
engagement ;; Hostile_encounter
resulted in ;; Causation || victory ;; Getting || commanded ;; Control || took place ;; Process_start
attack ;; Attack
break out ;; Process_start
killed ;; Killing || cost ;; Earnings_and_losses || repulsed ;; Defending || injured ;; Bodily_harm
operating ;; Being_in_operation
surrendered ;; Surrendering || working ;; Employment
remained ;; Presence
stripped ;; Removing || deny ;; Statement || burned ;; Destroying
supply ;; Supply || captured ;; Conquering
remained ;; Placing || scored ;; Getting
captured ;; Conquering || mauled ;; Hostile_encounter
operating ;; Being_in_operation || trained ;; Education_teaching
mount ;; Motion_directional || build ;; Building
destroying ;; Destroying || lasted ;; Being_in_operation || produced ;; Causation
taking ;; Getting || suffered ;; Bodily_harm || casualties ;; Death
wedding ;; Social_event || took place ;; Process_start
conducted ;; Action || given ;; Giving || presided ;; Control || preached ;; Statement || marriage ;; Forming_relationships
chosen ;; Choosing || attended ;; Perception_active
made ;; Manufacturing
decide ;; Deciding || wedding ;; Social_event
met ;; Come_together
announced ;; Expressing_publicly
wedding ;; Social_event || marriage ;; Forming_relationships || attracted ;; Influence
mark ;; Recording || held ;; Defending || held ;; Defending || held ;; Defending || wedding ;; Forming_relationships
viewed ;; Perception_active
watching ;; Perception_active || peaked ;; Self_motion
occurred ;; Coming_to_be || murder ;; Committing_crime
seen ;; Perception_active || building ;; Building
found ;; Placing || married ;; Forming_relationships
arrested ;; Arrest || worked ;; Employment
pleaded ;; Request || murder ;; Committing_crime
sentenced ;; Legal_rulings
trampled ;; Destroying || generated ;; Creating
War ;; Hostile_encounter || support ;; Supporting || control ;; Control
conflict ;; Hostile_encounter || followed ;; Motion
control ;; Control || ended ;; Process_end || ceded ;; Surrendering || occupied ;; Hold
supported ;; Supporting || kept ;; Presence
formed ;; Coming_to_be || declaring ;; Expressing_publicly || encroachment ;; Attack
enforce ;; Action || directed ;; Action
defeats ;; Conquering || suffered ;; Bodily_harm
killed ;; Killing || casualties ;; Catastrophe || forces ;; Hostile_encounter || suffered ;; Earnings_and_losses
ordered ;; Arranging || organize ;; Arranging || disaster ;; Catastrophe || train ;; Education_teaching
command ;; Control || took ;; Getting
Battle ;; Hostile_encounter || campaign ;; Military_operation || led ;; Bringing
establish ;; Building
defeated ;; Conquering || cede ;; Surrendering
arranged ;; Arranging
meeting ;; Social_event || follows ;; Motion || reunited ;; Cause_to_amalgamate || reunited ;; Cause_to_amalgamate || show ;; Social_event
announced ;; Expressing_publicly || demand ;; Request || perform ;; Hold || led to ;; Causation
performed ;; Hold || commitments ;; Commitment || consisted ;; Cause_to_be_included || took place ;; Process_start
announced ;; Expressing_publicly
consisted ;; Cause_to_be_included
governed ;; Control || Games ;; Competition || Games ;; Competition || event ;; Social_event || held ;; Hold
held ;; Hold
participated ;; Participation || Games ;; Competition || representing ;; Presence
competitions ;; Competition || snowboarding ;; Self_motion || expanded ;; Expansion
topped ;; Placing
placed ;; Placing
campaigns ;; Military_operation
began ;; Process_start || expansion ;; Expansion
dispatched ;; Sending || conquer ;; Conquering || leading to ;; Causation
took ;; Creating || assimilated ;; Forming_relationships
excavated ;; Know
extended ;; Expansion || led to ;; Causation
demand ;; Request || led to ;; Causation
focused ;; Aiming
performed ;; Hold
performed ;; Hold
consisted ;; Forming_relationships
released ;; Publishing || held ;; Hold
released ;; Publishing || contains ;; Containing || selection ;; Choosing
cyclone ;; Catastrophe || hurricane ;; Catastrophe || affected ;; Influence || made ;; Change || causing ;; Causation
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || disturbance ;; Attack
moving ;; Motion || landfall ;; Arriving || making ;; Arriving || intensified ;; Cause_change_of_strength || storm ;; Catastrophe
entering ;; Arriving || reached ;; Arriving || making ;; Arriving || crossing ;; Motion_directional || hurricane ;; Catastrophe || landfall ;; Attack
hurricane ;; Catastrophe || cyclone ;; Catastrophe || weakened ;; Cause_change_of_strength || becoming ;; Becoming || merged ;; Cause_to_amalgamate
caused ;; Causation || storm ;; Catastrophe || landfall ;; Arriving || winds ;; Motion || damage ;; Damaging
strengthening ;; Cause_change_of_strength || moved ;; Motion || hurricane ;; Catastrophe || generated ;; Creating || caused ;; Causation || damage ;; Damaging
landfall ;; Attack || hurricane ;; Catastrophe || produced ;; Causation
winds ;; Motion || damaged ;; Damaging
damages ;; Damaging || hurricane ;; Catastrophe || caused ;; Causation || death ;; Death
festival ;; Social_event
festival ;; Social_event
visitors ;; Traveling
advance ;; Cause_to_make_progress || sold ;; Commerce_sell
takes place ;; Process_start || festival ;; Social_event
festival ;; Social_event || called ;; Name_conferral || separate ;; Forming_relationships || stages ;; Placing
Including ;; Cause_to_be_included
organized ;; Arranging || put together ;; Come_together
festival ;; Social_event || chosen ;; Choosing || look ;; Perception_active || open ;; Openness || contest ;; Competition
festival ;; Social_event || organised ;; Arranging
profits ;; Earnings_and_losses || festival ;; Social_event || used ;; Using || support ;; Supporting || bands ;; Social_event
included ;; Cause_to_be_included
consisted ;; Forming_relationships
scheduled ;; Arranging
raids ;; Attack
conducted ;; Action
service ;; Supply || took place ;; Process_start
carried ;; Carry_goods || raids ;; Attack || consisted ;; Forming_relationships
ended ;; Process_end || caused ;; Causation || raids ;; Attack || order ;; Arranging || bring ;; Bringing
participated ;; Participation || competition ;; Competition
competition ;; Competition || began ;; Process_start
match ;; Competition || held ;; Hold
matches ;; Competition || comprised ;; Forming_relationships
allowed ;; Preventing_or_letting || play ;; Competition
event ;; Competition || produced ;; Manufacturing
took place ;; Process_start
pinning ;; Connect || event ;; Competition
match ;; Competition
included ;; Cause_to_be_included || hit ;; Attack || get ;; Getting
sales ;; Commerce_sell || grossed ;; Earnings_and_losses
began ;; Process_start
become ;; Becoming || match ;; Competition || win ;; Getting
defend ;; Defending
marry ;; Forming_relationships
marry ;; Forming_relationships
bombings ;; Attack || killed ;; Killing
injured ;; Bodily_harm
linked ;; Connect || bombings ;; Attack || bombings ;; Attack || occurred ;; Presence || lifted ;; Motion || said ;; Statement
bombings ;; Attack || occurred ;; Presence
bombings ;; Attack || killed ;; Killing
perpetrated ;; Committing_crime || attack ;; Attack || suicide bomber ;; Terrorism
attack ;; Attack || involved ;; Cause_to_be_included || placed ;; Placing || bombs ;; Attack || occurred ;; Presence
attack ;; Attack || occurred ;; Presence
bombings ;; Attack || killed ;; Killing
dead ;; Death || attack ;; Attack || wounded ;; Bodily_harm || attack ;; Attack || wounded ;; Bodily_harm || attack ;; Attack || wounded ;; Bodily_harm
concert ;; Social_event
supported ;; Supporting
planned ;; Arranging || sales ;; Commerce_sell || cancelled ;; Change_event_time
announced ;; Expressing_publicly || tour ;; Traveling
performed ;; Hold || beginning ;; Process_start || concluding ;; Process_end
consisted ;; Forming_relationships
opening ;; Openness || made ;; Manufacturing
received ;; Receiving || reception ;; Social_event
allowing ;; Preventing_or_letting || perform ;; Hold
released ;; Publishing
decide ;; Deciding || tournament ;; Competition
hosted ;; Social_event
tournament ;; Competition || held ;; Hold
tournament ;; Competition || playing ;; Participation || split ;; Forming_relationships
knockout ;; Removing || progressed ;; Cause_to_make_progress || final ;; Competition
final ;; Process_end || co-hosts ;; Social_event
win ;; Earnings_and_losses
game ;; Competition || attendance ;; Participation
tournament ;; Competition || tournament ;; Competition || match ;; Competition || crowd ;; Filling || crowd ;; Filling || record ;; Recording || estimated ;; Causation || estimated ;; Causation || watched ;; Perception_active || final ;; Competition || watched ;; Perception_active || drawn ;; Motion
sold ;; Commerce_sell || match ;; Competition || sale ;; Commerce_sell
Battle ;; Hostile_encounter
used ;; Using || battle ;; Hostile_encounter
hold ;; Defending || reduce ;; Cause_change_of_position_on_a_scale || purpose ;; Aiming || pressure ;; Influence || reinforce ;; Cause_change_of_strength || maintain ;; Preserving
causing ;; Causation || capturing ;; Conquering
pinned ;; Connect || surround ;; Surrounding
withdraw ;; Escaping || casualties ;; Killing || returning ;; Arriving || supply ;; Supply || control ;; Control
battle ;; Hostile_encounter || depicted ;; Statement
captured ;; Conquering || battle ;; Hostile_encounter || equipment ;; Supply
firing ;; Use_firearm || showed ;; Protest || titled ;; Name_conferral || dead ;; Death || captured ;; Conquering || drive ;; Motion
titled ;; Name_conferral || showed ;; Presence
evacuation ;; Removing || evacuation ;; Removing
commenced ;; Process_start || surrounded ;; Surrounding || troops ;; Hostile_encounter || Battle ;; Hostile_encounter || cut off ;; Removing
captured ;; Conquering || perish ;; Death || disaster ;; Catastrophe || seemed ;; Change_sentiment || called ;; Request || stranded ;; Motion || speech ;; Communication || saying ;; Statement
rescue ;; Rescuing || fight ;; Hostile_encounter || speech ;; Statement || hailed ;; Judgment_communication || deliverance ;; Assistance
invaded ;; Attack || declared ;; Expressing_publicly || imposed ;; Action
sent ;; Sending || help ;; Assistance || force ;; Hostile_encounter || defend ;; Defending
invaded ;; Attack
attacked ;; Attack || drove ;; Motion
forces ;; Hostile_encounter || forces ;; Hostile_encounter
began ;; Process_start || withdrawal ;; Escaping || planning ;; Arranging || closest ;; Motion || evacuation ;; Removing
send ;; Sending || order ;; Arranging || approved ;; Preventing_or_letting || order ;; Arranging || Command ;; Request || issued ;; Statement
Destroying ;; Destroying || rescinded ;; Removing || order ;; Arranging
gave ;; Giving || defensive ;; Defending || forces ;; Hostile_encounter || troops ;; Hostile_encounter || Battle ;; Hostile_encounter || construct ;; Creating || pull ;; Hold || fight ;; Defending
fought ;; Hostile_encounter || remaining ;; Temporary_stay || delaying ;; Change_event_time
rescued ;; Rescuing || end ;; Process_end || evacuated ;; Departing
ships ;; Sending || protective ;; Preventing_or_letting || wade ;; Self_motion || embark ;; Self_motion
service ;; Supply || ships ;; Sending || ferried ;; Sending || pleasure ;; Change_sentiment || called ;; Request
abandon ;; GiveUp || equipment ;; Supply || campaign ;; Motion
reminded ;; Telling || deliverance ;; Assistance || assign ;; Request
evacuations ;; Removing
given ;; Giving || encircled ;; Besieging
existed ;; Presence
surrounded ;; Surrounding
retreat ;; Escaping || resulted ;; Causation || defeat ;; Conquering || Battle ;; Hostile_encounter
achieved ;; Achieve || defence ;; Defending || warfare ;; Hostile_encounter || use ;; Using
decision ;; Deciding || Command ;; Request || Battle ;; Hostile_encounter || save ;; Rescuing || success ;; Achieve
war ;; Hostile_encounter || fought ;; Hostile_encounter
felt ;; Perception_active || represented ;; Presence || coexist ;; Cause_to_amalgamate
conflict ;; Hostile_encounter || resulted ;; Causation || preceded ;; Process_start
included ;; Cause_to_be_included || causes ;; Causation
production ;; Manufacturing || Control ;; Control || played ;; Competition
surrounded ;; Surrounding || capturing ;; Conquering
led to ;; Causation || imposed ;; Action
casualties ;; Catastrophe || died ;; Death
saturated ;; Filling
became ;; Becoming || enabling ;; Preventing_or_letting || rise ;; Cause_change_of_position_on_a_scale
supporters ;; Supporting || supported ;; Supporting
died ;; Death || elected ;; Choosing || infection ;; Bodily_harm || election ;; Choosing
surrounded ;; Surrounding || died ;; Death
announced ;; Expressing_publicly
gained ;; Getting
mourning ;; Social_event
conducted ;; Action
held ;; Hold
lay ;; Placing
buried ;; Placing || held ;; Hold
announced ;; Expressing_publicly
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || Battle ;; Hostile_encounter || War ;; Military_operation || took place ;; Process_start
expel ;; Removing || response ;; Response || Campaign ;; Military_operation || Offensive ;; Attack || end ;; Process_end || war ;; Military_operation || launched ;; Process_start
dubbed ;; Name_conferral || planned ;; Arranging || counteroffensive ;; Attack
attacks ;; Attack || destroying ;; Destroying || allowing ;; Preventing_or_letting || Campaign ;; Military_operation || Campaign ;; Military_operation || launched ;; Process_start || move ;; Self_motion
surrounded ;; Surrounding || lost ;; Earnings_and_losses || avoid ;; Preventing_or_letting || losses ;; Earnings_and_losses || battles ;; Hostile_encounter || withdrawals ;; Escaping || inflict ;; Causation || forces ;; Causation || managed ;; Control || retreating ;; Escaping
retreat ;; Escaping || battle ;; Hostile_encounter || losses ;; Earnings_and_losses || forced ;; Causation
began ;; Process_start || dispute ;; Quarreling || dispute ;; Quarreling || surrounding ;; Surrounding
began ;; Process_start || moved ;; Motion || claims ;; Statement || claims ;; Statement || adjudged ;; Judgment_communication || completed ;; Process_end
accused ;; Judgment_communication || expanded ;; Expansion || claims ;; Statement || occupying ;; Hold
moved ;; Motion || denied ;; Statement || killed ;; Killing
agreement ;; Communication || reached ;; Arriving || withdraw ;; Departing
declared ;; Expressing_publicly || decision ;; Deciding || withdraw ;; Departing || awarded ;; Award || forces ;; Military_operation
argument ;; Quarreling || awarded ;; Award || rejected ;; Agree_or_refuse_to_act || finding ;; Know || made ;; Creating
Battle ;; Hostile_encounter || forces ;; Military_operation || occurring ;; Presence
ended ;; Process_end || forces ;; Causation || push ;; Motion
resisting ;; Defending || reinforced ;; Cause_change_of_strength || arrived in ;; Arriving
casualties ;; Catastrophe || fight ;; Hostile_encounter || advancing ;; Cause_to_make_progress || inflict ;; Bodily_harm || hold ;; Defending || positions ;; Placing
performed ;; Hold || forced ;; Motion || withdraw ;; Escaping || fighting ;; Hostile_encounter || surrounding ;; Surrounding || forces ;; Military_operation
showing ;; Presence
criticized ;; Judgment_communication || retreat ;; Escaping
statements ;; Statement || described ;; Statement || downplaying ;; Judgment_communication || overstating ;; Judgment_communication
occurred ;; Coming_to_be || called ;; Name_conferral || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter
War ;; Military_operation || battle ;; Hostile_encounter
resulted in ;; Causation || forces ;; Causation || forces ;; Causation || victory ;; Earnings_and_losses || battle ;; Hostile_encounter || command ;; Control || commanded ;; Control
occurred ;; Coming_to_be || battle ;; Hostile_encounter
hold ;; Defending || take ;; Conquering
began ;; Process_start || engagement ;; Hostile_encounter || overwhelmed ;; Destroying || defenses ;; Defending
retreated ;; Escaping || left ;; Departing || flanked ;; Surrounding
counter-attack ;; Attack || broke ;; Destroying || look ;; Perception_active
attack ;; Attack || maintain ;; Preserving || convinced ;; Convincing || hope ;; Coming_to_believe
withdrew ;; Escaping || cut off ;; Dispersal
stretched ;; Expansion || fighting ;; Quarreling || casualties ;; Death || appeared ;; Presence
casualties ;; Death
losses ;; Earnings_and_losses || withdrew ;; Escaping || victory ;; Earnings_and_losses || engaged ;; Hostile_encounter
fought ;; Hostile_encounter || starting ;; Process_start || Battle ;; Hostile_encounter || War ;; Hostile_encounter || campaign ;; Military_operation || Campaign ;; Military_operation
surrounding ;; Surrounding || took place ;; Process_start
began ;; Process_start || battle ;; Hostile_encounter || invasion ;; Attack || Combined ;; Cause_to_amalgamate || invasion ;; Attack || established ;; Creating
battle ;; Hostile_encounter || control ;; Control || clashes ;; Quarreling
fled ;; Escaping || drowning ;; Death || collapsed ;; Destroying || retreated ;; Escaping
marking ;; Recording || battle ;; Hostile_encounter || re-entered ;; Motion_directional || end ;; Process_end
fighting ;; Hostile_encounter || killed ;; Killing || casualties ;; Death || wounded ;; Bodily_harm
killed ;; Killing || war ;; Hostile_encounter || wounded ;; Bodily_harm || taken ;; Action
battle ;; Hostile_encounter || marked ;; Recording || War ;; Hostile_encounter || war ;; Hostile_encounter || victory ;; Getting || victory ;; Getting || opening ;; Process_start || took place ;; Process_start
surrounded ;; Surrounding || played ;; Competition
played ;; Competition || match ;; Competition
hear ;; Perception_active || play ;; Competition || scored ;; Getting || match ;; Competition
determined ;; Deciding || ended ;; Process_end
stripped ;; Emptying || competition ;; Competition || received ;; Receiving
win ;; Getting
changed ;; Change || occurring ;; Presence
hijacked ;; Committing_crime
released ;; Releasing || flown ;; Self_motion
released ;; Releasing || detained ;; Arrest || hijacking ;; Committing_crime
taken ;; Conquering || identified ;; Coming_to_believe
demand ;; Request || speak ;; Telling
hijacking ;; Committing_crime
took off ;; Departing
due ;; Causation || scheduled ;; Arranging || land ;; Self_motion
believed ;; Know
released ;; Releasing || touched down ;; Arriving || taken ;; Conquering
left ;; Departing || seen ;; Traveling || entering ;; Arriving
surrounded ;; Surrounding || forces ;; Motion || remained ;; Temporary_stay
demanded ;; Request || reported ;; Reporting
claimed ;; Statement || said ;; Communication || carrying ;; Carry_goods
went ;; Motion || described ;; Statement || meeting ;; Communication
taken off ;; Conquering || evacuated ;; Departing
affected ;; Influence || floods ;; Catastrophe || mudslides ;; Catastrophe
died ;; Death || injured ;; Bodily_harm || made ;; Causation
built ;; Building
Damage ;; Damaging || product ;; Manufacturing
affected ;; Influence || died ;; Death || surrounding ;; Surrounding
reported ;; Reporting || Deaths ;; Death
declared ;; Expressing_publicly
declared ;; Expressing_publicly || dead ;; Death
started ;; Process_start || continued ;; Motion
falling ;; Motion_directional || predicted ;; Statement
said ;; Telling
forced ;; Motion
prevent ;; Preventing_or_letting || rescue ;; Rescuing || destroying ;; Destroying || used ;; Using || worked ;; Being_in_operation
admitted ;; Statement || added ;; Cause_to_be_included
hit ;; Attack
killed ;; Killing
missing ;; Death
cut off ;; Removing || hit ;; Attack
damage ;; Damaging || relocated ;; Removing || due ;; Causation || bulldozed ;; Removing
attack ;; Attack
demonstrated ;; Protest
reported ;; Reporting
surrounded ;; Surrounding
began ;; Process_start
died ;; Death || arrested ;; Arrest || wounded ;; Bodily_harm
claim ;; Statement
Battle ;; Hostile_encounter || forces ;; Causation || Confederate ;; Collaboration || marked ;; Recording || fought ;; Hostile_encounter || War ;; Military_operation || end ;; Process_end || offensive ;; Attack || Campaign ;; Military_operation
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || involved ;; Cause_to_be_included || war ;; Military_operation || defeat ;; Conquering
named ;; Name_conferral || battle ;; Hostile_encounter || flows ;; Motion || battle ;; Hostile_encounter || fought ;; Hostile_encounter
renewed ;; Recovering || aiming ;; Aiming || Campaign ;; Military_operation || force ;; Causation
forces ;; Causation || scattered ;; Dispersal || heading ;; Self_motion || consolidated ;; Cause_change_of_strength || forced ;; Causation
followed ;; Self_motion || brushed ;; Motion
determined ;; Deciding || decided ;; Deciding || reoccupy ;; Conquering || meet ;; Come_together || move back ;; Self_motion || defeat ;; Conquering
attack ;; Attack || headed ;; Self_motion
marched ;; Motion_directional || fought ;; Hostile_encounter
began ;; Process_start
break ;; Destroying || assaulted ;; Attack
resumed ;; Process_start || assault ;; Attack
misinformed ;; Coming_to_believe
detached ;; Removing || assault ;; Attack || moving ;; Motion || created ;; Creating
attack ;; Attack || drove ;; Motion
defensive ;; Defending || create ;; Creating || forces ;; Causation || command ;; Control || forming ;; Creating || assumed ;; Employment
held ;; Defending || determined ;; Deciding || assaults ;; Attack || launched ;; Process_start
besieging ;; Besieging || forces ;; Causation || surrounding ;; Surrounding || retired ;; Escaping || occupied ;; Control
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || recapture ;; Conquering || Campaign ;; Military_operation || fought ;; Hostile_encounter || complete ;; Process_end || final ;; Process_end || War ;; Military_operation
attacked ;; Attack
attack ;; Attack || beginning ;; Process_start || fighting ;; Hostile_encounter
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || victories ;; Earnings_and_losses || supported ;; Supporting || extended ;; Expansion || communication ;; Communication || forced ;; Causation || pushed ;; Self_motion
construction ;; Building || advance ;; Cause_to_make_progress
became ;; Becoming || reaching ;; Arriving || attack ;; Attack || formed ;; Coming_to_be
assault ;; Attack || defended ;; Defending || surrounded ;; Surrounding || rising ;; Cause_change_of_position_on_a_scale
encircled ;; Besieging
captured ;; Conquering || occupied ;; Control || defences ;; Defending
took place ;; Process_start
fought ;; Hostile_encounter
withdraw ;; Escaping || abandoned ;; GiveUp || damaged ;; Damaging || hit ;; Attack || disabled ;; Destroying
withdrew ;; Escaping || fire ;; Use_firearm
abandoned ;; GiveUp || arrived ;; Arriving || disabled ;; Destroying || advanced ;; Cause_to_make_progress
surrounded ;; Surrounding || recaptured ;; Conquering
restored ;; Recovering
Operation ;; Military_operation || referred ;; Name_conferral
supported ;; Supporting || attacks ;; Attack
opposing ;; Agree_or_refuse_to_act || force ;; Military_operation
break ;; Bodily_harm || open ;; Openness
planned ;; Arranging || tied ;; Connect || forces ;; Military_operation
employed ;; Employment
controlled ;; Control
supporting ;; Supporting || launched ;; Motion
opposed ;; Agree_or_refuse_to_act
broke ;; Destroying || defenses ;; Defending || drew ;; Motion || reducing ;; Cause_change_of_position_on_a_scale
pushed ;; Motion || supported ;; Supporting
attacked ;; Attack
fell ;; Motion_directional || collapsed ;; Catastrophe
attacked ;; Attack
attacked ;; Attack
breached ;; Damaging
forced ;; Causation
moving ;; Motion || ordered ;; Arranging || cutting off ;; Hindering || retreat ;; Escaping || turn ;; Change
capture ;; Conquering
avoided ;; Preventing_or_letting || surrounded ;; Surrounding
identified ;; Check || fought ;; Hostile_encounter
collided ;; Self_motion || occurred ;; Coming_to_be
accident ;; Catastrophe
investigated ;; Criminal_investigation || given ;; Giving || entering ;; Arriving || ran through ;; Motion
sending ;; Sending || faulted ;; Judgment_communication
brought ;; Bringing
requested ;; Request || included ;; Cause_to_be_included || rescue ;; Rescuing || search ;; Scrutiny
became ;; Becoming
remained ;; Presence
began ;; Process_start
reinvigorated ;; Recovering || surrounding ;; Surrounding || initiated ;; Process_start || resulted in ;; Causation
occurred ;; Presence
drew ;; Motion || tear ;; Destroying || met with ;; Come_together || detentions ;; Arrest || protests ;; Protest
arguing ;; Quarreling || caused ;; Causation || damage ;; Damaging || existing ;; Presence || plans ;; Arranging || plan ;; Arranging || plant ;; Placing || Protests ;; Protest || ignited ;; Causation || expand ;; Expansion || opposed ;; Agree_or_refuse_to_act
began ;; Process_start || Demonstrations ;; Social_event || surrounded ;; Surrounding || block ;; Hindering || project ;; Arranging || barricaded ;; Besieging
beat ;; Bodily_harm || tear ;; Destroying || fired ;; Use_firearm
charged ;; Judgment_communication || decided ;; Deciding || declaration ;; Expressing_publicly || detained ;; Arrest || announcing ;; Expressing_publicly || stop ;; Process_end || project ;; Arranging || said ;; Statement || published ;; Statement
reports ;; Reporting || circulated ;; Dispersal || denied ;; Judgment_communication
agreed ;; Agree_or_refuse_to_act || declared ;; Expressing_publicly || release ;; Releasing || party ;; Participation || expansion ;; Expansion || detained ;; Arrest || plans ;; Arranging || suspended ;; Process_end
told ;; Telling || demonstrations ;; Social_event || followed ;; Action || developments ;; Cause_to_make_progress || negotiate ;; Communication
strengthen ;; Cause_change_of_strength || undertaken ;; Action
began ;; Process_start || began ;; Process_start
called ;; Request || called ;; Request
became ;; Becoming || conquest ;; Conquering
stopped ;; Process_end
speaking ;; Statement || go ;; Motion
siege ;; Besieging || combined ;; Cause_to_amalgamate || failed ;; Earnings_and_losses
became ;; Becoming || surrounded ;; Surrounding || held ;; Hold || turned ;; Change
support ;; Supporting || targeting ;; Aiming
held ;; Defending
allowing ;; Preventing_or_letting || hosts ;; Come_together
conference ;; Social_event || use ;; Using || utilized ;; Using
conference ;; Social_event || fight ;; Hostile_encounter
conference ;; Social_event || held ;; Defending
supplying ;; Supply || speaking ;; Telling || feat ;; Achieve || structure ;; Building
named ;; Name_conferral
permitted ;; Preventing_or_letting || running ;; Self_motion
set ;; Placing || drowning ;; Death || rang ;; Surrounding
conference ;; Social_event || organization ;; Arranging || run ;; Self_motion
restored ;; Recovering || ousted ;; Change_of_leadership
aided ;; Assistance || elected ;; Change_of_leadership
began ;; Process_start
marched ;; Self_motion
confronted ;; Hostile_encounter
dead ;; Death
demanded ;; Request || started ;; Process_start || resign ;; Change_of_leadership
refused ;; Agree_or_refuse_to_act || arrested ;; Arrest
request ;; Request || denied ;; Agree_or_refuse_to_act || ordered ;; Request
declared ;; Expressing_publicly
declared ;; Expressing_publicly || dissolved ;; Removing
refused ;; Agree_or_refuse_to_act
began ;; Process_start || surrounded ;; Surrounding || spread ;; Dispersal
demanded ;; Request || seized ;; Conquering
resigned ;; Change_of_leadership || went ;; Motion || exile ;; Escaping
leading to ;; Causation || firing ;; Use_firearm || removal ;; Removing || re-installation ;; Recovering || retook ;; Control
planned ;; Arranging || felt ;; Change_sentiment || threatened ;; Influence
drop ;; Cause_change_of_position_on_a_scale
resign ;; Change_of_leadership || led ;; Causation || call ;; Request
overthrow ;; Removing || denied ;; Statement || ties ;; Connect
described ;; Statement || reporting ;; Reporting || accused ;; Judgment_communication
participated ;; Participation
traveled ;; Traveling || broke out ;; Process_start || accused ;; Judgment_communication
named ;; Name_conferral || records ;; Recording || revolt ;; Change_of_leadership || social ;; Social_event
encompassed ;; Surrounding
organised ;; Arranging
attacked ;; Attack || destroyed ;; Destroying || forced ;; Hostile_encounter
organizing ;; Arranging
killing ;; Killing
pushed ;; Motion || take ;; Creating || concerns ;; Influence
protested ;; Attack
party ;; Social_event || organizations ;; Collaboration
war ;; Hostile_encounter || used ;; Using || organize ;; Arranging
employed ;; Employment || utilizing ;; Using
taken ;; Creating
raise ;; Cause_change_of_position_on_a_scale || battle ;; Hostile_encounter
controlled ;; Control || surrounding ;; Surrounding
controlled ;; Control || went ;; Self_motion
employed ;; Employment || surrounding ;; Surrounding
marching ;; Self_motion || aware ;; Know
provisioned ;; Supply || siege ;; Besieging
controlling ;; Control || moving ;; Motion || aware ;; Know
went to ;; Self_motion
used ;; Using || get ;; Getting
surrounded ;; Surrounding || enclosed ;; Placing || changed ;; Change
driven ;; Motion
fought ;; Hostile_encounter || Battle ;; Hostile_encounter || battle ;; Hostile_encounter || command ;; Control
took ;; Conquering || successes ;; Achieve || left ;; Departing || storming ;; Attack
siege ;; Besieging || laid ;; Placing || forces ;; Hostile_encounter || beat ;; Bodily_harm
beginning ;; Process_start || retreat ;; Escaping || surprised ;; Change_sentiment
retreat ;; Escaping || march ;; Self_motion || overtaking ;; Change_of_leadership || pursued ;; Aiming || forces ;; Military_operation || rallied ;; Military_operation
reacted ;; Response || attack ;; Attack || leaving ;; Departing || capturing ;; Conquering
driven ;; Motion || attacks ;; Attack || casualties ;; Catastrophe || retreat ;; Escaping || counter-attack ;; Attack || encircled ;; Surrounding || rallying ;; Military_operation || pushed ;; Motion || led to ;; Causation
send ;; Sending || retreat ;; Escaping || attacked ;; Attack || slowing ;; Cause_change_of_position_on_a_scale || counter-attack ;; Attack || marching ;; Self_motion || forced ;; Causation
splitting ;; Destroying || defeat ;; Conquering || allowing ;; Preventing_or_letting || encircle ;; Besieging || pass ;; Motion_directional
halt ;; Process_end || press ;; Motion || moved ;; Motion
ended ;; Process_end || disengaged ;; Escaping || battle ;; Hostile_encounter || exhausted ;; Expend_resource
retreat ;; Escaping || allow ;; Preventing_or_letting || pass ;; Motion_directional
include ;; Cause_to_be_included || countering ;; Attack || driving ;; Motion || defeat ;; Conquering || infantry ;; Hostile_encounter || compensated ;; Supply
marking ;; Recording || studied ;; Education_teaching || covenant ;; Commitment || battle ;; Hostile_encounter || advance ;; Cause_to_make_progress || signing ;; Sign_agreement || cause ;; Causation || consider ;; Statement || led to ;; Causation || brought ;; Bringing || leading to ;; Causation || casualties ;; Death
conducted ;; Action || exploring ;; Scrutiny || surrounding ;; Surrounding || surveying ;; Scrutiny
requested ;; Request
created ;; Creating || authorized ;; Preventing_or_letting
called ;; Name_conferral
killed ;; Killing || conflict ;; Hostile_encounter
event ;; Social_event || produced ;; Creating
took place ;; Process_start
started ;; Process_start || event ;; Social_event || match ;; Competition || match ;; Competition || battle ;; Hostile_encounter || joined ;; Participation
competed ;; Competition || competitors ;; Competition || ring ;; Surrounding || eliminate ;; Removing
match ;; Competition || awarded ;; Giving
defended ;; Defending || retained ;; Getting
battle ;; Hostile_encounter || took place ;; Process_start
battles ;; Hostile_encounter
surround ;; Surrounding
forced ;; Motion || supplies ;; Supply || due ;; Causation || capitulate ;; Response
captured ;; Conquering || killed ;; Killing
escaped ;; Escaping
expelled ;; Removing
took place ;; Process_start || War ;; Hostile_encounter || called ;; Request || forces ;; Hostile_encounter || joint ;; Collaboration
possessing ;; Getting
dismantled ;; Destroying || relocated ;; Removing
surrounding ;; Surrounding
surrendered ;; Surrendering || refused ;; Agree_or_refuse_to_act || order ;; Arranging || relocate ;; Removing || control ;; Control
remove ;; Removing
fought ;; Quarreling || combined ;; Cause_to_amalgamate || battle ;; Hostile_encounter
resulted in ;; Causation || suffered ;; Bodily_harm || deaths ;; Death || death ;; Death
battle ;; Hostile_encounter || joint ;; Collaboration || occupied ;; Control
regaining ;; Recovering || control ;; Control || surrounding ;; Surrounding
fought ;; Hostile_encounter || War ;; Hostile_encounter
Battle ;; Hostile_encounter || advancing ;; Cause_to_make_progress || withdrawing ;; Escaping || followed ;; Motion
assisted ;; Assistance
saving ;; Rescuing || fire ;; Catastrophe || awarded ;; Giving
surrounded ;; Surrounding || destroyed ;; Destroying
set up ;; Military_operation || commanded ;; Control || force ;; Military_operation
given ;; Giving || penetrating ;; Attack
surrounds ;; Surrounding || taken captive ;; Conquering
killed ;; Killing || made it ;; Causation || raid ;; Attack || failure ;; Earnings_and_losses || taken ;; Conquering
lost ;; Earnings_and_losses
support ;; Supporting || Damage ;; Damaging || concert ;; Social_event
tour ;; Traveling
began ;; Process_start || ended ;; Process_end
supported ;; Supporting || support ;; Supporting || Armored ;; Hostile_encounter || headlined ;; Statement
skateboarding ;; Social_event || resulting in ;; Causation || accident ;; Catastrophe || filled ;; Filling
discussed ;; Communication || accident ;; Catastrophe || set ;; Placing || death ;; Death
postponed ;; Change_event_time || rest ;; Placing || Performances ;; Social_event || scheduled ;; Arranging || hired ;; Employment || complete ;; Arranging
became ;; Becoming || concerts ;; Social_event
causing ;; Causation || damage ;; Damaging
increased ;; Cause_change_of_position_on_a_scale || loss ;; Earnings_and_losses || revenue ;; Earnings_and_losses
formed ;; Coming_to_be || hurricanes ;; Catastrophe || hit ;; Attack
began ;; Process_start || moved ;; Motion || strengthen ;; Cause_change_of_strength || change ;; Change || turning ;; Becoming
reached ;; Arriving || making it ;; Causation
weakening ;; Cause_change_of_strength || storm ;; Catastrophe || veering ;; Motion || struck ;; Motion
moving ;; Motion || noted ;; Know || destruction ;; Destroying || decreased ;; Cause_change_of_position_on_a_scale || weakened ;; Cause_change_of_strength
helped ;; Assistance || storm ;; Catastrophe || record ;; Recording || produce ;; Creating || turned ;; Becoming
emerged ;; Coming_to_be || becoming ;; Becoming || dissipating ;; Dispersal
damage ;; Damaging || flooding ;; Motion || affected ;; Influence || suffered ;; Bodily_harm
storm ;; Catastrophe || lift ;; Body_movement || weakened ;; Cause_change_of_strength || maintained ;; Containing || resulted in ;; Causation
affected ;; Influence
flooding ;; Motion
causing ;; Causation || floods ;; Motion || record ;; Recording || produced ;; Manufacturing
destroyed ;; Destroying || Rushing ;; Self_motion || demolished ;; Damaging || breached ;; Damaging
killing ;; Killing || submerged ;; Destroying
killed ;; Killing || caused ;; Causation || disaster ;; Catastrophe || damage ;; Damaging
built ;; Building || destroying ;; Destroying || flooding ;; Motion || evacuate ;; Escaping || spread ;; Dispersal || forcing ;; Causation
storm ;; Catastrophe || damage ;; Damaging
Damage ;; Damaging || peaked ;; Cause_change_of_position_on_a_scale
destroying ;; Destroying || record ;; Recording || produced ;; Manufacturing || split ;; Destroying || cutting ;; Removing
record ;; Recording || flooded ;; Filling || reported ;; Reporting
reached ;; Arriving || record ;; Recording
destroyed ;; Destroying || rebuilt ;; Recovering
damaged ;; Damaging || flooded ;; Motion
flood ;; Catastrophe || surpassed ;; Cause_change_of_position_on_a_scale
damaged ;; Damaging || destroyed ;; Destroying || injured ;; Bodily_harm
killed ;; Killing || destroyed ;; Destroying || damaged ;; Damaging
declared ;; Expressing_publicly || hurricane ;; Catastrophe
released ;; Publishing
distributed ;; Dispersal || replaced ;; Change_of_leadership
made ;; Creating
promoted ;; Cause_to_make_progress
sent ;; Sending || saw ;; Self_motion
debuted ;; Process_start
criticized ;; Judgment_communication
accused ;; Judgment_communication || sprayed ;; Motion || filled ;; Filling
banned ;; Preventing_or_letting
responded ;; Response || suing ;; Legality
certified ;; Check
flying ;; Motion || crashed ;; Destroying
fatalities ;; Death
injuries ;; Bodily_harm || serious ;; Bodily_harm
loss ;; Earnings_and_losses || written ;; Writing
accident ;; Catastrophe || investigated ;; Check || report ;; Reporting || issued ;; Statement
blamed ;; Judgment_communication || accident ;; Catastrophe || cause ;; Causation || clogging ;; Filling
demanded ;; Request || final ;; Process_end || approach ;; Arriving || flow ;; Motion
identified ;; Check || mandated ;; Arranging || affected ;; Influence || developed ;; Coming_to_be || fitted ;; Change
flooding ;; Catastrophe || occurred ;; Presence
began ;; Process_start || triggered ;; Causation || storm ;; Catastrophe || flooding ;; Catastrophe || landfall ;; Arriving
spread ;; Dispersal || floods ;; Catastrophe
reached ;; Arriving || inundated ;; Filling
resulted in ;; Causation || deaths ;; Death || Flooding ;; Catastrophe || affected ;; Influence
damaged ;; Damaging || declared ;; Expressing_publicly
described ;; Statement || disaster ;; Catastrophe || flooding ;; Catastrophe || affected ;; Influence
damages ;; Damaging || losses ;; Earnings_and_losses || estimated ;; Statement || flooding ;; Catastrophe
floods ;; Catastrophe || inundated ;; Filling
production ;; Manufacturing || caused ;; Causation || shortage ;; Request || Disruptions ;; Damaging || affected ;; Influence
disaster ;; Catastrophe || disaster ;; Catastrophe || earthquake ;; Catastrophe || tsunami ;; Catastrophe || earthquake ;; Catastrophe || fires ;; Catastrophe || tsunami ;; Catastrophe || Hurricane ;; Catastrophe
suggests ;; Convincing || increasing ;; Cause_change_of_position_on_a_scale || flooding ;; Catastrophe || occur ;; Presence
floods ;; Catastrophe
Hurricane ;; Catastrophe
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
developed ;; Coming_to_be || storm ;; Catastrophe || skipping ;; Self_motion
remained ;; Temporary_stay || intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe
attained ;; Achieve
drifted ;; Motion || weakened ;; Cause_change_of_strength
storm ;; Catastrophe || made ;; Arriving
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || impacted ;; Influence || reported ;; Reporting
fallen ;; Motion
became ;; Becoming || inundated ;; Motion || flooded ;; Filling
storm ;; Catastrophe || brought ;; Bringing
Flooding ;; Motion
inundated ;; Filling
Damage ;; Damaging || deaths ;; Death || reached ;; Earnings_and_losses || reported ;; Reporting
caused ;; Causation
named ;; Name_conferral || developed ;; Cause_to_make_progress
became ;; Becoming || intensified ;; Cause_change_of_strength
caused ;; Causation || strengthen ;; Cause_change_of_strength
made ;; Creating
dissipated ;; Removing || weakened ;; Cause_change_of_strength
produced ;; Creating
filled ;; Filling || evacuate ;; Emptying
affected ;; Influence
reported ;; Reporting
brought ;; Bringing
produced ;; Creating
caused ;; Causation || impact ;; Influence
named ;; Name_conferral || storm ;; Catastrophe || hurricane ;; Catastrophe || developed ;; Cause_change_of_strength
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe
intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe || hurricane ;; Catastrophe
weakened ;; Cause_change_of_strength || hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || hurricane ;; Catastrophe
began ;; Process_start || curved ;; Motion_directional || weakening ;; Cause_change_of_strength
turned ;; Becoming || storm ;; Catastrophe
moved ;; Motion || veered ;; Motion
became ;; Becoming || centered ;; Placing
storm ;; Catastrophe || produced ;; Creating
reported ;; Reporting || flooding ;; Motion
flooded ;; Motion || inundated ;; Filling
occurred ;; Coming_to_be || struck ;; Attack || death ;; Death
effected ;; Influence
named ;; Name_conferral || due ;; Causation || sank ;; Motion_directional || overloading ;; Filling
present ;; Presence || sank ;; Motion_directional
died ;; Death
disaster ;; Catastrophe
disaster ;; Catastrophe || rule ;; Control || put ;; Self_motion
breakup ;; Destroying || regaining ;; Recovering || dedicated ;; Giving || victims ;; Catastrophe
floods ;; Catastrophe || affected ;; Influence || took place ;; Process_start
affected ;; Influence
flood ;; Catastrophe || floods ;; Catastrophe
damage ;; Damaging || brought ;; Causation || flooding ;; Catastrophe
flooded ;; Catastrophe || feeds ;; Filling || continued ;; Motion || rose ;; Cause_change_of_position_on_a_scale || affected ;; Influence
floods ;; Catastrophe || increase ;; Cause_change_of_position_on_a_scale || anticipated ;; Coming_to_believe
Championship ;; Competition || took place ;; Process_start
games ;; Competition || played ;; Participation
accommodates ;; Filling || renovated ;; Recovering
expanded ;; Expansion || capacity ;; Filling
gained ;; Getting || host ;; Social_event || Championship ;; Competition
tournament ;; Competition || named ;; Name_conferral
championship ;; Competition || final ;; Competition || winning ;; Earnings_and_losses || defeating ;; Hostile_encounter
tournament ;; Competition || named ;; Name_conferral
final ;; Competition || watched ;; Perception_active
earthquake ;; Catastrophe || earthquake ;; Catastrophe || occurred ;; Presence
earthquake ;; Catastrophe
earthquake ;; Catastrophe || recorded ;; Recording || record ;; Recording || surpassed ;; Cause_change_of_position_on_a_scale
record ;; Recording || earthquake ;; Catastrophe
quake ;; Catastrophe || epicenter ;; Catastrophe
quakes ;; Catastrophe || quake ;; Catastrophe || followed ;; Motion || foreshock ;; Attack
quake ;; Catastrophe
aftershocks ;; Catastrophe || detected ;; Know || quake ;; Catastrophe
earthquake ;; Catastrophe || cause ;; Causation || concluded ;; Statement || drilling ;; Action || injecting ;; Filling
earthquake ;; Catastrophe || reported ;; Reporting || undergone ;; Bodily_harm || seismic ;; Catastrophe || increase ;; Cause_change_of_position_on_a_scale
earthquakes ;; Catastrophe
earthquakes ;; Catastrophe
linked ;; Connect || Earthquake ;; Catastrophe || injection ;; Filling || studies ;; Scrutiny || increased ;; Cause_change_of_position_on_a_scale
took place ;; Process_start
battle ;; Hostile_encounter || started ;; Process_start || attack ;; Attack || sneak ;; Self_motion
found ;; Know
dripping ;; Motion || waded ;; Filling || failed ;; Earnings_and_losses
forced ;; Motion
crossing ;; Motion_directional || went to ;; Motion || chased ;; Motion
carried out ;; Action
find ;; Know
carried out ;; Action
determine ;; Deciding
chosen ;; Choosing || known ;; Know || held ;; Hold
placed ;; Placing || detonated ;; Terrorism || backfilled ;; Filling || gathered ;; Come_together
caused ;; Causation || took place ;; Process_start
produced ;; Creating
tackled ;; Resolve_problem
provided ;; Supply || installed ;; Placing
allowed ;; Preventing_or_letting
leads ;; Causation
conducted ;; Action
seen ;; Perception_active
assassination ;; Killing || started ;; Process_start || riots ;; Violence
represents ;; Presence
killed ;; Killing || attended ;; Participation || funeral ;; Social_event
violence ;; Violence || killed ;; Killing || engulfed ;; Filling || injured ;; Bodily_harm
losses ;; Earnings_and_losses || riots ;; Violence || estimated ;; Cause_change_of_position_on_a_scale
called ;; Name_conferral || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || fought ;; Hostile_encounter
existence ;; Presence || due ;; Causation || battle ;; Hostile_encounter
covering ;; Filling
occurred ;; Coming_to_be || Battle ;; Hostile_encounter
raids ;; Attack || established ;; Building
verify ;; Check || hindered ;; Hindering || report ;; Reporting || battle ;; Hostile_encounter
lashed ;; Connect
formed ;; Coming_to_be
moved ;; Motion || strengthened ;; Cause_change_of_strength
made it ;; Causation
weakened ;; Cause_change_of_strength || passed ;; Process_end
became ;; Becoming || moving ;; Motion
prompted ;; Causation || warnings ;; Warning || prompted ;; Causation
Damage ;; Destroying
caused ;; Causation || washed ;; Removing || damaged ;; Damaging
damaged ;; Damaging
Damage ;; Damaging || deaths ;; Death
extended ;; Cause_change_of_position_on_a_scale
covered ;; Filling
sank ;; Self_motion || presumed ;; Convincing
debate ;; Communication
starting ;; Process_start || viewing ;; Perception_active || took place ;; Process_start
agree ;; Agree_or_refuse_to_act
given ;; Giving || debate ;; Communication
debate ;; Communication || debate ;; Communication || answered ;; Response || broken up ;; Destroying || followed ;; Self_motion
built ;; Building || use ;; Using || debate ;; Communication || laced ;; Filling
operation ;; Military_operation || launched ;; Process_start || invasion ;; Attack || Expedition ;; Traveling
refusing ;; Agree_or_refuse_to_act || victory ;; Earnings_and_losses || grew ;; Cause_change_of_position_on_a_scale || showing ;; Communication || partiality ;; Filling || reinstated ;; Change_of_leadership || War ;; Military_operation || War ;; Military_operation
organized ;; Arranging || entered ;; Arriving || defeated ;; Conquering || occupied ;; Control || command ;; Control
fled ;; Escaping || broken ;; Destroying || negotiate ;; Communication || surrender ;; Surrendering
sent ;; Sending || killed ;; Killing || location ;; Placing || becoming ;; Becoming
independence ;; Change_of_leadership || annexed ;; Becoming_a_member
engagement ;; Hostile_encounter || occurred ;; Presence
campaigning ;; Military_operation || hindered ;; Hindering || forces ;; Military_operation
allow ;; Preventing_or_letting || dominate ;; Control || occupy ;; Control
fill ;; Filling || served ;; Giving || forces ;; Military_operation || taken ;; Giving
held ;; Defending || loss ;; Earnings_and_losses || supply ;; Supply
left ;; Departing || reconquer ;; Conquering
traveled ;; Motion
met ;; Come_together
surpassed ;; Expansion
fought ;; Hostile_encounter || formed ;; Coming_to_be
destruction ;; Destroying || forces ;; Military_operation || capitulation ;; Surrendering || accelerated ;; Cause_change_of_position_on_a_scale
abandoned ;; GiveUp
formed ;; Coming_to_be || became ;; Becoming || build ;; Building || make ;; Causation || organization ;; Collaboration || triumphed ;; Achieve
came ;; Arriving
assistance ;; Assistance || help ;; Assistance || help ;; Assistance
forces ;; Hostile_encounter || used ;; Using
participated ;; Participation || participated ;; Participation || formed ;; Coming_to_be
following ;; Self_motion || war ;; Hostile_encounter || joined ;; Participation || instigating ;; Convincing
support ;; Supporting
covered ;; Filling
ended ;; Process_end || war ;; Hostile_encounter || withdrew ;; Departing
resulted in ;; Causation || conference ;; Social_event || march ;; Self_motion || attended ;; Participation
accepted ;; Receiving || signed ;; Sign_agreement
known ;; Know || killing ;; Killing || storm ;; Catastrophe
become ;; Becoming || Forming ;; Coming_to_be || tracked ;; Self_motion
turning ;; Becoming || intensified ;; Cause_change_of_strength || approached ;; Arriving || storm ;; Catastrophe || response ;; Response
moving ;; Motion || attained ;; Getting
moving ;; Motion || weakened ;; Cause_change_of_strength || emerging ;; Coming_to_be || retaining ;; Preserving
succumbed ;; Death || degraded ;; Cause_change_of_position_on_a_scale
landfall ;; Self_motion || dissipating ;; Removing
passing ;; Motion_directional || resulted in ;; Causation
received ;; Receiving || brought ;; Bringing
falling ;; Motion_directional || took place ;; Process_start
creating ;; Creating
rushed ;; Motion || overwhelmed ;; Destroying
suffered ;; Damaging
devastated ;; Destroying || damaged ;; Damaging || destroyed ;; Destroying
perishing ;; Death || killed ;; Killing
killed ;; Killing
lost ;; Earnings_and_losses || presumed ;; Coming_to_believe
killed ;; Killing || killed ;; Killing || surpassing ;; Cause_change_of_position_on_a_scale || surpassed ;; Cause_change_of_position_on_a_scale
damaged ;; Damaging || destroyed ;; Destroying
amounted ;; Earnings_and_losses
took over ;; Conquering || reach ;; Arriving
aid ;; Assistance || flowed ;; Motion || established ;; Creating
provided ;; Supply
held ;; Hold || Competition ;; Competition || tournament ;; Competition || took place ;; Process_start
played ;; Competition
tournament ;; Competition || competed ;; Competition || followed ;; Motion || retained ;; Preserving
tournament ;; Competition
shared ;; Collaboration
final ;; Competition || finish ;; Process_end || preceded ;; Process_start || victory ;; Getting || win ;; Conquering || wins ;; Conquering || sealed ;; Getting || beating ;; Competition
became ;; Becoming || affect ;; Influence || cyclone ;; Catastrophe
named ;; Name_conferral
developed ;; Coming_to_be || drifted ;; Motion || storm ;; Catastrophe
began ;; Process_start || accelerate ;; Cause_change_of_position_on_a_scale
struggled ;; Hostile_encounter || make ;; Manufacturing || dry ;; Preserving || passed ;; Motion_directional
intensified ;; Cause_change_of_strength || landfall ;; Self_motion
produced ;; Creating || presented ;; Presence
took place ;; Process_start
matches ;; Competition
destruction ;; Destroying || match ;; Competition
become ;; Becoming
scored ;; Getting || awarded ;; Giving
gaining ;; Getting || hit ;; Bodily_harm || retaining ;; Preserving
included ;; Cause_to_be_included || matches ;; Competition
fought ;; Hostile_encounter || Battle ;; Military_operation || battle ;; Military_operation || control ;; Control
control ;; Control || gained ;; Getting || bombarding ;; Attack
surrendered ;; Surrendering
destroyed ;; Destroying || claimed ;; Statement || used ;; Using || evacuated ;; Rescuing
assault ;; Hostile_encounter || eliminate ;; Removing
maintain ;; Preserving || successes ;; Earnings_and_losses
Wars ;; Hostile_encounter || fought ;; Hostile_encounter || fought ;; Hostile_encounter
viewed ;; Perception_active || War ;; Hostile_encounter
control ;; Control || War ;; Hostile_encounter || fought ;; Hostile_encounter
War ;; Hostile_encounter || War ;; Hostile_encounter
ended ;; Process_end || fought ;; Hostile_encounter
retaining ;; Preserving || ceded ;; Surrendering
included ;; Cause_to_be_included || setting ;; Placing || conflicts ;; Hostile_encounter
fought ;; Hostile_encounter || supported ;; Supporting || consisting ;; Cause_to_be_included
ravaged ;; Destroying || seize ;; Getting || maintain ;; Preserving
launched ;; Process_start || aimed ;; Aiming
aided ;; Assistance || provided ;; Supply
abandoned ;; GiveUp || join ;; Becoming_a_member || collapsed ;; Destroying
developing ;; Coming_to_be || escalated ;; Cause_change_of_position_on_a_scale
sought ;; Getting || rebellion ;; Change_of_leadership
received ;; Receiving || opposed ;; Agree_or_refuse_to_act
drawn ;; Motion
regarded ;; Know || spurred ;; Causation || joined ;; Becoming_a_member
decided ;; Deciding || reestablish ;; Recovering
sent ;; Sending || negotiate ;; Communication
made ;; Manufacturing
abandoned ;; GiveUp || regarding ;; Know || sided ;; Placing
refused ;; Agree_or_refuse_to_act || declared ;; Expressing_publicly || begin ;; Process_start
declined ;; Agree_or_refuse_to_act
taking ;; Conquering || advanced ;; Cause_to_make_progress
left ;; Departing
defeat ;; Conquering || capitulated ;; Surrendering
regarded ;; Know || led to ;; Causation
conflict ;; Hostile_encounter || following ;; Self_motion
invaded ;; Attack || defeated ;; Conquering || lost ;; Earnings_and_losses || reversed ;; Change || recapturing ;; Conquering
followed ;; Self_motion
confirmed ;; Check || established ;; Creating
disputes ;; Quarreling
occupied ;; Hold
resisted ;; Defending
maintained ;; Preserving
caused ;; Causation || damage ;; Damaging || cyclone ;; Catastrophe
storm ;; Catastrophe
originated ;; Coming_to_be
gained ;; Getting || reached ;; Arriving
began ;; Process_start || ensued ;; Coming_to_be || attaining ;; Getting
began ;; Process_start || began ;; Process_start || maintained ;; Preserving
weakened ;; Cause_change_of_strength || landfall ;; Self_motion || tracking ;; Self_motion
transitioned ;; Change || dissipated ;; Dispersal
caused ;; Causation || damage ;; Damaging
affected ;; Influence
damaged ;; Damaging || destroyed ;; Destroying
storm ;; Catastrophe || resulted ;; Causation
amounted ;; Cause_change_of_position_on_a_scale || injured ;; Bodily_harm
occurred ;; Coming_to_be || damage ;; Damaging
declared ;; Expressing_publicly || declared ;; Expressing_publicly
killed ;; Killing || occurred ;; Coming_to_be || damage ;; Damaging || reached ;; Arriving
record ;; Recording || remains ;; Presence
destroyed ;; Destroying || effects ;; Influence || inundated ;; Motion
landfall ;; Self_motion || concentrated ;; Aiming
peaked ;; Cause_change_of_position_on_a_scale
participated ;; Participation
retained ;; Preserving || beating ;; Competition
played ;; Competition
played ;; Competition
played ;; Competition
placed ;; Placing
competed ;; Competition || competed ;; Competition
defeated ;; Conquering || take ;; Earnings_and_losses
took ;; Earnings_and_losses || earned ;; Earnings_and_losses
retain ;; Preserving || face ;; Hostile_encounter
beat ;; Competition
match ;; Competition || Final ;; Competition || contested ;; Competition
held ;; Hold || final ;; Competition
match ;; Competition || final ;; Competition
preventing ;; Preventing_or_letting || match ;; Competition || retaining ;; Preserving
goalless ;; Process_end
opened ;; Process_start || scoring ;; Hold || scored ;; Getting
looked ;; Change_sentiment || game ;; Competition || shoot-out ;; Hostile_encounter || went ;; Motion
win ;; Earnings_and_losses || scored ;; Getting || securing ;; Getting
decide ;; Deciding || tournament ;; Competition
Championship ;; Competition || maintained ;; Hold || joining ;; Participation
staged ;; Hold || relinquished ;; Surrendering || host ;; Social_event
retaining ;; Preserving || gaining ;; Getting || tournament ;; Competition
gained ;; Getting || finishing ;; Process_end || tournament ;; Competition || qualifying ;; Getting
lose ;; Earnings_and_losses || fail ;; Earnings_and_losses || associate ;; Collaboration || qualify ;; Getting || qualify ;; Getting || tournament ;; Competition
race ;; Competition
meeting ;; Social_event || concluded ;; Deciding || took place ;; Process_start
competed ;; Competition || permitted ;; Preventing_or_letting || record ;; Recording
sponsored ;; Supply
retains ;; Preserving || race ;; Competition || broadcast ;; Expressing_publicly || coverage ;; Social_event || held ;; Hold
win ;; Getting || race ;; Competition || become ;; Becoming
completed ;; Process_end || returned ;; Arriving
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter
proclaimed ;; Statement || attacked ;; Attack
attackers ;; Attack || coming ;; Arriving || seized ;; Conquering
maintain ;; Preserving
hold ;; Defending || left ;; Causation || force ;; Request
break out ;; Process_start
surrendered ;; Surrendering || command ;; Control || turned ;; Becoming
took place ;; Process_start || wrestling ;; Competition || produced ;; Manufacturing
wrestling ;; Competition
defended ;; Defending || match ;; Competition
gaining ;; Getting || winning ;; Getting
defended ;; Defending || match ;; Competition
pinned ;; Connect || retain ;; Preserving
remain ;; Presence || storm ;; Catastrophe || held ;; Defending || defeated ;; Conquering
match ;; Competition || defeated ;; Conquering
received ;; Receiving || received ;; Receiving || match ;; Competition
ranked ;; Placing
stating ;; Statement || wrestling ;; Competition
became ;; Becoming || demand ;; Request || launch ;; Motion
cyclone ;; Catastrophe || struck ;; Attack
maintained ;; Preserving || storm ;; Catastrophe || developed ;; Expansion
reached ;; Arriving || attained ;; Getting || made ;; Creating || landfall ;; Catastrophe || dissipated ;; Cause_change_of_position_on_a_scale
cyclones ;; Catastrophe || record ;; Recording || approach ;; Self_motion
brought ;; Bringing || rainfall ;; Catastrophe || causing ;; Causation || flooding ;; Catastrophe || creating ;; Creating || storm ;; Catastrophe
drowned ;; Killing || swept ;; Removing
caused ;; Causation || storm ;; Catastrophe
took place ;; Process_start || Battle ;; Hostile_encounter
fought ;; Hostile_encounter || Wars ;; Hostile_encounter
surrounded ;; Besieging
defended ;; Defending || led ;; Bringing || contained ;; Preserving
relief ;; Rescuing || felt ;; Perception_active || coming to ;; Motion || combining ;; Cause_to_be_included || warrant ;; Arranging
consequences ;; Influence || battle ;; Hostile_encounter || victory ;; Earnings_and_losses
took place ;; Process_start
contested ;; Competition
defeated ;; Conquering
included ;; Cause_to_be_included
defeated ;; Conquering
unified ;; Cause_to_amalgamate || retained ;; Preserving
became ;; Becoming
criticized ;; Judgment_communication
installment ;; Arranging || festival ;; Social_event
held ;; Defending
sale ;; Commerce_sell
announced ;; Expressing_publicly || sold ;; Commerce_sell
Awards ;; Social_event
achieved ;; Achieve || award ;; Award
established ;; Building || Dancing ;; Social_event || retained ;; Preserving
present ;; Presence || include ;; Cause_to_be_included
marked ;; Recording || launch ;; Process_start
broadcast ;; Expressing_publicly || kept ;; Temporary_stay
masquerade ;; Social_event || come ;; Motion || dancing ;; Social_event || loose ;; Earnings_and_losses || party ;; Social_event || sponsored ;; Supply
battle ;; Hostile_encounter || fleet ;; Military_operation || fleet ;; Military_operation || took place ;; Process_start
began ;; Process_start || retreated ;; Departing
battle ;; Hostile_encounter || drowning ;; Killing || storm ;; Catastrophe || sank ;; Motion_directional || anchored ;; Connect
returned ;; Arriving || damage ;; Damaging
discovered ;; Know || headed ;; Self_motion
decided ;; Deciding || battle ;; Hostile_encounter || battle ;; Hostile_encounter || started ;; Process_start || avoid ;; Preventing_or_letting || turned ;; Becoming
losses ;; Earnings_and_losses || death ;; Death || pursuit ;; Self_motion || anchored ;; Connect || bury ;; Placing || affect ;; Influence
storm ;; Catastrophe || broke out ;; Process_start
losses ;; Earnings_and_losses || fell ;; Motion_directional || flee ;; Escaping || survived ;; Preserving
drowned ;; Killing || surviving ;; Earnings_and_losses || smashed ;; Destroying || thrown ;; Self_motion
battle ;; Hostile_encounter || order ;; Arranging
Battle ;; Hostile_encounter || fought ;; Hostile_encounter || Battle ;; Hostile_encounter || engagements ;; Hostile_encounter
fought ;; Hostile_encounter
captured ;; Conquering || battle ;; Hostile_encounter || held ;; Defending || casualties ;; Death
preserved ;; Preserving
captured ;; Conquering || battle ;; Hostile_encounter || end ;; Process_end || relief ;; Assistance
counter-attack ;; Attack || regained ;; Recovering || attacks ;; Attack || battles ;; Hostile_encounter
reached ;; Arriving
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe
became ;; Becoming || intensified ;; Cause_change_of_strength || storm ;; Catastrophe || tracking ;; Motion
Moving ;; Motion || strengthen ;; Cause_change_of_strength || reaching ;; Arriving
weakening ;; Cause_change_of_strength || reached ;; Arriving
Located ;; Placing || produced ;; Manufacturing
damage ;; Damaging || occurred ;; Presence
began ;; Process_start || re-intensify ;; Cause_change_of_strength || curving ;; Motion_directional || storm ;; Catastrophe
hurricane ;; Catastrophe
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || re-curved ;; Self_motion
drowned ;; Killing || sank ;; Motion
damage ;; Damaging || pelted ;; Body_movement || occurred ;; Presence
maintained ;; Preserving || tracking ;; Motion
weakened ;; Cause_change_of_strength || became ;; Becoming
deaths ;; Death || occurred ;; Presence
death ;; Death || succumbed ;; Death || injuries ;; Bodily_harm || storm ;; Catastrophe || occurred ;; Presence
wrestling ;; Competition || produced ;; Creating
took place ;; Process_start
combined ;; Cause_to_amalgamate || wrestling ;; Competition
planned ;; Arranging || took place ;; Process_start
assigned ;; Choosing
retained ;; Preserving
match ;; Competition
match ;; Competition
existing ;; Presence || settled ;; Resolve_problem
drafted ;; Choosing
concluded ;; Process_end || entered ;; Arriving || ending ;; Process_end
led to ;; Causation
hurricane ;; Catastrophe || hit ;; Attack
developed ;; Coming_to_be || observed ;; Know
hurricane ;; Catastrophe || maintained ;; Preserving || reached ;; Achieve
causing ;; Causation || storm ;; Catastrophe || landfall ;; Arriving || flooding ;; Catastrophe || made ;; Bringing
caused ;; Causation || hurricane ;; Catastrophe || damage ;; Damaging || killed ;; Killing
Impact ;; Influence || recorded ;; Recording || storm ;; Catastrophe
damage ;; Damaging || occurred ;; Presence
destroyed ;; Destroying || damage ;; Damaging
moving ;; Motion || weakened ;; Cause_change_of_strength || hurricane ;; Catastrophe || reaching ;; Arriving || traversed ;; Motion_directional || continued ;; Motion || turning ;; Motion || observed ;; Know
wrestling ;; Competition || produced ;; Creating || took place ;; Process_start
schedule ;; Arranging
involved ;; Cause_to_be_included
storm ;; Catastrophe
defended ;; Defending
defeated ;; Conquering || retain ;; Preserving
held ;; Defending
contest ;; Competition
defeated ;; Conquering || determine ;; Deciding || match ;; Competition || match ;; Competition || took place ;; Process_start
debut ;; Process_start
dedicated ;; Giving
debut ;; Process_start || ranked ;; Placing
rated ;; Reporting || wrestling ;; Competition
became ;; Becoming || Wrestling ;; Competition || demand ;; Request
fought ;; Hostile_encounter
become ;; Becoming
opposed ;; Agree_or_refuse_to_act
began ;; Process_start
reorganised ;; Cause_to_amalgamate
retained ;; Preserving
opposed ;; Agree_or_refuse_to_act
opened ;; Openness
led to ;; Causation
responded ;; Response
fled ;; Escaping
produced ;; Manufacturing || wrestling ;; Competition
took place ;; Process_start
retain ;; Preserving
defended ;; Defending
defeated ;; Conquering || become ;; Becoming
fought ;; Hostile_encounter
held ;; Hold
contest ;; Competition
defeated ;; Conquering
announcement ;; Expressing_publicly || win ;; Getting || remembered ;; Change_sentiment
became ;; Becoming || launch ;; Process_start
occurred ;; Coming_to_be || revolution ;; Change_of_leadership || led to ;; Causation || overthrow ;; Change_of_leadership
granted ;; Giving || consisting ;; Cause_to_be_included
existence ;; Presence || preceding ;; Process_start || retaining ;; Preserving || elections ;; Choosing
election ;; Choosing || party ;; Social_event || party ;; Social_event || frustrated ;; Hindering || allied ;; Collaboration
overthrew ;; Change_of_leadership || proceeded ;; Self_motion
disputed ;; Quarreling || death ;; Death
became ;; Becoming || party ;; Social_event || granted ;; Giving || state ;; Statement
evacuated ;; Emptying || materialised ;; Coming_to_be || put ;; Placing
sending ;; Sending || established ;; Creating || recognising ;; Know
negotiating ;; Communication || form ;; Coming_to_be || prevent ;; Preventing_or_letting
ended ;; Process_end || commemorated ;; Social_event
fought ;; Hostile_encounter || Battle ;; Hostile_encounter || battle ;; Hostile_encounter || beginning ;; Process_start
fought ;; Hostile_encounter
conquer ;; Conquering || reunite ;; Cause_to_amalgamate || frustrated ;; Preventing_or_letting
gave ;; Giving || survival ;; Preserving || creation ;; Creating || ensured ;; Preventing_or_letting || provided ;; Supply
battle ;; Hostile_encounter || called ;; Name_conferral
Descriptions ;; Convincing || location ;; Presence || differ ;; Change
place ;; Placing || location ;; Presence || remains ;; Presence
hurricanes ;; Catastrophe || Hurricane ;; Catastrophe
storm ;; Catastrophe
attain ;; Getting
developed ;; Coming_to_be || initiate ;; Process_start
attained ;; Getting
moved ;; Motion || attaining ;; Getting
passing ;; Motion_directional || brushed ;; Motion || turned ;; Becoming
became ;; Becoming || dissipated ;; Dispersal
strike ;; Attack || evacuated ;; Removing
caused ;; Causation || produced ;; Manufacturing
killed ;; Killing || required ;; Preserving
damage ;; Damaging
damage ;; Damaging || downed ;; Motion
halted ;; Process_end || impact ;; Influence
caused ;; Causation || occurred ;; Presence
Sinking ;; Catastrophe || turning ;; Change
attack ;; Attack || attack ;; Attack || made ;; Creating || staged ;; Self_motion
approached ;; Arriving || embedded ;; Placing || detonated ;; Attack || avoiding ;; Preventing_or_letting || loss ;; Death || sank ;; Motion_directional
became ;; Becoming || attack ;; Attack || become ;; Becoming || lost ;; Earnings_and_losses || survive ;; Preserving || sink ;; Motion_directional
conducted ;; Action || campaign ;; Military_operation
serve ;; Assistance
centered ;; Action
became ;; Becoming || providing ;; Supply
stem ;; Preventing_or_letting
guaranteed ;; Commitment || maintain ;; Preserving
publicized ;; Dispersal || gain ;; Getting
emerged ;; Coming_to_be || devastated ;; Destroying
damage ;; Damaging || resulted in ;; Causation
emerged ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || originated ;; Coming_to_be
began ;; Process_start || consolidate ;; Cause_change_of_strength
upgraded ;; Cause_to_make_progress || storm ;; Catastrophe
Moving ;; Motion || occurred ;; Coming_to_be || hurricane ;; Catastrophe
reached ;; Arriving
Increased ;; Cause_change_of_position_on_a_scale || led to ;; Causation || weakening ;; Cause_change_of_strength
reached ;; Arriving
ensued ;; Coming_to_be || Weakening ;; Cause_change_of_strength || slowed ;; Cause_change_of_position_on_a_scale
began ;; Process_start || reached ;; Arriving || existence ;; Presence || steered ;; Control || attained ;; Getting
intensified ;; Cause_change_of_strength || Passing ;; Motion_directional || weakened ;; Cause_change_of_strength
became ;; Becoming || began ;; Process_start || passed ;; Motion_directional
retaining ;; Preserving
became ;; Becoming || merged ;; Cause_to_amalgamate
produced ;; Manufacturing || flooding ;; Motion
passed ;; Motion_directional || observed ;; Know
produced ;; Manufacturing || led to ;; Causation
merged ;; Cause_to_amalgamate || flooding ;; Motion || resulted in ;; Causation || leading to ;; Causation
caused ;; Causation || damage ;; Damaging || reported ;; Reporting
race ;; Competition
concluded ;; Process_end || steeplechase ;; Competition || took place ;; Process_start
made ;; Causation || permitted ;; Preventing_or_letting
race ;; Competition
completed ;; Process_end || fell ;; Motion_directional || unseated ;; Motion || returned ;; Arriving
race ;; Competition || broadcast ;; Expressing_publicly || took over ;; Getting
aired ;; Expressing_publicly || broadcast ;; Expressing_publicly || televised ;; Communication
retain ;; Preserving
produced ;; Creating || took place ;; Process_start
matches ;; Competition
retain ;; Preserving
match ;; Competition || win ;; Earnings_and_losses
match ;; Competition || defeated ;; Conquering || retain ;; Preserving
scheduled ;; Arranging || bouts ;; Hostile_encounter
retrieve ;; Getting || match ;; Competition || become ;; Becoming || match ;; Competition || defeat ;; Conquering || climb ;; Motion_directional
claimed ;; Statement || victory ;; Earnings_and_losses
became ;; Becoming || debut ;; Process_start || marked ;; Recording
rated ;; Hold
became ;; Becoming || launch ;; Process_start || stream ;; Supply
rivalry ;; Competition
played ;; Participation || winning ;; Earnings_and_losses || Starting ;; Process_start
reclaimed ;; Earnings_and_losses
became ;; Becoming || becoming ;; Becoming || win ;; Earnings_and_losses || resulted in ;; Causation || surpassing ;; Achieve || saving ;; Earnings_and_losses || progressed ;; Cause_to_make_progress
announcement ;; Expressing_publicly || preceded ;; Process_start
took ;; Getting
making ;; Causation || prevented ;; Causation
bowling ;; Motion || bowled ;; Motion
decide ;; Deciding || left ;; Causation
began ;; Process_start || remove ;; Removing || retain ;; Preserving || left ;; Departing
gave ;; Giving || give ;; Giving || losing ;; Earnings_and_losses || win ;; Earnings_and_losses || ran out of ;; Expend_resource
festival ;; Social_event || concert ;; Social_event
added ;; Cause_change_of_position_on_a_scale || takes place ;; Hold
moved ;; Motion || festival ;; Social_event
aired ;; Expressing_publicly || launch ;; Motion || television ;; Expressing_publicly
televised ;; Communication || kicking off ;; Process_start || broadcast ;; Expressing_publicly
emerging ;; Coming_to_be || festival ;; Social_event
festival ;; Social_event || wrote ;; Writing || established ;; Creating
includes ;; Cause_to_be_included || concert ;; Social_event || events ;; Social_event
took place ;; Process_start || bombings ;; Attack || attacks ;; Attack
injured ;; Bodily_harm || killed ;; Killing
blamed ;; Judgment_communication
condemned ;; Judgment_communication || attack ;; Attack
statement ;; Statement || said ;; Statement || called ;; Request || attack ;; Attack || carried out ;; Action
said ;; Statement || fighting ;; Defending
expressed ;; Expressing_publicly || negotiations ;; Communication || collapse ;; Destroying
said ;; Statement || plan ;; Arranging || place ;; Placing || attacks ;; Attack
said ;; Statement || restrict ;; Hindering || heighten ;; Cause_change_of_position_on_a_scale
rejected ;; Response || said ;; Statement
increased ;; Cause_change_of_position_on_a_scale
announced ;; Expressing_publicly || suspending ;; Process_end || operations ;; Military_operation
killed ;; Killing || started ;; Process_start || riots ;; Violence
damaging ;; Damaging || killing ;; Killing || resulted in ;; Causation || riot ;; Violence || escalated ;; Cause_change_of_position_on_a_scale || demonstrations ;; Social_event
spread ;; Dispersal || rioting ;; Violence || Demonstrations ;; Social_event
demonstrations ;; Social_event || riots ;; Violence || clashes ;; Violence || took place ;; Process_start
called ;; Name_conferral
expressing ;; Expressing_publicly || triggered ;; Causation || shooting ;; Use_firearm || described ;; Statement
tour ;; Traveling
supported ;; Supporting || visited ;; Traveling
began ;; Process_start || ended ;; Process_end || tour ;; Traveling
previewed ;; Perception_active || tour ;; Traveling
described ;; Statement
show ;; Social_event || held ;; Hold || broadcast ;; Expressing_publicly
received ;; Receiving || remarked ;; Statement || praised ;; Judgment_communication || tour ;; Traveling || critics ;; Judgment_communication || host ;; Being_in_operation
hosted ;; Social_event
attended ;; Participation
attending ;; Participation
attended ;; Participation
participate ;; Participation || regarded ;; Protest
conferences ;; Social_event || participated ;; Participation
agreed ;; Agree_or_refuse_to_act || reached ;; Achieve || support ;; Supporting
asking ;; Request || invite ;; Request || party ;; Social_event || raise ;; Cause_change_of_position_on_a_scale
voiced ;; Expressing_publicly || viewed ;; Perception_active
began ;; Process_start || protests ;; Response
held ;; Defending || supporting ;; Supporting || focused ;; Aiming || recruiting ;; Employment || elections ;; Choosing
aim ;; Aiming
evoked ;; Causation || revolution ;; Change_of_leadership || tread ;; Action
used ;; Using || form ;; Coming_to_be
promoted ;; Cause_to_make_progress || promoting ;; Cause_to_make_progress || protest ;; Attack
included ;; Cause_to_be_included || expressing ;; Expressing_publicly || support ;; Supporting || mocking ;; Judgment_communication
produced ;; Manufacturing
staged ;; Placing
introduce ;; Statement
broadcast ;; Expressing_publicly
took place ;; Process_start
expressed ;; Expressing_publicly
began ;; Process_start || request ;; Request || evicted ;; Removing
moved ;; Motion || set up ;; Building
set up ;; Building || diminished ;; Cause_change_of_position_on_a_scale || retirement ;; Process_end
adopted ;; Choosing || became ;; Becoming || influential ;; Influence || creation ;; Creating || dissipated ;; Dispersal
shot ;; Use_firearm || service ;; Supply || operation ;; Military_operation
believed ;; Know
killing ;; Killing || approached ;; Arriving
found ;; Know || discovered ;; Know || leading to ;; Causation || conspired ;; Collaboration
planning ;; Arranging || control ;; Control || ruled ;; Control || lethal ;; Killing
deaths ;; Death
attacked ;; Attack
driven ;; Self_motion || shot ;; Use_firearm
planning ;; Arranging || changing ;; Change || aware ;; Know || detonate ;; Attack
request ;; Request || travelled ;; Traveling || attack ;; Attack || tracked ;; Self_motion
seen ;; Perception_active || seen ;; Perception_active || crossing ;; Motion_directional || shootings ;; Use_firearm || used ;; Using || parking ;; Placing
handed over ;; Giving || control ;; Control || reported ;; Reporting || treated ;; Cure
moving ;; Motion || began ;; Process_start || running ;; Self_motion || split ;; Escaping
said ;; Telling || shooting ;; Use_firearm || pursued ;; Aiming || approached ;; Arriving
alleged ;; Statement || shot ;; Use_firearm || turned ;; Becoming || reaching ;; Arriving
resulting ;; Causation || found ;; Know || led ;; Causation || found ;; Know || found ;; Know
shootings ;; Use_firearm || broadcast ;; Expressing_publicly
presented ;; Presence || killed ;; Killing
described ;; Statement
began ;; Process_start
heard ;; Perception_active || sighted ;; Perception_active || lost ;; Earnings_and_losses || parking ;; Placing || tracked ;; Self_motion
testified ;; Statement || reaching ;; Arriving
gave ;; Giving || discovered ;; Know || gave ;; Giving || seeing ;; Perception_active
told ;; Telling || seen ;; Perception_active || statement ;; Statement || retracted ;; Agree_or_refuse_to_act || claiming ;; Statement
found ;; Know || combined ;; Cause_to_amalgamate || given ;; Giving || lethal ;; Killing || rendered ;; Supply
bombing ;; Attack || Bombing ;; Attack
named ;; Name_conferral || break ;; Destroying || produced ;; Creating || hosting ;; Come_together || targeted ;; Aiming || command ;; Control
killing ;; Killing || raid ;; Attack || occurred ;; Presence || dropped ;; Motion_directional || set fire ;; Damaging || obliterating ;; Destroying
destroyed ;; Destroying
damaged ;; Damaging || destroyed ;; Destroying
dated ;; Social_event || destroyed ;; Destroying
buildings ;; Building || reconstructed ;; Recovering
rebuilt ;; Recovering || end ;; Process_end || dead ;; Death
destroyed ;; Destroying || firebombing ;; Attack
began ;; Process_start || planned ;; Arranging
decided ;; Deciding || announced ;; Expressing_publicly
took place ;; Process_start
planned ;; Arranging
consisted ;; Cause_to_be_included
included ;; Cause_to_be_included
generated ;; Creating
broadcast ;; Expressing_publicly
released ;; Publishing
became ;; Becoming || concert ;; Social_event
grossed ;; Earnings_and_losses
returned ;; Arriving
remain ;; Presence
events ;; Social_event || organised ;; Arranging || promote ;; Cause_to_make_progress
named ;; Name_conferral
events ;; Social_event || took place ;; Process_start
event ;; Social_event || broadcast ;; Expressing_publicly
pledged ;; Commitment
celebration ;; Social_event || endorsed ;; Receiving
spoke ;; Statement
event ;; Social_event || event ;; Social_event || promote ;; Cause_to_make_progress || boycott ;; Agree_or_refuse_to_act || dubbed ;; Name_conferral
increase ;; Cause_change_of_position_on_a_scale || disease ;; Bodily_harm || associated ;; Collaboration || doubling ;; Cause_change_of_position_on_a_scale || promotion ;; Cause_change_of_position_on_a_scale
asked ;; Request || established ;; Creating || raise ;; Self_motion
released ;; Publishing
event ;; Social_event || cancelling ;; Removing || held ;; Hold
event ;; Social_event || help ;; Assistance || called ;; Name_conferral || launched ;; Process_start
included ;; Cause_to_be_included
sought ;; Getting
took ;; Action
centered ;; Placing
expressed ;; Expressing_publicly
include ;; Cause_to_be_included
enacted ;; Action
took place ;; Process_start || wrestling ;; Competition || produced ;; Creating
marked ;; Recording
aired ;; Expressing_publicly || aired ;; Expressing_publicly
defending ;; Defending
defended ;; Defending || defending ;; Defending || defending ;; Defending || making ;; Manufacturing
battled ;; Hostile_encounter || mixed ;; Cause_to_amalgamate || saw ;; Self_motion
attended ;; Participation
released ;; Publishing
parade ;; Social_event
took part ;; Dispersal
organised ;; Arranging || funded ;; Supply
took part ;; Dispersal || included ;; Cause_to_be_included
surpassing ;; Cause_change_of_position_on_a_scale || set ;; Placing
pass ;; Motion_directional || moored ;; Placing
estimated ;; Statement || watched ;; Perception_active
broadcast ;; Expressing_publicly
held ;; Defending
Started ;; Process_start
invited ;; Request
finished ;; Process_end
takes place ;; Process_start || attracts ;; Influence
breaking ;; Destroying || broadcast ;; Expressing_publicly
competition ;; Competition || interspersed ;; Dispersal
expressed ;; Expressing_publicly
aimed ;; Aiming || advance ;; Cause_change_of_position_on_a_scale
included ;; Cause_to_be_included || benefited ;; Earnings_and_losses || caused ;; Causation || control ;; Control || undermined ;; Bodily_harm
called ;; Name_conferral || formed ;; Coming_to_be
began ;; Process_start || receive ;; Receiving
taken place ;; Process_start
became ;; Becoming || started ;; Process_start
began ;; Process_start || change ;; Change || remove ;; Removing
evicted ;; Removing || cleared ;; Emptying
organizes ;; Arranging || uses ;; Using
described ;; Statement
became ;; Becoming || stating ;; Statement || adopt ;; Choosing
criticise ;; Judgment_communication || persuaded ;; Convincing || stated ;; Statement || behave ;; Action
better known ;; Know
launched ;; Process_start || owned ;; Action
news ;; Statement || shows ;; Presence
investigative ;; Scrutiny || news ;; Statement
aired ;; Expressing_publicly || tracked ;; Motion || award-winning ;; Award
programming ;; Arranging
stopped ;; Process_end || due ;; Causation || dealt ;; Hold
present ;; Presence
employees ;; Employment || demands ;; Request || taking over ;; Getting
refused ;; Agree_or_refuse_to_act || accepting ;; Receiving || presented ;; Presence || rescue ;; Rescuing || return ;; Arriving || plan ;; Arranging
employees ;; Employment || ceased ;; Process_end || produced ;; Creating
unemployed ;; Employment || employees ;; Employment || striking ;; Attack || referred ;; Cause_to_be_included
show ;; Presence
referred ;; Cause_to_be_included
concert ;; Social_event
negotiations ;; Communication || started ;; Process_start || support ;; Supporting || wanted ;; Request
run ;; Process_start || race ;; Competition
broadcast ;; Expressing_publicly
opening ;; Process_start || voiced ;; Statement || race ;; Competition
attended ;; Participation
race ;; Competition || set ;; Placing
taking ;; Getting || rode ;; Conquering
win ;; Getting || win ;; Getting
event ;; Social_event || produced ;; Creating || took place ;; Process_start
named ;; Name_conferral || event ;; Social_event || held ;; Creating
match ;; Competition || event ;; Social_event
defeating ;; Conquering || event ;; Social_event
injured ;; Bodily_harm || defeated ;; Conquering
included ;; Cause_to_be_included
wrestling ;; Competition || reported ;; Reporting || event ;; Social_event || attracted ;; Bringing
aired ;; Expressing_publicly
recording ;; Recording || held ;; Defending
raise ;; Cause_change_of_position_on_a_scale
gave ;; Giving || tour ;; Traveling
death ;; Death || took place ;; Process_start
expressed ;; Expressing_publicly || interview ;; Communication || re-united ;; Cause_to_amalgamate
joined ;; Becoming_a_member || concerts ;; Social_event
planned ;; Arranging || concert ;; Social_event || cancelled ;; Change_event_time
occurred ;; Presence
warning ;; Warning || injuries ;; Bodily_harm || response ;; Response || causing ;; Causation || crossing ;; Motion_directional
warning ;; Warning || warns ;; Warning || set ;; Placing || allow ;; Preventing_or_letting || express ;; Expressing_publicly || cross ;; Motion_directional || turned off ;; Being_in_operation || stop ;; Preventing_or_letting
known ;; Know || prevented ;; Preventing_or_letting || setting ;; Placing || operating ;; Being_in_operation
operating ;; Being_in_operation || inform ;; Telling || contravened ;; Hindering
apply ;; Using || packing ;; Placing || see ;; Perception_active
collapsed ;; Destroying || indicted ;; Criminal_investigation || case ;; Criminal_investigation || charged ;; Judgment_communication
warning ;; Warning || affected ;; Influence || managing ;; Control || fined ;; Judgment_communication || working ;; Employment || crash ;; Motion
event ;; Social_event || produced ;; Creating
held ;; Hold
served ;; Assistance
appeared ;; Presence
made ;; Manufacturing
destruction ;; Destroying || wrestled ;; Bodily_harm
revival ;; Recovering
beat ;; Bodily_harm || defeated ;; Conquering || graduate ;; Education_teaching || accompanied ;; Self_motion
named ;; Name_conferral || called ;; Name_conferral || participants ;; Participation
forgotten ;; GiveUp || wrote ;; Motion
conducted ;; Action || bombing ;; Attack || war ;; Hostile_encounter || war ;; Hostile_encounter
campaigns ;; Military_operation || attacks ;; Attack || War ;; Hostile_encounter || resupply ;; Supply || training ;; Education_teaching || targets ;; Aiming || utilized ;; Using
impact ;; Influence || disputed ;; Quarreling || bombing ;; Attack
bombing ;; Attack || record ;; Recording
bombing ;; Attack || report ;; Reporting
began ;; Process_start || bombing ;; Attack || believed ;; Statement
bombings ;; Attack || attacks ;; Attack
bomb ;; Attack || use ;; Using || authorized ;; Preventing_or_letting || carpet ;; Motion
deal ;; Arranging
bombing ;; Attack || expanded ;; Expansion || deal ;; Arranging
held ;; Hold || tournament ;; Competition
held ;; Hold || played ;; Competition
increased ;; Cause_change_of_position_on_a_scale || making ;; Action || debuts ;; Participation
Coached ;; Education_teaching || captained ;; Control || defeated ;; Conquering
qualified ;; Getting
losing ;; Earnings_and_losses
renewal ;; Recovering || took place ;; Process_start
ridden ;; Self_motion
trained ;; Education_teaching
trained ;; Education_teaching
finished ;; Process_end
ran ;; Self_motion || returned ;; Arriving
game ;; Competition
game ;; Competition || played ;; Participation
game ;; Competition || broadcast ;; Expressing_publicly
final ;; Process_end
coach ;; Education_teaching
coach ;; Education_teaching
game ;; Competition || touchdown ;; Body_movement || catches ;; Body_movement
race ;; Competition || took place ;; Process_start
packed ;; Placing
attendance ;; Participation
centred ;; Placing
made ;; Creating || unseating ;; Self_motion || losing ;; Earnings_and_losses
race ;; Competition || ridden ;; Self_motion || trained ;; Education_teaching
finished ;; Process_end
returned ;; Arriving || ran ;; Motion
participated ;; Participation || targeted ;; Aiming
slaughtered ;; Killing
equipped ;; Supply || trained ;; Education_teaching || motivated ;; Influence
struck ;; Attack || emerging ;; Coming_to_be || counterattack ;; Attack || capture ;; Conquering
occurred ;; Presence || besieged ;; Besieging || quell ;; Hindering || uprising ;; Violence || massacre ;; Killing
ended ;; Process_end || begun ;; Process_start || campaign ;; Social_event || massacre ;; Killing
killed ;; Killing || stated ;; Statement
killed ;; Killing || claiming ;; Statement || estimates ;; Statement || vary ;; Change
killed ;; Killing || destroyed ;; Destroying || operation ;; Military_operation
acts ;; Action || described ;; Statement || attack ;; Attack
killed ;; Killing || initiated ;; Process_start || mutilating ;; Bodily_harm || fighting ;; Hostile_encounter || pounced ;; Motion
taught ;; Education_teaching || crimes ;; Committing_crime || rose ;; Motion || confront ;; Confronting_problem || breath ;; Breathing
flight ;; Motion || scheduled ;; Arranging
crashed ;; Catastrophe || end ;; Process_end || landing ;; Motion || operating ;; Being_in_operation
injuries ;; Bodily_harm
played ;; Participation || trained ;; Education_teaching
departing ;; Departing || arriving at ;; Arriving || Due ;; Causation || cancelled ;; Change_event_time
due ;; Causation || arrive ;; Arriving || diverted ;; Removing
diverted ;; Removing
turned ;; Change
flight ;; Motion || crash ;; Catastrophe || hit ;; Attack
referred ;; Cause_to_be_included
described ;; Statement
investigated ;; Check || report ;; Reporting
decisions ;; Deciding || found ;; Know || landing ;; Motion || flight ;; Motion || crash ;; Catastrophe || made ;; Action || leading to ;; Causation
theatre ;; Social_event || theatre ;; Social_event || training ;; Education_teaching || Festival ;; Social_event || festival ;; Social_event || established ;; Creating
attract ;; Motion || grown ;; Expansion || Founded ;; Creating
Festival ;; Social_event || productions ;; Creating
festival ;; Social_event || festival ;; Social_event || acknowledged ;; Statement || theatre ;; Social_event || dedicated ;; Supply || theatre ;; Social_event || held ;; Hold || held ;; Hold
productions ;; Creating
attack ;; Attack || carried out ;; Attack || bombing ;; Terrorism
placed ;; Placing || detonated ;; Attack || passed ;; Motion_directional
killed ;; Killing || explosion ;; Attack || studying ;; Education_teaching || died ;; Death || bombing ;; Terrorism
injured ;; Bodily_harm || bombing ;; Terrorism
condemned ;; Judgment_communication || attack ;; Attack
praised ;; Judgment_communication
attack ;; Terrorism
identified ;; Check
questioning ;; Criminal_investigation || attack ;; Terrorism || claims ;; Statement
arrested ;; Arrest
bombing ;; Attack || trial ;; Criminal_investigation
Built ;; Building || operation ;; Military_operation
due ;; Causation || closed ;; Process_end || strike ;; Attack
taught ;; Education_teaching
used ;; Using || set ;; Placing || included ;; Cause_to_be_included || productions ;; Creating
destroyed ;; Destroying || fire ;; Catastrophe
resulting in ;; Causation || riots ;; Violence || broke out ;; Process_start || killed ;; Killing || injured ;; Bodily_harm
began ;; Process_start || replaced ;; Change_of_leadership || support ;; Supporting || tough ;; Confronting_problem || take ;; Action || suppress ;; Preventing_or_letting
banned ;; Preventing_or_letting
dissolved ;; Destroying
gathered ;; Come_together || protest ;; Preventing_or_letting || camped ;; Placing
organising ;; Arranging || meetings ;; Social_event || holding ;; Hold
issued ;; Statement || vacated ;; Departing
started ;; Process_start || spread ;; Dispersal || approached ;; Arriving
decided ;; Deciding
cleared ;; Emptying || entered ;; Arriving || using ;; Using || students ;; Education_teaching
headed ;; Motion
damaged ;; Damaging || overturned ;; Change
threw ;; Motion
killed ;; Killing || injured ;; Bodily_harm
arrested ;; Arrest
released ;; Releasing || party ;; Social_event || election ;; Choosing || gained ;; Getting || form ;; Creating
fought ;; Hostile_encounter
fought ;; Hostile_encounter || create ;; Creating || called ;; Name_conferral || Beginning ;; Process_start
defeated ;; Conquering || bringing ;; Bringing
caused ;; Causation || killed ;; Killing
died ;; Death
captured ;; Conquering || retake ;; Control
employed ;; Employment || resulted in ;; Causation
accused ;; Judgment_communication
declared ;; Expressing_publicly || signed ;; Sign_agreement || keeping ;; Temporary_stay
renewed ;; Recovering || escalate ;; Cause_change_of_position_on_a_scale || began ;; Process_start || launched ;; Process_start
capture ;; Conquering || declared ;; Expressing_publicly || struggle ;; Hostile_encounter || achieve ;; Achieve || aided ;; Assistance || alleging ;; Statement || shifted ;; Change || announced ;; Expressing_publicly || control ;; Control || controlled ;; Control || withdrawal ;; Removing || took ;; Creating
demand ;; Request
learnt ;; Education_teaching || appointed ;; Request || assess ;; Scrutiny
departing ;; Departing || designated ;; Choosing
formed ;; Coming_to_be || supplies ;; Supply || increased ;; Cause_change_of_position_on_a_scale || cut off ;; Removing
learned ;; Education_teaching || gained ;; Getting || attacks ;; Attack || developed ;; Coming_to_be
positioned ;; Placing
leaving ;; Departing || sailed ;; Self_motion
attack ;; Attack
bomb ;; Attack
attack ;; Attack || damage ;; Damaging
reached ;; Arriving || casualties ;; Catastrophe || suffered ;; Bodily_harm
inflicting ;; Bodily_harm || end ;; Process_end || forced ;; Motion || discontinue ;; Process_end || sunk ;; Motion_directional
coaches ;; Education_teaching || awards ;; Award
given out ;; Giving || awards ;; Award
win ;; Getting || placing ;; Placing || wins ;; Getting || award ;; Award
received ;; Receiving || named ;; Name_conferral
ceremony ;; Social_event || hosted ;; Social_event
operated ;; Being_in_operation
crashed ;; Catastrophe
suffered ;; Bodily_harm || crashed ;; Catastrophe
killed ;; Killing
remains ;; Presence || involving ;; Participation || surpassed ;; Cause_change_of_position_on_a_scale || crashed ;; Catastrophe
approach ;; Arriving || report ;; Reporting || concluded ;; Coming_to_believe || contributed ;; Giving
training ;; Education_teaching || contributed ;; Giving
begins ;; Process_start
centred ;; Placing
served ;; Assistance
subject ;; Causation
completed ;; Process_end || trained ;; Education_teaching
landed ;; Self_motion
strengthened ;; Cause_change_of_strength || war ;; Hostile_encounter || furthered ;; Self_motion
took place ;; Process_start
trained ;; Education_teaching || race ;; Competition || ridden ;; Motion
raced ;; Competition || remounted ;; Hold
remounted ;; Hold
ran ;; Motion || returned ;; Motion
given in ;; Name_conferral || fought ;; Hostile_encounter || name ;; Name_conferral
learned ;; Education_teaching
began ;; Process_start || retreat ;; Escaping || pursued ;; Self_motion || harassed ;; Hindering
reached ;; Arriving
break ;; Bodily_harm || cross ;; Motion_directional
led to ;; Causation || rule ;; Control
fighting ;; Hostile_encounter || control ;; Control || remained ;; Presence
demanding ;; Request || existence ;; Presence || participation ;; Participation || protests ;; Protest || end ;; Process_end
study ;; Education_teaching
built ;; Building || end ;; Process_end || transition ;; Change || swelled ;; Cause_change_of_position_on_a_scale
demands ;; Request || reflect ;; Influence || discontent ;; Judgment_communication || protests ;; Protest
violence ;; Violence || included ;; Cause_to_be_included || Protests ;; Protest || marches ;; Social_event
response ;; Response || replaced ;; Change_of_leadership || addressing ;; Resolve_problem || protests ;; Protest || proposal ;; Statement
rejected ;; Agree_or_refuse_to_act || proposals ;; Statement
achieved ;; Achieve || fall ;; Cause_change_of_position_on_a_scale || contributed ;; Influence || increased ;; Cause_change_of_position_on_a_scale
game ;; Competition
began ;; Process_start || held ;; Hold
game ;; Competition || televised ;; Communication
game ;; Competition || debuted ;; Process_start
game ;; Competition || took place ;; Process_start
took place ;; Process_start
game ;; Competition || selected ;; Choosing
game ;; Competition
lose ;; Competition
agreed ;; Agree_or_refuse_to_act || selected ;; Choosing || coached ;; Education_teaching || loss ;; Earnings_and_losses
took place ;; Process_start
defeated ;; Conquering
combined ;; Cause_to_amalgamate || scored ;; Recording
named ;; Name_conferral || set ;; Placing || catching ;; Self_motion || breaking ;; Destroying
coached ;; Education_teaching || led ;; Causation
fought ;; Hostile_encounter
fortified ;; Cause_change_of_strength
led ;; Conquering || commanded ;; Control
composed ;; Come_together
battle ;; Hostile_encounter
destroyed ;; Destroying || trained ;; Education_teaching
refuge ;; Escaping
captured ;; Conquering || slaughtered ;; Killing || taken ;; Giving
died ;; Death
battle ;; Hostile_encounter
took place ;; Process_start || running ;; Motion
ridden ;; Being_in_operation
became ;; Becoming || win ;; Earnings_and_losses
became ;; Becoming || bred ;; Creating
trained ;; Education_teaching
completed ;; Process_end
collapsed ;; Destroying || hear ;; Perception_active
geared ;; Motion
takes place ;; Process_start
includes ;; Cause_to_be_included
debut ;; Process_start || budding ;; Self_motion
include ;; Cause_to_be_included
program ;; Education_teaching
scheduled ;; Hold
approach ;; Arriving || crashed ;; Catastrophe
died ;; Death || injured ;; Bodily_harm
thrown ;; Motion || broke ;; Destroying || strapped ;; Connect || injured ;; Bodily_harm || striking ;; Attack
fatal ;; Killing || entered ;; Arriving || crash ;; Catastrophe || service ;; Supply
result ;; Causation || approach ;; Arriving || investigation ;; Scrutiny || concluded ;; Statement
training ;; Education_teaching || control ;; Control || cited ;; Coming_to_believe
affected ;; Influence
caused ;; Causation || became ;; Becoming || formed ;; Creating || disturbed ;; Change_sentiment
moved ;; Motion || resulted in ;; Causation
evacuated ;; Emptying || killed ;; Killing || affected ;; Influence || flooded ;; Motion
turned ;; Becoming || became ;; Becoming || headed ;; Motion
made ;; Manufacturing || causing ;; Causation
emerged ;; Coming_to_be || made ;; Manufacturing
causing ;; Causation || killing ;; Killing || cause ;; Causation || dissipated ;; Dispersal
made ;; Manufacturing || hit ;; Attack || hit ;; Attack
mudslide ;; Catastrophe || mudslide ;; Catastrophe || occurred ;; Coming_to_be
caused ;; Causation || flooding ;; Catastrophe
disaster ;; Catastrophe || floods ;; Catastrophe
killed ;; Killing || rescued ;; Rescuing || mudslides ;; Catastrophe
dead ;; Death || presumed ;; Coming_to_believe || stop ;; Preventing_or_letting || prevent ;; Preventing_or_letting
evacuated ;; Emptying || living ;; Placing
formed ;; Coming_to_be
crossed ;; Motion_directional
upgraded ;; Cause_to_make_progress
strengthened ;; Cause_change_of_strength
moved ;; Motion
struck ;; Attack
evacuated ;; Emptying
cross ;; Motion_directional || Operation ;; Military_operation || seize ;; Hold
marked ;; Recording || Launched ;; Military_operation
preceded ;; Process_start
used ;; Using || clear ;; Emptying || cross ;; Motion_directional
assaulted ;; Attack || counterattacked ;; Hostile_encounter
surprised ;; Change_sentiment || complete ;; Process_end || occupied ;; Control
established ;; Creating
arrived at ;; Arriving || launched ;; Military_operation
repel ;; Defending || employing ;; Employment
occupied ;; Control
laid ;; Placing
commemorated ;; Social_event
destructive ;; Destroying || inundated ;; Motion
intensified ;; Cause_change_of_strength || formed ;; Coming_to_be
fluctuated ;; Motion || reached ;; Arriving
began ;; Process_start || passing ;; Motion_directional
began ;; Process_start || reached ;; Arriving
became ;; Becoming
tracked ;; Self_motion
brought ;; Bringing
recorded ;; Recording || exceeded ;; Cause_change_of_position_on_a_scale || storm ;; Catastrophe
destroyed ;; Destroying || damaged ;; Damaging
storm ;; Catastrophe || brought ;; Bringing
killed ;; Killing || damage ;; Damaging
impacted ;; Influence || destruction ;; Destroying
evacuated ;; Emptying
began ;; Process_start || beginning ;; Process_start || fought ;; Hostile_encounter || cleared ;; Emptying
held ;; Hold || holding ;; Hold
broken ;; Destroying || attacks ;; Attack
retreating ;; Escaping || established ;; Creating || included ;; Cause_to_be_included
crossed ;; Motion_directional || attacked ;; Attack || retreating ;; Escaping || protecting ;; Defending || traveling ;; Traveling
defended ;; Defending || captured ;; Conquering || supported ;; Supporting || attack ;; Attack
withdrawn ;; Escaping || withdrawing ;; Escaping
struck ;; Attack
storm ;; Catastrophe || hurricane ;; Catastrophe || developed ;; Cause_change_of_position_on_a_scale
Forming ;; Coming_to_be || storm ;; Catastrophe || brushed ;; Motion || intensifying ;; Cause_change_of_strength
strengthen ;; Cause_change_of_strength || storm ;; Catastrophe
due ;; Causation || weakened ;; Cause_change_of_strength || emerging ;; Coming_to_be
allowed ;; Preventing_or_letting || become ;; Becoming || hurricane ;; Catastrophe
moved ;; Motion || moving ;; Motion || ensued ;; Coming_to_be || weakening ;; Cause_change_of_strength || storm ;; Catastrophe || diminishing ;; Cause_change_of_position_on_a_scale
emerged ;; Know || dissipating ;; Dispersal
monitored ;; Scrutiny || losing ;; Earnings_and_losses
left ;; Departing
left ;; Departing || flooding ;; Motion || observed ;; Know
produced ;; Manufacturing || exceeding ;; Cause_change_of_position_on_a_scale
left ;; Departing || destroyed ;; Destroying
blocked ;; Hindering || washed ;; Emptying || cutting ;; Removing
killed ;; Killing || resulted in ;; Causation || damage ;; Damaging
caused ;; Causation || flooding ;; Motion
caused ;; Causation || due ;; Causation || loss ;; Earnings_and_losses || damage ;; Damaging || storm ;; Catastrophe
fought ;; Hostile_encounter || War ;; Hostile_encounter
coupled ;; Cause_to_amalgamate || military ;; Military_operation || campaigns ;; Military_operation || introduction ;; Creating
sent ;; Sending || bring ;; Bringing
presence ;; Presence || obstacles ;; Hindering
presented ;; Presence || abandoning ;; GiveUp || comply ;; Action || disbanding ;; Emptying || war ;; Hostile_encounter || intent ;; Aiming
sent ;; Sending || invade ;; Attack || met ;; Response
defeat ;; Earnings_and_losses || war ;; Hostile_encounter || battles ;; Hostile_encounter || Battle ;; Hostile_encounter || opening ;; Process_start || force ;; Military_operation || victory ;; Getting
resulted in ;; Causation || war ;; Hostile_encounter || end ;; Process_end
refer ;; Name_conferral
aborted ;; Process_end
drained ;; Emptying
referred ;; Name_conferral
fought ;; Hostile_encounter
occupy ;; Control || operation ;; Military_operation
opened ;; Openness || clear ;; Emptying || subdue ;; Conquering
evacuated ;; Emptying || secured ;; Getting
capturing ;; Conquering || landed ;; Self_motion
withdraw ;; Escaping || losses ;; Earnings_and_losses
captured ;; Conquering || claimed ;; Statement
killed ;; Killing || accident ;; Catastrophe || happened ;; Presence
shunting ;; Motion
struck ;; Attack || clear ;; Emptying
killing ;; Killing
slaughtered ;; Killing || killed ;; Killing
escaped ;; Escaping
claimed ;; Statement || seen ;; Perception_active || aware ;; Know || advance ;; Cause_to_make_progress
applied ;; Using || avoid ;; Preventing_or_letting
criticized ;; Judgment_communication || allowing ;; Preventing_or_letting
criticized ;; Judgment_communication || seeing ;; Perception_active
placed ;; Placing
retreat ;; Escaping || abandonment ;; GiveUp
commenced ;; Process_start || defence ;; Defending
managed ;; Control || evacuated ;; Emptying
captured ;; Conquering || abandoned ;; GiveUp || forces ;; Hostile_encounter
took ;; Creating
occurred ;; Presence
began ;; Process_start
spread ;; Dispersal || invasion ;; Attack || prepared ;; GetReady
died ;; Death || injured ;; Bodily_harm || sank ;; Killing
determined ;; Deciding || concluded ;; Process_end || caused ;; Causation || detonated ;; Attack || dropped ;; Motion_directional
led to ;; Causation
cleared ;; Emptying
left ;; Temporary_stay
enforced ;; Action || talk ;; Telling || ordered ;; Request
classified ;; Check || well known ;; Know
found ;; Know
used ;; Using || believe ;; Know
made ;; Manufacturing || storm ;; Catastrophe
organized ;; Arranging
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || stay ;; Temporary_stay || headed ;; Self_motion
turned ;; Becoming
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || dissipated ;; Dispersal
warnings ;; Warning || watches ;; Perception_active || issued ;; Creating
reported ;; Reporting || injuries ;; Bodily_harm || flipped ;; Motion
damaged ;; Damaging || washed ;; Removing || flooding ;; Catastrophe
brought ;; Bringing || flooded ;; Catastrophe
reported ;; Reporting || resulting in ;; Causation
evacuated ;; Emptying || flooded ;; Catastrophe
affected ;; Influence
disaster ;; Catastrophe || happened ;; Presence
killed ;; Killing || collapsed ;; Catastrophe
match ;; Competition || faced ;; Confronting_problem
increase ;; Cause_change_of_position_on_a_scale || wanted ;; Request
approved ;; Agree_or_refuse_to_act
collapsed ;; Catastrophe || killing ;; Killing || injured ;; Bodily_harm || game ;; Competition || leaving ;; Causation
match ;; Competition
collapsed ;; Catastrophe || match ;; Competition || start ;; Process_start
trapped ;; Preventing_or_letting
match ;; Competition || played ;; Competition
committed ;; Commitment || dealing ;; Resolve_problem || injured ;; Bodily_harm || evacuated ;; Emptying || reported ;; Reporting
became ;; Becoming || typhoon ;; Catastrophe
organized ;; Arranging || Forming ;; Coming_to_be
intensified ;; Cause_change_of_strength || given ;; Giving
took place ;; Process_start
moved ;; Motion || attained ;; Getting || reporting ;; Reporting
attained ;; Getting || reported ;; Reporting
weakened ;; Cause_change_of_strength || storm ;; Catastrophe
leading to ;; Causation
weakened ;; Cause_change_of_strength || caused ;; Causation
dissipated ;; Dispersal || transitioned ;; Change
passed ;; Motion_directional || reported ;; Reporting
deemed ;; Coming_to_believe || destroyed ;; Destroying || downed ;; Motion
response ;; Response || evacuated ;; Emptying
accident ;; Catastrophe || crash ;; Catastrophe || took place ;; Process_start
approaches ;; Arriving || incoming ;; Arriving || collided ;; Catastrophe
killed ;; Killing || injured ;; Bodily_harm
give ;; Giving || used ;; Using || happened ;; Presence
present ;; Presence || found ;; Know || pulled ;; Motion || enquiry ;; Criminal_investigation || incoming ;; Arriving || set ;; Placing || set ;; Placing
prevented ;; Preventing_or_letting || clear ;; Emptying || changed ;; Change
passing ;; Motion_directional || setting ;; Placing
allowing ;; Preventing_or_letting || reached ;; Arriving || changed ;; Change || therefore ;; Causation
disaster ;; Catastrophe || combination ;; Cause_to_amalgamate || led to ;; Causation
assassinated ;; Killing
murdered ;; Committing_crime
set ;; Causation
killed ;; Killing || cleansing ;; Emptying || led ;; Bringing
murder ;; Killing || passing ;; Motion_directional || set up ;; Building || proceeded ;; Deciding
documented ;; Recording || used ;; Using || identify ;; Check || passing ;; Motion_directional
cleansing ;; Emptying || murdered ;; Committing_crime
massacre ;; Catastrophe
attacked ;; Attack || headed ;; Self_motion
help ;; Assistance || attacked ;; Attack || followed ;; Motion
began ;; Process_start || killing ;; Killing || rode ;; Self_motion
killed ;; Killing || shot ;; Use_firearm || wounding ;; Bodily_harm
concluded ;; Coming_to_believe
laid ;; Placing
commenced ;; Process_start || came to ;; Self_motion || stripped ;; Emptying
torn ;; Destroying || lain ;; Placing || said ;; Telling || felt ;; Perception_active
stomped ;; Body_movement || pierced ;; Bodily_harm || retired ;; Departing || move ;; Motion || returned ;; Arriving
brought ;; Bringing || taken up ;; Bringing
recovered ;; Recovering || picked up ;; Choosing
killed ;; Killing || swung ;; Motion
escaped ;; Escaping || wounded ;; Bodily_harm || came up ;; Self_motion
became ;; Becoming || retrieved ;; Getting || reattached ;; Placing
Battle ;; Hostile_encounter || fought ;; Hostile_encounter || commanded ;; Control
battle ;; Hostile_encounter || campaign ;; Military_operation || War ;; Military_operation || took place ;; Process_start
destruction ;; Destroying || burnt ;; Damaging || sacking ;; Attack || landing ;; Self_motion
invaded ;; Attack || marched ;; Self_motion
garrisoned ;; Control || followed ;; Self_motion
stripped ;; Emptying
marched ;; Motion_directional || ford ;; Motion_directional || blocking ;; Hindering || encountered ;; Hostile_encounter
lowered ;; Cause_change_of_position_on_a_scale || marched ;; Motion_directional || engaged ;; Participation || shooting ;; Use_firearm || suppress ;; Hindering || standing ;; Self_motion
attacked ;; Attack || push ;; Hold
fled ;; Escaping || fed ;; Preventing_or_letting || broke ;; Destroying || pushed ;; Hold
losses ;; Earnings_and_losses || reported ;; Recording
Battle ;; Hostile_encounter || loss ;; Earnings_and_losses || defeated ;; Getting
campaign ;; Military_operation || siege ;; Besieging || ended ;; Process_end || held ;; Conquering
forces ;; Military_operation || forces ;; Military_operation || incident ;; Attack || skirmish ;; Attack || attack ;; Attack || attack ;; Attack || attacks ;; Attack || occurred ;; Presence || engaged ;; Participation
entered ;; Arriving
killing ;; Killing || fire ;; Use_firearm || wounding ;; Bodily_harm || opened ;; Process_start
resulted in ;; Causation || attack ;; Attack || deterioration ;; Cause_change_of_position_on_a_scale
reacted ;; Response || evacuation ;; Emptying || closure ;; Process_end || protests ;; Protest || took ;; Action || affecting ;; Influence
losses ;; Earnings_and_losses || suffered ;; Earnings_and_losses || apologized ;; Judgment_communication
restored ;; Recovering
operation ;; Military_operation
clear ;; Emptying
began ;; Process_start
operation ;; Military_operation
reported ;; Reporting || avoiding ;; Preventing_or_letting
functioning ;; Being_in_operation || focused ;; Aiming
cleared ;; Emptying
began ;; Process_start || plan ;; Arranging || demonstrations ;; Protest || unrest ;; Violence || contest ;; Competition
sparked ;; Causation || plan ;; Arranging || protests ;; Protest || outrage ;; Violence || violent ;; Violence || eviction ;; Removing || sit-in ;; Protest || protesting ;; Protest
supporting ;; Supporting || took place ;; Process_start || protests ;; Protest || strikes ;; Attack || encroachment ;; Hindering || protesting ;; Protest
organized ;; Arranging || centralised ;; Control || protests ;; Protest || movement ;; Social_event || events ;; Social_event || protest ;; Protest
protests ;; Protest || protests ;; Protest || downplayed ;; Judgment_communication
connected ;; Connect || taken ;; Conquering || demonstrations ;; Protest || protest ;; Protest
killed ;; Killing || injured ;; Bodily_harm
restored ;; Recovering || withdrew ;; Escaping || developed ;; Coming_to_be || organising ;; Arranging || sit-in ;; Protest || distribution ;; Supply || protest ;; Protest
began ;; Process_start || organised ;; Arranging || protests ;; Protest || discuss ;; Communication || cleared ;; Emptying || meet ;; Come_together
dismissed ;; Judgment_communication
protests ;; Protest || suppressed ;; Hindering
injuries ;; Bodily_harm || deaths ;; Death || arrests ;; Arrest || made ;; Manufacturing
criticized ;; Judgment_communication || use ;; Using
described ;; Statement
curbs ;; Cause_change_of_position_on_a_scale || war ;; Hostile_encounter || issues ;; Creating || kissing ;; Body_movement
given ;; Giving || called ;; Name_conferral || fighting ;; Hostile_encounter || insult ;; Judgment_communication
changed ;; Change || used ;; Using
challenging ;; Confronting_problem || protests ;; Protest || events ;; Social_event
causing ;; Causation || flooding ;; Catastrophe || passed ;; Motion_directional || impact ;; Influence
impacted ;; Influence
set ;; Placing
damage ;; Damaging || affected ;; Influence
evacuated ;; Emptying
washed ;; Removing || destroyed ;; Destroying
damage ;; Damaging
rescues ;; Rescuing
conducted ;; Action || announced ;; Expressing_publicly || Operation ;; Military_operation || operation ;; Military_operation
passing ;; Motion_directional || provide ;; Supply
operation ;; Military_operation
advanced ;; Self_motion
fire ;; Use_firearm || held ;; Defending
began ;; Process_start
offensive ;; Attack
operation ;; Military_operation || took place ;; Process_start
using ;; Using || said ;; Statement
help ;; Assistance || receive ;; Receiving
spread ;; Dispersal
destroying ;; Destroying || operation ;; Military_operation || terror ;; Terrorism || support ;; Supporting || root ;; Removing || said ;; Statement || designed ;; Creating
found ;; Know || held ;; Defending || clearing ;; Emptying
found ;; Know || destroyed ;; Destroying
killed ;; Killing || came ;; Self_motion || clearing ;; Emptying || fight ;; Hostile_encounter
strikes ;; Attack || destroyed ;; Destroying || called in ;; Come_together || buildings ;; Building
said ;; Statement || evacuated ;; Emptying
Operation ;; Military_operation || Operation ;; Military_operation
engagement ;; Hostile_encounter || fought ;; Hostile_encounter
battle ;; Hostile_encounter || unsupported ;; Judgment_communication || left ;; GiveUp || surrender ;; Surrendering || forced ;; Conquering
avoid ;; Preventing_or_letting || refused ;; Agree_or_refuse_to_act || remove ;; Removing || engage ;; Hostile_encounter || grounded ;; Being_in_operation || capture ;; Surrendering
taken ;; Conquering || captured ;; Conquering || returned ;; Giving
demonstrated ;; Protest || lead ;; Bringing || operation ;; Military_operation || convinced ;; Convincing || eliminate ;; Emptying
raid ;; Military_operation || raid ;; Attack || eliminated ;; Removing
took place ;; Process_start || explosions ;; Catastrophe
destroyed ;; Destroying || explosions ;; Catastrophe || fires ;; Catastrophe
damaged ;; Damaging
caused ;; Causation || killed ;; Killing || catastrophe ;; Catastrophe || deaths ;; Death || reported ;; Reporting
injured ;; Bodily_harm
damage ;; Damaging
destroyed ;; Destroying || recognized ;; Know || affected ;; Influence
became ;; Becoming || explosion ;; Catastrophe || started ;; Process_start || complaining ;; Judgment_communication
stinging ;; Bodily_harm
found ;; Know || coming ;; Motion
dispatched ;; Sending || check ;; Scrutiny || found ;; Know
evacuate ;; Emptying
took place ;; Process_start
sent ;; Sending || defeating ;; Conquering || control ;; Control || counter-attack ;; Attack
passing ;; Motion_directional || defeated ;; Conquering || killed ;; Killing || met ;; Come_together
reached ;; Arriving || met ;; Come_together || forced ;; Request || retreat ;; Escaping
attacked ;; Attack || clear ;; Emptying || rebellion ;; Change_of_leadership
attacked ;; Attack || celebrated ;; Social_event || forces ;; Causation
taken ;; Conquering || attacked ;; Attack
escape ;; Escaping || died ;; Death || managed ;; Hold
attack ;; Attack || made ;; Manufacturing || left ;; Departing || conquest ;; Change_of_leadership || headed ;; Self_motion
captured ;; Conquering || ford ;; Motion_directional
pushed ;; Motion || managed ;; Hold
conducted ;; Action || operation ;; Military_operation || Operation ;; Military_operation
targeting ;; Aiming || operations ;; Military_operation
carried out ;; Action
associated ;; Collaboration || involved ;; Cause_to_be_included
forms ;; Coming_to_be
included ;; Cause_to_be_included
cooperating ;; Collaboration || happened ;; Presence || operation ;; Military_operation
consisted ;; Cause_to_be_included
offered ;; Giving || withdrew ;; Escaping || avoid ;; Preventing_or_letting
contributed ;; Giving
evade ;; Escaping || establish ;; Creating
taken place ;; Process_start || necessitated ;; Causation || failure ;; Earnings_and_losses
prevent ;; Preventing_or_letting
proclaimed ;; Statement || required ;; Imposing_obligation
carry ;; Carry_goods
operated ;; Being_in_operation
captured ;; Conquering || destroyed ;; Destroying
broken out ;; Process_start || commit ;; Commitment || provided ;; Supply || threatened ;; Warning || mentioned ;; Statement || assembled ;; Come_together || ceased ;; Process_end || deemed ;; Coming_to_believe || set ;; Placing || issued ;; Statement
prevent ;; Preventing_or_letting || posted ;; Sending
captured ;; Conquering || sent ;; Sending || warning ;; Warning || warned ;; Warning || deemed ;; Coming_to_believe || view ;; Perception_active || approach ;; Arriving || leave ;; Departing
proclaim ;; Statement || held ;; Defending || declare ;; Expressing_publicly
caused ;; Causation || set ;; Placing
bombing ;; Attack
dropped ;; Motion_directional
made ;; Manufacturing || attacks ;; Attack
raid ;; Attack || dropped ;; Motion_directional
destroying ;; Destroying || flew ;; Motion || bombed ;; Attack
raids ;; Attack || carried out ;; Action
targeted ;; Aiming
flown ;; Self_motion || killing ;; Killing
attack ;; Attack || carried out ;; Action
called ;; Name_conferral || using ;; Using
causing ;; Causation || breached ;; Damaging
destroyed ;; Destroying
destroyed ;; Destroying || damaged ;; Damaging
died ;; Death
return ;; Arriving
crash ;; Catastrophe || occurred ;; Presence
killed ;; Killing || injured ;; Bodily_harm
services ;; Being_in_operation
accident ;; Catastrophe || service ;; Being_in_operation
enter ;; Arriving || services ;; Being_in_operation || reverse ;; Change
calling ;; Request
took place ;; Process_start || Defence ;; Defending || military ;; Military_operation
carried out ;; Action
attacking ;; Attack || encountered ;; Hostile_encounter
capturing ;; Conquering || supply ;; Supply || pushed ;; Motion
placed ;; Placing || achieve ;; Achieve || declared ;; Expressing_publicly || besieged ;; Besieging || capture ;; Conquering || storm ;; Catastrophe
response ;; Response || attacks ;; Attack || established ;; Building || launched ;; Process_start
presented ;; Presence || viewed ;; Perception_active || supply ;; Supply || redeployed ;; Arranging || forces ;; Military_operation
feared ;; Change_sentiment || disaster ;; Catastrophe || war ;; Hostile_encounter || occur ;; Presence
protect ;; Defending
conducted ;; Action || operation ;; Military_operation
planned ;; Arranging
carried out ;; Action || carried out ;; Action || planned ;; Arranging
failed ;; Earnings_and_losses
gaining ;; Getting || failed ;; Earnings_and_losses
using ;; Using || stabilised ;; Cause_change_of_strength
bombing ;; Attack || attack ;; Attack || carried out ;; Action
injured ;; Bodily_harm || killed ;; Killing || surpassed ;; Cause_change_of_position_on_a_scale || attack ;; Attack || attacks ;; Terrorism || attack ;; Attack
targeted ;; Aiming || bombing ;; Attack
derailed ;; Self_motion || traveling ;; Traveling
found ;; Know || went off ;; Process_end || passed ;; Motion_directional
emerged ;; Coming_to_be || received ;; Receiving
received ;; Receiving || ruled out ;; Removing || demand ;; Request || prompting ;; Causation || inquiry ;; Criminal_investigation
attack ;; Attack || kept ;; Temporary_stay
cyclone ;; Catastrophe
developed ;; Coming_to_be || moved ;; Motion || storm ;; Catastrophe || intensifying ;; Cause_change_of_strength || attained ;; Getting || hurricane ;; Catastrophe || becoming ;; Becoming || hurricane ;; Catastrophe
turned ;; Becoming || response ;; Response || approaching ;; Arriving || passed ;; Motion_directional
began ;; Process_start || hurricane ;; Catastrophe || strengthened ;; Cause_change_of_strength || become ;; Becoming || hurricane ;; Catastrophe || reaching ;; Arriving || maintaining ;; Preserving || strength ;; Cause_change_of_strength || passed ;; Motion_directional || weaken ;; Cause_change_of_position_on_a_scale
became ;; Becoming || storm ;; Catastrophe || passing ;; Motion_directional || turned ;; Becoming || weakened ;; Cause_change_of_position_on_a_scale
produced ;; Creating
carried ;; Carry_goods
caused ;; Causation || hurricane ;; Catastrophe || flooding ;; Motion || death ;; Death || produced ;; Manufacturing
damage ;; Damaging || resulted in ;; Causation
produced ;; Creating
occurrence ;; Presence || cyclone ;; Catastrophe
carrying ;; Carry_goods || operated ;; Using || charter ;; Renting
killing ;; Killing || crashed ;; Catastrophe || takeoff ;; Departing || burned ;; Bodily_harm
occur ;; Coming_to_be || involving ;; Cause_to_be_included || accident ;; Catastrophe || accident ;; Catastrophe || crash ;; Catastrophe
determined ;; Deciding || crash ;; Catastrophe || lift ;; Motion || due ;; Causation || underestimated ;; Judgment_communication || accident ;; Catastrophe || cause ;; Causation || investigated ;; Check
stated ;; Statement || caused ;; Causation || explosion ;; Catastrophe || telling ;; Telling
changes ;; Change || report ;; Reporting || causing ;; Causation || delays ;; Change_event_time || crash ;; Catastrophe
investigations ;; Scrutiny || response ;; Response || shut down ;; Being_in_operation || replacing ;; Change_of_leadership
killing ;; Killing || occurred ;; Coming_to_be || crashed ;; Catastrophe
operated ;; Being_in_operation
sold ;; Commerce_sell || Built ;; Building
flying ;; Motion || charter ;; Renting
sanction ;; Preventing_or_letting
cited ;; Judgment_communication
carried ;; Carry_goods || supplied ;; Supply
carrying ;; Carry_goods || claimed ;; Statement
taking ;; Bringing
take-off ;; Motion || crashed ;; Catastrophe
slid ;; Motion || resulting in ;; Causation
stated ;; Statement || transporting ;; Bringing || crashed ;; Catastrophe
crash ;; Catastrophe
crash ;; Catastrophe
remains ;; Presence
prevented ;; Preventing_or_letting
damaged ;; Damaging
held ;; Hold
confirmed ;; Check || noted ;; Know || reopened ;; Openness || held ;; Hold
carried out ;; Action || given ;; Giving || dealt ;; Resolve_problem || dealt ;; Resolve_problem
ensued ;; Coming_to_be
massacres ;; Killing || carried out ;; Action
derives ;; Getting
emerged ;; Coming_to_be
aimed ;; Aiming || defined ;; Statement
ascension ;; Change_of_leadership || remained ;; Presence
support ;; Supporting || recruited ;; Becoming_a_member
began ;; Process_start
detained ;; Arrest || following ;; Self_motion || marched ;; Self_motion
killed ;; Killing
classified ;; Check
took place ;; Process_start
capture ;; Conquering
joint ;; Collaboration || attacked ;; Attack
command ;; Control || managed ;; Control || carried out ;; Action
allowing ;; Preventing_or_letting || decimated ;; Destroying || stabilized ;; Cause_change_of_strength
aimed ;; Aiming
conducted ;; Action
commenced ;; Process_start
carried out ;; Action
reached ;; Arriving || capture ;; Conquering || dubbed ;; Name_conferral
led to ;; Causation
conducted ;; Action || Operation ;; Military_operation || included ;; Cause_to_be_included || joint ;; Collaboration
carried out ;; Action || taking part ;; Dispersal || forces ;; Military_operation
aim ;; Aiming || target ;; Aiming
included ;; Cause_to_be_included
consisted ;; Cause_to_be_included
comprised ;; Cause_to_be_included
formed ;; Coming_to_be || joint ;; Collaboration
forms ;; Coming_to_be
led to ;; Causation
avoided ;; Preventing_or_letting || forces ;; Military_operation || focusing ;; Aiming
withdrew ;; Escaping
moved ;; Motion
contributed ;; Giving
swayed ;; Convincing || Encouraged ;; Influence || repelled ;; Defending
occurred ;; Presence || defected ;; Escaping
rejoined ;; Becoming_a_member || forces ;; Military_operation
left ;; Departing
forces ;; Military_operation
strengthened ;; Cause_change_of_strength || defections ;; Escaping
ruled ;; Control
war ;; Hostile_encounter || revolution ;; Change_of_leadership || fought ;; Hostile_encounter
began ;; Process_start || war ;; Hostile_encounter || dethroned ;; Change_of_leadership || carried out ;; Action || revolutionary ;; Change_of_leadership || crowned ;; Change_of_leadership || declared ;; Expressing_publicly || command ;; Control
escaped ;; Escaping || escalating ;; Cause_change_of_position_on_a_scale || support ;; Supporting || war ;; Hostile_encounter
gave ;; Giving || supported ;; Supporting || supplied ;; Supply || supplied ;; Supply || aid ;; Assistance || support ;; Supporting
involved ;; Participation
supported ;; Supporting
war ;; Hostile_encounter || conferences ;; Communication || sank ;; Causation
found ;; Know || began ;; Process_start || war ;; Hostile_encounter || involvement ;; Participation || maintain ;; Preserving || pull ;; Hold
approached ;; Arriving || surprising ;; Change_sentiment || removal ;; Removing || supported ;; Supporting || shift ;; Change || resulted in ;; Causation
fled ;; Escaping || leaving ;; Causation || control ;; Control || headed ;; Motion || resigned ;; Escaping
became ;; Becoming || war ;; Hostile_encounter || turning ;; Becoming
siege ;; Besieging || remaining ;; Presence || keeping ;; Temporary_stay || control ;; Control || lifted ;; Process_end
talks ;; Telling || ceasefire ;; Process_end || came into effect ;; Change || continued ;; Motion || recognized ;; Statement
war ;; Hostile_encounter || refer ;; Name_conferral
disastrous ;; Catastrophe || War ;; Hostile_encounter || wrote ;; Statement || dubbed ;; Name_conferral
killing ;; Killing
abandon ;; GiveUp || recaptured ;; Conquering || carried out ;; Action || ordered ;; Request
killed ;; Killing || burned ;; Bodily_harm || cause ;; Causation
left ;; GiveUp
dynamited ;; Destroying
left ;; Departing || secured ;; Assistance
toured ;; Traveling || held ;; Defending || calling ;; Name_conferral
persuaded ;; Convincing
charged ;; Judgment_communication
killed ;; Killing || ruled ;; Legal_rulings || stated ;; Statement || produced ;; Manufacturing
exploded ;; Attack
died ;; Death
carrying ;; Carry_goods
developed ;; Coming_to_be || decided ;; Deciding || taking off ;; Departing || return ;; Arriving
land ;; Motion || elected ;; Choosing
destroyed ;; Destroying || killing ;; Killing
cause ;; Causation || determined ;; Deciding
Campaign ;; Military_operation
recapture ;; Conquering
began ;; Process_start || Campaigns ;; Military_operation || achieved ;; Achieve || advancing ;; Cause_to_make_progress
deployed ;; Arranging || capturing ;; Conquering || prevent ;; Preventing_or_letting
detached ;; Departing || besieging ;; Besieging
attacks ;; Attack || disrupted ;; Hindering || carrying ;; Carry_goods
forced ;; Motion
decided ;; Deciding || withdrawn ;; Escaping || evacuate ;; Escaping || departing ;; Departing
started ;; Process_start || started ;; Process_start || departure ;; Departing || funeral ;; Social_event
remained ;; Presence || carried ;; Carry_goods || taken ;; Bringing
lowered ;; Cause_change_of_position_on_a_scale
finished ;; Process_end || held ;; Defending || ceremony ;; Social_event
viewing ;; Perception_active || ceremony ;; Social_event || attended ;; Participation
watched ;; Perception_active || worldwide ;; Dispersal || making it ;; Causation || events ;; Social_event || event ;; Social_event
carried out ;; Military_operation
establish ;; Building
released ;; Publishing || crashed ;; Catastrophe
captured ;; Conquering || held ;; Defending || arrived at ;; Arriving
surrendered ;; Surrendering
frustrated ;; Influence
aided ;; Assistance
carried out ;; Action
overthrew ;; Change_of_leadership
abandoning ;; GiveUp || emerging ;; Coming_to_be || inspired ;; Influence || signed ;; Sign_agreement || invited ;; Request
offered ;; Giving || declined ;; Cause_change_of_position_on_a_scale
became ;; Becoming || remained ;; Presence || death ;; Death
attack ;; Attack
discovered ;; Know || spotted ;; Know || alerted ;; Warning
caused ;; Causation || casualties ;; Catastrophe || disarmed ;; Removing
arrested ;; Arrest
arrested ;; Arrest
bombing ;; Terrorism
intent ;; Aiming || kill ;; Killing || said ;; Statement
charged ;; Judgment_communication || explosives ;; Attack
arrested ;; Arrest
financed ;; Supply || said ;; Statement
terrorism ;; Terrorism || sentenced ;; Judgment_communication || guilty ;; Committing_crime
killing ;; Killing || injuring ;; Bodily_harm || bombings ;; Terrorism || occurred ;; Presence
bombings ;; Terrorism || came ;; Arriving
triggered ;; Causation
terrorist ;; Terrorism || believe ;; Communication || attacks ;; Attack
condolences ;; Statement || condemned ;; Judgment_communication || sent ;; Sending
appealed ;; Request || help ;; Assistance || relief ;; Rescuing || rescue ;; Rescuing
blasts ;; Attack || moved ;; Cause_change_of_position_on_a_scale
bombings ;; Terrorism || occurred ;; Coming_to_be || detonated ;; Terrorism
targeted ;; Aiming
killed ;; Killing
wounded ;; Bodily_harm
detonated ;; Attack
killed ;; Killing
evacuated ;; Escaping || exploded ;; Attack
became ;; Becoming || attacks ;; Attack || siege ;; Besieging
thrown ;; Motion
died ;; Death || explosion ;; Attack
detonated ;; Terrorism || injured ;; Bodily_harm || said ;; Statement
misused ;; Using || bombings ;; Terrorism || passing ;; Motion_directional || contributed ;; Causation
conducted ;; Action || operation ;; Military_operation || ambush ;; Terrorism
obliterated ;; Destroying || alleged ;; Statement
targeted ;; Aiming || explosions ;; Destroying || fire ;; Use_firearm
received ;; Receiving || support ;; Supporting
killed ;; Killing || reports ;; Reporting || suggest ;; Statement
tightened ;; Cause_change_of_strength || operation ;; Military_operation
terrorist ;; Terrorism || attacks ;; Attack || targeting ;; Aiming || occurred ;; Presence
wounded ;; Bodily_harm || killed ;; Killing
occurred ;; Presence || detonated ;; Motion || attack ;; Attack || bomb ;; Attack
killed ;; Killing || wounded ;; Bodily_harm || succumb ;; Death
asked ;; Request || killed ;; Killing || occurred ;; Presence || found ;; Know || retrieve ;; Hold || triggering ;; Causation || wounded ;; Bodily_harm || attack ;; Attack
fired ;; Use_firearm || killed ;; Killing || injured ;; Bodily_harm || attack ;; Attack
campaigns ;; Military_operation || raids ;; Attack || firebombing ;; Attack || war ;; Hostile_encounter
conducted ;; Action || raid ;; Attack || destructive ;; Destroying || bombing ;; Attack
destroyed ;; Destroying || dead ;; Death || leaving ;; Causation
raid ;; Attack || mounted ;; Military_operation
began ;; Process_start || deployed ;; Arranging || bombing ;; Attack || bombing ;; Attack || service ;; Supply || bomber ;; Attack || entered ;; Arriving
began ;; Process_start || raids ;; Attack || lasted ;; Process_end
firebombing ;; Terrorism || spread ;; Dispersal || cut ;; Cause_change_of_position_on_a_scale
called ;; Statement || loss ;; Earnings_and_losses || raid ;; Attack || war ;; Hostile_encounter || due ;; Causation || crime ;; Committing_crime || targeting ;; Aiming
crashed ;; Attack || suicide attack ;; Terrorism || described ;; Statement
incident ;; Attack || died ;; Death
identified ;; Check
injured ;; Bodily_harm
attack ;; Attack || terrorist ;; Terrorism || described ;; Statement || incident ;; Catastrophe
attacks ;; Attack || warned ;; Warning || movement ;; Social_event || party ;; Social_event || claimed ;; Statement
exploded ;; Attack || occurred ;; Coming_to_be || bombings ;; Terrorism || suicide ;; Terrorism
explosion ;; Attack || injured ;; Bodily_harm || dead ;; Killing || hit ;; Attack || heard ;; Perception_active
explosion ;; Attack || targeted ;; Aiming
bombings ;; Terrorism || attack ;; Terrorism || claimed ;; Statement || took place ;; Process_start
bombings ;; Terrorism
injured ;; Bodily_harm || killed ;; Killing || attack ;; Terrorism || killed ;; Killing || attack ;; Terrorism || injured ;; Bodily_harm
claiming ;; Statement || attacks ;; Terrorism
meeting ;; Social_event || attacks ;; Terrorism || held ;; Hold || condemn ;; Judgment_communication
planned ;; Arranging || attack ;; Terrorism
crashed ;; Catastrophe || takeoff ;; Self_motion || exploded ;; Damaging
died ;; Death
arriving ;; Arriving || terrorist ;; Terrorism || cause ;; Causation || traveled ;; Traveling
initiated ;; Process_start || joint ;; Collaboration || force ;; Hostile_encounter
announced ;; Expressing_publicly || found ;; Know || closed ;; Self_motion
ending ;; Process_end || investigation ;; Criminal_investigation
cause ;; Causation || explosion ;; Attack
determined ;; Deciding
found ;; Know
remarked ;; Statement || known ;; Know || called ;; Name_conferral
developed ;; Coming_to_be || investigation ;; Criminal_investigation || prevent ;; Preventing_or_letting
bombing ;; Terrorism || bombing ;; Terrorism || reported ;; Reporting || massacre ;; Violence || carried out ;; Attack
carrying ;; Bringing || leave ;; Departing
bombing ;; Terrorism || killed ;; Killing || wounded ;; Bodily_harm
killed ;; Killing || wounded ;; Bodily_harm || bombing ;; Terrorism
suicide bombing ;; Terrorism || dead ;; Death || left ;; Causation || reported ;; Reporting || losing ;; Death || fighting ;; Hostile_encounter
suicide bombing ;; Terrorism || response ;; Response || followed ;; Manufacturing || claimed ;; Statement || attack ;; Attack || attack ;; Terrorism || strikes ;; Attack
helped ;; Assistance || arrested ;; Arrest || attack ;; Terrorism || used ;; Using || construct ;; Creating
planned ;; Arranging || attack ;; Terrorism
targeting ;; Aiming || bombing ;; Terrorism
killed ;; Killing || attack ;; Terrorism
killed ;; Killing || believed ;; Know
known ;; Know || reported ;; Reporting || took ;; Conquering
take ;; Change
become ;; Becoming || guess ;; Coming_to_believe
warn ;; Warning || saying ;; Communication || said ;; Communication || known ;; Know
told ;; Telling || took ;; Action || co-ordinate ;; Arranging
terrorist ;; Terrorism || attack ;; Attack || bombing ;; Attack || housing ;; Building || complex ;; Cause_to_amalgamate
used ;; Using || watch ;; Perception_active || assigned ;; Choosing
deployed ;; Arranging || rescue ;; Rescuing || deployed ;; Arranging || building ;; Building || structure ;; Building || housing ;; Building || bomb ;; Use_firearm || detonated ;; Attack
killed ;; Killing || wounded ;; Bodily_harm
statement ;; Statement || named ;; Name_conferral
found ;; Know || attack ;; Attack || orchestrating ;; Arranging
bombing ;; Terrorism || took place ;; Process_start
killed ;; Killing || injured ;; Bodily_harm
statement ;; Statement || giving ;; Giving || reconstruct ;; Building
travelled ;; Motion || participate ;; Participation || staying ;; Temporary_stay || preparing ;; Process_start || cross ;; Motion_directional
identified ;; Check || suicide attack ;; Terrorism || caused ;; Causation || detonated ;; Attack
claimed ;; Statement
made ;; Action
links ;; Connect
blamed ;; Judgment_communication
bombing ;; Terrorism || resulted in ;; Causation || engaging ;; Action
led to ;; Causation || launching ;; Military_operation || airstrikes ;; Attack
began ;; Process_start
met with ;; Response || condemnation ;; Judgment_communication || tighten ;; Cause_change_of_strength
criticised ;; Judgment_communication || scrutiny ;; Scrutiny || endorsing ;; Judgment_communication
killed ;; Killing || wounded ;; Bodily_harm || car bomb ;; Terrorism || explosion ;; Attack
claimed ;; Statement
broke ;; Bodily_harm
detonated ;; Terrorism || crashing ;; Attack
caused ;; Causation || bomb ;; Terrorism
said ;; Telling
damaged ;; Damaging || collapsed ;; Destroying
removing ;; Removing || dead ;; Death || rushing ;; Motion || wounded ;; Bodily_harm
brought ;; Bringing || move ;; Removing || ensure ;; Preventing_or_letting || trapped ;; Preventing_or_letting
killed ;; Killing || injured ;; Bodily_harm || blast ;; Attack
said ;; Communication
attack ;; Terrorism
described ;; Statement || attack ;; Terrorism
alleged ;; Statement || offered ;; Giving
appeared ;; Presence || remanded ;; Arrest || charged ;; Judgment_communication || organising ;; Arranging || hearing ;; Legal_rulings
killed ;; Killing || injured ;; Bodily_harm || bombing ;; Terrorism
destroyed ;; Destroying || fired ;; Use_firearm || attack ;; Terrorism
damage ;; Damaging || suffered ;; Bodily_harm
say ;; Statement
fired ;; Use_firearm || emerged ;; Presence
breach ;; Committing_crime || detonated ;; Attack
provided ;; Supply || claimed ;; Statement
began ;; Process_start || ended ;; Process_end || Festival ;; Social_event || concert ;; Social_event || Tour ;; Traveling || tour ;; Traveling
concert ;; Social_event || shot ;; Use_firearm || tour ;; Traveling || Tour ;; Traveling || re-released ;; Publishing
focus ;; Aiming || tour ;; Traveling || consisted ;; Cause_to_be_included
effects ;; Influence || appearances ;; Presence || tour ;; Traveling || show ;; Social_event
inspired ;; Influence || Tour ;; Traveling || Tour ;; Traveling || tour ;; Traveling
opening ;; Openness || tour ;; Traveling || took place ;; Process_start
opening ;; Openness || tour ;; Traveling
concluding ;; Process_end || final ;; Process_end || encompassing ;; Expansion || tour ;; Traveling
shows ;; Social_event
received ;; Receiving || receiving ;; Receiving || praise ;; Judgment_communication || performances ;; Hold || acclaim ;; Judgment_communication || tour ;; Traveling || show ;; Social_event
revenue ;; Earnings_and_losses || performances ;; Hold || grossed ;; Earnings_and_losses
performances ;; Hold || festival ;; Social_event || concert ;; Social_event || tour ;; Traveling || show ;; Social_event
became ;; Becoming || confirm ;; Check || appearance ;; Presence || sold ;; Commerce_sell
concert ;; Social_event || record-breaking ;; Social_event || Festival ;; Social_event || Tour ;; Traveling || returned ;; Arriving || show ;; Social_event
began ;; Process_start || organised ;; Arranging || mark ;; Recording || event ;; Social_event || concert ;; Social_event || War ;; Hostile_encounter || featuring ;; Cause_to_be_included || tour ;; Traveling
hosted ;; Social_event || Championship ;; Competition || event ;; Competition || held ;; Hold
matches ;; Competition || played ;; Competition
carrying ;; Bringing || advancing ;; Cause_to_make_progress
tournament ;; Competition || final ;; Competition || defeating ;; Conquering || defeating ;; Conquering
tournament ;; Competition || qualified ;; Getting
finished ;; Process_end || play ;; Participation || Tournaments ;; Competition
sold ;; Commerce_sell
create ;; Creating || championship ;; Competition || extends ;; Expansion
took place ;; Process_start || Grand ;; Competition || Grand ;; Competition
found ;; Know || shown ;; Presence || race ;; Competition || auction ;; Commerce_sell
caused ;; Causation || fall ;; Motion_directional
failed ;; Causation || finish ;; Process_end
race ;; Competition || ridden ;; Body_movement
victory ;; Earnings_and_losses
race ;; Competition || followed ;; Self_motion || finish ;; Process_end
held ;; Hold
closed ;; Self_motion || saw ;; Motion
expanded ;; Expansion
attended ;; Participation
increased ;; Cause_change_of_position_on_a_scale || sold ;; Commerce_sell
launched ;; Process_start
ran ;; Self_motion || attended ;; Participation
held ;; Giving || wwdc ;; Social_event || conference ;; Social_event || conference ;; Social_event
uses ;; Using || event ;; Social_event || showcase ;; Social_event
participate ;; Participation || attend ;; Participation || sessions ;; Social_event
began ;; Process_start || WWDC ;; Social_event
moved ;; Motion || became ;; Becoming || sold ;; Commerce_sell || conference ;; Social_event || event ;; Social_event
WWDC ;; Social_event || returned ;; Arriving
held ;; Hold || occurred ;; Presence || concert ;; Social_event
raised ;; Creating
watched ;; Perception_active
sold ;; Commerce_sell || sold ;; Commerce_sell || issued ;; Creating
contributing ;; Giving
made ;; Creating || introduced ;; Statement || appearance ;; Presence
began ;; Process_start || shown ;; Presence
started ;; Process_start || represent ;; Presence || waving ;; Action
streamed ;; Creating || live ;; Being_in_operation
broadcast ;; Expressing_publicly
support ;; Supporting || launched ;; Process_start || tour ;; Traveling
Selling ;; Commerce_sell || tour ;; Traveling
commenced ;; Process_start || tour ;; Traveling
inspired ;; Influence
presented ;; Presence || protect ;; Preventing_or_letting
composed ;; Collaboration
tour ;; Traveling || concluded ;; Process_end
organised ;; Arranging || concerts ;; Social_event
sold ;; Commerce_sell || held ;; Hold
represented ;; Change_sentiment
moved ;; Motion || used ;; Using || acquired ;; Getting
acquired ;; Getting || announced ;; Expressing_publicly || selling ;; Commerce_sell
said ;; Communication || seek ;; Motion
perform ;; Hold
tour ;; Traveling || Tour ;; Traveling || concert ;; Social_event
support ;; Supporting || kicked off ;; Process_start
tour ;; Traveling || embarked ;; Process_start || take ;; Bringing
selling ;; Commerce_sell || tour ;; Traveling || tickets ;; Preventing_or_letting || embarked ;; Process_start
viewed ;; Perception_active || sales ;; Commerce_sell || issue ;; Creating
broke ;; Destroying || tour ;; Traveling || record ;; Recording
Tour ;; Traveling
released ;; Publishing || Tour ;; Traveling || concert ;; Social_event || tour ;; Traveling || support ;; Supporting
tour ;; Traveling || see ;; Perception_active || playing ;; Competition
passed ;; Motion_directional || playing ;; Competition || presenting ;; Presence || concerts ;; Social_event
entitled ;; Name_conferral || playing ;; Competition || seated ;; Placing
managed ;; Control || improvement ;; Cause_to_make_progress || seen ;; Perception_active || tour ;; Traveling || presenting ;; Presence || concert ;; Social_event || selling ;; Commerce_sell || tickets ;; Preventing_or_letting || seated ;; Placing || concerts ;; Social_event
managed ;; Control || play ;; Competition || festivals ;; Social_event || festival ;; Social_event || open ;; Openness || open ;; Openness
released ;; Publishing || let ;; Preventing_or_letting || concert ;; Social_event || burn ;; Bodily_harm || concert ;; Social_event
concert ;; Social_event || tours ;; Traveling || embarked ;; Process_start
tour ;; Traveling || Tour ;; Traveling || debuted ;; Process_start
bringing ;; Bringing || Tour ;; Traveling || Tour ;; Traveling || tour ;; Traveling
tour ;; Traveling || attracting ;; Influence
performed ;; Hold || tour ;; Traveling || finale ;; Process_end
departure ;; Departing || concert ;; Social_event || tour ;; Traveling || titled ;; Name_conferral || remaining ;; Presence || tour ;; Traveling || held ;; Hold
tour ;; Traveling || concert ;; Social_event || mobilized ;; Influence || held ;; Hold
record ;; Recording || concert ;; Social_event || tour ;; Traveling || concert ;; Social_event || tour ;; Traveling || broke ;; Conquering || visit ;; Traveling
tour ;; Traveling || finale ;; Process_end || headline ;; Reporting || pushing ;; Motion
held ;; Hold || sales ;; Commerce_sell || Tour ;; Traveling || concert ;; Social_event || tour ;; Traveling || grossing ;; Earnings_and_losses
bringing in ;; Bringing || tour ;; Traveling || Tour ;; Traveling || held ;; Hold
Tour ;; Traveling || concert ;; Social_event || tour ;; Traveling || celebrated ;; Social_event || debut ;; Process_start
attended ;; Participation
Tour ;; Traveling || concert ;; Social_event || service ;; Supply || enlist ;; Becoming_a_member
begin ;; Process_start || service ;; Supply || tours ;; Traveling || tour ;; Traveling || tour ;; Traveling || completion ;; Process_end || headlined ;; Reporting
perform ;; Hold || records ;; Recording || concerts ;; Social_event || tour ;; Traveling || concert ;; Social_event || helm ;; Placing || headline ;; Reporting || set ;; Placing || act ;; Action
performed ;; Hold || concerts ;; Social_event || mobilization ;; Influence
becoming ;; Becoming || records ;; Recording || concert ;; Social_event || tour ;; Traveling || broke ;; Destroying || hold ;; Hold || mobilizing ;; Influence || grossing ;; Earnings_and_losses || play ;; Action
competition ;; Competition || used ;; Using || final ;; Competition
began ;; Process_start || played ;; Competition || final ;; Competition
competition ;; Competition
held ;; Hold
represented ;; Presence
auction ;; Commerce_sell || held ;; Hold || auction ;; Commerce_sell || took place ;; Process_start
began ;; Process_start || strike ;; Attack || typhoon ;; Catastrophe
formed ;; Coming_to_be
moving ;; Motion || intensified ;; Cause_change_of_strength || becoming ;; Becoming || storm ;; Catastrophe
developed ;; Coming_to_be || strengthening ;; Cause_change_of_strength || facilitated ;; Cause_to_make_progress || reached ;; Achieve
began ;; Process_start || turning ;; Becoming
passed ;; Motion_directional || recorded ;; Recording || produced ;; Creating
maintain ;; Preserving || landfall ;; Self_motion || made ;; Creating
became ;; Becoming || persisted ;; Presence
affected ;; Influence
damaged ;; Damaging || left ;; Causation
caused ;; Causation || record ;; Recording || setting ;; Placing
died ;; Death || struck ;; Attack
caused ;; Causation || blocked ;; Hindering || canceled ;; Change_event_time
deaths ;; Death || damage ;; Damaging
moved ;; Motion || Damage ;; Damaging
records ;; Recording || produced ;; Creating || setting ;; Creating || broke ;; Destroying
reached ;; Achieve
damage ;; Damaging || exports ;; Commerce_sell
destroyed ;; Destroying || damaged ;; Damaging || leaving ;; Causation
lost ;; Earnings_and_losses || damage ;; Damaging || occurred ;; Coming_to_be
killed ;; Killing || damage ;; Damaging
tour ;; Traveling
said ;; Communication || entitled ;; Name_conferral || extended ;; Cause_change_of_strength || playing ;; Social_event
restricting ;; Control
announced ;; Expressing_publicly || extend ;; Cause_change_of_strength || rename ;; Name_conferral || taking place ;; Process_start
visited ;; Traveling || returning ;; Motion
appeared ;; Presence
sold ;; Commerce_sell || sold ;; Commerce_sell
announced ;; Expressing_publicly
announced ;; Expressing_publicly || added ;; Cause_change_of_position_on_a_scale
added ;; Cause_change_of_position_on_a_scale
support ;; Supporting || Tour ;; Traveling || tour ;; Traveling
concluding ;; Process_end || beginning ;; Process_start || shows ;; Social_event || tour ;; Traveling || played ;; Participation
visited ;; Traveling || tour ;; Traveling
became ;; Becoming || earning ;; Earnings_and_losses || selling ;; Commerce_sell || tour ;; Traveling || ranking ;; Placing
earning ;; Earnings_and_losses || ranked ;; Placing
Awards ;; Social_event || award ;; Award
marketed ;; Commerce_sell || operated ;; Being_in_operation
used ;; Using || crashed ;; Catastrophe || take off ;; Departing
used ;; Using || takeoff ;; Self_motion || assigned ;; Deciding
takeoff ;; Self_motion || become ;; Becoming || causing ;; Causation
crashed ;; Catastrophe || killing ;; Killing
crash ;; Catastrophe || accident ;; Catastrophe || flying ;; Being_in_operation
cause ;; Causation || crash ;; Catastrophe || concluded ;; Coming_to_believe || crash ;; Attack
festival ;; Social_event || held ;; Hold
festival ;; Social_event || held ;; Hold
announced ;; Expressing_publicly || event ;; Social_event || tour ;; Traveling
support ;; Supporting
securing ;; Causation
debuted ;; Process_start || expanded ;; Expansion
added ;; Cause_to_be_included
festival ;; Social_event || taken out ;; Removing || returned ;; Arriving
dissolved ;; Destroying || event ;; Social_event || sold ;; Commerce_sell
announced ;; Expressing_publicly || festival ;; Social_event || festival ;; Social_event || event ;; Social_event || option ;; Choosing || left ;; Departing || open ;; Openness || attained ;; Getting || cancellation ;; Removing
event ;; Social_event || plans ;; Arranging || event ;; Social_event || held ;; Hold
Battle ;; Hostile_encounter || engagements ;; Hostile_encounter || Uprising ;; Violence
fought ;; Hostile_encounter || control ;; Control || assaulting ;; Attack
battle ;; Hostile_encounter || losses ;; Earnings_and_losses || hands ;; Conquering || suffered ;; Bodily_harm
replenish ;; Recovering
saved ;; Rescuing || holding ;; Defending || repelled ;; Defending || fighting ;; Hostile_encounter || burning ;; Destroying
saved ;; Rescuing
damage ;; Damaging || hurricane ;; Catastrophe || cyclone ;; Catastrophe || wrought ;; Causation
named ;; Name_conferral || storm ;; Catastrophe
cyclone ;; Catastrophe || Originating ;; Coming_to_be || development ;; Cause_change_of_position_on_a_scale || tracked ;; Self_motion || intensified ;; Expansion
began ;; Process_start || hurricane ;; Catastrophe || reaching ;; Achieve || curve ;; Motion_directional || bringing ;; Bringing
began ;; Process_start || hurricane ;; Catastrophe || weaken ;; Cause_change_of_strength || accelerate ;; Cause_change_of_position_on_a_scale
cyclone ;; Catastrophe || cyclone ;; Catastrophe || grazed ;; Bodily_harm || transitioning ;; Self_motion
damage ;; Damaging || disrupted ;; Damaging || inflicted ;; Damaging || cyclone ;; Catastrophe || landfall ;; Arriving
hurricane ;; Catastrophe || brought ;; Bringing || felled ;; Motion_directional
damage ;; Damaging
damage ;; Damaging
cyclone ;; Catastrophe || tracked ;; Self_motion || passing ;; Motion_directional
became ;; Becoming || hurricane ;; Catastrophe || encountered ;; Confronting_problem
rescued ;; Rescuing || died ;; Death || perished ;; Death
Beginning ;; Process_start || hurricane ;; Catastrophe || rainfall ;; Catastrophe || tracked ;; Self_motion || producing ;; Creating
flooded ;; Damaging || caused ;; Causation
killed ;; Killing
caused ;; Causation || hurricane ;; Catastrophe || fatalities ;; Death
fought ;; Hostile_encounter || forces ;; Military_operation || Battle ;; Hostile_encounter
forces ;; Military_operation
forces ;; Military_operation
took ;; Conquering
started ;; Process_start || attack ;; Attack
withdrawal ;; Escaping || save ;; Rescuing || try ;; Self_motion || fighting ;; Hostile_encounter
forces ;; Military_operation || battle ;; Hostile_encounter || lost ;; Death
lost ;; Death || forces ;; Military_operation
storm ;; Catastrophe
organized ;; Arranging || supported ;; Supporting || Forming ;; Coming_to_be
became ;; Becoming || storm ;; Catastrophe || deepening ;; Cause_change_of_strength || attain ;; Getting || hurricane ;; Catastrophe || underwent ;; Change
hurricane-force ;; Attack || rose ;; Cause_change_of_position_on_a_scale
ensued ;; Coming_to_be || peaked ;; Process_end
began ;; Process_start || storm ;; Catastrophe || reaching ;; Arriving || impact ;; Influence
downgraded ;; Cause_change_of_position_on_a_scale || hurricane ;; Catastrophe || becoming ;; Becoming || weakened ;; Cause_change_of_strength || storm ;; Catastrophe
decreasing ;; Cause_change_of_position_on_a_scale
passing ;; Motion_directional || weakened ;; Cause_change_of_strength || degenerated ;; Change
produced ;; Manufacturing || approach ;; Arriving || weakening ;; Cause_change_of_strength
recorded ;; Recording || causing ;; Causation || flooding ;; Motion
helped ;; Assistance || increasing ;; Cause_change_of_position_on_a_scale
produced ;; Manufacturing || rescues ;; Rescuing || flooding ;; Motion || resulted in ;; Causation || storm ;; Catastrophe || resulted in ;; Causation
impacts ;; Influence || caused ;; Causation
Cyclone ;; Catastrophe || powerful ;; Influence || strongest ;; Influence
developed ;; Coming_to_be
drifted ;; Motion || moving ;; Motion || turned ;; Motion || cyclone ;; Catastrophe
prevented ;; Preventing_or_letting || shear ;; Destroying || interaction ;; Collaboration || low ;; Cause_change_of_position_on_a_scale
moved ;; Motion || moved ;; Motion || storm ;; Catastrophe || curved ;; Motion
became ;; Becoming || moved ;; Motion
passed ;; Motion_directional || deepening ;; Cause_change_of_strength
estimated ;; Statement
making it ;; Causation
caused ;; Causation || weaken ;; Cause_change_of_strength || cyclone ;; Catastrophe || advisories ;; Warning || discontinued ;; Process_end
cyclone ;; Catastrophe || affected ;; Influence || produced ;; Creating
moving ;; Motion || damaged ;; Damaging || downed ;; Motion
cyclone ;; Catastrophe || gusted ;; Motion || making it ;; Causation || affect ;; Influence
storm ;; Catastrophe || search ;; Rescuing || affected ;; Influence
murdered ;; Killing || shooting ;; Use_firearm
survived ;; Rescuing
arrested ;; Arrest
targeted ;; Aiming
found ;; Know || convicted ;; Criminal_investigation
sentenced ;; Judgment_communication
charged ;; Judgment_communication
pleaded ;; Request || sentenced ;; Judgment_communication
espoused ;; Statement
triggered ;; Causation
fought ;; Hostile_encounter || War ;; Hostile_encounter
took place ;; Process_start || engagement ;; Hostile_encounter
caused ;; Causation || damaged ;; Damaging || sank ;; Motion || explosion ;; Attack
killed ;; Killing || killed ;; Killing || explosion ;; Attack || captured ;; Conquering
rescued ;; Rescuing || killed ;; Killing
returned ;; Arriving
shootings ;; Use_firearm || took place ;; Process_start
killing ;; Killing || executing ;; Action || fired ;; Use_firearm || injuring ;; Bodily_harm
assist ;; Assistance || shot ;; Use_firearm
brought in ;; Bringing
siege ;; Besieging || lasting ;; Presence || fire ;; Use_firearm || developed ;; Cause_change_of_position_on_a_scale || retrieve ;; Rescuing
assistance ;; Assistance
found ;; Know || dead ;; Death
campaigns ;; Military_operation || Battle ;; Hostile_encounter
Battle ;; Hostile_encounter || war ;; Hostile_encounter || pursued ;; Self_motion
met ;; Come_together || battle ;; Hostile_encounter
held ;; Hold || met ;; Come_together || battle ;; Hostile_encounter || approaching ;; Arriving || avoid ;; Preventing_or_letting || seized ;; Hold || offered ;; Giving
drew ;; Motion || began ;; Process_start || slain ;; Killing || saved ;; Rescuing || fight ;; Hostile_encounter || got away ;; Escaping || battle ;; Hostile_encounter || overwhelmed ;; Destroying || captured ;; Conquering
restore ;; Recovering || casualties ;; Death || defeat ;; Conquering || effect ;; Influence
besieged ;; Besieging || went ;; Motion
crashed ;; Catastrophe || flown ;; Motion || flight ;; Motion
due ;; Causation || support ;; Supporting
caused ;; Causation || losing ;; Earnings_and_losses || overturn ;; Self_motion
survived ;; Rescuing
write-off ;; Causation
installed ;; Placing
killing ;; Killing
explosion ;; Catastrophe || reported ;; Reporting || caused ;; Causation || shot ;; Use_firearm || ignited ;; Causation
explosion ;; Catastrophe || killed ;; Killing || killed ;; Killing || burns ;; Bodily_harm || injuries ;; Bodily_harm
rescued ;; Rescuing || effects ;; Influence || died ;; Death
escaped ;; Escaping
began ;; Process_start
rescued ;; Rescuing || brought ;; Bringing
held up ;; Hold
began ;; Process_start
rescued ;; Rescuing
suffering ;; Bodily_harm
rescued ;; Rescuing
arrested ;; Arrest
slavery ;; Committing_crime || servitude ;; Committing_crime || investigated ;; Criminal_investigation
centred ;; Placing
decided ;; Deciding || move ;; Motion || raid ;; Attack
became ;; Becoming || control ;; Control || intensified ;; Cause_change_of_strength
rescued ;; Rescuing
revealed ;; Check
fought ;; Hostile_encounter || battle ;; Hostile_encounter || operation ;; Military_operation
captured ;; Conquering || held ;; Conquering
attacks ;; Attack || rushed ;; Self_motion || rescue ;; Rescuing || hearing ;; Know || seize ;; Control
arriving at ;; Arriving || said ;; Communication || dispatched ;; Sending || alligator ;; Hold || engage ;; Hostile_encounter
commanding ;; Control || assaulted ;; Attack
abandoning ;; GiveUp || fight ;; Hostile_encounter || force ;; Causation || inflict ;; Causation || casualties ;; Death
ceased ;; Process_end || recover ;; Recovering || pursuit ;; Self_motion || held ;; Hold || wounded ;; Bodily_harm
invasion ;; Attack || took place ;; Process_start
ended ;; Process_end || captured ;; Conquering || massacring ;; Killing || burned ;; Bodily_harm
captured ;; Conquering || invasion ;; Attack || regarded ;; Perception_active
captured ;; Conquering || crossing ;; Motion_directional
besieged ;; Besieging
murdered ;; Killing || breaking ;; Destroying || fled ;; Escaping
massacred ;; Killing || saved ;; Rescuing
used ;; Using || threw ;; Motion
captured ;; Conquering || headed ;; Self_motion
capture ;; Conquering || Battle ;; Hostile_encounter || took place ;; Process_start
captured ;; Conquering || massacring ;; Killing || razing ;; Destroying || sieged ;; Besieging
tournament ;; Competition || Cup ;; Competition || played ;; Participation
came ;; Arriving || preceding ;; Presence || Cup ;; Competition || Cup ;; Competition || Hosted ;; Social_event
tournament ;; Competition || organised ;; Arranging || matches ;; Competition || played ;; Participation
tournament ;; Competition || final ;; Process_end || defeating ;; Earnings_and_losses
tournament ;; Competition || match ;; Competition || last ;; Process_end || failing ;; Earnings_and_losses || win ;; Getting || defeated ;; Earnings_and_losses || placed ;; Placing
tournament ;; Competition
tournaments ;; Competition || forced ;; Request || failing ;; Earnings_and_losses || invited ;; Participation
tournament ;; Competition || led ;; Causation
scored ;; Getting || taken ;; Action || placed ;; Placing
received ;; Receiving || donated ;; Giving
called ;; Name_conferral
spread ;; Dispersal || involve ;; Participation || found ;; Know
repressed ;; Hindering
wounded ;; Bodily_harm
supported ;; Supporting || condemned ;; Judgment_communication
started ;; Process_start
elected ;; Change_of_leadership
elected ;; Change_of_leadership
found ;; Know
supported ;; Supporting || included ;; Cause_to_be_included
spread ;; Dispersal || joined ;; Becoming_a_member
began ;; Process_start || offered ;; Giving
defied ;; Agree_or_refuse_to_act || ignored ;; Response
face ;; Confronting_problem
took place ;; Process_start
hoisted ;; Placing
sent ;; Sending || settle ;; Resolve_problem
end ;; Process_end || echoed ;; Communication
gave way ;; Giving
made ;; Action
reinstated ;; Recovering
invasion ;; Attack
opposed ;; Agree_or_refuse_to_act
ended ;; Process_end
decided ;; Deciding || turn ;; Change
declared ;; Expressing_publicly || managed ;; Preventing_or_letting
requested ;; Request || assist ;; Assistance
advanced ;; Cause_to_make_progress || marched ;; Self_motion
defended ;; Defending || taken ;; Motion
arrived ;; Arriving || withstand ;; Defending
lifted ;; Process_end || prepared ;; GetReady
approached ;; Self_motion
took ;; Motion || harried ;; Damaging
seize ;; Arrest || set upon ;; Attack
slain ;; Killing
prompted ;; Causation
defending ;; Defending || contested ;; Competition
appearing ;; Presence || win ;; Getting
received ;; Receiving || reached ;; Arriving || entered ;; Arriving
ended ;; Process_end || contested ;; Competition || break ;; Process_end
scoring ;; Getting || winning ;; Getting || breezed ;; Motion || drawing ;; Getting || conceding ;; Agree_or_refuse_to_act
contained ;; Containing
dispatched ;; Sending || earn ;; Earnings_and_losses
become ;; Becoming || winning ;; Getting || win ;; Getting
conducted ;; Action || force ;; Military_operation
supplies ;; Supply || flow ;; Motion || Supply ;; Supply
controlled ;; Control
carved ;; Creating
extension ;; Expansion || saw ;; Self_motion
agreed ;; Agree_or_refuse_to_act || joint ;; Collaboration
conducted ;; Action || bombing ;; Attack || participating ;; Participation
flown ;; Motion || end ;; Process_end || absorption ;; Cause_to_amalgamate
supplemented ;; Supply
shot down ;; Use_firearm
fortification ;; Defending
built ;; Building || became ;; Becoming || end ;; Process_end || added ;; Cause_change_of_position_on_a_scale
fired ;; Use_firearm
filmed ;; Social_event
attack ;; Attack
gave ;; Giving || protect ;; Defending || occupy ;; Control || ordered ;; Arranging
advanced ;; Cause_to_make_progress || occupy ;; Control
ejected ;; Removing || reached ;; Arriving
contributed ;; Giving || defence ;; Defending || attack ;; Attack
reopened ;; Openness
support ;; Supporting || attack ;; Attack || fired ;; Use_firearm
support ;; Supporting || creeping ;; Self_motion || advanced ;; Cause_to_make_progress
captured ;; Conquering || taken ;; Conquering
took place ;; Process_start
capture ;; Conquering
attacked ;; Attack
gathered ;; Come_together || advanced ;; Cause_to_make_progress
defended ;; Defending
began ;; Process_start || abandoned ;; GiveUp || launched ;; Process_start
received ;; Receiving || captured ;; Conquering || use ;; Using
died ;; Death
lifted ;; Process_end
imprisoned ;; Arrest || stripped ;; Removing
invaded ;; Attack || besieged ;; Besieging || suffering ;; Bodily_harm || took ;; Creating
inflicted ;; Causation
reached ;; Arriving || granted ;; Giving
took place ;; Process_start || Battle ;; Hostile_encounter || War ;; Hostile_encounter
regarded ;; Judgment_communication || battle ;; Hostile_encounter || conflict ;; Hostile_encounter || beginning ;; Process_start || set up ;; Reforming_a_system
reach ;; Arriving || takes ;; Bringing || lies ;; Placing
harass ;; Attack || shell ;; Attack
conducted ;; Action || taking ;; Conquering || attack ;; Attack
taken ;; Conquering || fled ;; Escaping
sent ;; Sending || organizing ;; Arranging || loss ;; Earnings_and_losses || reinforcements ;; Cause_change_of_strength || defence ;; Defending || loss ;; Earnings_and_losses
aimed ;; Aiming || defended ;; Defending || operation ;; Attack || attack ;; Attack || launched ;; Process_start
assault ;; Attack || stationed ;; Placing || retaken ;; Conquering || bombardment ;; Attack || lost ;; Earnings_and_losses
began ;; Process_start || causing ;; Causation || lost ;; Earnings_and_losses || defending ;; Defending || fortified ;; Cause_change_of_strength
battle ;; Hostile_encounter || lost ;; Earnings_and_losses
last ;; Process_end || War ;; Hostile_encounter || counterattack ;; Attack || victory ;; Earnings_and_losses
concluded ;; Statement || ended ;; Process_end || found ;; Know
operating ;; Being_in_operation || crashed ;; Catastrophe || approach ;; Arriving
crashed ;; Catastrophe || protecting ;; Defending
broke ;; Destroying || injured ;; Bodily_harm
approach ;; Arriving || contained ;; Containing || investigation ;; Criminal_investigation || report ;; Reporting || adverse ;; Influence || approved ;; Preventing_or_letting || abort ;; Process_end
made ;; Manufacturing || go ;; Self_motion || avoid ;; Preventing_or_letting || impact ;; Influence
operating ;; Being_in_operation || determined ;; Deciding || issues ;; Creating
organized ;; Arranging || sponsorship ;; Earnings_and_losses || championship ;; Competition
competition ;; Competition || took place ;; Hold || co-hosted ;; Social_event
ended ;; Process_end || process ;; Process_start || tournament ;; Competition
matches ;; Competition || played ;; Competition || played ;; Competition
began ;; Process_start || selected ;; Choosing || tournament ;; Competition || participation ;; Participation
took place ;; Process_start
change ;; Change || tournament ;; Competition || brought ;; Bringing || upheaval ;; Social_event || qualified ;; Getting
affected ;; Influence || failed ;; Earnings_and_losses || qualify ;; Getting
pleaded ;; Request || end ;; Process_end
competed ;; Competition || proceeding ;; Self_motion
decided ;; Deciding || participate ;; Participation || advanced ;; Self_motion
competition ;; Competition || eliminated ;; Removing
defeated ;; Conquering
dedicated ;; Giving || team ;; Participation || died ;; Death
conquest ;; Conquering || campaign ;; Military_operation || ruled ;; Control
began ;; Process_start || annexation ;; Change_of_leadership || ally ;; Collaboration || war ;; Military_operation
suspicious ;; Know || grew ;; Coming_to_be || switched ;; Change
expansion ;; Expansion || conquest ;; Conquering
dispatched ;; Sending || led ;; Bringing || commanded ;; Control
revolt ;; Change_of_leadership || allied ;; Collaboration || support ;; Supporting || reinforced ;; Cause_change_of_strength
defeated ;; Conquering || battle ;; Hostile_encounter
strengthened ;; Cause_change_of_strength || ruled ;; Control || control ;; Control
installed ;; Placing || administer ;; Control || acquired ;; Getting
achieved ;; Achieve || reached ;; Arriving
revolts ;; Change_of_leadership || remained ;; Temporary_stay || ended ;; Process_end
conflict ;; Hostile_encounter || absorbed ;; Influence
spread ;; Dispersal || influences ;; Influence
acceleration ;; Cause_change_of_position_on_a_scale || stationed ;; Placing || declined ;; Cause_change_of_position_on_a_scale
competed ;; Competition || control ;; Control
massacre ;; Killing || committed ;; Committing_crime
estimated ;; Statement
occurred ;; Presence
broken ;; Process_end || thrown ;; Causation
conducted ;; Action || suppression ;; Hindering
camping ;; Traveling || appeal ;; Request || improve ;; Cause_to_make_progress
decided ;; Deciding || crush ;; Removing
informed ;; Telling || disband ;; Dispersal
gave ;; Giving || stood ;; Placing
aimed ;; Aiming || felled ;; Motion_directional
played ;; Participation
took place ;; Process_start
events ;; Competition
defending ;; Defending || lost ;; Competition || final ;; Competition
marked ;; Process_end || final ;; Competition || final ;; Competition || final ;; Competition
famine ;; Catastrophe || occurred ;; Coming_to_be
famines ;; Catastrophe || hit ;; Bodily_harm
contains ;; Containing
record ;; Recording || set ;; Placing
discovered ;; Know || disabled ;; Destroying
left ;; Departing
reported ;; Reporting || noticed ;; Know || attending ;; Participation
noticed ;; Know || reported ;; Reporting || transported ;; Sending || heard ;; Perception_active
made ;; Manufacturing
recovered ;; Recovering || found ;; Know || trigger ;; Causation
dismissed ;; Agree_or_refuse_to_act
attack ;; Attack || established ;; Building
arrested ;; Arrest || attack ;; Attack || found ;; Know || commit murder ;; Killing
produced ;; Manufacturing
event ;; Social_event
took place ;; Process_start
overcoming ;; Conquering || defeated ;; Conquering
defeated ;; Conquering || match ;; Competition
event ;; Social_event
gave ;; Giving
damage ;; Damaging || produced ;; Manufacturing
contained ;; Containing || skipped ;; Self_motion || named ;; Name_conferral || originated ;; Coming_to_be
develop ;; Coming_to_be || allowed ;; Preventing_or_letting
moving ;; Motion || intensified ;; Cause_change_of_strength
caused ;; Causation || turn ;; Motion || attained ;; Getting
began ;; Process_start || entered ;; Arriving || tracked ;; Self_motion
flooding ;; Motion || produced ;; Manufacturing
causing ;; Causation || flooding ;; Motion
loss ;; Earnings_and_losses || led to ;; Causation
recorded ;; Recording || hurricane ;; Catastrophe || widespread ;; Dispersal
caused ;; Causation
flooded ;; Motion || occurred ;; Coming_to_be
passed ;; Motion_directional || recorded ;; Recording
caused ;; Causation || obscured ;; Know || washed ;; Removing
riot ;; Violence || occurred ;; Coming_to_be
stated ;; Statement || killed ;; Killing || injured ;; Bodily_harm
reported ;; Reporting
fight ;; Hostile_encounter || operate ;; Being_in_operation
mentioned ;; Statement || escaped ;; Escaping || riot ;; Violence
went ;; Removing
escaped ;; Escaping || massacre ;; Committing_crime || confirmed ;; Check
believed ;; Know
organization ;; Arranging
fight ;; Hostile_encounter || broke out ;; Process_start
allowed ;; Preventing_or_letting || surge ;; Motion_directional
taken ;; Conquering || set on fire ;; Damaging
regained ;; Earnings_and_losses
contained ;; Containing || separated ;; Forming_relationships
dead ;; Death || started ;; Process_start || riot ;; Violence || speculated ;; Check
riot ;; Violence || brawl ;; Hostile_encounter || confirmed ;; Check || attacked ;; Attack
murder ;; Killing || massacre ;; Killing
violence ;; Violence || plagued ;; Bodily_harm || operate ;; Being_in_operation || criminal ;; Committing_crime || mentioned ;; Statement
built ;; Building || riot ;; Violence || incarcerated ;; Arrest
battling ;; Hostile_encounter
fight ;; Hostile_encounter || used ;; Using || rivalry ;; Competition
escape ;; Escaping || massacre ;; Killing || involvement ;; Participation || facing ;; Confronting_problem
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || acceded ;; Agree_or_refuse_to_act
battle ;; Hostile_encounter || resulted in ;; Causation || took place ;; Process_start
aided ;; Assistance || turned ;; Change || ascendancy ;; Control || ensuring ;; Statement || regain ;; Recovering
battle ;; Hostile_encounter || took place ;; Process_start
defeated ;; Conquering
fled ;; Escaping || war ;; Military_operation || continued ;; Motion
battle ;; Hostile_encounter || battles ;; Hostile_encounter || made it ;; Causation
records ;; Recording || held ;; Creating || commemoration ;; Social_event
Battle ;; Hostile_encounter || engagement ;; Hostile_encounter || fighting ;; Hostile_encounter || War ;; Military_operation
Battle ;; Hostile_encounter || engagements ;; Hostile_encounter || fought ;; Hostile_encounter
ended ;; Process_end || battle ;; Hostile_encounter || offensive ;; Attack || drive ;; Motion || victory ;; Earnings_and_losses
defended ;; Defending || supported ;; Supporting
crossed ;; Motion_directional || battle ;; Hostile_encounter || ensued ;; Coming_to_be || contained ;; Containing
fought ;; Hostile_encounter || battles ;; Hostile_encounter || taking ;; Conquering || retaking ;; Conquering || gain ;; Getting
retreat ;; Escaping || forced ;; Causation || breakdown ;; Destroying
battle ;; Hostile_encounter || seen ;; Perception_active || war ;; Military_operation || rendered ;; Causation
competition ;; Competition
competition ;; Competition || participated ;; Participation || representing ;; Presence
competition ;; Competition || began ;; Process_start
defeated ;; Conquering || final ;; Competition
used ;; Using || season ;; Process_start || comprising ;; Containing || matches ;; Competition || final ;; Competition || final ;; Competition
matches ;; Competition
referred ;; Name_conferral
began ;; Process_start || viewed ;; Perception_active
arose ;; Coming_to_be || ending ;; Process_end || objected ;; Agree_or_refuse_to_act || took ;; Conquering
erupted ;; Process_start || became ;; Becoming
declared ;; Expressing_publicly
ended ;; Process_end
formed ;; Coming_to_be
defeat ;; Hostile_encounter
resulted in ;; Causation
reach ;; Achieve
changed ;; Change || leading to ;; Causation
passed ;; Process_end || provided ;; Supply || elected ;; Change_of_leadership
contained ;; Containing || superseded ;; Removing
established ;; Creating || interrupted ;; Hindering || delayed ;; Change_event_time || increasing ;; Cause_change_of_position_on_a_scale || scheduled ;; Arranging || created ;; Creating
granted ;; Giving
fought ;; Hostile_encounter || led ;; Conquering || command ;; Control || Battle ;; Hostile_encounter
battle ;; Hostile_encounter || took place ;; Process_start
result ;; Causation || battle ;; Hostile_encounter
campaign ;; Military_operation || actuated ;; Causation
increased ;; Cause_change_of_position_on_a_scale
capitulated ;; Surrendering || moved ;; Self_motion
attacked ;; Attack
freed ;; Releasing || battle ;; Hostile_encounter || headed ;; Motion
conducted ;; Action || census ;; Scrutiny || directed ;; Action || Census ;; Scrutiny
increase ;; Cause_change_of_position_on_a_scale || Census ;; Scrutiny || determined ;; Check
received ;; Receiving || contained ;; Containing || census ;; Scrutiny
census ;; Scrutiny
designate ;; Choosing || census ;; Scrutiny
increase ;; Cause_change_of_position_on_a_scale || participation ;; Participation || recruited ;; Becoming_a_member || Census ;; Scrutiny
census ;; Scrutiny
Census ;; Scrutiny
ranks ;; Placing
rent ;; Renting
offers ;; Giving
hired ;; Employment
took place ;; Process_start
playing ;; Competition
became ;; Becoming || winning ;; Getting
finishing ;; Process_end
finished ;; Process_end
employed ;; Employment
decide ;; Deciding || given ;; Giving || tied ;; Connect || tied ;; Connect
used ;; Using
played ;; Competition || establish ;; Building
relegated ;; Judgment_communication || promoted ;; Cause_to_make_progress
went ;; Self_motion || defeating ;; Conquering || tying ;; Connect
organized ;; Arranging || hosted ;; Social_event
relegated ;; Judgment_communication
joined ;; Becoming_a_member || earned ;; Earnings_and_losses
Conquest ;; Conquering || took place ;; Process_start || War ;; Military_operation
beginning ;; Process_start
hire ;; Employment || stationed ;; Placing
attacked ;; Attack || kill ;; Killing || burn ;; Destroying || orders ;; Request || retreat ;; Escaping || regroup ;; Cause_to_amalgamate
became ;; Becoming || contact ;; Communication || order ;; Request || known ;; Know
open ;; Openness || made ;; Creating || leave ;; Departing || agreement ;; Agree_or_refuse_to_act
began ;; Process_start || attack ;; Attack
hands ;; Conquering
surrendered ;; Surrendering || cleanse ;; Removing
Battle ;; Hostile_encounter || took place ;; Process_start
defend ;; Defending || set up ;; Reforming_a_system || commanded ;; Control
invading ;; Attack || heading ;; Self_motion || cut off ;; Removing
reinforce ;; Cause_change_of_strength || liberate ;; Hold || pillage ;; Violence || raze ;; Damaging || suggested ;; Convincing
met ;; Come_together || invading ;; Attack
employing ;; Employment || scored ;; Getting || victory ;; Earnings_and_losses
operating ;; Being_in_operation || scored ;; Getting || Battle ;; Hostile_encounter || help ;; Assistance
failure ;; Earnings_and_losses || deal ;; Communication
confronted ;; Confronting_problem || reinforced ;; Cause_change_of_strength || inflicting ;; Bodily_harm || fought ;; Hostile_encounter || force ;; Military_operation || defeating ;; Conquering || casualties ;; Death
used ;; Using
selected ;; Choosing
hired ;; Employment || increase ;; Cause_change_of_position_on_a_scale || drive ;; Cause_change_of_position_on_a_scale
increase ;; Cause_change_of_position_on_a_scale
recorded ;; Recording || recorded ;; Recording
rose ;; Change_of_leadership || election ;; Choosing
faced ;; Confronting_problem || election ;; Choosing
worked ;; Employment
moved ;; Motion
defeated ;; Conquering
run ;; Motion || election ;; Choosing
placed ;; Placing || election ;; Choosing
tour ;; Traveling
joined ;; Becoming_a_member
organised ;; Arranging
worked ;; Employment
Battle ;; Hostile_encounter || attack ;; Attack || supported ;; Supporting || War ;; Hostile_encounter
attack ;; Attack || planned ;; Arranging || commanded ;; Control
mounted ;; Connect || employed ;; Employment || massed ;; Motion || attacks ;; Attack || combined ;; Cause_to_amalgamate || evolution ;; Change || war ;; Hostile_encounter || use ;; Using || arms ;; Hold || illustrated ;; Statement || military ;; Military_operation
achieved ;; Achieve || battle ;; Hostile_encounter || calculated ;; Control
give ;; Giving || arrived ;; Arriving || involved ;; Cause_to_be_included || augmented ;; Cause_change_of_strength
withdrawn ;; Escaping || seeing ;; Perception_active
War ;; Hostile_encounter || commanded ;; Control
founded ;; Creating || worked ;; Employment
grew ;; Expansion
initiated ;; Process_start
culminated ;; Process_end
left ;; Departing || lost ;; Earnings_and_losses || party ;; Social_event || election ;; Choosing
lost ;; Earnings_and_losses || race ;; Competition || nominee ;; Choosing
announced ;; Expressing_publicly || President-elect ;; Change_of_leadership || nominate ;; Choosing
began ;; Process_start
confirmed ;; Check || approved ;; Agree_or_refuse_to_act || vote ;; Choosing || vote ;; Choosing
announced ;; Expressing_publicly || stepping down ;; Change_of_leadership
took effect ;; Change
named ;; Name_conferral
occurred ;; Coming_to_be
departed ;; Departing || carrying ;; Bringing
experienced ;; Change_sentiment || required ;; Preventing_or_letting
caused ;; Causation || caused ;; Causation || fly ;; Motion || run out of ;; Expend_resource || combined ;; Cause_to_amalgamate || rendezvous ;; Arriving
crashed ;; Catastrophe || impact ;; Influence || ejected ;; Removing
claims ;; Statement || refused ;; Agree_or_refuse_to_act || refueling ;; Supply || ran out of ;; Expend_resource || bypassed ;; Self_motion
broke up ;; Destroying || ejected ;; Removing
prevented ;; Preventing_or_letting || explosion ;; Catastrophe
blames ;; Judgment_communication || flight ;; Motion || failures ;; Earnings_and_losses || use ;; Using || overcome ;; Resolve_problem
detonate ;; Damaging || worked ;; Employment
killed ;; Killing || injured ;; Bodily_harm
massacre ;; Killing || revolt ;; Attack
took place ;; Process_start
victims ;; Bodily_harm
viewed ;; Perception_active || revolt ;; Change_of_leadership || agree ;; Agree_or_refuse_to_act || revolt ;; Change_of_leadership || revolution ;; Change_of_leadership || brought on ;; Causation || followed ;; Motion
taken ;; Conquering || recruitment ;; Employment || brought ;; Bringing
became ;; Becoming
violent ;; Violence || response ;; Response
serving ;; Assistance || crashed ;; Catastrophe
takeoff ;; Self_motion || reported ;; Reporting
climbed ;; Self_motion || descended ;; Motion_directional
working ;; Employment || shut down ;; Being_in_operation
crashing ;; Catastrophe
killing ;; Killing || involving ;; Participation || crashed ;; Catastrophe
crashed ;; Catastrophe || killing ;; Killing
investigated ;; Criminal_investigation
crash ;; Catastrophe || investigated ;; Criminal_investigation
control ;; Control || concluded ;; Coming_to_believe
determine ;; Deciding
led to ;; Causation || crash ;; Catastrophe || controlled ;; Control
issues ;; Creating || attributed ;; Causation
decided ;; Deciding || determined ;; Deciding || determined ;; Deciding || working ;; Employment || allowed ;; Preventing_or_letting || conclusions ;; Statement || caused ;; Causation || caused ;; Causation || crash ;; Catastrophe || lawsuit ;; Legality
controls ;; Control || reached ;; Arriving || settlement ;; Resolve_problem
dispute ;; Quarreling
prompted ;; Causation
began ;; Process_start || put ;; Placing
took place ;; Process_start
suffered ;; Bodily_harm || launched ;; Military_operation
died ;; Death
ended ;; Process_end || emerged ;; Coming_to_be
started ;; Process_start
opposed ;; Agree_or_refuse_to_act || employed ;; Employment
support ;; Supporting
claims ;; Statement || shown ;; Presence
seen ;; Perception_active
began ;; Process_start || made ;; Manufacturing
led to ;; Causation
made ;; Manufacturing
worked ;; Employment || worked ;; Employment
received ;; Receiving
ended ;; Process_end || started ;; Process_start || receiving ;; Receiving
defence ;; Defending || fought ;; Hostile_encounter || War ;; Military_operation || battle ;; Hostile_encounter
defended ;; Defending || command ;; Control
fought ;; Hostile_encounter || seized ;; Hold
positions ;; Employment || overrun ;; Attack
defending ;; Defending || surrender ;; Surrendering || besieged ;; Besieging || ordered ;; Request
suffered ;; Bodily_harm || bombardment ;; Attack || defending ;; Defending || strikes ;; Attack || casualties ;; Death || subjected ;; Confronting_problem
interdiction ;; Action || expansion ;; Expansion
Launched ;; Using
bombing ;; Attack || support ;; Supporting || struggle ;; Confronting_problem || carried out ;; Attack
expanded ;; Expansion || took place ;; Process_start
led ;; Causation || pass ;; Motion_directional
expanded ;; Expansion || bombing ;; Attack || conducted ;; Action
carried out ;; Attack
bombing ;; Attack || fatalities ;; Catastrophe || killed ;; Killing
sent ;; Sending || elected ;; Choosing
declared ;; Expressing_publicly
moved ;; Motion || occupied ;; Control
blockaded ;; Besieging
captured ;; Conquering
ended ;; Process_end || enforced ;; Action
agreed ;; Agree_or_refuse_to_act || assumed ;; Choosing
acknowledged ;; Statement || accepted ;; Receiving
criticism ;; Judgment_communication || inspired ;; Influence
intensified ;; Cause_change_of_strength
left ;; Placing
contested ;; Competition
plays ;; Participation || divided ;; Creating
divided ;; Creating || divided ;; Creating
advance ;; Cause_to_make_progress
promoted ;; Cause_to_make_progress || relegated ;; Judgment_communication
implemented ;; Action || join ;; Becoming_a_member || play ;; Participation
gives ;; Giving
bomb ;; Attack || used ;; Using
involved ;; Cause_to_be_included || drive ;; Motion || placing ;; Placing
bombings ;; Attack
adopted ;; Action
used ;; Using || bomb ;; Attack
occurred ;; Coming_to_be || rally ;; Self_motion
called ;; Name_conferral || governed ;; Control
implemented ;; Action || organized ;; Arranging || called ;; Name_conferral
served ;; Assistance
march ;; Self_motion
joined ;; Becoming_a_member || organizing ;; Arranging || marked ;; Recording || beating ;; Bodily_harm || murder ;; Committing_crime || expand ;; Expansion || prompted ;; Causation
sponsored ;; Supply || decentralized ;; Dispersal || states ;; Statement || develop ;; Cause_to_make_progress
Operation ;; Military_operation || demands ;; Request || War ;; Hostile_encounter || impose ;; Action
dispatched ;; Sending || bring about ;; Causation || re-entry ;; Arriving || Wars ;; Hostile_encounter
receiving ;; Receiving || set about ;; Process_start || war ;; Hostile_encounter
deployed ;; Arranging || requested ;; Request || alarmed ;; Warning || fight ;; Hostile_encounter || war ;; Hostile_encounter || assistance ;; Assistance || developments ;; Cause_to_make_progress || advancing ;; Self_motion
requests ;; Request || meet ;; Come_together || fell ;; Conquering || impose ;; Action
command ;; Control || sailed ;; Self_motion || assault ;; Attack
demanded ;; Request || added ;; Cause_to_be_included || attack ;; Attack || resist ;; Defending
chosen ;; Choosing || operation ;; Military_operation
sent ;; Sending || support ;; Supporting || join ;; Becoming_a_member || exit ;; Departing || made ;; Manufacturing
commanded ;; Control || bombard ;; Attack || orders ;; Request || seize ;; Conquering
held ;; Hold
moved ;; Motion || earthquake ;; Catastrophe || redistribute ;; Change
hosted ;; Social_event || include ;; Cause_to_be_included
evolved ;; Coming_to_be || gathering ;; Come_together
event ;; Social_event || discuss ;; Communication || become ;; Becoming || congregate ;; Come_together || occasion ;; Social_event
complaint ;; Statement || made ;; Action || suppression ;; Action
operating ;; Being_in_operation || undertaken ;; Action
drove ;; Motion || implemented ;; Action || sending ;; Sending || increasing ;; Cause_change_of_position_on_a_scale
dubbed ;; Name_conferral
combined ;; Cause_to_amalgamate
defected ;; Escaping || attacks ;; Attack
increasing ;; Cause_change_of_position_on_a_scale || improve ;; Cause_to_make_progress || initiated ;; Process_start
attached ;; Connect || operating ;; Being_in_operation || plan ;; Arranging
blizzard ;; Catastrophe
produced ;; Manufacturing || Originating ;; Creating || traveled ;; Motion
formed ;; Coming_to_be || moved ;; Motion || blizzard ;; Catastrophe || weakened ;; Cause_change_of_strength || moved ;; Motion || deepened ;; Cause_change_of_strength || producing ;; Causation
moved ;; Motion || weakened ;; Cause_change_of_strength
given ;; Giving || storm ;; Catastrophe
declared ;; Expressing_publicly || disrupted ;; Hindering || enacted ;; Action
service ;; Supply || cancelled ;; Change_event_time
cancellations ;; Change_event_time
blizzard ;; Catastrophe || storm ;; Catastrophe || struck ;; Attack
cut ;; Removing || storm ;; Catastrophe || fell ;; Motion_directional || shift ;; Change
criticised ;; Judgment_communication || storm ;; Catastrophe || shutting down ;; Preventing_or_letting
flight ;; Motion
crashed ;; Catastrophe || executing ;; Action
killing ;; Killing || impact ;; Catastrophe
surpassed ;; Cause_change_of_position_on_a_scale || crashed ;; Catastrophe || bombing ;; Attack || involving ;; Cause_to_be_included || involving ;; Cause_to_be_included || surpassed ;; Cause_change_of_position_on_a_scale || disaster ;; Catastrophe || accident ;; Catastrophe || crash ;; Catastrophe
prevention ;; Preventing_or_letting || report ;; Reporting || issued ;; Creating || investigated ;; Scrutiny || investigation ;; Criminal_investigation
caused ;; Causation || concluded ;; Coming_to_believe || committed ;; Committing_crime
defeated ;; Conquering || made up ;; Come_together
following ;; Self_motion || remained ;; Presence
stood ;; Placing
destroyed ;; Destroying
crowning ;; Change_of_leadership || arrest ;; Arrest || joining ;; Becoming_a_member || came to ;; Motion
involved ;; Participation || combined ;; Cause_to_amalgamate || captured ;; Conquering
brought ;; Bringing || end ;; Process_end
reinforced ;; Cause_change_of_strength || signed ;; Sign_agreement || took ;; Creating
confirmed ;; Check || allowed ;; Preventing_or_letting || imposed ;; Action || defeated ;; Conquering
permitted ;; Preventing_or_letting
ceased ;; Process_end || abdicated ;; Change_of_leadership || emerging ;; Coming_to_be
establish ;; Building
driven ;; Motion || invasion ;; Attack || protected ;; Defending
growing ;; Expansion || sparked ;; Causation
titled ;; Name_conferral
violence ;; Violence || associated ;; Collaboration || established ;; Creating || investigate ;; Criminal_investigation
keeping ;; Temporary_stay || pursued ;; Aiming || governing ;; Action || party ;; Social_event || seen ;; Change_sentiment
led ;; Causation || enquiries ;; Criminal_investigation || investigation ;; Criminal_investigation
involved ;; Cause_to_be_included || avoid ;; Preventing_or_letting || facilitated ;; Cause_to_make_progress || benefited ;; Earnings_and_losses
known ;; Know || fought ;; Hostile_encounter
brought ;; Bringing || overthrown ;; Change_of_leadership
marked ;; Recording
aided ;; Assistance || perceived ;; Know || fought ;; Hostile_encounter
withdraw ;; Escaping || called ;; Name_conferral
became ;; Becoming
committed ;; Commitment
faced ;; Confronting_problem || imposed ;; Action
become ;; Becoming || played ;; Participation
resulted in ;; Causation
faced ;; Confronting_problem
followed ;; Motion || resulted in ;; Causation || claimed ;; Statement
became ;; Becoming || existed ;; Presence
became ;; Becoming
focuses ;; Aiming
creates ;; Causation || effects ;; Influence
supported ;; Supporting || implementation ;; Action
playing ;; Action || creating ;; Creating
supported ;; Supporting
called ;; Name_conferral || conflict ;; Hostile_encounter
pitted ;; Hostile_encounter || conflict ;; Hostile_encounter
renamed ;; Name_conferral || signed ;; Sign_agreement || led to ;; Causation || implementation ;; Action || end ;; Process_end
order ;; Arranging || win ;; Getting || continued ;; Motion
achieved ;; Achieve || reached ;; Arriving
negotiations ;; Communication || agreement ;; Agree_or_refuse_to_act || took place ;; Process_start || signed ;; Sign_agreement
control ;; Control || returned ;; Motion || held ;; Hold || elections ;; Change_of_leadership
became ;; Becoming || achieved ;; Achieve || election ;; Choosing || recognised ;; Know
conducted ;; Action || murder ;; Killing
conducted ;; Action || realise ;; Know
identified ;; Check || killed ;; Killing || killed ;; Killing || imprisoned ;; Arrest || selected ;; Choosing
effecting ;; Influence || facilitate ;; Cause_to_make_progress || selected ;; Choosing
eliminate ;; Removing
implementing ;; Action || facilitate ;; Cause_to_make_progress || manage ;; Hold
identified ;; Check || killed ;; Killing || Prosecution ;; Legality
occurred ;; Coming_to_be
war ;; Military_operation || occurred ;; Coming_to_be
war ;; Military_operation
uprising ;; Change_of_leadership || granted ;; Giving || incidents ;; Hostile_encounter
control ;; Control || initiated ;; Process_start || placed ;; Placing
reach ;; Arriving || launched ;; Process_start || offensives ;; Attack
capturing ;; Conquering || advanced ;; Cause_to_make_progress
ended ;; Process_end || declared ;; Expressing_publicly || withdrawal ;; Escaping || announced ;; Expressing_publicly
combat ;; Hostile_encounter || took place ;; Process_start
noted ;; Know
confronting ;; Confronting_problem || resolved ;; Resolve_problem || occurred ;; Coming_to_be || saw ;; Self_motion || receive ;; Receiving
war ;; Military_operation
end ;; Process_end || fighting ;; Hostile_encounter || ensued ;; Coming_to_be || war ;; Military_operation || clashes ;; Hostile_encounter || broke out ;; Process_start
competition ;; Competition
contested ;; Competition
built ;; Building || played ;; Participation
penalties ;; Causation
ended ;; Process_end || equalised ;; Hold
sent ;; Sending
scored ;; Getting || answered ;; Resolve_problem
score ;; Getting || failed ;; Earnings_and_losses
ending ;; Process_end
solutions ;; Resolve_problem || hopes ;; Body_movement || encourage ;; Influence || change ;; Change || develop ;; Cause_to_make_progress
puts together ;; Come_together || tournament ;; Competition || compete ;; Competition
hosted ;; Social_event || tournament ;; Competition
encompassed ;; Coming_to_be
fought ;; Hostile_encounter || contrasts ;; Perception_active
developed ;; Coming_to_be || invade ;; Attack
plan ;; Arranging || criticised ;; Judgment_communication || threat ;; Judgment_communication
send ;; Sending || defend ;; Defending || invading ;; Attack || planned ;; Arranging
beaten ;; Bodily_harm || opening ;; Openness || invasion ;; Hostile_encounter
invaded ;; Attack || defeating ;; Conquering
failed ;; Earnings_and_losses
casualties ;; Catastrophe || retreat ;; Escaping || dealing ;; Resolve_problem || forcing ;; Request
replaced ;; Change_of_leadership
failed ;; Earnings_and_losses
became ;; Becoming || operation ;; Military_operation || gains ;; Earnings_and_losses || make ;; Getting
entered ;; Arriving
promised ;; Commitment || support ;; Supporting
invaded ;; Attack || attacked ;; Attack || stop ;; Process_end || pushed ;; Motion
occurred ;; Presence
forced ;; Motion || founded ;; Creating || abdicate ;; Change_of_leadership || replaced ;; Change_of_leadership
formed ;; Coming_to_be || fight ;; Hostile_encounter || overthrown ;; Change_of_leadership
caused ;; Causation || oversaw ;; Scrutiny || collapse ;; Catastrophe || failure ;; Earnings_and_losses
taking ;; Conquering || signed ;; Sign_agreement || established ;; Creating
forced ;; Motion || signed ;; Sign_agreement || surrender ;; Surrendering || nullified ;; Destroying || surrender ;; Surrendering
started ;; Process_start || revolution ;; Change_of_leadership || conflicts ;; Hostile_encounter || occurred ;; Coming_to_be
pulled ;; Motion || influence ;; Influence || involved ;; Cause_to_be_included || left ;; Departing
involved ;; Cause_to_be_included || disputed ;; Quarreling
casualties ;; Catastrophe || use ;; Using || established ;; Building || occurred ;; Coming_to_be
resolved ;; Resolve_problem
aimed ;; Aiming || include ;; Cause_to_be_included
pitting ;; Hostile_encounter || occurred ;; Presence
began ;; Process_start || Inspired ;; Influence
brokered ;; Resolve_problem
defeated ;; Conquering || expired ;; Process_end
taken ;; Conquering || killed ;; Killing
withstood ;; Defending || suffered ;; Bodily_harm
lasted ;; Process_end || disputes ;; Quarreling
caused ;; Causation || recovered ;; Recovering
shifted ;; Change || marked ;; Recording
War ;; Hostile_encounter || war ;; Hostile_encounter
began ;; Process_start
began ;; Process_start
consisted ;; Cause_to_be_included || founded ;; Creating
included ;; Cause_to_be_included
signed ;; Sign_agreement || resolve ;; Resolve_problem
signed ;; Sign_agreement || signed ;; Sign_agreement
granted ;; Giving || called ;; Request || integrate ;; Cause_to_amalgamate
signed on ;; Sign_agreement || signed ;; Sign_agreement
sign ;; Sign_agreement || signed ;; Sign_agreement
took place ;; Process_start
commenced ;; Process_start || seen ;; Perception_active
besieged ;; Besieging || driven ;; Motion
incorporated ;; Cause_to_amalgamate
stormed ;; Attack
given ;; Giving || overcoming ;; Resolve_problem
called ;; Name_conferral
impact ;; Influence
resulted in ;; Causation
involved ;; Cause_to_be_included || address ;; Resolve_problem || perform ;; Being_in_operation
involving ;; Cause_to_be_included
conducted ;; Action || caused ;; Causation || concluded ;; Statement
held ;; Hold
died ;; Death
dropped ;; Removing
address ;; Resolve_problem
surmounted ;; Resolve_problem
organize ;; Arranging
game ;; Competition
played ;; Competition
earning ;; Earnings_and_losses || defeated ;; Conquering
named ;; Name_conferral
following ;; Self_motion || win ;; Getting
replay ;; Competition
came ;; Self_motion
losing ;; Earnings_and_losses || reigning ;; Change_of_leadership || came ;; Self_motion || winning ;; Getting
reported ;; Reporting || dealing ;; Resolve_problem
released ;; Publishing
match ;; Competition
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || attack ;; Attack || retreating ;; Escaping
overwhelmed ;; Destroying || capturing ;; Conquering
commanded ;; Control
Wars ;; Hostile_encounter || occurred ;; Presence || War ;; Military_operation
escaping ;; Escaping || began ;; Process_start || Campaign ;; Military_operation || war ;; Hostile_encounter || disastrous ;; Catastrophe || envelopment ;; Besieging
fled ;; Escaping || Battle ;; Hostile_encounter || joined ;; Cause_to_amalgamate
caught ;; Self_motion
retreating ;; Escaping || shaken ;; Cause_change_of_position_on_a_scale || struggle ;; Resolve_problem
fight ;; Hostile_encounter
decided ;; Deciding || Battle ;; Hostile_encounter || war ;; Military_operation
support ;; Supporting || tour ;; Traveling
visited ;; Traveling || ranked ;; Placing
grossed ;; Earnings_and_losses || gross ;; Earnings_and_losses || bringing ;; Bringing || played ;; Participation
received ;; Receiving || praised ;; Judgment_communication
announcing ;; Expressing_publicly || announced ;; Expressing_publicly || caused ;; Causation || postponement ;; Change_event_time || injury ;; Bodily_harm || cancellation ;; Removing || involved ;; Cause_to_be_included
damage ;; Damaging || caused ;; Causation || went through ;; Getting || fix ;; Resolve_problem
reigning ;; Change_of_leadership || Cup ;; Competition || Shield ;; Competition || Shield ;; Competition || match ;; Competition || contested ;; Competition
held ;; Hold
beat ;; Competition || win ;; Getting || game ;; Competition || Cup ;; Competition || League ;; Competition || Final ;; Competition || played ;; Competition
Shield ;; Competition
began ;; Process_start || created ;; Creating || match ;; Competition || dominating ;; Control
created ;; Creating || struggled ;; Resolve_problem
ended ;; Process_end || held ;; Defending
shared ;; Communication
causing ;; Causation || Cyclone ;; Catastrophe || struck ;; Attack || floods ;; Catastrophe || record ;; Recording
began ;; Process_start || moved ;; Motion || cyclone ;; Catastrophe
named ;; Name_conferral || becoming ;; Becoming || named ;; Name_conferral || storm ;; Catastrophe || cyclone ;; Catastrophe
developed ;; Coming_to_be || strengthened ;; Cause_change_of_strength || winds ;; Motion || approaching ;; Arriving || turning ;; Motion
weakened ;; Cause_change_of_strength || cyclone ;; Catastrophe || Brushing ;; Motion || winds ;; Motion || turned ;; Motion
curved ;; Motion_directional || making ;; Causation || landfall ;; Motion
dissipated ;; Removing
cyclone ;; Catastrophe || drifting ;; Motion || produced ;; Creating || cause ;; Causation || outages ;; Hindering
damaged ;; Damaging || spawned ;; Causation
damage ;; Damaging || flooding ;; Catastrophe
increased ;; Cause_change_of_position_on_a_scale
damage ;; Damaging || decreased ;; Cause_change_of_position_on_a_scale || suffered ;; Confronting_problem
obtain ;; Getting || rose ;; Cause_change_of_position_on_a_scale || forcing ;; Causation || stranded ;; Preventing_or_letting || evacuate ;; Departing
storm ;; Catastrophe || alleviated ;; Resolve_problem || diminished ;; Cause_change_of_position_on_a_scale || fires ;; Catastrophe
killed ;; Killing || caused ;; Causation || damage ;; Damaging
declaration ;; Expressing_publicly || name ;; Name_conferral || names ;; Name_conferral || cyclone ;; Catastrophe || issued ;; Statement || retired ;; GiveUp
impacted ;; Influence
due ;; Causation || produced ;; Manufacturing
hit ;; Attack
killed ;; Killing
attributed ;; Causation || used ;; Using || poisoning ;; Bodily_harm
deal ;; Resolve_problem || called up ;; Request
response ;; Response || assistance ;; Assistance || supply ;; Supply || impacted ;; Influence || provided ;; Supply
wars ;; Hostile_encounter || fought ;; Hostile_encounter || Wars ;; Hostile_encounter
ended ;; Process_end || started ;; Process_start || war ;; Hostile_encounter || settlement ;; Resolve_problem
war ;; Hostile_encounter || lasted ;; Process_end
ended ;; Process_end || captured ;; Conquering || killed ;; Killing || war ;; Hostile_encounter || lasted ;; Process_end
resulted in ;; Causation || war ;; Hostile_encounter || defined ;; Coming_to_be || crossed ;; Motion_directional
treaty ;; Sign_agreement || rebellion ;; Change_of_leadership || lasted ;; Process_end || established ;; Creating
attaining ;; Getting || conflict ;; Hostile_encounter
formed ;; Coming_to_be
taken place ;; Process_start || ousted ;; Change_of_leadership || election ;; Choosing
suspended ;; Process_end || reversed ;; Change
began ;; Process_start || disputed ;; Quarreling || fighting ;; Hostile_encounter || conflict ;; Hostile_encounter || control ;; Control || claiming ;; Statement
accomplished ;; Achieve || called ;; Name_conferral || capture ;; Conquering || said ;; Communication
proclaimed ;; Statement || independence ;; Change_of_leadership
conflicting ;; Hostile_encounter || reconcile ;; Resolve_problem || struggled ;; Hostile_encounter
clashed ;; Hostile_encounter || wounding ;; Bodily_harm || taking ;; Creating
pushed ;; Self_motion
start ;; Process_start || asked ;; Request || negotiations ;; Communication || claim ;; Statement || renounced ;; Statement
shot ;; Use_firearm
said ;; Communication || attacked ;; Attack || fired ;; Use_firearm || ensued ;; Coming_to_be
initiated ;; Process_start || stated ;; Statement || shooting ;; Use_firearm || grabbing ;; Body_movement || threatening ;; Warning
fled ;; Escaping || state ;; Statement || pursuing ;; Self_motion
stated ;; Statement || stopped ;; Process_end
stating ;; Statement || shot ;; Use_firearm || contradicted ;; Response || turned ;; Becoming || raised ;; Cause_change_of_position_on_a_scale
shot ;; Use_firearm || fell ;; Motion_directional
fired ;; Use_firearm
hit ;; Body_movement
said ;; Communication || shot ;; Use_firearm || shoot ;; Use_firearm || found ;; Know || believed ;; Know || used ;; Using
established ;; Creating
criticized ;; Judgment_communication || dealing ;; Resolve_problem
response ;; Response
ordered ;; Request || cede ;; Surrendering
given ;; Giving || called ;; Name_conferral
decided ;; Deciding || announced ;; Expressing_publicly
reported ;; Reporting || shooting ;; Use_firearm
supported ;; Supporting || found ;; Know || corroborated ;; Check || incriminated ;; Judgment_communication || admitting ;; Statement || seen ;; Perception_active
shot ;; Use_firearm || concluded ;; Coming_to_believe
bombing ;; Attack || mission ;; Military_operation
carried out ;; Action || plan ;; Arranging || cripple ;; Destroying
disperse ;; Dispersal || bombed ;; Attack || attacking ;; Attack
postponed ;; Change_event_time
flown ;; Motion
strike ;; Attack
inflicted ;; Causation || damaged ;; Damaging || lost ;; Earnings_and_losses
attack ;; Attack || crippled ;; Hindering || follow ;; Motion
attacked ;; Attack || addressed ;; Resolve_problem
curtailed ;; Cause_change_of_position_on_a_scale
hosted ;; Placing
conference ;; Social_event || independence ;; Statement
demanded ;; Request || support ;; Supporting || addressed ;; Resolve_problem || opposed ;; Agree_or_refuse_to_act
include ;; Cause_to_be_included || insisted ;; Statement
advised ;; Convincing || advised ;; Convincing || becoming ;; Becoming
recognised ;; Know || seek ;; Creating || continue ;; Motion || advised ;; Convincing || advised ;; Convincing
given ;; Giving || operations ;; Military_operation || formed ;; Coming_to_be
Wars ;; Hostile_encounter
took place ;; Process_start
captured ;; Conquering || besieged ;; Besieging
ended ;; Process_end || restored ;; Recovering || war ;; Hostile_encounter || resolve ;; Resolve_problem || treaty ;; Sign_agreement
began ;; Process_start || expedition ;; Departing || expedition ;; Departing
invasion ;; Attack || resolve ;; Resolve_problem || dispute ;; Quarreling || purported ;; Statement || establish ;; Creating
conquered ;; Conquering || occupying ;; Control
rule ;; Control || remained ;; Temporary_stay
initiated ;; Process_start || head ;; Causation || perceived ;; Know || intended ;; Deciding
invasion ;; Attack || feared ;; Change_sentiment || obsessed ;; Judgment_communication
received ;; Receiving || assurances ;; Telling
assurances ;; Telling || dispatch ;; Sending || noted ;; Statement
fought ;; Quarreling || reached ;; Arriving || expedition ;; Departing
fled ;; Escaping || block ;; Hindering || armed ;; Hold || advance ;; Motion
sign ;; Sign_agreement || treaty ;; Sign_agreement || remaining ;; Temporary_stay || understanding ;; Know || permit ;; Statement || forced ;; Request || withdrawing ;; Escaping
recognized ;; Know || expedition ;; Departing || military ;; Military_operation || campaign ;; Military_operation || issued ;; Supply || took part ;; Dispersal
reach ;; Arriving || brought ;; Bringing
resolve ;; Resolve_problem || withdraw ;; Escaping
concert ;; Social_event || tour ;; Traveling || tour ;; Traveling
Starting ;; Process_start || supports ;; Supporting
tour ;; Traveling || tour ;; Traveling
take ;; Creating || surfaced ;; Coming_to_be
announced ;; Expressing_publicly || tour ;; Traveling
Promoting ;; Cause_to_make_progress || stating ;; Statement || tour ;; Traveling || talked ;; Telling || show ;; Social_event
show ;; Social_event || show ;; Presence || said ;; Statement
mix ;; Creating || think ;; Coming_to_believe || said ;; Statement
concert ;; Social_event || included ;; Cause_to_be_included || performed ;; Hold || Tour ;; Traveling
included ;; Cause_to_be_included || concert ;; Social_event
included ;; Cause_to_be_included || show ;; Social_event || show ;; Social_event || answer ;; Resolve_problem || question ;; Communication
post ;; Sending || invited ;; Participation
taken ;; Bringing || pulled ;; Hold || answer ;; Resolve_problem
War ;; Military_operation || War ;; Military_operation || confrontation ;; Hostile_encounter
killed ;; Killing || compromise ;; Resolve_problem || captured ;; Conquering || injured ;; Bodily_harm
forged ;; Creating || compromise ;; Statement || treaty ;; Statement || met ;; Come_together
engaged ;; Hostile_encounter || combat ;; Hostile_encounter
described ;; Statement || incident ;; Hostile_encounter
capture ;; Conquering
blockaded ;; Besieging
relief ;; Assistance || entered ;; Arriving
sortied ;; Hostile_encounter || destroyed ;; Destroying || planned ;; Arranging
took over ;; Change_of_leadership || going ;; Motion || command ;; Control || siege ;; Besieging || reinforced ;; Cause_change_of_strength
gathered ;; Come_together || launched ;; Process_start
floating ;; Motion
losses ;; Earnings_and_losses || failure ;; Earnings_and_losses || resulting in ;; Causation
defeat ;; Conquering || relief ;; Assistance || slipped ;; Motion || arrived at ;; Arriving || settled ;; Resolve_problem
negotiated ;; Communication || end ;; Process_end || victory ;; Earnings_and_losses || lifted ;; Motion
fought ;; Hostile_encounter
sieges ;; Besieging || siege ;; Besieging
recording ;; Recording
support ;; Supporting || released ;; Publishing
tour ;; Traveling || kicked off ;; Process_start
tour ;; Traveling
held ;; Defending
perform ;; Hold
opening ;; Openness
renamed ;; Name_conferral || organised ;; Arranging || Final ;; Competition || match ;; Competition || tournament ;; Competition
played ;; Competition
came ;; Self_motion || final ;; Competition || league ;; Competition || cup ;; Competition
scored ;; Getting
scored ;; Getting || confirmed ;; Check || kick ;; Body_movement || put ;; Placing || game ;; Competition
coming ;; Getting || competition ;; Competition || sealed ;; Getting
defeat ;; Conquering || final ;; Competition || record ;; Recording
play ;; Competition || earned ;; Earnings_and_losses || match ;; Competition
qualified ;; Becoming_a_member || final ;; Competition || beat ;; Competition || enter ;; Becoming_a_member
Trophy ;; Competition || final ;; Competition
scheduled ;; Arranging || final ;; Competition || play ;; Participation
scored ;; Getting || batted ;; Body_movement
match ;; Competition || stopped ;; Process_end || rain ;; Motion || played ;; Participation
scored ;; Getting || batted ;; Body_movement
abandoned ;; GiveUp || match ;; Competition || reached ;; Achieve
declared ;; Expressing_publicly || Match ;; Competition || joint ;; Collaboration || awarded ;; Giving
competition ;; Competition
tournament ;; Competition || final ;; Competition || began ;; Process_start || held ;; Hold
knocked out ;; Competition
finished ;; Process_end || wins ;; Earnings_and_losses || loss ;; Earnings_and_losses
reach ;; Arriving
semi-final ;; Competition || make it ;; Causation || defeated ;; Hostile_encounter
semi-final ;; Competition || defeated ;; Hostile_encounter
failed ;; Earnings_and_losses || advance ;; Self_motion
final ;; Competition || beat ;; Competition || lift ;; Body_movement
finished ;; Process_end
named ;; Name_conferral || scoring ;; Getting
finished ;; Process_end || amassed ;; Getting
matches ;; Competition || took ;; Getting
took ;; Getting || capturing ;; Getting
qualified ;; Getting
fight ;; Hostile_encounter
decided ;; Deciding
decided ;; Deciding || keep ;; Temporary_stay
stretched ;; Body_movement
competition ;; Competition || match ;; Competition || final ;; Process_end
match ;; Competition || played ;; Participation || contested ;; Competition
match ;; Competition
gave ;; Giving || lifting ;; Body_movement
match ;; Competition
connecting ;; Connect || stops ;; Preventing_or_letting
started ;; Process_start || takeoff ;; Self_motion
impacted ;; Influence || end ;; Process_end || lifting off ;; Motion_directional
impacted ;; Influence || landing ;; Self_motion || thrust ;; Self_motion
hit ;; Attack || hit ;; Attack
regulate ;; Control || initiating ;; Process_start || cause ;; Causation || crash ;; Catastrophe || following ;; Motion || failure ;; Earnings_and_losses
clarified ;; Check
organised ;; Arranging
tour ;; Traveling
ended ;; Process_end || started ;; Process_start
made ;; Manufacturing
included ;; Cause_to_be_included || took place ;; Process_start
released ;; Publishing
released ;; Publishing
opened ;; Openness
tour ;; Traveling || included ;; Cause_to_be_included
record ;; Recording || perform ;; Hold || end ;; Process_end || returned ;; Arriving || kicking off ;; Process_start
performed ;; Hold
hurricane ;; Catastrophe || Hurricane ;; Catastrophe || record ;; Recording
developed ;; Coming_to_be || hurricane ;; Catastrophe || hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength || storm ;; Attack || headed ;; Self_motion
recurving ;; Body_movement || hurricane ;; Catastrophe || intensified ;; Cause_change_of_strength
began ;; Process_start || hurricane ;; Catastrophe || hurricane ;; Catastrophe || records ;; Recording || landfalling ;; Self_motion
moved ;; Motion || dissipated ;; Dispersal
Hurricane ;; Catastrophe || brought ;; Causation
damaged ;; Damaging || downed ;; Motion || flooded ;; Motion
reported ;; Reporting
caused ;; Causation || Landslides ;; Catastrophe || forced ;; Causation
storm ;; Catastrophe || warnings ;; Warning || issued ;; Creating
opened ;; Openness || storm ;; Catastrophe
reported ;; Reporting
damaged ;; Damaging
left ;; Departing
impact ;; Influence || reported ;; Reporting || destroyed ;; Destroying
caused ;; Causation || damage ;; Damaging
played ;; Competition || match ;; Competition
game ;; Competition || held ;; Hold || rivalry ;; Competition
included ;; Cause_to_be_included || match ;; Competition || scored ;; Getting
become ;; Becoming || using ;; Using || scored ;; Getting
became ;; Becoming || dribble ;; Body_movement || past ;; Self_motion
win ;; Getting
led ;; Causation || besiege ;; Besieging || occurred ;; Coming_to_be
conquered ;; Conquering
arrived at ;; Arriving
pushed ;; Body_movement
entered ;; Becoming_a_member
brought ;; Bringing
brought ;; Bringing
went ;; Motion || took ;; Motion
unlocked ;; Self_motion
reached ;; Arriving
tried ;; Creating
prevented ;; Preventing_or_letting || lift ;; Body_movement
jump ;; Self_motion
derailed ;; Motion_directional
wrecked ;; Catastrophe
killed ;; Killing
recommended ;; Convincing
attack ;; Attack
carried out ;; Action
attack ;; Attack || came ;; Arriving
roadside bomb ;; Terrorism || detonated ;; Attack
opened ;; Action || threw ;; Body_movement
killed ;; Killing || injuries ;; Bodily_harm
storm ;; Catastrophe || caused ;; Causation
developed ;; Coming_to_be || named ;; Name_conferral || emerged ;; Coming_to_be || storm ;; Catastrophe
developed ;; Coming_to_be || crossing ;; Motion_directional || classified ;; Check
strengthened ;; Cause_change_of_strength || becoming ;; Becoming || tracked ;; Self_motion
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || brushed ;; Motion
became ;; Becoming || made ;; Manufacturing
weakening ;; Cause_change_of_strength || making ;; Manufacturing
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
damage ;; Damaging || knocked out ;; Hindering
triggered ;; Causation || causing ;; Causation
shut down ;; Having_or_lacking_access || due ;; Causation
reported ;; Reporting
reported ;; Reporting || flooding ;; Motion
occurred ;; Coming_to_be || leaving ;; Departing
storm ;; Catastrophe || damage ;; Damaging || associated ;; Collaboration || produced ;; Manufacturing
renamed ;; Name_conferral
defeat ;; Preventing_or_letting || coming back ;; Competition
hit ;; Body_movement || scored ;; Getting
win ;; Getting || reach ;; Achieve
giving ;; Giving
achieving ;; Achieve || completed ;; Process_end || becoming ;; Becoming || preventing ;; Preventing_or_letting || finished ;; Process_end
competed ;; Competition || losing ;; Earnings_and_losses
became ;; Becoming || competition ;; Competition || recorded ;; Recording
qualified ;; Earnings_and_losses
entered ;; Participation
entered ;; Participation || entered ;; Participation || entered ;; Participation
eliminated ;; Removing
concluding ;; Process_end || tournament ;; Competition || Final ;; Competition
held ;; Hold
tournament ;; Competition || hosted ;; Social_event
Final ;; Competition || beating ;; Competition
hit ;; Body_movement || receiving ;; Receiving || Final ;; Competition || assist ;; Assistance || win ;; Getting
tournament ;; Competition || Final ;; Competition
cyclone ;; Attack || strike ;; Bodily_harm || hurricane ;; Catastrophe
storm ;; Catastrophe || hurricane ;; Catastrophe || formed ;; Coming_to_be
moved ;; Motion
acquired ;; Getting || passed ;; Body_movement || causing ;; Causation || damage ;; Damaging
affected ;; Influence || ruined ;; Destroying
weakened ;; Cause_change_of_position_on_a_scale
moved ;; Body_movement
blew ;; Bodily_harm || prostrated ;; Removing
received ;; Receiving || destruction ;; Destroying || damaged ;; Damaging
weakened ;; Cause_change_of_strength || produced ;; Creating || causing ;; Change || collapse ;; Catastrophe
caused ;; Causation || damaging ;; Damaging || destroying ;; Destroying
storm ;; Attack || struck ;; Bodily_harm || hit ;; Bodily_harm
campaigns ;; Military_operation || seize ;; Conquering
destroyed ;; Destroying || siege ;; Besieging
attacking ;; Attack || approached ;; Arriving || commanded ;; Control
gathered ;; Come_together
launched ;; Process_start || assault ;; Attack || resistance ;; Defending || ambush ;; Attack || pushed ;; Motion
fired ;; Use_firearm || threw ;; Body_movement
launched ;; Process_start || attack ;; Attack
lit ;; Self_motion || closed ;; Self_motion
began ;; Process_start
battle ;; Hostile_encounter || blown out ;; Removing || plunged ;; Motion_directional
used ;; Using || killed ;; Killing
assault ;; Attack || siege ;; Besieging
found ;; Know || attacked ;; Attack
spread ;; Dispersal || destroying ;; Destroying || combined ;; Cause_to_amalgamate
took place ;; Process_start
exploded ;; Attack
killed ;; Killing || injured ;; Bodily_harm
said ;; Statement
condemned ;; Judgment_communication
facilitated ;; Cause_to_make_progress
responded ;; Response
shook ;; Body_movement || seen ;; Perception_active || spurred ;; Cause_change_of_position_on_a_scale
tie ;; Connect
played ;; Participation || winning ;; Earnings_and_losses
marred ;; Damaging || spitting ;; Body_movement
struck ;; Bodily_harm || thrown ;; Body_movement || start ;; Process_start
replaced ;; Change
win ;; Earnings_and_losses
went to ;; Change
loss ;; Earnings_and_losses || exacerbated ;; Cause_change_of_position_on_a_scale
sent ;; Sending
refused ;; Agree_or_refuse_to_act || played ;; Competition || sent ;; Sending
scored ;; Getting || game ;; Competition || becoming ;; Becoming || winning ;; Earnings_and_losses
attacked ;; Attack || entered ;; Arriving
killed ;; Killing || died ;; Death || wounded ;; Bodily_harm || responding ;; Response || wounds ;; Bodily_harm
died ;; Death || woke up ;; Body_movement || injured ;; Bodily_harm
shot ;; Use_firearm
declared ;; Expressing_publicly || claimed ;; Statement || attack ;; Attack || declarations ;; Expressing_publicly
stated ;; Statement
attack ;; Attack
organized ;; Arranging || attacks ;; Attack || attack ;; Attack
condemned ;; Judgment_communication || attack ;; Attack
held ;; Hold
played ;; Competition || determine ;; Deciding
scored ;; Getting || tournament ;; Competition
final ;; Process_end
tournament ;; Competition
competition ;; Competition || game ;; Competition || took ;; Getting
game ;; Competition || hit ;; Body_movement || took ;; Getting
defeated ;; Conquering || beat ;; Competition
final ;; Process_end || beating ;; Preventing_or_letting || tournament ;; Competition
scored ;; Getting
Battle ;; Hostile_encounter || played ;; Competition || match ;; Competition || used ;; Using
match ;; Competition || fouls ;; Body_movement || overlooked ;; Judgment_communication || challenge ;; Competition
play ;; Competition || game ;; Competition || openings ;; Openness || dictated ;; Deciding || created ;; Creating || progressed ;; Cause_to_make_progress
awarded ;; Award || tumbled ;; Body_movement
converted ;; Change || scored ;; Getting
ended ;; Process_end || record-breaking ;; Recording
given ;; Giving || dived ;; Motion_directional || believed ;; Statement
thrown ;; Motion || match ;; Competition || clubs ;; Becoming_a_member
criticising ;; Judgment_communication || describing ;; Statement
comments ;; Statement || fined ;; Judgment_communication || conduct ;; Action
challenge ;; Competition || banned ;; Preventing_or_letting || missed ;; Earnings_and_losses
result ;; Causation
suffered ;; Catastrophe || entered ;; Arriving || found ;; Know
finished ;; Process_end
met ;; Come_together || Final ;; Competition
Battle ;; Hostile_encounter || retirement ;; Departing || strain ;; Bodily_harm || said ;; Telling || put ;; Motion || considered ;; Statement || disoriented ;; Destroying
contested ;; Competition
determine ;; Deciding
marking ;; Recording
shots ;; Motion
score ;; Getting
made ;; Motion || kick ;; Body_movement
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || battle ;; Hostile_encounter || supported ;; Supporting || fought ;; Hostile_encounter
battle ;; Hostile_encounter || debated ;; Quarreling || occurred ;; Coming_to_be || suggests ;; Protest
describes ;; Statement
run ;; Motion || flows ;; Motion
match ;; Competition || decided ;; Deciding
match ;; Competition || competition ;; Competition
appearing ;; Presence || final ;; Competition
appearing ;; Presence || final ;; Competition
competition ;; Competition || met ;; Come_together || Final ;; Competition
received ;; Receiving || played ;; Competition || reach ;; Arriving || meant ;; Protest || final ;; Competition
beat ;; Competition
scored ;; Competition
became ;; Becoming || win ;; Getting
Bombardment ;; Attack || engagement ;; Hostile_encounter || War ;; Military_operation
bombardment ;; Attack || initiated ;; Process_start || war ;; Military_operation
command ;; Control || approached ;; Arriving
shelling ;; Attack || wrote ;; Statement || demonstration ;; Protest || disturb ;; Damaging || diminish ;; Cause_change_of_position_on_a_scale
entering ;; Arriving || fired ;; Use_firearm
firing ;; Use_firearm || hit ;; Attack || destroyed ;; Destroying || retreated ;; Escaping
defeating ;; Conquering
came ;; Arriving || came ;; Arriving
disallow ;; Hindering
scored ;; Getting
argue ;; Protest
scored ;; Getting || recovered ;; Recovering || reduced ;; Cause_change_of_position_on_a_scale
declared ;; Expressing_publicly
win ;; Getting
ended ;; Process_end
got ;; Getting
lost ;; Earnings_and_losses
ended ;; Process_end || lost ;; Earnings_and_losses
Championship ;; Competition || hosted ;; Social_event
competing ;; Competition
tournament ;; Competition || hosted ;; Social_event
competition ;; Competition || tournament ;; Competition || selection ;; Choosing || host ;; Social_event || debate ;; Protest || calling ;; Request || move ;; Change
championship ;; Competition || mix ;; Collaboration || comprised ;; Cause_to_be_included
losing ;; Earnings_and_losses || Quarterfinals ;; Competition || rematch ;; Competition || victory ;; Earnings_and_losses || captured ;; Conquering || defeating ;; Conquering || final ;; Competition || captured ;; Conquering
losing ;; Earnings_and_losses || Host ;; Come_together || playoffs ;; Competition || made ;; Earnings_and_losses || quarterfinal ;; Competition
played ;; Competition || championship ;; Competition || tournaments ;; Competition || establish ;; Creating
attended ;; Perception_active || record ;; Recording || tournament ;; Competition || games ;; Competition || Championship ;; Competition || tournament ;; Competition || surpassing ;; Cause_change_of_position_on_a_scale
protests ;; Protest || protests ;; Protest
causes ;; Causation || protests ;; Protest || earthquakes ;; Catastrophe || pollution ;; Damaging
claim ;; Statement || resignation ;; Change_of_leadership || changed ;; Change
opposed ;; Agree_or_refuse_to_act || supports ;; Supporting
began ;; Process_start || organized ;; Arranging || protests ;; Protest || movements ;; Participation
injured ;; Bodily_harm || protests ;; Protest || degenerated ;; Change || clashes ;; Hostile_encounter
protests ;; Protest
Beginning ;; Process_start || protested ;; Protest
raid ;; Attack || arrested ;; Arrest || named ;; Name_conferral
protests ;; Protest
violence ;; Violence || incident ;; Protest || led to ;; Causation || fighting ;; Hostile_encounter
control ;; Control || took ;; Conquering || fighting ;; Hostile_encounter
secured ;; Getting || regain ;; Getting
launched ;; Process_start || offensive ;; Attack || conjunction ;; Collaboration || assault ;; Attack
controlled ;; Control
collision ;; Catastrophe
accident ;; Catastrophe || occurred ;; Coming_to_be
come to ;; Self_motion
killed ;; Killing || collision ;; Catastrophe || accident ;; Catastrophe || injured ;; Bodily_harm
accident ;; Catastrophe || held ;; Hold
determined ;; Deciding || passed ;; Motion_directional || accident ;; Catastrophe
contested ;; Protest
passed ;; Motion_directional
accident ;; Catastrophe || installed ;; Placing
summoned ;; Request
disaster ;; Catastrophe || rollout ;; Cause_to_make_progress || led to ;; Causation
fitted ;; Change
reasons ;; Protest || marathon ;; Competition
event ;; Social_event
race ;; Competition
race ;; Competition
records ;; Recording || set ;; Placing || run ;; Self_motion
race ;; Competition || record ;; Recording || completing ;; Process_end || broke ;; Destroying
walks ;; Self_motion || participate ;; Participation || event ;; Social_event
tour ;; Traveling
began ;; Process_start || ended ;; Process_end || promoted ;; Cause_change_of_position_on_a_scale
marked ;; Recording
fact ;; Protest || getting ;; Getting
interpreter ;; Communication
think ;; Coming_to_believe
war ;; Hostile_encounter || taking place ;; Process_start || War ;; Hostile_encounter || War ;; Hostile_encounter || War ;; Hostile_encounter
conflicts ;; Hostile_encounter || took place ;; Process_start || war ;; Hostile_encounter
War ;; Hostile_encounter || Wars ;; Hostile_encounter
participants ;; Participation || aided ;; Assistance || involved ;; Participation || support ;; Supporting || considered ;; Coming_to_believe || sought ;; Motion || War ;; Hostile_encounter || War ;; Hostile_encounter || war ;; Hostile_encounter || advance ;; Self_motion || influenced ;; Influence
began ;; Process_start || became ;; Becoming || conflict ;; Hostile_encounter || involved ;; Participation || War ;; Hostile_encounter
provided ;; Supply || expansion ;; Expansion || preventing ;; Preventing_or_letting || shared ;; Communication
alliance ;; Collaboration || formed ;; Coming_to_be || put ;; Removing || rebellion ;; Change_of_leadership
war ;; Hostile_encounter || ended ;; Process_end || surrender ;; Surrendering || forced ;; Request
Capture ;; Conquering || Wedding ;; Social_event || referred ;; Statement
concluded ;; Process_end || fighting ;; Hostile_encounter || captured ;; Conquering || took place ;; Process_start || forces ;; Military_operation || drove ;; Motion
named ;; Name_conferral || Wedding ;; Social_event
proved ;; Convincing
loss ;; Earnings_and_losses || marked ;; Recording || led to ;; Causation || held ;; Hold
aimed ;; Aiming
rallies ;; Social_event || held ;; Defending
protest ;; Response || held ;; Defending
reactions ;; Response || rally ;; Social_event || organized ;; Arranging
turnout ;; Social_event || demonstrations ;; Protest || occupation ;; Attack
demanding ;; Request || reunification ;; Cause_to_amalgamate || carried ;; Bringing || condemning ;; Judgment_communication
hang ;; Placing || rally ;; Social_event || made ;; Action
tournament ;; Competition || Cup ;; Competition || played ;; Competition
Cup ;; Competition || Cup ;; Competition || Hosted ;; Social_event
tournament ;; Competition
attend ;; Becoming_a_member || protest ;; Protest || invited ;; Request || withdrew ;; Removing
replaced ;; Removing
winning ;; Getting || tournament ;; Competition
matches ;; Competition || matches ;; Competition
Cup ;; Competition || tied ;; Connect || marked ;; Recording
tournament ;; Competition || tournament ;; Competition
Battle ;; Hostile_encounter || defeated ;; Conquering || commanded ;; Control || commanded ;; Control || took place ;; Process_start
battle ;; Hostile_encounter || accounts ;; Statement
battle ;; Hostile_encounter || places ;; Placing || favoured ;; Choosing
encounter ;; Hostile_encounter || encountered ;; Hostile_encounter || took place ;; Process_start
accounts ;; Statement || happened ;; Presence
increase ;; Cause_change_of_position_on_a_scale || used ;; Using
fled ;; Escaping || sunk ;; Motion_directional || leaving ;; Departing || burnt ;; Destroying || captured ;; Conquering
blamed ;; Judgment_communication || capture ;; Conquering
sent ;; Sending || said ;; Telling
gouged ;; Bodily_harm || left ;; Departing
navigate ;; Self_motion || sign ;; Sign_agreement || brought ;; Bringing
sparked ;; Process_start
held ;; Hold || culminated ;; Process_end || storm ;; Attack
asserted ;; Statement || uprising ;; Change_of_leadership || Taking ;; Conquering
disputed ;; Quarreling
saying ;; Telling || acknowledged ;; Statement
fighting ;; Hostile_encounter || occurred ;; Coming_to_be
fighting ;; Hostile_encounter || end ;; Process_end
surrendered ;; Surrendering || broke out ;; Process_start || made ;; Creating
mutinied ;; Change_of_leadership || Defence ;; Defending
called ;; Name_conferral || formed ;; Coming_to_be
sponsored ;; Supply || composed ;; Collaboration
took ;; Conquering
fighting ;; Hostile_encounter || flee ;; Escaping || end ;; Process_end || forced ;; Request || forced ;; Request
agreed ;; Agree_or_refuse_to_act || withdraw ;; Escaping || left ;; Departing || making ;; Creating || repelling ;; Defending
signed ;; Sign_agreement || bring ;; Bringing
told ;; Telling || finished ;; Process_end || pushed ;; Self_motion
surrendered ;; Surrendering || crossed ;; Motion_directional
exploded ;; Attack || occurred ;; Coming_to_be || bombing ;; Attack
killed ;; Killing || blast ;; Attack || injured ;; Bodily_harm
blast ;; Attack || frequented ;; Traveling
attack ;; Attack || claimed ;; Statement
use ;; Using || attack ;; Attack
involvement ;; Participation || accused ;; Judgment_communication
tour ;; Traveling || recording ;; Recording
tour ;; Traveling || visited ;; Traveling || support ;; Supporting
tour ;; Traveling || announced ;; Expressing_publicly
stated ;; Statement
received ;; Receiving || tour ;; Traveling
tour ;; Traveling || took place ;; Process_start
promote ;; Cause_change_of_position_on_a_scale || set out ;; Placing
visited ;; Traveling
visited ;; Traveling
visited ;; Traveling
started ;; Process_start
came to a close ;; Process_end
embarked ;; Process_start
debuted ;; Process_start
performing ;; Hold || made ;; Manufacturing
commenced ;; Process_start || visited ;; Traveling
cancelled ;; Change_event_time || entered ;; Arriving || announced ;; Expressing_publicly || tour ;; Traveling
leading to ;; Causation
restarted ;; Process_start
visiting ;; Traveling
celebrated ;; Social_event || touring ;; Traveling
taking ;; Conquering || tour ;; Traveling || enlist ;; Becoming_a_member
performed ;; Hold
overlapped ;; Motion_directional
records ;; Recording || headline ;; Statement
grossed ;; Earnings_and_losses || attracted ;; Motion
becoming ;; Becoming || attendance ;; Participation || revenue ;; Earnings_and_losses || grossing ;; Earnings_and_losses
tour ;; Traveling
support ;; Supporting
visited ;; Traveling
sit ;; Placing || used ;; Using
use ;; Using
saying ;; Statement || released ;; Publishing
performed ;; Hold
Final ;; Competition || final ;; Competition
match ;; Competition || contested ;; Competition
beaten ;; Competition || final ;; Competition || visit ;; Traveling
final ;; Competition || played ;; Competition || contested ;; Competition
televised ;; Communication || matches ;; Competition
defeated ;; Conquering || final ;; Competition
defeated ;; Conquering || final ;; Competition
support ;; Supporting || took place ;; Process_start || concert ;; Social_event || Tour ;; Traveling || tour ;; Traveling
took place ;; Process_start || tour ;; Traveling || consisted ;; Cause_to_be_included
perform ;; Hold || tour ;; Traveling
perform ;; Hold
started ;; Process_start || concerts ;; Social_event || tour ;; Traveling
festival ;; Social_event || tour ;; Traveling || headlined ;; Statement || appearances ;; Presence || sharing ;; Communication
support ;; Supporting || included ;; Cause_to_be_included
beginning ;; Process_start || finishing ;; Process_end || tour ;; Traveling || legs ;; Social_event || consisted ;; Cause_to_be_included
promote ;; Cause_change_of_position_on_a_scale || concert ;; Social_event || organised ;; Arranging || tour ;; Traveling
began ;; Process_start || ended ;; Process_end || tour ;; Traveling
participated ;; Participation
shows ;; Social_event || cancelled ;; Change_event_time
ended ;; Process_end || tour ;; Traveling
cancelled ;; Change_event_time
used ;; Using || recordings ;; Recording || appearance ;; Presence || made ;; Creating || videotaped ;; Recording
appearance ;; Presence || broadcast ;; Expressing_publicly || released ;; Publishing
recorded ;; Recording || released ;; Publishing
Tour ;; Traveling || concert ;; Social_event || tour ;; Traveling
toured ;; Traveling || joining ;; Becoming_a_member
tours ;; Traveling
effects ;; Influence || showed ;; Presence
known ;; Know
tours ;; Traveling || played ;; Competition || opened ;; Process_start
began ;; Process_start || ended ;; Process_end || tour ;; Traveling
visited ;; Traveling || final ;; Process_end || returning ;; Arriving
received ;; Receiving || gained ;; Getting || tour ;; Traveling || reactions ;; Communication
tours ;; Traveling || concert ;; Social_event || rebels ;; Hostile_encounter || tour ;; Traveling
tour ;; Traveling
began ;; Process_start || concluded ;; Process_end
announced ;; Expressing_publicly
expanded ;; Expansion
documented ;; Recording
prompting ;; Causation || add ;; Cause_change_of_position_on_a_scale
reached ;; Achieve || included ;; Cause_to_be_included
held ;; Defending
placed ;; Placing
support ;; Supporting || tour ;; Traveling
went ;; Arriving
announced ;; Expressing_publicly || canceled ;; Removing
supports ;; Supporting || release ;; Publishing || tour ;; Traveling
appearance ;; Presence
performed ;; Hold
joined ;; Becoming_a_member
performed ;; Hold
performed ;; Hold || Renewed ;; Recovering
performed ;; Hold
Tour ;; Traveling || tour ;; Traveling || recording ;; Recording
support ;; Supporting || launched ;; Process_start
Tour ;; Traveling || Announced ;; Statement || extended ;; Expansion || include ;; Cause_to_be_included || run ;; Motion
served ;; Assistance || support ;; Supporting
performed ;; Hold || visited ;; Traveling || marks ;; Recording
noted ;; Know || tour ;; Traveling || inspired ;; Influence
performed ;; Hold || set ;; Placing || consisted ;; Cause_to_be_included
praise ;; Judgment_communication || mixed ;; Cause_to_amalgamate || garnered ;; Getting || reaction ;; Response
commended ;; Judgment_communication
tour ;; Traveling
included ;; Cause_to_be_included
cancelled ;; Change_event_time || attacks ;; Attack
support ;; Supporting || concert ;; Social_event || tour ;; Traveling
announced ;; Expressing_publicly || announced ;; Expressing_publicly || shows ;; Social_event
announced ;; Expressing_publicly
served ;; Assistance
tour ;; Traveling
starting ;; Process_start || Tour ;; Traveling || ending ;; Process_end
support ;; Supporting
travelled ;; Traveling || marks ;; Recording
announced ;; Expressing_publicly
shows ;; Social_event
tour ;; Traveling
focused ;; Aiming || tour ;; Traveling
tour ;; Traveling || released ;; Presence
released ;; Presence
present ;; Presence || omitted ;; Removing
featured ;; Presence
released ;; Publishing || debut ;; Process_start
included ;; Cause_to_be_included
held ;; Defending || tours ;; Traveling || concert ;; Social_event
became ;; Becoming || concert ;; Social_event || support ;; Supporting || debut ;; Process_start || hold ;; Hold
concert ;; Social_event || support ;; Supporting || debut ;; Process_start || held ;; Hold
concert ;; Social_event || support ;; Supporting || held ;; Hold
began ;; Process_start || debut ;; Process_start || concert ;; Social_event || tour ;; Traveling || support ;; Supporting || debut ;; Process_start || tour ;; Traveling || Tour ;; Traveling || visited ;; Traveling || embarked ;; Process_start
concert ;; Social_event || tours ;; Traveling || support ;; Supporting || embarked ;; Process_start
visited ;; Traveling || visited ;; Traveling
tours ;; Traveling || tour ;; Traveling || embarked ;; Process_start
held ;; Hold
visited ;; Traveling || becoming ;; Becoming || tour ;; Traveling
became ;; Becoming || perform ;; Hold
visited ;; Traveling || Tour ;; Traveling
held ;; Defending || held ;; Defending || concert ;; Social_event || meetings ;; Social_event
meeting ;; Social_event || party ;; Social_event || held ;; Hold
support ;; Supporting || taking place ;; Process_start || concert ;; Social_event || tour ;; Traveling || Tour ;; Traveling
began ;; Process_start || visit ;; Traveling || supporting ;; Supporting || tour ;; Traveling || leg ;; Traveling || playing ;; Competition
supporting ;; Supporting || supporting ;; Supporting || tour ;; Traveling || tour ;; Traveling
supporting ;; Supporting || festival ;; Social_event || tour ;; Traveling || run ;; Self_motion || Festivals ;; Social_event || playing ;; Competition
chosen ;; Choosing || support ;; Supporting || took place ;; Process_start || supporting ;; Supporting || fall ;; Motion_directional || supporting ;; Supporting || tour ;; Traveling || tour ;; Traveling
supported ;; Supporting || played ;; Competition || tour ;; Traveling || played ;; Competition || tour ;; Traveling || added ;; Cause_to_be_included
festival ;; Social_event || shows ;; Social_event || played ;; Competition
supported ;; Supporting || joining ;; Becoming_a_member || supporting ;; Supporting || leg ;; Traveling || show ;; Social_event || tour ;; Traveling
visiting ;; Traveling || visited ;; Traveling || headlined ;; Communication || tour ;; Traveling || touring ;; Traveling
supported ;; Supporting || tour ;; Traveling
supported ;; Supporting || leg ;; Traveling
landing ;; Self_motion
appear ;; Change_sentiment || arrival ;; Arriving
end ;; Process_end
named ;; Name_conferral
visitors ;; Traveling
end ;; Process_end
parking ;; Placing
become ;; Becoming
took place ;; Process_start
visitors ;; Traveling || fairs ;; Social_event
begins ;; Process_start
Festival ;; Social_event || festival ;; Social_event || takes place ;; Process_start
include ;; Cause_to_be_included
visiting ;; Traveling
festival ;; Social_event || attending ;; Participation
situated ;; Placing
take place ;; Hold
concerts ;; Social_event
tour ;; Traveling
began ;; Process_start || support ;; Supporting
opted ;; Choosing || abandoned ;; GiveUp || participate ;; Participation || embarked ;; Participation
visited ;; Traveling
recruited ;; Becoming_a_member
tour ;; Traveling || praising ;; Judgment_communication || produced ;; Creating
tour ;; Traveling || grossed ;; Earnings_and_losses
released ;; Publishing
tour ;; Traveling
support ;; Supporting || tour ;; Traveling || launched ;; Process_start
acclaimed ;; Judgment_communication || tours ;; Traveling
tour ;; Traveling || grossed ;; Earnings_and_losses
became ;; Becoming || tour ;; Traveling
launched ;; Process_start
began ;; Process_start
performed ;; Hold || visited ;; Traveling
collaborated ;; Collaboration || support ;; Supporting || sponsored ;; Supply
concluded ;; Process_end
took place ;; Process_start || include ;; Cause_to_be_included
grossed ;; Earnings_and_losses
tour ;; Traveling
support ;; Supporting || launched ;; Bringing || visited ;; Traveling
released ;; Publishing || announced ;; Expressing_publicly || announced ;; Expressing_publicly
produced ;; Manufacturing
praising ;; Judgment_communication || tour ;; Traveling || performing ;; Hold
tour ;; Traveling || Tour ;; Traveling
started ;; Process_start || came to ;; Motion || end ;; Process_end
performed ;; Hold || visiting ;; Traveling
concerts ;; Social_event
released ;; Publishing || recorded ;; Recording
recording ;; Recording || Tour ;; Traveling || tour ;; Traveling || support ;; Supporting
began ;; Process_start || tour ;; Traveling || shows ;; Social_event || shows ;; Social_event || shows ;; Social_event || shows ;; Social_event || shows ;; Social_event || concluded ;; Process_end || consisting ;; Cause_to_be_included
tour ;; Traveling || show ;; Social_event || Given ;; Causation || acknowledged ;; Statement
tour ;; Traveling || promoted ;; Cause_to_make_progress
tour ;; Traveling || promoted ;; Cause_to_make_progress
earning ;; Earnings_and_losses || ranked ;; Placing || tour ;; Traveling || shows ;; Social_event
earning ;; Earnings_and_losses || placed ;; Placing || tour ;; Traveling || shows ;; Social_event
earned ;; Earnings_and_losses || placing ;; Placing || tour ;; Traveling || shows ;; Social_event
tour ;; Traveling || grossed ;; Earnings_and_losses
support ;; Supporting || tour ;; Traveling || put on ;; Placing
spanned ;; Expansion
named ;; Name_conferral || promote ;; Cause_change_of_position_on_a_scale
sent ;; Sending || concert ;; Social_event
opened ;; Openness || starting ;; Process_start || open ;; Openness
held ;; Hold || tournament ;; Competition
held ;; Hold || switched ;; Change || tournament ;; Competition || granted ;; Supporting
matches ;; Competition || played ;; Competition
played ;; Competition || matches ;; Competition
participate ;; Participation || tournament ;; Competition || visited ;; Traveling
competed ;; Competition
split ;; Change
played ;; Competition || proceeded ;; Self_motion
defeated ;; Earnings_and_losses || lost ;; Earnings_and_losses
washed ;; Removing
tour ;; Traveling || Tour ;; Traveling
supported ;; Supporting || tour ;; Traveling
shows ;; Social_event || Beginning ;; Process_start || tour ;; Traveling || played ;; Participation
launched ;; Motion || success ;; Earnings_and_losses || tour ;; Traveling || capitalise ;; Hold
tour ;; Traveling || marks ;; Recording
tour ;; Traveling
shot ;; Use_firearm || wounded ;; Bodily_harm || shooting ;; Use_firearm || took place ;; Process_start
shooting ;; Use_firearm || shot ;; Use_firearm || suicide ;; Killing
drew ;; Getting
attended ;; Participation
visited ;; Traveling
occurred ;; Coming_to_be || killed ;; Killing || killing ;; Killing || shooting ;; Killing || shootings ;; Killing || injured ;; Bodily_harm
massacre ;; Killing
began ;; Process_start || assistance ;; Assistance || moved ;; Motion
found ;; Know || abandoned ;; GiveUp || dead ;; Death
used ;; Using || appeared ;; Coming_to_be || recovered ;; Recovering
investigated ;; Criminal_investigation
massacre ;; Killing || dead ;; Death || left ;; Causation || shooting ;; Killing
paid ;; Giving || visited ;; Traveling
visited ;; Traveling
aid ;; Assistance || set up ;; Placing
promoting ;; Cause_to_make_progress || concert ;; Social_event || tour ;; Traveling || release ;; Publishing
tour ;; Traveling || consisted ;; Cause_to_be_included
started ;; Process_start || included ;; Cause_to_be_included || ending ;; Process_end || concerts ;; Social_event || tour ;; Traveling
included ;; Cause_to_be_included || supporting ;; Supporting
opening ;; Openness
performed ;; Action || shows ;; Social_event
included ;; Cause_to_be_included
held ;; Hold
visited ;; Traveling || Festival ;; Social_event
held ;; Hold
varies ;; Change
constructed ;; Building || held ;; Hold
enjoy ;; Perception_active
enjoy ;; Perception_active
created ;; Creating
commenced ;; Process_start || concluded ;; Process_end
earning ;; Earnings_and_losses || performed ;; Action
made ;; Creating
appeared ;; Presence
visited ;; Traveling || alleged ;; Statement || taken place ;; Process_start
played ;; Competition
beat ;; Competition || went ;; Motion
beat ;; Competition || travelled ;; Motion
played ;; Competition
travelling ;; Traveling || make ;; Motion || entered ;; Arriving
started ;; Process_start || scored ;; Getting
scoring ;; Getting || struck ;; Attack || lead ;; Causation
extended ;; Expansion
wrestling ;; Competition || event ;; Social_event || produced ;; Manufacturing || took place ;; Process_start
combined ;; Cause_to_amalgamate || competitive ;; Competition || wrestling ;; Competition
matches ;; Competition || event ;; Social_event || took place ;; Process_start || planned ;; Arranging
Wrestling ;; Competition || guest ;; Traveling || event ;; Social_event || starred ;; Participation
event ;; Social_event
wrestling ;; Competition
held ;; Hold || matches ;; Competition
match ;; Competition || match ;; Competition || event ;; Social_event || defeated ;; Conquering
matches ;; Competition || match ;; Competition || match ;; Competition || match ;; Competition || included ;; Cause_to_be_included || defeated ;; Conquering || defeat ;; Conquering || defeated ;; Conquering || defeated ;; Conquering
released ;; Publishing || attendance ;; Participation || event ;; Social_event
events ;; Social_event
reaching ;; Arriving || tour ;; Traveling
support ;; Supporting || concerts ;; Social_event
performed ;; Hold
concerts ;; Social_event || mixed ;; Cause_to_amalgamate || worked ;; Action || produced ;; Creating
selected ;; Choosing
tour ;; Traveling
becoming ;; Becoming || tour ;; Traveling || grossed ;; Earnings_and_losses || saw ;; Self_motion
noted ;; Know || praised ;; Judgment_communication || leave ;; Bringing
held ;; Hold
participated ;; Participation
held ;; Hold
participated ;; Participation
decided ;; Deciding || launch ;; Process_start
signed ;; Sign_agreement
included ;; Cause_to_be_included
conflict ;; Hostile_encounter || crisis ;; Hostile_encounter || present ;; Presence || aftermath ;; Hostile_encounter || uprising ;; Hostile_encounter
agreed ;; Agree_or_refuse_to_act || started ;; Process_start || ceasefire ;; Process_end || uprising ;; Hostile_encounter
conflict ;; Hostile_encounter
signed ;; Sign_agreement || Negotiations ;; Communication || led to ;; Causation || agreements ;; Sign_agreement || complied ;; Agree_or_refuse_to_act || stagnated ;; Hindering
increasing ;; Cause_change_of_position_on_a_scale || resulted in ;; Causation
incidents ;; Catastrophe || increased ;; Cause_change_of_position_on_a_scale || killing ;; Killing || conflict ;; Hostile_encounter || culminating ;; Process_end
violence ;; Violence || supporters ;; Supporting || erupts ;; Process_start || rebel ;; Hostile_encounter
occurred ;; Presence || killed ;; Killing || incident ;; Hostile_encounter || wounded ;; Bodily_harm
started ;; Process_start
offering ;; Supply
playing ;; Competition || played ;; Competition
called ;; Name_conferral || run ;; Self_motion
became ;; Becoming || playing ;; Competition
stated ;; Statement || replaced ;; Change_of_leadership
stated ;; Statement || due ;; Causation || running out of ;; Expend_resource
begin ;; Process_start || tour ;; Traveling
tour ;; Traveling
began ;; Process_start
announced ;; Expressing_publicly || return ;; Arriving
took place ;; Process_start
signing ;; Sign_agreement || recording ;; Recording || led to ;; Causation
Battle ;; Hostile_encounter
started ;; Process_start || play ;; Competition
determine ;; Deciding || voting ;; Choosing || win ;; Getting
select ;; Choosing || play ;; Competition
Battle ;; Hostile_encounter || battles ;; Hostile_encounter || Campaign ;; Military_operation
agree ;; Agree_or_refuse_to_act || negotiations ;; Communication
refused ;; Agree_or_refuse_to_act || released ;; Releasing || refused ;; Agree_or_refuse_to_act || sign ;; Sign_agreement
caused ;; Causation || negotiations ;; Communication || derail ;; Hindering
decided ;; Deciding || aimed ;; Aiming || launch ;; Motion
scoring ;; Getting || offensive ;; Defending
began ;; Process_start || completed ;; Process_end || withdrawal ;; Escaping
peaked ;; Cause_change_of_position_on_a_scale
withdrawal ;; Escaping
shifted ;; Change || progressed ;; Achieve || believed ;; Know || favored ;; Supporting
vetoed ;; Agree_or_refuse_to_act || set ;; Placing
signed ;; Sign_agreement
included ;; Cause_to_be_included || withdraw ;; Escaping
left ;; Departing
prompted ;; Causation || intervene ;; Hindering
put ;; Placing
became ;; Becoming || starting ;; Process_start || held ;; Hold
organized ;; Arranging || sign ;; Sign_agreement || originated ;; Coming_to_be || took effect ;; Change
blocked ;; Hindering
commemorate ;; Social_event || motivated ;; Causation
issued ;; Creating
revolutionary ;; Change_of_leadership || ruled ;; Control
War ;; Hostile_encounter || led to ;; Causation || capture ;; Conquering || beginning ;; Process_start || collapse ;; Destroying
siege ;; Besieging || moved ;; Motion
defended ;; Defending
surrendered ;; Surrendering || disarmed ;; Removing || signed ;; Sign_agreement
refused ;; Agree_or_refuse_to_act || killed ;; Killing
beginning ;; Process_start || governed ;; Control || suppressed ;; Hindering
described ;; Statement
confrontation ;; Hostile_encounter
called ;; Name_conferral || struggle ;; Hostile_encounter
disputed ;; Quarreling || reported ;; Reporting || faced ;; Confronting_problem
came ;; Motion
ended ;; Process_end || attacks ;; Attack || bombarding ;; Attack || besieged ;; Besieging || launched ;; Motion
attacked ;; Attack || returned ;; Arriving || surrender ;; Surrendering || defeated ;; Conquering || left ;; Causation || starve ;; Bodily_harm
signed ;; Sign_agreement || agreement ;; Agree_or_refuse_to_act || ceded ;; Surrendering
emigrated ;; Removing || retreated ;; Escaping
became ;; Becoming
captured ;; Conquering || held ;; Defending
revolution ;; Change_of_leadership || revolution ;; Change_of_leadership || uprisings ;; Change_of_leadership || uprisings ;; Change_of_leadership || started ;; Process_start || known ;; Know || rebellions ;; Change_of_leadership || revolts ;; Change_of_leadership || war ;; Military_operation || lasted ;; Process_end
supported ;; Supporting || party ;; Social_event
began ;; Process_start || uprisings ;; Change_of_leadership || signing ;; Sign_agreement || giving ;; Giving || siege ;; Besieging || opposition ;; Judgment_communication || concessions ;; Agree_or_refuse_to_act
revolution ;; Change_of_leadership || suppress ;; Hindering || becoming ;; Becoming || grew ;; Cause_change_of_position_on_a_scale
rebellions ;; Change_of_leadership || occurred ;; Coming_to_be || insurrections ;; Change_of_leadership
sending ;; Sending || supplies ;; Supply || fighting ;; Hostile_encounter || aid ;; Assistance
opposition ;; Judgment_communication || faced ;; Confronting_problem
combined ;; Cause_to_amalgamate || called ;; Name_conferral || faced ;; Confronting_problem
used ;; Using || control ;; Control || counter-revolution ;; Change_of_leadership || commanded ;; Control
battle ;; Hostile_encounter || capture ;; Conquering || campaign ;; Military_operation || war ;; Military_operation
prevent ;; Preventing_or_letting || seizure ;; Conquering || deny ;; Preventing_or_letting
began ;; Process_start || seizure ;; Conquering
campaign ;; Military_operation || secure ;; Getting || opened ;; Process_start
secured ;; Getting || forces ;; Causation || linking ;; Connect || broke ;; Destroying
ceased ;; Process_end || granted ;; Giving || armistice ;; Sign_agreement
combining ;; Cause_to_amalgamate || operation ;; Military_operation || War ;; Military_operation
conflict ;; Hostile_encounter || War ;; Military_operation || War ;; Military_operation
bogged ;; Motion
recover ;; Recovering || lost ;; Earnings_and_losses || attacked ;; Attack
withdraw ;; Escaping || war ;; Military_operation || campaign ;; Military_operation || move ;; Change || outbreak ;; Process_start || provided ;; Supply
suffered ;; Bodily_harm
yielded ;; Getting
became ;; Becoming || control ;; Control || signing ;; Sign_agreement || treaty ;; Sign_agreement
erected ;; Building
attacked ;; Attack
led to ;; Causation || attacking ;; Attack || took place ;; Process_start || changing ;; Change
defeat ;; Conquering
reached ;; Arriving || took place ;; Process_start || signed ;; Sign_agreement
built ;; Building
War ;; Hostile_encounter || disputes ;; Quarreling
war ;; Hostile_encounter || affected ;; Influence
captured ;; Conquering
defeated ;; Conquering
ended ;; Process_end || signing ;; Sign_agreement || war ;; Hostile_encounter
captured ;; Conquering || return ;; Arriving
began ;; Process_start || dominated ;; Control
organize ;; Arranging
supported ;; Supporting
occurred ;; Coming_to_be
dragged on ;; Change_event_time
killed ;; Killing
signed ;; Sign_agreement
forces ;; Causation || War ;; Military_operation
war ;; Military_operation || dispute ;; Quarreling
pressure ;; Convincing || held ;; Defending || moved ;; Self_motion || restoring ;; Recovering
response ;; Response || besieged ;; Besieging || sent ;; Sending || relieve ;; Conquering || urged ;; Request || send ;; Sending
besieging ;; Besieging
forces ;; Causation || defeated ;; Getting
forces ;; Causation || Battle ;; Hostile_encounter
defend ;; Defending || left ;; Departing
signed ;; Sign_agreement || war ;; Military_operation
allowing ;; Preventing_or_letting || War ;; Military_operation || challenge ;; Confronting_problem || campaigns ;; Military_operation
attack ;; Attack || took place ;; Process_start
began ;; Process_start || took place ;; Process_start
began ;; Process_start || attack ;; Attack || Fought ;; Hostile_encounter
retreat ;; Escaping || attacked ;; Attack || captured ;; Conquering
retreat ;; Escaping || attacked ;; Attack || battle ;; Hostile_encounter || captured ;; Conquering
began ;; Process_start || hold ;; Defending || leaving ;; Departing || capture ;; Conquering || capturing ;; Conquering || attacking ;; Attack
retreat ;; Escaping || attack ;; Attack || capture ;; Conquering
surrendered ;; Surrendering || ending ;; Process_end
attack ;; Attack || resulted in ;; Causation || capture ;; Conquering
retreat ;; Escaping || forced ;; Motion
resulting in ;; Causation || battle ;; Hostile_encounter || capture ;; Conquering || extended ;; Expansion
captured ;; Conquering
escaped ;; Escaping || occupied ;; Attack || captured ;; Conquering
ending ;; Process_end || signed ;; Sign_agreement
struggles ;; Hostile_encounter || invaded ;; Attack || called ;; Name_conferral || conflicts ;; Hostile_encounter || proclaimed ;; Statement || signing ;; Sign_agreement || war ;; Military_operation || war ;; Military_operation || war ;; Military_operation
war ;; Military_operation
involved ;; Cause_to_be_included || supported ;; Supporting || war ;; Military_operation
added ;; Cause_to_be_included || force ;; Hostile_encounter || allied ;; Collaboration
leading to ;; Causation || open ;; Openness || war ;; Military_operation
developed ;; Coming_to_be || dissolving ;; Dispersal
included ;; Cause_to_be_included || interested ;; Change_sentiment || eliminating ;; Cause_change_of_position_on_a_scale || fighting ;; Hostile_encounter || allied ;; Collaboration || war ;; Military_operation || War ;; Military_operation
Battle ;; Hostile_encounter || War ;; Military_operation || War ;; Military_operation
surrendered ;; Surrendering || battle ;; Hostile_encounter || took place ;; Process_start
signing ;; Sign_agreement || occupied ;; Control || ruled ;; Control
began ;; Process_start
arrived at ;; Arriving || made ;; Manufacturing
entered ;; Arriving
surrendered ;; Surrendering || established ;; Building
arrived ;; Arriving || control ;; Control
occurred ;; Presence
co-existed ;; Presence
arising ;; Coming_to_be || combined ;; Cause_to_amalgamate || produce ;; Creating || disputes ;; Quarreling || recreate ;; Recovering
became ;; Becoming || handing over ;; Giving || achieving ;; Achieve || unify ;; Cause_to_amalgamate
viewed ;; Perception_active || obstacle ;; Hindering
provided ;; Supply || aid ;; Assistance || overthrow ;; Change_of_leadership
began ;; Process_start || demanded ;; Request || decided ;; Deciding || push ;; Motion || meeting ;; Social_event
send ;; Sending || brought ;; Bringing || prompting ;; Preventing_or_letting
taken captive ;; Motion || crossed ;; Motion_directional || killed ;; Killing || leading to ;; Causation || defeated ;; Conquering || wounded ;; Bodily_harm
agreed ;; Agree_or_refuse_to_act || reaching ;; Arriving || brought ;; Bringing || encountering ;; Hostile_encounter
agreed ;; Agree_or_refuse_to_act || negotiations ;; Communication || led to ;; Causation || signed ;; Sign_agreement || return ;; Motion
known ;; Know || taken place ;; Process_start || storm ;; Catastrophe
used ;; Using
begins ;; Process_start || ends ;; Process_end
included ;; Cause_to_be_included
donated ;; Giving || naming ;; Name_conferral
held ;; Hold
become ;; Becoming || signed on ;; Sign_agreement || changing ;; Change
erupted ;; Process_start
starting ;; Process_start || took ;; Creating || defeated ;; Conquering
demanded ;; Request || stop ;; Process_end || resulted ;; Causation
signed on ;; Sign_agreement
changed ;; Change
acknowledged ;; Statement
war ;; Hostile_encounter || fought ;; Hostile_encounter || party ;; Social_event
launched ;; Process_start || establishing ;; Building
ended ;; Process_end || signing ;; Sign_agreement
conflict ;; Hostile_encounter
deaths ;; Death || resulted in ;; Causation
dead ;; Death
supported ;; Supporting || fought ;; Hostile_encounter
avoided ;; Preventing_or_letting
fighting ;; Hostile_encounter || defeat ;; Conquering
supported ;; Supporting || founded ;; Creating || included ;; Cause_to_be_included || fought ;; Hostile_encounter
left ;; Departing || started ;; Process_start
targeted ;; Aiming || end ;; Process_end
became ;; Becoming
joined ;; Becoming_a_member || ended ;; Process_end
committed ;; Commitment || caused ;; Causation || agreed ;; Agree_or_refuse_to_act || support ;; Supporting || documented ;; Recording
occurred ;; Coming_to_be
emerged ;; Coming_to_be || influence ;; Influence || differing ;; Change || controlled ;; Control
Starting ;; Process_start || scattered ;; Dispersal || reached ;; Arriving || included ;; Cause_to_be_included
took place ;; Process_start || retreated ;; Escaping
called ;; Name_conferral || order ;; Arranging || resigned ;; Change_of_leadership
erupted ;; Process_start || opened ;; Openness || injuring ;; Bodily_harm || standing ;; Placing || killing ;; Killing
collaborators ;; Collaboration || organised ;; Arranging || signed ;; Sign_agreement
signing ;; Sign_agreement || lost ;; Earnings_and_losses || become ;; Becoming
escalating ;; Cause_change_of_position_on_a_scale
erupted ;; Process_start || organized ;; Arranging || found ;; Know || controlled ;; Control
deciding ;; Deciding || recognized ;; Know || formed ;; Coming_to_be || elections ;; Choosing
formed ;; Coming_to_be || used ;; Using
offered ;; Giving || support ;; Supporting || operating ;; Being_in_operation
suffered ;; Bodily_harm || increased ;; Cause_change_of_position_on_a_scale || aid ;; Assistance
helped ;; Assistance || led to ;; Causation
lead to ;; Causation
began ;; Process_start || declaration ;; Expressing_publicly || issued ;; Statement || entered ;; Arriving
deaths ;; Death || occurred ;; Coming_to_be
revolt ;; Change_of_leadership || developed ;; Cause_change_of_position_on_a_scale || developed ;; Cause_change_of_position_on_a_scale
erupted ;; Process_start || divide ;; Creating
declaration ;; Expressing_publicly || transformed ;; Change
declared ;; Expressing_publicly
took ;; Conquering || attacked ;; Attack
took place ;; Process_start || interrupted ;; Hindering
proposed ;; Statement || proposed ;; Statement || assembly ;; Building || controlled ;; Control || recommended ;; Convincing
took ;; Conquering || took ;; Conquering
conference ;; Communication
change ;; Change || triggered ;; Causation || conflict ;; Hostile_encounter
fled ;; Escaping || expelled ;; Removing || became ;; Becoming || became ;; Becoming
expelled ;; Removing
took place ;; Process_start
captured ;; Conquering || besieged ;; Besieging
broken out ;; Process_start || boosted ;; Cause_change_of_position_on_a_scale || return ;; Arriving
oppose ;; Agree_or_refuse_to_act
compelled ;; Convincing || surrender ;; Surrendering
forced ;; Causation || withdraw ;; Removing
damaged ;; Damaging || protect ;; Defending
abandoned ;; GiveUp || joined ;; Becoming_a_member
followed ;; Motion || taking ;; Conquering
remained ;; Presence || returned ;; Arriving || evacuated ;; Removing
killing ;; Killing || attacked ;; Attack || wounding ;; Bodily_harm
occurred ;; Coming_to_be || attack ;; Attack || adjourned ;; Change_event_time
parliament ;; Social_event
suicide ;; Killing || found ;; Know
suspected ;; Judgment_communication || suggesting ;; Convincing || searched ;; Scrutiny
flew ;; Motion || attack ;; Attack
took place ;; Process_start
took place ;; Process_start
battle ;; Hostile_encounter
operation ;; Military_operation
participating ;; Participation
battled ;; Hostile_encounter || airlifted ;; Bringing || obtain ;; Getting
fired ;; Use_firearm
join ;; Becoming_a_member
suggested ;; Convincing
noted ;; Know || found ;; Know || suggested ;; Convincing || killed ;; Killing || driven ;; Self_motion
declined ;; Agree_or_refuse_to_act || host ;; Come_together || war ;; Hostile_encounter
suggested ;; Convincing || change ;; Change
stated ;; Statement || help ;; Assistance || set ;; Placing
declined ;; Agree_or_refuse_to_act || hosting ;; Come_together
host ;; Come_together
opened ;; Openness || closed ;; Self_motion
event ;; Social_event
takes place ;; Process_start
open ;; Openness
included ;; Cause_to_be_included
takes place ;; Process_start
advised ;; Convincing
used ;; Using
changed ;; Change
uses ;; Using
shooting ;; Use_firearm
murdered ;; Killing || injured ;; Bodily_harm
ordered ;; Arranging || entered ;; Arriving
separated ;; Forming_relationships || instructing ;; Request
stated ;; Statement || opened ;; Action
shot ;; Use_firearm || killing ;; Killing
moved ;; Motion || turning ;; Becoming || targeting ;; Aiming
shooting ;; Use_firearm
debates ;; Communication
characterize ;; Judgment_communication
massacre ;; Catastrophe || commemorated ;; Social_event
effect ;; Influence || suggest ;; Convincing
blamed ;; Judgment_communication
led to ;; Causation
changes ;; Change || introduced ;; Statement
began ;; Process_start || landing ;; Self_motion || War ;; Hostile_encounter || invasion ;; Hostile_encounter || Battle ;; Hostile_encounter
landed ;; Self_motion
put ;; Placing
became ;; Becoming || mixed ;; Cause_to_amalgamate
discarded ;; GiveUp || coming ;; Motion || battle ;; Hostile_encounter || thrown ;; Motion || received ;; Receiving
ordered ;; Arranging || advanced ;; Cause_to_make_progress || diverted ;; Removing
achieve ;; Achieve || failed ;; Earnings_and_losses || formed ;; Creating
convinced ;; Convincing || ask ;; Request || decided ;; Deciding || stay ;; Temporary_stay || taking ;; Getting
known ;; Know || casualties ;; Death
killed ;; Killing || wounded ;; Bodily_harm || landed ;; Self_motion || casualties ;; Death
becoming ;; Becoming || commemorated ;; Social_event
commemorated ;; Social_event
occurred ;; Presence
killed ;; Killing || injured ;; Bodily_harm
derailed ;; Self_motion
started ;; Process_start || control ;; Control
ordered ;; Arranging || rushed ;; Motion
suggested ;; Convincing
announced ;; Expressing_publicly
Death ;; Death
invasion ;; Attack || used ;; Using
repaired ;; Self_motion || used ;; Using || War ;; Hostile_encounter || invasion ;; Attack || forces ;; Military_operation
deaths ;; Death || offensive ;; Judgment_communication || leave ;; Departing || destruction ;; Destroying || forces ;; Military_operation || attacked ;; Attack || resulting in ;; Causation
abandoned ;; GiveUp || hit ;; Attack
suggested ;; Convincing || war ;; Hostile_encounter || declare ;; Expressing_publicly
escaped ;; Escaping || fled ;; Escaping || defense ;; Defending || defeated ;; Conquering || evading ;; Escaping || capture ;; Conquering || forces ;; Military_operation
fought ;; Hostile_encounter
ended ;; Process_end || response ;; Response || invasion ;; Attack || occupation ;; Control
captured ;; Conquering || entered ;; Arriving
declared ;; Expressing_publicly || achieved ;; Achieve || open ;; Openness
withdrew ;; Escaping
claimed ;; Statement
remained ;; Presence || dissuading ;; Convincing
protect ;; Defending || deter ;; Hindering || demonstrating ;; Protest
crossing ;; Motion_directional || March ;; Motion_directional || Battle ;; Hostile_encounter
situated ;; Placing || crossing ;; Motion_directional
Fleeing ;; Escaping || forces ;; Causation || cross ;; Motion_directional || pursuing ;; Aiming || found ;; Know
built ;; Building || use ;; Using || forced ;; Request
consisted ;; Cause_to_be_included
provided ;; Supply || lashed ;; Connect
received ;; Receiving || captured ;; Conquering || order ;; Request
marched ;; Motion_directional
blocked ;; Hindering || forces ;; Causation || engaged ;; Hostile_encounter || defeated ;; Conquering
reached ;; Arriving || discover ;; Know || allied ;; Collaboration || removed ;; Removing
occupied ;; Control
reputed ;; Coming_to_believe
deployed ;; Arranging || deployed ;; Arranging
forces ;; Causation || defending ;; Defending || attacking ;; Attack || enjoyed ;; Getting
lost ;; Earnings_and_losses || reached ;; Arriving || march ;; Motion_directional || fallen ;; Earnings_and_losses || march ;; Motion_directional
founded ;; Creating || named ;; Name_conferral
held ;; Hold
taken place ;; Process_start
attended ;; Participation
took place ;; Process_start
returned ;; Arriving
estimated ;; Statement || generated ;; Creating
tied ;; Connect
announce ;; Expressing_publicly
spawned ;; Coming_to_be
championship ;; Competition || championship ;; Competition || championship ;; Competition || held ;; Hold
finished ;; Process_end || gained ;; Getting
leader ;; Becoming_a_member || strokes ;; Self_motion || tied ;; Connect
play ;; Competition
groups ;; Participation || score ;; Getting || lead ;; Placing
Championship ;; Competition || stroke ;; Self_motion || bogeyed ;; Motion || dropped ;; Cause_change_of_position_on_a_scale
Entering ;; Self_motion || round ;; Competition || final ;; Competition || tied ;; Connect
contention ;; Quarreling || tie ;; Connect || shot ;; Motion || knock ;; Motion || carded ;; Getting
tied ;; Connect || hurricane ;; Catastrophe || make ;; Causation || landfall ;; Catastrophe
strike ;; Attack
passing ;; Motion_directional || hurricane ;; Catastrophe || intensified ;; Cause_change_of_strength
storm ;; Catastrophe || swept ;; Motion || passed ;; Motion_directional
preventing ;; Preventing_or_letting || receded ;; Cause_change_of_position_on_a_scale || carving ;; Bodily_harm
weakening ;; Cause_change_of_strength || storm ;; Catastrophe || continued ;; Motion
caused ;; Causation || swept ;; Motion || damage ;; Damaging
destroyed ;; Destroying
obliterated ;; Destroying
destroyed ;; Destroying || damaged ;; Damaging
caused ;; Causation || damage ;; Damaging
record ;; Recording
moved ;; Motion || developed ;; Cause_to_make_progress
became ;; Becoming || entered ;; Arriving
strengthening ;; Cause_change_of_strength || making ;; Manufacturing || defied ;; Agree_or_refuse_to_act
becoming ;; Becoming
caused ;; Causation || dissipating ;; Dispersal || flooding ;; Motion
caused ;; Causation || affected ;; Influence || damage ;; Damaging
linked ;; Connect || storm ;; Catastrophe
contributed ;; Giving || impact ;; Influence || followed ;; Self_motion
help ;; Assistance || suffering ;; Bodily_harm || believed ;; Know
aided ;; Assistance || flooding ;; Motion || storm ;; Catastrophe
caused ;; Causation || leading to ;; Causation || flood ;; Motion
declared ;; Expressing_publicly || storm ;; Catastrophe || allowing ;; Preventing_or_letting
aid ;; Assistance || brought in ;; Bringing
caused ;; Causation || called ;; Name_conferral || damages ;; Damaging
protests ;; Response || meetings ;; Communication
called ;; Name_conferral || protests ;; Response || involved ;; Participation
dominated ;; Control
protest ;; Response || events ;; Social_event || led ;; Causation || protest ;; Response || meetings ;; Social_event
protest ;; Response || protests ;; Response
protest ;; Response || take place ;; Process_start
response ;; Response || events ;; Social_event || event ;; Social_event
linked ;; Connect || meetings ;; Communication || chained ;; Connect || getting ;; Getting || prevent ;; Preventing_or_letting
responded ;; Response || used ;; Using || permitted ;; Preventing_or_letting || reported ;; Reporting
tied ;; Connect
damage ;; Damaging || causing ;; Causation || inflicted ;; Causation
developed ;; Coming_to_be || strength ;; Cause_change_of_strength || reached ;; Arriving
began ;; Process_start || intensifying ;; Cause_change_of_strength || hurricane ;; Catastrophe || reaching ;; Arriving
weakened ;; Cause_change_of_strength || making ;; Manufacturing
weakened ;; Cause_change_of_strength || tracked ;; Self_motion || crossing ;; Motion_directional
tracked ;; Self_motion
avoid ;; Preventing_or_letting
posted ;; Sending
began ;; Process_start || took ;; Creating
declared ;; Expressing_publicly || opening ;; Openness
threat ;; Warning || stranded ;; Motion
damage ;; Damaging || inflicted ;; Causation
occurred ;; Coming_to_be
cut ;; Bodily_harm
flooding ;; Motion || causing ;; Causation || occurred ;; Coming_to_be
brought ;; Bringing
led to ;; Causation
held ;; Hold
became ;; Becoming || win ;; Getting
defending ;; Defending || tied ;; Connect
exceeded ;; Cause_change_of_position_on_a_scale || score ;; Getting || delayed ;; Change_event_time
recorded ;; Recording
Battle ;; Hostile_encounter || War ;; Hostile_encounter || took place ;; Process_start
battle ;; Hostile_encounter || occurred ;; Coming_to_be
Battle ;; Hostile_encounter || commanded ;; Control || withdrew ;; Escaping
gathering ;; Come_together || seized ;; Conquering || overstretched ;; Change || force ;; Military_operation || victory ;; Getting || strength ;; Cause_change_of_strength
garrisoned ;; Hostile_encounter || remaining ;; Temporary_stay || sending ;; Sending
force ;; Military_operation || left ;; GiveUp || garrisoned ;; Control
besieged ;; Besieging || crossed ;; Motion_directional || force ;; Military_operation
assaulted ;; Attack
became ;; Becoming || Battle ;; Hostile_encounter || defeated ;; Conquering || withdrew ;; Escaping
mounted ;; Connect || force ;; Military_operation || attack ;; Attack
consisting ;; Cause_to_be_included || detachment ;; Causation || held ;; Hold
consisted ;; Cause_to_be_included
supported ;; Supporting || mounted ;; Motion
crossed ;; Motion_directional || reaching ;; Arriving || left ;; Departing
commenced ;; Process_start || attacked ;; Attack || attack ;; Attack
withdrew ;; Escaping || retreat ;; Escaping
surrendered ;; Surrendering || fight ;; Hostile_encounter
losses ;; Earnings_and_losses || killed ;; Killing || wounded ;; Bodily_harm
losses ;; Earnings_and_losses || killed ;; Killing || included ;; Cause_to_be_included || war ;; Hostile_encounter || wounded ;; Bodily_harm
battle ;; Hostile_encounter || lost ;; Earnings_and_losses || defeat ;; Conquering
destroyed ;; Destroying || thwarting ;; Preventing_or_letting || advance ;; Motion || defeated ;; Conquering
gave ;; Giving || leave ;; Departing || locked ;; Hold
crashed ;; Catastrophe || take-off ;; Motion
killed ;; Killing
taking off ;; Departing || failed ;; Causation
claim ;; Statement || hitched ;; Connect
confirmed ;; Check || died ;; Death
supported ;; Supporting || Operation ;; Military_operation || plan ;; Arranging
due ;; Causation
begin ;; Process_start || advanced ;; Cause_to_make_progress || linked ;; Connect
conducted ;; Action
supported ;; Supporting || used ;; Using || captured ;; Conquering
cancelled ;; Change_event_time || begin ;; Process_start || meet ;; Come_together
raided ;; Attack
occupied ;; Attack
responding ;; Response || killed ;; Killing
killed ;; Killing
called ;; Name_conferral || described ;; Statement || murder ;; Killing
changed ;; Change || murder ;; Killing
linked ;; Connect || feud ;; Quarreling || admitted ;; Statement
sentenced ;; Legal_rulings
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || took place ;; Process_start
abandoned ;; GiveUp || withdrawing ;; Escaping || besieging ;; Besieging || situated ;; Placing || command ;; Control
supported ;; Supporting || arrived ;; Arriving || attacked ;; Attack || retreating ;; Escaping
ended ;; Process_end || withdrew ;; Escaping || battle ;; Hostile_encounter || left ;; Giving
battle ;; Hostile_encounter || war ;; Hostile_encounter || cooperation ;; Collaboration
begin ;; Process_start || storm ;; Catastrophe || damaged ;; Damaging || planned ;; Arranging || frustrated ;; Hindering || joint ;; Collaboration
battle ;; Hostile_encounter || consisted ;; Cause_to_be_included || participation ;; Participation || command ;; Control
decide ;; Deciding || played ;; Competition
representing ;; Presence || faced ;; Hostile_encounter
match ;; Competition || scoring ;; Getting
triumph ;; Earnings_and_losses || playing ;; Competition
defeated ;; Conquering
beat ;; Competition || defeated ;; Conquering
considered ;; Influence || left ;; GiveUp || led ;; Conquering || led ;; Conquering
became ;; Becoming || reach ;; Achieve
lost ;; Earnings_and_losses || triumphed ;; Earnings_and_losses || defeating ;; Conquering
beat ;; Competition
tournament ;; Competition || held ;; Hold
involving ;; Cause_to_be_included || event ;; Social_event
final ;; Process_end || event ;; Social_event || sweep ;; Earnings_and_losses
matches ;; Competition || shaping up ;; Becoming || contests ;; Competition
matches ;; Competition || saw ;; Self_motion || rain ;; Motion || caused ;; Causation || cyclone ;; Catastrophe || abandoned ;; GiveUp
matches ;; Competition || taking ;; Getting
final ;; Process_end || tournament ;; Competition || defeated ;; Conquering || win ;; Earnings_and_losses
lost ;; Earnings_and_losses
lost ;; Earnings_and_losses
tournament ;; Competition
retired ;; Departing || final ;; Competition
involved ;; Cause_to_be_included
retreated ;; Escaping
decided ;; Deciding || discussed ;; Communication
occupied ;; Control
abandoned ;; GiveUp
signaled ;; Communication
encounter ;; Hostile_encounter
captured ;; Conquering
consisted ;; Cause_to_be_included || siege ;; Besieging || retake ;; Conquering
assault ;; Attack
wounded ;; Bodily_harm
abandoned ;; GiveUp || remained ;; Presence
became ;; Becoming || fought ;; Hostile_encounter || command ;; Control
included ;; Cause_to_be_included || established ;; Creating
attacked ;; Attack || built ;; Building || destroyed ;; Destroying
attacked ;; Attack
ordered ;; Request || encountered ;; Hostile_encounter
abandon ;; GiveUp || attack ;; Attack || bribed ;; Committing_crime
wounded ;; Bodily_harm
attack ;; Attack || ordered ;; Request
ended ;; Process_end
named ;; Name_conferral || storm ;; Catastrophe
warned ;; Warning || started ;; Process_start || organize ;; Arranging || intensify ;; Expansion || formed ;; Creating || reaching ;; Achieve
disaster ;; Catastrophe || affect ;; Influence
caused ;; Causation || cyclone ;; Catastrophe || damage ;; Damaging
storm ;; Catastrophe || deaths ;; Death || left ;; GiveUp
confirmed ;; Check || become ;; Becoming || issued ;; Statement
designated ;; Deciding
upgraded ;; Cause_to_make_progress || intensify ;; Expansion || designated ;; Deciding
came ;; Causation || depression ;; Catastrophe
infected ;; Bodily_harm || dying ;; Death || reported ;; Reporting || intensified ;; Cause_change_of_strength
clashes ;; Violence || riots ;; Violence
violence ;; Violence || motivated ;; Causation || plagued ;; Causation
violence ;; Violence || violence ;; Violence || clashes ;; Violence || characterised ;; Judgment_communication
violence ;; Violence || started ;; Process_start || spread ;; Dispersal
fighting ;; Hostile_encounter
killed ;; Killing
died ;; Death || clashes ;; Violence
killed ;; Killing || dumped ;; GiveUp
rioting ;; Violence
violence ;; Violence || riots ;; Violence || died ;; Death || killed ;; Killing
fought ;; Hostile_encounter || Battle ;; Hostile_encounter || battle ;; Hostile_encounter
obtained ;; Getting || victory ;; Earnings_and_losses
abandoned ;; GiveUp
stoned ;; Bodily_harm || death ;; Death
defeated ;; Conquering || capture ;; Conquering
prevented ;; Preventing_or_letting || loss ;; Earnings_and_losses || operations ;; Military_operation
mounted ;; Military_operation || retake ;; Conquering
held ;; Hold
supported ;; Supporting || conflict ;; Violence || siege ;; Besieging
reverses ;; Escaping || left ;; Causation
improved ;; Cause_to_make_progress || compensated ;; Supply
brought ;; Bringing || dig ;; Motion
broke out ;; Process_start
refused ;; Agree_or_refuse_to_act || abandon ;; GiveUp || die ;; Death || becoming ;; Becoming
Battle ;; Hostile_encounter || took place ;; Process_start
battle ;; Hostile_encounter
forces ;; Causation || joined ;; Cause_to_amalgamate
allies ;; Collaboration
fighting ;; Hostile_encounter || called ;; Request
refused ;; Agree_or_refuse_to_act || commanded ;; Control || abandon ;; GiveUp
refused ;; Agree_or_refuse_to_act || asked ;; Request || mutinied ;; Change_of_leadership || fight ;; Defending || commanded ;; Control
Cyclone ;; Catastrophe || cyclone ;; Catastrophe || affected ;; Influence
cyclone ;; Catastrophe || cyclone ;; Catastrophe || cyclone ;; Catastrophe
developed ;; Coming_to_be
moved ;; Motion || storm ;; Catastrophe
moved ;; Motion || began ;; Process_start || strengthen ;; Cause_change_of_strength || lessened ;; Cause_change_of_strength || existing ;; Presence
beginning ;; Process_start || intensifying ;; Cause_change_of_strength || reached ;; Arriving
weakened ;; Cause_change_of_strength || caused ;; Causation || cyclone ;; Catastrophe || entered ;; Arriving || expand ;; Expansion
cyclone ;; Catastrophe || steered ;; Change || weakening ;; Cause_change_of_strength
cyclone ;; Catastrophe || entered ;; Arriving || entered ;; Arriving || degenerated ;; Change || dissipated ;; Dispersal
existence ;; Presence || affected ;; Influence
rainfall ;; Motion || brought ;; Bringing
brought ;; Creating
occurred ;; Coming_to_be || result ;; Causation
destroy ;; Destroying
cyclone ;; Catastrophe || affected ;; Influence
Cyclone ;; Catastrophe || affected ;; Influence
cyclone ;; Catastrophe || recorded ;; Recording || associated ;; Collaboration || due ;; Causation
cyclone ;; Catastrophe || name ;; Name_conferral || names ;; Name_conferral || retired ;; GiveUp
Invasion ;; Attack || occupy ;; Control
invaded ;; Attack || construct ;; Building || occupied ;; Control || establish ;; Creating
withdrew ;; Escaping || contest ;; Competition || landed ;; Self_motion
struck ;; Attack || landings ;; Self_motion
supporting ;; Supporting
damaged ;; Damaging || raid ;; Attack || sank ;; Motion
began ;; Process_start || occupied ;; Control
supported ;; Supporting
abandoned ;; GiveUp || supported ;; Supporting
took place ;; Process_start
abandoned ;; GiveUp
administered ;; Control
contested ;; Competition
qualified ;; Earnings_and_losses
played ;; Participation || winning ;; Getting
defeated ;; Conquering
occurred ;; Presence
took place ;; Process_start
killed ;; Killing || collided ;; Self_motion
accident ;; Catastrophe
caused ;; Causation || collision ;; Catastrophe
standing ;; Placing || forgot ;; GiveUp
left ;; Departing || protected ;; Preventing_or_letting || prevented ;; Preventing_or_letting || allowing ;; Preventing_or_letting
used ;; Using || believed ;; Know
allowed ;; Preventing_or_letting || collided ;; Self_motion
carrying ;; Bringing
occurred ;; Presence
tournament ;; Competition || took place ;; Hold
formed ;; Coming_to_be
tournament ;; Competition
tournament ;; Competition
lost ;; Earnings_and_losses
abandoned ;; GiveUp || reach ;; Achieve || replay ;; Competition
affect ;; Influence || cancellation ;; Change_event_time
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || battle ;; Hostile_encounter || defeated ;; Hostile_encounter || War ;; Military_operation
present ;; Presence || battle ;; Hostile_encounter
battle ;; Hostile_encounter || involved ;; Cause_to_be_included || initiated ;; Process_start
attacks ;; Attack || resulted in ;; Causation || rout ;; Earnings_and_losses
wounds ;; Bodily_harm || firing ;; Use_firearm || received ;; Receiving
creating ;; Creating || Confusion ;; Confronting_problem || seized ;; Control
ordered ;; Request || counter-attack ;; Attack || command ;; Control || assumed ;; Receiving
prevailed ;; Conquering || sent ;; Sending
wounded ;; Bodily_harm || losses ;; Death || dead ;; Death
battle ;; Hostile_encounter || positioned ;; Placing || block ;; Hindering || casualties ;; Killing || suffered ;; Bodily_harm
wounded ;; Bodily_harm || suffered ;; Bodily_harm || dead ;; Death || captured ;; Arrest
retreating ;; Escaping || victory ;; Hostile_encounter || liberate ;; Conquering || advance ;; Motion
abandon ;; GiveUp || damaged ;; Damaging || forced ;; Causation || loss ;; Earnings_and_losses
took place ;; Process_start || advance ;; Motion
describes ;; Communication
ended ;; Process_end || retreated ;; Escaping || occupied ;; Control
resulted in ;; Causation
reached ;; Arriving || conduct ;; Action
operations ;; Military_operation || offensive ;; Military_operation || carried out ;; Action
achieved ;; Achieve || concluded ;; Coming_to_believe
abandoned ;; GiveUp || create ;; Creating
established ;; Creating || became ;; Becoming
dispersed ;; Dispersal || capture ;; Conquering
used ;; Using
govern ;; Control || issued ;; Creating
built ;; Building || restored ;; Recovering
began ;; Process_start
Cyclone ;; Catastrophe || cyclone ;; Catastrophe || record ;; Recording
noted ;; Know || developed ;; Coming_to_be
moved ;; Motion || crossed ;; Motion_directional
became ;; Becoming || declared ;; Expressing_publicly || named ;; Name_conferral || cyclone ;; Catastrophe || organized ;; Arranging
move ;; Motion || intensified ;; Cause_change_of_strength
became ;; Becoming || cyclone ;; Catastrophe || cyclone ;; Catastrophe || hurricane ;; Catastrophe
peaked ;; Cause_change_of_position_on_a_scale || affected ;; Influence
becoming ;; Becoming || cyclone ;; Catastrophe || result ;; Causation || performed ;; Hold
provided ;; Supply || strengthening ;; Cause_change_of_strength || response ;; Response || move ;; Motion || started ;; Process_start
weakened ;; Cause_change_of_strength || degenerated ;; Change
noted ;; Know
Cyclone ;; Catastrophe || affected ;; Influence
rainfall ;; Self_motion || winds ;; Self_motion || disastrous ;; Catastrophe
destroyed ;; Destroying
cyclone ;; Catastrophe || due ;; Causation || eroded ;; Destroying
destroyed ;; Destroying || experienced ;; Confronting_problem || flooding ;; Catastrophe || effects ;; Influence
usage ;; Using || retired ;; GiveUp
formed ;; Creating || resulted in ;; Causation
called ;; Name_conferral
began ;; Process_start
election ;; Choosing
began ;; Process_start
led to ;; Causation
dissolved ;; Dispersal
became ;; Becoming
abandoned ;; GiveUp
dissolved ;; Dispersal
impact ;; Influence || felt ;; Change_sentiment
abandoned ;; GiveUp
varied ;; Change
turned ;; Becoming
suffered ;; Bodily_harm || took place ;; Process_start
began ;; Process_start
changed ;; Change
competition ;; Competition || Championship ;; Competition
competition ;; Competition || defunct ;; GiveUp || championships ;; Competition || replaced ;; Change
organised ;; Arranging || event ;; Competition
played ;; Competition || tournament ;; Competition
defending ;; Defending || tournament ;; Competition
defeating ;; Conquering || final ;; Competition
finishing ;; Process_end || participate ;; Participation || event ;; Competition || Championship ;; Competition
replaced ;; Change
play-off ;; Competition || losing ;; Earnings_and_losses
replaced ;; Change
remained ;; Presence
made ;; Manufacturing
include ;; Cause_to_be_included || fought ;; Hostile_encounter
abandoned ;; GiveUp || found ;; Know || decided ;; Deciding || ignoring ;; Response || entered ;; Arriving
decided ;; Deciding || operated ;; Being_in_operation
ordered ;; Request || made ;; Creating
sent ;; Sending || fought ;; Hostile_encounter
crossed ;; Motion_directional || withdraw ;; Escaping || protected ;; Defending || forcing ;; Control || occupied ;; Control
placed ;; Placing
remained ;; Presence || make ;; Creating || observation ;; Perception_active
fought ;; Hostile_encounter || routed ;; Bodily_harm || returned ;; Arriving
created ;; Creating || committed ;; Commitment || reinforcing ;; Cause_change_of_strength
conflict ;; Hostile_encounter || lasted ;; Motion_directional
concluded ;; Process_end || subjugate ;; Conquering
joined ;; Participation
make ;; Manufacturing
abandon ;; GiveUp || forced ;; Control || make ;; Manufacturing
hurricane ;; Catastrophe || struck ;; Attack
storm ;; Catastrophe || moved ;; Self_motion || moving ;; Self_motion
warnings ;; Warning || landfall ;; Catastrophe
storm ;; Motion
storm ;; Catastrophe || struck ;; Attack || cyclone ;; Catastrophe || flooding ;; Catastrophe || landslides ;; Catastrophe
struck ;; Attack || landslides ;; Catastrophe
storm ;; Catastrophe || Damages ;; Damaging || perished ;; Death
broke ;; Destroying
moved ;; Motion || developed ;; Coming_to_be
turned ;; Becoming || attained ;; Getting
weakening ;; Cause_change_of_strength || dissipated ;; Removing
caused ;; Causation || threatened ;; Warning
resulted in ;; Causation || flooding ;; Catastrophe || dropped ;; Motion_directional
damage ;; Damaging
flood ;; Catastrophe || affected ;; Influence
damage ;; Damaging || received ;; Receiving
began ;; Process_start || reached ;; Cause_change_of_position_on_a_scale || use ;; Using || kept ;; Temporary_stay
warnings ;; Warning || flood ;; Catastrophe || inundated ;; Destroying || injured ;; Bodily_harm
declared ;; Expressing_publicly || flood ;; Catastrophe || affected ;; Influence || making ;; Causation || aid ;; Assistance || flooded ;; Destroying
flood ;; Catastrophe || effects ;; Influence || effect ;; Influence
destroyed ;; Destroying || known ;; Know
formed ;; Coming_to_be
moved ;; Motion
intensified ;; Cause_change_of_strength || named ;; Name_conferral
developed ;; Coming_to_be || strengthened ;; Cause_change_of_strength
began ;; Process_start || passed ;; Motion_directional
reached ;; Achieve
weakened ;; Cause_change_of_strength || became ;; Becoming
affected ;; Influence || damaged ;; Damaging || destroyed ;; Destroying
damaged ;; Damaging || flooded ;; Motion
Damage ;; Damaging
wrecked ;; Destroying || contaminated ;; Destroying
reached ;; Achieve || reached ;; Achieve
totaled ;; Earnings_and_losses
prompted ;; Causation || declare ;; Expressing_publicly
became ;; Becoming || becoming ;; Becoming || threaten ;; Warning
dropped ;; Motion
Cyclone ;; Catastrophe || cyclone ;; Catastrophe || cyclone ;; Catastrophe
Cyclone ;; Catastrophe || form ;; Coming_to_be || cyclone ;; Catastrophe || cyclone ;; Catastrophe
storm ;; Catastrophe || name ;; Name_conferral || retired ;; Removing
upgraded ;; Cause_to_make_progress
moved ;; Motion || cyclone ;; Catastrophe || joint ;; Collaboration || warning ;; Warning || upgraded ;; Cause_change_of_position_on_a_scale
cyclone ;; Catastrophe || cyclone ;; Catastrophe || Cyclone ;; Catastrophe || reached ;; Arriving || made ;; Causation
cyclone ;; Catastrophe || reached ;; Arriving || made it ;; Causation
damage ;; Damaging || causing ;; Causation || Cyclone ;; Catastrophe || brushed ;; Motion
weakened ;; Cause_change_of_strength || dissipated ;; Removing
known ;; Know
formed ;; Coming_to_be || progressed ;; Cause_change_of_position_on_a_scale
intensified ;; Cause_change_of_strength || moving ;; Motion
became ;; Becoming || entered ;; Arriving
allowed ;; Preventing_or_letting || intensify ;; Cause_change_of_strength
turned ;; Becoming
weakened ;; Cause_change_of_strength || curved ;; Motion_directional || landfall ;; Self_motion
landfall ;; Self_motion
emerged ;; Coming_to_be || becoming ;; Becoming || dissipating ;; Dispersal
moved ;; Motion || causing ;; Causation
departed ;; Departing || warning ;; Warning || capsized ;; Catastrophe
died ;; Death || reached ;; Arriving
causing ;; Causation || damaging ;; Damaging || caught ;; Hold || sink ;; Removing
assisted ;; Assistance || found ;; Know || killed ;; Killing
wrecked ;; Catastrophe || damaged ;; Damaging
moved ;; Motion
totaled ;; Earnings_and_losses
killed ;; Killing || reached ;; Achieve || sweeping ;; Motion
killed ;; Killing || extended ;; Expansion
War ;; Hostile_encounter || included ;; Cause_to_be_included || offensive ;; Hostile_encounter || reduce ;; Cause_change_of_position_on_a_scale
gain ;; Getting || control ;; Control
situated ;; Placing
crossed ;; Motion_directional
restricted ;; Hindering || threat ;; Warning
made ;; Manufacturing || named ;; Name_conferral || develop ;; Coming_to_be
formed ;; Coming_to_be || issued ;; Creating
warnings ;; Warning || entered ;; Arriving || prompting ;; Causation
crossing ;; Motion_directional || became ;; Becoming || storms ;; Catastrophe
dissipated ;; Dispersal
making it ;; Causation || destructive ;; Destroying || damage ;; Damaging
damaged ;; Damaging || leaving ;; Departing
storm ;; Catastrophe
struck ;; Attack
killing ;; Killing
earthquake ;; Catastrophe || shaking ;; Motion || occurred ;; Coming_to_be
felt ;; Perception_active
Survey ;; Scrutiny || experiencing ;; Perception_active
experienced ;; Perception_active
felt ;; Perception_active || earthquake ;; Catastrophe
felt ;; Perception_active
felt ;; Perception_active
triggered ;; Causation || devastated ;; Destroying || damaged ;; Damaging
warnings ;; Warning || damage ;; Damaging || damage ;; Damaging || issued ;; Statement
affected ;; Influence || earthquake ;; Catastrophe || generated ;; Causation
declared ;; Expressing_publicly || sent ;; Sending || control ;; Control || catastrophe ;; Catastrophe
lost ;; Death || lost ;; Earnings_and_losses
estimated ;; Statement || cost ;; Earnings_and_losses
made ;; Creating
losses ;; Earnings_and_losses
warning ;; Warning || cyclone ;; Catastrophe || affected ;; Influence
classified ;; Check
organizing ;; Arranging || tracked ;; Motion || intensifying ;; Cause_change_of_strength
attaining ;; Achieve || strengthened ;; Cause_change_of_strength || turned ;; Becoming
moving ;; Motion || attained ;; Achieve
weakened ;; Cause_change_of_strength || turned ;; Becoming || passed ;; Motion_directional || accelerating ;; Cause_to_make_progress
becoming ;; Becoming || passed ;; Motion_directional || dissipated ;; Dispersal
affect ;; Influence || produced ;; Manufacturing
killed ;; Killing || warning ;; Warning || Due ;; Causation || injured ;; Bodily_harm
damaged ;; Damaging
passed ;; Motion_directional || forcing ;; Causation || shutting down ;; Having_or_lacking_access
killed ;; Killing || rainfall ;; Motion || affected ;; Influence || produced ;; Manufacturing
damage ;; Damaging
storm ;; Catastrophe
formed ;; Coming_to_be || strengthened ;; Cause_change_of_strength
reported ;; Reporting || reached ;; Arriving
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe
reached ;; Arriving
traveled ;; Traveling
weakened ;; Cause_change_of_strength
crossed ;; Motion_directional || warnings ;; Warning || discontinued ;; Process_end || track ;; Recording
caused ;; Causation
disrupted ;; Hindering
destroyed ;; Destroying || leaving ;; Causation
destroyed ;; Destroying || contaminated ;; Destroying
reported ;; Reporting || injuries ;; Bodily_harm
declared ;; Expressing_publicly || allowing ;; Preventing_or_letting || storm ;; Catastrophe || assist ;; Assistance
requested ;; Request
hurricanes ;; Catastrophe || threaten ;; Influence
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe
intensifying ;; Cause_change_of_strength || hurricane ;; Catastrophe || moved ;; Motion
prompted ;; Causation || evacuations ;; Escaping || warnings ;; Warning || hurricane ;; Catastrophe || hit ;; Arriving
caused ;; Causation || damage ;; Damaging || occurred ;; Presence
maintained ;; Preserving || track ;; Self_motion || turned ;; Becoming || impact ;; Influence
moved ;; Motion
death ;; Death || due ;; Causation || left ;; Departing
weakened ;; Cause_change_of_strength || moving ;; Motion || dissipated ;; Dispersal
earthquake ;; Catastrophe || occurred ;; Coming_to_be
magnitude ;; Cause_change_of_position_on_a_scale || shock ;; Motion
earthquakes ;; Catastrophe
felt ;; Perception_active
Damage ;; Damaging || reported ;; Reporting
warning ;; Warning || issued ;; Creating || Warning ;; Warning
dead ;; Death || hit ;; Bodily_harm || earthquake ;; Catastrophe
warning ;; Warning || joint ;; Collaboration || affect ;; Influence
originated ;; Coming_to_be || remained ;; Presence
matured ;; Cause_to_make_progress || attained ;; Getting
attained ;; Getting
passed ;; Motion_directional || damage ;; Damaging
began ;; Process_start
observed ;; Perception_active
losses ;; Earnings_and_losses || left ;; Departing || affected ;; Influence || killed ;; Killing || wrought ;; Causation
destroyed ;; Destroying
losses ;; Earnings_and_losses || lost ;; Earnings_and_losses || due ;; Causation || affected ;; Influence
requested ;; Request || aid ;; Assistance || Following ;; Self_motion
damage ;; Damaging || replaced ;; Change_of_leadership
strike ;; Attack || storms ;; Catastrophe
Hurricane ;; Catastrophe || hurricane ;; Catastrophe || form ;; Coming_to_be
developed ;; Coming_to_be || named ;; Name_conferral || storm ;; Catastrophe || hurricane ;; Catastrophe
drifted ;; Motion || intensified ;; Cause_change_of_strength || becoming ;; Becoming || disturbance ;; Attack || hurricane ;; Catastrophe || strength ;; Cause_change_of_strength
intensified ;; Cause_change_of_strength || storm ;; Catastrophe || hurricane ;; Catastrophe
dropping ;; Motion || dissipating ;; Dispersal || remained ;; Presence || turning ;; Motion
issued ;; Creating || warnings ;; Warning || hastening ;; Self_motion || evacuations ;; Escaping
damage ;; Damaging || flooding ;; Motion || ruptured ;; Destroying
became ;; Becoming || rising ;; Cause_change_of_position_on_a_scale || flooding ;; Motion || flee ;; Escaping
hurricane ;; Catastrophe || due ;; Causation
observed ;; Perception_active || suffering ;; Bodily_harm || damage ;; Damaging
recorded ;; Recording || damage ;; Damaging || deaths ;; Death
attack ;; Attack || led ;; Conquering || Battle ;; Hostile_encounter || commanded ;; Control
pushed ;; Motion
withdraw ;; Removing
occurred ;; Presence
threatened ;; Warning
attacked ;; Attack || assumed ;; Choosing
took ;; Getting
sent ;; Sending || attack ;; Attack
attacked ;; Attack || assault ;; Attack || launched ;; Military_operation
decide ;; Deciding
conducted ;; Action || offensive ;; Attack
gained ;; Getting || supply ;; Supply || designed ;; Creating
facilitated ;; Cause_to_make_progress || command ;; Control
captured ;; Conquering || capture ;; Conquering || placed ;; Placing
achieved ;; Achieve || withdrawal ;; Escaping || move ;; Motion || reach ;; Arriving || negotiated ;; Communication
aimed ;; Aiming || assault ;; Attack || threatened ;; Warning
placed ;; Placing || control ;; Control
caused ;; Causation || killing ;; Killing || storm ;; Catastrophe
developed ;; Coming_to_be
intensified ;; Cause_change_of_strength || attaining ;; Getting
joint ;; Collaboration || warning ;; Warning
weakened ;; Cause_change_of_strength || landfall ;; Self_motion
dissipated ;; Dispersal
triggered ;; Causation || flooding ;; Motion
Widespread ;; Dispersal || took place ;; Process_start
sent ;; Sending || conducted ;; Action
Operation ;; Military_operation
involved ;; Participation
stop ;; Process_end || reversed ;; Change
requested ;; Request || defeat ;; Conquering || threatened ;; Warning
removing ;; Removing || allowing ;; Preventing_or_letting || launched ;; Process_start || come into effect ;; Change || resume ;; Process_start
ended ;; Process_end || resume ;; Process_start
commenced ;; Process_start || targeted ;; Aiming
sparked ;; Causation
assisted ;; Assistance || hampered ;; Hindering
affect ;; Influence
originated ;; Coming_to_be || developed ;; Cause_to_make_progress
intensifying ;; Cause_change_of_strength || storm ;; Catastrophe || hurricane ;; Catastrophe || upgraded ;; Cause_change_of_position_on_a_scale || attained ;; Getting
began ;; Process_start || encountered ;; Hostile_encounter || weaken ;; Cause_change_of_strength
Moving ;; Motion || accelerated ;; Cause_to_make_progress || downgraded ;; Cause_change_of_position_on_a_scale
began ;; Process_start || becoming ;; Becoming || storm ;; Catastrophe || re-intensify ;; Cause_change_of_strength
began ;; Process_start || approach ;; Arriving || continued ;; Motion
passing ;; Motion_directional || reached ;; Arriving
began ;; Process_start || threatened ;; Warning || weaken ;; Cause_change_of_strength
passing ;; Motion_directional || downgraded ;; Cause_change_of_position_on_a_scale
dissipated ;; Dispersal
moved ;; Motion || noted ;; Know || passing ;; Motion_directional
devastated ;; Destroying || opened ;; Openness || issued ;; Creating || used ;; Using || warnings ;; Warning
affecting ;; Influence || reported ;; Reporting
stung ;; Bodily_harm
damage ;; Damaging || damage ;; Damaging || led to ;; Causation || lead to ;; Causation
damage ;; Damaging || flooding ;; Motion
died ;; Death || presumed ;; Coming_to_believe
injured ;; Bodily_harm
affected ;; Influence
damage ;; Damaging
hurricane ;; Catastrophe || hurricane ;; Catastrophe || landfall ;; Arriving
developed ;; Coming_to_be || storm ;; Catastrophe
strengthening ;; Cause_change_of_strength || storm ;; Catastrophe || Moving ;; Self_motion || approaching ;; Self_motion
crossed ;; Self_motion || intensified ;; Cause_change_of_strength
storm ;; Catastrophe || made ;; Manufacturing || landfall ;; Arriving
storm ;; Catastrophe || reaching ;; Arriving || intensifying ;; Cause_change_of_strength || occurred ;; Arriving
struck ;; Bodily_harm
weakened ;; Cause_change_of_strength || became ;; Becoming || storm ;; Catastrophe
winds ;; Motion || reported ;; Reporting || damage ;; Damaging
uprooted ;; Removing
inundated ;; Destroying || storm ;; Catastrophe
wrecked ;; Destroying || damage ;; Damaging || sunk ;; Motion_directional
warnings ;; Warning || estimated ;; Statement || damage ;; Damaging || evaded ;; Escaping
suffered ;; Damaging
winds ;; Motion || damage ;; Damaging
reported ;; Reporting || destroyed ;; Destroying
inundated ;; Destroying
suffered ;; Damaging
storm ;; Catastrophe || deaths ;; Killing || damage ;; Damaging || caused ;; Causation
took place ;; Process_start || War ;; Military_operation || Raid ;; Hostile_encounter || engagement ;; Hostile_encounter
descended on ;; Motion_directional
taken captive ;; Conquering || killed ;; Killing || attack ;; Attack
escaped ;; Escaping || killed ;; Killing || skirmish ;; Hostile_encounter || gave ;; Self_motion || chase ;; Self_motion
planned ;; Arranging || engage ;; Participation || raids ;; Hostile_encounter
reduce ;; Cause_change_of_position_on_a_scale || choose ;; Choosing || participate ;; Participation || expedition ;; Attack || forced ;; Causation
raids ;; Attack || warned ;; Warning || raid ;; Hostile_encounter
threatened ;; Warning
originated ;; Coming_to_be || moved ;; Motion
upgraded ;; Cause_to_make_progress || Moving ;; Motion
upgraded ;; Cause_to_make_progress
formed ;; Coming_to_be || occur ;; Coming_to_be || attain ;; Getting
began ;; Process_start || becoming ;; Becoming
re-assessed ;; Judgment_communication
reached ;; Arriving
commenced ;; Process_start || began ;; Process_start || turning ;; Change
weakened ;; Cause_change_of_strength
merged ;; Cause_to_amalgamate
noted ;; Know || approached ;; Arriving || surf ;; Self_motion || coming ;; Motion
surf ;; Self_motion || turned ;; Change || rise ;; Cause_change_of_position_on_a_scale
threatened ;; Warning
named ;; Name_conferral || storm ;; Catastrophe || forming ;; Coming_to_be || developed ;; Coming_to_be
organized ;; Arranging || strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || gained ;; Getting
occurred ;; Coming_to_be || hurricane ;; Catastrophe || reaching ;; Arriving
storm ;; Catastrophe || remaining ;; Temporary_stay || headed ;; Self_motion
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || peaked ;; Arriving
hurricane ;; Catastrophe || becoming ;; Becoming || weakened ;; Cause_change_of_strength
headed ;; Self_motion
beginning ;; Process_start || storm ;; Catastrophe || weakened ;; Cause_change_of_strength
became ;; Becoming || loop ;; Motion_directional
produced ;; Manufacturing || remained ;; Temporary_stay || storm ;; Catastrophe || effects ;; Influence
caused ;; Causation || damage ;; Damaging
occurred ;; Coming_to_be || flooding ;; Motion
occurred ;; Coming_to_be || drowned ;; Killing || sailing ;; Self_motion || falling ;; Motion_directional
cyclone ;; Catastrophe
formed ;; Coming_to_be
became ;; Becoming || classified ;; Check
drifted ;; Motion || storm ;; Catastrophe || intensified ;; Cause_change_of_strength || Situated ;; Placing
named ;; Name_conferral || attained ;; Achieve
improved ;; Cause_to_make_progress || culminated ;; Process_end || intensification ;; Cause_change_of_position_on_a_scale
cyclone ;; Catastrophe || estimated ;; Statement
considered ;; Deciding
weakening ;; Cause_change_of_strength || storm ;; Catastrophe || ensued ;; Coming_to_be || made ;; Creating
noted ;; Know || dissipation ;; Dispersal || took place ;; Process_start
damage ;; Damaging || warnings ;; Warning || evacuations ;; Escaping || minimized ;; Cause_change_of_position_on_a_scale
damage ;; Damaging || resulted ;; Causation || spawned ;; Causation
damaged ;; Damaging
killed ;; Killing
storm ;; Catastrophe || damage ;; Damaging || claimed ;; Causation
set up ;; Building || aid ;; Assistance || distributed ;; Giving
donated ;; Giving
forces ;; Causation || battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || fought ;; Hostile_encounter || War ;; Military_operation
ended ;; Process_end || battle ;; Hostile_encounter || complete ;; Process_end || rout ;; Destroying
battle ;; Hostile_encounter || estimates ;; Cause_change_of_position_on_a_scale || ranging ;; Aiming || varied ;; Change
estimated ;; Cause_change_of_position_on_a_scale
disputed ;; Quarreling || killed ;; Killing || cites ;; Judgment_communication || losses ;; Earnings_and_losses || casualties ;; Catastrophe || returning ;; Arriving
fought ;; Hostile_encounter || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter
forces ;; Causation || battle ;; Hostile_encounter || said ;; Statement
battle ;; Hostile_encounter || involved ;; Cause_to_be_included
disputed ;; Quarreling
support ;; Supporting || exceeded ;; Cause_change_of_position_on_a_scale || come ;; Arriving || accepted ;; Agree_or_refuse_to_act || involved ;; Cause_to_be_included
took place ;; Process_start || dispute ;; Quarreling || dead ;; Death || Tragedy ;; Catastrophe || leaving ;; Causation
inspired ;; Influence || incident ;; Catastrophe
includes ;; Cause_to_be_included || conflict ;; Hostile_encounter
disagreement ;; Quarreling || incident ;; Catastrophe || incident ;; Catastrophe
glorified ;; Judgment_communication || used ;; Using
registered ;; Recording || site ;; Placing
marker ;; Recording
Battle ;; Hostile_encounter || organized ;; Arranging || uprising ;; Violence || uprisings ;; Violence || War ;; Military_operation
Wars ;; Hostile_encounter || disputes ;; Quarreling || conflict ;; Violence || occurred ;; Coming_to_be
killed ;; Killing || arrested ;; Arrest
declines ;; Cause_change_of_position_on_a_scale || improvements ;; Cause_to_make_progress || led to ;; Causation
confronted ;; Hostile_encounter || unionize ;; Becoming_a_member || called ;; Name_conferral || tensions ;; Hostile_encounter || rose ;; Cause_change_of_position_on_a_scale
ended ;; Process_end || fired ;; Use_firearm || battle ;; Hostile_encounter || order ;; Arranging || intervened ;; Preventing_or_letting
conflicts ;; Violence
carved ;; Removing
development ;; Cause_to_make_progress || emergence ;; Coming_to_be || characterized ;; Judgment_communication || growing ;; Cause_change_of_position_on_a_scale
resistance ;; Defending || Wars ;; Hostile_encounter || threatened ;; Influence || conflict ;; Violence || presented ;; Giving
emerged ;; Coming_to_be || began ;; Process_start || arriving in ;; Arriving
opened ;; Openness || growth ;; Cause_change_of_position_on_a_scale || proved ;; Coming_to_believe || enmity ;; Quarreling
warfare ;; Hostile_encounter || resulting ;; Causation
tension ;; Violence || conflicts ;; Violence
created ;; Creating || culminated ;; Process_end
described ;; Statement
match ;; Competition || Shield ;; Competition || played ;; Competition
held ;; Hold
contested ;; Competition || match ;; Competition || champions ;; Competition || beat ;; Competition || final ;; Process_end
Watched ;; Perception_active || match ;; Competition
appearance ;; Coming_to_be
game ;; Competition || clubs ;; Becoming_a_member || met ;; Come_together
Tournament ;; Competition || match ;; Competition || clubs ;; Becoming_a_member || faced ;; Hostile_encounter
lost ;; Earnings_and_losses || given ;; Giving || game ;; Competition || beating ;; Earnings_and_losses || argued ;; Quarreling
competitive ;; Competition || started ;; Process_start || debut ;; Process_start
starting ;; Process_start || replaced ;; Removing
found ;; Placing || score ;; Getting || floated ;; Motion || match ;; Competition || came ;; Causation
spoke ;; Statement || performance ;; Achieve || opponents ;; Hostile_encounter
War ;; Hostile_encounter || War ;; Hostile_encounter || war ;; Hostile_encounter || fought ;; Hostile_encounter
war ;; Hostile_encounter
devastated ;; Destroying || disputed ;; Quarreling || losses ;; Earnings_and_losses || known ;; Know || dispute ;; Quarreling || suffered ;; Bodily_harm || catastrophic ;; Catastrophe || forced ;; Causation
began ;; Process_start || caused ;; Causation || war ;; Hostile_encounter || War ;; Hostile_encounter || conflict ;; Hostile_encounter
became ;; Becoming || war ;; Hostile_encounter || War ;; Hostile_encounter
ended ;; Process_end || war ;; Hostile_encounter || defeat ;; Conquering
warfare ;; Hostile_encounter || conducted ;; Action || destruction ;; Destroying || casualties ;; Catastrophe || lost ;; Military_operation || battle ;; Hostile_encounter || military ;; Military_operation
killed ;; Killing || Battle ;; Hostile_encounter || action ;; Response || forces ;; Military_operation
occupied ;; Control
losses ;; Earnings_and_losses
losses ;; Earnings_and_losses || recover ;; Recovering || took ;; Conquering
organized ;; Arranging || celebrate ;; Social_event || Games ;; Competition || tournament ;; Competition
participate ;; Participation || compromised ;; Hindering || tournament ;; Competition
playing ;; Competition || playing ;; Competition || tournament ;; Competition
took place ;; Process_start
disputed ;; Quarreling || tournament ;; Competition
eliminated ;; Removing
eliminated ;; Removing
final ;; Process_end || defeated ;; Conquering
organized ;; Arranging || starting ;; Process_start || seen ;; Perception_active || tournament ;; Competition
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || coalition ;; Collaboration || forces ;; Causation
battle ;; Hostile_encounter || Due ;; Causation || debating ;; Quarreling
battle ;; Hostile_encounter || fought ;; Hostile_encounter
combined ;; Cause_to_amalgamate || combined ;; Cause_to_amalgamate || commanded ;; Control
ended ;; Process_end || battle ;; Hostile_encounter || resulted in ;; Causation || rebellion ;; Violence || abdication ;; Change_of_leadership
battle ;; Hostile_encounter || Battle ;; Hostile_encounter
operation ;; Military_operation || occupied ;; Attack
directed ;; Aiming
named ;; Name_conferral || associated ;; Collaboration
participated ;; Participation
commenced ;; Process_start || received ;; Receiving || disagreement ;; Quarreling || conflict ;; Hostile_encounter || attack ;; Attack || repulsed ;; Defending || opposing ;; Agree_or_refuse_to_act
counter-attacked ;; Attack || driven ;; Motion
took place ;; Process_start
battle ;; Hostile_encounter
sent ;; Sending || defeat ;; Conquering || suffered ;; Bodily_harm
avoided ;; Preventing_or_letting || ended ;; Process_end || escape ;; Escaping || joined ;; Becoming_a_member || retreat ;; Escaping
attack ;; Attack || repulsed ;; Defending || blocked ;; Hindering
brought ;; Causation || quarrel ;; Quarreling
Battle ;; Hostile_encounter || called ;; Request || Battle ;; Hostile_encounter || War ;; Military_operation || took place ;; Process_start
captured ;; Conquering || siege ;; Besieging
recapture ;; Conquering
battle ;; Hostile_encounter || due ;; Causation || interrupted ;; Hindering
battle ;; Hostile_encounter || recapture ;; Conquering || battle ;; Hostile_encounter
dispute ;; Quarreling || derive ;; Creating
battle ;; Hostile_encounter || offensive ;; Hostile_encounter || War ;; Hostile_encounter || took place ;; Process_start
attack ;; Attack || capture ;; Conquering
fought ;; Hostile_encounter || called ;; Name_conferral || battle ;; Hostile_encounter
attacks ;; Attack || called ;; Name_conferral || Battle ;; Hostile_encounter || preceded ;; Process_start
began ;; Process_start || casualties ;; Catastrophe || attacked ;; Attack || offensive ;; Hostile_encounter || seize ;; Conquering
conducted ;; Action || capture ;; Conquering || advance ;; Cause_to_make_progress || secure ;; Getting || launched ;; Process_start
ended ;; Process_end || broken ;; Bodily_harm || defences ;; Defending || penetrated ;; Attack || advanced ;; Cause_to_make_progress || stopped ;; Process_end
began ;; Process_start || caused ;; Causation || attack ;; Attack || forced ;; Causation || failed ;; Earnings_and_losses || collapse ;; Destroying
sought ;; Motion
landed ;; Motion
defeated ;; Conquering
fought ;; Quarreling
ended ;; Process_end || fall ;; Motion_directional
started ;; Process_start || conflict ;; Quarreling || involved ;; Participation || clashes ;; Hostile_encounter
involved ;; Participation || happened ;; Presence
charged ;; Judgment_communication || clashes ;; Hostile_encounter
accused ;; Judgment_communication || resigned ;; Change_of_leadership || attacks ;; Judgment_communication
called ;; Request
announced ;; Expressing_publicly || group ;; Participation
announced ;; Expressing_publicly || possibility ;; Causation
stopped ;; Process_end || announced ;; Expressing_publicly || violence ;; Violence || ceasefire ;; Process_end
event ;; Competition || produced ;; Creating
took place ;; Process_start
vote ;; Choosing
match ;; Competition
match ;; Competition
started ;; Process_start || ended ;; Process_end || voting ;; Choosing
feuds ;; Quarreling
match ;; Competition || feud ;; Quarreling || led to ;; Causation
challenged ;; Competition || match ;; Competition || losing ;; Earnings_and_losses || interfered ;; Hindering
became ;; Becoming || became ;; Becoming || match ;; Competition
match ;; Competition || faced ;; Hostile_encounter
ended ;; Process_end || match ;; Competition || feud ;; Quarreling
event ;; Competition
hurricane ;; Catastrophe || cyclone ;; Catastrophe || Cyclone ;; Catastrophe || struck ;; Destroying
strengthened ;; Cause_change_of_position_on_a_scale || becoming ;; Becoming || formed ;; Coming_to_be || hurricane ;; Catastrophe || moved ;; Motion
crossing ;; Motion_directional || Hurricane ;; Catastrophe || corresponding ;; Presence || peaked ;; Cause_change_of_position_on_a_scale
moved ;; Motion || hurricane ;; Catastrophe || loop ;; Motion_directional || hit ;; Attack
tracked ;; Motion || landing ;; Motion
moving ;; Motion || hugged ;; Motion
developed ;; Coming_to_be || Due ;; Causation || storms ;; Catastrophe || loop ;; Motion_directional || suggested ;; Statement || hit ;; Attack
identified ;; Check || storm ;; Catastrophe || subject ;; Causation || debate ;; Quarreling
gave ;; Giving || Analysis ;; Scrutiny
hurricane ;; Catastrophe
left ;; Departing || Damage ;; Damaging
flooding ;; Catastrophe || destruction ;; Destroying || impact ;; Influence
losses ;; Earnings_and_losses || storm ;; Catastrophe || damage ;; Damaging || exceed ;; Cause_change_of_position_on_a_scale
deaths ;; Death || occurred ;; Presence
decided ;; Deciding || organized ;; Arranging
contested ;; Quarreling
hosted ;; Social_event || hosted ;; Social_event
match ;; Competition || hosted ;; Social_event
winning ;; Getting || beat ;; Competition
held ;; Hold
run ;; Action
recognized ;; Know || playing ;; Competition || made up ;; Come_together
hosted ;; Social_event
sponsored ;; Supply || saw ;; Motion || gain ;; Getting
play ;; Competition || disputed ;; Quarreling || Hosted ;; Social_event
applying ;; Using || enter ;; Arriving || overcome ;; Conquering
weak ;; Cause_change_of_strength || attendance ;; Participation || reported ;; Reporting
revolution ;; Change_of_leadership || battle ;; Hostile_encounter || allied ;; Collaboration || control ;; Control
faced ;; Confronting_problem
allied ;; Collaboration
fought ;; Hostile_encounter || battle ;; Hostile_encounter
fought ;; Hostile_encounter
revolution ;; Change_of_leadership || command ;; Control || determining ;; Deciding
chose ;; Choosing || arrived in ;; Arriving || defensive ;; Defending || open ;; Openness || knowing ;; Know
existing ;; Presence || supply ;; Supply
defense ;; Defending || utilize ;; Using
use ;; Using || massed ;; Come_together
seen ;; Traveling || use ;; Using
met ;; Come_together || called ;; Name_conferral || battle ;; Hostile_encounter
revolution ;; Change_of_leadership || revolution ;; Change_of_leadership || helped ;; Assistance || battles ;; Hostile_encounter || determine ;; Deciding
battles ;; Hostile_encounter || bring ;; Bringing || warring ;; Hostile_encounter || defeat ;; Conquering
lost ;; Earnings_and_losses || ceased ;; Process_end || contend ;; Quarreling
began ;; Process_start || begun ;; Process_start || began ;; Process_start || referred ;; Statement
captured ;; Conquering || beginning ;; Process_start
fled ;; Escaping || took ;; Conquering || control ;; Control || disputed ;; Quarreling
called ;; Name_conferral
control ;; Control || lost ;; Earnings_and_losses
called ;; Name_conferral
allow ;; Preventing_or_letting || declare ;; Expressing_publicly || opposed ;; Preventing_or_letting
siege ;; Besieging || battle ;; Hostile_encounter
beginning ;; Process_start
battle ;; Hostile_encounter
siege ;; Besieging
surrender ;; Surrendering
fought ;; Quarreling || forces ;; Hostile_encounter
helping ;; Assistance || defeated ;; Conquering
contributed ;; Giving || elimination ;; Removing || proposed ;; Statement || led to ;; Causation
organized ;; Arranging || Championship ;; Competition || Cup ;; Competition || Championship ;; Competition || championship ;; Competition
held ;; Hold
defending ;; Defending || formed ;; Coming_to_be || tournament ;; Competition || tournament ;; Competition || become ;; Becoming
postponed ;; Change_event_time || tournament ;; Competition || scheduled ;; Arranging || take place ;; Hold || led to ;; Causation
tournament ;; Competition || tournament ;; Competition || participation ;; Participation || dispute ;; Quarreling || withdrew ;; Departing
decided ;; Deciding || chosen ;; Choosing || host ;; Social_event
tournament ;; Competition || matches ;; Competition || announced ;; Expressing_publicly || held ;; Hold
tournament ;; Competition || Cup ;; Competition || hosted ;; Social_event
triggered ;; Causation
supported ;; Supporting || occurred ;; Presence
argued ;; Quarreling
began ;; Process_start
began ;; Process_start || ended ;; Process_end
suspended ;; Process_end
happened ;; Presence
Championship ;; Competition || championship ;; Competition
hosted ;; Social_event
selected ;; Choosing
competition ;; Competition || begin ;; Process_start || scheduled ;; Arranging || match ;; Competition || final ;; Process_end || taking place ;; Hold
competition ;; Competition || postponed ;; Change_event_time || released ;; Publishing || showed ;; Presence
competition ;; Competition || pulled out ;; GiveUp
bye ;; Departing || allowing ;; Statement || get ;; Getting || changed ;; Change
qualified ;; Participation
competition ;; Competition
competition ;; Competition || began ;; Process_start
Competition ;; Competition || determined ;; Deciding || delay ;; Change_event_time || reported ;; Reporting || remain ;; Temporary_stay
Final ;; Competition || played ;; Competition
match ;; Competition || defeated ;; Conquering
reduced ;; Cause_change_of_position_on_a_scale || scored ;; Getting || target ;; Aiming
concert ;; Social_event || support ;; Supporting || debut ;; Process_start || tour ;; Traveling
began ;; Process_start || followed ;; Self_motion
postponed ;; Change_event_time || followed ;; Self_motion || shows ;; Social_event
traveling ;; Traveling || incorporating ;; Cause_to_amalgamate || described ;; Statement
planned ;; Arranging || show ;; Social_event
ended ;; Process_end || followed ;; Self_motion || consisted ;; Cause_to_be_included
debut ;; Process_start || consisted ;; Cause_to_be_included
appeared ;; Presence
performed ;; Hold || changes ;; Change
received ;; Receiving || complimenting ;; Judgment_communication
held ;; Hold
citing ;; Judgment_communication || hosted ;; Social_event || hosted ;; Social_event || hosted ;; Social_event
pulled out ;; GiveUp || cancelled ;; Change_event_time
host ;; Social_event
host ;; Social_event
closed ;; Process_end || opened ;; Process_start
started ;; Process_start
organized ;; Arranging || reacted ;; Response || host ;; Social_event
help ;; Assistance || created ;; Creating
turned out ;; Participation
include ;; Cause_to_be_included
received ;; Receiving
canceled ;; Change_event_time || declining ;; Cause_change_of_position_on_a_scale
asserted ;; Statement || scrutiny ;; Scrutiny
known ;; Know || struck ;; Attack || Typhoon ;; Catastrophe
formed ;; Coming_to_be
moved ;; Motion
intensify ;; Cause_change_of_strength || allowed ;; Preventing_or_letting
reaching ;; Arriving || strengthened ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength || landfall ;; Self_motion
re-intensified ;; Cause_change_of_strength || dissipating ;; Dispersal || making ;; Creating
causing ;; Causation || flooding ;; Motion
destroyed ;; Destroying
disrupted ;; Hindering
destroyed ;; Destroying || damaged ;; Damaging
deaths ;; Death
killed ;; Killing || knocking ;; Motion
fell ;; Motion_directional || wrecked ;; Damaging
canceled ;; Change_event_time
increased ;; Cause_change_of_position_on_a_scale
killed ;; Killing || reached ;; Achieve
Tour ;; Traveling || support ;; Supporting || tour ;; Traveling
played ;; Self_motion || play ;; Self_motion || tour ;; Traveling
declared ;; Expressing_publicly || passing ;; Process_end
cancelled ;; Change_event_time || refunded ;; Commerce_pay
Championship ;; Competition || held ;; Hold
Championship ;; Competition
postponed ;; Change_event_time || tournament ;; Competition || withdrawal ;; Escaping || scheduled ;; Arranging
tournament ;; Competition || arranged ;; Arranging || withdrew ;; Escaping || resulting in ;; Causation || championship ;; Competition
tournament ;; Competition || tournament ;; Competition || Cup ;; Competition
given ;; Giving
called ;; Name_conferral
takes place ;; Process_start
associated ;; Collaboration
took place ;; Process_start
cancelled ;; Change_event_time || held ;; Hold
took place ;; Process_start
record ;; Recording || finish ;; Process_end
record ;; Recording || broke ;; Destroying
competitions ;; Competition || forcing ;; Causation || move ;; Change || shortening ;; Cause_change_of_position_on_a_scale || winds ;; Motion
earning ;; Earnings_and_losses || win ;; Earnings_and_losses
defended ;; Defending || become ;; Becoming || shortened ;; Change_event_time
held ;; Hold || completed ;; Process_end || race ;; Competition
tour ;; Traveling
support ;; Supporting
took place ;; Process_start
changed ;; Change || scheduled ;; Arranging
opening ;; Openness
appear ;; Presence || appear ;; Coming_to_be
stop ;; Temporary_stay || cancelled ;; Change_event_time
Games ;; Competition || Games ;; Competition || event ;; Competition || took place ;; Process_start
participated ;; Participation || games ;; Competition || events ;; Competition
games ;; Competition || held ;; Hold
Games ;; Competition || hosted ;; Social_event
Games ;; Competition || hosted ;; Social_event
hosting ;; Come_together || due ;; Causation || delays ;; Change_event_time
became ;; Becoming || Games ;; Competition || host ;; Social_event
games ;; Competition || opened ;; Process_start || closed ;; Process_end
led ;; Getting
games ;; Competition || Games ;; Competition || records ;; Recording || broken ;; Destroying
rising ;; Cause_change_of_position_on_a_scale || games ;; Competition || competition ;; Competition
Marathon ;; Competition
marathon ;; Competition
running ;; Self_motion || events ;; Competition
organized ;; Arranging || race ;; Competition || run ;; Action || cancelled ;; Change_event_time
sponsored ;; Supply
began ;; Process_start
attracts ;; Influence || race ;; Competition
chosen ;; Choosing || participation ;; Participation
gained ;; Getting || Guaranteed ;; Commitment || support ;; Supporting || donate ;; Supply || completed ;; Process_end
gain ;; Getting || joining ;; Becoming_a_member || raise ;; Cause_change_of_position_on_a_scale
competition ;; Competition || Paralympics ;; Competition || skiing ;; Motion || held ;; Hold
held ;; Defending || events ;; Competition || due ;; Causation
postponed ;; Change_event_time || Events ;; Competition || scheduled ;; Arranging || due ;; Causation
becoming ;; Becoming || sitting ;; Placing
became ;; Becoming || win ;; Getting || Paralympics ;; Competition || winning ;; Getting || Impaired ;; Influence
Paralympics ;; Competition
sets ;; Placing || Games ;; Competition || record ;; Recording
affected ;; Influence
began ;; Process_start
affected ;; Influence
affected ;; Influence
caused ;; Causation || bringing ;; Bringing
affected ;; Influence
standing ;; Placing || collapsed ;; Destroying || collapsed ;; Destroying || led to ;; Causation
occurred ;; Presence || trapped ;; Bodily_harm
flooded ;; Catastrophe
running ;; Motion || plant ;; Building || affected ;; Influence
damaged ;; Damaging || cancelled ;; Change_event_time
moved ;; Motion || disrupted ;; Hindering
going to ;; Motion || described ;; Statement
developed ;; Coming_to_be
organized ;; Arranging || designated ;; Choosing
upgraded ;; Cause_to_make_progress
hampered ;; Hindering || strengthened ;; Cause_change_of_strength
caused ;; Causation
regained ;; Recovering || attained ;; Getting
led to ;; Causation
degenerated ;; Change
declared ;; Expressing_publicly
prompted ;; Causation
opened ;; Openness || closed ;; Process_end
caused ;; Causation
combined ;; Cause_to_amalgamate
downed ;; Change
suspended ;; Change_event_time
caused ;; Causation
invasion ;; Attack || invasion ;; Attack
invasion ;; Attack || directed ;; Aiming
went off ;; Process_end
attack ;; Attack || opened ;; Openness
gave ;; Giving || delayed ;; Change_event_time || evacuate ;; Escaping
retreat ;; Escaping || managed ;; Control || halt ;; Process_end
catch ;; Motion
determined ;; Deciding
sent ;; Sending || delayed ;; Change_event_time
defeated ;; Conquering
bring ;; Bringing
escaped ;; Escaping || forced ;; Request
gave ;; Giving
tournament ;; Competition || held ;; Hold
matches ;; Competition || held ;; Hold
awarded ;; Giving || played ;; Competition
matches ;; Competition || matches ;; Competition || meeting ;; Social_event
touring ;; Traveling || lost ;; Earnings_and_losses || win ;; Earnings_and_losses
playing ;; Participation
postponed ;; Change_event_time || touring ;; Traveling || completing ;; Process_end || lost ;; Earnings_and_losses || played ;; Participation
concert ;; Social_event || tour ;; Traveling
planning ;; Arranging
put together ;; Come_together || promote ;; Cause_change_of_position_on_a_scale
cancel ;; Change_event_time || traveling ;; Traveling || happen ;; Presence || success ;; Achieve
concerts ;; Social_event
used ;; Using
Battle ;; Hostile_encounter || invading ;; Attack || took place ;; Process_start || led ;; Causation
commanded ;; Control || consisted ;; Cause_to_be_included
ruled ;; Control || recognizing ;; Know
ruled ;; Control || battle ;; Hostile_encounter || fought ;; Hostile_encounter
battle ;; Hostile_encounter
killed ;; Killing || battle ;; Hostile_encounter || wiped ;; Removing
suffered ;; Bodily_harm || delayed ;; Change_event_time || casualties ;; Death || managed ;; Hold
left ;; Departing || defend ;; Defending
became ;; Becoming
ran ;; Process_start
opened ;; Process_start
canceled ;; Change_event_time
canceled ;; Change_event_time
organized ;; Arranging || festivals ;; Social_event
festival ;; Social_event || event ;; Social_event || established ;; Creating || billed ;; Arranging
festival ;; Social_event || held ;; Hold || hosted ;; Come_together
held ;; Defending
festival ;; Social_event || changed ;; Change || event ;; Social_event
held ;; Hold || held ;; Hold
festival ;; Social_event || held ;; Hold || held ;; Hold
included ;; Cause_to_be_included
canceled ;; Change_event_time
event ;; Competition || celebrated ;; Social_event
received ;; Receiving || Games ;; Competition
participated ;; Participation || representing ;; Change_sentiment
Games ;; Competition
scheduled ;; Arranging || delays ;; Change_event_time || postponed ;; Change_event_time || Asiad ;; Competition || preparations ;; Cause_to_make_progress || held ;; Hold
announced ;; Expressing_publicly || established ;; Coming_to_be
games ;; Competition || managed ;; Hold
invited ;; Request
Asiad ;; Competition
finished ;; Process_end
organised ;; Arranging
competition ;; Competition || contested ;; Competition
organized ;; Arranging || played ;; Competition
delayed ;; Change_event_time || tournament ;; Competition || scheduled ;; Arranging || event ;; Competition || played ;; Participation || cancelled ;; Change_event_time
played ;; Competition
played ;; Competition || moved ;; Change
play ;; Competition || accepted ;; Receiving
played ;; Competition
competitions ;; Competition || given ;; Giving || participate ;; Participation || cup ;; Competition
competition ;; Competition || played ;; Competition
competitions ;; Competition || contested ;; Competition
Battle ;; Hostile_encounter || War ;; Military_operation || took place ;; Process_start
opening ;; Openness || clash ;; Hostile_encounter || Offensive ;; Attack
forces ;; Causation || forces ;; Causation
retreat ;; Escaping || retreat ;; Escaping || forces ;; Causation || organized ;; Arranging || forced ;; Causation || delayed ;; Change_event_time
wounded ;; Bodily_harm
battle ;; Hostile_encounter || involving ;; Cause_to_be_included || War ;; Military_operation
inspired ;; Influence
consented ;; Commitment
taking ;; Conquering
enlisted ;; Becoming_a_member
postponed ;; Change_event_time || discovered ;; Know || planned ;; Arranging || planning ;; Arranging
invasion ;; Attack || Operation ;; Military_operation || campaign ;; Military_operation || War ;; Hostile_encounter || took ;; Conquering
began ;; Process_start || initiated ;; Process_start || operation ;; Military_operation || campaign ;; Military_operation || Campaign ;; Military_operation
began ;; Process_start || ended ;; Process_end
achieved ;; Achieve || drove ;; Motion || opened ;; Openness || set out ;; Military_operation || forces ;; Motion
opened ;; Openness || Allied ;; Cause_to_amalgamate || invasion ;; Attack || toppled ;; Change_of_leadership
canceled ;; Change_event_time || resulting in ;; Causation || offensive ;; Attack || reduction ;; Cause_change_of_position_on_a_scale || divert ;; Motion
end ;; Process_end || war ;; Hostile_encounter || collapse ;; Earnings_and_losses || lesser ;; Cause_change_of_position_on_a_scale || resulting in ;; Causation || replacing ;; Change_of_leadership || diverted ;; Removing
tournament ;; Competition || played ;; Competition
Hosted ;; Social_event || came ;; Bringing
organised ;; Arranging || tournament ;; Competition || matches ;; Competition || played ;; Competition
received ;; Receiving || cancelled ;; Change_event_time || donation ;; Supply
defeating ;; Preventing_or_letting
participated ;; Participation || joining ;; Participation || record ;; Recording
making ;; Getting || debuts ;; Self_motion
tournament ;; Competition || tournament ;; Competition
forces ;; Military_operation || conquest ;; Conquering || conquest ;; Conquering || war ;; Hostile_encounter || Wars ;; Hostile_encounter
invaded ;; Attack
control ;; Control || came ;; Arriving
allied ;; Collaboration || defeated ;; Earnings_and_losses || War ;; Hostile_encounter
defeated ;; Earnings_and_losses || Wars ;; Hostile_encounter || War ;; Hostile_encounter || end ;; Process_end || occupied ;; Control
warfare ;; Hostile_encounter
passed ;; Process_end || confiscated ;; Removing
reconquest ;; Conquering
debated ;; Communication || command ;; Control || campaign ;; Military_operation
war ;; Hostile_encounter || claims ;; Statement || exaggerated ;; Judgment_communication || distorted ;; Hold || argue ;; Statement || challenged ;; Confronting_problem
war ;; Hostile_encounter || loss ;; Earnings_and_losses
resulted in ;; Causation || war ;; Hostile_encounter || famine ;; Catastrophe || plague ;; Catastrophe || worsened ;; Influence
resulting ;; Causation || campaign ;; Military_operation
transported ;; Bringing
losses ;; Earnings_and_losses
parade ;; Social_event
organized ;; Arranging || march ;; Social_event
organized ;; Arranging
marched ;; Self_motion
present ;; Presence || stated ;; Statement || march ;; Self_motion || excluded ;; Preventing_or_letting || purpose ;; Aiming
obtaining ;; Getting || kicked off ;; Motion
parade ;; Self_motion || consisted ;; Cause_to_be_included
building ;; Building
impeding ;; Hindering
present ;; Presence || heckling ;; Judgment_communication || subjected ;; Confronting_problem
rally ;; Hold
arrested ;; Arrest || violence ;; Violence || formed ;; Coming_to_be || existed ;; Presence
turned ;; Becoming || violence ;; Violence || change ;; Change
allowed ;; Preventing_or_letting || heard ;; Perception_active
violence ;; Violence || experienced ;; Confronting_problem || resulted in ;; Causation
influenced ;; Influence
violence ;; Violence || leading to ;; Causation
known ;; Know
attacked ;; Attack || prevent ;; Preventing_or_letting || agreement ;; Agree_or_refuse_to_act || held ;; Hold || elections ;; Choosing
capturing ;; Conquering
engaged ;; Participation || faced ;; Confronting_problem
attack ;; Attack || halted ;; Process_end
war ;; Hostile_encounter || expanded ;; Expansion
left ;; Departing || called ;; Name_conferral || led to ;; Causation
led to ;; Causation
Crash ;; Catastrophe || accident ;; Catastrophe || occurred ;; Coming_to_be
causing ;; Causation || death ;; Death
failed ;; Confronting_problem || takeoff ;; Motion
initiating ;; Process_start || return ;; Arriving || descended ;; Motion || ditch ;; Self_motion
killed ;; Killing
event ;; Social_event || takes place ;; Process_start || Week ;; Social_event
allows ;; Preventing_or_letting
established ;; Creating
faced ;; Confronting_problem || increasing ;; Cause_change_of_position_on_a_scale || becomes ;; Becoming || setbacks ;; Hindering
engage ;; Participation || experiencing ;; Perception_active || participating ;; Participation || week ;; Social_event || programming ;; Social_event || allow ;; Preventing_or_letting || joining ;; Participation
contributes ;; Cause_to_make_progress || offer ;; Supply || evolve ;; Coming_to_be
fought ;; Hostile_encounter
sent ;; Sending || take ;; Conquering || prevent ;; Preventing_or_letting || driven ;; Motion
campaign ;; Military_operation || expand ;; Expansion || pursued ;; Coming_to_be
invasion ;; Attack || launched ;; Process_start
become ;; Becoming || war ;; Military_operation || faced ;; Confronting_problem || resistance ;; Defending || disaster ;; Catastrophe
defeat ;; Earnings_and_losses
commanded ;; Control
captured ;; Conquering || surrendered ;; Surrendering || siege ;; Besieging
occurred ;; Presence
moved ;; Motion || relieve ;; Assistance
defended ;; Defending || defended ;; Defending
stiffened ;; Hindering
assisted ;; Assistance || prevent ;; Preventing_or_letting || tightening ;; Cause_change_of_strength
capitulated ;; Surrendering || taken ;; Conquering || taken ;; Conquering
getting ;; Getting || succor ;; Assistance || faced ;; Confronting_problem
Battle ;; Hostile_encounter
caused ;; Causation || Hurricane ;; Catastrophe || damage ;; Damaging
named ;; Name_conferral || developed ;; Coming_to_be || hurricane ;; Catastrophe || storm ;; Catastrophe
emerged ;; Coming_to_be || moved ;; Motion
began ;; Process_start || upgraded ;; Cause_to_make_progress || storm ;; Catastrophe || organize ;; Arranging
intensify ;; Cause_change_of_strength || becoming ;; Becoming || hurricane ;; Catastrophe || hurricane ;; Catastrophe || storm ;; Catastrophe || storm ;; Catastrophe || peaked ;; Cause_change_of_position_on_a_scale || continued ;; Process_start || reclassified ;; Name_conferral
caused ;; Causation || storm ;; Catastrophe || approached ;; Arriving || downgraded ;; Cause_change_of_position_on_a_scale
storm ;; Catastrophe
moving ;; Motion || weakened ;; Cause_change_of_strength || deteriorated ;; Cause_change_of_strength
merged ;; Cause_to_amalgamate || centered ;; Placing
caused ;; Causation || becoming ;; Becoming || killing ;; Killing || flooding ;; Motion
crossing ;; Motion_directional || storm ;; Catastrophe || experiencing ;; Confronting_problem || dropped ;; Motion_directional
reaching ;; Arriving || reported ;; Reporting
brought ;; Bringing || storm ;; Catastrophe || due ;; Causation
damage ;; Damaging || left ;; Departing
damage ;; Damaging || flooding ;; Motion || occurred ;; Coming_to_be
caused ;; Causation || damage ;; Damaging
affects ;; Influence || flooding ;; Motion || reported ;; Reporting || occurred ;; Coming_to_be
caused ;; Causation || damage ;; Damaging
spanned ;; Motion
seize ;; Hold || cut ;; Removing || form ;; Coming_to_be
defended ;; Defending
hoped ;; Change_sentiment
provided ;; Supply
called ;; Request
reach ;; Arriving || capture ;; Hold
encountered ;; Confronting_problem || proved ;; Coming_to_believe || forced ;; Request
casualties ;; Death
Strength ;; Cause_change_of_strength
began ;; Process_start || began ;; Process_start
push ;; Hold
created ;; Creating
encountered ;; Hostile_encounter
encountered ;; Hostile_encounter
prevented ;; Preventing_or_letting
reached ;; Arriving || ordered ;; Arranging
floods ;; Catastrophe || occurred ;; Presence
affected ;; Influence || floods ;; Catastrophe
flooding ;; Catastrophe || submerged ;; Destroying || experienced ;; Confronting_problem
died ;; Death
found ;; Know || went ;; Motion
pronounced ;; Statement || dead ;; Death
battle ;; Hostile_encounter || occurring ;; Coming_to_be || fought ;; Hostile_encounter || fighting ;; Hostile_encounter || war ;; Military_operation
battles ;; Hostile_encounter || battles ;; Hostile_encounter || Campaign ;; Military_operation
killed ;; Killing || fortified ;; Cause_change_of_strength || assault ;; Attack || wounded ;; Bodily_harm
attacks ;; Attack || arrived ;; Arriving || holding ;; Defending || swung ;; Self_motion || seized ;; Conquering
received ;; Receiving || suffered ;; Catastrophe || Campaign ;; Military_operation || reinforcements ;; Cause_change_of_strength
arrived ;; Arriving || assaulted ;; Attack
built ;; Building || arrived ;; Arriving
attacked ;; Attack || repulsed ;; Defending
assault ;; Attack || assaults ;; Attack
made ;; Manufacturing || battle ;; Hostile_encounter || assault ;; Attack || said ;; Statement
gained ;; Getting || loss ;; Earnings_and_losses
confronted ;; Confronting_problem || advanced ;; Cause_to_make_progress || marching ;; Motion_directional
war ;; Military_operation
fleeing ;; Escaping || besieged ;; Besieging
storm ;; Catastrophe || hit ;; Attack
chosen ;; Choosing || named ;; Name_conferral || used ;; Using || storm ;; Catastrophe
strength ;; Cause_change_of_strength || hurricane ;; Catastrophe
caused ;; Causation || storm ;; Catastrophe || damage ;; Damaging || suffered ;; Confronting_problem || blown ;; Motion || damaged ;; Damaging
resulted in ;; Causation
lost ;; Earnings_and_losses
disasters ;; Catastrophe || death ;; Death || killed ;; Killing || victims ;; Death || making it ;; Causation
Hurricane ;; Catastrophe || impacted ;; Influence
originated ;; Coming_to_be
became ;; Becoming || developed ;; Cause_change_of_position_on_a_scale || tracking ;; Self_motion
turned ;; Becoming || intensified ;; Cause_change_of_strength
made ;; Creating
began ;; Process_start || move ;; Motion || re-entered ;; Arriving
helped ;; Assistance || passed ;; Motion_directional
transitioned ;; Change || neared ;; Presence
damage ;; Damaging
killed ;; Killing || caused ;; Causation || colliding ;; Self_motion
experienced ;; Confronting_problem || evacuated ;; Escaping
occurred ;; Coming_to_be || damage ;; Damaging
caused ;; Causation || brought ;; Bringing || damage ;; Damaging
occurred ;; Coming_to_be
present ;; Presence
becoming ;; Becoming || sunk ;; Motion || produced ;; Coming_to_be
Damage ;; Damaging
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || Battle ;; Hostile_encounter || fought ;; Hostile_encounter || Campaign ;; Military_operation || War ;; Hostile_encounter
war ;; Hostile_encounter || battles ;; Hostile_encounter || casualties ;; Death
withdrawal ;; Escaping || battle ;; Hostile_encounter || attacks ;; Attack || confederate ;; Collaboration || battle ;; Hostile_encounter || confederate ;; Collaboration || control ;; Control || confederate ;; Collaboration || much-needed ;; Assistance || repulse ;; Defending || boost ;; Cause_to_make_progress || dashed ;; Attack || defeat ;; Conquering
marched ;; Self_motion || challenge ;; Confronting_problem
planned ;; Arranging || attack ;; Attack || struck ;; Attack
assault ;; Attack || commanded ;; Control
defense ;; Defending
defensive ;; Defending || prevented ;; Preventing_or_letting || collapse ;; Destroying || assumed ;; Choosing
attacks ;; Attack || repulsed ;; Defending
arriving ;; Arriving || attacks ;; Attack || assault ;; Attack
ordered ;; Arranging || well-fortified ;; Cause_change_of_strength || Fighting ;; Hostile_encounter || assault ;; Attack
losses ;; Earnings_and_losses || repulsed ;; Defending || Faced ;; Confronting_problem
chose ;; Choosing || withdraw ;; Escaping || receiving ;; Receiving || reinforcements ;; Cause_change_of_strength
caused ;; Causation || lose ;; Earnings_and_losses
conflict ;; Hostile_encounter
described ;; Statement || conflict ;; Hostile_encounter
fighting ;; Hostile_encounter
conflict ;; Hostile_encounter
faced ;; Confronting_problem
fled ;; Escaping || conflict ;; Hostile_encounter || confined ;; Placing
hurricane ;; Catastrophe || killed ;; Killing || Hurricane ;; Catastrophe || hurricane ;; Catastrophe
developed ;; Coming_to_be || strengthened ;; Cause_change_of_strength || moved ;; Motion
hurricane ;; Catastrophe || attained ;; Achieve
passing ;; Motion_directional || made ;; Manufacturing || landfall ;; Arriving || continued ;; Self_motion
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
entered ;; Arriving || extended ;; Expansion
produced ;; Causation
hurricane ;; Catastrophe || killing ;; Killing || unprepared ;; Confronting_problem || move ;; Motion || wrecked ;; Destroying
caused ;; Causation || deaths ;; Death || damage ;; Damaging || resulted in ;; Causation
hurricane ;; Catastrophe || destroyed ;; Destroying || leaving ;; Causation
storm ;; Catastrophe || extended ;; Expansion || causing ;; Causation || damage ;; Damaging
develop ;; Coming_to_be || hurricane ;; Catastrophe || hurricane ;; Catastrophe
developed ;; Coming_to_be || struck ;; Attack || Originating ;; Coming_to_be
became ;; Becoming || entering ;; Arriving || encountered ;; Confronting_problem
hurricane ;; Catastrophe || approached ;; Arriving || hurricane ;; Catastrophe || Hurricane ;; Catastrophe || attained ;; Getting || intensified ;; Cause_change_of_strength
hurricane ;; Catastrophe || came ;; Self_motion
caused ;; Causation || produced ;; Creating
causing ;; Causation || hurricane ;; Catastrophe || produced ;; Creating
killed ;; Killing
killed ;; Killing
caused ;; Causation || reported ;; Reporting
triggered ;; Causation
described ;; Statement || faced ;; Confronting_problem
declared ;; Expressing_publicly
ruined ;; Destroying || affected ;; Influence
departed ;; Departing
started ;; Process_start || takeoff ;; Motion
fire ;; Use_firearm || landing ;; Motion || unable ;; Confronting_problem || intention ;; Aiming
weakened ;; Cause_change_of_strength || broke ;; Destroying || crashed ;; Catastrophe || spread ;; Dispersal
killed ;; Killing || occupants ;; Causation
accident ;; Catastrophe || remains ;; Presence
cyclone ;; Catastrophe || affect ;; Influence
identified ;; Check
organized ;; Arranging
brushed ;; Motion
took place ;; Process_start || moved ;; Motion || occurred ;; Presence
moving ;; Motion || redeveloped ;; Recovering
made ;; Manufacturing || reached ;; Arriving
ensued ;; Coming_to_be || storm ;; Catastrophe || doubling ;; Cause_change_of_position_on_a_scale || looped ;; Motion
moving ;; Motion || noted ;; Statement
existence ;; Presence || caused ;; Causation || damage ;; Damaging
experienced ;; Confronting_problem || measured up ;; Coming_to_be
storm ;; Catastrophe
engagement ;; Hostile_encounter || fought ;; Hostile_encounter || Wars ;; Hostile_encounter
intercepted ;; Preventing_or_letting
participating ;; Participation || carrying ;; Bringing
sent ;; Sending || reached ;; Arriving || supplement ;; Supply || rebellion ;; Change_of_leadership || defeated ;; Preventing_or_letting
Battle ;; Hostile_encounter || defeated ;; Preventing_or_letting || suffered ;; Confronting_problem || defeated ;; Preventing_or_letting
discovered ;; Know || separated ;; Forming_relationships
surrender ;; Surrendering || force ;; Control || outrun ;; Arrest
fire ;; Use_firearm || opened ;; Openness || reply ;; Response || overhauled ;; Reforming_a_system
surrendered ;; Surrendering
taken ;; Bringing || repaired ;; Recovering
ended ;; Process_end || signified ;; Statement
fought ;; Hostile_encounter || War ;; Hostile_encounter
began ;; Process_start || defeat ;; Conquering || retreat ;; Escaping
attempted ;; Hold || block ;; Hindering || retreat ;; Escaping
arrived at ;; Arriving || defeated ;; Conquering
opening ;; Process_start || retreat ;; Escaping
face ;; Confronting_problem || invasion ;; Attack || battle ;; Hostile_encounter || allowing ;; Preventing_or_letting || retreat ;; Escaping || recover ;; Recovering
beginning ;; Process_start || Battle ;; Hostile_encounter
took place ;; Process_start || Battle ;; Hostile_encounter
battle ;; Hostile_encounter
crowned ;; Change_of_leadership || invasions ;; Attack || faced ;; Confronting_problem
gathered ;; Come_together || defeated ;; Conquering || defeated ;; Conquering || Battle ;; Hostile_encounter
left ;; Departing
recovering ;; Recovering || established ;; Creating || conquest ;; Conquering
gathering ;; Come_together
battle ;; Hostile_encounter
found ;; Know || reported ;; Reporting || battlefield ;; Hostile_encounter || appears ;; Change_sentiment || marched ;; Self_motion
battle ;; Hostile_encounter
turning on ;; Motion || break ;; Bodily_harm || adopted ;; Choosing
retreat ;; Escaping || battle ;; Hostile_encounter || led to ;; Causation || defeat ;; Conquering || death ;; Death
crowned ;; Change_of_leadership || marching ;; Motion
marked ;; Recording
estimate ;; Statement || died ;; Death || hard ;; Confronting_problem
founded ;; Creating || battle ;; Hostile_encounter || placed ;; Placing
fly ;; Motion || chartered ;; Renting
lost ;; Earnings_and_losses || climbing ;; Motion_directional || incapacitated ;; Destroying
flying ;; Motion || failed ;; Confronting_problem || turn ;; Change || climbing ;; Motion_directional
crashed ;; Catastrophe || ran out of ;; Expend_resource
formed ;; Coming_to_be || hurricane ;; Catastrophe
moved ;; Self_motion
reemerged ;; Cause_to_amalgamate || strengthened ;; Cause_change_of_strength
moved ;; Self_motion
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe
caused ;; Causation || downed ;; Motion
caused ;; Causation
intensify ;; Cause_change_of_strength || hurricane ;; Catastrophe || continued ;; Process_start
passing ;; Motion_directional || weakened ;; Cause_change_of_strength
brought ;; Bringing
suffering ;; Confronting_problem
caused ;; Causation || felled ;; Motion_directional
destroyed ;; Destroying
curved ;; Motion_directional || weakened ;; Cause_change_of_strength
transitioned ;; Change
killed ;; Killing
attacked ;; Attack || returning ;; Arriving
killed ;; Killing
accusing ;; Judgment_communication || attack ;; Attack || broke ;; Process_start || claimed ;; Statement
offered ;; Giving || condemned ;; Judgment_communication || refuted ;; Judgment_communication || stated ;; Statement || confirmed ;; Check || claims ;; Statement
strike ;; Attack
killings ;; Attack || faced ;; Confronting_problem || suspicion ;; Judgment_communication || underscored ;; Cause_change_of_position_on_a_scale
conquered ;; Conquering
invasion ;; Attack || war ;; Hostile_encounter || weakened ;; Cause_change_of_strength
besieged ;; Besieging || command ;; Control || dispatched ;; Sending
captured ;; Conquering || defeated ;; Conquering || faced ;; Confronting_problem || forcing ;; Causation || flee ;; Escaping
beheaded ;; Killing || turned ;; Becoming
invade ;; Attack || defeating ;; Conquering
broke out ;; Process_start || escalated ;; Cause_change_of_position_on_a_scale
negotiations ;; Communication || violence ;; Violence || help ;; Assistance || entered ;; Arriving || control ;; Control
reported ;; Reporting
casualties ;; Death
protect ;; Defending || stated ;; Statement
arrested ;; Arrest
beginning ;; Process_start || War ;; Hostile_encounter || war ;; Hostile_encounter
began ;; Process_start || became ;; Becoming || war ;; Hostile_encounter || independent ;; Creating
war ;; Hostile_encounter || Independence ;; Creating
used ;; Using || war ;; Hostile_encounter || War ;; Hostile_encounter
aim ;; Aiming || ending ;; Process_end
played ;; Participation || war ;; Hostile_encounter || War ;; Hostile_encounter || fought ;; Hostile_encounter
association ;; Collaboration || independence ;; Creating || fought ;; Hostile_encounter
fighting ;; Hostile_encounter || war ;; Hostile_encounter || divided ;; Causation
achieved ;; Achieve || died ;; Death || displaced ;; Destroying
devastated ;; Destroying || damaged ;; Damaging || war ;; Hostile_encounter
due ;; Causation || War ;; Hostile_encounter || military ;; Military_operation || involvement ;; Participation
provided ;; Supply || war ;; Hostile_encounter || War ;; Hostile_encounter || considered ;; Response
became ;; Becoming || War ;; Hostile_encounter || War ;; Hostile_encounter || conflict ;; Violence
massacre ;; Killing || massacre ;; Killing || carried out ;; Military_operation
occurred ;; Coming_to_be || leaving ;; Departing || celebrating ;; Social_event || massacre ;; Killing
massacre ;; Killing || resulted in ;; Causation || murder ;; Killing
confirmed ;; Check || attack ;; Attack || wounded ;; Bodily_harm || wounded ;; Bodily_harm
condemned ;; Judgment_communication || gained ;; Getting || widespread ;; Dispersal || massacre ;; Killing
violence ;; Violence || massacre ;; Killing || outbreaks ;; Process_start || murder ;; Killing
found ;; Know || bludgeoned ;; Attack
wounds ;; Bodily_harm
yielded ;; Creating
ended ;; Process_end || ended ;; Process_end
crime ;; Violence
conducted ;; Action || operation ;; Military_operation || state ;; Statement
began ;; Process_start || attacked ;; Attack || killing ;; Killing || operation ;; Military_operation || military ;; Military_operation || injuring ;; Bodily_harm
named ;; Name_conferral || dead ;; Death
airstrikes ;; Attack || involved ;; Cause_to_be_included || airstrikes ;; Attack
airstrikes ;; Attack || launched ;; Process_start || taking off ;; Departing
killed ;; Killing || confirmed ;; Check || alleged ;; Statement || airstrikes ;; Attack || killed ;; Killing || killed ;; Killing || reported ;; Reporting
airstrikes ;; Attack || stopped ;; Process_end
violence ;; Violence || airstrikes ;; Attack || increase ;; Cause_change_of_position_on_a_scale || resulted in ;; Causation
conducted ;; Action || airstrikes ;; Attack
airstrikes ;; Attack || shift ;; Change || military ;; Military_operation || referred ;; Name_conferral
began ;; Process_start || spread ;; Dispersal || overthrowing ;; Change_of_leadership || riots ;; Violence || unrest ;; Violence || demonstrations ;; Social_event
riots ;; Violence || scenes ;; Violence || War ;; Military_operation || violent ;; Violence || seen ;; Perception_active || end ;; Process_end
rioting ;; Violence || unrest ;; Violence || activism ;; Social_event || took place ;; Process_start || occur ;; Presence
riots ;; Violence || name ;; Name_conferral || events ;; Social_event || news ;; Statement || used ;; Using || describing ;; Statement || taken ;; Causation
said ;; Statement || talk ;; Communication
riots ;; Violence || followed ;; Motion || calmed ;; Statement || protesting ;; Self_motion || continued ;; Hold
due ;; Causation || decreasing ;; Cause_change_of_position_on_a_scale || faded ;; Change || participation ;; Participation
said ;; Statement || ran out of ;; Expend_resource
nicknamed ;; Name_conferral || war ;; Hostile_encounter || conflict ;; Hostile_encounter || took place ;; Process_start
invasion ;; Attack || replaced ;; Change_of_leadership || conflict ;; Hostile_encounter || culminated ;; Process_end
strife ;; Hostile_encounter
destabilized ;; Destroying || collapsed ;; Destroying
ill ;; Bodily_harm || control ;; Control || become ;; Becoming || keep ;; Preventing_or_letting
become ;; Becoming || support ;; Supporting || eroded ;; Destroying
invaded ;; Attack || escalated ;; Cause_change_of_strength || refuge ;; Escaping || defeat ;; Conquering
invasion ;; Attack || invasion ;; Attack || escalated ;; Cause_change_of_strength || alliance ;; Cause_to_amalgamate || joined ;; Participation || assembled ;; Building
put up ;; Military_operation || supported ;; Supporting || collapsed ;; Destroying
marked ;; Recording || violence ;; Violence || killed ;; Killing || fighting ;; Hostile_encounter
renamed ;; Name_conferral || brought ;; Bringing || change ;; Change || installed ;; Creating
expelled ;; Removing
cause ;; Causation || event ;; Social_event
prefer ;; Choosing || view ;; Perception_active
riots ;; Violence || redirects ;; Hold
riots ;; Violence || events ;; Social_event || see ;; Perception_active || riots ;; Violence
took place ;; Process_start || Battle ;; Hostile_encounter || riot ;; Violence
fighting ;; Hostile_encounter || organised ;; Arranging
erupted ;; Process_start || parade ;; Motion || rioting ;; Violence || end ;; Process_end || passing ;; Motion_directional
broke out ;; Process_start || rioting ;; Violence
Rioting ;; Violence
deployed ;; Arranging || enter ;; Arriving || restore ;; Recovering
violence ;; Violence || sparked ;; Causation || riot ;; Violence || conflict ;; Hostile_encounter || seen ;; Perception_active
insurgency ;; Military_operation
aims ;; Aiming || establish ;; Building
running ;; Self_motion || conflict ;; Violence || resulted in ;; Causation
widespread ;; Dispersal || accused ;; Judgment_communication || use ;; Using
abducted ;; Committing_crime || killed ;; Killing
revolution ;; Change_of_leadership || uprising ;; Change_of_leadership
played ;; Action
uprising ;; Change_of_leadership || provoked ;; Causation
defend ;; Defending || uprising ;; Change_of_leadership || reinstate ;; Recovering || repealed ;; Removing || carried out ;; Military_operation
warfare ;; Hostile_encounter || ending ;; Process_end || forces ;; Military_operation || battles ;; Hostile_encounter || defeat ;; Preventing_or_letting || broke out ;; Process_start || developed ;; Coming_to_be || revolt ;; Violence
identified ;; Check || uprisings ;; Change_of_leadership
uprising ;; Change_of_leadership || occurred ;; Coming_to_be || broke out ;; Process_start
War ;; Hostile_encounter || execution ;; Killing
massacred ;; Killing || drowned ;; Death
pushed ;; Motion || invasion ;; Attack || aid ;; Assistance || launched ;; Process_start || come to ;; Motion
surrender ;; Surrendering
resisted ;; Defending || fought ;; Hostile_encounter
surrendered ;; Surrendering || running out of ;; Expend_resource
killed ;; Killing || drowned ;; Killing || taking ;; Conquering || battle ;; Hostile_encounter || sunk ;; Motion || executed ;; Killing
massacres ;; Killing || committed ;; Commitment || war ;; Hostile_encounter || war ;; Hostile_encounter || atrocities ;; Violence
begun ;; Process_start || riots ;; Violence || conflicts ;; Violence
started ;; Process_start || came ;; Causation || rape ;; Committing_crime || murder ;; Killing || riots ;; Violence
started ;; Process_start || protest ;; Preventing_or_letting
killed ;; Killing || firing ;; Use_firearm || started ;; Process_start
declared ;; Expressing_publicly || participate ;; Participation || allowing ;; Preventing_or_letting
violence ;; Violence || displaced ;; Destroying
belonged ;; Presence || belonged ;; Presence || burned ;; Attack
accused ;; Judgment_communication || arrests ;; Arrest || violence ;; Violence || provided ;; Supply || playing ;; Action || targeting ;; Aiming || reported ;; Reporting
citing ;; Judgment_communication || response ;; Response || praised ;; Judgment_communication
resettle ;; Recovering || proposal ;; Agree_or_refuse_to_act || rejected ;; Agree_or_refuse_to_act
broke out ;; Process_start || resulting in ;; Causation || deaths ;; Death || burning ;; Destroying
allowed ;; Preventing_or_letting || leave ;; Departing || boycott ;; Response
took place ;; Process_start || blasts ;; Attack || service ;; Supply
killed ;; Killing || attacks ;; Attack || wounded ;; Bodily_harm
violence ;; Violence || killing ;; Killing
attack ;; Attack || attack ;; Attack
announced ;; Expressing_publicly || arrested ;; Arrest
statement ;; Statement || received ;; Receiving || planned ;; Arranging || came to ;; Motion || attack ;; Attack || support ;; Supporting
planning ;; Arranging || attacks ;; Attack || confessed ;; Reveal_secret
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || control ;; Control || fought ;; Hostile_encounter
began ;; Process_start || conflict ;; Violence || formed ;; Coming_to_be
seize ;; Hold || bring ;; Bringing || sought ;; Hold || generated ;; Creating
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Battles ;; Hostile_encounter || War ;; Hostile_encounter || distinguish ;; Check
battle ;; Hostile_encounter || due ;; Causation || found ;; Know
control ;; Control || left ;; Departing
War ;; Hostile_encounter
formed ;; Coming_to_be
violence ;; Violence
conflicts ;; Hostile_encounter || damaged ;; Damaging
violence ;; Violence || wounded ;; Bodily_harm || deaths ;; Death || resulted in ;; Causation
conflict ;; Hostile_encounter || called ;; Name_conferral
took place ;; Process_start
sailed ;; Self_motion || support ;; Supporting || command ;; Control || uprising ;; Violence
joined ;; Becoming_a_member
strengthened ;; Cause_change_of_strength || move ;; Self_motion || provided ;; Giving || withdrew ;; Escaping
began ;; Process_start || riots ;; Violence
began ;; Process_start || fled ;; Escaping || fortifying ;; Defending
began ;; Process_start || refused ;; Agree_or_refuse_to_act || bombardment ;; Attack || cease ;; Process_end || assistance ;; Assistance
argue ;; Judgment_communication || force ;; Control || exaggerated ;; Judgment_communication
attacked ;; Attack || invasion ;; Attack || restore ;; Recovering || proceeded ;; Cause_change_of_position_on_a_scale
occupation ;; Control || remained ;; Temporary_stay
airshow ;; Social_event || held ;; Hold || Airshow ;; Social_event
takes place ;; Process_start || attracts ;; Influence
plays ;; Social_event
made ;; Creating
held ;; Hold || planned ;; Arranging || attracted ;; Motion
became ;; Becoming
flew ;; Motion || cancelled ;; Removing
stayed ;; Temporary_stay
ends ;; Process_end || shows ;; Presence || rounded ;; Motion
fought ;; Hostile_encounter || Battle ;; Hostile_encounter || forces ;; Hostile_encounter
ending ;; Process_end || executed ;; Killing
forced ;; Motion
open ;; Openness
remained ;; Temporary_stay
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
Due ;; Causation || war ;; Hostile_encounter || send ;; Sending
forced ;; Motion || leave ;; Departing || advanced ;; Cause_to_make_progress || initiated ;; Process_start
recapturing ;; Conquering || defeated ;; Conquering || travelled ;; Traveling
ended ;; Process_end || ended ;; Process_end
tournament ;; Competition || competition ;; Competition || evolved ;; Creating
started ;; Process_start || tournament ;; Competition || held ;; Hold
gives ;; Giving || helps ;; Assistance || rankings ;; Competition || experience ;; Perception_active || form ;; Building
given ;; Giving || matches ;; Competition
tournament ;; Competition || Championship ;; Competition || qualified ;; Becoming_a_member || remained ;; Temporary_stay
tournament ;; Competition || tournament ;; Competition || held ;; Hold || qualified ;; Becoming_a_member || winning ;; Earnings_and_losses
determined ;; Deciding
decided ;; Deciding
Raid ;; Attack || states ;; Statement || War ;; Hostile_encounter
raid ;; Attack || named ;; Name_conferral || took place ;; Process_start
raid ;; Attack || caused ;; Causation
raid ;; Attack || beginning ;; Process_start || ending ;; Process_end
demanding ;; Request || Campaign ;; Military_operation || Campaign ;; Military_operation || return ;; Departing
surrender ;; Surrendering || forced ;; Hostile_encounter || remained ;; Presence || command ;; Control || thwarted ;; Hindering
kept ;; Temporary_stay || crossed ;; Motion_directional || state ;; Statement
Cyclone ;; Catastrophe || cyclones ;; Catastrophe || affect ;; Influence
remained ;; Temporary_stay || Forming ;; Coming_to_be
named ;; Name_conferral || achieving ;; Achieve || Moving ;; Motion || cyclone ;; Catastrophe
intensify ;; Cause_change_of_strength || reached ;; Achieve
began ;; Process_start || started ;; Process_start || moving ;; Motion || weaken ;; Cause_change_of_strength || cyclone ;; Catastrophe
cyclone ;; Catastrophe
cyclone ;; Catastrophe
destroyed ;; Destroying
destroyed ;; Destroying
damaged ;; Damaging
destroyed ;; Destroying
reported ;; Reporting
usage ;; Using
record ;; Recording || cyclone ;; Catastrophe || classified ;; Check
reached ;; Arriving
storm ;; Catastrophe || developed ;; Coming_to_be
intensified ;; Cause_change_of_strength || storm ;; Catastrophe
entered ;; Arriving || moved ;; Motion
began ;; Process_start || intensify ;; Cause_change_of_strength || reached ;; Arriving || reaching ;; Arriving
began ;; Process_start || forced ;; Control || hurricane ;; Catastrophe || weaken ;; Cause_change_of_strength
encountered ;; Hostile_encounter || weakened ;; Cause_change_of_strength
approach ;; Arriving || weakened ;; Cause_change_of_strength || made ;; Manufacturing || hurricane ;; Catastrophe
dissipated ;; Dispersal
caused ;; Causation || brought ;; Bringing || resulted in ;; Causation || damage ;; Destroying
reported ;; Reporting || remained ;; Temporary_stay
battle ;; Hostile_encounter || capture ;; Conquering
battle ;; Hostile_encounter || took place ;; Process_start
took ;; Conquering || control ;; Control
responded ;; Response || assault ;; Attack || operation ;; Attack || recapture ;; Conquering || launching ;; Process_start
attack ;; Attack || assault ;; Attack || reinforced ;; Cause_change_of_strength
battle ;; Hostile_encounter || Fighting ;; Hostile_encounter || retreating ;; Escaping || victory ;; Earnings_and_losses
declaration ;; Expressing_publicly
defeated ;; Conquering || retake ;; Conquering || made ;; Action
responded ;; Response || attacking ;; Attack
control ;; Control || Battle ;; Hostile_encounter || remained ;; Temporary_stay
floods ;; Catastrophe || affected ;; Influence
destroyed ;; Destroying || died ;; Death || injured ;; Bodily_harm
evacuate ;; Escaping || forced ;; Causation
affected ;; Influence
killed ;; Killing || staying ;; Temporary_stay || buried ;; Placing || death ;; Death || rise ;; Cause_change_of_position_on_a_scale
plans ;; Arranging || made ;; Creating || make ;; Causation || evacuation ;; Motion
died ;; Death || collapsed ;; Motion_directional || went ;; Motion
caused ;; Causation || triggered ;; Causation || beginning ;; Process_start || run-off ;; Motion || flooding ;; Motion || produced ;; Creating
invasions ;; Attack
occupied ;; Control
caused ;; Causation || damage ;; Damaging || allying ;; Collaboration || war ;; Military_operation || engaged ;; Participation
revolt ;; Change_of_leadership || ravaged ;; Destroying || kept ;; Temporary_stay || cooped ;; Arrest || took part ;; Conquering
placing ;; Placing || govern ;; Control
invade ;; Attack || subjugate ;; Conquering
siege ;; Besieging || penetrated ;; Attack
held ;; Defending || destroyed ;; Destroying
withdraw ;; Escaping || lost ;; Earnings_and_losses || battle ;; Hostile_encounter || fought ;; Hostile_encounter
named ;; Name_conferral || caused ;; Causation || damage ;; Damaging
developed ;; Coming_to_be
intensified ;; Cause_change_of_strength || upgraded ;; Cause_to_make_progress
tracked ;; Self_motion || upgraded ;; Cause_to_make_progress || approached ;; Arriving
peaked ;; Self_motion
weakened ;; Cause_change_of_strength
occurred ;; Coming_to_be || weakened ;; Cause_change_of_strength
curving ;; Motion_directional || turned ;; Becoming
decreased ;; Cause_change_of_position_on_a_scale || allowing ;; Preventing_or_letting || passing ;; Motion_directional || re-intensify ;; Cause_change_of_strength
increased ;; Cause_change_of_position_on_a_scale || decreased ;; Cause_change_of_position_on_a_scale || causing ;; Causation || following ;; Self_motion || weaken ;; Cause_change_of_strength
brought ;; Bringing
occurred ;; Coming_to_be
uprooted ;; Destroying || damaging ;; Damaging
destroyed ;; Destroying || flooding ;; Motion || disrupted ;; Hindering || left ;; Temporary_stay
caused ;; Causation
force ;; Hostile_encounter || toppled ;; Change_of_leadership
caused ;; Causation || damage ;; Damaging
caused ;; Causation || flooding ;; Motion || impacted ;; Influence
damaged ;; Damaging || Flooding ;; Motion || low-lying ;; Placing
death ;; Death || reported ;; Reporting
caused ;; Causation || damage ;; Damaging
following ;; Self_motion || issued ;; Creating
record ;; Recording
issued ;; Creating || release ;; Publishing || pushed ;; Self_motion
produced ;; Manufacturing
differs ;; Change
debuted ;; Process_start || peaked ;; Cause_change_of_position_on_a_scale
became ;; Becoming || debuted ;; Process_start || following ;; Self_motion || stayed ;; Temporary_stay
promote ;; Cause_change_of_position_on_a_scale || made ;; Manufacturing
performed ;; Hold || supporting ;; Supporting
tour ;; Traveling
stayed ;; Temporary_stay || open ;; Openness || cyclone ;; Catastrophe
became ;; Becoming || storm ;; Catastrophe || storm ;; Catastrophe || hurricane ;; Catastrophe || named ;; Name_conferral || weak ;; Cause_change_of_strength || originated ;; Coming_to_be
became ;; Becoming || hurricane ;; Catastrophe || exceed ;; Cause_change_of_position_on_a_scale || passing ;; Motion_directional || defied ;; Agree_or_refuse_to_act
began ;; Process_start || lose ;; Earnings_and_losses || storm ;; Catastrophe || impacted ;; Influence
cyclone ;; Catastrophe || degenerated ;; Change
approach ;; Arriving || storm ;; Catastrophe || brushed ;; Motion || rain ;; Motion_directional
occurred ;; Presence
carried out ;; Military_operation || resided ;; Temporary_stay
entry ;; Arriving || gaining ;; Getting || stopped ;; Preventing_or_letting
wounded ;; Bodily_harm || killed ;; Killing
wounded ;; Bodily_harm
harmed ;; Bodily_harm
took place ;; Process_start
became ;; Becoming || cease ;; Process_end
commanded ;; Control
launched ;; Motion
beginning ;; Process_start || receiving ;; Receiving || launched ;; Motion
arrived ;; Arriving || waited ;; Temporary_stay
attacked ;; Attack || combined ;; Cause_to_amalgamate
retreat ;; Escaping
restored ;; Recovering
monitored ;; Scrutiny
developed ;; Coming_to_be || acquired ;; Getting
moved ;; Motion
became ;; Becoming || degenerated ;; Change
moved ;; Motion || traversed ;; Motion_directional
acquired ;; Getting || indicated ;; Statement
began ;; Process_start || weakened ;; Cause_change_of_position_on_a_scale
declared ;; Expressing_publicly || took place ;; Process_start
raced ;; Competition || struck ;; Destroying
stalled ;; Temporary_stay
occurred ;; Coming_to_be || took place ;; Process_start
resulted in ;; Causation
triggered ;; Causation
took place ;; Process_start
resulted in ;; Causation
produced ;; Manufacturing || hurricane ;; Catastrophe || damage ;; Damaging || Hurricane ;; Catastrophe
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe
moved ;; Motion || hitting ;; Attack || attaining ;; Getting || hurricane ;; Catastrophe
moved ;; Motion || struck ;; Attack || hurricane ;; Catastrophe || state ;; Statement || tracked ;; Self_motion
approached ;; Arriving || remained ;; Temporary_stay
turned ;; Becoming || intensified ;; Cause_change_of_strength || reach ;; Arriving || hurricane ;; Catastrophe || becoming ;; Becoming || due ;; Causation
produced ;; Manufacturing || hurricane ;; Catastrophe || causing ;; Causation || deaths ;; Death || damage ;; Damaging
hurricane ;; Catastrophe
dropped ;; Motion_directional || causing ;; Causation || flooding ;; Motion || Hurricane ;; Catastrophe
caused ;; Causation || hurricane ;; Catastrophe || deaths ;; Death || damage ;; Damaging
produced ;; Manufacturing || caused ;; Causation || hurricane ;; Catastrophe || flooding ;; Motion || death ;; Death
produced ;; Manufacturing || adding ;; Cause_change_of_position_on_a_scale || flooding ;; Motion
caused ;; Causation || flooding ;; Motion || closed ;; Openness || damage ;; Damaging
hurricane ;; Catastrophe || producing ;; Creating || winds ;; Motion || rainfall ;; Catastrophe || moved ;; Motion
storm ;; Catastrophe || cyclone ;; Catastrophe || hurricane ;; Catastrophe || originated ;; Coming_to_be
hurricane ;; Catastrophe || Moving ;; Self_motion || intensified ;; Cause_change_of_strength
hurricane ;; Catastrophe || recurving ;; Self_motion || passed ;; Self_motion
achieved ;; Achieve || hurricane ;; Catastrophe || intensify ;; Cause_change_of_strength
began ;; Process_start || weakening ;; Cause_change_of_strength || storm ;; Catastrophe || hurricane ;; Catastrophe || proceeded ;; Self_motion || degenerated ;; Change || made ;; Arriving
storm ;; Catastrophe || cyclone ;; Catastrophe || transitioned ;; Change || landfall ;; Arriving
merging ;; Cause_to_amalgamate
caused ;; Causation || hurricane ;; Catastrophe || landfall ;; Arriving || damage ;; Damaging
devastating ;; Destroying || hurricane ;; Catastrophe || effects ;; Achieve || struck ;; Bodily_harm || storm ;; Catastrophe
hurricane ;; Catastrophe || hurricane ;; Catastrophe || damage ;; Damaging || occurred ;; Presence || produced ;; Creating
make ;; Causation || storm ;; Catastrophe
floods ;; Catastrophe || damaged ;; Damaging
Damage ;; Damaging
reported ;; Reporting || damage ;; Damaging
incidents ;; Catastrophe || storm ;; Catastrophe || landfalls ;; Arriving || damage ;; Damaging || caused ;; Causation
caused ;; Causation || hurricane ;; Catastrophe || fatalities ;; Death
shootings ;; Use_firearm || shot ;; Use_firearm || shooting ;; Use_firearm
committed ;; Commitment || shootings ;; Use_firearm || occurred ;; Coming_to_be || using ;; Using
shot ;; Use_firearm
killings ;; Killing || established ;; Coming_to_be
report ;; Reporting
use ;; Using
shootings ;; Use_firearm || remain ;; Presence
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || War ;; Hostile_encounter || campaign ;; Military_operation
fought ;; Hostile_encounter
battle ;; Hostile_encounter || fighting ;; Hostile_encounter || death ;; Death || resulted in ;; Causation
battle ;; Hostile_encounter || murder ;; Killing || committed ;; Commitment
battle ;; Hostile_encounter || destroyed ;; Destroying || loss ;; Earnings_and_losses || dating ;; Social_event
ended ;; Process_end || battle ;; Hostile_encounter
marked ;; Recording || campaign ;; Military_operation || capture ;; Conquering
battles ;; Hostile_encounter
operation ;; Military_operation
performed ;; Hold || included ;; Cause_to_be_included
send ;; Sending || attacks ;; Attack || attack ;; Attack
driven ;; Motion || made ;; Giving
promised ;; Commitment || exchange ;; Exchange || providing ;; Supply
consisted ;; Cause_to_be_included
weakened ;; Cause_change_of_strength || formed ;; Coming_to_be
fell ;; Earnings_and_losses
launched ;; Process_start || invasion ;; Military_operation
launched ;; Process_start
assault ;; Attack || assault ;; Attack || preceded ;; Process_start
crossed ;; Motion_directional
comprised ;; Cause_to_be_included || named ;; Name_conferral
assigned ;; Deciding || chosen ;; Choosing
nicknamed ;; Name_conferral || meet ;; Response || developed ;; Coming_to_be
conducted ;; Action || using ;; Using
placed ;; Placing || developing ;; Cause_to_make_progress
captured ;; Conquering || gained ;; Getting || accomplish ;; Achieve || expanded ;; Expansion
counterattack ;; Attack
launched ;; Process_start
retreated ;; Escaping
cyclone ;; Catastrophe || resulted in ;; Causation
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || formed ;; Coming_to_be || reach ;; Achieve
storm ;; Catastrophe || landfall ;; Arriving
blamed ;; Judgment_communication || storm ;; Catastrophe || deaths ;; Death || reaching ;; Achieve
deaths ;; Death || cyclone ;; Catastrophe || reported ;; Reporting || deaths ;; Death
pledged ;; Commitment || aid ;; Assistance || estimated ;; Statement
tournament ;; Competition || took place ;; Process_start
formed ;; Coming_to_be || qualifying ;; Becoming_a_member
matches ;; Competition || lost ;; Earnings_and_losses
starts ;; Process_start || made ;; Action
matches ;; Competition || secure ;; Getting
contention ;; Competition
defeating ;; Hostile_encounter || defeat ;; Hostile_encounter || progress ;; Cause_to_make_progress || victory ;; Earnings_and_losses || guarantee ;; Commitment || proceed ;; Motion
end ;; Process_end || rate ;; Cause_change_of_position_on_a_scale || happened ;; Presence
began ;; Process_start || uprising ;; Change_of_leadership
started ;; Process_start || uprising ;; Change_of_leadership
spread ;; Dispersal
committed ;; Commitment || uprising ;; Change_of_leadership
expanded ;; Expansion || suppress ;; Hindering
called ;; Name_conferral
chosen ;; Choosing
took place ;; Process_start || struggle ;; Hostile_encounter || engagement ;; Hostile_encounter
conquered ;; Conquering
offered ;; Giving || besieged ;; Besieging || surrender ;; Surrendering
rejected ;; Response || knew ;; Know || commanding ;; Control || defeated ;; Conquering
suffered ;; Bodily_harm
killed ;; Killing || fell ;; Motion_directional
ended ;; Process_end || loss ;; Earnings_and_losses || control ;; Control || regain ;; Getting
praised ;; Judgment_communication || displayed ;; Action || admired ;; Judgment_communication
refused ;; Agree_or_refuse_to_act || promised ;; Commitment || supplies ;; Supply || surrender ;; Surrendering || answer ;; Communication || come ;; Self_motion
wars ;; Hostile_encounter || assisted ;; Assistance || fought ;; Hostile_encounter
declared ;; Expressing_publicly || joined ;; Becoming_a_member
withdrew ;; Escaping || besieged ;; Besieging || committed ;; Killing || suicide ;; Killing
brought ;; Bringing || wars ;; Hostile_encounter || end ;; Process_end || plagued ;; Bodily_harm
became ;; Becoming || bestowed ;; Name_conferral
transform ;; Change
beginning ;; Process_start || mark ;; Recording
contested ;; Competition
competition ;; Competition
played ;; Competition
match ;; Competition
contested ;; Competition || assuring ;; Commitment
attack ;; Attack || took place ;; Process_start
fired ;; Use_firearm
killed ;; Killing || injured ;; Bodily_harm || suffered ;; Bodily_harm || injuries ;; Bodily_harm || attack ;; Attack
suicide ;; Killing || committed ;; Killing || shooting ;; Killing
murdered ;; Killing
attack ;; Attack || shootings ;; Killing || occurred ;; Presence
killing ;; Killing
defending ;; Defending || includes ;; Cause_to_be_included
surrendered ;; Surrendering || killed ;; Killing || became ;; Becoming || died ;; Death || combat ;; Hostile_encounter
supported ;; Supporting || attack ;; Attack || fight ;; Defending
declared ;; Expressing_publicly
attack ;; Attack || persecuted ;; Bodily_harm
imprisoned ;; Arrest || abused ;; Committing_crime || killed ;; Killing || flee ;; Escaping || forced ;; Control
charged ;; Judgment_communication || committed ;; Commitment
forming ;; Creating || leading to ;; Causation || killings ;; Killing || support ;; Supporting || covered ;; Creating
adopted ;; Choosing || support ;; Supporting
Championship ;; Competition || match ;; Competition || Final ;; Competition || played ;; Competition
determined ;; Deciding || final ;; Process_end || promoted ;; Cause_to_make_progress
final ;; Process_end || held ;; Hold || completed ;; Process_end || final ;; Competition
Championship ;; Competition || final ;; Process_end || finished ;; Process_end || finished ;; Process_end
defeated ;; Earnings_and_losses
named ;; Name_conferral || match ;; Competition || match ;; Competition
scored ;; Getting || scored ;; Getting || final ;; Process_end || make ;; Action || assured ;; Commitment
uprising ;; Change_of_leadership || took place ;; Process_start
distinguished ;; Check
murder ;; Killing || launched ;; Military_operation || seize ;; Hold || holding ;; Hold || creating ;; Creating
flee ;; Escaping || forcing ;; Request
prevent ;; Preventing_or_letting || defenses ;; Defending || incursion ;; Attack
lasted ;; Process_end || gave rise to ;; Cause_change_of_position_on_a_scale || marriages ;; Forming_relationships
negotiation ;; Communication || conflict ;; Hostile_encounter || uprising ;; Change_of_leadership || suppressed ;; Hindering
killed ;; Killing || killed ;; Killing || wounded ;; Bodily_harm || claim ;; Statement
committed ;; Commitment || uprising ;; Change_of_leadership
Battle ;; Hostile_encounter || forces ;; Military_operation || forces ;; Military_operation || defeated ;; Conquering || War ;; Military_operation
dubbed ;; Name_conferral || battle ;; Hostile_encounter || took place ;; Process_start
fighting ;; Hostile_encounter
committed ;; Commitment || Reinforcements ;; Cause_change_of_strength || bolster ;; Cause_change_of_strength
battle ;; Hostile_encounter || engagements ;; Hostile_encounter || Battle ;; Hostile_encounter
attacks ;; Attack || launched ;; Process_start
attacks ;; Attack || occurred ;; Coming_to_be || supported ;; Supporting || advanced ;; Self_motion || support ;; Supporting
attack ;; Attack || positioned ;; Placing || ran ;; Self_motion || counter ;; Attack
ravaged ;; Attack || Strikes ;; Attack || attacking ;; Attack
fighting ;; Hostile_encounter || fought ;; Hostile_encounter
attacks ;; Attack || forces ;; Military_operation || broke ;; Process_start || pushed ;; Causation
Battle ;; Hostile_encounter || outflanked ;; Defending || push ;; Self_motion
running ;; Motion
inspired ;; Creating || idea ;; Creating
listening ;; Perception_active || travelling ;; Traveling || running ;; Motion
run ;; Motion
founded ;; Creating || help ;; Assistance || volunteers ;; Commitment || run ;; Motion || maintain ;; Statement || drive ;; Motion
travels ;; Traveling
begins ;; Process_start || ends ;; Process_end
events ;; Traveling
turns ;; Motion || return ;; Arriving || stops ;; Process_end
held ;; Hold || Games ;; Competition || event ;; Competition
took place ;; Process_start
organized ;; Arranging || Games ;; Competition
ceremony ;; Social_event || took place ;; Hold
Games ;; Competition || opened ;; Process_start
Games ;; Competition
sworn ;; Commitment
occurred ;; Presence || state ;; Statement || accident ;; Catastrophe || struck ;; Attack
killed ;; Killing || injured ;; Bodily_harm
returning ;; Arriving || prayers ;; Social_event || victims ;; Death
beat ;; Bodily_harm || attacked ;; Attack || accident ;; Catastrophe || triggered ;; Causation
called ;; Name_conferral
pledged ;; Commitment || urged ;; Request
engagement ;; Hostile_encounter || formed ;; Cause_to_amalgamate
divide ;; Cause_to_amalgamate || split ;; Destroying
battle ;; Hostile_encounter || fought ;; Hostile_encounter
defeat ;; Conquering
pursued ;; Self_motion || lasted ;; Process_end
surrendered ;; Surrendering || fight ;; Hostile_encounter
escaped ;; Escaping
refitting ;; Process_start || returned ;; Arriving
divided ;; Creating
concluded ;; Process_end
undermined ;; Destroying
dispatched ;; Sending || failed ;; Agree_or_refuse_to_act
referred ;; Name_conferral
supported ;; Supporting
ended ;; Process_end || surrendered ;; Surrendering
demanded ;; Request
used ;; Using
remarks ;; Statement
reshaped ;; Change
reduced ;; Cause_change_of_position_on_a_scale || established ;; Creating
became ;; Becoming || devastated ;; Destroying || regained ;; Getting
wrought ;; Causation
transformed ;; Change
marked ;; Recording
followed ;; Arriving
War ;; Hostile_encounter
formed ;; Coming_to_be || ensued ;; Coming_to_be
concluded ;; Process_end
concluding ;; Process_end || resulted in ;; Causation
governed ;; Control
join ;; Becoming_a_member || governed ;; Control
declared ;; Expressing_publicly || ordered ;; Arranging || defeated ;; Conquering || claimed ;; Statement
ordered ;; Arranging
fought ;; Hostile_encounter || capitulated ;; Surrendering
surrendered ;; Surrendering
took place ;; Process_start
clashed ;; Hostile_encounter
resulted in ;; Causation
resumed ;; Process_start
capture ;; Conquering
commanded ;; Control || siege ;; Besieging || repelled ;; Defending
followed ;; Self_motion || siege ;; Besieging
included ;; Cause_to_be_included || brought ;; Bringing
began ;; Process_start || shelling ;; Attack
agreed ;; Agree_or_refuse_to_act || surrender ;; Surrendering
entered ;; Arriving
recapture ;; Conquering || victory ;; Earnings_and_losses
built ;; Building || commemorate ;; Social_event
remained ;; Presence
battle ;; Hostile_encounter || commanded ;; Control || controlled ;; Control
came to ;; Self_motion
surrender ;; Surrendering || resulting in ;; Causation
made ;; Manufacturing || prevent ;; Preventing_or_letting || prevent ;; Preventing_or_letting || siege ;; Besieging || leaving ;; Departing
used ;; Using || use ;; Using || siege ;; Besieging || Siege ;; Besieging || sieges ;; Besieging
provided ;; Supply
conquering ;; Conquering
organized ;; Arranging || attack ;; Attack
mixed ;; Cause_to_amalgamate || joined ;; Becoming_a_member
attack ;; Attack || attacked ;; Attack || allied ;; Collaboration || fighting ;; Hostile_encounter
turned ;; Becoming || supplies ;; Supply || retreat ;; Escaping || occupying ;; Control || marched ;; Self_motion || dwindling ;; Cause_change_of_position_on_a_scale || forced ;; Conquering
attack ;; Attack || resulted in ;; Causation || losses ;; Earnings_and_losses || surrender ;; Surrendering
agreed ;; Agree_or_refuse_to_act
Battle ;; Hostile_encounter || fighting ;; Hostile_encounter || saw ;; Causation || led ;; Causation
rout ;; Destroying || siege ;; Besieging || raise ;; Cause_change_of_position_on_a_scale
Wars ;; Hostile_encounter || action ;; Response || took place ;; Process_start || War ;; Military_operation
invaded ;; Attack
attacks ;; Attack || siege ;; Besieging || repulsed ;; Defending || seize ;; Conquering || forced ;; Causation
resist ;; Defending || siege ;; Besieging || posted ;; Sending || advanced ;; Self_motion || raise ;; Cause_change_of_position_on_a_scale
attack ;; Attack || fleeing ;; Escaping
attacking ;; Attack || made ;; Creating || contest ;; Hostile_encounter
gained ;; Getting || flight ;; Escaping || put ;; Causation
surrendered ;; Surrendering || put ;; Causation || limped ;; Self_motion
took place ;; Process_start
started ;; Process_start || siege ;; Besieging
surrendered ;; Surrendering
marched ;; Self_motion || commanded ;; Control || join ;; Becoming_a_member || blocking ;; Hindering || contributed ;; Assistance
occupied ;; Control
landed ;; Self_motion || invasion ;; Attack
invaded ;; Attack || provide ;; Supply || support ;; Supporting
began ;; Process_start
constructed ;; Building
allowed ;; Preventing_or_letting || conduct ;; Action || attack ;; Attack
began ;; Process_start || landed ;; Self_motion
established ;; Building || constructed ;; Creating || landed ;; Self_motion
replaced ;; Change_of_leadership || replaced ;; Change_of_leadership || invasion ;; Attack
ended ;; Process_end || surrender ;; Surrendering
pitted ;; Hostile_encounter
depended ;; Influence
declared ;; Expressing_publicly || viewed ;; Perception_active
ended ;; Process_end || call ;; Name_conferral
supported ;; Supporting
took place ;; Process_start
began ;; Process_start
erupted ;; Process_start
planned ;; Arranging || met with ;; Come_together
lost ;; Earnings_and_losses || died ;; Death
captured ;; Conquering || ordered ;; Arranging
given ;; Giving
expelled ;; Removing || captured ;; Conquering
driven ;; Motion
increased ;; Cause_change_of_position_on_a_scale || came to ;; Self_motion
launched ;; Process_start
lost ;; Earnings_and_losses || ceded ;; Surrendering
ceded ;; Surrendering
ceded ;; Surrendering
reduced ;; Cause_change_of_position_on_a_scale
conflict ;; Hostile_encounter || protests ;; Agree_or_refuse_to_act
arranging ;; Arranging || surfaced ;; Motion || portrayed ;; Change_sentiment
resignation ;; Change_of_leadership || outcry ;; Judgment_communication || resulted in ;; Causation
demonstration ;; Social_event || called ;; Request
corruption ;; Committing_crime || protest ;; Agree_or_refuse_to_act || called ;; Motion
killings ;; Killing || protest ;; Agree_or_refuse_to_act || led to ;; Causation
died ;; Death
tournament ;; Competition
organised ;; Arranging || associate ;; Collaboration || tournament ;; Competition
qualified ;; Becoming_a_member || qualify ;; Becoming_a_member
net ;; Earnings_and_losses || tournament ;; Competition
tournament ;; Competition
winning ;; Getting || tournament ;; Competition || qualified ;; Becoming_a_member
tournament ;; Competition || qualify ;; Becoming_a_member
ruling ;; Control
cutting ;; Bodily_harm || converted ;; Becoming || led to ;; Causation
claimed ;; Statement || descended ;; Becoming_a_member
marked ;; Recording
Remembered ;; Change_sentiment || reoriented ;; Change
competitions ;; Competition || played ;; Competition
resembles ;; Change_sentiment
moved ;; Motion || planned ;; Arranging
began ;; Process_start || competing ;; Competition || play ;; Competition
concluded ;; Process_end
Campaigns ;; Military_operation || support ;; Supporting || War ;; Hostile_encounter || operations ;; Military_operation
surprised ;; Change_sentiment || rest ;; Placing
transported ;; Sending
destroyed ;; Destroying || bombing ;; Attack || enjoyed ;; Getting || negated ;; Destroying
war ;; Hostile_encounter || commanded ;; Control
occupied ;; Control
provided ;; Supply || afloat ;; Motion || evacuated ;; Removing
operational ;; Being_in_operation || reduced ;; Cause_change_of_position_on_a_scale
earthquake ;; Catastrophe || struck ;; Attack
shaking ;; Motion || felt ;; Motion
died ;; Death || destroyed ;; Destroying
remembered ;; Change_sentiment
death ;; Death
proved ;; Coming_to_believe
formed ;; Coming_to_be
triggered ;; Causation
killed ;; Killing || dead ;; Death
feared ;; Change_sentiment
tournament ;; Competition || took place ;; Process_start || involving ;; Cause_to_be_included
matches ;; Competition || played ;; Competition || matches ;; Competition || played ;; Competition
tournament ;; Competition || played ;; Competition || playing ;; Competition || progressed ;; Achieve
contested ;; Competition || defeating ;; Preventing_or_letting
prevailing ;; Conquering
represented ;; Change_sentiment
play ;; Competition || season ;; Competition || series ;; Competition || toured ;; Traveling
match ;; Competition || series ;; Competition
tour ;; Traveling || tour ;; Traveling
began ;; Process_start || tour ;; Traveling
impression ;; Change_sentiment || put up ;; Placing
match ;; Competition || victories ;; Earnings_and_losses || Matches ;; Competition
fought ;; Hostile_encounter
felt ;; Change_sentiment || married ;; Forming_relationships
raised ;; Cause_change_of_position_on_a_scale
held ;; Hold || forced ;; Control
siege ;; Besieging
sent ;; Sending
exiled ;; Departing
butchered ;; Killing || lost ;; Earnings_and_losses
died ;; Death
demonstration ;; Participation || march ;; Self_motion || occurred ;; Coming_to_be
demanded ;; Request || establishment ;; Creating
organized ;; Arranging || began ;; Process_start || march ;; Self_motion
participants ;; Participation || estimated ;; Scrutiny || quoted ;; Statement || ranges ;; Cause_to_be_included
feared ;; Change_sentiment || crowded ;; Come_together || bring ;; Bringing
demonstration ;; Participation || march ;; Self_motion || described ;; Statement || surpassing ;; Achieve || considered ;; Influence || held ;; Defending
became ;; Becoming
struggled ;; Motion
occurred ;; Presence
dissipated ;; Dispersal
opened ;; Openness || suspended ;; Process_end
produced ;; Creating
caused ;; Causation
attributed ;; Causation
brought ;; Bringing
founded ;; Creating || includes ;; Cause_to_be_included || festival ;; Social_event || held ;; Hold
became ;; Becoming || death ;; Death
became ;; Becoming || donated ;; Giving || festival ;; Social_event || established ;; Building
given in ;; Giving
used ;; Using
discovered ;; Know || reconstructed ;; Recovering
approaches ;; Arriving || resulted in ;; Causation || restored ;; Recovering
Included ;; Cause_to_be_included
holding ;; Hold
included ;; Cause_to_be_included
held ;; Hold || held ;; Hold
increase ;; Cause_change_of_position_on_a_scale || used ;; Using || implied ;; Statement || sponsored ;; Supply
announced ;; Expressing_publicly
took place ;; Process_start
took place ;; Process_start
took place ;; Process_start
announced ;; Expressing_publicly || take place ;; Process_start
returned ;; Arriving
put on ;; Process_start
bringing in ;; Bringing
dubbed ;; Name_conferral || held ;; Hold
returned ;; Arriving
returning ;; Arriving
reported ;; Recording
died ;; Death || injured ;; Bodily_harm || crashed ;; Catastrophe || occurred ;; Presence
found ;; Know || concluded ;; Coming_to_believe
remained ;; Presence || forced ;; Motion || crushed ;; Bodily_harm
rode ;; Motion
applied ;; Using || depressed ;; Influence
attended ;; Perception_active
showed ;; Presence
include ;; Cause_to_be_included || established ;; Building
showed ;; Presence || prosecuted ;; Legality
introduced ;; Statement
became ;; Becoming
ended ;; Process_end || started ;; Process_start
battle ;; Military_operation || battle ;; Military_operation || war ;; Military_operation
victory ;; Earnings_and_losses || took place ;; Process_start
fought ;; Hostile_encounter || forces ;; Causation || took ;; Conquering || led ;; Causation || command ;; Control || killed ;; Killing
fought ;; Hostile_encounter || establish ;; Creating || battle ;; Military_operation || attempt ;; Influence || campaigning ;; Military_operation || ended ;; Process_end
battle ;; Hostile_encounter
stationed ;; Being_in_operation || get ;; Getting || forces ;; Request || defending ;; Defending
arrived ;; Arriving || defeated ;; Preventing_or_letting || forces ;; Causation
affect ;; Influence || affected ;; Influence || cyclone ;; Catastrophe
developed ;; Coming_to_be
moved ;; Motion || existence ;; Presence
caused ;; Causation || increasing ;; Cause_change_of_position_on_a_scale || storm ;; Catastrophe || degenerated ;; Change
became ;; Becoming || regenerated ;; Recovering
intensifying ;; Cause_change_of_strength || reached ;; Arriving
began ;; Process_start || entered ;; Arriving || weaken ;; Cause_change_of_strength
begun ;; Process_start || moved ;; Motion || passing ;; Motion_directional || weakening ;; Cause_change_of_strength
completed ;; Process_end || dissipated ;; Departing
causing ;; Causation || impacted ;; Influence
affected ;; Influence || killed ;; Killing
lost ;; Earnings_and_losses
functioning ;; Being_in_operation
existing ;; Presence || spread ;; Cause_change_of_position_on_a_scale
caused ;; Causation || damages ;; Damaging
deaths ;; Death || damage ;; Damaging || retired ;; Removing
campaigns ;; Military_operation || lasting ;; Being_in_operation || initiated ;; Process_start || completed ;; Process_end
declared ;; Expressing_publicly || remain ;; Presence
conquering ;; Conquering || used ;; Using || attack ;; Attack || beset ;; Hostile_encounter
reconquer ;; Conquering
campaign ;; Military_operation
withdrew ;; Escaping || halt ;; Process_end || defeated ;; Conquering
called ;; Name_conferral || maintained ;; Preserving || remained ;; Presence || ruled ;; Control
ceased ;; Process_end
held ;; Hold
operated ;; Being_in_operation
founded ;; Creating
likened ;; Judgment_communication
attended ;; Participation
programmed ;; Arranging
included ;; Cause_to_be_included
killing ;; Killing || serving ;; Assistance || crashed ;; Catastrophe
occur ;; Coming_to_be || accident ;; Catastrophe
lost ;; Earnings_and_losses || contact ;; Communication || land ;; Motion || crashed ;; Catastrophe
involving ;; Cause_to_be_included || accident ;; Catastrophe || crash ;; Catastrophe
operated ;; Being_in_operation
crash ;; Catastrophe
crashed ;; Catastrophe || operating ;; Being_in_operation || flight ;; Motion
participation ;; Participation || celebration ;; Social_event
killed ;; Killing || destroyed ;; Destroying
determined ;; Deciding || caused ;; Causation
determine ;; Deciding || set ;; Placing || theorized ;; Coming_to_believe || malfunction ;; Motion
produced ;; Manufacturing || show ;; Social_event || show ;; Social_event || opening ;; Openness || commemorate ;; Social_event
event ;; Social_event || shows ;; Social_event || takes place ;; Process_start
documented ;; Recording || planned ;; Arranging || events ;; Social_event || matches ;; Competition || event ;; Social_event || confirmation ;; Check || found ;; Know || found ;; Know || leading ;; Causation || took place ;; Process_start || found ;; Know
show ;; Social_event
show ;; Social_event || took place ;; Process_start
ended ;; Process_end || change ;; Change || name ;; Name_conferral || association ;; Collaboration || operated ;; Being_in_operation || promote ;; Cause_change_of_position_on_a_scale || prompted ;; Cause_change_of_position_on_a_scale
Battle ;; Hostile_encounter || forces ;; Causation || engagement ;; Hostile_encounter || War ;; Military_operation || took place ;; Process_start
Battle ;; Hostile_encounter || engagements ;; Hostile_encounter || fought ;; Hostile_encounter
ended ;; Process_end || battle ;; Hostile_encounter || attacks ;; Attack || repel ;; Defending
placed ;; Placing || Operating ;; Being_in_operation
battle ;; Hostile_encounter || attacked ;; Attack || attack ;; Attack || break ;; Destroying
stopping ;; Process_end || counteroffensive ;; Attack || advancing ;; Cause_to_make_progress
fight ;; Hostile_encounter || repel ;; Defending || Battle ;; Hostile_encounter
performed ;; Hold || forcing ;; Causation || battles ;; Hostile_encounter
caused ;; Causation || held ;; Defending || expansion ;; Expansion || defeat ;; Conquering || speed ;; Cause_change_of_position_on_a_scale || occurred ;; Presence
became ;; Becoming || occupied ;; Control
demanded ;; Request
became ;; Becoming || due ;; Causation
formed ;; Coming_to_be || present ;; Presence || relief ;; Assistance
operated ;; Being_in_operation || involved ;; Cause_to_be_included || invasion ;; Attack || operation ;; Military_operation
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || engagement ;; Hostile_encounter || fought ;; Hostile_encounter
enlisted ;; Becoming_a_member || supply ;; Supply
arrived in ;; Arriving || commanded ;; Control
arrested ;; Arrest || disapproved ;; Agree_or_refuse_to_act
escaped ;; Escaping || arrest ;; Arrest
ships ;; Sending || seized ;; Hold || sailed ;; Self_motion
wounded ;; Bodily_harm || captured ;; Conquering
fought ;; Hostile_encounter || Battle ;; Hostile_encounter || control ;; Control || captured ;; Conquering
operating ;; Being_in_operation
held ;; Defending
initiated ;; Process_start || hosted ;; Being_in_operation
performing ;; Hold || perform ;; Hold || leading ;; Causation
connotes ;; Statement
grown ;; Expansion
began ;; Process_start || Festival ;; Social_event || festival ;; Social_event || started ;; Process_start || Festival ;; Social_event
festival ;; Social_event || restarted ;; Process_start || operations ;; Being_in_operation
became ;; Becoming || festival ;; Social_event || started ;; Process_start || support ;; Supporting
began ;; Process_start || became ;; Becoming || concert ;; Social_event || events ;; Social_event
festival ;; Social_event || concerts ;; Social_event || offered ;; Giving
ceased ;; Process_end || expanded ;; Expansion || gave ;; Giving || operations ;; Being_in_operation || include ;; Cause_to_be_included || meet ;; Come_together
included ;; Cause_to_be_included
Campaign ;; Military_operation || fought ;; Hostile_encounter
took place ;; Process_start
aimed ;; Aiming
operate ;; Being_in_operation || used ;; Using || restricted ;; Hindering
losing ;; Earnings_and_losses || sank ;; Attack
competition ;; Competition || replacing ;; Change || introduced ;; Creating || changed ;; Change
plays ;; Competition || joining ;; Becoming_a_member || consisting ;; Cause_to_be_included || games ;; Competition || progress ;; Achieve || knock-out ;; Competition || operated ;; Being_in_operation
tournament ;; Competition || final ;; Competition
held ;; Hold || competition ;; Competition || final ;; Competition || win ;; Getting
match ;; Competition || refereed ;; Scrutiny
eliminated ;; Removing || defending ;; Defending || dropping ;; Motion
fair ;; Social_event
held ;; Hold
topped ;; Self_motion || attended ;; Participation
moved ;; Motion
operated ;; Being_in_operation
held ;; Hold
generates ;; Creating
operating ;; Being_in_operation || take off ;; Departing
crashed ;; Catastrophe || killing ;; Killing
died ;; Death || injuries ;; Bodily_harm
fatal ;; Killing || crash ;; Catastrophe
Battle ;; Hostile_encounter || fought ;; Hostile_encounter || forces ;; Causation || allied ;; Collaboration
invasion ;; Attack || Conqueror ;; Conquering || launched ;; Process_start
scored ;; Getting
resist ;; Defending || counterattacks ;; Attack
move ;; Self_motion || met ;; Come_together
strike ;; Attack || offensive ;; Attack || ravaging ;; Destroying || burning ;; Bodily_harm || allowed ;; Preventing_or_letting || launch ;; Process_start
Battle ;; Hostile_encounter || forays ;; Attack || confront ;; Hostile_encounter || operating ;; Being_in_operation || continued ;; Process_start
began ;; Process_start || brought ;; Bringing
snowstorm ;; Catastrophe || blizzard ;; Catastrophe || described ;; Statement
storm ;; Catastrophe || fell ;; Motion_directional
stopped ;; Process_end
storm ;; Catastrophe || increased ;; Cause_change_of_position_on_a_scale || records ;; Recording || fed ;; Supply
begin ;; Process_start || created ;; Creating || halted ;; Process_end || blocked ;; Hindering
resumed ;; Process_start || ran ;; Being_in_operation
blizzard ;; Catastrophe || attributed ;; Causation || deaths ;; Death
end ;; Process_end || Festival ;; Social_event || event ;; Social_event || taking place ;; Process_start
organised ;; Arranging || festival ;; Social_event || took place ;; Process_start
festival ;; Social_event || takes places ;; Process_start
charity ;; Social_event || run ;; Being_in_operation
aim ;; Aiming || promote ;; Cause_to_make_progress
festival ;; Social_event || hosts ;; Come_together || promotes ;; Cause_to_make_progress
festival ;; Social_event
held ;; Hold
operated ;; Being_in_operation
returned ;; Arriving
held ;; Hold
operate ;; Being_in_operation || moved ;; Motion
recapture ;; Conquering || expedition ;; Attack || retake ;; Conquering
captured ;; Conquering
ordered ;; Arranging || recovering ;; Control
crossed ;; Motion_directional || arrived at ;; Arriving || Sailing ;; Self_motion || appointed ;; Deciding
besieged ;; Besieging || recaptured ;; Conquering
expulsion ;; Removing || resulted in ;; Causation
control ;; Control || struggle ;; Hostile_encounter
war ;; Hostile_encounter || War ;; Hostile_encounter
war ;; Hostile_encounter || took place ;; Process_start
fighting ;; Hostile_encounter || took place ;; Process_start
war ;; Hostile_encounter
declared ;; Expressing_publicly || began ;; Process_start || fall ;; Self_motion
war ;; Hostile_encounter || control ;; Control || left ;; Causation
stationed ;; Placing || joint ;; Collaboration || war ;; Hostile_encounter
developed ;; Coming_to_be || war ;; Hostile_encounter || waged ;; Hold
began ;; Process_start || change ;; Change || election ;; Choosing || reaching ;; Arriving || deteriorate ;; Cause_change_of_position_on_a_scale
begun ;; Process_start || response ;; Response || shelling ;; Use_firearm
attacks ;; Attack || broke ;; Causation
put an end to ;; Process_end || attacks ;; Attack || restore ;; Recovering || order ;; Arranging || conflict ;; Hostile_encounter || sent ;; Sending
control ;; Control || took ;; Getting
crossed ;; Motion_directional || state ;; Statement || response ;; Response || advanced ;; Cause_to_make_progress
invasion ;; Attack || accused ;; Judgment_communication || launched ;; Process_start
retreated ;; Escaping || fought ;; Hostile_encounter
attacking ;; Attack || held ;; Defending || opened ;; Process_start
blockaded ;; Besieging
attacked ;; Attack
warfare ;; Hostile_encounter
war ;; Hostile_encounter || conflict ;; Hostile_encounter || waged ;; Hold
negotiated ;; Communication
occupied ;; Control || holding on to ;; Defending
destroyed ;; Destroying
recognised ;; Agree_or_refuse_to_act || severed ;; Process_end
withdrawal ;; Escaping || completed ;; Process_end
war ;; Hostile_encounter || displaced ;; Motion || returned ;; Motion || remained ;; Presence
occupied ;; Control
expedition ;; Self_motion
broken out ;; Process_start || revolution ;; Change_of_leadership || halt ;; Process_end
invaded ;; Attack || defeated ;; Conquering || advancing ;; Cause_to_make_progress
support ;; Supporting || appealed ;; Request
agreed ;; Agree_or_refuse_to_act || assist ;; Assistance
achieving ;; Achieve
besieged ;; Besieging || ending ;; Process_end || captured ;; Conquering || stronghold ;; Hostile_encounter
battle ;; Hostile_encounter || fought ;; Hostile_encounter || War ;; Military_operation
Operation ;; Military_operation || attacked ;; Attack || conducting ;; Action
operation ;; Military_operation || struck ;; Attack || sank ;; Motion_directional
fired ;; Use_firearm || wounding ;; Bodily_harm
prompted ;; Causation || construction ;; Building || use ;; Using || conflict ;; Hostile_encounter || begin ;; Process_start
blockading ;; Besieging || gave ;; Giving || incentive ;; Influence || take ;; Action
Battle ;; Hostile_encounter || fought ;; Hostile_encounter
battle ;; Hostile_encounter || fought ;; Hostile_encounter
present ;; Presence || attacked ;; Attack
held ;; Defending || check ;; Scrutiny || infiltrate ;; Arriving
outflank ;; Besieging || seized ;; Conquering
withdrawal ;; Escaping || enabling ;; Preventing_or_letting || build ;; Building || forces ;; Causation || defeat ;; Conquering || led to ;; Causation
concentrated ;; Aiming || solidifying ;; Cause_change_of_strength
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || forces ;; Causation
battle ;; Hostile_encounter || held ;; Defending
command ;; Control || marched ;; Self_motion || outbreak ;; Process_start
besiege ;; Besieging || failed ;; Earnings_and_losses
pulled ;; Motion
defeated ;; Conquering || caught ;; Social_event
pull ;; Motion
fought ;; Hostile_encounter
commanded ;; Control
includes ;; Cause_to_be_included || capture ;; Conquering
siege ;; Besieging || started ;; Process_start
release ;; Releasing || casualties ;; Catastrophe
capture ;; Conquering
fought ;; Hostile_encounter
named ;; Name_conferral
attacked ;; Attack
became ;; Becoming || tried ;; Change
besieged ;; Besieging
launched ;; Process_start
agreed ;; Agree_or_refuse_to_act
arrived in ;; Arriving || followed ;; Self_motion
blocked ;; Hindering
fought ;; Hostile_encounter
destroying ;; Destroying || shattered ;; Destroying || defeat ;; Conquering
attacked ;; Attack || destroying ;; Destroying || encircling ;; Besieging
destroyed ;; Destroying
destroy ;; Destroying || encircle ;; Besieging || collapse ;; Destroying
end ;; Process_end
used ;; Using
enabling ;; Preventing_or_letting || removing ;; Removing || diverted ;; Removing
allowed ;; Preventing_or_letting || reach ;; Arriving || put ;; Placing
Battle ;; Military_operation || battle ;; Military_operation || War ;; Military_operation
battle ;; Military_operation || victory ;; Earnings_and_losses || defeats ;; Earnings_and_losses
occupied ;; Control
occupying ;; Control || occupy ;; Control || witnessed ;; Know
outflank ;; Besieging || believed ;; Know
emphasise ;; Statement
battles ;; Hostile_encounter || occur ;; Presence
fought ;; Hostile_encounter
wounded ;; Bodily_harm || resulted in ;; Causation || wounding ;; Bodily_harm
casualties ;; Catastrophe
blockade ;; Besieging || occupation ;; Attack || erupted ;; Process_start
Battle ;; Hostile_encounter || offensive ;; Attack
attack ;; Attack || build ;; Building || called ;; Request || encircling ;; Besieging || Siege ;; Besieging
formed ;; Cause_to_amalgamate || offensive ;; Attack
began ;; Process_start || offensive ;; Attack
produced ;; Creating || gains ;; Earnings_and_losses || turned ;; Becoming
defense ;; Defending || gave ;; Giving || forces ;; Causation || positions ;; Placing || reinforce ;; Cause_change_of_strength
stopped ;; Process_end || forces ;; Causation || offensive ;; Attack
held ;; Hold || event ;; Competition
hosted ;; Social_event
participated ;; Participation || games ;; Competition || events ;; Competition
host ;; Social_event || games ;; Competition
games ;; Competition || opened ;; Process_start
records ;; Recording || games ;; Competition || broken ;; Destroying
games ;; Competition || deemed ;; Coming_to_believe
hurricane ;; Catastrophe || hurricane ;; Catastrophe || caused ;; Causation || impacts ;; Influence
hurricane ;; Catastrophe || formed ;; Coming_to_be || storm ;; Catastrophe
intensified ;; Cause_change_of_strength || storm ;; Catastrophe
hurricane ;; Catastrophe || traveled ;; Self_motion
storm ;; Catastrophe
damages ;; Damaging || killed ;; Killing || hurricane ;; Destroying
killed ;; Killing || storm ;; Catastrophe
production ;; Manufacturing || hurricane ;; Catastrophe || led to ;; Causation
caused ;; Causation
caused ;; Causation || affected ;; Influence || affected ;; Influence
affected ;; Influence || affecting ;; Influence || called ;; Request
affected ;; Influence
flooded ;; Catastrophe
lost ;; Earnings_and_losses
forced ;; Motion || close ;; Openness || disrupted ;; Hindering
devastated ;; Destroying || flooded ;; Motion || killed ;; Killing
experiencing ;; Perception_active || smashed ;; Destroying || said ;; Statement
affected ;; Influence || said ;; Statement
described ;; Statement
registered ;; Know || struck ;; Attack || earthquake ;; Catastrophe
occurred ;; Coming_to_be || quake ;; Catastrophe
joins ;; Becoming_a_member || earthquakes ;; Catastrophe || affected ;; Influence
earthquake ;; Catastrophe || earthquake ;; Catastrophe
lies ;; Placing || affected ;; Influence
reaching ;; Arriving || earthquake ;; Catastrophe || activity ;; Motion_directional
recorded ;; Recording || downgraded ;; Cause_change_of_position_on_a_scale
quake ;; Catastrophe || damage ;; Damaging || spanning ;; Expansion || incident ;; Catastrophe || reported ;; Reporting || felt ;; Perception_active
earthquake ;; Catastrophe || reported ;; Reporting
nicknamed ;; Name_conferral || caused ;; Causation || flooding ;; Catastrophe || floods ;; Catastrophe || blocked ;; Hindering || flood ;; Catastrophe
building ;; Building || falling ;; Motion_directional || resulted ;; Causation
formed ;; Creating || held ;; Defending || flood ;; Catastrophe
lost ;; Death || affected ;; Influence
damage ;; Damaging || damage ;; Damaging
happen ;; Presence || flood ;; Catastrophe
brought ;; Bringing || known ;; Know || Typhoon ;; Catastrophe
developed ;; Coming_to_be
intensify ;; Cause_change_of_strength
caused ;; Causation || weakened ;; Cause_change_of_strength || tracked ;; Motion
began ;; Process_start || intensify ;; Cause_change_of_strength || regained ;; Getting
weaken ;; Cause_change_of_strength
landfall ;; Self_motion || transitioning ;; Change || entered ;; Arriving || recurved ;; Motion
move ;; Motion || dissipating ;; Dispersal
affected ;; Influence
reported ;; Reporting || totaled ;; Earnings_and_losses
caused ;; Causation || landfall ;; Self_motion || resulted in ;; Causation
evacuated ;; Escaping
began ;; Process_start
reported ;; Reporting || landfall ;; Self_motion
reported ;; Reporting || totaled ;; Earnings_and_losses
prompted ;; Causation || caused ;; Causation || tracked ;; Self_motion
killed ;; Killing
flooded ;; Catastrophe
occurred ;; Presence
killed ;; Killing || spurred ;; Influence
death ;; Death || damage ;; Damaging || resulted in ;; Causation
earthquake ;; Catastrophe || occurred ;; Presence
centered ;; Placing || affected ;; Influence
registered ;; Know
earthquake ;; Catastrophe || affected ;; Influence
caused ;; Causation || damage ;; Damaging
earthquake ;; Catastrophe || considered ;; Causation || hit ;; Attack
affect ;; Influence
developed ;; Coming_to_be
strengthened ;; Cause_change_of_strength
turned ;; Becoming
made ;; Manufacturing
approach ;; Arriving || earned ;; Earnings_and_losses || nickname ;; Name_conferral
took place ;; Process_start
help ;; Assistance || goal ;; Aiming || endangered ;; Destroying || captured ;; Arrest
included ;; Cause_to_be_included || threaten ;; Hindering
aimed ;; Aiming
command ;; Control
achieved ;; Achieve || contributed ;; Cause_to_make_progress
ended ;; Process_end || came into effect ;; Change || followed ;; Motion
held ;; Defending
evolved ;; Coming_to_be
perform ;; Hold
add ;; Cause_change_of_position_on_a_scale
approach ;; Arriving
received ;; Receiving
given ;; Giving || impact ;; Influence || due ;; Causation
celebrated ;; Social_event
event ;; Social_event
established ;; Creating
hosted ;; Come_together
includes ;; Cause_to_be_included
includes ;; Cause_to_be_included
travel ;; Traveling
race ;; Competition
race ;; Competition || opens ;; Process_start
impact ;; Influence
storm ;; Catastrophe
formed ;; Coming_to_be || approached ;; Arriving || reached ;; Arriving
peaking ;; Self_motion || produced ;; Creating
caused ;; Causation || damage ;; Damaging || flooding ;; Motion || killed ;; Killing
forced ;; Hostile_encounter
turned ;; Becoming || impact ;; Influence
moved ;; Motion || storm ;; Catastrophe || storms ;; Catastrophe || affect ;; Influence
occurred ;; Coming_to_be || impact ;; Influence || final ;; Process_end || dissipated ;; Dispersal
observed ;; Perception_active
formed ;; Coming_to_be || hurricane ;; Catastrophe || peaked ;; Motion
existence ;; Presence || followed ;; Self_motion
existed ;; Presence || designated ;; Choosing
affected ;; Influence || bringing ;; Bringing
affected ;; Influence
died ;; Death || state ;; Statement || floods ;; Catastrophe || flooding ;; Catastrophe
floods ;; Catastrophe || occurred ;; Presence
used ;; Using || came to ;; Motion || due ;; Causation || flooding ;; Catastrophe || standstill ;; Process_end
lost ;; Earnings_and_losses || walked ;; Self_motion || stranded ;; Motion
caused ;; Causation || recorded ;; Recording || floods ;; Catastrophe || lashed ;; Attack
received ;; Receiving
record ;; Recording || suggest ;; Statement || reports ;; Reporting
record ;; Recording
affected ;; Influence
affected ;; Influence || floods ;; Catastrophe
caused ;; Causation || attributed ;; Causation || hit ;; Attack || floods ;; Catastrophe || Typhoon ;; Catastrophe
affected ;; Influence || flood ;; Catastrophe
flooded ;; Catastrophe || typhoon ;; Catastrophe
starting ;; Process_start || hit ;; Attack
floods ;; Catastrophe || rainfall ;; Catastrophe || experienced ;; Perception_active || resulted in ;; Causation
recorded ;; Recording
began ;; Process_start || caused ;; Causation || flooding ;; Catastrophe
flooding ;; Catastrophe
died ;; Death
caused ;; Causation || flooding ;; Catastrophe || rainfall ;; Catastrophe || experienced ;; Perception_active || beginning ;; Process_start
cyclone ;; Catastrophe
developed ;; Coming_to_be
intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe || storm ;; Catastrophe
hurricane ;; Catastrophe || weakening ;; Cause_change_of_strength
storm ;; Catastrophe || weakened ;; Cause_change_of_strength
began ;; Process_start || re-strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || hurricane ;; Catastrophe
dissipated ;; Removing
became ;; Becoming || Hurricane ;; Catastrophe
impact ;; Influence || passing ;; Motion_directional || resulting in ;; Causation || damage ;; Damaging || Hurricane ;; Catastrophe
hurricane ;; Catastrophe || originated ;; Coming_to_be
existence ;; Presence || storm ;; Catastrophe || Moving ;; Motion || remained ;; Presence
intensified ;; Cause_change_of_strength || reach ;; Achieve || coming ;; Arriving
hurricane ;; Catastrophe || strengthened ;; Cause_change_of_strength || attained ;; Getting
made ;; Action
hurricane ;; Catastrophe || weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
impacts ;; Influence || landfall ;; Motion || documented ;; Recording
storm ;; Catastrophe || approached ;; Arriving || took place ;; Process_start || evacuations ;; Escaping || evacuation ;; Escaping
landfall ;; Motion || damage ;; Damaging || caused ;; Causation
storm ;; Catastrophe || damaged ;; Damaging || inundated ;; Motion || surge ;; Motion_directional
storm ;; Catastrophe || wreaked ;; Destroying
uprooted ;; Motion || leveled ;; Destroying
affected ;; Influence || losses ;; Earnings_and_losses
hurricane ;; Catastrophe || associated ;; Collaboration || forward ;; Sending
storm ;; Catastrophe || damages ;; Damaging || caused ;; Causation || deaths ;; Death
triggered ;; Causation || described ;; Statement
affected ;; Influence
placed ;; Placing || rose ;; Motion_directional || declared ;; Expressing_publicly || activated ;; Causation
confirmed ;; Check || displaced ;; Cause_change_of_position_on_a_scale
response ;; Response || help ;; Assistance || deployed ;; Arranging
damages ;; Damaging || made ;; Manufacturing || exceeded ;; Expansion
aided ;; Assistance || assisted ;; Assistance || Receding ;; Expansion || affected ;; Influence || complete ;; Process_end
took place ;; Process_start
included ;; Cause_to_be_included
occurred ;; Presence
raise ;; Influence
influenced ;; Influence
spread ;; Dispersal || took place ;; Process_start
impacted ;; Influence
became ;; Becoming || organized ;; Arranging || developed ;; Coming_to_be
strengthened ;; Cause_change_of_strength || forming ;; Coming_to_be
occurred ;; Coming_to_be || reached ;; Arriving || strength ;; Cause_change_of_strength || developed ;; Coming_to_be
became ;; Becoming || hurricane ;; Catastrophe
made ;; Manufacturing || dissipated ;; Dispersal
caused ;; Causation || flooding ;; Motion
reported ;; Reporting
occurred ;; Coming_to_be
killed ;; Killing || attacked ;; Attack || injured ;; Bodily_harm || using ;; Using || getting ;; Getting || stormed ;; Attack
killing ;; Killing || due ;; Causation || open ;; Openness || leaving ;; Departing || escape ;; Escaping || die ;; Death || deaths ;; Death
charged ;; Committing_crime
sentenced ;; Legal_rulings || death ;; Death || acquitted ;; Committing_crime
received ;; Receiving || received ;; Receiving || received ;; Receiving || remaining ;; Presence || remaining ;; Presence
received ;; Receiving || sentenced ;; Legal_rulings || ordered ;; Arranging || death ;; Death
affected ;; Influence || shut down ;; Being_in_operation
formed ;; Coming_to_be || became ;; Becoming
remained ;; Presence
began ;; Process_start
dissipated ;; Dispersal
dropped ;; Motion_directional
received ;; Receiving
devastated ;; Destroying
dead ;; Death || wrought ;; Causation
affected ;; Influence
killed ;; Killing
cyclone ;; Catastrophe
storm ;; Catastrophe || identified ;; Check
confirmed ;; Check || storm ;; Catastrophe || monitored ;; Scrutiny || due ;; Causation || reports ;; Reporting
intensify ;; Cause_change_of_strength || reaching ;; Arriving || hurricane ;; Catastrophe || naming ;; Name_conferral
reached ;; Arriving || reaching ;; Arriving
weakened ;; Cause_change_of_strength || tracked ;; Motion || making ;; Causation
dissipating ;; Dispersal || hurricane ;; Catastrophe
damage ;; Damaging || produced ;; Creating || passed ;; Motion_directional || resulting in ;; Causation
damage ;; Damaging || reported ;; Reporting
deaths ;; Death || Damage ;; Damaging || occurred ;; Presence
damage ;; Damaging || occurred ;; Presence || producing ;; Creating || toppling ;; Change_of_leadership
affected ;; Influence || causing ;; Causation
damaged ;; Damaging || losses ;; Earnings_and_losses || suffered ;; Bodily_harm
losses ;; Earnings_and_losses || sustained ;; Statement
damage ;; Damaging || took effect ;; Change || disaster ;; Catastrophe || hurricane ;; Catastrophe
originated ;; Coming_to_be
traveled ;; Motion
strengthened ;; Cause_change_of_strength || moved ;; Motion
moved ;; Motion || caused ;; Causation || killed ;; Killing
moved ;; Motion || set up ;; Reforming_a_system
re-intensified ;; Cause_change_of_strength || turned ;; Motion
veered ;; Motion
issued ;; Creating || left ;; Departing
suffered ;; Influence
caused ;; Causation || killed ;; Killing
stabbed ;; Bodily_harm || determined ;; Deciding || death ;; Death || murder ;; Committing_crime || murder ;; Committing_crime || married ;; Forming_relationships
open ;; Openness || involved ;; Cause_to_be_included
attracted ;; Influence || received ;; Receiving
watched ;; Perception_active
unified ;; Cause_to_amalgamate || separate ;; Forming_relationships
said ;; Statement
call ;; Request || release ;; Publishing
massacre ;; Killing || killing ;; Killing
fall ;; Motion_directional || occurred ;; Coming_to_be || capture ;; Conquering
fled ;; Escaping || expelled ;; Removing || killed ;; Killing || remained ;; Presence
capture ;; Conquering || included ;; Cause_to_be_included || expulsion ;; Removing || killing ;; Killing
convicted ;; Legal_rulings || war ;; Military_operation
rebuilt ;; Building
attacked ;; Attack || getting out ;; Motion
suffered ;; Bodily_harm
rushed ;; Motion
agree ;; Agree_or_refuse_to_act || aimed ;; Aiming
rein ;; Hold || denied ;; Agree_or_refuse_to_act || saw ;; Self_motion
attended ;; Perception_active
put up ;; Placing
arrested ;; Arrest
fled ;; Escaping
sentenced ;; Legal_rulings
occurred ;; Coming_to_be || beginning ;; Process_start
started ;; Process_start || attacking ;; Attack || began ;; Process_start
becoming ;; Becoming
became ;; Becoming || conflict ;; Hostile_encounter || injured ;; Bodily_harm
marked ;; Recording || conflict ;; Hostile_encounter
arrival ;; Arriving
perpetrated ;; Committing_crime
flying ;; Motion
discovered ;; Know
boarded ;; Self_motion
convicted ;; Legal_rulings
took place ;; Process_start
carried out ;; Military_operation
died ;; Death
give ;; Giving
found ;; Know || received ;; Receiving || released ;; Releasing || brought ;; Bringing || sentenced ;; Legal_rulings
sentenced ;; Legal_rulings
found ;; Know
stated ;; Statement || prosecuted ;; Legality
apologised ;; Judgment_communication
war ;; Hostile_encounter || fighting ;; Hostile_encounter || control ;; Control || party ;; Social_event
included ;; Cause_to_be_included
controlled ;; Control || winning ;; Earnings_and_losses || elections ;; Choosing
included ;; Cause_to_be_included || oust ;; Removing
committed ;; Committing_crime
killed ;; Killing || broke out ;; Process_start
seized ;; Hold || established ;; Building
echoed ;; Response
using ;; Using || ruled ;; Control
raided ;; Attack || shipping ;; Self_motion
departed ;; Departing
picking up ;; Choosing || sank ;; Killing || encountered ;; Hostile_encounter
arriving ;; Arriving || returned ;; Arriving
murdered ;; Committing_crime
war ;; Hostile_encounter || war ;; Hostile_encounter || sentenced ;; Legal_rulings || crime ;; Committing_crime
killing ;; Killing
arrested ;; Arrest
arrested ;; Arrest
taken ;; Conquering || shot ;; Use_firearm
killings ;; Killing || ordered ;; Arranging
killings ;; Killing
killings ;; Killing || investigation ;; Scrutiny || launched ;; Motion || informed ;; Telling
arrested ;; Arrest
found ;; Know || sentenced ;; Legal_rulings
murders ;; Committing_crime
stabbing ;; Bodily_harm || killed ;; Killing
injured ;; Bodily_harm || found ;; Placing
investigations ;; Scrutiny || identified ;; Check || presented ;; Presence
arrested ;; Arrest || murders ;; Committing_crime || found ;; Know
murderer ;; Committing_crime || discovered ;; Know
final ;; Competition
final ;; Competition || played ;; Competition || contested ;; Competition
meeting ;; Come_together
appearance ;; Presence || losing ;; Competition
appearance ;; Presence
appearance ;; Presence || beginning ;; Process_start || reach ;; Becoming_a_member
scoring ;; Getting || getting ;; Self_motion
Battle ;; Hostile_encounter || conflict ;; Hostile_encounter || campaign ;; Military_operation || Raid ;; Attack || War ;; Military_operation
destroy ;; Destroying || manufacturing ;; Manufacturing || ordered ;; Arranging || Confederate ;; Collaboration
crowded ;; Come_together
laid waste ;; Placing || took ;; Conquering
battle ;; Hostile_encounter || battle ;; Hostile_encounter || occurred ;; Coming_to_be || Battle ;; Hostile_encounter || classified ;; Check || claim ;; Statement || War ;; Military_operation || vanquished ;; Change_of_leadership
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter
held ;; Hold
hosted ;; Social_event
hosted ;; Social_event
sent ;; Sending || declined ;; Agree_or_refuse_to_act || games ;; Competition || participate ;; Participation || due ;; Causation
renamed ;; Name_conferral
closed ;; Process_end || games ;; Competition || opened ;; Openness
final ;; Process_end || host ;; Come_together
met ;; Come_together
game ;; Competition
met ;; Come_together || match ;; Competition
ended ;; Process_end || match ;; Competition
beaten ;; Competition || win ;; Getting || competition ;; Competition
scored ;; Getting
match ;; Competition || Final ;; Competition || beaten ;; Competition
attack ;; Attack || carried out ;; Attack
launched ;; Attack
assassinate ;; Killing || war ;; Hostile_encounter || discuss ;; Communication || meeting ;; Come_together
exploded ;; Attack
received ;; Receiving || injuries ;; Bodily_harm || hurt ;; Bodily_harm
landed ;; Motion
match ;; Competition || took place ;; Hold
competition ;; Competition || record ;; Recording || causing ;; Causation || reached ;; Achieve || lose ;; Earnings_and_losses || injury ;; Bodily_harm
absences ;; Presence || given ;; Giving || sharing ;; Communication || earned ;; Earnings_and_losses || forces ;; Causation || close ;; Openness || match ;; Competition || met ;; Come_together
ended ;; Process_end || match ;; Competition || brought ;; Bringing || loss ;; Earnings_and_losses || scored ;; Getting || led ;; Causation
ending ;; Process_end || match ;; Competition || scored ;; Getting
selected ;; Choosing || match ;; Competition
marked ;; Recording || records ;; Recording
marked ;; Recording
become ;; Becoming || scoring ;; Getting || reach ;; Arriving || overtake ;; Getting
record ;; Recording || scored ;; Getting || surpassed ;; Cause_change_of_position_on_a_scale
broke ;; Bodily_harm || broke ;; Destroying || equalled ;; Cause_change_of_position_on_a_scale || loss ;; Earnings_and_losses || lost ;; Earnings_and_losses || match ;; Competition || loss ;; Earnings_and_losses || record ;; Recording || matches ;; Competition || conceded ;; Earnings_and_losses
match ;; Competition || described ;; Statement
dubbed ;; Name_conferral || lost ;; Earnings_and_losses || evoking ;; Know
defeating ;; Conquering || lost ;; Earnings_and_losses
injured ;; Bodily_harm || made ;; Action || landing ;; Self_motion
injuries ;; Bodily_harm || included ;; Cause_to_be_included
met ;; Come_together || service ;; Supply || airlifted ;; Bringing || attending ;; Participation
injuries ;; Bodily_harm || injuries ;; Bodily_harm || suffered ;; Bodily_harm || suffered ;; Bodily_harm
found ;; Know
commemorate ;; Social_event || held ;; Hold
came ;; Self_motion || held ;; Hold || formed ;; Coming_to_be || promote ;; Cause_to_make_progress
attended ;; Participation
tickets ;; Preventing_or_letting
hosts ;; Come_together
made ;; Manufacturing || speech ;; Communication
finished ;; Process_end
joined ;; Participation
competition ;; Competition
finals ;; Process_end || hosting ;; Come_together || tournament ;; Competition || played ;; Competition
started ;; Process_start || ended ;; Process_end || tournament ;; Competition
finals ;; Process_end || defeating ;; Getting
finals ;; Process_end || coming ;; Motion || contested ;; Competition || finals ;; Competition
scoring ;; Getting || tournament ;; Competition
hosted ;; Social_event
end ;; Process_end || hosts ;; Come_together
starting ;; Process_start || ending ;; Process_end
earthquakes ;; Catastrophe || earthquakes ;; Catastrophe || occurred ;; Presence
mainshock ;; Catastrophe || aftershocks ;; Catastrophe || struck ;; Attack
associated ;; Collaboration || converge ;; Come_together
shocks ;; Catastrophe || damaged ;; Damaging || destroyed ;; Destroying || injured ;; Bodily_harm || loss ;; Earnings_and_losses || fire ;; Catastrophe || consumed ;; Expend_resource
recorded ;; Recording
took place ;; Process_start || landslides ;; Catastrophe
discovered ;; Know || uplift ;; Motion
caused ;; Causation || tsunami ;; Catastrophe || reached ;; Arriving
tsunami ;; Catastrophe || reached ;; Arriving
associated ;; Collaboration || affected ;; Influence || ruptured ;; Destroying || associated ;; Collaboration
controlled ;; Control || Attacked ;; Attack || repulsed ;; Defending || brought ;; Bringing
attacked ;; Attack || attacked ;; Attack
captured ;; Conquering || gained ;; Getting || met ;; Come_together
taken ;; Conquering || repulsed ;; Defending
captured ;; Conquering
association ;; Collaboration || Cup ;; Competition || Cup ;; Competition || championship ;; Competition
organized ;; Arranging || took place ;; Process_start
played ;; Participation || matches ;; Competition
Cup ;; Competition || participate ;; Participation
given ;; Giving || due ;; Causation || Cup ;; Competition
enter ;; Becoming_a_member || chosen ;; Choosing || hosts ;; Come_together || tournament ;; Competition || tournament ;; Competition || qualified ;; Becoming_a_member
obtained ;; Getting || played ;; Participation
Cup ;; Competition || match ;; Competition || took place ;; Hold
ended ;; Process_end || match ;; Competition || produced ;; Creating
scored ;; Getting
competition ;; Competition || named ;; Name_conferral
earned ;; Earnings_and_losses || became ;; Becoming || defeating ;; Conquering || wins ;; Getting || Cup ;; Competition || win ;; Getting
tournament ;; Competition || took place ;; Hold
tournament ;; Competition
defending ;; Defending || lost ;; Earnings_and_losses
defend ;; Defending || retired ;; Process_end
final ;; Process_end || record ;; Recording || met ;; Come_together || defeating ;; Conquering
record ;; Recording || defeating ;; Conquering || met ;; Come_together
teamed up ;; Cause_to_amalgamate || win ;; Getting
teamed up ;; Cause_to_amalgamate || win ;; Getting
teamed up ;; Cause_to_amalgamate || win ;; Getting
held ;; Defending
gaming ;; Competition || meet ;; Come_together
given ;; Giving
conference ;; Social_event
conference ;; Social_event || held ;; Defending
held ;; Defending
Final ;; Competition || defeat ;; Hostile_encounter
match ;; Competition || played ;; Competition
premiership ;; Competition || premiership ;; Competition
win ;; Getting
Final ;; Competition || beaten ;; Hostile_encounter
final ;; Competition || final ;; Competition || played ;; Competition
played ;; Competition
entered ;; Becoming_a_member || win ;; Getting || met ;; Come_together || final ;; Competition || Final ;; Competition || beaten ;; Competition
determine ;; Deciding || played ;; Competition
beat ;; Competition
became ;; Becoming || win ;; Getting
competition ;; Competition || met ;; Come_together
began ;; Process_start || competition ;; Competition
scoring ;; Getting
came ;; Motion || came ;; Motion
denied ;; Agree_or_refuse_to_act || ruled ;; Control || put ;; Placing
War ;; Hostile_encounter || contest ;; Competition
began ;; Process_start || exiled ;; Removing
assembled ;; Come_together
took over ;; Conquering || capturing ;; Conquering
became ;; Becoming || resulted in ;; Causation
inspired ;; Influence
organised ;; Arranging || festival ;; Social_event || Festival ;; Social_event
held ;; Hold
started ;; Process_start || Festival ;; Social_event
Festival ;; Social_event
Festival ;; Social_event || hosted ;; Come_together
dissolved ;; Dispersal || early ;; Process_start || partnership ;; Collaboration
began ;; Process_start || Expedition ;; Attack || campaign ;; Military_operation || War ;; Military_operation
received ;; Receiving || organize ;; Arranging || conduct ;; Action || forces ;; Causation || expedition ;; Attack || stop ;; Preventing_or_letting || build-up ;; Cause_to_amalgamate
assembled ;; Come_together
secured ;; Getting
began ;; Process_start || final ;; Process_end || consolidated ;; Cause_change_of_strength
withdrawal ;; Escaping || Battle ;; Hostile_encounter || forces ;; Causation
held ;; Hold
formed ;; Coming_to_be
served ;; Giving
invited ;; Participation || converged ;; Come_together
took place ;; Process_start
encouraged ;; Influence
arose ;; Coming_to_be
issued ;; Statement
held ;; Hold
contested ;; Competition
played ;; Participation
began ;; Process_start || winning ;; Getting
participate ;; Participation || enter ;; Participation
winning ;; Getting
met ;; Come_together || play ;; Participation
received ;; Receiving
final ;; Process_end
developed ;; Coming_to_be || named ;; Name_conferral
moved ;; Motion || intensified ;; Cause_change_of_strength || intensifying ;; Cause_change_of_strength
intensified ;; Cause_change_of_strength || prevented ;; Preventing_or_letting || strengthening ;; Cause_change_of_strength
approaching ;; Arriving || turned ;; Change || weakening ;; Cause_change_of_strength
downgraded ;; Cause_change_of_position_on_a_scale
hurricane ;; Catastrophe || issuing ;; Creating || due ;; Causation || met ;; Come_together
moved ;; Motion || redeveloped ;; Recovering || dissipating ;; Dispersal
reports ;; Reporting || damage ;; Damaging || dropped ;; Motion_directional
combined ;; Cause_to_amalgamate || produce ;; Creating
Battle ;; Hostile_encounter || Massacre ;; Killing || took place ;; Process_start
raids ;; Attack || attacked ;; Attack || gathered ;; Come_together
led ;; Causation
killed ;; Killing || died ;; Death
reported ;; Reporting || reported ;; Reporting
played ;; Competition || took place ;; Hold
match ;; Competition || representing ;; Presence || played ;; Competition
representing ;; Presence || scheduled ;; Arranging || compete ;; Competition
competition ;; Competition || leaving ;; Departing || play ;; Competition
selected ;; Choosing
touring ;; Traveling || comprised ;; Cause_to_be_included
commenced ;; Process_start
batted ;; Motion || scored ;; Getting
scored ;; Getting || setting ;; Placing || hosts ;; Come_together
match ;; Competition || win ;; Getting || match ;; Competition
awarded ;; Award
match ;; Competition || contest ;; Social_event || recognised ;; Know
played ;; Competition || played ;; Competition
contest ;; Competition || causing ;; Causation || met ;; Come_together || thrown ;; Self_motion
managed ;; Control || ran ;; Motion || set ;; Placing
scored ;; Getting
passed ;; Motion_directional
scoring ;; Getting
emerged ;; Coming_to_be || hitting ;; Attack || end ;; Process_end || meet ;; Come_together
celebrated ;; Social_event || governed ;; Control || Games ;; Competition || multi-sport ;; Social_event
began ;; Process_start || held ;; Hold
hosted ;; Social_event
held ;; Hold
organized ;; Arranging || Organizing ;; Arranging
marking ;; Recording || hosted ;; Come_together || competing ;; Competition || multi-sport ;; Social_event || hosted ;; Social_event
competitors ;; Competition || record ;; Recording
included ;; Cause_to_be_included || contested ;; Competition || contested ;; Competition
debut ;; Process_start || competitions ;; Competition || made ;; Action
reported ;; Reporting || organizing ;; Arranging || came ;; Motion
organized ;; Arranging
ongoing ;; Hostile_encounter || hosting ;; Come_together || hosted ;; Social_event || rescinded ;; Removing
hosted ;; Social_event
defeating ;; Getting
finished ;; Process_end || beating ;; Getting
qualified ;; Getting
win ;; Getting || eliminated ;; Removing || failing ;; Earnings_and_losses
cyclone ;; Catastrophe
formed ;; Coming_to_be || hurricane ;; Catastrophe || storm ;; Catastrophe
intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe || storm ;; Catastrophe || Moving ;; Motion || attaining ;; Getting
became ;; Becoming || began ;; Process_start || intensify ;; Cause_change_of_strength || development ;; Coming_to_be
hurricane ;; Catastrophe || reached ;; Arriving || attained ;; Getting || intensity ;; Cause_change_of_position_on_a_scale || intensity ;; Cause_change_of_position_on_a_scale
hurricane ;; Catastrophe || came ;; Self_motion || recurving ;; Self_motion
weakened ;; Cause_change_of_strength || transitioned ;; Change || passed ;; Motion_directional || cyclone ;; Catastrophe || Accelerating ;; Cause_change_of_position_on_a_scale
traversed ;; Motion_directional || dissipating ;; Dispersal
caused ;; Causation || damage ;; Damaging || close ;; Openness || landfall ;; Arriving
widespread ;; Dispersal || resulted in ;; Causation || cutting ;; Bodily_harm
setting ;; Placing || record ;; Recording || reported ;; Reporting || reported ;; Reporting
reaching ;; Arriving || widespread ;; Dispersal
damages ;; Damaging || death ;; Death || reached ;; Arriving
produced ;; Manufacturing || impacting ;; Influence
storm ;; Catastrophe
damage ;; Damaging || cut ;; Bodily_harm
loss ;; Earnings_and_losses || landfall ;; Arriving || reported ;; Reporting
Damage ;; Damaging
storm ;; Catastrophe || damages ;; Damaging || associated ;; Collaboration
participated ;; Participation || games ;; Competition
hosted ;; Social_event
host ;; Social_event
opened ;; Openness || final ;; Process_end || led ;; Getting
games ;; Competition || broken ;; Destroying
games ;; Competition
opened ;; Openness
aims ;; Aiming || improving ;; Cause_to_make_progress
end ;; Process_end
concert ;; Social_event || Evening ;; Social_event
began ;; Process_start
began ;; Process_start || opening ;; Openness
went through ;; Motion
promoted ;; Cause_to_make_progress
promoted ;; Cause_to_make_progress
Festival ;; Social_event || Festival ;; Social_event || Festival ;; Social_event || festival ;; Social_event
perform ;; Hold || plays ;; Competition || festival ;; Social_event
plays ;; Competition || performed ;; Hold || festival ;; Social_event || festival ;; Social_event || festival ;; Social_event
festival ;; Social_event || plays ;; Competition
presented ;; Presence || opening ;; Openness || recognize ;; Know
Games ;; Competition || held ;; Hold
hosted ;; Social_event
held ;; Hold || commenced ;; Process_start || games ;; Competition
participated ;; Participation
opened ;; Openness || closed ;; Process_end
led ;; Getting
broken ;; Destroying
games ;; Competition || rising ;; Cause_change_of_position_on_a_scale
revived ;; Recovering || take place ;; Process_start
held ;; Hold
attended ;; Participation
open ;; Openness
camping ;; Social_event
party ;; Social_event || took place ;; Process_start || festival ;; Social_event
recorded ;; Recording || established ;; Creating
takes place ;; Process_start
opened ;; Openness || grown ;; Cause_to_make_progress || start ;; Process_start
presented ;; Presence || organised ;; Arranging || called ;; Name_conferral
involved ;; Cause_to_be_included
occurred ;; Coming_to_be
killed ;; Killing
fled ;; Escaping || headed ;; Self_motion
returned ;; Arriving
burned ;; Damaging
decided ;; Deciding || change ;; Change || become ;; Becoming
bring in ;; Bringing || developed ;; Coming_to_be
opening ;; Openness || expanding ;; Expansion || returned ;; Arriving
given ;; Giving || help ;; Assistance || seeing ;; Perception_active
Festival ;; Social_event
festival ;; Social_event || festival ;; Social_event
grown ;; Expansion || festival ;; Social_event
blooming ;; Self_motion
takes place ;; Process_start || festival ;; Social_event
open ;; Openness
festival ;; Social_event || start ;; Process_start
begins ;; Process_start
festival ;; Social_event || dancing ;; Self_motion
includes ;; Cause_to_be_included || festival ;; Social_event || utilize ;; Using
regatta ;; Social_event || held ;; Hold
formed ;; Coming_to_be
regatta ;; Competition || preceded ;; Process_start
held over ;; Process_start || opening ;; Openness
competitors ;; Competition || race ;; Competition || regatta ;; Competition || held ;; Hold
providing ;; Supply
compete ;; Competition || determine ;; Deciding
celebrated ;; Social_event || commemorate ;; Social_event
occurred ;; Coming_to_be || shootings ;; Use_firearm
killed ;; Killing || wounded ;; Bodily_harm
killed ;; Killing || wounded ;; Bodily_harm || freedom ;; Change_of_leadership || shooting ;; Use_firearm || took place ;; Process_start
attack ;; Attack || attended ;; Participation || opened ;; Process_start || took place ;; Process_start
described ;; Statement
shooting ;; Use_firearm || took place ;; Process_start
killed ;; Killing || wounded ;; Bodily_harm
killed ;; Killing || shot ;; Use_firearm || surveillance ;; Scrutiny || building ;; Building || enter ;; Arriving || tracking ;; Motion || opened ;; Openness
identified ;; Check || attacks ;; Attack
held ;; Hold
Started ;; Process_start || considered ;; Deciding
form ;; Creating || host ;; Social_event || played ;; Participation || opening ;; Openness
opera ;; Social_event || comprises ;; Cause_to_be_included
presented ;; Presence || held ;; Hold
generated ;; Creating || attracts ;; Hold
playing ;; Competition
promoted ;; Cause_to_make_progress
go off ;; Process_end || explained ;; Statement
tour ;; Traveling
included ;; Cause_to_be_included
opened ;; Openness
performed ;; Hold || appeared ;; Presence || selected ;; Choosing
included ;; Cause_to_be_included
occurred ;; Coming_to_be || recovering ;; Recovering || cancelled ;; Removing
make ;; Manufacturing
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || centered ;; Placing
moving ;; Motion || intensified ;; Cause_change_of_strength || storm ;; Catastrophe
reached ;; Arriving || hurricane ;; Catastrophe || hurricane ;; Catastrophe
made ;; Manufacturing || Hurricane ;; Catastrophe || weakened ;; Cause_change_of_strength || hurricane ;; Catastrophe || hurricane ;; Catastrophe
weakened ;; Cause_change_of_strength || re-emerged ;; Coming_to_be || open ;; Openness || dissipated ;; Dispersal
storm ;; Catastrophe || produced ;; Creating
hurricane ;; Catastrophe || deluged ;; Motion || resulting in ;; Causation
left ;; Departing
control ;; Control || damaged ;; Damaging
destroyed ;; Destroying
caused ;; Causation || injured ;; Bodily_harm || damage ;; Damaging
storm ;; Catastrophe || flooding ;; Catastrophe
developed ;; Coming_to_be
developing ;; Coming_to_be || storm ;; Catastrophe || strengthened ;; Cause_change_of_strength || upgraded ;; Cause_to_make_progress
caused ;; Causation || storm ;; Catastrophe || halted ;; Process_end
intensified ;; Cause_change_of_strength
landfall ;; Catastrophe
weakened ;; Cause_change_of_strength || degenerated ;; Change || downgraded ;; Cause_change_of_position_on_a_scale
led to ;; Causation
death ;; Death || occurred ;; Presence
storm ;; Catastrophe || flooding ;; Catastrophe
damaged ;; Damaging
storm ;; Catastrophe || flooding ;; Catastrophe
damage ;; Damaging || closed ;; Openness
flooding ;; Motion || occurred ;; Presence
damaged ;; Damaging || downed ;; Self_motion
storm ;; Catastrophe || damage ;; Damaging
competition ;; Competition
played ;; Competition || opening ;; Openness || match ;; Competition
played ;; Competition || matches ;; Competition
match ;; Competition
match ;; Competition || final ;; Competition
hosted ;; Social_event
named ;; Name_conferral || Tournament ;; Competition || tournament ;; Competition
named ;; Name_conferral || Tournament ;; Competition
played ;; Participation
open ;; Openness
playoff ;; Competition || Open ;; Openness
became ;; Becoming || win ;; Earnings_and_losses
championship ;; Competition || played ;; Participation
moved ;; Self_motion
win ;; Earnings_and_losses || championships ;; Competition || captured ;; Conquering
happened ;; Presence
happened ;; Presence
movement ;; Social_event
defined ;; Statement || described ;; Statement
endorsed ;; Agree_or_refuse_to_act
prevented ;; Preventing_or_letting
set up ;; Building
granted ;; Giving || remained ;; Presence
remove ;; Removing || began ;; Process_start || refused ;; Agree_or_refuse_to_act
began ;; Process_start || support ;; Supporting
established ;; Creating
opened ;; Openness
Named ;; Name_conferral || evicted ;; Removing || occupiers ;; Attack
established ;; Creating
objected ;; Agree_or_refuse_to_act || reached ;; Achieve
evicted ;; Removing
cleared ;; Removing
began ;; Process_start || ended ;; Process_end || concert ;; Social_event || support ;; Supporting
released ;; Publishing || announced ;; Expressing_publicly || announce ;; Expressing_publicly || called ;; Name_conferral || statement ;; Statement
open ;; Openness || finish ;; Process_end || announcement ;; Expressing_publicly
travel ;; Traveling || selected ;; Choosing || planning ;; Arranging || shows ;; Social_event
tour ;; Traveling || performing ;; Hold || led to ;; Causation
tour ;; Traveling || tour ;; Traveling
supporting ;; Supporting
progressed ;; Cause_to_make_progress || Starting ;; Process_start
included ;; Cause_to_be_included || opening ;; Openness || celebrate ;; Social_event
tour ;; Traveling
show ;; Social_event
ended ;; Process_end || shows ;; Social_event
open ;; Openness || open ;; Openness || event ;; Social_event
included ;; Cause_to_be_included
open ;; Openness || known ;; Know || tours ;; Traveling
event ;; Social_event
organised ;; Arranging || event ;; Social_event
open ;; Openness || event ;; Social_event
played ;; Competition
took place ;; Process_start
changed ;; Change
reduce ;; Cause_change_of_position_on_a_scale
faced ;; Perception_active || open ;; Openness
change ;; Change
defending ;; Defending || losing ;; Earnings_and_losses || losing ;; Earnings_and_losses
win ;; Getting || defeating ;; Conquering || defeated ;; Conquering
score ;; Getting
championship ;; Competition
played ;; Competition
reduced ;; Cause_change_of_position_on_a_scale
opened ;; Openness || hosted ;; Social_event
returned ;; Arriving
tournament ;; Competition || challenge ;; Competition || hosted ;; Social_event
challenge ;; Competition || challenge ;; Competition || tournament ;; Competition
made ;; Action
open ;; Openness || tournament ;; Competition
challenge ;; Competition
organised ;; Arranging || competition ;; Competition
occurred ;; Coming_to_be || Wreck ;; Catastrophe
killing ;; Killing || injuring ;; Bodily_harm || collided ;; Self_motion
became ;; Becoming || catastrophe ;; Catastrophe
Growing ;; Expansion
opened ;; Openness
ship ;; Sending
becoming ;; Becoming || occurred ;; Coming_to_be
hurricane ;; Catastrophe || Hurricane ;; Catastrophe
storm ;; Catastrophe || named ;; Name_conferral || named ;; Name_conferral || hurricane ;; Catastrophe || hurricane ;; Catastrophe || storm ;; Catastrophe || make ;; Creating
reorganize ;; Cause_to_amalgamate || strengthening ;; Cause_change_of_strength || performing ;; Action
strengthening ;; Cause_change_of_strength || passing ;; Motion_directional || hurricane ;; Catastrophe || headed ;; Self_motion
made ;; Creating || struck ;; Attack
turned ;; Becoming || brought ;; Bringing || Building ;; Building || open ;; Openness
blamed ;; Judgment_communication || deaths ;; Death || floods ;; Catastrophe || mudslides ;; Catastrophe
caused ;; Causation || deaths ;; Death || bringing ;; Bringing || deaths ;; Death || known ;; Know || storm ;; Catastrophe
damage ;; Damaging
Cup ;; Competition || Cup ;; Competition || tournament ;; Competition
participated ;; Participation
Cup ;; Competition || tournament ;; Competition
playing ;; Competition || scored ;; Competition
tournament ;; Competition || semi-finals ;; Competition || qualified ;; Becoming_a_member
matches ;; Competition || played ;; Competition || played ;; Competition
semi-finals ;; Competition || qualified ;; Becoming_a_member
matches ;; Competition || consisted ;; Cause_to_be_included || played ;; Competition
played ;; Competition
tournament ;; Competition || Championship ;; Competition
Championship ;; Competition
Championships ;; Competition || stroke ;; Hold
tournament ;; Competition || completed ;; Process_end
played ;; Competition || pairing ;; Hostile_encounter
Tournament ;; Competition || hold ;; Hold
win ;; Getting || joining ;; Becoming_a_member || expanded ;; Expansion
finished ;; Process_end
insurrection ;; Hostile_encounter
aimed ;; Aiming || control ;; Control
began ;; Process_start
inflicted ;; Bodily_harm
found ;; Know || suffered ;; Bodily_harm || casualties ;; Catastrophe || retreat ;; Escaping || broke ;; Destroying
uprising ;; Change_of_leadership || laying ;; Placing
joined ;; Becoming_a_member || uprising ;; Change_of_leadership || support ;; Supporting || stationed ;; Placing
formed ;; Coming_to_be
spread ;; Dispersal || fighting ;; Hostile_encounter
retreat ;; Escaping
resisted ;; Defending
fought ;; Hostile_encounter
travel ;; Motion
forced ;; Conquering || sailed ;; Self_motion || join ;; Becoming_a_member || intercepted ;; Preventing_or_letting
attack ;; Attack || destroyed ;; Destroying || slipping ;; Motion
reinforced ;; Cause_change_of_strength || escaping ;; Escaping
left ;; Departing || sailing ;; Self_motion
sighted ;; Know
attacked ;; Attack || approach ;; Arriving || reinforced ;; Cause_change_of_strength
attack ;; Attack || made ;; Creating || on fire ;; Damaging || set ;; Placing
determine ;; Deciding || Final ;; Competition || Final ;; Competition || played ;; Action
contested ;; Competition || finished ;; Process_end || match ;; Competition
match ;; Competition || defeated ;; Conquering
become ;; Becoming || win ;; Getting
achieve ;; Achieve || became ;; Becoming
ended ;; Process_end || finishing ;; Process_end || Final ;; Competition
match ;; Competition || match ;; Competition || win ;; Getting
earned ;; Earnings_and_losses || Final ;; Process_end
competition ;; Competition || match ;; Competition || qualified ;; Becoming_a_member
contested ;; Competition
determine ;; Deciding || staged ;; Hold
marking ;; Recording
qualified ;; Becoming_a_member || coming ;; Arriving
breaking ;; Removing
broken ;; Destroying
Championship ;; Competition || Championship ;; Competition || organised ;; Arranging || championship ;; Competition
tournament ;; Competition || held ;; Hold
tournament ;; Competition || compete ;; Competition
renamed ;; Name_conferral || tournament ;; Competition || Championship ;; Competition || Championship ;; Competition
Championship ;; Competition || tournament ;; Competition || acted ;; Using || replacing ;; Change || qualifiers ;; Preventing_or_letting
tournament ;; Competition || qualified ;; Becoming_a_member || tournament ;; Competition
tournament ;; Competition || final ;; Process_end
qualified ;; Becoming_a_member || Olympics ;; Competition
consolidated ;; Cause_change_of_strength
began ;; Process_start || became ;; Becoming || completed ;; Process_end
promoted ;; Cause_to_make_progress || suffered ;; Bodily_harm || designates ;; Choosing
join ;; Becoming_a_member
include ;; Cause_to_be_included || completion ;; Process_end || extended ;; Expansion
Final ;; Competition || Final ;; Competition || match ;; Competition
competition ;; Competition || teams ;; Cause_to_amalgamate || season ;; Process_start
competition ;; Competition || match ;; Competition || final ;; Competition || United ;; Becoming_a_member || held ;; Hold || contested ;; Competition || looking ;; Action || appearing ;; Presence
needed ;; Self_motion || club ;; Becoming_a_member || reach ;; Achieve || progress ;; Cause_to_make_progress || final ;; Competition || final ;; Competition
scoring ;; Getting || saw ;; Motion_directional || final ;; Competition || progress ;; Cause_to_make_progress || games ;; Competition || process ;; Participation || concede ;; Earnings_and_losses
included ;; Cause_to_be_included || final ;; Competition
scored ;; Getting || conceded ;; Earnings_and_losses || defeat ;; Conquering
included ;; Cause_to_be_included || defeat ;; Conquering || overcoming ;; Conquering || encounter ;; Hostile_encounter
finished ;; Process_end || went to ;; Change || match ;; Competition || final ;; Competition
game ;; Competition || following ;; Action || score ;; Getting
match ;; Competition || Final ;; Competition || took place ;; Process_start
competition ;; Competition || match ;; Competition || final ;; Competition
appearing ;; Presence || reach ;; Arriving || final ;; Competition
appearing ;; Presence || final ;; Competition
entering ;; Becoming_a_member || match ;; Competition || qualified ;; Becoming_a_member
competition ;; Competition || participation ;; Participation
organized ;; Arranging
join ;; Becoming_a_member || invited ;; Request
invited ;; Request
decided ;; Deciding
increasing ;; Cause_change_of_position_on_a_scale || sparked ;; Causation
targeted ;; Aiming || backing ;; Supporting || use ;; Using
appeared ;; Presence || tournament ;; Competition || tournament ;; Competition || qualified ;; Becoming_a_member
defended ;; Defending || final ;; Competition
employ ;; Using || competition ;; Competition || tournament ;; Competition
tournament ;; Competition
began ;; Process_start || final ;; Process_end || concluded ;; Process_end || tournament ;; Competition
qualify ;; Participation || tournament ;; Competition || moved ;; Change
affected ;; Influence || gives ;; Giving
featuring ;; Presence || tournament ;; Competition || tournament ;; Competition
qualified ;; Becoming_a_member || tournament ;; Competition
final ;; Process_end || defeated ;; Conquering
came ;; Getting || came ;; Getting
tournament ;; Competition || Championship ;; Competition || Championship ;; Competition
championships ;; Competition || players ;; Participation
tournament ;; Competition || took place ;; Process_start
hosted ;; Social_event || tournament ;; Competition
tournament ;; Competition || Championship ;; Competition || matches ;; Competition || matches ;; Competition
tournament ;; Competition || knocked out ;; Competition || attributed ;; Causation
release ;; Releasing || players ;; Participation || clubs ;; Becoming_a_member
change ;; Change || announced ;; Expressing_publicly || tournament ;; Competition
renamed ;; Name_conferral || organised ;; Arranging || League ;; Competition || tournament ;; Competition || Cup ;; Competition || League ;; Competition
quarter-finals ;; Competition || eliminated ;; Preventing_or_letting
played ;; Competition || League ;; Competition || Final ;; Competition
faced ;; Hostile_encounter || tournament ;; Competition || clubs ;; Presence || final ;; Competition
win ;; Getting || final ;; Competition || defeated ;; Preventing_or_letting
qualified ;; Becoming_a_member || earned ;; Earnings_and_losses || play ;; Competition || League ;; Competition || Cup ;; Competition || Cup ;; Competition || League ;; Competition || Champions ;; Competition || Cup ;; Competition
Championship ;; Competition || Championship ;; Competition
organized ;; Arranging || event ;; Competition || supervision ;; Scrutiny || took place ;; Process_start
participated ;; Participation || tournament ;; Competition
defended ;; Defending || game ;; Competition
finished ;; Process_end
qualified ;; Becoming_a_member || Championship ;; Competition
fought ;; Hostile_encounter
battle ;; Hostile_encounter
became ;; Becoming
identify ;; Check
liberated ;; Change_of_leadership
founded ;; Creating || obtained ;; Getting
stretched ;; Expansion
inflicted ;; Bodily_harm
death ;; Death
organized ;; Arranging || realized ;; Know
recruited ;; Becoming_a_member
broke ;; Destroying || destroyed ;; Destroying || came ;; Motion
killed ;; Killing
estimated ;; Cause_change_of_position_on_a_scale || lost ;; Death || lost ;; Death
sent ;; Sending
founded ;; Creating
moved ;; Motion
killed ;; Killing
offensive ;; Military_operation || offensive ;; Military_operation || offensive ;; Military_operation || launched ;; Process_start || called ;; Name_conferral || called ;; Name_conferral
taking over ;; Military_operation || offensive ;; Military_operation || stated ;; Statement
stated ;; Statement || force ;; Request || redeploy ;; Arranging || relieve ;; Cause_change_of_position_on_a_scale
sent ;; Sending || bolster ;; Cause_change_of_strength
joined ;; Becoming_a_member || offensive ;; Military_operation
offensive ;; Military_operation || losing ;; Earnings_and_losses || fighting ;; Hostile_encounter || stalled ;; Hindering
remained ;; Presence || established ;; Building
campaign ;; Military_operation || recaptured ;; Conquering
offensive ;; Military_operation || involvement ;; Participation || deportation ;; Removing || killings ;; Killing || massacres ;; Killing || compared ;; Check || attributed ;; Judgment_communication
took place ;; Process_start
promoted ;; Cause_change_of_position_on_a_scale || tournament ;; Competition || compete ;; Competition
included ;; Cause_to_be_included || tournament ;; Competition
played ;; Competition || tournament ;; Competition
recorded ;; Recording || recorded ;; Recording
joined ;; Becoming_a_member || tournament ;; Competition
losing it ;; Earnings_and_losses || regained ;; Recovering
Inclusion ;; Participation || play ;; Competition
joined ;; Becoming_a_member
finalists ;; Social_event || play ;; Competition
competition ;; Competition
played ;; Competition
appearance ;; Presence || losing ;; Earnings_and_losses || made ;; Action
entered ;; Becoming_a_member
entered ;; Becoming_a_member || beat ;; Preventing_or_letting || reach ;; Arriving
entered ;; Becoming_a_member || faced ;; Hostile_encounter
scored ;; Getting || conceded ;; Earnings_and_losses
joined ;; Becoming_a_member
scored ;; Getting
Watched ;; Perception_active
Championship ;; Competition || Championship ;; Competition
tournament ;; Competition || host ;; Social_event
finals ;; Process_end || qualified ;; Becoming_a_member
finished ;; Process_end || tournament ;; Competition || defeating ;; Conquering
defeated ;; Conquering || finish ;; Process_end
served ;; Supply || tournament ;; Competition || tournament ;; Competition || tournament ;; Competition
initiated ;; Process_start || coup ;; Change_of_leadership || put an end to ;; Process_end
refashioned ;; Recovering || resulted ;; Causation || revolution ;; Change_of_leadership
created ;; Creating
planned ;; Arranging
decided ;; Deciding || agreed ;; Agree_or_refuse_to_act || join ;; Becoming_a_member
commanded ;; Control || started ;; Process_start
acclaimed ;; Judgment_communication || triumphed ;; Achieve || marched ;; Self_motion
cyclone ;; Catastrophe || cyclone ;; Catastrophe || strike ;; Destroying
storm ;; Catastrophe || storm ;; Catastrophe || developed ;; Coming_to_be
moved ;; Motion || strengthened ;; Cause_change_of_strength || reach ;; Arriving
crossing ;; Motion_directional || landfall ;; Catastrophe || hit ;; Bodily_harm || weakened ;; Cause_change_of_strength
storm ;; Catastrophe || degenerated ;; Change || merging ;; Becoming_a_member
storm ;; Catastrophe || cyclone ;; Catastrophe || cyclone ;; Catastrophe || hit ;; Destroying || storm ;; Catastrophe || hit ;; Destroying
cyclone ;; Catastrophe || storm ;; Catastrophe
broke ;; Destroying || storms ;; Catastrophe || deepened ;; Cause_change_of_position_on_a_scale || records ;; Recording || cyclone ;; Catastrophe
damaging ;; Damaging || destroying ;; Destroying || produced ;; Creating
died ;; Death || cyclone ;; Catastrophe
season ;; Competition
contested ;; Competition
plays ;; Competition
join ;; Becoming_a_member || tournament ;; Competition
wins ;; Getting
decided ;; Deciding || matches ;; Competition
launched ;; Process_start
began ;; Process_start || scattered ;; Dispersal || uprooted ;; Removing || defended ;; Defending || resettled ;; Placing
taken over ;; Conquering || killed ;; Killing || infiltrated ;; Becoming_a_member
ordered ;; Request || raids ;; Attack
conducted ;; Action || supported ;; Supporting
sweep ;; Motion || pushed ;; Motion
took ;; Conquering || blamed ;; Judgment_communication || killed ;; Killing
broke ;; Destroying || overflowed ;; Motion
included ;; Cause_to_be_included
assisted ;; Assistance || mobilized ;; Becoming_a_member
match ;; Competition || took place ;; Process_start || Final ;; Competition
contested ;; Competition || match ;; Competition || Final ;; Competition
qualified ;; Becoming_a_member || Cup ;; Competition || Cup ;; Competition
competition ;; Competition || defending ;; Defending || eliminated ;; Removing
played ;; Competition || Final ;; Competition
Final ;; Competition || Cup ;; Competition
tournament ;; Competition || held ;; Hold
tournament ;; Competition || involve ;; Cause_to_be_included
semi-finals ;; Competition
match ;; Competition || reduced ;; Cause_change_of_position_on_a_scale || semi-final ;; Competition || made ;; Creating
semi-final ;; Competition || qualified ;; Becoming_a_member || defeated ;; Conquering
tournament ;; Competition || final ;; Competition || played ;; Competition
took ;; Getting || scored ;; Getting || scored ;; Getting
achieved ;; Achieve || losing ;; Earnings_and_losses
scored ;; Getting
named ;; Name_conferral
Tour ;; Traveling || tour ;; Traveling || released ;; Publishing
began ;; Process_start || starting ;; Process_start || finishing ;; Process_end || tour ;; Traveling
jaunt ;; Traveling || commencing ;; Process_start || culminating ;; Process_end
group ;; Cause_to_amalgamate || headed ;; Motion
began ;; Process_start || starting ;; Process_start || group ;; Cause_to_amalgamate
performed ;; Hold || wrapping up ;; Process_end || group ;; Cause_to_amalgamate
joined ;; Becoming_a_member || continuing ;; Motion
fought ;; Hostile_encounter || War ;; Hostile_encounter
took place ;; Process_start
retreated ;; Escaping || offered ;; Giving
fought ;; Hostile_encounter || victory ;; Earnings_and_losses
defeat ;; Conquering || established ;; Building
ended ;; Process_end || decided ;; Deciding || asked ;; Request || joined ;; Becoming_a_member || demanded ;; Request
hurricanes ;; Catastrophe
retired ;; Removing
formed ;; Coming_to_be || hurricane ;; Catastrophe || hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength || Moving ;; Motion || fluctuated ;; Motion || hurricane ;; Catastrophe || reaching ;; Achieve
hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength || moved ;; Motion || weakening ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength || dissipating ;; Removing
caused ;; Causation
flew ;; Motion || perished ;; Death || lost ;; Earnings_and_losses || believed ;; Know
hurricane ;; Catastrophe || occurred ;; Presence || association ;; Collaboration
caused ;; Causation
caused ;; Causation || hurricanes ;; Catastrophe || ensued ;; Coming_to_be
killed ;; Killing || leading to ;; Causation
hurricane ;; Catastrophe || known ;; Know || taken place ;; Process_start
recorded ;; Recording || associated ;; Collaboration
hurricane ;; Catastrophe || attributed ;; Causation
named ;; Name_conferral || developing ;; Coming_to_be || storm ;; Catastrophe || storm ;; Catastrophe
storm ;; Catastrophe || designated ;; Choosing
brought ;; Bringing || due ;; Causation || flooding ;; Catastrophe
caused ;; Causation || drowning ;; Death || associated ;; Collaboration || damage ;; Damaging || reported ;; Reporting
helped ;; Assistance || destroyed ;; Destroying || killed ;; Killing || resulted in ;; Causation || death ;; Death
drowned ;; Death
confirmed ;; Check || spawned ;; Creating
caused ;; Causation || becoming ;; Becoming || flooding ;; Catastrophe
make ;; Manufacturing || storm ;; Catastrophe
damage ;; Damaging || estimated ;; Scrutiny
race ;; Competition || Festival ;; Social_event || races ;; Competition || held ;; Hold
event ;; Social_event
record ;; Recording || record ;; Recording || holds ;; Creating || holds ;; Creating
begins ;; Process_start || travels ;; Traveling
race ;; Competition || proceeds ;; Self_motion
ends ;; Process_end || starts ;; Process_start || winding ;; Motion || race ;; Competition
competition ;; Competition || races ;; Competition || evolved ;; Coming_to_be
touring ;; Traveling || took place ;; Process_start
become ;; Becoming || took place ;; Process_start
include ;; Cause_to_be_included
sponsored ;; Supply
tour ;; Traveling || assembled ;; Come_together
confirmed ;; Check || festival ;; Social_event
named ;; Name_conferral || hurricane ;; Catastrophe || storm ;; Catastrophe || hurricane ;; Catastrophe
Developing ;; Cause_to_make_progress || strengthened ;; Cause_change_of_strength || reaching ;; Arriving
caused ;; Causation || increased ;; Cause_change_of_position_on_a_scale || storm ;; Catastrophe || weaken ;; Cause_change_of_strength
became ;; Becoming || storm ;; Catastrophe || Traveling ;; Traveling || due ;; Causation || regenerated ;; Recovering || hurricane ;; Catastrophe
made ;; Manufacturing || storm ;; Catastrophe
storm ;; Catastrophe || weakening ;; Cause_change_of_strength || dissipated ;; Dispersal
storm ;; Catastrophe || effects ;; Influence || destroyed ;; Destroying || left ;; Departing
injuries ;; Bodily_harm || damage ;; Damaging || reports ;; Reporting
concert ;; Social_event || tours ;; Traveling
concerts ;; Social_event || embarked ;; Process_start || tours ;; Traveling || tours ;; Traveling
tour ;; Traveling
Tour ;; Traveling
Tour ;; Traveling || tour ;; Traveling
began ;; Process_start || ended ;; Process_end
tours ;; Traveling || tours ;; Traveling
support ;; Supporting || tour ;; Traveling
began ;; Process_start || comprising ;; Cause_to_be_included || ended ;; Process_end
included ;; Cause_to_be_included || began ;; Process_start
took place ;; Process_start
taking place ;; Process_start || took place ;; Process_start
took place ;; Process_start
focused ;; Aiming || hit ;; Attack
confirmed ;; Check || took place ;; Process_start
placed ;; Placing || end ;; Process_end || earning ;; Earnings_and_losses || appeared ;; Presence
support ;; Supporting || mentioned ;; Statement || titled ;; Name_conferral || tour ;; Traveling
began ;; Process_start
ending ;; Process_end
took place ;; Process_start
called ;; Name_conferral || release ;; Publishing || following ;; Self_motion
performed ;; Hold
festival ;; Social_event
concert ;; Social_event || concert ;; Social_event || described ;; Statement
released ;; Publishing || documented ;; Recording
burns ;; Bodily_harm || suffered ;; Bodily_harm
promoting ;; Cause_to_make_progress || Tour ;; Traveling || tour ;; Traveling || release ;; Publishing
tour ;; Traveling || consisted ;; Cause_to_be_included
started ;; Process_start || included ;; Cause_to_be_included || concerts ;; Social_event || ending ;; Process_end || tour ;; Traveling
included ;; Cause_to_be_included || tour ;; Traveling
became ;; Becoming || known ;; Know || touring ;; Traveling
given ;; Giving || preview ;; Perception_active || expanded ;; Expansion || Building ;; Building || performance ;; Hold
recorded ;; Recording || show ;; Social_event || released ;; Publishing
retreat ;; Escaping || march ;; Self_motion || evade ;; Preventing_or_letting
escaped ;; Escaping
began ;; Process_start
escaped ;; Escaping || retreat ;; Escaping || traversed ;; Motion_directional || circling ;; Self_motion || command ;; Control
passed ;; Motion_directional || traveling ;; Traveling
began ;; Process_start || retreat ;; Escaping || gained ;; Getting || support ;; Supporting
party ;; Social_event || come to ;; Self_motion || completed ;; Process_end || represent ;; Presence || seal ;; Placing
tour ;; Traveling || tour ;; Traveling
supported ;; Supporting
announced ;; Expressing_publicly
demand ;; Request || Due ;; Causation || added ;; Cause_to_be_included
performed ;; Hold
commenced ;; Process_start
use ;; Using
performed ;; Hold
Tour ;; Traveling || tour ;; Traveling
Build ;; Building
began ;; Process_start || concerts ;; Social_event
shows ;; Social_event
release ;; Publishing || concert ;; Social_event || beginning ;; Process_start
began ;; Process_start || ended ;; Process_end
toured ;; Traveling || selected ;; Choosing || appearing ;; Presence
changed ;; Change
focused ;; Aiming
called ;; Name_conferral || sponsored ;; Supply || inaugurated ;; Social_event
created ;; Creating
creating ;; Creating
tour ;; Traveling || running out of ;; Expend_resource
plans ;; Arranging || attempted ;; Self_motion
take place ;; Process_start || said ;; Communication
party ;; Social_event || known ;; Know || concert ;; Social_event || support ;; Supporting || party ;; Social_event || tour ;; Traveling
ended up ;; Process_end || party ;; Social_event || founding ;; Creating || finish ;; Process_end || tour ;; Traveling || record ;; Recording || founding ;; Creating || returned ;; Arriving
tour ;; Traveling
develop ;; Coming_to_be || started ;; Process_start || started ;; Process_start || tour ;; Traveling || fall ;; Motion_directional
end ;; Process_end || died ;; Death || tour ;; Traveling
regrouped ;; Cause_to_amalgamate
decided ;; Deciding || decided ;; Deciding || death ;; Death || quit ;; Departing
released ;; Publishing
shot ;; Use_firearm || performed ;; Hold || set ;; Placing
released ;; Publishing
occurred ;; Coming_to_be || derailed ;; Self_motion || travelling ;; Traveling
died ;; Death || injured ;; Bodily_harm
travelling ;; Traveling || entered ;; Arriving
recorded ;; Recording
injury ;; Bodily_harm || charged ;; Judgment_communication
crash ;; Catastrophe || accident ;; Catastrophe
remains ;; Presence
organized ;; Arranging
touring ;; Traveling
become ;; Becoming
took place ;; Process_start || coverage ;; Social_event
held ;; Defending
released ;; Publishing
tour ;; Traveling
promote ;; Cause_to_make_progress
started ;; Process_start
travelled ;; Traveling
came to a close ;; Process_end
created ;; Creating || Tour ;; Traveling || tour ;; Traveling
announced ;; Expressing_publicly || tour ;; Traveling || festival ;; Social_event || tour ;; Traveling
began ;; Process_start || tour ;; Traveling || competition ;; Competition || due ;; Causation || Tour ;; Traveling || Tour ;; Traveling
returned ;; Arriving
recorded ;; Recording || documenting ;; Recording || Tour ;; Traveling || confirmed ;; Statement
caused ;; Causation || hurricane ;; Catastrophe
cyclone ;; Catastrophe || hurricane ;; Catastrophe || storm ;; Catastrophe
made ;; Manufacturing
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || emerging ;; Coming_to_be
moved ;; Motion
storm ;; Catastrophe || made ;; Manufacturing || landfall ;; Arriving
brushed ;; Damaging || emerging ;; Coming_to_be
began ;; Process_start || became ;; Becoming || storm ;; Catastrophe || hurricane ;; Catastrophe
landfall ;; Arriving || peaked ;; Motion_directional
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || dissipated ;; Destroying
wrecked ;; Damaging
damage ;; Damaging || reported ;; Reporting
damage ;; Damaging
damage ;; Damaging || destroyed ;; Destroying
beached ;; Placing
wrecked ;; Damaging || damaged ;; Damaging
destroyed ;; Destroying || damage ;; Damaging
confirmed ;; Check || deaths ;; Death || losses ;; Earnings_and_losses
inaugurated ;; Social_event || held ;; Hold
given ;; Giving
streamed ;; Coming_to_be
become ;; Becoming || takes place ;; Process_start
debut ;; Process_start || made ;; Giving
renamed ;; Name_conferral
confirmed ;; Check
released ;; Publishing
certified ;; Check
started ;; Process_start || finished ;; Process_end
finished ;; Process_end || participate ;; Participation
formed ;; Coming_to_be
expanded ;; Expansion || confirmed ;; Check
announced ;; Expressing_publicly
marks ;; Recording
performed ;; Hold
represented ;; Presence || disease ;; Catastrophe || outbreak ;; Catastrophe || outbreak ;; Catastrophe
detected ;; Know
began ;; Process_start || spread ;; Dispersal
spread ;; Dispersal
confirmed ;; Check
declared ;; Expressing_publicly || epidemic ;; Catastrophe
confirmed ;; Check
confirmed ;; Check
epidemic ;; Catastrophe
engagement ;; Hostile_encounter || reconnaissance ;; Scrutiny
occurred ;; Presence || War ;; Hostile_encounter
captured ;; Conquering || completed ;; Process_end
reconnaissance ;; Scrutiny || conducted ;; Action || determine ;; Deciding || approaching ;; Arriving
comprised ;; Cause_to_be_included
scout ;; Scrutiny || ordered ;; Arranging
moved ;; Motion || captured ;; Conquering || crossed ;; Self_motion
captured ;; Conquering || broke ;; Destroying || scattered ;; Dispersal || Moving ;; Self_motion || charged ;; Attack
comprised ;; Cause_to_be_included
captured ;; Conquering || consolidated ;; Cause_to_amalgamate
burned ;; Bodily_harm
wounded ;; Bodily_harm || lost ;; Earnings_and_losses
initiated ;; Process_start || search ;; Scrutiny || operation ;; Military_operation
taken up ;; Temporary_stay || drive ;; Motion
conducted ;; Action || took place ;; Process_start
deployed ;; Arranging
attacked ;; Attack
made ;; Manufacturing || settled ;; Resolve_problem
support ;; Supporting || taking place ;; Process_start
began ;; Process_start
performed ;; Hold || finished ;; Process_end
began ;; Process_start || supporting ;; Supporting || release ;; Publishing
supported ;; Supporting || supported ;; Supporting
beginning ;; Process_start || embarked ;; Process_start || supported ;; Supporting || supported ;; Supporting
returned ;; Departing || supported ;; Supporting || embarked ;; Process_start
supported ;; Supporting || supported ;; Supporting
returned ;; Arriving || playing ;; Participation || supported ;; Supporting
took place ;; Process_start
release ;; Publishing
changes ;; Change
backing ;; Supporting || referred ;; Statement
released ;; Publishing || recording ;; Recording || album ;; Recording
feature ;; Presence
returns ;; Arriving || made ;; Creating
released ;; Publishing
peaked ;; Cause_change_of_position_on_a_scale
released ;; Publishing || featuring ;; Presence || peaked ;; Cause_change_of_position_on_a_scale
released ;; Publishing
earthquake ;; Catastrophe || occurred ;; Coming_to_be
recorded ;; Recording
affected ;; Influence
quake ;; Catastrophe || felt ;; Perception_active
dead ;; Death || reports ;; Reporting || injured ;; Bodily_harm
destroyed ;; Destroying || damaged ;; Damaging
earthquake ;; Catastrophe || earthquake ;; Catastrophe
quake ;; Catastrophe
caused ;; Causation || damaged ;; Damaging || earthquake ;; Catastrophe || hit ;; Attack
struck ;; Attack || quake ;; Catastrophe
disrupted ;; Hindering || sent ;; Sending
celebrated ;; Social_event
decided ;; Deciding
events ;; Social_event || programs ;; Social_event || organised ;; Arranging
made ;; Creating
released ;; Publishing
occurred ;; Coming_to_be
broke ;; Destroying
killed ;; Killing || damage ;; Damaging || flow ;; Motion
relief ;; Assistance || undertook ;; Action
Support ;; Supporting
damages ;; Damaging || suffered ;; Bodily_harm || flood ;; Catastrophe
changing ;; Change || prompted ;; Causation
occurred ;; Coming_to_be || released ;; Publishing
attacked ;; Attack
climbed ;; Self_motion
killed ;; Killing || injured ;; Bodily_harm
held ;; Hold
resulted in ;; Causation
released ;; Publishing
release ;; Publishing
started ;; Process_start
debuted ;; Process_start
carried out ;; Attack
killed ;; Killing
apprehended ;; Arrest
became ;; Becoming || election ;; Choosing
jailed ;; Arrest || election ;; Choosing
landings ;; Self_motion || execution ;; Killing
arrival ;; Arriving || offensive ;; Attack || flee ;; Escaping
arresting ;; Arrest || ordered ;; Request
sent ;; Sending || lost ;; Death
claimed ;; Statement
involved ;; Participation || massacre ;; Killing
triggered ;; Causation || protests ;; Attack
refusal ;; Agree_or_refuse_to_act || marched ;; Self_motion
happen ;; Presence || took ;; Removing
seized ;; Hold || entombed ;; Placing || stormed ;; Catastrophe
declared ;; Expressing_publicly
returned ;; Arriving || cracked ;; Destroying
arrested ;; Arrest || died ;; Death
control ;; Control
tournament ;; Competition || held ;; Hold
held ;; Hold
arrest ;; Arrest
tournament ;; Competition || final ;; Competition
operation ;; Military_operation
operation ;; Military_operation || launched ;; Process_start
consisted ;; Cause_to_be_included
concluded ;; Process_end
achieved ;; Achieve || reported ;; Reporting
arrested ;; Arrest || reported ;; Reporting
appeared ;; Presence
attacked ;; Attack || approached ;; Arriving
seized ;; Arrest || set on fire ;; Damaging || found ;; Know
started ;; Process_start
destroyed ;; Destroying || went ;; Motion
spiked ;; Bodily_harm
killed ;; Killing || wounded ;; Bodily_harm
fled ;; Escaping
began ;; Process_start || occurred ;; Coming_to_be || roll ;; Motion
rolled ;; Motion || chose ;; Choosing || lead ;; Communication || derailed ;; Self_motion
used ;; Using
crashed ;; Catastrophe
killed ;; Killing || reports ;; Reporting || injured ;; Bodily_harm
received ;; Receiving || injuries ;; Bodily_harm
injured ;; Bodily_harm
occurred ;; Coming_to_be || derailed ;; Self_motion
caused ;; Causation || occurred ;; Coming_to_be || damage ;; Damaging
brought ;; Bringing
spawned ;; Coming_to_be
allowed ;; Preventing_or_letting
reached ;; Achieve
followed ;; Self_motion
moved ;; Motion || weakened ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
landfall ;; Arriving
cancelled ;; Process_end
peaked ;; Cause_change_of_position_on_a_scale
resulted ;; Causation
reported ;; Reporting
resulted in ;; Causation || took place ;; Process_start
caused ;; Causation
incidents ;; Catastrophe || occurred ;; Presence
resignation ;; Change_of_leadership || fall ;; Motion_directional || led to ;; Causation || triggered ;; Causation
targets ;; Aiming || due ;; Causation || occurred ;; Presence
died ;; Death
valued ;; Earnings_and_losses || reported ;; Reporting
completed ;; Process_end
occurred ;; Coming_to_be
exploded ;; Attack || injured ;; Bodily_harm || killed ;; Killing
caused ;; Causation || triggered ;; Causation
suffered ;; Bodily_harm
banned ;; Preventing_or_letting || reported ;; Reporting
maintain ;; Statement || ruling out ;; Removing || attribute ;; Agree_or_refuse_to_act
followed ;; Self_motion || bombings ;; Attack
storm ;; Catastrophe || storms ;; Catastrophe || attain ;; Achieve || failed ;; Agree_or_refuse_to_act
named ;; Name_conferral || developed ;; Coming_to_be
moved ;; Motion
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe
occurred ;; Coming_to_be || attained ;; Getting || coming ;; Arriving
weakened ;; Cause_change_of_strength || emerging ;; Coming_to_be || storm ;; Catastrophe || re-intensified ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
producing ;; Creating || caused ;; Causation
struck ;; Attack || leaving ;; Departing || downed ;; Motion
dropped ;; Motion_directional
reported ;; Reporting
triggered ;; Causation || killed ;; Killing
damaged ;; Damaging || states ;; Statement
damaging ;; Damaging || caused ;; Causation
causes ;; Causation || died ;; Death
caused ;; Causation || cholera ;; Catastrophe || deaths ;; Death || outbreak ;; Catastrophe
outbreak ;; Catastrophe || outbreak ;; Catastrophe || reported ;; Reporting
outbreak ;; Catastrophe || affected ;; Influence
outbreak ;; Catastrophe
stated ;; Statement || reported ;; Reporting || cases ;; Death || cholera ;; Catastrophe
outbreak ;; Catastrophe || occurred ;; Coming_to_be || reported ;; Reporting
televised ;; Communication || took place ;; Hold
defeating ;; Conquering
report ;; Reporting
became ;; Becoming || win ;; Earnings_and_losses
landfall ;; Self_motion
developed ;; Coming_to_be || strengthening ;; Cause_change_of_strength
strengthened ;; Cause_change_of_strength || moved ;; Motion
recurved ;; Motion || accelerated ;; Cause_change_of_position_on_a_scale
made ;; Manufacturing || dissipated ;; Dispersal
entered ;; Arriving || crossing ;; Motion_directional
caused ;; Causation || damage ;; Damaging || death ;; Death
caused ;; Causation || killed ;; Killing || damage ;; Damaging || produced ;; Manufacturing
reported ;; Reporting
took place ;; Process_start
named ;; Name_conferral
included ;; Cause_to_be_included
included ;; Cause_to_be_included || took place ;; Process_start
evolved ;; Coming_to_be || refused ;; Agree_or_refuse_to_act || organised ;; Arranging || held ;; Hold
received ;; Receiving || reported ;; Reporting
taken place ;; Process_start
hurricane ;; Catastrophe || caused ;; Causation
hurricane ;; Catastrophe || formed ;; Coming_to_be || cyclone ;; Catastrophe
hurricane ;; Catastrophe || storm ;; Catastrophe || passing ;; Motion_directional || Moving ;; Motion || intensified ;; Expansion || reaching ;; Arriving || attaining ;; Getting
hurricane ;; Catastrophe || recurved ;; Motion || intensified ;; Expansion || reaching ;; Arriving || landfall ;; Motion
moving ;; Motion || moved ;; Motion || cyclone ;; Catastrophe || weakened ;; Cause_change_of_strength || dissipating ;; Dispersal
hurricane ;; Catastrophe || caused ;; Causation
caused ;; Causation || destroying ;; Destroying || disrupting ;; Hindering
peaking ;; Self_motion || helped ;; Assistance
occurred ;; Coming_to_be
damages ;; Damaging || reported ;; Reporting
Hurricane ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || reach ;; Achieve
moved ;; Motion || storm ;; Catastrophe || Forming ;; Coming_to_be || exited ;; Departing
formed ;; Coming_to_be || caused ;; Causation || storm ;; Catastrophe || storm ;; Catastrophe || hurricane ;; Catastrophe || strength ;; Cause_change_of_strength || report ;; Reporting || dissipate ;; Dispersal || reached ;; Achieve || lost ;; Earnings_and_losses
moved ;; Motion || storm ;; Catastrophe || making ;; Manufacturing || weakening ;; Cause_change_of_position_on_a_scale
became ;; Becoming || storm ;; Catastrophe || lost ;; Earnings_and_losses
crossed ;; Motion_directional || becoming ;; Becoming
maintaining ;; Preserving || storm ;; Catastrophe || storm ;; Catastrophe
hurricane ;; Catastrophe || occurred ;; Coming_to_be || hurricane ;; Catastrophe || upgraded ;; Cause_change_of_position_on_a_scale
passed ;; Motion_directional || effects ;; Influence || reported ;; Reporting
altering ;; Change || allowing ;; Preventing_or_letting || takeoff ;; Self_motion
prevented ;; Preventing_or_letting
killed ;; Killing
appointed ;; Choosing
revived ;; Recovering
campaign ;; Military_operation
revolution ;; Change_of_leadership || reunify ;; Recovering || become ;; Becoming || fragmented ;; Destroying || purpose ;; Aiming
ended ;; Process_end
marked ;; Recording || end ;; Process_end
stepped down ;; Change_of_leadership || mend ;; Self_motion || went ;; Motion
began ;; Process_start
advanced ;; Cause_to_make_progress
assistance ;; Assistance || secured ;; Getting
assassinated ;; Killing || flee ;; Escaping || approached ;; Arriving
took over ;; Change_of_leadership || announced ;; Expressing_publicly
concluded ;; Process_end || control ;; Control || start ;; Process_start || reunified ;; Recovering
held ;; Hold
start ;; Process_start || bringing in ;; Bringing || benefit ;; Earnings_and_losses || held ;; Hold
promoted ;; Cause_to_make_progress
set ;; Placing || record ;; Recording
restored ;; Recovering || modified ;; Change
competitions ;; Competition || held over ;; Process_start || held ;; Hold
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
commanded ;; Control
battle ;; Hostile_encounter
slain ;; Killing || encounter ;; Hostile_encounter || routed ;; Destroying
appears ;; Presence || supported ;; Supporting || stated ;; Statement || rebellion ;; Change_of_leadership
shifted ;; Change || rebellion ;; Change_of_leadership || appears ;; Presence
gaining ;; Getting || control ;; Control || defeated ;; Conquering
invasion ;; Attack
renewing ;; Recovering || support ;; Supporting
conquer ;; Conquering
occupied ;; Control
lost ;; Earnings_and_losses || rebellion ;; Change_of_leadership || consolidation ;; Cause_change_of_position_on_a_scale
invaded ;; Attack || counter ;; Hostile_encounter
fought ;; Hostile_encounter || said ;; Communication || battle ;; Hostile_encounter
battle ;; Hostile_encounter || death ;; Death || fractured ;; Destroying
overcome ;; Conquering || gained ;; Getting
reestablishment ;; Recovering
Battle ;; Hostile_encounter || victory ;; Earnings_and_losses
hurricane ;; Catastrophe || hurricane ;; Catastrophe
developed ;; Coming_to_be
tracked ;; Self_motion || intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe
turned ;; Becoming
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || hurricane ;; Catastrophe || attain ;; Getting
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal || crossed ;; Motion_directional || redeveloping ;; Recovering
hurricane ;; Catastrophe || produced ;; Creating
reported ;; Reporting
caused ;; Causation || hurricane ;; Catastrophe
caused ;; Causation
caused ;; Causation || killed ;; Killing || reaching ;; Achieve
occurred ;; Presence || wars ;; Hostile_encounter
encompassed ;; Surrounding || conflict ;; Hostile_encounter
occurred ;; Presence || pushed ;; Motion || clashes ;; Hostile_encounter
conquered ;; Conquering
raids ;; Attack || restored ;; Recovering
broke out ;; Process_start || diminishing ;; Cause_change_of_position_on_a_scale
fought ;; Hostile_encounter || control ;; Control
caused ;; Causation || hurricane ;; Catastrophe || hurricane ;; Catastrophe || damage ;; Damaging
named ;; Name_conferral || cyclone ;; Catastrophe || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
reached ;; Arriving || strength ;; Cause_change_of_strength || formed ;; Coming_to_be
caused ;; Causation || damage ;; Damaging || damage ;; Damaging || storm ;; Catastrophe || storm ;; Catastrophe || damage ;; Damaging || hurricane ;; Catastrophe
causing ;; Causation || hurricane ;; Catastrophe || storm ;; Catastrophe || damage ;; Damaging || strength ;; Cause_change_of_strength || moved ;; Motion || strike ;; Attack
becoming ;; Becoming || cyclone ;; Catastrophe || dropped ;; Motion || progressed ;; Self_motion
storm ;; Catastrophe || regenerated ;; Recovering || causing ;; Causation || damage ;; Damaging || cyclone ;; Catastrophe || moved ;; Motion || moved ;; Motion
degenerated ;; Change || dissipating ;; Dispersal
caused ;; Causation || damage ;; Damaging || occurred ;; Presence
appointed ;; Request
declared ;; Expressing_publicly || took over ;; Change_of_leadership
aroused ;; Recovering
rose ;; Cause_to_make_progress
defeated ;; Conquering || launched ;; Process_start
expanded ;; Expansion
called ;; Name_conferral
took place ;; Process_start
situated ;; Placing
advanced ;; Cause_to_make_progress
became ;; Becoming || began ;; Process_start || attacked ;; Attack
captured ;; Conquering
captured ;; Conquering || repaired ;; Recovering || taken ;; Conquering
recaptured ;; Conquering || lost ;; Earnings_and_losses
blackouts ;; Motion || affected ;; Influence
blackout ;; Motion || affected ;; Influence || affected ;; Influence || blackout ;; Motion
blackout ;; Motion || outage ;; Motion
outage ;; Motion || affected ;; Influence
lacked ;; Causation
restored ;; Recovering || affected ;; Influence
fought ;; Hostile_encounter
invasions ;; Attack
attacked ;; Attack || remaining ;; Presence
ended ;; Process_end
broken ;; Destroying || celebrated ;; Social_event
re-established ;; Recovering || following ;; Self_motion
fought ;; Hostile_encounter
formed ;; Coming_to_be || stationed ;; Placing
aimed ;; Aiming || concluded ;; Process_end
conducted ;; Action || deployed ;; Arranging || counter ;; Response
placed ;; Placing || marked ;; Recording
re-established ;; Recovering
resulted in ;; Causation
began ;; Process_start
launched ;; Process_start
supplied ;; Supply || break ;; Destroying || goal ;; Aiming || seize ;; Conquering
pushed ;; Motion || adjusted ;; Change
designed ;; Creating
ended ;; Process_end || suffered ;; Bodily_harm || managed ;; Hold || halt ;; Preventing_or_letting
fought ;; Hostile_encounter || left ;; Departing
named ;; Name_conferral
marked ;; Recording
recover ;; Recovering || took ;; Conquering
reversed ;; Change || achieve ;; Achieve || failed ;; Earnings_and_losses
referred ;; Name_conferral
track ;; Motion
storm ;; Catastrophe || developed ;; Coming_to_be
caused ;; Causation || increased ;; Cause_change_of_position_on_a_scale || weaken ;; Cause_change_of_strength || degenerate ;; Death
regenerated ;; Recovering || owing ;; Causation || weakened ;; Cause_change_of_strength
became ;; Becoming || redeveloped ;; Recovering || weakened ;; Cause_change_of_strength
re-intensified ;; Cause_change_of_strength || intensified ;; Cause_change_of_strength || storm ;; Catastrophe || deepening ;; Cause_change_of_strength
strengthened ;; Cause_change_of_strength || crossed ;; Motion_directional || named ;; Name_conferral || becoming ;; Becoming
reached ;; Arriving
started ;; Process_start || began ;; Process_start || weaken ;; Cause_change_of_strength || weaken ;; Cause_change_of_strength
crossed ;; Motion_directional || storm ;; Catastrophe || weakened ;; Cause_change_of_strength
storm ;; Catastrophe || weakened ;; Cause_change_of_strength || diminished ;; Cause_change_of_position_on_a_scale
hurricane ;; Catastrophe || hurricane ;; Catastrophe
developed ;; Coming_to_be || strengthened ;; Cause_change_of_strength || passing ;; Motion_directional
intensification ;; Expansion
strengthened ;; Cause_change_of_strength || became ;; Becoming || storm ;; Catastrophe
destruction ;; Destroying || brought ;; Bringing || moving ;; Self_motion || reached ;; Arriving || resulted in ;; Causation
reported ;; Reporting
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || peaked ;; Self_motion
strike ;; Attack || remains ;; Presence
destroyed ;; Destroying || damaged ;; Damaging || resulted in ;; Causation || leaving ;; Departing
damage ;; Damaging || led to ;; Causation
weakened ;; Cause_change_of_strength || crossing ;; Motion_directional || emerging ;; Coming_to_be
began ;; Process_start || crossing ;; Motion_directional || resulted in ;; Causation
destroyed ;; Destroying || effects ;; Influence
caused ;; Causation || storm ;; Catastrophe || flooding ;; Catastrophe
drowned ;; Killing
weakened ;; Cause_change_of_strength || crossing ;; Motion_directional
made ;; Creating || curved ;; Motion_directional || emerged ;; Coming_to_be
weakened ;; Cause_change_of_strength || became ;; Becoming
caused ;; Causation || killed ;; Killing
promote ;; Cause_change_of_position_on_a_scale
extended ;; Expansion
included ;; Cause_to_be_included
stated ;; Statement
received ;; Receiving || praising ;; Judgment_communication
kicked off ;; Motion
grossed ;; Earnings_and_losses
fought ;; Hostile_encounter
died ;; Death || succeeded ;; Change_of_leadership
stirred ;; Cause_to_amalgamate
declared ;; Expressing_publicly || escalated ;; Expansion
fought ;; Hostile_encounter || stationed ;; Placing
defeated ;; Conquering || bringing ;; Causation
ended ;; Process_end || brought ;; Bringing
increased ;; Cause_change_of_position_on_a_scale
caused ;; Causation || floods ;; Catastrophe || cyclone ;; Catastrophe || hurricane ;; Catastrophe || landfall ;; Motion
began ;; Process_start || rainfall ;; Self_motion || passed ;; Motion_directional
formed ;; Coming_to_be
increased ;; Cause_change_of_position_on_a_scale || dropped ;; Motion_directional || moved ;; Motion || rainfall ;; Self_motion || rainfall ;; Self_motion
dissipated ;; Dispersal
adding ;; Cause_change_of_position_on_a_scale || rainfall ;; Self_motion
causing ;; Causation || rainfall ;; Self_motion || extended ;; Expansion
ending ;; Process_end || rainfall ;; Self_motion
damaged ;; Damaging || damaged ;; Damaging || killing ;; Killing || Floods ;; Catastrophe
killed ;; Killing || damaged ;; Damaging || destroyed ;; Destroying || rainfall ;; Self_motion
reached ;; Arriving || rainfall ;; Self_motion
leave ;; Departing || flooding ;; Motion || forcing ;; Causation
damaged ;; Damaging || damaged ;; Damaging || destroyed ;; Destroying || floods ;; Catastrophe
Damage ;; Damaging || deaths ;; Death
damaged ;; Damaging || damage ;; Damaging || floods ;; Catastrophe
deaths ;; Death || damaged ;; Damaging
Hurricane ;; Catastrophe || Hurricane ;; Catastrophe
died ;; Death || forced ;; Causation
flooded ;; Catastrophe || due ;; Causation || evacuation ;; Escaping || forcing ;; Causation || reasons ;; Causation
caused ;; Causation || storm ;; Catastrophe || damaging ;; Damaging || striking ;; Attack || flooding ;; Catastrophe || rebuilt ;; Building
caused ;; Causation || floods ;; Catastrophe || damage ;; Damaging || deaths ;; Death
extended ;; Expansion || concept ;; Statement || developed ;; Coming_to_be || produced ;; Creating
conflict ;; Hostile_encounter
stable ;; Placing || setting ;; Placing
moved ;; Motion || provided ;; Supply || situated ;; Placing
Dominion ;; Change_of_leadership || introduced ;; Creating
conflict ;; Hostile_encounter || introduced ;; Creating || escalated ;; Cause_change_of_strength
present ;; Presence || received ;; Receiving || response ;; Response || mixed ;; Cause_to_amalgamate
altered ;; Change || shifting ;; Change || Developing ;; Cause_to_make_progress
competition ;; Competition
contested ;; Competition || took place ;; Process_start
played ;; Competition
added ;; Cause_to_be_included || taking ;; Conquering || lead ;; Conquering
win ;; Getting || extending ;; Expansion
war ;; Hostile_encounter
arose ;; Coming_to_be
involved ;; Cause_to_be_included
described ;; Statement
forms ;; Coming_to_be
describes ;; Statement
remains ;; Presence
made ;; Manufacturing || assign ;; Choosing
marks ;; Recording || range ;; Arranging || holds ;; Defending
modified ;; Change || formed ;; Coming_to_be || expanded ;; Expansion
slow-moving ;; Self_motion || produced ;; Creating
developed ;; Coming_to_be || Originating ;; Coming_to_be
decreasing ;; Cause_change_of_position_on_a_scale || classified ;; Check
enabled ;; Preventing_or_letting || sprawling ;; Expansion || deepen ;; Cause_change_of_strength
Becoming ;; Becoming || improved ;; Cause_to_make_progress || acquired ;; Getting
achieved ;; Achieve
caused ;; Causation || made ;; Manufacturing
weakened ;; Cause_change_of_strength || strength ;; Cause_change_of_strength
saw ;; Self_motion
record ;; Recording || took place ;; Process_start
took place ;; Process_start
replaced ;; Change_of_leadership
attacks ;; Attack || referred ;; Name_conferral
conducted ;; Action || raids ;; Attack || shelling ;; Use_firearm
hit ;; Attack || fired ;; Use_firearm
attacks ;; Attack || targeted ;; Aiming
participated ;; Participation
attacks ;; Attack || conflict ;; Hostile_encounter || banning ;; Preventing_or_letting
began ;; Process_start
played ;; Self_motion
played ;; Self_motion
lifted ;; Motion
shot ;; Use_firearm
killings ;; Killing || murders ;; Killing
began ;; Process_start || murders ;; Killing
killed ;; Killing || led to ;; Causation || shootout ;; Use_firearm || death ;; Death
gunned ;; Use_firearm || pursued ;; Aiming
incidents ;; Catastrophe || Tragedy ;; Catastrophe
death ;; Death
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
command ;; Control || command ;; Control
turned ;; Becoming || occurred ;; Presence || battle ;; Hostile_encounter || engagement ;; Hostile_encounter || war ;; Hostile_encounter || fire ;; Use_firearm
retreated ;; Escaping || killed ;; Killing || loss ;; Earnings_and_losses || wounded ;; Bodily_harm || captured ;; Conquering || fallen ;; Becoming
killed ;; Killing || lost ;; Earnings_and_losses || wounded ;; Bodily_harm || captured ;; Conquering
battle ;; Hostile_encounter || battle ;; Hostile_encounter || War ;; Hostile_encounter || turning ;; Change
killing ;; Killing || bombed ;; Use_firearm
killed ;; Killing || wounded ;; Bodily_harm || attended ;; Participation
demolished ;; Destroying
attack ;; Attack || carried out ;; Attack
struck ;; Attack || consisted ;; Cause_to_be_included
conducted ;; Action || attack ;; Attack || named ;; Name_conferral || military ;; Hostile_encounter || strikes ;; Attack || Operation ;; Military_operation
occupied ;; Control || remained ;; Presence || defeated ;; Conquering
included ;; Cause_to_be_included || battles ;; Hostile_encounter
held ;; Defending || loss ;; Earnings_and_losses || resulted in ;; Causation
campaign ;; Military_operation || Bombing ;; Use_firearm
caused ;; Causation || loss ;; Earnings_and_losses
capture ;; Conquering || fall ;; Motion_directional || lead to ;; Causation
ended ;; Process_end
involved ;; Participation || battle ;; Hostile_encounter
battle ;; Hostile_encounter || War ;; Military_operation || took place ;; Process_start
destroyed ;; Destroying || operation ;; Military_operation || ambush ;; Attack || launched ;; Process_start
shot down ;; Use_firearm || led ;; Conquering || operation ;; Military_operation
devastating ;; Destroying
employed ;; Using || conducting ;; Action || raids ;; Attack
concluded ;; Process_end || battle ;; Hostile_encounter || losing ;; Earnings_and_losses
shot down ;; Use_firearm || serving ;; Assistance
flew ;; Motion || left ;; Departing
became ;; Becoming
shot down ;; Use_firearm || entered ;; Arriving || make ;; Action
attacks ;; Attack || killed ;; Killing || wounded ;; Bodily_harm
killed ;; Killing || wounded ;; Bodily_harm
killed ;; Killing || wounded ;; Bodily_harm
die ;; Death || injured ;; Bodily_harm
killed ;; Killing || wounded ;; Bodily_harm
dead ;; Death || shoot ;; Use_firearm
took place ;; Process_start
captured ;; Conquering || encountered ;; Hostile_encounter
became ;; Becoming || said ;; Communication
fired ;; Use_firearm || lasted ;; Process_end
kept ;; Temporary_stay || making ;; Causation
gave ;; Giving || devastated ;; Destroying
remained ;; Presence
found ;; Know
preferred ;; Choosing
governed ;; Control
replaced ;; Change_of_leadership || arrival ;; Arriving
described ;; Statement || massacre ;; Killing || contributed ;; Giving
earthquake ;; Catastrophe || struck ;; Bodily_harm
deaths ;; Death || caused ;; Causation || damage ;; Damaging
aftershocks ;; Catastrophe || occurred ;; Presence
occurred ;; Presence || occurred ;; Presence
damage ;; Damaging || sits ;; Placing || suffered ;; Bodily_harm
caused ;; Causation || damaged ;; Damaging || damage ;; Damaging || collapsed ;; Damaging
attacked ;; Attack || launched ;; Process_start
establish ;; Building
came from ;; Motion
participated ;; Participation || provided ;; Supply
began ;; Process_start
commenced ;; Process_start
used ;; Using
gave ;; Giving || planned ;; Arranging
launched ;; Process_start
attacked ;; Attack
damaged ;; Damaging || lost ;; Earnings_and_losses || suffered ;; Bodily_harm
stationed ;; Placing
accident ;; Catastrophe || crash ;; Catastrophe
operating ;; Being_in_operation || service ;; Supply
crashed ;; Catastrophe
killed ;; Killing
found ;; Placing
crashed ;; Catastrophe || encountering ;; Hostile_encounter
died ;; Death || making it ;; Causation
discovered ;; Know || located ;; Placing
victims ;; Catastrophe
protected ;; Preventing_or_letting
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
occurred ;; Presence
fought ;; Hostile_encounter
stationed ;; Placing || attacked ;; Attack || comprised ;; Cause_to_be_included
chose ;; Choosing || death ;; Death || fight ;; Hostile_encounter || stands ;; Self_motion
recaptured ;; Conquering || post ;; Sending
battle ;; Hostile_encounter || commemorates ;; Social_event
caused ;; Causation || flooding ;; Motion
named ;; Name_conferral || developed ;; Coming_to_be
named ;; Name_conferral || beaten ;; Bodily_harm || marked ;; Recording || storm ;; Catastrophe || record ;; Recording || record ;; Recording
moving ;; Motion || headed ;; Self_motion
strengthened ;; Cause_change_of_strength || attained ;; Getting
prevented ;; Preventing_or_letting
weakened ;; Cause_change_of_strength || storm ;; Catastrophe
landfall ;; Self_motion
weaken ;; Cause_change_of_strength || emerging ;; Coming_to_be || crossing ;; Motion_directional || dissipated ;; Dispersal
dropped ;; Motion_directional
peaked ;; Self_motion
reached ;; Arriving || flooded ;; Motion
reached ;; Arriving
causing ;; Causation || overflowed ;; Motion || flooding ;; Motion
overflowed ;; Motion || inundated ;; Motion
flooding ;; Motion || inundated ;; Motion
caused ;; Causation || caused ;; Causation || damage ;; Damaging
caused ;; Causation || losses ;; Earnings_and_losses
earthquake ;; Catastrophe || occurred ;; Coming_to_be
earthquake ;; Catastrophe || occurred ;; Coming_to_be
shifted ;; Motion
earthquake ;; Catastrophe
reported ;; Reporting
tsunami ;; Catastrophe || reported ;; Reporting
represented ;; Presence
took place ;; Process_start
began ;; Process_start || ended ;; Process_end
present ;; Presence
dominated ;; Control
held ;; Hold
record ;; Recording
hurricane ;; Catastrophe || hurricane ;; Catastrophe
records ;; Recording || set ;; Placing
record ;; Recording
formed ;; Coming_to_be || marked ;; Recording
developed ;; Coming_to_be
developed ;; Coming_to_be
strengthened ;; Cause_change_of_strength
intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe || hurricane ;; Catastrophe
reached ;; Arriving || landfall ;; Self_motion
weakened ;; Cause_change_of_strength || moving ;; Motion
dissipated ;; Dispersal
caused ;; Causation || damage ;; Damaging
damage ;; Damaging || standing ;; Placing || fatality ;; Death
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || attack ;; Attack
made up ;; Come_together || surprised ;; Change_sentiment || captured ;; Conquering
fell ;; Motion_directional || beginning ;; Process_start
losses ;; Earnings_and_losses || sparring ;; Hostile_encounter || drove ;; Motion
wounded ;; Bodily_harm || replaced ;; Change_of_leadership
included ;; Cause_to_be_included
caused ;; Causation || disaster ;; Catastrophe || call ;; Preventing_or_letting
belonged ;; Presence
War ;; Military_operation || Wars ;; Attack || occurred ;; Presence
earthquakes ;; Catastrophe || occurred ;; Coming_to_be
struck ;; Attack || quake ;; Catastrophe
placed ;; Placing || earthquake ;; Catastrophe
quake ;; Catastrophe || occurred ;; Coming_to_be
recorded ;; Recording || aftershocks ;; Catastrophe
earthquakes ;; Catastrophe || felt ;; Perception_active
caused ;; Causation || deaths ;; Death || earthquakes ;; Catastrophe || injured ;; Bodily_harm
brought ;; Bringing || flooding ;; Motion
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe
strengthen ;; Cause_change_of_strength || failed ;; Earnings_and_losses || headed ;; Motion
began ;; Process_start || curved ;; Motion_directional || intensifying ;; Cause_change_of_strength
upgraded ;; Cause_to_make_progress || storm ;; Catastrophe
deepen ;; Cause_change_of_strength || turned ;; Becoming
reached ;; Arriving
became ;; Becoming || weakened ;; Cause_change_of_strength
re-strengthened ;; Cause_change_of_strength
caused ;; Causation || damage ;; Damaging || surf ;; Self_motion || produced ;; Manufacturing
weakened ;; Cause_change_of_strength
passing ;; Motion_directional || combined ;; Cause_to_amalgamate || storm ;; Catastrophe || flooding ;; Motion || produced ;; Manufacturing
caused ;; Causation || hurricane ;; Catastrophe || deaths ;; Death || died ;; Death
damage ;; Damaging
transitioning ;; Change
causing ;; Causation || storm ;; Catastrophe || flooding ;; Motion || brought ;; Bringing
present ;; Presence
selections ;; Choosing
renamed ;; Name_conferral || revolt ;; Change_of_leadership || reconquered ;; Conquering
found ;; Know
became ;; Becoming || centered ;; Placing
drove ;; Motion || build ;; Building
controlled ;; Control
established ;; Creating
warred ;; Hostile_encounter || control ;; Control || established ;; Creating
followed ;; Motion
declined ;; Cause_change_of_position_on_a_scale || vied ;; Competition
prevailed ;; Conquering
prevailed ;; Conquering
conquered ;; Conquering
renamed ;; Name_conferral
marked ;; Recording
became ;; Becoming
accepted ;; Receiving
changed ;; Change
put an end to ;; Process_end
determined ;; Deciding
replace ;; Change_of_leadership || topple ;; Change_of_leadership
set out ;; Placing || stormed ;; Attack
comment ;; Statement || glorified ;; Judgment_communication
overran ;; Hold
stationed ;; Placing
died ;; Death || ordered ;; Arranging
aroused ;; Causation
hurricane ;; Catastrophe || hurricane ;; Catastrophe || Hurricane ;; Catastrophe
formed ;; Coming_to_be || strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || formed ;; Coming_to_be
Located ;; Placing || intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe
hurricane ;; Catastrophe || attained ;; Achieve
weakened ;; Cause_change_of_strength || strength ;; Cause_change_of_strength
hurricane ;; Catastrophe || passing ;; Motion_directional
dissipated ;; Dispersal || deteriorating ;; Cause_change_of_strength
prompted ;; Causation || arrival ;; Arriving || hurricane ;; Catastrophe || hurricane ;; Catastrophe || evacuated ;; Removing
deaths ;; Death || causing ;; Causation || damage ;; Damaging || Hurricane ;; Catastrophe || produced ;; Causation
storm ;; Catastrophe || damage ;; Damaging || state ;; Statement || produced ;; Causation
Final ;; Competition || took place ;; Process_start
match ;; Competition || played ;; Competition
became ;; Becoming || becoming ;; Becoming || match ;; Competition || claiming ;; Statement
sending ;; Sending || marred ;; Damaging || loss ;; Earnings_and_losses || match ;; Competition || contributed ;; Causation
sent ;; Sending
infuriated ;; Response
final ;; Process_end || decision ;; Deciding || thought ;; Know || ruined ;; Destroying
held ;; Hold
change ;; Change
uses ;; Using
used ;; Using
stated ;; Statement
used ;; Using
criticised ;; Judgment_communication || used ;; Using
used ;; Using
announced ;; Expressing_publicly
focused ;; Aiming
assert ;; Statement
festival ;; Social_event || takes place ;; Process_start
held ;; Hold
festival ;; Social_event
noted ;; Know
festival ;; Social_event || aim ;; Aiming
performed ;; Hold || include ;; Cause_to_be_included
held ;; Hold
showcases ;; Social_event
attended ;; Perception_active
focuses ;; Aiming
takes place ;; Process_start
operation ;; Military_operation || Operation ;; Military_operation
begun ;; Process_start || operation ;; Military_operation || push ;; Self_motion || aim ;; Aiming || oust ;; Removing
request ;; Request || assistance ;; Assistance
ended ;; Process_end || operation ;; Military_operation || launched ;; Process_start
fled ;; Escaping || fled ;; Escaping || killed ;; Killing
named ;; Name_conferral || operation ;; Military_operation
destroy ;; Destroying || reached ;; Arriving || occurred ;; Coming_to_be
built ;; Building || started ;; Process_start || raiding ;; Attack || command ;; Control
moved up ;; Motion || reached ;; Arriving || raided ;; Attack
increased ;; Cause_change_of_position_on_a_scale
targeted ;; Aiming || campaigns ;; Military_operation
hurricanes ;; Catastrophe || record-breaking ;; Recording
formed ;; Coming_to_be || Originating ;; Coming_to_be
storm ;; Catastrophe || due ;; Causation || transition ;; Change
due ;; Causation || due ;; Causation || retaining ;; Preserving || strength ;; Cause_change_of_strength || defied ;; Agree_or_refuse_to_act
began ;; Process_start || due ;; Causation || approaching ;; Arriving || attained ;; Getting || hurricane ;; Catastrophe
turning ;; Becoming || hurricane ;; Catastrophe || developed ;; Cause_change_of_strength
turned ;; Becoming || attained ;; Getting
hurricane ;; Catastrophe || making ;; Manufacturing || hurricane ;; Catastrophe || record ;; Recording || dropped ;; Motion_directional
caused ;; Causation || classified ;; Check || weakening ;; Cause_change_of_strength || storm ;; Catastrophe
dissipated ;; Dispersal
starts ;; Process_start || finishes ;; Process_end
descend ;; Motion_directional || ascend ;; Motion_directional
commences ;; Process_start
receive ;; Receiving
sponsored ;; Supply
causing ;; Causation || looped ;; Motion
forming ;; Coming_to_be
moved ;; Motion || formation ;; Coming_to_be
became ;; Becoming || reaching ;; Arriving || hurricane ;; Catastrophe
looped ;; Motion
turned ;; Becoming || crossing ;; Motion_directional || Weakening ;; Cause_change_of_strength
turning ;; Motion
contributed ;; Causation
move ;; Motion || hurricanes ;; Catastrophe
allowing ;; Preventing_or_letting || formed ;; Coming_to_be
died ;; Death
hurricane ;; Catastrophe || dropped ;; Motion_directional
causing ;; Causation || flooded ;; Motion
approached ;; Arriving
forced ;; Motion || damaged ;; Damaging
causing ;; Causation || damage ;; Damaging || spawned ;; Causation
caused ;; Causation || inflicted ;; Causation || hurricanes ;; Catastrophe || making it ;; Causation
excludes ;; Preventing_or_letting
produced ;; Manufacturing
developed ;; Coming_to_be
occurred ;; Coming_to_be || becoming ;; Becoming || storm ;; Catastrophe || following ;; Self_motion
reached ;; Arriving || made ;; Manufacturing
weakened ;; Cause_change_of_strength || moving ;; Motion
state ;; Statement || remained ;; Presence || completed ;; Process_end
combined ;; Cause_to_amalgamate || occurred ;; Coming_to_be || damage ;; Damaging
losses ;; Earnings_and_losses
caused ;; Causation || storm ;; Catastrophe
caused ;; Causation || impact ;; Influence || spawned ;; Coming_to_be
dropped ;; Motion_directional || reaching ;; Arriving
caused ;; Causation || damage ;; Damaging
strike ;; Attack || cyclone ;; Catastrophe
developed ;; Coming_to_be
became ;; Becoming || Moving ;; Motion || storm ;; Catastrophe || hurricane ;; Catastrophe
moved ;; Motion
storm ;; Catastrophe || peaked ;; Cause_change_of_position_on_a_scale
hurricane ;; Catastrophe || damage ;; Damaging || made ;; Bringing
began ;; Process_start || moving ;; Motion || weaken ;; Cause_change_of_strength || hurricane ;; Catastrophe || falling ;; Cause_change_of_position_on_a_scale
curved ;; Motion_directional || approached ;; Arriving
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || storm ;; Catastrophe || emerged ;; Coming_to_be
cyclone ;; Catastrophe || made ;; Causation
weaken ;; Cause_change_of_strength || transitioned ;; Change
storm ;; Catastrophe || deaths ;; Death || caused ;; Causation || damage ;; Damaging
hit ;; Bodily_harm
destroyed ;; Destroying || burned ;; Destroying || storm ;; Catastrophe
destroyed ;; Destroying || damaged ;; Damaging
died ;; Death
destroyed ;; Destroying
hurricane ;; Catastrophe
destroyed ;; Destroying
ripped ;; Destroying
losses ;; Earnings_and_losses || declared ;; Expressing_publicly
tossed ;; Motion
storm ;; Catastrophe || flooding ;; Catastrophe || causing ;; Causation || produced ;; Creating
hurricane ;; Catastrophe || fatalities ;; Death || resulted in ;; Causation || damage ;; Damaging
brought ;; Bringing || cyclone ;; Catastrophe
formed ;; Coming_to_be || originated ;; Coming_to_be || cyclone ;; Catastrophe
strengthened ;; Cause_change_of_strength || remained ;; Presence
caused ;; Causation || forcing ;; Causation || cyclone ;; Catastrophe || track ;; Motion
progression ;; Motion || intensified ;; Cause_change_of_strength || reached ;; Achieve
landfall ;; Motion || storm ;; Self_motion
re-curved ;; Motion || weaken ;; Cause_change_of_strength || dissipating ;; Dispersal
existence ;; Presence || brought ;; Bringing || Cyclone ;; Catastrophe
peaked ;; Self_motion
damage ;; Damaging || landfall ;; Arriving
destruction ;; Destroying || occurred ;; Coming_to_be
damage ;; Damaging || sustained ;; Achieve || felled ;; Motion_directional
drowned ;; Killing || cyclones ;; Catastrophe
retired ;; Process_end
caused ;; Causation
developed ;; Coming_to_be
tracked ;; Motion
caused ;; Causation || struck ;; Attack
strengthened ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength
crossed ;; Motion_directional
dropped ;; Motion_directional
destroyed ;; Destroying || damage ;; Damaging
caused ;; Causation
peaked ;; Cause_change_of_position_on_a_scale
caused ;; Causation || occurred ;; Coming_to_be
caused ;; Causation
wrestling ;; Social_event
took place ;; Hold
moved ;; Motion
defeat ;; Conquering
win ;; Getting
defeating ;; Conquering
take place ;; Process_start
announced ;; Expressing_publicly || dropping ;; Motion_directional
hurricanes ;; Catastrophe
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe
became ;; Becoming || becoming ;; Becoming || storm ;; Catastrophe || intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe
became ;; Becoming || reached ;; Arriving || hurricane ;; Catastrophe
brushing ;; Motion || weakened ;; Cause_change_of_strength
storm ;; Catastrophe || weakened ;; Cause_change_of_strength
dissipating ;; Dispersal || weakened ;; Cause_change_of_strength
damage ;; Damaging || occurred ;; Coming_to_be || produced ;; Creating
split ;; Destroying || inundated ;; Motion
caused ;; Causation || broken ;; Destroying || lose ;; Earnings_and_losses
damaged ;; Damaging || damaged ;; Damaging
occurred ;; Coming_to_be || flooding ;; Motion || fell ;; Motion_directional
caused ;; Causation || strike ;; Attack
destroyed ;; Destroying || spawned ;; Coming_to_be
known ;; Know || fell ;; Motion_directional || damage ;; Damaging || occurred ;; Coming_to_be
caused ;; Causation || damage ;; Damaging
peaking ;; Cause_change_of_position_on_a_scale
developed ;; Coming_to_be || moving ;; Motion || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || forming ;; Coming_to_be || headed ;; Self_motion
intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe
curved ;; Motion_directional || grazed ;; Bodily_harm
strengthen ;; Cause_change_of_strength || peaked ;; Cause_change_of_position_on_a_scale || continued ;; Process_start
passed ;; Motion_directional || reaching ;; Arriving || weakened ;; Cause_change_of_strength
moving ;; Motion || accelerated ;; Cause_change_of_position_on_a_scale
approaching ;; Arriving || transitioned ;; Change
impact ;; Influence
caused ;; Causation || damage ;; Damaging || knocked ;; Destroying
dropped ;; Motion_directional || produced ;; Manufacturing
cyclone ;; Catastrophe || cyclone ;; Damaging || strike ;; Attack
formed ;; Coming_to_be || storm ;; Catastrophe || reach ;; Cause_change_of_position_on_a_scale || curved ;; Motion_directional
storm ;; Catastrophe || struck ;; Attack || dissipating ;; Cause_change_of_strength
Producing ;; Creating || storm ;; Catastrophe || cyclone ;; Catastrophe || Cyclone ;; Catastrophe || hit ;; Destroying || hit ;; Attack
damaged ;; Damaging
dropped ;; Motion_directional || cyclone ;; Catastrophe
killed ;; Killing || storm ;; Catastrophe
earthquake ;; Catastrophe || occurred ;; Coming_to_be
causing ;; Causation || damage ;; Damaging || perceived ;; Know || wave ;; Attack
caused ;; Causation || casualties ;; Death
caused ;; Causation || damage ;; Damaging || Landslides ;; Catastrophe || falls ;; Motion_directional || avalanches ;; Catastrophe
earthquake ;; Catastrophe || restoration ;; Building || led to ;; Causation
causing ;; Causation || damage ;; Damaging || suffered ;; Bodily_harm
played ;; Competition
defeated ;; Conquering || come off ;; Motion || win ;; Earnings_and_losses
injury ;; Bodily_harm || dropped ;; Motion_directional
changes ;; Change || injuries ;; Bodily_harm || replaced ;; Change
left ;; Departing || replaced ;; Change
killed ;; Killing || caused ;; Causation || hurricane ;; Catastrophe
named ;; Name_conferral || developed ;; Coming_to_be
moved ;; Motion
intensified ;; Cause_change_of_strength || changed ;; Change
strengthened ;; Cause_change_of_strength || attained ;; Getting || turning ;; Becoming
reaching ;; Arriving || accelerated ;; Cause_change_of_position_on_a_scale
landfall ;; Self_motion || moved ;; Motion
landfall ;; Self_motion || dropped ;; Motion_directional
causing ;; Causation || flooding ;; Motion
killed ;; Killing || occurring ;; Presence || occurred ;; Coming_to_be
attributed ;; Causation
strength ;; Cause_change_of_strength || damage ;; Damaging || landfall ;; Self_motion
known ;; Know || strike ;; Attack
formed ;; Coming_to_be
moved ;; Motion || intensified ;; Cause_change_of_strength
developing ;; Coming_to_be || began ;; Process_start || attained ;; Achieve
moving ;; Motion || reached ;; Achieve
weakened ;; Cause_change_of_strength || entered ;; Arriving
landfall ;; Self_motion || dissipating ;; Dispersal
damage ;; Damaging || hit ;; Attack
died ;; Death
killed ;; Killing || left ;; Departing
killed ;; Killing || dead ;; Death || fell ;; Motion_directional
died ;; Death || lost ;; Earnings_and_losses
died ;; Death
left ;; Departing
destroyed ;; Destroying || damaged ;; Damaging
injuries ;; Bodily_harm
totaled ;; Earnings_and_losses
killed ;; Killing
storm ;; Catastrophe
died ;; Death
hurricane ;; Catastrophe || hurricanes ;; Catastrophe || Hurricane ;; Catastrophe || strike ;; Attack
named ;; Name_conferral || formed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe
intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe || crossed ;; Motion_directional || moving ;; Motion
dropped ;; Motion_directional || destroyed ;; Destroying || produced ;; Causation
damaged ;; Damaging
Damage ;; Damaging || deaths ;; Death
hurricane ;; Catastrophe || causing ;; Causation || damage ;; Damaging || moved ;; Motion
strengthened ;; Cause_change_of_strength || reaching ;; Arriving || reach ;; Arriving
hurricane ;; Catastrophe || Hurricane ;; Catastrophe || moved ;; Motion || flooding ;; Catastrophe
struck ;; Attack
dropped ;; Motion_directional || damaged ;; Damaging || flooded ;; Destroying || leaving ;; Causation
killed ;; Killing || caused ;; Causation || storm ;; Catastrophe
caused ;; Causation || caused ;; Causation || Hurricane ;; Catastrophe || damage ;; Damaging || outbreak ;; Process_start || dysentery ;; Bodily_harm || flooding ;; Catastrophe || struck ;; Attack || struck ;; Attack || flooding ;; Catastrophe
storms ;; Catastrophe || damage ;; Damaging
named ;; Name_conferral
controlled ;; Control || incorporated ;; Cause_to_amalgamate
fall ;; Motion_directional
supported ;; Supporting || designated ;; Choosing
risen ;; Cause_change_of_position_on_a_scale || eradicate ;; Removing
named ;; Name_conferral || hurricane ;; Catastrophe
moved ;; Motion || Forming ;; Coming_to_be || hurricane ;; Catastrophe || attained ;; Getting
moved ;; Motion || curved ;; Motion_directional || Hurricane ;; Catastrophe || peaking ;; Cause_change_of_position_on_a_scale
passed ;; Motion_directional || storm ;; Catastrophe || dissipated ;; Dispersal
hurricane ;; Catastrophe || Damage ;; Damaging
damage ;; Damaging || occurred ;; Coming_to_be
injuries ;; Bodily_harm || due ;; Causation || left ;; Causation
killing ;; Killing || sunk ;; Self_motion
competition ;; Competition || organized ;; Arranging || combined ;; Cause_to_amalgamate || competition ;; Competition || sport ;; Competition
arranged ;; Arranging
held ;; Hold
record ;; Recording || race ;; Competition || ski ;; Motion
exists ;; Presence
decided ;; Deciding || start ;; Process_start
tournament ;; Competition || played ;; Participation || refer ;; Request
merge ;; Cause_to_amalgamate || following ;; Action
promoted ;; Cause_to_make_progress || promoted ;; Cause_to_make_progress
win ;; Earnings_and_losses || Scoring ;; Getting || tries ;; Creating || winning ;; Getting
record ;; Recording || forming ;; Coming_to_be || form ;; Coming_to_be
caused ;; Causation || formed ;; Coming_to_be || damage ;; Damaging || passing ;; Motion_directional || cyclone ;; Catastrophe
intensified ;; Cause_change_of_strength || traversed ;; Motion_directional || storm ;; Catastrophe || marking ;; Recording
landfall ;; Arriving
emerged ;; Coming_to_be || crossing ;; Motion_directional || reorganized ;; Cause_to_amalgamate
hurricane ;; Catastrophe || struck ;; Attack || dissipated ;; Dispersal
caused ;; Causation || damages ;; Damaging || damage ;; Damaging || recorded ;; Recording || causing ;; Causation || fatalities ;; Death
storm ;; Catastrophe || retired ;; Process_end
capture ;; Conquering || Fall ;; Conquering || Liberation ;; Change_of_leadership
start ;; Process_start || marked ;; Recording || end ;; Process_end || War ;; Hostile_encounter || transition ;; Change || reunification ;; Cause_to_amalgamate
began ;; Process_start || final ;; Process_end || attack ;; Attack || forces ;; Hostile_encounter || bombardment ;; Attack || command ;; Control || commanded ;; Control || suffering ;; Bodily_harm
killed ;; Killing || killed ;; Killing || combat ;; Hostile_encounter || bombardment ;; Attack
occupied ;; Control || raised ;; Self_motion
renamed ;; Name_conferral
capture ;; Conquering || associated ;; Collaboration || Operation ;; Military_operation || evacuation ;; Removing || preceded ;; Process_start
evacuation ;; Removing || evacuation ;; Removing
end ;; Process_end || war ;; Hostile_encounter || decline ;; Cause_change_of_position_on_a_scale || flight ;; Self_motion || contributed ;; Influence
incident ;; Catastrophe
built ;; Building || overlooking ;; Perception_active
consisting ;; Cause_to_be_included || defensive ;; Defending || formed ;; Cause_to_amalgamate
defended ;; Defending || arrived in ;; Arriving
captured ;; Conquering || bombardments ;; Attack || counter-attacks ;; Attack
attacked ;; Attack || bombardment ;; Attack
got across ;; Motion || using ;; Using || defeated ;; Conquering
captured ;; Conquering || gained ;; Getting || repulsed ;; Defending
cyclone ;; Catastrophe || carved ;; Causation
formed ;; Coming_to_be || storm ;; Catastrophe
strengthened ;; Cause_change_of_strength
turned ;; Motion || attained ;; Getting
crossing ;; Motion_directional || accelerated ;; Cause_change_of_position_on_a_scale || transitioned ;; Change
determined ;; Statement || lost ;; Earnings_and_losses
caused ;; Causation || flooding ;; Catastrophe
became ;; Becoming || causing ;; Causation || rainfall ;; Catastrophe || occurring ;; Presence
merged ;; Cause_to_amalgamate || struck ;; Attack || contributed ;; Causation
compounded ;; Cause_to_amalgamate || came ;; Motion
became ;; Becoming
turned ;; Motion
caused ;; Causation
lost ;; Earnings_and_losses
fought ;; Hostile_encounter || competing ;; Competition
fought ;; Hostile_encounter
break ;; Destroying || form ;; Creating || suppress ;; Hindering
included ;; Cause_to_be_included || alleged ;; Statement || resented ;; Perception_active
united ;; Cause_to_amalgamate || suppress ;; Hindering
fought ;; Hostile_encounter
attacked ;; Attack || negotiate ;; Communication
defeated ;; Conquering || pushed ;; Motion
withdrew ;; Escaping
taken ;; Conquering
discredited ;; Judgment_communication || generated ;; Causation
support ;; Supporting || supported ;; Supporting || merged ;; Cause_to_amalgamate
bringing ;; Bringing
culminated ;; Process_end || crushed ;; Destroying
sent ;; Sending
set off ;; Placing
given ;; Giving || found ;; Know
suffocated ;; Death
seen ;; Perception_active
impacted ;; Influence || cyclone ;; Catastrophe
developed ;; Coming_to_be || named ;; Name_conferral || hurricane ;; Catastrophe || hurricane ;; Catastrophe
deepened ;; Cause_change_of_strength || storm ;; Catastrophe || upgraded ;; Cause_to_make_progress
storm ;; Catastrophe || landfall ;; Motion
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || emerging ;; Coming_to_be
crossed ;; Motion_directional || accelerated ;; Cause_change_of_position_on_a_scale
merging ;; Cause_to_amalgamate || dissipated ;; Dispersal
ripped ;; Destroying || slammed ;; Motion
observed ;; Perception_active
moved ;; Motion || flooding ;; Catastrophe
washed ;; Destroying
damage ;; Damaging || suffered ;; Bodily_harm
destroyed ;; Destroying || suffered ;; Bodily_harm || impact ;; Influence
damaged ;; Damaging
drowned ;; Killing || cross ;; Motion_directional
received ;; Receiving || causing ;; Causation || damage ;; Damaging || flood ;; Catastrophe
ceremony ;; Social_event || games ;; Competition || took place ;; Hold
combined ;; Cause_to_amalgamate || opening ;; Process_start || mandated ;; Giving || hoisting ;; Placing || host ;; Social_event
Games ;; Competition || opened ;; Process_start
ceremony ;; Social_event || ceremony ;; Social_event || described ;; Action || seen ;; Perception_active
Opening ;; Process_start || Ceremony ;; Social_event || ceremony ;; Social_event || cast ;; Causation
production ;; Manufacturing || ensure ;; Preventing_or_letting || pre-recorded ;; Arranging
invasion ;; Attack || resulted in ;; Causation
consisted ;; Cause_to_be_included || waged ;; Hold
resistance ;; Defending || described ;; Statement
control ;; Control
wars ;; Hostile_encounter || incorporated ;; Cause_to_amalgamate
taken ;; Conquering
Operation ;; Military_operation
destruction ;; Destroying || operation ;; Military_operation
varied ;; Change || involved ;; Cause_to_be_included || fighting ;; Hostile_encounter || fighting ;; Hostile_encounter
destruction ;; Destroying || capture ;; Conquering
improved ;; Cause_to_make_progress || fighting ;; Hostile_encounter || called ;; Name_conferral || War ;; Hostile_encounter
compounded ;; Cause_to_amalgamate || command ;; Control || control ;; Control || battle ;; Hostile_encounter || plagued ;; Bodily_harm || forces ;; Military_operation
defence ;; Defending || suffered ;; Bodily_harm || worsening ;; Cause_change_of_position_on_a_scale
battle ;; Hostile_encounter || War ;; Hostile_encounter || led to ;; Causation
saw ;; Self_motion
produced ;; Manufacturing
took place ;; Process_start
took place ;; Process_start
become ;; Becoming
win ;; Getting || becoming ;; Becoming || defeated ;; Conquering
defeated ;; Conquering || retain ;; Preserving
match ;; Competition || defeated ;; Conquering
moved ;; Motion || held ;; Defending
held ;; Defending || replaced ;; Change_of_leadership
reviews ;; Statement
rating ;; Statement
received ;; Receiving || match ;; Competition
event ;; Social_event || produced ;; Creating
took place ;; Process_start
event ;; Social_event || replaced ;; Removing
contested ;; Competition
retain ;; Getting
received ;; Receiving || event ;; Social_event
competition ;; Competition
decided ;; Deciding
received ;; Receiving || played ;; Competition
contested ;; Competition
beat ;; Competition
drawn ;; Motion
scored ;; Getting || took ;; Conquering
scored ;; Getting
scored ;; Getting || reduced ;; Cause_change_of_position_on_a_scale
win ;; Getting
caused ;; Causation
replaced ;; Change_of_leadership
organized ;; Arranging || competition ;; Competition
played ;; Competition || defeated ;; Conquering
competed ;; Competition || put up ;; Placing
tournament ;; Competition
get ;; Getting || receive ;; Receiving
received ;; Receiving
started ;; Process_start || started ;; Process_start || invasion ;; Attack || conquest ;; Conquering || establishing ;; Creating
received ;; Receiving || recognition ;; Judgment_communication || remained ;; Presence
created ;; Creating || defense ;; Defending
included ;; Cause_to_be_included
received ;; Receiving || support ;; Supporting || support ;; Supporting
deploying ;; Arranging || intervened ;; Preventing_or_letting
supported ;; Supporting || telegraph ;; Communication
floods ;; Catastrophe || killed ;; Killing || flooding ;; Catastrophe
floods ;; Catastrophe || rainfall ;; Motion || originated ;; Coming_to_be || produced ;; Causation
reached ;; Arriving
received ;; Receiving || damage ;; Damaging || flooding ;; Catastrophe || left ;; Causation
damaged ;; Damaging || deaths ;; Death || crashed ;; Catastrophe
floods ;; Catastrophe || left ;; Causation
floods ;; Catastrophe
destroyed ;; Destroying || deaths ;; Death
flooding ;; Catastrophe || killed ;; Killing || mudslides ;; Catastrophe || caused ;; Causation
flooding ;; Catastrophe || deaths ;; Death
became ;; Becoming || spawned ;; Causation || storm ;; Catastrophe
held ;; Hold
held ;; Hold || Established ;; Creating
takes place ;; Process_start
starts ;; Process_start || runs ;; Self_motion
attended ;; Participation
participate ;; Participation
received ;; Receiving || attained ;; Getting
increase ;; Cause_change_of_position_on_a_scale
began ;; Process_start || ended ;; Process_end || supported ;; Supporting
attract ;; Influence || played ;; Competition || played ;; Competition
building ;; Building
received ;; Receiving || become ;; Becoming || debuted ;; Process_start
produced ;; Manufacturing || took place ;; Process_start
earn ;; Earnings_and_losses || facing ;; Perception_active
receive ;; Receiving
match ;; Competition || scoring ;; Getting
took place ;; Process_start
defeated ;; Conquering || becoming ;; Becoming
match ;; Competition
decide ;; Deciding || promoted ;; Cause_to_make_progress || involved ;; Cause_to_be_included
defeated ;; Conquering || chosen ;; Choosing
stating ;; Statement || posted ;; Sending
became ;; Becoming || Wrestling ;; Competition
produced ;; Manufacturing || wrestling ;; Competition || took place ;; Process_start || event ;; Social_event
held ;; Hold
consisted ;; Cause_to_be_included
attacked ;; Attack || lose it ;; Competition || become ;; Becoming || defeated ;; Conquering
match ;; Competition || defeated ;; Conquering || defeated ;; Conquering
received ;; Receiving
played ;; Competition
determined ;; Deciding || received ;; Receiving || tournament ;; Competition
tournament ;; Competition || receive ;; Receiving
tournament ;; Competition
played ;; Competition
tournament ;; Competition || defending ;; Defending
organised ;; Arranging || tournament ;; Competition
play ;; Competition || tournament ;; Competition || due ;; Causation || scheduled ;; Arranging
played ;; Competition || matches ;; Competition
ended ;; Process_end
replay ;; Competition || defeated ;; Conquering
played ;; Competition || rise ;; Cause_change_of_position_on_a_scale
competitions ;; Competition
win ;; Getting || finish ;; Process_end
allowed ;; Preventing_or_letting
received ;; Receiving
match ;; Competition || played ;; Competition || determine ;; Deciding
ended ;; Process_end || defeated ;; Preventing_or_letting
topped ;; Placing || stood ;; Placing
semi-finals ;; Competition || defeated ;; Preventing_or_letting
opted ;; Choosing || Winning ;; Getting
scored ;; Getting || loss ;; Earnings_and_losses
scored ;; Getting
took ;; Action
build ;; Building || failed ;; Earnings_and_losses
reached ;; Arriving || earned ;; Earnings_and_losses
named ;; Name_conferral
concert ;; Social_event
included ;; Cause_to_be_included
select ;; Choosing
ended ;; Process_end || kicked off ;; Motion
sponsored ;; Participation
record ;; Recording || set ;; Placing
starting ;; Process_start || finals ;; Process_end || took place ;; Process_start
played ;; Competition
competition ;; Competition || competing ;; Competition
contested ;; Competition || remove ;; Removing || voted ;; Choosing
inclusion ;; Cause_to_be_included || meeting ;; Social_event || rejected ;; Agree_or_refuse_to_act
conducted ;; Action || following ;; Motion
involved ;; Cause_to_be_included
supported ;; Supporting
commenced ;; Process_start || included ;; Cause_to_be_included || intensified ;; Cause_change_of_strength
left ;; Departing
invited ;; Request || returned ;; Arriving
overwhelmed ;; Destroying || collapsed ;; Catastrophe
became ;; Becoming || become ;; Becoming || approve ;; Agree_or_refuse_to_act || voted ;; Choosing
created ;; Creating || occurred ;; Coming_to_be || replaced ;; Change_of_leadership || produced ;; Creating || resulted in ;; Causation
helped ;; Assistance
competition ;; Competition
held ;; Hold || conclusion ;; Process_end || included ;; Cause_to_be_included
continued ;; Hold
scored ;; Getting || innings ;; Social_event || taken ;; Getting
matches ;; Competition || held ;; Hold || hosting ;; Social_event || Due ;; Causation
contested ;; Competition || final ;; Competition
held ;; Hold || earned ;; Earnings_and_losses || host ;; Social_event || finishing ;; Process_end || choose ;; Choosing || Due ;; Causation || final ;; Competition || final ;; Competition
opted ;; Choosing
finishing ;; Process_end || final ;; Competition || drawn ;; Process_end
named ;; Name_conferral || leading ;; Motion || took ;; Getting
combat ;; Hostile_encounter || operation ;; Military_operation
includes ;; Cause_to_be_included
operation ;; Military_operation || run ;; Motion
accomplish ;; Achieve || assigned ;; Choosing
Force ;; Military_operation
announced ;; Expressing_publicly || took over ;; Conquering
consists ;; Cause_to_be_included
comprises ;; Cause_to_be_included
consists ;; Cause_to_be_included
killed ;; Killing
include ;; Cause_to_be_included
Championships ;; Competition
host ;; Social_event || selected ;; Choosing
changed ;; Change || made ;; Action || competition ;; Competition
exhibition ;; Social_event || added ;; Cause_to_be_included
provided ;; Supply
participated ;; Participation
comments ;; Statement
success ;; Earnings_and_losses || push ;; Causation
challenge ;; Competition
beat ;; Competition || sailed ;; Hold
became ;; Becoming || lose ;; Earnings_and_losses || win ;; Getting
held ;; Hold
organiser ;; Arranging
record ;; Recording || organiser ;; Arranging || appointed ;; Choosing
defended ;; Defending || used ;; Using
Championship ;; Competition || Championship ;; Competition
tournament ;; Competition || hosted ;; Social_event
tournament ;; Competition || entrants ;; Participation
competed ;; Participation
chosen ;; Choosing || tournament ;; Competition || finals ;; Process_end
competition ;; Competition || qualified ;; Earnings_and_losses
competition ;; Competition || qualified ;; Earnings_and_losses || finals ;; Competition
announced ;; Expressing_publicly || revelation ;; Causation || serve ;; Assistance
selected ;; Choosing
removed ;; Removing || due ;; Causation || replaced ;; Change_of_leadership
debuted ;; Process_start || seen ;; Perception_active
injured ;; Bodily_harm || replaced ;; Change_of_leadership
ran ;; Self_motion || festival ;; Social_event
included ;; Cause_to_be_included || called ;; Name_conferral || festival ;; Social_event
started ;; Process_start || named ;; Name_conferral || held ;; Hold
folded ;; Cause_to_amalgamate || exited ;; Departing
picked up ;; Choosing || organized ;; Arranging || arranging ;; Arranging
replaced ;; Change_of_leadership
recognized ;; Know || known ;; Know
recognised ;; Know
formed ;; Coming_to_be
issued ;; Creating
named ;; Name_conferral || designated ;; Choosing
upgraded ;; Cause_change_of_position_on_a_scale
upgraded ;; Cause_change_of_position_on_a_scale
reported ;; Reporting || reached ;; Achieve || made ;; Causation
weakened ;; Cause_change_of_strength || declared ;; Expressing_publicly || reaching ;; Achieve || downgraded ;; Cause_change_of_position_on_a_scale || issued ;; Creating
issue ;; Creating
damaged ;; Damaging
caused ;; Causation || killed ;; Killing || injured ;; Bodily_harm
took place ;; Hold
marathon ;; Competition
Organized ;; Arranging || held ;; Hold
started ;; Process_start
competition ;; Competition || opted ;; Choosing || take up ;; Participation || reaching ;; Arriving || running ;; Motion || held ;; Hold || registered ;; Recording
races ;; Competition
racing ;; Competition
composed ;; Come_together || held ;; Defending
election ;; Change_of_leadership || held ;; Defending
election ;; Change_of_leadership || taken place ;; Process_start
consisted ;; Cause_to_be_included || elected ;; Choosing
summoned ;; Request || meeting ;; Social_event
enjoyed ;; Perception_active
festival ;; Social_event || held ;; Hold
took place ;; Process_start
promote ;; Cause_to_make_progress
choose ;; Choosing || holds ;; Hold || selection ;; Choosing
became ;; Becoming
represented ;; Presence
started ;; Process_start
hosted ;; Social_event || took place ;; Process_start
hosted ;; Social_event || competed ;; Competition || defending ;; Defending
defended ;; Defending || achieving ;; Achieve || defeating ;; Conquering
defeated ;; Conquering
campaigns ;; Military_operation || remembered ;; Perception_active
assassinated ;; Killing
destruction ;; Destroying || culminating ;; Process_end || refusal ;; Agree_or_refuse_to_act
campaigns ;; Military_operation || carried out ;; Military_operation
recorded ;; Recording || campaigns ;; Military_operation
destruction ;; Destroying || death ;; Death || welcomed ;; Judgment_communication
reached ;; Achieve
included ;; Cause_to_be_included || bring ;; Bringing || included ;; Cause_to_be_included
War ;; Hostile_encounter || took place ;; Process_start
wars ;; Hostile_encounter || fought ;; Hostile_encounter
loss ;; Earnings_and_losses || reduced ;; Cause_change_of_position_on_a_scale
governed ;; Control
achieved ;; Achieve
attack ;; Attack
bombing ;; Attack || mission ;; Military_operation
resulted in ;; Causation
lost ;; Earnings_and_losses
raid ;; Attack || war ;; Hostile_encounter || referred ;; Name_conferral
service ;; Supply || awards ;; Award
Copa ;; Competition || hosted ;; Social_event
organized ;; Arranging
tournament ;; Competition
participate ;; Participation || tournament ;; Competition || making ;; Creating || competing ;; Competition
became ;; Becoming || feat ;; Achieve || tournament ;; Competition || Copa ;; Competition || Cup ;; Competition
caused ;; Causation || deaths ;; Death || Cyclone ;; Catastrophe || affected ;; Influence
developed ;; Coming_to_be || Cyclone ;; Catastrophe
moved ;; Motion || crossing ;; Motion_directional || forming ;; Coming_to_be
began ;; Process_start || cyclone ;; Catastrophe || strengthen ;; Cause_change_of_strength || turned ;; Motion
reached ;; Achieve
increase ;; Cause_change_of_position_on_a_scale || weakening ;; Cause_change_of_strength
moved back ;; Motion || cyclone ;; Catastrophe || passed ;; Motion_directional || turned ;; Motion
weakened ;; Cause_change_of_strength || re-entering ;; Arriving || affecting ;; Influence || turned ;; Motion
cyclone ;; Catastrophe
entered ;; Arriving || intensification ;; Cause_change_of_position_on_a_scale || proceeding ;; Motion
weakening ;; Cause_change_of_strength || recurved ;; Motion
began ;; Process_start || cyclone ;; Catastrophe || divert ;; Motion || move ;; Motion || landfall ;; Arriving
transitioned ;; Change || bringing ;; Bringing
caused ;; Causation || cyclone ;; Catastrophe || deaths ;; Death || affecting ;; Influence
drowned ;; Death || storm ;; Catastrophe || route ;; Motion
became ;; Becoming || died ;; Death || occurred ;; Coming_to_be || trapped ;; Bodily_harm
caused ;; Causation || drownings ;; Death || death ;; Death || flooding ;; Catastrophe || took place ;; Process_start
usage ;; Using || retired ;; Removing
Awards ;; Achieve
award ;; Award || winning ;; Getting
awarded ;; Giving
becomes ;; Becoming
named ;; Name_conferral
named ;; Name_conferral || named ;; Name_conferral
named ;; Name_conferral
awarded ;; Award
awarded ;; Giving
named ;; Name_conferral
given ;; Giving
awarded ;; Giving
produced ;; Creating
storm ;; Catastrophe || originated ;; Coming_to_be || transitioned ;; Change
hurricane ;; Catastrophe || Accelerating ;; Cause_change_of_position_on_a_scale || strengthened ;; Cause_change_of_strength || attaining ;; Achieve
began ;; Process_start || became ;; Becoming || weakening ;; Cause_change_of_strength
storm ;; Catastrophe || transition ;; Change
brought ;; Bringing || triggering ;; Causation || Drifting ;; Motion
Damage ;; Damaging
recurving ;; Motion || proceeded ;; Self_motion || affecting ;; Influence
Championship ;; Competition || held ;; Creating
participated ;; Participation
tournament ;; Competition || beating ;; Competition
conceived ;; Action
victory ;; Getting
played ;; Competition
lost ;; Earnings_and_losses || promoted ;; Achieve || take part ;; Dispersal
impact ;; Influence || cyclone ;; Catastrophe
noted ;; Know
developed ;; Coming_to_be || developed ;; Coming_to_be || named ;; Name_conferral
became ;; Becoming || move ;; Motion || proceeded ;; Self_motion || passed ;; Motion_directional
becoming ;; Becoming || cyclones ;; Catastrophe || reached ;; Achieve
maintained ;; Statement || cyclone ;; Catastrophe
started ;; Process_start || weakening ;; Cause_change_of_strength || passing ;; Motion_directional
absorbed ;; Control
competition ;; Competition
win ;; Earnings_and_losses || appearing ;; Presence
reached ;; Arriving
win ;; Earnings_and_losses || reach ;; Arriving
progressed ;; Achieve || losing ;; Earnings_and_losses || winning ;; Earnings_and_losses
begin ;; Process_start
competition ;; Competition
lost ;; Earnings_and_losses || finals ;; Competition
played ;; Competition || received ;; Getting
reach ;; Achieve || progressed ;; Cause_to_make_progress || progressed ;; Cause_to_make_progress
progressed ;; Cause_to_make_progress || beat ;; Competition
Watched ;; Perception_active || took ;; Getting || scored ;; Getting
scored ;; Getting
took ;; Getting
went to ;; Motion
missing ;; Earnings_and_losses || win ;; Earnings_and_losses
lead to ;; Causation
reached ;; Arriving || beaten ;; Competition
faltered ;; Cause_change_of_strength
reaching ;; Arriving
began ;; Process_start
tracked ;; Motion
began ;; Process_start
assigned ;; Choosing
attained ;; Achieve
increased ;; Cause_change_of_position_on_a_scale
dissipated ;; Removing
brushed ;; Motion
support ;; Supporting
organized ;; Arranging || supported ;; Supporting
took place ;; Process_start
achieve ;; Achieve
presented ;; Presence
included ;; Cause_to_be_included
participate ;; Participation
help ;; Assistance || organize ;; Arranging || mobilized ;; Arranging
took place ;; Process_start
tournament ;; Competition || Championship ;; Competition
contested ;; Competition
participated ;; Participation || championships ;; Competition || joined ;; Participation
tournament ;; Competition || finishing ;; Process_end || promoted ;; Achieve
agreed ;; Agree_or_refuse_to_act || tournaments ;; Competition || include ;; Cause_to_be_included || included ;; Cause_to_be_included || designed ;; Creating || asked ;; Request
recorded ;; Recording || victory ;; Earnings_and_losses || defeating ;; Conquering
held ;; Hold
played ;; Competition
given ;; Giving || came ;; Bringing
offer ;; Supply
gained ;; Getting || qualified ;; Earnings_and_losses
achieved ;; Achieve || beating ;; Competition || finish ;; Getting
beating ;; Competition
named ;; Name_conferral
developed ;; Coming_to_be
upgraded ;; Cause_change_of_position_on_a_scale
attained ;; Achieve
caused ;; Causation
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
tournament ;; Competition
tournament ;; Competition || held ;; Hold
emerged ;; Coming_to_be || defeating ;; Preventing_or_letting || final ;; Competition
declared ;; Expressing_publicly || declared ;; Expressing_publicly || awarded ;; Award || final ;; Competition
performed ;; Hold || tournament ;; Competition || ceremony ;; Social_event
includes ;; Cause_to_be_included
awarded ;; Award
appeared ;; Coming_to_be
eliminated ;; Removing
competed ;; Competition
developed ;; Coming_to_be
strengthened ;; Cause_change_of_strength
strengthened ;; Cause_change_of_strength || reached ;; Achieve
landfall ;; Arriving
peaked ;; Cause_change_of_position_on_a_scale
moved ;; Motion || landfall ;; Arriving
began ;; Process_start
turned ;; Motion
produced ;; Manufacturing
destroyed ;; Destroying
caused ;; Causation
caused ;; Causation
destroyed ;; Destroying || damaged ;; Damaging || suffered ;; Bodily_harm
destroyed ;; Destroying || inflicted ;; Causation
occurred ;; Coming_to_be
weakened ;; Cause_change_of_strength
spawned ;; Causation
competition ;; Competition
tournament ;; Competition || held ;; Hold
contested ;; Competition
qualification ;; Competition || held ;; Hold
promoted ;; Achieve || championships ;; Competition
qualification ;; Competition
Championships ;; Competition || represent ;; Presence
conducted ;; Action || contributes ;; Cause_change_of_position_on_a_scale
record ;; Recording
celebrated ;; Social_event || accomplishments ;; Achieve || lasted ;; Process_end
offers ;; Giving
announced ;; Expressing_publicly || concert ;; Social_event
competition ;; Competition || awarded ;; Award
fixtures ;; Competition || played ;; Competition
changed ;; Change
tournament ;; Competition
compete ;; Competition || division ;; Competition
contest ;; Competition || ranked ;; Placing
competition ;; Competition
play ;; Competition || playing ;; Competition
awarded ;; Giving
competition ;; Competition || altered ;; Change
replaced ;; Change
enlarged ;; Cause_change_of_position_on_a_scale
relegated ;; Judgment_communication || promoted ;; Achieve
placed ;; Placing || relegated ;; Judgment_communication || promoted ;; Achieve
qualifying ;; Earnings_and_losses
tournament ;; Competition
organized ;; Arranging || held ;; Hold || competition ;; Competition
held ;; Hold
defeating ;; Preventing_or_letting || giving ;; Giving || final ;; Process_end
earned ;; Earnings_and_losses || achieved ;; Achieve || reach ;; Achieve || winning ;; Earnings_and_losses
failing ;; Earnings_and_losses || defending ;; Defending || knocked out ;; Competition || got ;; Getting
earned ;; Earnings_and_losses || held ;; Hold
finished ;; Process_end || defeating ;; Preventing_or_letting
Championships ;; Competition
participated ;; Participation || competition ;; Competition || tournament ;; Competition || dissolution ;; Dispersal || formation ;; Creating
competition ;; Competition || competition ;; Competition || qualifications ;; Competition
played ;; Competition || tournament ;; Competition || games ;; Competition || took place ;; Process_start
advancing ;; Cause_to_make_progress || quarter-finals ;; Competition || split ;; Change
become ;; Becoming || beat ;; Competition || final ;; Competition
game ;; Competition || victory ;; Earnings_and_losses || tied ;; Competition || scored ;; Competition || seal ;; Getting
win ;; Earnings_and_losses || game ;; Competition || overtime ;; Competition || scored ;; Competition
finished ;; Process_end || lose ;; Earnings_and_losses || created ;; Creating || tournament ;; Competition || tournament ;; Competition || dissolution ;; Dispersal || competing ;; Competition
known ;; Know || impact ;; Influence
Originating ;; Coming_to_be || tracked ;; Self_motion
moved ;; Motion || Crossing ;; Motion_directional || attained ;; Getting
reached ;; Arriving
weakened ;; Cause_change_of_strength || landfall ;; Self_motion
striking ;; Attack || dissipating ;; Dispersal || entered ;; Arriving
produced ;; Creating
destroyed ;; Destroying || damaged ;; Damaging || took place ;; Process_start
triggered ;; Causation
strengthened ;; Cause_change_of_strength || formed ;; Coming_to_be
brushed ;; Removing || landfall ;; Self_motion
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
caused ;; Causation || killed ;; Killing
inflicted ;; Causation
cyclone ;; Catastrophe
began ;; Process_start || storm ;; Catastrophe || storms ;; Catastrophe || Forming ;; Coming_to_be || crossed ;; Motion_directional
landfall ;; Arriving || made ;; Causation
weakened ;; Cause_change_of_strength || moving ;; Motion || dissipated ;; Dispersal
storm ;; Catastrophe || hurricane ;; Catastrophe || Damage ;; Damaging || cut off ;; Removing
damage ;; Damaging || losses ;; Earnings_and_losses || suffered ;; Bodily_harm
hurricane ;; Catastrophe || died ;; Death
engaged ;; Participation
blamed ;; Judgment_communication || dispersed ;; Dispersal
fled ;; Escaping
existed ;; Presence || hurricane ;; Catastrophe
tracked ;; Motion || entering ;; Arriving
developed ;; Cause_to_make_progress || organized ;; Cause_change_of_position_on_a_scale
strengthened ;; Cause_change_of_strength || earning ;; Getting
strengthening ;; Cause_change_of_strength || started ;; Process_start || intensify ;; Cause_change_of_strength || hurricane ;; Catastrophe
moved ;; Motion || reaching ;; Arriving
dissipating ;; Dispersal || hurricane ;; Catastrophe || weakened ;; Cause_change_of_strength || degenerated ;; Change
storm ;; Catastrophe || brought ;; Bringing
record ;; Recording || storm ;; Catastrophe
named ;; Name_conferral || hurricane ;; Catastrophe
aided ;; Assistance || formed ;; Coming_to_be
took ;; Conquering || making ;; Manufacturing
flooding ;; Motion || torrential ;; Motion
blamed ;; Judgment_communication || flooding ;; Motion || record ;; Recording
dissipated ;; Dispersal
storms ;; Catastrophe || hurricane ;; Catastrophe || Hurricane ;; Catastrophe || make ;; Causation
developed ;; Coming_to_be || named ;; Name_conferral || storm ;; Catastrophe || hurricane ;; Catastrophe
became ;; Becoming || hurricane ;; Catastrophe || Curving ;; Motion
began ;; Process_start || moved ;; Motion || reached ;; Arriving || storm ;; Catastrophe || weaken ;; Cause_change_of_strength || encountered ;; Motion || downgraded ;; Cause_change_of_position_on_a_scale || making ;; Causation
dissipated ;; Dispersal || storm ;; Catastrophe || weakened ;; Cause_change_of_strength
caused ;; Causation || damage ;; Damaging || storm ;; Catastrophe || resulting in ;; Causation
Flooding ;; Motion || deaths ;; Death || damage ;; Damaging || resulted in ;; Causation
brought ;; Bringing || cyclone ;; Catastrophe
storm ;; Catastrophe || hurricane ;; Catastrophe || originated ;; Coming_to_be
developed ;; Coming_to_be || dissipated ;; Dispersal || storm ;; Catastrophe || reaching ;; Arriving
made ;; Manufacturing
produced ;; Manufacturing || resulted in ;; Causation || death ;; Death
killed ;; Killing
deaths ;; Death || occurred ;; Coming_to_be
dissipated ;; Dispersal
killed ;; Killing || stated ;; Statement
caused ;; Causation || damage ;; Damaging
used ;; Using || caused ;; Causation || damages ;; Damaging
fought ;; Hostile_encounter
conquered ;; Conquering || proclaimed ;; Statement
hearing ;; Know || marched ;; Self_motion
clashed ;; Hostile_encounter
held ;; Defending
wounded ;; Bodily_harm || fell ;; Motion_directional
dispersed ;; Dispersal || Seeing ;; Know || going ;; Motion
beheaded ;; Killing || captured ;; Conquering
ended ;; Process_end
contributed ;; Cause_to_make_progress
developed ;; Coming_to_be
moved ;; Motion
intensified ;; Cause_change_of_strength
indicated ;; Communication
brought ;; Bringing
evacuated ;; Removing
spread ;; Dispersal
damaged ;; Damaging || destroyed ;; Destroying
result in ;; Causation || capsized ;; Destroying
landfall ;; Self_motion
destruction ;; Destroying || brought ;; Bringing
caused ;; Causation || damage ;; Damaging || killed ;; Killing || flooded ;; Motion
existed ;; Presence
strengthened ;; Cause_change_of_strength || formed ;; Coming_to_be || recurved ;; Self_motion
Hurricane ;; Catastrophe
landfall ;; Self_motion || dissipated ;; Dispersal
hurricane ;; Catastrophe || impact ;; Influence
began ;; Process_start || Forming ;; Coming_to_be
intensified ;; Cause_change_of_strength || tracked ;; Self_motion
acquired ;; Getting || turned ;; Motion
attained ;; Getting || hurricane ;; Catastrophe || intensified ;; Cause_change_of_strength
hurricane ;; Catastrophe || weakened ;; Cause_change_of_strength
dissipated ;; Dispersal || weakened ;; Cause_change_of_strength
produced ;; Creating
caused ;; Causation
resulted ;; Causation || spawned ;; Causation
resulted ;; Causation || hurricane ;; Catastrophe
existed ;; Presence
occurred ;; Coming_to_be
started ;; Process_start
spread ;; Dispersal || damaged ;; Damaging
died ;; Death
injured ;; Bodily_harm || lost ;; Death
celebration ;; Social_event
invited ;; Request
cyclone ;; Catastrophe || strike ;; Attack
cyclone ;; Catastrophe || formed ;; Coming_to_be
storm ;; Catastrophe || intensified ;; Cause_change_of_position_on_a_scale
began ;; Process_start || storm ;; Catastrophe || due ;; Causation
developed ;; Coming_to_be || cyclone ;; Catastrophe || storm ;; Catastrophe || approaching ;; Arriving
cyclone ;; Catastrophe || landfall ;; Arriving || made ;; Causation
storm ;; Catastrophe || spread ;; Dispersal || weakened ;; Cause_change_of_strength
cyclone ;; Catastrophe || storm ;; Catastrophe || reached ;; Motion
cyclone ;; Catastrophe || storm ;; Catastrophe || storms ;; Catastrophe || report ;; Reporting
caused ;; Causation || cyclone ;; Catastrophe || damaging ;; Damaging || destroying ;; Destroying || damage ;; Damaging || homeless ;; Causation
damage ;; Damaging || damaged ;; Damaging || resulting in ;; Causation
storm ;; Catastrophe || Damage ;; Damaging || deaths ;; Death || death ;; Death
spread ;; Dispersal || knocked ;; Destroying || effects ;; Influence || capsized ;; Destroying
formed ;; Coming_to_be || organized ;; Arranging
fluctuated ;; Motion
intensified ;; Cause_change_of_strength
increased ;; Cause_change_of_position_on_a_scale || entered ;; Arriving
began ;; Process_start || decreased ;; Cause_change_of_position_on_a_scale
degenerated ;; Change || storm ;; Catastrophe
dissipated ;; Dispersal || drifted ;; Motion
prompted ;; Causation
brought ;; Bringing
contributed ;; Giving
launched ;; Process_start
wrecked ;; Damaging || massacred ;; Killing
landed ;; Self_motion
retreated ;; Escaping || deployed ;; Arranging || ambushed ;; Attack || skirmished ;; Hostile_encounter || disengaged ;; Escaping
killed ;; Killing || retreated ;; Escaping || ceased ;; Process_end || dispersing ;; Dispersal || retreating ;; Escaping
took ;; Conquering || began ;; Process_start || bringing ;; Bringing || hurricane ;; Catastrophe || hurricane ;; Catastrophe || records ;; Recording
developed ;; Coming_to_be || hurricane ;; Catastrophe || storm ;; Catastrophe || start ;; Process_start
storm ;; Catastrophe || intensified ;; Cause_change_of_strength
hurricane ;; Catastrophe || storm ;; Catastrophe || reached ;; Arriving || strength ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || reaching ;; Arriving
weakened ;; Cause_change_of_strength || turned ;; Becoming || storm ;; Catastrophe || due ;; Causation
made ;; Manufacturing || storm ;; Catastrophe || dissipating ;; Dispersal
formed ;; Coming_to_be
moved ;; Motion
strengthen ;; Cause_change_of_strength
weakening ;; Cause_change_of_strength || accelerating ;; Cause_change_of_position_on_a_scale
weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
caused ;; Causation
destroyed ;; Destroying
damaged ;; Damaging
destroyed ;; Destroying
destroyed ;; Destroying || damaged ;; Damaging
left ;; Departing
drowned ;; Killing || injured ;; Bodily_harm
declared ;; Expressing_publicly
declared ;; Expressing_publicly
organized ;; Arranging
tracked ;; Recording
re-strengthened ;; Cause_change_of_strength || weakened ;; Cause_change_of_strength
passed ;; Process_end
crossed ;; Motion_directional || landfall ;; Catastrophe
dissipated ;; Dispersal
washed ;; Removing
destroyed ;; Destroying
displaced ;; Change
caused ;; Causation || damage ;; Damaging || loss ;; Earnings_and_losses || widespread ;; Dispersal || hurricane ;; Catastrophe || hurricane ;; Catastrophe
formed ;; Coming_to_be
strengthening ;; Cause_change_of_strength || strength ;; Cause_change_of_strength || moved ;; Motion
crossed ;; Motion_directional || hurricane ;; Catastrophe
passed ;; Motion_directional || hurricane ;; Catastrophe || Weakening ;; Cause_change_of_strength
weakening ;; Cause_change_of_strength || becoming ;; Becoming || hurricane ;; Catastrophe || occurred ;; Presence || re-emerging ;; Process_start || downgraded ;; Cause_change_of_position_on_a_scale
re-strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || landfall ;; Arriving
weakened ;; Cause_change_of_strength
Hurricane ;; Catastrophe || cyclone ;; Catastrophe || strike ;; Attack
caused ;; Causation || drowning ;; Death || damage ;; Damaging || damaging ;; Damaging || hurricane ;; Catastrophe || recorded ;; Recording || Hurricane ;; Catastrophe || fatalities ;; Death || electrocution ;; Death || left ;; Causation || resulted in ;; Causation || making it ;; Causation
storm ;; Catastrophe || impact ;; Influence || ranking ;; Placing
known ;; Know
intensified ;; Cause_change_of_strength || Forming ;; Coming_to_be || moved ;; Motion
intensified ;; Cause_change_of_strength || attained ;; Getting
reached ;; Arriving
moved ;; Motion || took place ;; Process_start
struck ;; Attack || dissipating ;; Dispersal
losses ;; Earnings_and_losses
moved ;; Motion || took place ;; Process_start
caused ;; Causation || destroying ;; Destroying
start ;; Process_start || record ;; Recording || marked ;; Recording || record ;; Recording || hurricanes ;; Catastrophe
named ;; Name_conferral || developed ;; Coming_to_be || hurricane ;; Catastrophe || storm ;; Catastrophe || hurricane ;; Catastrophe
moved ;; Motion || strengthened ;; Cause_change_of_strength || attain ;; Getting || passing ;; Motion_directional
strengthened ;; Cause_change_of_strength || caused ;; Causation || devastated ;; Destroying || hurricanes ;; Catastrophe || reached ;; Arriving || reach ;; Arriving
hurricane ;; Catastrophe || produced ;; Creating || flooding ;; Motion || damage ;; Damaging
damaged ;; Damaging || disabled ;; Destroying || flooding ;; Motion
Damage ;; Damaging
produced ;; Manufacturing || causing ;; Causation || death ;; Death || injuries ;; Bodily_harm
hurricane ;; Catastrophe
named ;; Name_conferral || originated ;; Coming_to_be
became ;; Becoming || intensified ;; Cause_change_of_strength || coming ;; Motion || hurricane ;; Catastrophe
began ;; Process_start || emerging ;; Coming_to_be
made ;; Manufacturing
turning ;; Becoming || approaching ;; Arriving || causing ;; Causation || traversed ;; Motion_directional || induced ;; Causation || weaken ;; Cause_change_of_strength
causing ;; Causation || decrease ;; Cause_change_of_position_on_a_scale || allowing ;; Preventing_or_letting || regain ;; Earnings_and_losses
turned ;; Becoming || strength ;; Cause_change_of_strength || attained ;; Getting
change ;; Change || began ;; Process_start || completed ;; Process_end || making ;; Manufacturing
declared ;; Expressing_publicly || weakened ;; Cause_change_of_strength
broke ;; Destroying || becoming ;; Becoming || observed ;; Perception_active
Battle ;; Hostile_encounter || invasion ;; Attack || took place ;; Process_start
ended ;; Process_end || commanded ;; Control
began ;; Process_start || reached ;; Arriving || cross ;; Motion_directional
decided ;; Deciding || attack ;; Attack
killed ;; Killing || attacked ;; Attack || defeated ;; Conquering
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || headed ;; Self_motion || engage ;; Participation
moved ;; Motion
noted ;; Know
reached ;; Arriving || crossed ;; Motion_directional
named ;; Name_conferral
strike ;; Attack || brought ;; Bringing || damage ;; Damaging
damage ;; Damaging || brought ;; Bringing || flooding ;; Motion
strike ;; Attack || recorded ;; Recording
named ;; Name_conferral || developed ;; Coming_to_be
moved ;; Motion || passed ;; Motion_directional || intensifying ;; Cause_change_of_strength
reaching ;; Arriving || weakened ;; Cause_change_of_strength
regained ;; Getting || reorganized ;; Reforming_a_system
killed ;; Killing || known ;; Know
intensified ;; Cause_change_of_strength || causing ;; Causation
damage ;; Damaging
hurricane ;; Catastrophe || swept ;; Self_motion
formation ;; Coming_to_be || formed ;; Coming_to_be
strengthening ;; Cause_change_of_strength || storm ;; Catastrophe || hurricane ;; Catastrophe || disturbance ;; Attack
recurved ;; Self_motion || reached ;; Self_motion
weakened ;; Cause_change_of_strength || making ;; Change
Moving ;; Motion || transitioned ;; Change
passed ;; Motion_directional || curved ;; Self_motion || dissipating ;; Removing
caused ;; Causation || incidents ;; Catastrophe || hurricane ;; Catastrophe
incidents ;; Catastrophe || deaths ;; Death || resulted in ;; Causation
brought ;; Bringing
storm ;; Catastrophe || peaking ;; Cause_change_of_position_on_a_scale || sunk ;; Bodily_harm || damaged ;; Bodily_harm
hurt ;; Bodily_harm
services ;; Supply || disrupted ;; Damaging
damaged ;; Damaging
lost ;; Earnings_and_losses || production ;; Manufacturing || storm ;; Catastrophe || losses ;; Earnings_and_losses
caused ;; Causation || damages ;; Damaging || occurred ;; Presence
caused ;; Causation || hurricane ;; Catastrophe
strike ;; Attack
hurricane ;; Catastrophe
moved ;; Motion || strengthening ;; Cause_change_of_strength || formed ;; Coming_to_be
causing ;; Causation || damage ;; Damaging || crossed ;; Motion_directional
reached ;; Arriving || made ;; Manufacturing
damaged ;; Damaging || destroyed ;; Destroying
caused ;; Causation || damage ;; Damaging
weakening ;; Cause_change_of_strength || moved ;; Motion || caused ;; Causation || damage ;; Damaging
damage ;; Damaging
surpassing ;; Cause_change_of_position_on_a_scale || landfalling ;; Self_motion
storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
formed ;; Coming_to_be || strengthening ;; Cause_change_of_strength || passing ;; Motion_directional || hurricane ;; Catastrophe || attain ;; Getting
strengthened ;; Cause_change_of_strength || strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || upgraded ;; Cause_to_make_progress || downgraded ;; Cause_change_of_position_on_a_scale
damages ;; Damaging || causing ;; Causation || deaths ;; Death
impact ;; Influence
landfall ;; Arriving || Hurricane ;; Catastrophe || made ;; Action
left ;; Bringing || hurricane ;; Catastrophe || detected ;; Know
hurricane ;; Catastrophe || Hurricane ;; Catastrophe
observed ;; Know || storm ;; Catastrophe || strike ;; Attack
moved ;; Motion || passed ;; Motion_directional || moving ;; Motion
observed ;; Know || struck ;; Attack || became ;; Becoming
destructive ;; Catastrophe
game ;; Competition
game ;; Competition || played ;; Competition
victory ;; Getting
rushed ;; Motion
posted ;; Sending || added ;; Cause_change_of_position_on_a_scale
broke ;; Destroying
viewed ;; Perception_active || game ;; Competition
known ;; Know
followed ;; Self_motion
brought ;; Bringing
observed ;; Perception_active
evacuated ;; Removing
suffered ;; Bodily_harm || inundated ;; Destroying
cancelled ;; Change_event_time
downgraded ;; Cause_change_of_position_on_a_scale
Shield ;; Competition || Shield ;; Competition || match ;; Competition || played ;; Participation || League ;; Competition || Cup ;; Competition
match ;; Competition || beat ;; Competition || contested ;; Competition || League ;; Competition || final ;; Competition || Cup ;; Competition
Watched ;; Perception_active || match ;; Competition
changes ;; Change || started ;; Process_start || Final ;; Competition || made ;; Change || played ;; Participation
match ;; Competition || lead ;; Earnings_and_losses || scored ;; Getting
lead ;; Earnings_and_losses || extended ;; Cause_change_of_position_on_a_scale || added ;; Cause_to_be_included
scored ;; Getting
Final ;; Competition || played ;; Competition
competition ;; Competition || final ;; Competition || expanded ;; Cause_change_of_position_on_a_scale
beaten ;; Competition || final ;; Competition
beat ;; Competition || match ;; Competition
became ;; Becoming || win ;; Getting || win ;; Getting || competition ;; Competition || conference ;; Social_event || failed ;; Earnings_and_losses
became ;; Becoming || win ;; Getting || competition ;; Competition || played ;; Competition
watch ;; Perception_active || tuned ;; Self_motion
due ;; Causation || played ;; Participation || see ;; Perception_active
promoted ;; Cause_change_of_position_on_a_scale || promoted ;; Cause_change_of_position_on_a_scale
drops ;; Removing || replaces ;; Change
winning ;; Getting || Scoring ;; Getting
Sabotage ;; Destroying || took place ;; Process_start
flew ;; Motion || heard ;; Perception_active || broken ;; Destroying
exploded ;; Attack
injured ;; Bodily_harm
died ;; Death
occurred ;; Coming_to_be || earthquake ;; Catastrophe || earthquake ;; Catastrophe || earthquake ;; Catastrophe
shock ;; Catastrophe || originated ;; Coming_to_be
quake ;; Catastrophe || felt ;; Perception_active
felt ;; Perception_active
occurred ;; Coming_to_be || damage ;; Damaging
captured ;; Conquering || War ;; Hostile_encounter || fought ;; Hostile_encounter
fought ;; Hostile_encounter
defeated ;; Conquering
became ;; Becoming
control ;; Control || protect ;; Defending
seen ;; Perception_active
consolidated ;; Cause_change_of_strength
event ;; Competition || match ;; Motion
noted ;; Know
played ;; Competition || dating ;; Social_event
conduct ;; Action || invasions ;; Attack || embracing ;; Motion || promoted ;; Cause_to_make_progress
tolerated ;; Agree_or_refuse_to_act
event ;; Competition
held ;; Defending
held ;; Defending
grown ;; Cause_to_make_progress
concerts ;; Social_event
administered ;; Control
provided ;; Supply
held ;; Hold
started ;; Process_start || held ;; Hold
developed ;; Cause_to_make_progress
created ;; Creating
call ;; Communication
called ;; Communication
became ;; Becoming
perform ;; Hold
reached ;; Arriving
acquired ;; Getting
attended ;; Participation
wrestling ;; Competition || held ;; Hold
wrestling ;; Competition
starting ;; Process_start || held ;; Hold
announced ;; Expressing_publicly
competed ;; Competition || promoted ;; Cause_to_make_progress
compete ;; Competition || completed ;; Process_end || known ;; Know || determine ;; Deciding
took place ;; Process_start
tournament ;; Competition || competed ;; Competition
finished ;; Process_end || play ;; Competition
beating ;; Competition || win ;; Getting
ended ;; Process_end || advanced ;; Cause_to_make_progress || advanced ;; Cause_to_make_progress || match ;; Competition
claimed ;; Statement
made ;; Creating
competition ;; Competition || took ;; Getting
organized ;; Arranging
held ;; Hold
reduced ;; Cause_change_of_position_on_a_scale || remaining ;; Presence
participate ;; Participation
modified ;; Change || held ;; Hold
competition ;; Competition
changes ;; Change || allow ;; Preventing_or_letting || development ;; Cause_to_make_progress
qualified ;; Earnings_and_losses
hosts ;; Social_event || qualified ;; Earnings_and_losses || play ;; Competition || represented ;; Presence
contributed ;; Causation
killed ;; Killing || destroyed ;; Destroying
included ;; Cause_to_be_included
originated ;; Coming_to_be
produced ;; Manufacturing
improved ;; Cause_to_make_progress || led to ;; Causation
tournament ;; Competition || tournament ;; Competition || played ;; Participation
promoted ;; Cause_to_make_progress || promoted ;; Cause_to_make_progress
drops ;; Cause_change_of_position_on_a_scale
winner ;; Getting || replace ;; Change
Battle ;; Hostile_encounter || fought ;; Hostile_encounter || attack ;; Attack || set ;; Placing || Battle ;; Hostile_encounter
captured ;; Conquering || retreating ;; Escaping || force ;; Military_operation
attack ;; Attack || attack ;; Attack || capture ;; Conquering || retreat ;; Escaping || defending ;; Defending || capture ;; Conquering || force ;; Military_operation || proceeded ;; Self_motion || riding ;; Self_motion
attack ;; Attack || attacks ;; Attack || forces ;; Military_operation
began ;; Process_start || attacks ;; Attack || Battle ;; Hostile_encounter || commanded ;; Control
began ;; Process_start || defending ;; Defending || became ;; Becoming || held ;; Defending || began ;; Process_start || attack ;; Attack || attacks ;; Attack || capture ;; Conquering || Battle ;; Hostile_encounter
advanced ;; Cause_to_make_progress || attack ;; Attack || capture ;; Conquering || hold ;; Defending || leaving ;; Departing || retreating ;; Escaping || force ;; Military_operation
retreat ;; Escaping || captured ;; Conquering || attacked ;; Attack || determined ;; Deciding || fought ;; Hostile_encounter
end ;; Process_end
retreat ;; Escaping || capture ;; Conquering || resulted in ;; Causation
took place ;; Process_start
help ;; Assistance || involved ;; Cause_to_be_included
damaged ;; Damaging || killing ;; Killing || sunk ;; Catastrophe || boarded ;; Self_motion || exploding ;; Attack
taken ;; Conquering
took over ;; Change_of_leadership || sailed ;; Self_motion || sailed ;; Self_motion
Battle ;; Hostile_encounter || fought ;; Hostile_encounter || War ;; Hostile_encounter
sorties ;; Attack
planned ;; Arranging || assist ;; Assistance || exploit ;; Control
strengthened ;; Cause_change_of_strength || reduced ;; Cause_change_of_position_on_a_scale || attack ;; Attack || advanced ;; Self_motion || operation ;; Military_operation || saw ;; Self_motion
attack ;; Attack || made ;; Action
attacked ;; Attack || advanced ;; Self_motion
began ;; Process_start || attacked ;; Attack || suspended ;; Process_end || attack ;; Attack || sortie ;; Attack || bombardment ;; Attack
Festival ;; Social_event || festival ;; Social_event || takes place ;; Process_start
founded ;; Creating
festival ;; Social_event
serves ;; Assistance || held ;; Hold
produced ;; Manufacturing
founded ;; Creating || developed ;; Coming_to_be
occurred ;; Coming_to_be
created ;; Creating || named ;; Name_conferral || taken place ;; Process_start
Battle ;; Hostile_encounter || fought ;; Hostile_encounter
retreat ;; Escaping
ordered ;; Arranging
retreat ;; Escaping || ordered ;; Arranging || stop ;; Process_end || defend ;; Defending || support ;; Supporting
help ;; Assistance
allowed ;; Preventing_or_letting || resulted in ;; Causation
failed ;; Earnings_and_losses || enter ;; Arriving || take ;; Conquering || stabilized ;; Cause_change_of_strength
held ;; Defending
held ;; Defending || played ;; Competition
reigning ;; Control
involved ;; Cause_to_be_included
beating ;; Bodily_harm || attended ;; Participation
push ;; Self_motion || help ;; Assistance || played ;; Competition
competition ;; Competition
performed ;; Hold || tournament ;; Competition
organizers ;; Arranging || compete ;; Competition
began ;; Process_start
broke out ;; Process_start
aroused ;; Causation || formed ;; Coming_to_be
ended ;; Process_end
become ;; Becoming
served ;; Assistance
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || War ;; Hostile_encounter
defeated ;; Conquering || threw ;; Motion
battle ;; Hostile_encounter || engagements ;; Hostile_encounter || Battle ;; Hostile_encounter
given ;; Giving || casualties ;; Catastrophe || aid ;; Assistance || order ;; Request || commanded ;; Control || Battle ;; Hostile_encounter || turn ;; Motion
created ;; Creating || exploited ;; Control
clashed ;; Hostile_encounter
retreat ;; Escaping || escape ;; Escaping || loss ;; Earnings_and_losses
Blame ;; Judgment_communication || help ;; Assistance || order ;; Request || put on ;; Placing
war ;; Hostile_encounter || re-captured ;; Attack
tournament ;; Competition || played ;; Competition
began ;; Process_start || culminated ;; Process_end
tournament ;; Competition || Challenge ;; Competition || match ;; Competition || staged ;; Hold || replaced ;; Change
participating ;; Participation
matches ;; Competition || match ;; Competition || broadcast ;; Expressing_publicly || televised ;; Communication
matches ;; Competition || televised ;; Communication
tournament ;; Competition || final ;; Competition
took place ;; Process_start
started ;; Process_start || uprising ;; Change_of_leadership
joined ;; Participation
destruction ;; Destroying
defeated ;; Conquering
talks ;; Communication || submitted ;; Agree_or_refuse_to_act || forced ;; Causation
held ;; Hold || televised ;; Communication
reported ;; Reporting
formed ;; Coming_to_be
interacted ;; Communication
caused ;; Causation || flooded ;; Catastrophe
reached ;; Arriving || intensified ;; Cause_change_of_strength
became ;; Becoming || landfall ;; Self_motion
produced ;; Creating
flooded ;; Attack
produced ;; Creating
spawned ;; Causation
moved ;; Motion
caused ;; Causation
flooded ;; Catastrophe
contributed ;; Giving
moved ;; Motion
Stakes ;; Competition || Stakes ;; Competition || running ;; Action
run ;; Action || televised ;; Communication
Stakes ;; Competition || victories ;; Earnings_and_losses || complete ;; Process_end
race ;; Competition
placed ;; Placing || took ;; Conquering
finished ;; Process_end
race ;; Competition || increase ;; Cause_change_of_position_on_a_scale
Stakes ;; Competition || Attendance ;; Participation
record ;; Recording || race ;; Competition || handle ;; Hold || handle ;; Hold || set ;; Bringing
television ;; Communication
promoted ;; Creating || held ;; Hold
televised ;; Communication || held ;; Hold
carried ;; Earnings_and_losses
attempt ;; Hold
reported ;; Reporting
launched ;; Process_start
uprising ;; Change_of_leadership || joined ;; Participation || drive ;; Motion
named ;; Name_conferral || conflict ;; Hostile_encounter
began ;; Process_start || attacked ;; Attack || offended ;; Bodily_harm
killed ;; Killing || captured ;; Conquering || fleeing ;; Escaping || destroyed ;; Destroying
negotiations ;; Communication || end ;; Process_end || led to ;; Causation || came to ;; Motion
prompted ;; Causation || provoked ;; Causation || drive ;; Self_motion || uprising ;; Change_of_leadership || conflict ;; Hostile_encounter || modify ;; Change
crashed ;; Catastrophe || flying ;; Self_motion
killed ;; Killing
approach ;; Arriving || land ;; Motion || granted ;; Giving
entered ;; Arriving || impacted ;; Influence
broke ;; Destroying || damaging ;; Damaging || broke out ;; Process_start
championship ;; Competition || organized ;; Arranging
held ;; Hold
became ;; Becoming || tournament ;; Competition || win ;; Getting
tournament ;; Competition || awarded ;; Giving || host ;; Social_event || taken place ;; Process_start
move ;; Change || outbreak ;; Catastrophe || affected ;; Influence || prompted ;; Causation || hosted ;; Social_event
arrangements ;; Arranging || hosting ;; Social_event || compensation ;; Earnings_and_losses || granted ;; Giving || made ;; Action || host ;; Social_event
originated ;; Coming_to_be
intensified ;; Cause_change_of_strength
became ;; Becoming || Emerging ;; Coming_to_be
re-intensified ;; Cause_change_of_strength
pushed ;; Motion || weakened ;; Cause_change_of_strength
became ;; Becoming
produced ;; Manufacturing || caused ;; Causation
caused ;; Causation || flattened ;; Damaging
caused ;; Causation || killed ;; Killing
hurricane ;; Catastrophe || storms ;; Catastrophe
developed ;; Coming_to_be || moving ;; Motion || intensified ;; Cause_change_of_strength
increased ;; Cause_change_of_position_on_a_scale || strengthening ;; Cause_change_of_strength || hurricane ;; Catastrophe || passing ;; Self_motion
hurricane ;; Catastrophe || reached ;; Arriving
weakening ;; Cause_change_of_strength || destroying ;; Destroying || made ;; Manufacturing
killed ;; Killing || storm ;; Catastrophe
weakened ;; Cause_change_of_strength || hurricane ;; Catastrophe || storm ;; Catastrophe || re-intensified ;; Cause_change_of_strength
made ;; Manufacturing || dissipated ;; Cause_change_of_position_on_a_scale
Damage ;; Damaging || deaths ;; Death
damaged ;; Damaging
prompted ;; Causation
produced ;; Manufacturing || wrestling ;; Competition || event ;; Competition
event ;; Competition || took place ;; Hold
wrestling ;; Competition || match ;; Competition
defeated ;; Conquering || retain ;; Preserving
included ;; Cause_to_be_included
included ;; Cause_to_be_included || matches ;; Competition
intensified ;; Cause_change_of_strength
reached ;; Change
began ;; Process_start
weakened ;; Cause_change_of_strength || led to ;; Causation
produced ;; Manufacturing
triggered ;; Causation
killed ;; Killing
destroyed ;; Destroying || flooded ;; Catastrophe
deployed ;; Arranging
held ;; Hold || festival ;; Social_event || Festival ;; Social_event
festival ;; Social_event
Festival ;; Social_event || festival ;; Social_event
festival ;; Social_event || becoming ;; Becoming || removed ;; Removing || sponsorship ;; Supply || changed ;; Change || Festival ;; Social_event
founded ;; Creating || festival ;; Social_event || Festival ;; Social_event || management ;; Control || took over ;; Getting || production ;; Manufacturing
festival ;; Social_event || festival ;; Social_event || called ;; Name_conferral
included ;; Cause_to_be_included
given ;; Giving
supported ;; Supporting
revolted ;; Change_of_leadership
abolished ;; Removing
invaded ;; Attack
invade ;; Attack
given ;; Giving || defeated ;; Conquering
opposed ;; Agree_or_refuse_to_act || faced ;; Confronting_problem
opposed ;; Agree_or_refuse_to_act
defended ;; Defending
forced ;; Request
fought ;; Hostile_encounter
deposed ;; Change_of_leadership || fought ;; Hostile_encounter || defeated ;; Conquering
invaded ;; Attack || repelled ;; Defending
overthrew ;; Change_of_leadership
fled ;; Escaping || went to ;; Self_motion
supported ;; Supporting
captured ;; Conquering || defeated ;; Conquering
led to ;; Causation || go ;; Self_motion
contributed ;; Giving || uprising ;; Change_of_leadership || uprising ;; Change_of_leadership || start ;; Process_start
supported ;; Supporting || remained ;; Presence
defeated ;; Conquering || combat ;; Hostile_encounter
became ;; Becoming || controlled ;; Control || defeat ;; Conquering || defeat ;; Conquering
revolution ;; Change_of_leadership || defeat ;; Conquering || beginning ;; Process_start || beginning ;; Process_start || marked ;; Recording || opposed ;; Agree_or_refuse_to_act
held ;; Hold
held ;; Hold
held ;; Hold
added ;; Cause_to_be_included
replaced ;; Change_of_leadership
added ;; Cause_to_be_included
occurring ;; Coming_to_be || wars ;; Hostile_encounter
boosted ;; Cause_change_of_position_on_a_scale
revolted ;; Change_of_leadership
reached ;; Arriving || control ;; Control || seized ;; Getting
conflict ;; Hostile_encounter || collapsed ;; Destroying || crushed ;; Destroying
ceremony ;; Social_event || Olympics ;; Competition || opening ;; Process_start || took place ;; Hold
began ;; Process_start || finished ;; Process_end
filmed ;; Create_artwork || produced ;; Creating
ceremony ;; Social_event || opening ;; Process_start || presidency ;; Change_of_leadership
ceremony ;; Social_event || opening ;; Process_start || held ;; Hold
opened ;; Process_start || Games ;; Competition
attendance ;; Participation
sponsored ;; Supply || sanctioned ;; Agree_or_refuse_to_act
championship ;; Competition || took place ;; Hold
organize ;; Arranging || championship ;; Competition
opening ;; Process_start || ceremony ;; Social_event || opened ;; Process_start
competed ;; Competition
held ;; Hold || first ;; Process_start
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || War ;; Hostile_encounter
occurred ;; Presence
end ;; Process_end || move ;; Motion || fallen ;; Conquering || forced ;; Causation
requested ;; Request || supplies ;; Supply || retake ;; Conquering
request ;; Request || gave ;; Giving || wars ;; Hostile_encounter || involved ;; Cause_to_be_included || acceded ;; Agree_or_refuse_to_act
assaulted ;; Attack || returned ;; Arriving
captured ;; Conquering || killed ;; Killing || held ;; Bringing || threw ;; Motion
marked ;; Recording || start ;; Process_start || fighting ;; Hostile_encounter
beginning ;; Process_start || marked ;; Recording || evolved ;; Coming_to_be
game ;; Competition
game ;; Competition || played ;; Competition
final ;; Process_end
game ;; Competition || participate ;; Participation || invited ;; Request || injury ;; Bodily_harm
show ;; Social_event
launched ;; Process_start || occupied ;; Control
confrontations ;; Hostile_encounter
took place ;; Process_start
prompted ;; Causation || enact ;; Action || drive ;; Motion
formed ;; Coming_to_be || forcing ;; Request
losses ;; Earnings_and_losses || managed ;; Causation || beginning ;; Process_start || evade ;; Escaping || held ;; Defending
known ;; Know || took place ;; Process_start
Wounded ;; Bodily_harm
operation ;; Military_operation
by-pass ;; Motion || defeat ;; Conquering
became ;; Becoming || suffered ;; Bodily_harm || launched ;; Process_start || attack ;; Attack
destroyed ;; Destroying
reached ;; Arriving || relieving ;; Cure
forced ;; Motion || narrow ;; Cause_change_of_position_on_a_scale
ordered ;; Request || withdrew ;; Escaping
captured ;; Conquering || taking ;; Conquering
launched ;; Process_start || drove ;; Motion || counter-attack ;; Attack
followed ;; Motion
festival ;; Social_event
festival ;; Social_event || sponsored ;; Supply || inception ;; Coming_to_be
held ;; Hold || performed ;; Action
offered ;; Supply
festival ;; Social_event || culminated ;; Process_end
festival ;; Social_event || hosted ;; Social_event || provide ;; Supply
hosted ;; Social_event || return ;; Bringing
festival ;; Social_event || hosted ;; Social_event || brings ;; Bringing
festivals ;; Social_event
festival ;; Social_event
took place ;; Process_start
Held ;; Hold
race ;; Competition
competitors ;; Competition
sponsored ;; Supply
beat ;; Competition
began ;; Process_start
heating ;; Supply || initiation ;; Process_start
rose ;; Cause_change_of_position_on_a_scale
dropped ;; Motion
rose ;; Cause_change_of_position_on_a_scale || recorded ;; Recording
dissipated ;; Removing || moved ;; Motion
involves ;; Cause_to_be_included
support ;; Supply || designed ;; Creating
promoting ;; Cause_change_of_position_on_a_scale
organised ;; Arranging || managed ;; Hold
organised ;; Arranging || semi-finals ;; Competition || played ;; Participation || tournament ;; Competition || consisted ;; Cause_to_be_included || finals ;; Competition
tournaments ;; Competition || supposed ;; Request || called ;; Request || include ;; Cause_to_be_included
tournament ;; Competition || called ;; Request || due ;; Causation
tournament ;; Competition || schedule ;; Arranging || complete ;; Causation || sponsorship ;; Supply
tournaments ;; Competition || season ;; Competition
tournament ;; Competition
tournament ;; Competition
tournament ;; Competition || defeating ;; Earnings_and_losses || winners ;; Earnings_and_losses || final ;; Competition
race ;; Competition
race ;; Competition || draws ;; Getting
race ;; Competition || includes ;; Cause_to_be_included
produced ;; Creating
held ;; Hold
race ;; Competition || held ;; Hold
race ;; Competition || sponsored ;; Supply
given ;; Giving || consisting ;; Cause_to_be_included
race ;; Competition || giving ;; Giving || supports ;; Supporting
raised ;; Earnings_and_losses
race ;; Competition || gives out ;; Giving || draws ;; Getting
sponsored ;; Supply || played ;; Competition
played ;; Competition
finished ;; Process_end
opened ;; Action
went to ;; Motion || went ;; Motion
saved ;; Causation || win ;; Earnings_and_losses
played ;; Competition
contested ;; Competition || played ;; Competition || winning ;; Earnings_and_losses
forces ;; Hostile_encounter || carried out ;; Action
carried out ;; Action
participated ;; Participation
kill ;; Killing || capture ;; Conquering || operation ;; Military_operation || meant ;; Statement
called ;; Name_conferral
named ;; Name_conferral || described ;; Statement
includes ;; Cause_to_be_included || remaining ;; Presence || meant ;; Statement
organized ;; Arranging || festival ;; Social_event || festival ;; Social_event || taking place ;; Process_start
festival ;; Social_event || offered ;; Giving || offered ;; Giving || selection ;; Choosing
moved ;; Motion || announced ;; Expressing_publicly || festival ;; Social_event || canceled ;; Removing
announced ;; Expressing_publicly || festival ;; Social_event || relaunched ;; Arranging || take place ;; Process_start
festival ;; Social_event
festival ;; Social_event || cancelled ;; Removing
organized ;; Arranging || championship ;; Competition || held ;; Hold
tournament ;; Competition || switched ;; Change || Starting ;; Process_start || clash ;; Hostile_encounter || held ;; Hold
tournament ;; Competition || Cup ;; Competition || hosted ;; Social_event
tournament ;; Competition
shoot-out ;; Motion || eliminated ;; Removing
eliminated ;; Removing
championship ;; Competition || final ;; Competition
participated ;; Participation
began ;; Process_start
evicted ;; Removing
named ;; Name_conferral
occurred ;; Coming_to_be
removed ;; Removing || re-establish ;; Creating
held ;; Hold
replaced ;; Removing || withdrew ;; Removing
emerged ;; Coming_to_be || defeating ;; Conquering || final ;; Competition
began ;; Process_start
reinforced ;; Cause_change_of_strength
captured ;; Conquering || withdrew ;; Removing
held ;; Defending
commenced ;; Process_start
tournament ;; Competition
participate ;; Participation || played ;; Competition || set ;; Placing
took ;; Getting
Cup ;; Competition
unsanctioned ;; Preventing_or_letting || recognised ;; Know || tournament ;; Competition || players ;; Competition
reigning ;; Control || representing ;; Presence || hosts ;; Social_event
final ;; Process_end || playing ;; Competition || pitted ;; Placing
final ;; Process_end || beating ;; Competition
Marathon ;; Competition || Marathon ;; Competition
took place ;; Process_start || marathon ;; Competition || race ;; Competition
race ;; Competition || breaking ;; Destroying
means ;; Statement || set ;; Conquering
finishing ;; Process_end || surpassed ;; Destroying
route ;; Motion || set ;; Placing
race ;; Competition
became ;; Becoming || Finishing ;; Process_end
passed ;; Arriving || surpassing ;; Destroying
organized ;; Arranging
festival ;; Social_event
took place ;; Process_start || attracted ;; Influence
perform ;; Hold
takes place ;; Process_start || performing ;; Hold || held ;; Hold || left ;; Causation || attending ;; Participation
competition ;; Competition || knockout ;; Competition || establishment ;; Creating
failed ;; Earnings_and_losses || knocked out ;; Competition
final ;; Process_end || tournament ;; Competition || finished ;; Process_end || reached ;; Arriving
final ;; Process_end || beating ;; Competition
win ;; Earnings_and_losses || first ;; Process_start
tournament ;; Competition || reach ;; Arriving || Surprise ;; Motion || managed ;; Control
promotion ;; Cause_change_of_position_on_a_scale
wrestling ;; Competition || produced ;; Creating
created ;; Creating || held ;; Hold
changed ;; Change || held ;; Hold
held ;; Hold
produced ;; Creating
held ;; Hold
taking place ;; Hold || held ;; Hold
competing ;; Competition || wrestling ;; Competition
matches ;; Competition || taken place ;; Process_start
developed ;; Coming_to_be
began ;; Process_start
intensified ;; Cause_change_of_strength
approached ;; Motion
encountered ;; Hostile_encounter
dissipated ;; Dispersal
produced ;; Creating
damaged ;; Damaging
occurred ;; Presence
destroyed ;; Destroying
damaged ;; Damaging
caused ;; Causation
caused ;; Causation
donated ;; Giving
produced ;; Creating
issued ;; Creating
began ;; Process_start || recognised ;; Know
Championships ;; Competition
held ;; Hold || tournament ;; Competition
tournament ;; Competition || took place ;; Process_start
played ;; Competition || matches ;; Competition || played ;; Competition || matches ;; Competition || played ;; Competition || match ;; Competition
defeating ;; Preventing_or_letting || breaking ;; Destroying
lost ;; Earnings_and_losses
developed ;; Coming_to_be
moved ;; Motion || struck ;; Attack
intensified ;; Cause_change_of_strength
struck ;; Attack || dissipated ;; Cause_change_of_strength
produced ;; Creating
damaged ;; Damaging || destroyed ;; Destroying
caused ;; Causation || killed ;; Killing
began ;; Process_start || contest ;; Competition
originated ;; Coming_to_be
held ;; Hold || held ;; Hold
began ;; Process_start || war ;; Hostile_encounter || contest ;; Competition || served ;; Creating
contest ;; Competition || originated ;; Coming_to_be
held ;; Hold
contests ;; Competition || organization ;; Arranging
massacre ;; Killing
force ;; Causation || killed ;; Killing
attack ;; Attack || massacre ;; Killing
attack ;; Attack || dead ;; Death || injured ;; Bodily_harm
claimed ;; Statement
reported ;; Reporting || massacre ;; Killing
defense ;; Defending || massacre ;; Killing
known ;; Know || storm ;; Catastrophe
reaching ;; Arriving || typhoon ;; Catastrophe
storm ;; Bodily_harm || Forming ;; Coming_to_be || strengthened ;; Cause_change_of_strength || moved ;; Motion
brushed ;; Motion
becoming ;; Becoming || accelerated ;; Cause_change_of_position_on_a_scale
occurred ;; Coming_to_be
festival ;; Social_event || held ;; Hold
festival ;; Social_event || allows ;; Preventing_or_letting || Holding ;; Defending || gives ;; Giving
festival ;; Social_event || held ;; Hold || relocated ;; Removing
festival ;; Social_event || held ;; Hold
subtitled ;; Name_conferral || festival ;; Social_event
Held ;; Hold
festival ;; Social_event || subtitled ;; Name_conferral
Held ;; Hold
festival ;; Social_event || subtitled ;; Name_conferral
Held ;; Hold || show ;; Social_event
occurred ;; Coming_to_be
declared ;; Expressing_publicly || killed ;; Killing
occurred ;; Coming_to_be
begun ;; Process_start || changed ;; Change
formed ;; Coming_to_be
turned ;; Becoming
landfall ;; Self_motion || transitioned ;; Change
submerged ;; Motion || washed ;; Removing
died ;; Death
Tournament ;; Competition || held ;; Hold || Tournament ;; Competition
playoff ;; Competition || defeating ;; Conquering
Tour ;; Competition
finished ;; Process_end
finished ;; Process_end || contention ;; Competition || knocked ;; Motion
tournament ;; Competition || posted ;; Statement || propelled ;; Change
playoff ;; Competition
organised ;; Arranging
took place ;; Hold
changed ;; Change
replaced ;; Change
consisting ;; Cause_to_be_included
announced ;; Expressing_publicly
Games ;; Competition || Games ;; Competition || held ;; Hold
Games ;; Competition || hosted ;; Social_event
games ;; Competition || Games ;; Competition
participated ;; Participation || games ;; Competition || participating ;; Participation
host ;; Social_event || Games ;; Competition
commenced ;; Process_start || games ;; Competition || held ;; Hold
named ;; Name_conferral || games ;; Competition || opened ;; Hold
final ;; Process_end || host ;; Social_event || followed ;; Causation
broken ;; Destroying || Games ;; Competition || games ;; Competition
games ;; Competition || competition ;; Competition || deemed ;; Know || rising ;; Cause_change_of_position_on_a_scale
took place ;; Hold
rowing ;; Self_motion || Held ;; Hold
saw ;; Self_motion || race ;; Competition || race ;; Competition
competing ;; Competition || event ;; Social_event
defeated ;; Conquering || race ;; Competition
named ;; Name_conferral
held ;; Hold
Games ;; Competition || held ;; Hold
tournament ;; Competition || tournaments ;; Competition
matches ;; Competition || took place ;; Hold
tournaments ;; Competition
defended ;; Defending || competition ;; Competition
defended ;; Defending || win ;; Getting
held ;; Hold
contested ;; Competition
appearance ;; Arriving
lost ;; Competition
final ;; Process_end || granted ;; Getting
appearance ;; Arriving
lost ;; Competition
attended ;; Participation || held ;; Hold
held ;; Hold || attended ;; Participation
held ;; Hold
culminates ;; Process_end || held ;; Hold
festival ;; Social_event || held ;; Hold
held ;; Hold || returning ;; Arriving
held ;; Hold || occurring ;; Coming_to_be
became ;; Becoming || became ;; Becoming
held ;; Hold
spawned ;; Coming_to_be
held ;; Hold
win ;; Earnings_and_losses || shot ;; Motion
tied ;; Getting
turned ;; Becoming
made ;; Getting || climbed ;; Motion_directional
tied ;; Getting
defense ;; Defending
enter ;; Participation || made ;; Self_motion
hosted ;; Social_event
hosted ;; Social_event
competition ;; Competition || nicknamed ;; Name_conferral || held ;; Hold
competed ;; Competition
took place ;; Process_start
happened ;; Presence
becoming ;; Becoming
debut ;; Process_start
competed ;; Competition
held ;; Hold
played ;; Competition || played ;; Competition
competed ;; Competition
played ;; Competition
announced ;; Expressing_publicly
took place ;; Process_start
hold ;; Hold
described ;; Statement
re-entered ;; Arriving
tournament ;; Competition
held ;; Hold
tournament ;; Competition || hosted ;; Social_event
defeated ;; Conquering || final ;; Competition
succeeded ;; Conquering || winning ;; Getting
held ;; Hold || called ;; Name_conferral || tournament ;; Competition
held ;; Hold || played ;; Competition
hosted ;; Social_event || tournament ;; Competition
tournament ;; Competition || sponsor ;; Social_event
tournament ;; Competition || qualified ;; Earnings_and_losses
final ;; Process_end || beat ;; Competition
played ;; Competition || tournament ;; Competition
began ;; Process_start || ended ;; Process_end || defeated ;; Conquering
tournament ;; Competition || held ;; Hold || staged ;; Social_event
final ;; Process_end || beaten ;; Competition
attendance ;; Participation || match ;; Competition
Games ;; Competition || held ;; Hold
opened ;; Process_start
participate ;; Participation || returned ;; Arriving || compete ;; Competition
participated ;; Participation
host ;; Social_event
hosted ;; Social_event
closed ;; Process_end || opened ;; Process_start
final ;; Competition
held ;; Hold || Games ;; Competition || Games ;; Competition || Games ;; Competition
participated ;; Participation || hosted ;; Social_event || Games ;; Competition || Games ;; Competition
host ;; Social_event || Games ;; Competition
participated ;; Participation || games ;; Competition || events ;; Competition
games ;; Competition || opened ;; Process_start
followed ;; Causation || led ;; Getting || final ;; Process_end
games ;; Competition || broken ;; Destroying
competition ;; Competition || games ;; Competition
tournament ;; Competition
held ;; Hold
tournament ;; Competition
hosted ;; Social_event
tournament ;; Competition || defeating ;; Hostile_encounter
defeating ;; Hostile_encounter
championship ;; Competition
held ;; Hold || hosted ;; Social_event
matches ;; Competition || hosted ;; Social_event || tournament ;; Competition
qualifying ;; Earnings_and_losses || qualifying ;; Earnings_and_losses || Qualifier ;; Competition
tournament ;; Competition
played ;; Participation || joining ;; Participation
contested ;; Hostile_encounter
final ;; Competition || played ;; Competition || defeated ;; Conquering
named ;; Name_conferral || tournament ;; Competition || tournament ;; Competition
tournament ;; Competition
contested ;; Competition
tournament ;; Competition || played ;; Competition || advanced ;; Cause_change_of_position_on_a_scale
tournament ;; Competition || held ;; Hold
games ;; Competition || hosted ;; Social_event
championships ;; Competition
held ;; Hold
held ;; Hold
captured ;; Hold || defeating ;; Preventing_or_letting || finals ;; Competition
held ;; Hold
organised ;; Arranging || tournament ;; Competition
concluding ;; Process_end
held ;; Hold
played ;; Competition
played ;; Participation || hosted ;; Social_event
beat ;; Competition
finished ;; Process_end || took ;; Getting
Final ;; Competition || Final ;; Competition || played ;; Competition
contested ;; Competition || finished ;; Process_end || match ;; Competition || finals ;; Competition
beat ;; Competition || drawn ;; Competition || match ;; Competition || game ;; Competition
match ;; Competition || played ;; Competition
victory ;; Earnings_and_losses
match ;; Competition || loss ;; Earnings_and_losses
tournament ;; Competition || took place ;; Hold
tournament ;; Competition
tournament ;; Competition || tournament ;; Competition || earned ;; Getting
played ;; Competition
organised ;; Arranging
took place ;; Process_start || beating ;; Competition || beating ;; Competition
competitions ;; Competition || Games ;; Competition || diving ;; Motion || took place ;; Process_start
swimming ;; Motion || swimming ;; Motion || Games ;; Competition
competitions ;; Competition || Games ;; Competition || events ;; Competition || events ;; Competition
diving ;; Motion || representing ;; Presence
competition ;; Competition
played ;; Competition
contested ;; Competition
match ;; Competition
match ;; Competition
played ;; Competition || losing ;; Earnings_and_losses
competition ;; Competition
represented ;; Presence
declared ;; Expressing_publicly || played ;; Participation || rain ;; Motion
finishing ;; Process_end
consisted ;; Cause_to_be_included || matches ;; Competition
tournament ;; Competition || defeating ;; Conquering
Final ;; Competition || final ;; Competition || Cup ;; Competition || competition ;; Competition
match ;; Competition || contested ;; Competition || took place ;; Process_start
final ;; Competition || cup ;; Competition
game ;; Competition || overtime ;; Competition || goals ;; Getting || goal ;; Getting
named ;; Name_conferral || game ;; Competition
Championships ;; Competition || games ;; Competition || played ;; Participation
splitting ;; Cause_change_of_position_on_a_scale || advancing ;; Arriving
play ;; Competition || final ;; Process_end
Championships ;; Competition || Championships ;; Competition
became ;; Becoming || Champions ;; Competition
commented ;; Statement
tournament ;; Competition || play ;; Participation
game ;; Competition || loss ;; Earnings_and_losses || scored ;; Getting || shot ;; Motion
returned ;; Arriving
match ;; Competition || competitions ;; Competition || contested ;; Competition
match ;; Competition || played ;; Competition || contested ;; Competition
match ;; Competition
match ;; Competition || went ;; Motion
contested ;; Competition || qualified ;; Getting
contested ;; Competition
played ;; Competition
connected ;; Connect
sanctioned ;; Preventing_or_letting
participated ;; Participation || competition ;; Competition || advance ;; Getting
competition ;; Competition
took place ;; Process_start
split ;; Cause_change_of_position_on_a_scale
capture ;; Getting || beat ;; Preventing_or_letting
returned ;; Arriving || failed ;; Earnings_and_losses || earn ;; Earnings_and_losses
debuted ;; Process_start || winning ;; Getting || winning ;; Getting
Final ;; Competition
final ;; Competition || played ;; Participation
contested ;; Competition
win ;; Getting
match ;; Competition || finished ;; Process_end
included ;; Cause_to_be_included || Final ;; Competition || Final ;; Competition
began ;; Process_start
called ;; Name_conferral
called ;; Name_conferral
ambush ;; Attack || took place ;; Process_start
attack ;; Attack
became ;; Becoming
ambush ;; Attack || dubbed ;; Name_conferral
attacks ;; Attack || carried out ;; Attack
killed ;; Killing || battle ;; Attack
grew ;; Causation
began ;; Process_start
ended ;; Process_end
paved ;; Cause_change_of_position_on_a_scale
crashed ;; Catastrophe || take-off ;; Self_motion
died ;; Death
involving ;; Cause_to_be_included
crash ;; Catastrophe
worsened ;; Cause_change_of_position_on_a_scale
ceased ;; Process_end
