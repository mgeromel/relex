Battle ;; Hostile_encounter || War ;; Hostile_encounter || took place ;; Process_start
command ;; Control || capture ;; Conquering || moved ;; Self_motion || occupied ;; Control
blocked ;; Hindering || destroy ;; Destroying
gave ;; Giving || presence ;; Presence || attack ;; Attack
obtained ;; Getting || brought in ;; Bringing || filled ;; Filling || consisted ;; Cause_to_be_included || armed ;; Giving
purchased ;; Commerce_buy || reinforced ;; Cause_change_of_strength
hired ;; Employment || assist ;; Assistance || managing ;; Control
consisting ;; Cause_to_be_included || bribed ;; Committing_crime
sent ;; Sending || blowing ;; Motion || on fire ;; Damaging || anchored ;; Connect || setting ;; Placing || pushing ;; Motion
cutting ;; Removing || save ;; Rescuing || flee ;; Escaping || attack ;; Attack || attempted ;; Self_motion
burned ;; Destroying
Retreating ;; Escaping || came ;; Self_motion || fire ;; Catastrophe || waiting ;; Change_event_time
escaped ;; Escaping || engage ;; Participation || fight ;; Hostile_encounter
pursue ;; Self_motion
fell ;; Conquering
impact ;; Influence || war ;; Hostile_encounter || victory ;; Earnings_and_losses || assault ;; Attack || contributed ;; Causation || halting ;; Process_end
Battle ;; Hostile_encounter || fought ;; Hostile_encounter
killed ;; Killing || led ;; Causation || battle ;; Hostile_encounter
battle ;; Hostile_encounter || battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Wars ;; Hostile_encounter || retreat ;; Escaping
began ;; Process_start || found ;; Know || Battle ;; Hostile_encounter || attack ;; Attack || closed ;; Openness || taken up ;; Building
refused ;; Agree_or_refuse_to_act || began ;; Process_start || battle ;; Hostile_encounter || hoisted ;; Body_movement || surrender ;; Surrendering
ill-equipped ;; Supply || fortified ;; Cause_change_of_strength || facing ;; Confronting_problem
surrounded ;; Surrounding || protected ;; Preventing_or_letting || war ;; Hostile_encounter || cross ;; Motion_directional
killed ;; Killing || casualties ;; Catastrophe || battle ;; Hostile_encounter || attacked ;; Attack || placed ;; Placing || war ;; Hostile_encounter || led ;; Causation
rushed ;; Motion || killed ;; Killing || Battle ;; Hostile_encounter || held ;; Defending || mired ;; Placing || led ;; Causation
found ;; Know || dismounted ;; Self_motion
equipped ;; Supply || finish ;; Process_end
ended ;; Process_end || battle ;; Hostile_encounter || escape ;; Escaping
casualties ;; Catastrophe || escape ;; Escaping || defeated ;; Conquering
beat ;; Preventing_or_letting
deployment ;; Arranging
called ;; Name_conferral
held ;; Defending
festival ;; Social_event || held ;; Process_start || encompassed ;; Surrounding
created ;; Creating
duplicate ;; Creating || held ;; Defending
contacted ;; Communication
published ;; Publishing
joined ;; Becoming_a_member
relief ;; Assistance
reached ;; Arriving
protect ;; Defending
attendance ;; Participation
concert ;; Social_event
aid ;; Assistance
performed ;; Hold
tore up ;; Destroying || played ;; Competition || show ;; Create_artwork
supposed ;; Coming_to_believe
order ;; Arranging
headlined ;; Statement
managed ;; Control
aired ;; Expressing_publicly
broadcast ;; Expressing_publicly || unplug ;; Removing || equipment ;; Supply || set ;; Placing || took ;; Conquering
aired ;; Expressing_publicly || placed ;; Placing
performed ;; Hold || called ;; Name_conferral
bumped ;; Self_motion
confirmed ;; Check
booed ;; Communication || cut ;; Removing || throwing up ;; Body_movement || due ;; Causation
refused ;; Agree_or_refuse_to_act || issues ;; Creating || due ;; Causation || play ;; Competition
bumped ;; Bodily_harm || scheduled ;; Arranging || open ;; Openness
confirmed ;; Check
held ;; Hold
decided ;; Deciding
Turned ;; Change
moved back ;; Motion || order ;; Arranging
looked ;; Perception_active
stolen ;; Theft || returned ;; Giving
vowed ;; Commitment || handled ;; Hold || lit ;; Lighting
motioned ;; Body_movement || set ;; Placing || played ;; Competition || play ;; Competition || came ;; Self_motion
stolen ;; Theft
recovered ;; Recovering || held ;; Hold
explained ;; Statement || destroyed ;; Destroying
smashed ;; Destroying || threw ;; Self_motion
came ;; Motion
joined ;; Becoming_a_member
recalls ;; Statement
performed ;; Hold || claims ;; Statement || performing ;; Hold
ended ;; Process_end || tour ;; Traveling
joined ;; Becoming_a_member || perform ;; Hold
filmed ;; Create_artwork
seemed ;; Change_sentiment
enjoyed ;; Perception_active
joined ;; Becoming_a_member
reached ;; Arriving || pass ;; Motion_directional || lifted ;; Motion || prompting ;; Convincing
sprayed ;; Motion
lost ;; Earnings_and_losses
promised ;; Commitment
formed ;; Coming_to_be
began ;; Process_start || establish ;; Building || become ;; Becoming
originated ;; Coming_to_be || perceived ;; Know
occurred ;; Coming_to_be || known ;; Know
hunted ;; Killing || gathered ;; Come_together
diminished ;; Cause_change_of_position_on_a_scale || declined ;; Agree_or_refuse_to_act
replacing ;; Change_of_leadership
began ;; Process_start || develop ;; Cause_to_make_progress
became ;; Becoming
replaced ;; Change_of_leadership
transition ;; Change
took ;; Creating
gave ;; Giving || asserted ;; Statement || influence ;; Influence
creating ;; Creating || mixed ;; Cause_to_amalgamate || contributing ;; Giving
changed ;; Change_tool
diminished ;; Cause_change_of_position_on_a_scale || limit ;; Limiting
changed ;; Exchange || shifted ;; Change
achieved ;; Achieve || influence ;; Influence
led to ;; Causation
died ;; Death || detain ;; Arrest || deportation ;; Removing
wrapped ;; Filling || raid ;; Attack || restrained ;; Hindering || gagged ;; Preventing_or_letting
breathe ;; Breathing || suffered ;; Bodily_harm || damage ;; Bodily_harm || asphyxia ;; Bodily_harm
died ;; Death || placed ;; Placing || support ;; Supporting || arrest ;; Arrest
trial ;; Criminal_investigation || manslaughter ;; Killing || involved ;; Cause_to_be_included || acquitted ;; Judgment_communication
became ;; Becoming || cause ;; Causation || used ;; Using || deportation ;; Removing || attention ;; Perception_active || orders ;; Request || brought ;; Bringing || called ;; Name_conferral
death ;; Death || inquest ;; Criminal_investigation || inquiry ;; Criminal_investigation || held ;; Hold
crashed ;; Catastrophe
accident ;; Catastrophe
surrounded ;; Surrounding || descent ;; Motion_directional || requires ;; Imposing_obligation
service ;; Supply
begin ;; Process_start || approach ;; Arriving || reported ;; Reporting || turned ;; Self_motion
caused ;; Causation || strayed ;; Motion
exploded ;; Destroying || crashed ;; Catastrophe
dead ;; Death
given ;; Giving || accident ;; Catastrophe || investigation ;; Criminal_investigation || determine ;; Deciding || suspected ;; Suspicion
criticized ;; Judgment_communication || crash ;; Catastrophe || handle ;; Hold
accident ;; Catastrophe || crash ;; Catastrophe
crash ;; Catastrophe || wrote in ;; Recording
serviced ;; Assistance
failed ;; Confronting_problem
informed ;; Telling
qualified ;; Incident
approach ;; Arriving || crashed ;; Catastrophe
crash ;; Catastrophe || monument ;; Social_event
named ;; Name_conferral || crash ;; Catastrophe
uses ;; Using
operated ;; Being_in_operation || service ;; Supply || agreement ;; Agree_or_refuse_to_act
operating ;; Being_in_operation || crashed ;; Catastrophe || approach ;; Arriving
killed ;; Killing
killed ;; Killing || War ;; Hostile_encounter
crash ;; Catastrophe || damaged ;; Damaging
accident ;; Catastrophe || crash ;; Catastrophe || incident ;; Catastrophe || involving ;; Cause_to_be_included || surpassing ;; Self_motion
crash ;; Catastrophe || cause ;; Causation || lost ;; Earnings_and_losses
accident ;; Catastrophe || management ;; Control || consumption ;; Ingestion || contributed ;; Influence
disaster ;; Catastrophe || led to ;; Causation || rebranding ;; Name_conferral
arose ;; Coming_to_be || wars ;; Hostile_encounter || wars ;; Hostile_encounter || dispute ;; Quarreling
strike ;; Attack
demanded ;; Request || pay ;; Commerce_pay || work ;; Being_in_operation || practice ;; Practice || forcing ;; Hostile_encounter || help ;; Assistance
refused ;; Agree_or_refuse_to_act || hired ;; Employment || equipped ;; Supply || demands ;; Request || guard ;; Patrolling
moved ;; Motion || moved ;; Motion || arrived ;; Arriving || renting ;; Renting || supported ;; Supporting || evicted ;; Removing
began ;; Process_start || began ;; Process_start || provoked ;; Causation
began ;; Process_start || supplying ;; Supply
declared ;; Expressing_publicly || crossed ;; Motion_directional || destroy ;; Destroying || kill ;; Killing
threat ;; Warning || armed ;; Bearing_arms || deployed ;; Arranging
seizing ;; Hold || proclaimed ;; Statement
became ;; Becoming || shootout ;; Hostile_encounter || sparked ;; Process_start || joined ;; Becoming_a_member
conflict ;; Hostile_encounter || called ;; Name_conferral
fought ;; Hostile_encounter || surrounding ;; Surrounding
struggled ;; Hostile_encounter || called ;; Name_conferral || encompassed ;; Surrounding
emerged ;; Coming_to_be
extending ;; Change_event_time || set about ;; Process_start
guaranteed ;; Commitment
accepting ;; Receiving || decision ;; Deciding || designed ;; Creating
resolved ;; Resolve_problem || brought ;; Bringing || war ;; Hostile_encounter || aimed ;; Aiming
took place ;; Process_start
fighting ;; Hostile_encounter || favoured ;; Supporting
defected ;; Escaping || exhausted ;; Expend_resource
give up ;; Process_end || retained ;; Preserving || forced ;; Hostile_encounter
accepted ;; Receiving || help ;; Assistance || acquired ;; Getting
conflict ;; Hostile_encounter || approaching ;; Arriving || ailing ;; Cure || embroiled ;; Catastrophe
scoring ;; Getting || match ;; Competition
scored ;; Getting || winning ;; Getting
lost ;; Earnings_and_losses || match ;; Competition || winning ;; Getting
surrendering ;; Surrendering || spending ;; Expend_resource || suffered ;; Bodily_harm
scored ;; Getting || conceded ;; Earnings_and_losses
started ;; Process_start || scored ;; Getting
scored ;; Getting || added ;; Cause_to_be_included
scored ;; Getting || restored ;; Recovering
opening ;; Openness || set up ;; Competition
scored ;; Getting || involving ;; Participation
reducing ;; Cause_change_of_position_on_a_scale || defeat ;; Conquering || sparked ;; Causation || winning ;; Getting || confirmed ;; Check
competition ;; Competition
described ;; Statement || said ;; Telling
celebrating ;; Social_event || awarded ;; Award
lost ;; Earnings_and_losses || regained ;; Recovering || race ;; Competition || match ;; Competition || played ;; Competition
revolt ;; Change_of_leadership || widespread ;; Dispersal
slaughtered ;; Killing
achieved ;; Achieve || fined ;; Judgment_communication
took ;; Conquering || supported ;; Supporting || consisted ;; Cause_to_be_included
revolution ;; Change_of_leadership || widespread ;; Dispersal || uprising ;; Terrorism
fighting ;; Hostile_encounter
began ;; Process_start || spread ;; Dispersal || beginning ;; Process_start
suppressed ;; Control || flared ;; Lighting
command ;; Control
fled ;; Escaping || combat ;; Hostile_encounter || turned ;; Change
well-equipped ;; Supply || funding ;; Supply
incorporated ;; Cause_to_amalgamate || emerging ;; Coming_to_be
supported ;; Supporting
condemned ;; Judgment_communication
condemned ;; Judgment_communication || work ;; Employment || called ;; Name_conferral || put ;; Placing
causes ;; Causation || interpreted ;; Statement
played ;; Competition
claimed ;; Receiving
run ;; Control
held ;; Hold || held ;; Hold
celebrate ;; Social_event || holding ;; Hold
included ;; Cause_to_be_included
served ;; Assistance || selecting ;; Choosing || uses ;; Using
formalize ;; Institutionalization || announced ;; Expressing_publicly || awarding ;; Giving
take place ;; Hold
changes ;; Change
played ;; Competition || happened ;; Presence
consisted ;; Cause_to_be_included
matches ;; Competition
died ;; Death
Efforts ;; Assistance || revive ;; Cure
present ;; Presence || died ;; Death
run ;; Action
developed ;; Coming_to_be
hospitalized ;; Institutionalization
attended ;; Participation || lying ;; Placing || filed ;; Sending
Starting ;; Process_start
conflict ;; Hostile_encounter || supported ;; Supporting || War ;; Hostile_encounter
support ;; Supporting || supported ;; Supporting
began ;; Process_start || invaded ;; Attack || referred ;; Name_conferral
joined ;; Becoming_a_member || support ;; Supporting || stabilised ;; Cause_change_of_strength || formalised ;; Institutionalization
became ;; Becoming || agreed ;; Agree_or_refuse_to_act || remain ;; Temporary_stay || involved ;; Cause_to_be_included
support ;; Supporting || made ;; Manufacturing
opposition ;; Hostile_encounter || led to ;; Causation
agreed ;; Agree_or_refuse_to_act || married ;; Forming_relationships
returned ;; Arriving
received ;; Receiving || establishing ;; Creating
marked ;; Recording || hold ;; Defending
battle ;; Hostile_encounter || engagement ;; Hostile_encounter
shot down ;; Use_firearm || flying ;; Self_motion
ejected ;; Removing || crash-landed ;; Attack
attack ;; Attack || reported ;; Reporting
crashed ;; Catastrophe || recovered ;; Recovering
attack ;; Attack || reports ;; Reporting
fired ;; Use_firearm || losing ;; Earnings_and_losses || control ;; Control || patrol ;; Patrolling || spotted ;; Know || appear ;; Presence || reported ;; Reporting || bailed ;; Extradition
associates ;; Collaboration || disclosed ;; Reveal_secret || connected ;; Connect || governing ;; Control
end ;; Process_end || service ;; Supply || investigation ;; Criminal_investigation || indicted ;; Criminal_investigation || investigation ;; Criminal_investigation
pleas ;; Request || announced ;; Expressing_publicly || indictments ;; Committing_crime
investigation ;; Criminal_investigation || collusion ;; Collaboration || revolved ;; Surrounding
competitions ;; Competition || tournaments ;; Competition || tournaments ;; Competition
serving ;; Supply || investigation ;; Criminal_investigation || arrested ;; Arrest
investigation ;; Criminal_investigation || arrest ;; Arrest || made ;; Action
arrested ;; Arrest
election ;; Change_of_leadership || attend ;; Participation || scheduled ;; Arranging || include ;; Cause_to_be_included || preparing ;; GetReady
extradited ;; Extradition || receiving ;; Receiving
raid ;; Attack || handed ;; Giving || arrest ;; Arrest
arrests ;; Arrest || occurred ;; Coming_to_be
investigations ;; Criminal_investigation || arrests ;; Arrest || triggered ;; Causation || open ;; Process_start || separate ;; Hold || intensify ;; Cause_change_of_strength
attacks ;; Attack || bombings ;; Terrorism || disrupted ;; Attack
explosions ;; Attack || occurred ;; Presence
set ;; Being_in_operation || dumped ;; Removing
evacuated ;; Emptying
causing ;; Causation || injury ;; Bodily_harm || cause ;; Causation || bombs ;; Attack || reported ;; Reporting || reported ;; Reporting || intention ;; Aiming || sounds ;; Perception_active || said ;; Statement || loss ;; Death || exploded ;; Process_start
fled ;; Escaping || explode ;; Process_start
bombings ;; Attack || released ;; Releasing
identified ;; Check
manhunt ;; Scouring || described ;; Statement || challenge ;; Confronting_problem
killed ;; Killing || manhunt ;; Scouring || shot ;; Use_firearm
bombing ;; Attack || bombings ;; Attack || arrested ;; Arrest
arrested ;; Arrest
arrested ;; Arrest
extradited ;; Extradition || arrested ;; Arrest
investigations ;; Criminal_investigation || arrested ;; Prison
found ;; Know || conspiracy ;; Committing_crime || imprisonment ;; Prison || imprisonment ;; Prison || sentenced ;; Legal_rulings
obtained ;; Getting || used ;; Using || fired ;; Use_firearm
attack ;; Attack || explosions ;; Attack || injuries ;; Bodily_harm || reported ;; Reporting || injury ;; Bodily_harm || suffered ;; Bodily_harm
Cyclone ;; Catastrophe || cyclone ;; Catastrophe || Cyclone ;; Catastrophe || inflict ;; Causation || damage ;; Damaging || landfall ;; Arriving || make ;; Causation
storm ;; Catastrophe || originated ;; Coming_to_be || named ;; Name_conferral || cyclone ;; Catastrophe
cyclone ;; Catastrophe || christened ;; Name_conferral || name ;; Name_conferral || organizing ;; Arranging || recognized ;; Know || gaining ;; Getting
began ;; Process_start || cyclone ;; Catastrophe || intensifying ;; Cause_change_of_strength || Meandering ;; Motion || curve ;; Motion_directional || turning ;; Motion
came ;; Arriving || cyclone ;; Catastrophe || winds ;; Motion || producing ;; Creating
drifted ;; Motion || Weakening ;; Cause_change_of_strength || landfall ;; Arriving || persisted ;; Wearing || dissipating ;; Removing
approach ;; Arriving || warnings ;; Warning || cyclone ;; Catastrophe || issued ;; Expressing_publicly || prompting ;; Cause_change_of_position_on_a_scale || evacuations ;; Emptying
Damage ;; Damaging || damaged ;; Damaging || flooding ;; Catastrophe || damage ;; Damaging || landfall ;; Arriving || result ;; Causation
obstructed ;; Hindering || warn ;; Warning || service ;; Supply || disrupted ;; Damaging
caused ;; Causation || damages ;; Damaging || cyclone ;; Catastrophe
storm ;; Catastrophe || damage ;; Damaging
caused ;; Causation || deaths ;; Death || damage ;; Damaging || inflicted ;; Causation
distributed ;; Dispersal || cyclone ;; Catastrophe || aid ;; Assistance || assistance ;; Assistance || provide ;; Supply
deployed ;; Arranging || restore ;; Recovering || provide ;; Supply || repair ;; Recovering || service ;; Supply || services ;; Supply || protect ;; Preventing_or_letting || evacuate ;; Emptying
sent ;; Sending || requests ;; Request || receive ;; Receiving || aid ;; Assistance || employees ;; Employment || benefits ;; Earnings_and_losses || damage ;; Damaging
initiated ;; Process_start || provide ;; Supply || relief ;; Assistance || damaged ;; Damaging
distributed ;; Dispersal || payments ;; Commerce_pay
impeded ;; Hindering || cyclone ;; Catastrophe || confusion ;; Quarreling || stole ;; Theft || possessions ;; Committing_crime
accused ;; Judgment_communication || warning ;; Warning || approach ;; Arriving || cyclone ;; Catastrophe || storm ;; Catastrophe || criticism ;; Judgment_communication || concerns ;; Quarreling || addressed ;; Statement || landfall ;; Arriving || giving ;; Giving || report ;; Reporting || impact ;; Influence
ended ;; Process_end || storm ;; Catastrophe || damage ;; Damaging || name ;; Name_conferral || Due ;; Causation || retired ;; GiveUp
starting ;; Process_start || beginning ;; Process_start || bushfires ;; Catastrophe || bushfires ;; Catastrophe || fires ;; Catastrophe || becoming ;; Becoming
provided ;; Supply || fires ;; Catastrophe || fuelled ;; Cause_change_of_position_on_a_scale
fires ;; Catastrophe || fires ;; Catastrophe || burning ;; Change
declared ;; Expressing_publicly || emergency ;; Emergency || evict ;; Removing || demolish ;; Emptying
fires ;; Catastrophe
destroyed ;; Destroying
fires ;; Catastrophe || fatalities ;; Death || attributed ;; Causation
announced ;; Expressing_publicly
claimed ;; Statement || coup ;; Change_of_leadership || occurred ;; Presence || foiled ;; Removing
arrested ;; Arrest || claimed ;; Statement || involved ;; Participation
emergency ;; Emergency || lifted ;; Process_end
hold ;; Hold || led to ;; Causation || revocation ;; Process_end
known ;; Statement || suspended ;; Change_event_time
allowed ;; Preventing_or_letting || detain ;; Preventing_or_letting
assured ;; Telling
present ;; Presence || discovered ;; Know || justified ;; Justifying || thwarted ;; Preventing_or_letting || statement ;; Statement
seize ;; Hold || declared ;; Expressing_publicly || claimed ;; Protest || similarities ;; Incident
lifted ;; Process_end
defense ;; Defending || beginning ;; Process_start || took place ;; Process_start || marked ;; Recording || end ;; Process_end || Battle ;; Hostile_encounter || War ;; Hostile_encounter || opening ;; Openness
held ;; Defending || retreating ;; Escaping || Battle ;; Hostile_encounter || forces ;; Hostile_encounter || covered ;; Hiding_objects || forces ;; Hostile_encounter || Defenders ;; Defending
defense ;; Defending || provided ;; Supply || invasion ;; Attack || consolation ;; Influence || aftermath ;; Catastrophe
battle ;; Hostile_encounter || took place ;; Process_start
provoke ;; Causation || strikes ;; Attack || shot ;; Use_firearm || dare ;; Risk || keep ;; Temporary_stay || call ;; Request || land ;; Arriving || incident ;; Hostile_encounter || war ;; Hostile_encounter
dared ;; Risk || use ;; Using
support ;; Supporting || aggression ;; Attack || bid ;; Expressing_publicly || drew ;; Getting
provided ;; Supply || defenders ;; Defending || reveal ;; Reveal_secret || announce ;; Expressing_publicly
Championships ;; Competition || held ;; Hold
championships ;; Competition || hosted ;; Social_event || held ;; Hold
competition ;; Competition || competition ;; Competition || dominated ;; Control || taking ;; Conquering
finish ;; Process_end || took ;; Conquering
race ;; Competition
becoming ;; Becoming || race ;; Competition || win ;; Earnings_and_losses
took ;; Conquering
made ;; Manufacturing || led ;; Bringing
present ;; Presence || defend ;; Defending
running ;; Motion || rendered ;; Creating
finished ;; Process_end
led ;; Bringing
sending ;; Sending || competition ;; Competition || decline ;; Cause_change_of_position_on_a_scale || tournament ;; Competition || dominance ;; Control || entered ;; Participation || accompanied ;; Causation
supported ;; Supporting || War ;; Hostile_encounter || struggle ;; Hostile_encounter || claim ;; Statement
died ;; Death || succeeded ;; Change_of_leadership
found ;; Know || believed ;; Know
accused ;; Judgment_communication || increased ;; Cause_change_of_position_on_a_scale || giving ;; Giving
accused ;; Judgment_communication || uprising ;; Change_of_leadership || planning ;; Arranging
cooperated ;; Collaboration
persuaded ;; Convincing || support ;; Supporting
fled ;; Escaping || received ;; Receiving || refuge ;; Escaping || set up ;; Reforming_a_system || defeated ;; Conquering
dragged on ;; Change_event_time
captured ;; Conquering || reached ;; Arriving || resistance ;; Attack || forces ;; Military_operation || forces ;; Military_operation || turned ;; Becoming
forces ;; Military_operation || crushed ;; Attack
murdered ;; Committing_crime || request ;; Request || assistance ;; Assistance || capture ;; Conquering || called ;; Name_conferral || venture ;; Risk
forces ;; Hostile_encounter || ran away ;; Self_motion || offered ;; Supply
surrendered ;; Surrendering || lured ;; Hold || fallen ;; Motion_directional || gain ;; Getting || banished ;; Removing
killed ;; Killing || shooting ;; Use_firearm
took place ;; Process_start
shooting ;; Use_firearm || approached ;; Arriving || shot ;; Use_firearm || driving ;; Self_motion || said ;; Telling || close ;; Body_movement
attack ;; Attack || stating ;; Statement || resistance ;; Control || claimed ;; Statement || carried out ;; Action
classified ;; Check || shocked ;; Body_movement
arrested ;; Arrest
attacks ;; Attack || carried out ;; Action
attacks ;; Attack || stationed ;; Placing || occurred ;; Coming_to_be
opened ;; Action
traveling ;; Motion
bomb ;; Attack
attack ;; Attack || hit ;; Attack || killing ;; Killing
attack ;; Attack
killed ;; Killing || killing ;; Killing || reported ;; Reporting || reported ;; Reporting
killed ;; Killing
killed ;; Killing || killed ;; Killing || said ;; Telling || fled ;; Escaping
row ;; Quarreling || triggered ;; Causation || led to ;; Causation
reports ;; Reporting || threatened ;; Warning || withdraw ;; Removing
sent ;; Sending || agreed ;; Agree_or_refuse_to_act || expressed ;; Expressing_publicly || conduct ;; Action || apology ;; Statement || ordered ;; Arranging || investigation ;; Research || probe ;; Scrutiny
attacks ;; Attack || agreed ;; Agree_or_refuse_to_act
attacks ;; Attack || resistance ;; Control || denies ;; Statement || accused ;; Judgment_communication || orchestrating ;; Causation
killing ;; Killing || attacked ;; Attack
killed ;; Killing || wounded ;; Bodily_harm || called ;; Request
killing ;; Killing || fired ;; Use_firearm || wounding ;; Bodily_harm
killing ;; Killing || broken ;; Bodily_harm
called ;; Statement
killed ;; Killing
began ;; Process_start || referred ;; Name_conferral
ended ;; Process_end
went out ;; Self_motion
killed ;; Killing
burned ;; Bodily_harm || destroyed ;; Destroying
feared ;; Change_sentiment || rising ;; Cause_change_of_position_on_a_scale
represented ;; Presence
aided ;; Assistance || fought ;; Hostile_encounter || organized ;; Arranging || organized ;; Arranging
widespread ;; Dispersal || supported ;; Supporting
suppressed ;; Control
work ;; Being_in_operation || work ;; Being_in_operation || organize ;; Arranging
built ;; Building || support ;; Supporting || suppress ;; Control || stand ;; Placing
founded ;; Creating || provide ;; Supply
established ;; Building || plan ;; Arranging
associated ;; Collaboration
attacked ;; Attack || occurred ;; Presence
burned ;; Bodily_harm || killed ;; Killing
took ;; Conquering || left ;; Departing
died ;; Death
became ;; Becoming || adopted ;; Choosing
involved ;; Cause_to_be_included
conflict ;; Hostile_encounter || raid ;; Attack
achieve ;; Achieve || entered ;; Arriving
retreat ;; Escaping || held off ;; Defending
aimed ;; Aiming || raid ;; Attack || take captives ;; Conquering
destroyed ;; Destroying || taken ;; Removing
raid ;; Attack || predicted ;; Statement || shocked ;; Change_sentiment
increased ;; Cause_change_of_position_on_a_scale
prepare ;; GetReady
raid ;; Attack || immortalized ;; Preserving
adopted ;; Choosing || became ;; Becoming || married ;; Forming_relationships
published ;; Publishing
siege ;; Besieging
besieged ;; Besieging || fell ;; Motion_directional
besieged ;; Besieging || bombardment ;; Attack || held ;; Hold
saw ;; Self_motion || determined ;; Deciding || fighting ;; Hostile_encounter || take ;; Conquering || made ;; Creating
named ;; Name_conferral || established ;; Creating
held ;; Defending
began ;; Process_start
arm ;; Bodily_harm || accelerate ;; Cause_change_of_position_on_a_scale || put ;; Removing
sent ;; Sending || put ;; Removing
reaching ;; Arriving || defected ;; Escaping
resigned ;; Change_of_leadership
refused ;; Agree_or_refuse_to_act || arrange ;; Arranging || ousted ;; Removing
agreed ;; Agree_or_refuse_to_act || delivered ;; Sending || working ;; Employment || defence ;; Defending
broadcast ;; Expressing_publicly || coined ;; Bringing
urging ;; Convincing || pass ;; Arriving
become ;; Becoming || defence ;; Defending || cause ;; Causation
launch ;; Process_start || preparing ;; GetReady
compound ;; Cause_to_amalgamate || forced ;; Motion || march ;; Motion_directional
became ;; Becoming || stormed ;; Catastrophe
broken ;; Destroying || attackers ;; Attack || killing ;; Killing || fighting ;; Hostile_encounter || surrender ;; Surrendering || indicated ;; Coming_to_believe
opened ;; Openness || bombard ;; Attack || fell ;; Conquering || brought ;; Bringing
allowed ;; Preventing_or_letting || killed ;; Killing || breach ;; Damaging
enraged ;; Change_sentiment || surrenders ;; Surrendering || massacred ;; Committing_crime
held ;; Hold
contained ;; Containing
saw ;; Self_motion || uprising ;; Change_of_leadership || killed ;; Killing || followed ;; Self_motion
storming ;; Catastrophe || massacred ;; Committing_crime
conquer ;; Conquering
landed ;; Arriving
command ;; Control || raised ;; Creating
planned ;; Arranging || advancing ;; Cause_to_make_progress || take ;; Conquering || took ;; Creating
supplied ;; Supply || routed ;; Motion_directional
besieged ;; Besieging || relieving ;; Cure || overruled ;; Destroying || argued ;; Quarreling
attack ;; Attack || held up ;; Change_event_time || prepare ;; GetReady
reformed ;; Reforming_a_system
party ;; Social_event || included ;; Cause_to_be_included
gained ;; Getting || access ;; Having_or_lacking_access
controlled ;; Control || commanded ;; Control || sent ;; Sending
raised ;; Cause_change_of_position_on_a_scale || elected ;; Choosing
control ;; Control || command ;; Control
commanded ;; Control || supplied ;; Supply
reached ;; Arriving || approaching ;; Motion
beaten ;; Bodily_harm
leaving ;; Departing || fell ;; Motion_directional
remarked ;; Statement
became ;; Becoming
made ;; Manufacturing || move ;; Motion || fall ;; Motion_directional
remained ;; Presence || organise ;; Arranging || formed ;; Creating
capture ;; Conquering
equipped ;; Supply || trained ;; Education_teaching || outnumbered ;; Expansion
surround ;; Surrounding || help ;; Assistance || cut ;; Removing
recording ;; Recording || concert ;; Social_event
supported ;; Supporting
visited ;; Traveling
announced ;; Expressing_publicly
release ;; Publishing
finishing ;; Process_end || took ;; Action
toured ;; Traveling || toured ;; Traveling
fair ;; Social_event || Fair ;; Social_event
fair ;; Social_event
trailing ;; Self_motion || runs ;; Self_motion || fair ;; Social_event || Fair ;; Social_event || Fair ;; Social_event
learn ;; Education_teaching || products ;; Manufacturing || services ;; Supply || fair ;; Social_event || come to ;; Motion || entertained ;; Change_sentiment
ending ;; Process_end || fair ;; Social_event
named ;; Name_conferral || Fair ;; Social_event
Fair ;; Social_event
Fair ;; Social_event || attend ;; Participation
record ;; Recording
visitors ;; Traveling || fair ;; Social_event
riots ;; Violence || death ;; Death || took place ;; Process_start || attacked ;; Attack || stabbed ;; Attack
aiding ;; Assistance || said ;; Communication || burned ;; Bodily_harm || death ;; Death || died ;; Death || confirmed ;; Check || plundered ;; Theft
raped ;; Bodily_harm || lost ;; Earnings_and_losses
injured ;; Bodily_harm || riots ;; Violence
transferred ;; Sending || transported ;; Sending || injured ;; Bodily_harm
evacuated ;; Escaping
sent ;; Sending || attend ;; Participation
returned ;; Arriving
sent ;; Sending || rescuing ;; Rescuing
sent ;; Sending || conduct ;; Action || keep ;; Temporary_stay || searches ;; Scrutiny
saying ;; Communication || protect ;; Preventing_or_letting || questioning ;; Criminal_investigation
arrested ;; Arrest
held ;; Defending || show ;; Social_event || Show ;; Social_event
show ;; Social_event
organised ;; Arranging
beginning ;; Process_start || held ;; Hold || hosted ;; Come_together
steal ;; Theft || debuts ;; Process_start
debates ;; Communication || announced ;; Expressing_publicly
regarded ;; Change_sentiment || aided ;; Assistance || playing ;; Competition
tour ;; Traveling || promote ;; Creating || Tour ;; Traveling
started ;; Process_start
performed ;; Hold || festival ;; Social_event || Festival ;; Social_event
sang ;; Vocalizations
shows ;; Social_event || lasted ;; Process_end
started ;; Process_start
joined ;; Becoming_a_member || singing ;; Vocalizations
took place ;; Process_start || shooting ;; Use_firearm
shot ;; Use_firearm
informed ;; Telling || reaching ;; Arriving || shouting ;; Vocalizations || believed ;; Know
charged ;; Judgment_communication || dropped ;; Motion_directional
filed ;; Submitting_documents || settled ;; Resolve_problem || lawsuit ;; Legality || lawsuit ;; Legality
found ;; Know || shooting ;; Use_firearm
found ;; Know || benefits ;; Earnings_and_losses || offered ;; Supply
opted ;; Choosing
used ;; Using || shooting ;; Use_firearm || explored ;; Scrutiny
allow ;; Preventing_or_letting || terminated ;; Process_end || resigned ;; Change_of_leadership
responded ;; Response || keeps ;; Temporary_stay || dispatch ;; Sending || pointing ;; Self_motion
reported ;; Reporting || pointing ;; Self_motion
says ;; Communication || beginning ;; Process_start || call ;; Name_conferral
dispatch ;; Sending || end ;; Process_end || relayed ;; Communication
reported ;; Reporting || patrol ;; Patrolling || arrival ;; Arriving || yelled ;; Vocalizations
knew ;; Know || knew ;; Know || claimed ;; Statement || coming ;; Motion
shot ;; Use_firearm || response ;; Response || hitting ;; Attack
died ;; Death || following ;; Self_motion
found ;; Know
released ;; Releasing || surveillance ;; Scrutiny || shooting ;; Use_firearm
released ;; Statement || declared ;; Expressing_publicly || turned ;; Change
appears ;; Change_sentiment || declined ;; Cause_change_of_position_on_a_scale || drawing ;; Create_artwork || arrived ;; Arriving || presented ;; Presence
brought ;; Bringing || reduce ;; Cause_change_of_position_on_a_scale || settled ;; Resolve_problem || lawsuit ;; Legality
revealed ;; Reveal_secret || shooting ;; Use_firearm || deemed ;; Coming_to_believe || recruit ;; Becoming_a_member
join ;; Becoming_a_member || disclose ;; Reveal_secret || reviewed ;; Scrutiny
following ;; Self_motion || fired ;; Use_firearm
found ;; Know || justified ;; Justifying || review ;; Statement || response ;; Response
received ;; Receiving || occurred ;; Coming_to_be
shooting ;; Use_firearm || occurred ;; Coming_to_be
shooting ;; Use_firearm || left ;; Causation || dead ;; Death
wounded ;; Bodily_harm || shot ;; Use_firearm
shooting ;; Use_firearm || perpetrators ;; Committing_crime || believed ;; Statement || committed ;; Committing_crime
accused ;; Judgment_communication || defense ;; Defending || victims ;; Catastrophe || argued ;; Statement
planned ;; Arranging || committing ;; Committing_crime || accused ;; Judgment_communication || statement ;; Statement || robbery ;; Robbery || angry ;; Change_sentiment || adamant ;; Statement || take ;; Removing || explained ;; Statement || coming ;; Arriving || leaving ;; Causation
began ;; Process_start || shooting ;; Use_firearm || robbery ;; Robbery || yelling ;; Vocalizations
escaped ;; Escaping || fled ;; Escaping || involvement ;; Participation || shooting ;; Use_firearm || denied ;; Statement
murder ;; Killing || robbery ;; Robbery || convicted ;; Legal_rulings || sentenced ;; Rewards_and_punishments
begin ;; Process_start || decline ;; Cause_change_of_position_on_a_scale || recover ;; Recovering || violence ;; Violence || killings ;; Killing || tourism ;; Traveling || led to ;; Causation
convicted ;; Legal_rulings || transfer ;; Sending || hijacked ;; Terrorism
escaped ;; Escaping || land ;; Motion
confirmed ;; Check || spent ;; Expend_resource || recaptured ;; Conquering
deceased ;; Death
crashed ;; Catastrophe || killing ;; Killing
recalled ;; Statement || exploded ;; Motion || takeoff ;; Departing
extended ;; Expansion
rose ;; Motion_directional || stalled ;; Motion
crash ;; Catastrophe || cause ;; Causation || report ;; Reporting || due ;; Causation || concluded ;; Coming_to_believe
aggravated ;; Cause_change_of_position_on_a_scale || control ;; Control || understanding ;; Know || take over ;; Getting
accident ;; Catastrophe || crash ;; Catastrophe || occur ;; Coming_to_be || crash ;; Catastrophe || surpassed ;; Cause_change_of_position_on_a_scale
crash ;; Catastrophe || killing ;; Killing || surpassed ;; Cause_change_of_position_on_a_scale || crashed ;; Motion
happened ;; Presence || earthquakes ;; Catastrophe
earthquake ;; Catastrophe || formed ;; Coming_to_be
earthquake ;; Catastrophe || destroyed ;; Destroying
ended ;; Process_end || demanded ;; Request || interrupted ;; Hindering || earthquake ;; Catastrophe
earthquake ;; Catastrophe
earthquake ;; Catastrophe
began ;; Process_start || control ;; Control || force ;; Hostile_encounter || seize ;; Hold
developed ;; Coming_to_be || conflict ;; Hostile_encounter
began ;; Process_start || conflict ;; Hostile_encounter
rising ;; Cause_change_of_position_on_a_scale || fears ;; Change_sentiment || invasion ;; Attack || forces ;; Hostile_encounter || sparked ;; Process_start || defying ;; Agree_or_refuse_to_act
helped ;; Assistance || turn ;; Change || rebels ;; Hostile_encounter || start ;; Process_start || suspected ;; Suspicion
refused ;; Agree_or_refuse_to_act || raise ;; Cause_change_of_position_on_a_scale || army ;; Bearing_arms || command ;; Control
formed ;; Coming_to_be || broke out ;; Process_start
became ;; Becoming || control ;; Control || free ;; Releasing || aligned ;; Cause_to_amalgamate
defeated ;; Conquering
seen ;; Perception_active || collapse ;; Destroying
gunmen ;; Bearing_arms || shopping ;; Commerce_buy || attacked ;; Attack
siege ;; Besieging || conflicting ;; Hostile_encounter || killed ;; Killing || due ;; Causation || started ;; Process_start || attack ;; Attack
resulted in ;; Causation
wounded ;; Bodily_harm || shooting ;; Use_firearm
deployment ;; Arranging || claimed ;; Statement
warnings ;; Warning || issued ;; Creating || attack ;; Attack || suspected ;; Suspicion
arrested ;; Arrest || attack ;; Attack || announced ;; Expressing_publicly
pleading ;; Request || charged ;; Legality
reported ;; Reporting || victim ;; Catastrophe || attack ;; Attack || corroborated ;; Check
declared ;; Expressing_publicly
suicide bomber ;; Killing || attack ;; Attack || bombing ;; Terrorism || carried out ;; Attack || transporting ;; Bringing
arriving ;; Arriving || carrying ;; Carry_goods
killed ;; Killing || explosion ;; Attack || injured ;; Bodily_harm || resulting in ;; Causation || bombing ;; Terrorism
attack ;; Attack || said ;; Statement
stated ;; Statement
point to ;; Protest || blast ;; Attack
denied ;; Agree_or_refuse_to_act
stated ;; Statement || bombing ;; Terrorism
discovered ;; Know || attacks ;; Attack || revealed ;; Reveal_secret || connected ;; Connect
used ;; Using || found ;; Know || attack ;; Terrorism || facilitated ;; Preventing_or_letting
stated ;; Statement || bombing ;; Catastrophe || say ;; Statement
bombing ;; Terrorism || suspected ;; Suspicion
voted ;; Deciding || bombing ;; Terrorism || response ;; Response
identified ;; Check || named ;; Name_conferral || announced ;; Expressing_publicly
killing ;; Killing
end ;; Process_end || truce ;; Process_end || War ;; Hostile_encounter || War ;; Hostile_encounter || happened ;; Presence || outbreak ;; Process_start
killings ;; Killing || dead ;; Death || described ;; Statement || led to ;; Causation
killed ;; Killing || killed ;; Killing
raid ;; Attack || executed ;; Killing || kidnapped ;; Kidnapping || revenge ;; Revenge || killing ;; Killing
shot ;; Use_firearm || injuries ;; Bodily_harm
ordered ;; Arranging || attacks ;; Attack || carried ;; Creating
stated ;; Statement || looting ;; Robbery || murder ;; Killing || operations ;; Military_operation || released ;; Releasing || managing ;; Hold || claim ;; Statement || stating ;; Statement || taken part ;; Dispersal || robbing ;; Robbery
murdered ;; Killing || shot ;; Use_firearm || dead ;; Death
killings ;; Killing || killings ;; Killing || controlled ;; Control || controlled ;; Control || condemned ;; Judgment_communication || took place ;; Process_start
remains ;; Temporary_stay
agreed ;; Agree_or_refuse_to_act || provoked ;; Causation || raided ;; Attack || shooting ;; Use_firearm
targeted ;; Aiming || War ;; Hostile_encounter || War ;; Hostile_encounter || claimed ;; Statement || claim ;; Statement || suspected ;; Suspicion || argue ;; Protest || associated ;; Collaboration || listed ;; Writing
battle ;; Hostile_encounter || fought ;; Hostile_encounter || Wars ;; Hostile_encounter
raided ;; Attack || engagement ;; Hostile_encounter || contesting ;; Competition || control ;; Control || war ;; Hostile_encounter
captured ;; Conquering || Battle ;; Hostile_encounter || lost ;; Earnings_and_losses || recaptured ;; Conquering || came ;; Arriving || aftermath ;; Catastrophe
became ;; Becoming || losses ;; Earnings_and_losses || reinforcements ;; Cause_change_of_strength || rushed ;; Self_motion || blockading ;; Besieging || suffered ;; Earnings_and_losses
despatched ;; Sending || Battle ;; Hostile_encounter || reinforce ;; Cause_change_of_strength
spotted ;; Know || chase ;; Self_motion || gave ;; Giving || supported ;; Supporting
surrendered ;; Surrendering || reached ;; Arriving || forced ;; Causation || engage ;; Hostile_encounter || damage ;; Damaging || wounded ;; Bodily_harm || inflicting ;; Change_sentiment || run ;; Self_motion
surrender ;; Surrendering || recaptured ;; Conquering || forced ;; Causation || broke ;; Process_end || arrived ;; Arriving || drove ;; Self_motion || capturing ;; Conquering
invasion ;; Attack || contest ;; Competition || supplies ;; Supply || control ;; Control || failed ;; Earnings_and_losses || attempt ;; Hold || disrupt ;; Hindering || invasion ;; Attack
took place ;; Process_start
ruled ;; Control || burned ;; Damaging
fled ;; Escaping || defeat ;; Conquering
chased ;; Self_motion
heading ;; Self_motion || siege ;; Besieging
kill ;; Killing || destroying ;; Destroying || slaughtering ;; Killing || proceeded ;; Self_motion
regarded ;; Know
prevented ;; Preventing_or_letting || Terrorising ;; Change_sentiment
ruled ;; Control || inherited ;; Change_of_leadership
lost ;; Earnings_and_losses || rose ;; Cause_change_of_position_on_a_scale
took ;; Conquering
destroyed ;; Destroying || destroyed ;; Destroying || siege ;; Besieging || repaired ;; Recovering
left ;; Departing || died ;; Death || Finding ;; Action
refused ;; Agree_or_refuse_to_act || ending ;; Process_end
seizure ;; Arrest || took place ;; Process_start
captured ;; Arrest
strayed ;; Self_motion || seized ;; Arrest || said ;; Statement || training ;; Education_teaching
threatened ;; Warning || discussions ;; Statement || released ;; Releasing
returned ;; Giving || confiscated ;; Removing
released ;; Releasing || agreed ;; Agree_or_refuse_to_act
put on display ;; Presence || returned ;; Giving
cocked ;; Body_movement || detention ;; Arrest || endured ;; Change_sentiment || marched ;; Self_motion || stand ;; Placing
forced ;; Request || appeared ;; Presence || apologise ;; Judgment_communication
operating ;; Control
causing ;; Causation || crossing ;; Motion_directional || contributed ;; Causation
requested ;; Request || analysed ;; Coming_to_believe || return ;; Giving || returned ;; Giving || affirmed ;; Judgment_communication
request ;; Request || report ;; Reporting || released ;; Publishing
victim ;; Catastrophe || murder ;; Killing
sighted ;; Perception_active || getting ;; Self_motion
generated ;; Causation
arrested ;; Arrest
found ;; Know || guilty ;; Committing_crime || abduction ;; Kidnapping || murder ;; Killing
released ;; Releasing || ordering ;; Arranging || imprisonment ;; Prison || sentenced ;; Legal_rulings
died ;; Death
died ;; Death || vigil ;; Rite
arranged ;; Arranging || funeral ;; Rite || mourning ;; Rite
voiced ;; Expressing_publicly || seen ;; Perception_active
revolution ;; Change_of_leadership || left ;; Placing || took ;; Bringing
wrestling ;; Competition || produced ;; Manufacturing
took place ;; Process_start
threat ;; Warning || involving ;; Participation
recover ;; Recovering || run ;; Self_motion || take ;; Creating
suffered ;; Bodily_harm || delivered ;; Sending || included ;; Cause_to_be_included || defending ;; Defending || defending ;; Defending || involving ;; Participation || debut ;; Process_start
killings ;; Killing || shootings ;; Use_firearm || shootings ;; Use_firearm || deaths ;; Death
killed ;; Killing || named ;; Name_conferral || held ;; Defending
survived ;; Rescuing || attack ;; Attack
hidden ;; Hiding_objects || discovered ;; Know || dead ;; Death
shot ;; Use_firearm || wound ;; Bodily_harm || suffered ;; Bodily_harm || returned ;; Arriving
death ;; Death || investigated ;; Criminal_investigation || attack ;; Attack
involved ;; Cause_to_be_included || probed ;; Criminal_investigation || attack ;; Attack
attack ;; Attack || killing ;; Killing
said ;; Telling || investigation ;; Criminal_investigation || explain ;; Statement || murders ;; Killing
targeted ;; Aiming || suggested ;; Statement
fought ;; Hostile_encounter
war ;; Military_operation || wars ;; Military_operation || consisted ;; Cause_to_be_included
war ;; Military_operation
began ;; Process_start || defeated ;; Conquering
winning ;; Getting || using ;; Using
defeated ;; Conquering
conquering ;; Conquering || counterattacked ;; Attack || scored ;; Getting
ravaging ;; Destroying || consolidated ;; Cause_change_of_strength || making ;; Causation
caused ;; Causation || defeated ;; Conquering || enter ;; Arriving
flared ;; Process_start
defeated ;; Conquering || regained ;; Getting || revolted ;; Change_of_leadership
ended ;; Process_end || ratified ;; Ratification
maintained ;; Preserving
domination ;; Control || dominated ;; Control || continued ;; Process_start
became ;; Becoming || returned ;; Arriving
restarted ;; Process_start || war ;; Military_operation || leading to ;; Causation
ended ;; Process_end || occupied ;; Control
revolution ;; Change_of_leadership || started ;; Process_start || end ;; Process_end || revolution ;; Change_of_leadership || concluded ;; Process_end
began ;; Process_start || focused ;; Aiming
War ;; Hostile_encounter || left ;; Departing || state ;; Statement || took place ;; Process_start
control ;; Control || forming ;; Coming_to_be || dominated ;; Control
felt ;; Change_sentiment || revolution ;; Change_of_leadership || resulting in ;; Causation || suppress ;; Hindering || abdication ;; Change_of_leadership
assemblies ;; Building || called ;; Name_conferral || working ;; Employment || permitted ;; Preventing_or_letting || dominated ;; Control || insisted ;; Statement || control ;; Control
state ;; Statement || ensued ;; Coming_to_be || held ;; Hold || led ;; Conquering
strikes ;; Attack || mutinies ;; Committing_crime || protests ;; Protest
vied ;; Competition || end ;; Process_end || war ;; Hostile_encounter || engaged ;; Participation || campaigned ;; Military_operation
chose ;; Choosing || revolution ;; Change_of_leadership || fighting ;; Hostile_encounter
turned ;; Becoming || control ;; Control || control ;; Control
overthrew ;; Change_of_leadership || transferring ;; Giving || relocated ;; Removing
support ;; Supporting || governing ;; Control || party ;; Social_event || state ;; Statement || practicing ;; Practice || established ;; Creating || reorganizing ;; Reforming_a_system
promise ;; Commitment || signing ;; Sign_agreement || march ;; Self_motion || treaty ;; Ratification
punish ;; Rewards_and_punishments || revolution ;; Change_of_leadership || service ;; Supply || revolutionary ;; Change_of_leadership || state ;; Statement || established ;; Creating || functioned ;; Being_in_operation
erupted ;; Process_start || war ;; Hostile_encounter || opposed ;; Agree_or_refuse_to_act
defeated ;; Conquering || party ;; Social_event || reconstituted ;; Recovering
led to ;; Causation || established ;; Creating
occurred ;; Coming_to_be || state ;; Statement || took over ;; Getting
War ;; Hostile_encounter || conflict ;; Hostile_encounter || War ;; Hostile_encounter || establishment ;; Creating || followed ;; Motion
war ;; Hostile_encounter || treaty ;; Ratification || opposing ;; Agree_or_refuse_to_act
became ;; Becoming || state ;; Statement || supported ;; Supporting || betrayal ;; GiveUp || proclaimed ;; Statement || rising ;; Change_of_leadership
conflict ;; Hostile_encounter || War ;; Hostile_encounter || fought ;; Hostile_encounter
provided ;; Supply || War ;; Hostile_encounter || benefitted ;; Influence || forces ;; Military_operation
conflict ;; Hostile_encounter || War ;; Hostile_encounter || claimed ;; Statement || left ;; Causation || preceded ;; Process_start
war ;; Hostile_encounter || opposing ;; Agree_or_refuse_to_act
began ;; Process_start || treaty ;; Ratification
expanded ;; Expansion || providing ;; Supply || carrying out ;; Action
contributed ;; Giving || end ;; Process_end || flown ;; Self_motion
played ;; Participation
bombed ;; Attack || combat ;; Hostile_encounter
helped ;; Assistance || operate ;; Being_in_operation || adapted ;; Coming_to_be
helped ;; Assistance
helped ;; Assistance || conflict ;; Hostile_encounter || led to ;; Causation || establish ;; Creating
arose ;; Coming_to_be || bombing ;; Attack || taken ;; Conquering || response ;; Response
played ;; Participation || conflict ;; Hostile_encounter || spanned ;; Self_motion
use ;; Using
bombing ;; Attack || played ;; Participation || operation ;; Military_operation || strikes ;; Attack || flown ;; Self_motion || led to ;; Causation || ending ;; Process_end
conflict ;; Hostile_encounter || recognized ;; Know || involves ;; Cause_to_be_included || conflict ;; Hostile_encounter || independent ;; Change_of_leadership
conflict ;; Hostile_encounter || conflict ;; Hostile_encounter || intensified ;; Cause_change_of_strength || view ;; Perception_active || dissolution ;; Destroying || end ;; Process_end
conflict ;; Hostile_encounter || unresolved ;; Confronting_problem
offered ;; Giving
refuse ;; Agree_or_refuse_to_act || form ;; Coming_to_be
regard ;; Judgment_communication || independence ;; Creating || liberation ;; Change_of_leadership || believe ;; Statement || result ;; Causation || war ;; Military_operation || formed ;; Coming_to_be
independent ;; Creating || remain ;; Presence || formed ;; Coming_to_be || left ;; Departing
conduct ;; Action || hostilities ;; Hostile_encounter || diplomacy ;; Forming_relationships || war ;; Military_operation
resulted in ;; Causation || killing ;; Killing || campaign ;; Military_operation || expulsion ;; Removing || war ;; Military_operation || carried out ;; Action
mentions ;; Statement || cleansing ;; Military_operation || resolution ;; Action || assembly ;; Ratification || recognized ;; Check
cease-fire ;; Process_end || appeals ;; Request || resolutions ;; Action || passed ;; Process_end
fought ;; Hostile_encounter || lost ;; Earnings_and_losses || conflict ;; Hostile_encounter
involved ;; Cause_to_be_included
promoted ;; Cause_to_make_progress || promoted ;; Cause_to_make_progress
causes ;; Causation || expense ;; Cost || allow ;; Preventing_or_letting || decline ;; Cause_change_of_position_on_a_scale || gain ;; Getting || involved ;; Cause_to_be_included
causes ;; Causation || revealed ;; Reveal_secret || noted ;; Know || noted ;; Know || involving ;; Cause_to_be_included
refused ;; Agree_or_refuse_to_act || worked out ;; Know || came to ;; Motion
placed ;; Placing || issued ;; Statement
agreed ;; Agree_or_refuse_to_act || arranged ;; Arranging || mediate ;; Resolve_problem || attempted ;; Hold
demanded ;; Request || changes ;; Change || refused ;; Agree_or_refuse_to_act || prepared ;; GetReady
declared ;; Expressing_publicly || support ;; Supporting || obtained ;; Getting
began ;; Process_start || started ;; Process_start || occupied ;; Control
defensive ;; Defending || Led ;; Conquering || fought ;; Hostile_encounter || stopped ;; Preventing_or_letting
destroyed ;; Destroying || attempt ;; Hold || reinforce ;; Cause_change_of_strength
rushed ;; Motion || forces ;; Causation
moved ;; Motion || arriving ;; Arriving || abandon ;; GiveUp
helping ;; Assistance || quipped ;; Statement
decided ;; Deciding || attack ;; Attack || Frustrated ;; Influence
landed ;; Motion || marched ;; Self_motion || forces ;; Causation || extended ;; Expansion
became ;; Becoming || depleting ;; Expend_resource || forces ;; Causation || counterattacked ;; Attack || repulsed ;; Defending || cost ;; Cost || Battle ;; Hostile_encounter
ended ;; Process_end || counterattack ;; Attack
settled ;; Resolve_problem
took place ;; Process_start
began ;; Process_start || fell ;; Motion_directional || join ;; Becoming_a_member
Isolated ;; Placing || invasion ;; Attack || sued ;; Request || facing ;; Confronting_problem
growing ;; Expansion || conflict ;; Hostile_encounter || welcomed ;; Request
ended ;; Process_end || treaty ;; Ratification || signed on ;; Sign_agreement
forbade ;; Preventing_or_letting
became ;; Becoming
regained ;; Recovering || control ;; Control || granted ;; Preventing_or_letting
used ;; Using || conflicts ;; Hostile_encounter
documented ;; Recording || reports ;; Reporting || photographs ;; Recording
became ;; Becoming
achieved ;; Achieve || demand ;; Request || wounded ;; Bodily_harm || gained ;; Getting || treating ;; Cure
proved ;; Convincing
forced ;; Motion || educated ;; Education_teaching || aimed ;; Aiming || restoring ;; Recovering || identify ;; Check || recognize ;; Know
studied ;; Education_teaching || reforms ;; Reforming_a_system || social ;; Social_event || service ;; Supply
turned ;; Becoming || impact ;; Influence
disasters ;; Catastrophe || experienced ;; Change_sentiment
moved ;; Motion || developing ;; Coming_to_be || noted ;; Know
posing ;; Causation || establishing ;; Building || reached ;; Achieve
become ;; Becoming || sheared ;; Body_movement || appeared ;; Presence
moved ;; Motion || became ;; Becoming || organized ;; Arranging || shearing ;; Motion
drifted ;; Motion || situated ;; Placing
passed ;; Motion_directional
pulled ;; Motion
causing ;; Causation || rain ;; Motion || affected ;; Influence
resulted in ;; Causation || collapse ;; Catastrophe || obstructing ;; Destroying
casualties ;; Catastrophe || died ;; Death || injured ;; Bodily_harm
damaged ;; Damaging || supplies ;; Supply || cut off ;; Removing
destroyed ;; Destroying || wrecked ;; Destroying
terrorist ;; Terrorism || killed ;; Killing
joint ;; Collaboration || raid ;; Attack || carried out ;; Action || command ;; Control || operation ;; Military_operation || operation ;; Military_operation || operation ;; Military_operation || code-named ;; Wearing || coordinating ;; Collaboration || involved ;; Cause_to_be_included
participating ;; Participation || included ;; Cause_to_be_included || recruits ;; Becoming_a_member
ended ;; Process_end || search ;; Scrutiny || attacks ;; Attack || operation ;; Military_operation
raid ;; Attack || compound ;; Cause_to_amalgamate || launched ;; Process_start
raid ;; Attack || buried ;; Placing || death ;; Death || identification ;; Check || said ;; Telling || took ;; Action
confirmed ;; Check || vowing ;; Commitment || death ;; Death || killing ;; Killing || avenge ;; Revenge
vowed ;; Commitment || preventing ;; Preventing_or_letting || operation ;; Military_operation || retaliation ;; Revenge
condemned ;; Judgment_communication || raid ;; Attack || supported ;; Supporting
killing ;; Killing || taken ;; Conquering || unarmed ;; Bearing_arms || questioned ;; Communication
death ;; Death || decision ;; Deciding || release ;; Publishing
killing ;; Killing || attack ;; Attack || surrounding ;; Surrounding || aftermath ;; Catastrophe || formed ;; Coming_to_be || investigate ;; Criminal_investigation
enabled ;; Preventing_or_letting || hide ;; Hiding_objects || revealed ;; Reveal_secret || report ;; Reporting || leaked ;; Reveal_secret
engagement ;; Hostile_encounter || Wars ;; Hostile_encounter
arrived ;; Arriving || prisoner ;; Prison || coming ;; Arriving || making ;; Action || disguised ;; Wearing
sent ;; Sending || lay ;; Placing || knew ;; Know || challenging ;; Competition || battle ;; Hostile_encounter
accepted ;; Receiving || disseminated ;; Dispersal || challenge ;; Competition || meet ;; Come_together || sailed ;; Self_motion || crowded ;; Filling
seemed ;; Change_sentiment || engagement ;; Hostile_encounter || contested ;; Competition || taking ;; Getting || damage ;; Damaging || thrown ;; Placing
debate ;; Communication || claiming ;; Statement || killed ;; Killing
gave ;; Giving || circulated ;; Dispersal || rumours ;; Communication || knocked ;; Body_movement || jettison ;; GiveUp || credited ;; Hold || taken up ;; Getting || defended ;; Defending
damage ;; Damaging || escape ;; Escaping || gone ;; Earnings_and_losses || continued ;; Hold || suffer ;; Bodily_harm || ordered ;; Request
proved ;; Confronting_problem || fell ;; Motion_directional
escaped ;; Escaping || encounter ;; Hostile_encounter
bombing ;; Terrorism || terrorist ;; Terrorism || attack ;; Terrorism || carried out ;; Attack
killed ;; Killing || injured ;; Bodily_harm
planned ;; Arranging || cancelled ;; Change_event_time || operation ;; Military_operation || attack ;; Terrorism || approval ;; Preventing_or_letting || carried out ;; Attack
raids ;; Attack || conducted ;; Action || response ;; Response || conceived ;; Creating || directed ;; Aiming
planted ;; Placing || Disguised ;; Wearing
caused ;; Causation || explosion ;; Attack || collapse ;; Catastrophe
injuries ;; Bodily_harm || deaths ;; Death || occurred ;; Coming_to_be
sent ;; Sending || warnings ;; Warning || warnings ;; Warning || decided ;; Deciding
received ;; Receiving || search ;; Scouring || carried out ;; Attack
caused ;; Causation || calls ;; Communication || notified ;; Know
explosion ;; Attack || ordered ;; Arranging || called ;; Communication || evacuation ;; Removing
warnings ;; Warning || warnings ;; Warning || arisen ;; Coming_to_be || given ;; Giving || evacuated ;; Removing || made ;; Creating
affected ;; Influence || known ;; Know
developed ;; Coming_to_be || moved ;; Motion
declared ;; Expressing_publicly || become ;; Becoming || remained ;; Presence || organized ;; Cause_change_of_position_on_a_scale
intensified ;; Cause_change_of_strength || attained ;; Achieve
allowed ;; Preventing_or_letting || strengthening ;; Cause_change_of_strength || storm ;; Catastrophe || attained ;; Achieve
caused ;; Causation || turning ;; Change || weakening ;; Cause_change_of_strength || response ;; Response || weaken ;; Cause_change_of_strength
moving back ;; Motion || struck ;; Attack || turning ;; Change
weakening ;; Cause_change_of_strength || succumbed ;; Death
became ;; Becoming
posed ;; Causation || prompted ;; Causation || issuance ;; Creating || warnings ;; Warning
remained ;; Presence
evacuated ;; Escaping
storm ;; Catastrophe || led to ;; Causation
perished ;; Death || damage ;; Damaging
take place ;; Hold
held ;; Hold
sold ;; Commerce_sell
played ;; Action
adopted ;; Choosing
run ;; Action
abolished ;; Removing || reclaimed ;; Justifying
attacks ;; Attack
targeted ;; Aiming
killed ;; Killing || wounded ;; Bodily_harm
shot ;; Use_firearm
killed ;; Killing || wounded ;; Bodily_harm
opened ;; Openness || killing ;; Killing
raised ;; Cause_change_of_position_on_a_scale
condemned ;; Judgment_communication
criminal ;; Committing_crime
claimed ;; Statement || justified ;; Justifying || carried out ;; Action || said ;; Telling || kill ;; Killing
killed ;; Killing || wounded ;; Bodily_harm || rented ;; Renting
convicted ;; Legal_rulings
sailed ;; Self_motion || command ;; Control || escorting ;; Patrolling
made ;; Causation
aim ;; Aiming || stop ;; Process_end || caused ;; Causation || interfered ;; Hindering
enabling ;; Preventing_or_letting || sailed ;; Self_motion || attack ;; Attack || sailed ;; Self_motion
carried ;; Carry_goods || anchored ;; Connect || occupied ;; Control || meet ;; Come_together || invaded ;; Attack
damaged ;; Damaging || captured ;; Conquering
anchored ;; Connect
abandoned ;; GiveUp || scattered ;; Dispersal || attack ;; Attack || blockaded ;; Besieging
damaged ;; Damaging || changed ;; Change || running ;; Self_motion
withdrew ;; Escaping || driven ;; Motion
storms ;; Catastrophe || disrupted ;; Hindering
wrecked ;; Catastrophe || return ;; Motion
miscarried ;; Death || explain ;; Justifying
use ;; Using
expedition ;; Self_motion
organised ;; Arranging || called ;; Name_conferral
defense ;; Defending || battle ;; Hostile_encounter
resistance ;; Defending || supported ;; Supporting || Campaign ;; Military_operation || broke out ;; Process_start || oppose ;; Agree_or_refuse_to_act
planned ;; Arranging
resistance ;; Defending || enforcing ;; Action || used ;; Using || insurgency ;; Hostile_encounter || deportations ;; Removing || concluded ;; Coming_to_believe || instigated ;; Process_start || justify ;; Justifying
decisions ;; Deciding || deportation ;; Removing || extermination ;; Killing || made ;; Action
resistance ;; Defending || maintained ;; Statement || massacre ;; Killing || assessment ;; Testing
fought ;; Hostile_encounter
offered ;; Giving || fuelled ;; Supply || approach ;; Arriving
emerged ;; Coming_to_be || attacked ;; Attack || uprising ;; Change_of_leadership || join ;; Participation || hiding ;; Hiding_objects
supported ;; Supporting || defected ;; Escaping || fighting ;; Hostile_encounter
counter-attacked ;; Attack || slowed ;; Change_event_time
withdraw ;; Escaping || signed ;; Sign_agreement || obeyed ;; Action
entered ;; Arriving
crimes ;; Committing_crime
justified ;; Justifying || revenge ;; Revenge || flee ;; Escaping
ordered ;; Request || aid ;; Assistance
presented ;; Presence || increase ;; Cause_change_of_position_on_a_scale
Festival ;; Social_event
takes place ;; Process_start || festival ;; Social_event
began ;; Process_start
started ;; Process_start
dressed ;; Wearing || dressed ;; Wearing
cast ;; Create_artwork
interact ;; Communication || known ;; Know || bring ;; Bringing || cast ;; Motion || shows ;; Social_event || marry ;; Forming_relationships
clothing ;; Wearing || dress ;; Wearing || cast ;; Motion || inspired ;; Influence
wear ;; Wearing
wear ;; Wearing
offering ;; Supply || hand ;; Giving || crafted ;; Create_artwork
provide ;; Supply || clothing ;; Wearing
festival ;; Social_event || includes ;; Cause_to_be_included
visitors ;; Traveling || reenactment ;; Creating || cast ;; Motion || show ;; Social_event
visitors ;; Traveling || built ;; Building || open ;; Openness
opened ;; Openness || become ;; Becoming
opens ;; Openness || runs ;; Self_motion
ends ;; Process_end || festival ;; Social_event || closing ;; Openness
received ;; Receiving || award ;; Award || selected ;; Choosing
sponsor ;; Supply
occurred ;; Coming_to_be
spread ;; Dispersal || started ;; Process_start
overthrew ;; Change_of_leadership
occurred ;; Coming_to_be || evict ;; Escaping
present ;; Presence
designed ;; Create_artwork
switched ;; Change || broadcast ;; Expressing_publicly
flee ;; Escaping || led ;; Causation
portrayed ;; Statement
Captured ;; Arrest
convicted ;; Legal_rulings || sentenced ;; Legal_rulings || condemned ;; Judgment_communication
toppled ;; Change_of_leadership || elections ;; Choosing
implemented ;; Action || Elected ;; Choosing
become ;; Becoming
became ;; Becoming
issues ;; Creating || proven ;; Coming_to_believe
passed ;; Motion_directional || collapsed ;; Destroying || killing ;; Killing || occurred ;; Coming_to_be
cast ;; Motion || supported ;; Supporting
designs ;; Create_artwork
designing ;; Create_artwork || made ;; Creating || design ;; Create_artwork || sought ;; Getting
control ;; Control
died ;; Death || ruined ;; Destroying
allow ;; Preventing_or_letting
used ;; Using
began ;; Process_start
designed ;; Create_artwork
assumed ;; Choosing
opposed ;; Agree_or_refuse_to_act
ended ;; Process_end
event ;; Social_event || Fiesta ;; Social_event || takes place ;; Process_start
put on ;; Communication || perform ;; Hold || participate ;; Participation
raised ;; Cause_change_of_position_on_a_scale || offered ;; Giving
received ;; Receiving || drawing ;; Bringing
promote ;; Cause_change_of_position_on_a_scale
exhibits ;; Create_artwork
transformed ;; Change || called ;; Name_conferral
travelling ;; Traveling
fell ;; Motion_directional || derailed ;; Self_motion
killed ;; Killing || injured ;; Bodily_harm
caused ;; Causation || design ;; Create_artwork
responded ;; Response || derailed ;; Self_motion || rescue ;; Rescuing || ordering ;; Arranging
elicited ;; Causation
response ;; Response || issued ;; Creating || limited ;; Limiting || restrict ;; Hindering || met with ;; Come_together
fatal ;; Killing || involving ;; Participation
moving ;; Motion
impact ;; Influence
using ;; Using
suspended ;; Process_end || investigation ;; Criminal_investigation
reduced ;; Cause_change_of_position_on_a_scale
scrutinized ;; Scrutiny
response ;; Response || announced ;; Expressing_publicly
blamed ;; Judgment_communication || completed ;; Process_end || carrying out ;; Action
helped ;; Assistance || helping ;; Assistance
accidents ;; Catastrophe
lost ;; Earnings_and_losses || killing ;; Killing || injuring ;; Bodily_harm || finish ;; Process_end || detonated ;; Attack || race ;; Competition || Marathon ;; Competition || bombs ;; Terrorism
identified ;; Check || released ;; Releasing
kidnapped ;; Kidnapping || killed ;; Killing || shootout ;; Use_firearm || injured ;; Bodily_harm || died ;; Death
escaping ;; Escaping || shot ;; Use_firearm || died ;; Death
asked ;; Request || unprecedented ;; Catastrophe || ensued ;; Coming_to_be || surrounding ;; Surrounding || manhunt ;; Scouring || searching ;; Scrutiny || closed ;; Being_in_operation
discovered ;; Know || hiding ;; Hiding_objects
shot ;; Use_firearm || wounded ;; Bodily_harm || custody ;; Criminal_investigation || taken ;; Bringing
wars ;; Hostile_encounter || terrorist ;; Terrorism || questioning ;; Communication || said ;; Statement || motivated ;; Influence
learned ;; Education_teaching || build ;; Building || explosive ;; Attack || said ;; Statement
bomb ;; Terrorism || travel ;; Arriving || said ;; Telling
destruction ;; Destroying || use ;; Using || death ;; Death || convicted ;; Legal_rulings || resulting in ;; Causation
death ;; Death || sentenced ;; Legal_rulings
referred ;; Adducing
organised ;; Arranging || co-founded ;; Collaboration
founded ;; Creating
became ;; Becoming || games ;; Competition
end ;; Process_end || held ;; Defending
sent ;; Sending || compete ;; Competition || existence ;; Presence || shooting ;; Use_firearm
held ;; Defending
held ;; Defending
sent ;; Sending
competed ;; Competition || completing ;; Process_end
competitors ;; Competition || competitors ;; Competition
came ;; Self_motion || winning ;; Getting
coming ;; Self_motion
competition ;; Competition || swept ;; Scouring
held ;; Defending
held ;; Defending
established ;; Creating
crashed ;; Catastrophe
crash ;; Catastrophe || giving rise ;; Causation || search ;; Scouring
began ;; Process_start || emerge ;; Coming_to_be
became ;; Becoming || flying ;; Self_motion
started ;; Process_start || descent ;; Motion_directional || crashed ;; Catastrophe || killing ;; Killing || believing ;; Know || cleared ;; Emptying
received ;; Receiving || landing ;; Arriving || repeated ;; Telling || explained ;; Statement
accident ;; Catastrophe
given ;; Giving || disaster ;; Catastrophe || disaster ;; Catastrophe
started ;; Process_start
detecting ;; Know || shut down ;; Being_in_operation
started ;; Process_start
ceased ;; Process_end || powering ;; Supply || remove ;; Removing
arrived ;; Arriving || filled ;; Filling || sweeping ;; Scouring || generated ;; Creating || flooding ;; Destroying
led to ;; Causation
expose ;; Reveal_secret || increased ;; Cause_change_of_position_on_a_scale || boil down ;; Change
declare ;; Expressing_publicly || culminating ;; Process_end || forced ;; Causation
caused ;; Causation || evacuated ;; Escaping
estimated ;; Statement || flowing ;; Motion
built ;; Building || created ;; Creating
effects ;; Influence || increase ;; Cause_change_of_position_on_a_scale || disaster ;; Catastrophe || projected ;; Statement || report ;; Reporting
developing ;; Coming_to_be || found ;; Coming_to_believe || preparing ;; GetReady
regulating ;; Control || oversight ;; Scrutiny || meeting ;; Communication || promoting ;; Cause_to_make_progress || saying ;; Statement || faced ;; Confronting_problem
lawsuits ;; Legality
Murders ;; Killing || deaths ;; Death || poisoning ;; Bodily_harm || resulting ;; Causation
taken ;; Ingestion || laced ;; Motion
died ;; Death || poisonings ;; Bodily_harm || deaths ;; Death
poisonings ;; Bodily_harm || charged ;; Legality || convicted ;; Legality
sending ;; Sending || demanded ;; Request || emerged ;; Coming_to_be || took ;; Giving || deaths ;; Death || extortion ;; Committing_crime || poisoning ;; Bodily_harm || convicted ;; Legality || stop ;; Preventing_or_letting
reforms ;; Reforming_a_system || led to ;; Causation || incidents ;; Catastrophe
reduce ;; Cause_change_of_position_on_a_scale || deaths ;; Death || warn ;; Warning || praised ;; Judgment_communication || response ;; Response || crisis ;; Catastrophe || poisoning ;; Bodily_harm
entered ;; Arriving
caused ;; Causation
became ;; Becoming
observed ;; Scrutiny
felt ;; Perception_active || heat ;; Change
burst ;; Motion || exploded ;; Attack
explosion ;; Catastrophe || penetrated ;; Self_motion || producing ;; Creating || generated ;; Creating
impact ;; Influence || released ;; Causation || detonated ;; Attack
undetected ;; Know
treatment ;; Cure || injured ;; Bodily_harm || created ;; Creating || explosion ;; Catastrophe
injuries ;; Bodily_harm || broken ;; Destroying || blown ;; Motion || arrived ;; Arriving || flash ;; Lighting || effects ;; Influence
damaged ;; Damaging || help ;; Assistance || explosion ;; Catastrophe || scrambled ;; Self_motion || repair ;; Recovering
destroyed ;; Destroying || entered ;; Arriving
injuries ;; Bodily_harm || resulted in ;; Causation || confirmed ;; Check
deaths ;; Death || reported ;; Reporting
approach ;; Arriving || occurred ;; Presence
obtained ;; Getting || dressed ;; Wearing || placed ;; Placing || eating ;; Ingestion || died ;; Death || identifying ;; Check
placed ;; Placing || planned ;; Arranging || suggested ;; Statement
began ;; Process_start || transporting ;; Sending || approval ;; Supporting || picked up ;; Choosing
shared ;; Communication
examination ;; Scrutiny || showed ;; Presence || showed ;; Presence
received ;; Receiving || shifted ;; Change || invasion ;; Attack
losses ;; Earnings_and_losses
depicted ;; Create_artwork || planned ;; Arranging || wrote ;; Writing || carried out ;; Action
work ;; Creating || formed ;; Creating
Hurricane ;; Catastrophe || destructive ;; Destroying || hurricanes ;; Catastrophe || affect ;; Influence
developed ;; Coming_to_be || named ;; Name_conferral || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || attained ;; Achieve
hurricane ;; Catastrophe || crossing ;; Motion_directional || storm ;; Catastrophe || weakened ;; Cause_change_of_position_on_a_scale
became ;; Becoming || storms ;; Catastrophe
beginning ;; Process_start || hurricane ;; Catastrophe || storm ;; Catastrophe || interacted ;; Connect || interacted ;; Connect
moving ;; Motion || re-intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe || attained ;; Achieve
hurricane ;; Catastrophe || dissipating ;; Dispersal || accelerated ;; Cause_change_of_position_on_a_scale || absorbed ;; Ingestion
became ;; Becoming || storm ;; Catastrophe || weakened ;; Cause_change_of_position_on_a_scale || affecting ;; Influence
storm ;; Catastrophe || produced ;; Creating
caused ;; Causation || flooding ;; Motion || flooding ;; Motion || led to ;; Causation
hurricane ;; Catastrophe || halted ;; Process_end || evacuations ;; Escaping || eruption ;; Process_start || threatened ;; Warning
deaths ;; Death
began ;; Process_start || floods ;; Catastrophe || affected ;; Influence
floods ;; Catastrophe || damage ;; Damaging || brunt ;; Destroying || casualties ;; Death || affected ;; Influence
destruction ;; Destroying || floods ;; Catastrophe || affected ;; Influence || death ;; Death
asked ;; Request || noting ;; Statement
received ;; Receiving || relief ;; Assistance || funds ;; Supporting
drink ;; Ingestion || reported ;; Reporting || arriving ;; Arriving
damage ;; Damaging || harmed ;; Damaging
damages ;; Damaging || Damage ;; Damaging
tied ;; Connect || hurricane ;; Catastrophe
formed ;; Coming_to_be || organizing ;; Creating || tracked ;; Self_motion
executed ;; Self_motion
began ;; Process_start || deepening ;; Cause_change_of_strength
becoming ;; Becoming
weakened ;; Cause_change_of_strength
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || accelerating ;; Cause_to_make_progress || absorbed ;; Ingestion
developing ;; Expansion || produced ;; Manufacturing
destroyed ;; Destroying
killed ;; Killing
caused ;; Causation || brought ;; Bringing
casualties ;; Catastrophe
named ;; Name_conferral || remaining ;; Temporary_stay || cross ;; Motion_directional
formed ;; Coming_to_be || moving ;; Motion || named ;; Name_conferral || intensified ;; Cause_change_of_strength
became ;; Becoming
approached ;; Arriving || veered ;; Self_motion
reaching ;; Arriving
crossing ;; Motion_directional || made ;; Manufacturing || weakened ;; Cause_change_of_strength
renamed ;; Name_conferral || re-intensified ;; Cause_change_of_strength || dissipating ;; Dispersal || entering ;; Arriving
devastating ;; Catastrophe || storm ;; Catastrophe || damage ;; Damaging || threatened ;; Warning || feared ;; Change_sentiment
damaged ;; Damaging
damaged ;; Damaging || death ;; Death
flooding ;; Motion || downed ;; Ingestion || produced ;; Manufacturing
damaged ;; Damaging || destroyed ;; Destroying || made ;; Manufacturing
damaged ;; Damaging || flipped ;; Attack
Damage ;; Damaging
sponsored ;; Supply || marathon ;; Social_event || held ;; Hold
run ;; Motion
organized ;; Arranging
run ;; Self_motion || changed ;; Change
include ;; Cause_to_be_included
includes ;; Cause_to_be_included
achieve ;; Achieve || achieve ;; Achieve || accredited ;; Judgment_communication
achieved ;; Achieve || accredited ;; Judgment_communication
hosted ;; Social_event
set ;; Placing
held ;; Hold || starting ;; Process_start || finishing ;; Process_end
Operation ;; Military_operation
supported ;; Supporting || involved ;; Cause_to_be_included
began ;; Process_start || moved ;; Motion
Battle ;; Hostile_encounter || operation ;; Military_operation
War ;; Hostile_encounter || airlift ;; Arranging
stretch ;; Expansion || pushed ;; Self_motion
armored ;; Wearing
pushed ;; Self_motion
pushed ;; Self_motion || entered ;; Arriving
capture ;; Conquering || Battle ;; Hostile_encounter
began ;; Process_start || destroy ;; Destroying
controlling ;; Control || transformed ;; Change
started ;; Process_start || launching ;; Military_operation
siege ;; Besieging || led ;; Conquering || lead to ;; Causation
casualties ;; Catastrophe || suffered ;; Catastrophe || forces ;; Causation || battle ;; Hostile_encounter || rallied ;; Filling
assaulted ;; Attack
destroyed ;; Destroying || removing ;; Removing || combined ;; Cause_to_amalgamate
took ;; Conquering || agreed ;; Agree_or_refuse_to_act || control ;; Control
conducted ;; Action || campaign ;; Military_operation
joined ;; Connect || crossed ;; Motion_directional || returned ;; Arriving
destroyed ;; Destroying || end ;; Process_end
Believing ;; Coming_to_believe || withdrew ;; Escaping
based ;; Presence || involved ;; Participation || miscalculated ;; Judgment_communication
raid ;; Attack || Taking ;; Bringing || forces ;; Military_operation || avoiding ;; Preventing_or_letting || forces ;; Military_operation || ordered ;; Request || instructions ;; Education_teaching
sailing ;; Self_motion || Departing ;; Departing || cruise ;; Self_motion
dispatched ;; Arranging
deserted ;; Escaping || learned ;; Education_teaching || crossed ;; Motion_directional || joined ;; Participation
discovered ;; Know || destroyed ;; Destroying || managed ;; Control || escape ;; Escaping
provided ;; Supply || raiding ;; Attack || failed ;; Agree_or_refuse_to_act || defeated ;; Conquering || became ;; Becoming || forces ;; Military_operation || draw ;; Removing || operating ;; Being_in_operation || intercepted ;; Preventing_or_letting
achieved ;; Achieve || return ;; Motion || scattered ;; Dispersal || caught ;; Bodily_harm
destroyed ;; Destroying || damaged ;; Damaging || shelter ;; Assistance || forces ;; Military_operation || forced ;; Military_operation || intercepted ;; Preventing_or_letting
arriving ;; Arriving || returned ;; Arriving
campaign ;; Military_operation
weakened ;; Cause_change_of_strength || ended ;; Process_end || losses ;; Earnings_and_losses || suffered ;; Bodily_harm || participate ;; Participation || break out ;; Process_start
hurricane ;; Catastrophe || hurricane ;; Catastrophe
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || named ;; Name_conferral || hurricane ;; Catastrophe
storm ;; Catastrophe || intensified ;; Cause_change_of_strength || upgraded ;; Cause_to_make_progress
strengthen ;; Cause_change_of_strength || attained ;; Getting
hurricane ;; Catastrophe || weakening ;; Cause_change_of_strength
storm ;; Catastrophe || hurricane ;; Catastrophe || deteriorated ;; Change
caused ;; Causation || re-intensify ;; Cause_change_of_strength
hurricane ;; Catastrophe || tracked ;; Self_motion || re-strengthened ;; Cause_change_of_strength
hurricane ;; Catastrophe || peaked ;; Arriving
began ;; Process_start || storm ;; Catastrophe || weakened ;; Cause_change_of_strength
storm ;; Catastrophe || storm ;; Catastrophe || curved ;; Motion_directional || weakened ;; Cause_change_of_strength
transitioned ;; Change
brushed ;; Motion || producing ;; Manufacturing
storm ;; Catastrophe
storm ;; Catastrophe || occurred ;; Coming_to_be || generated ;; Creating || overturned ;; Self_motion || interaction ;; Influence
affected ;; Influence
tour ;; Traveling
supported ;; Supporting
reaching ;; Arriving
make ;; Manufacturing || incorporated ;; Cause_to_amalgamate
trained ;; Education_teaching || jogging ;; Self_motion
collaborated ;; Collaboration || expanding ;; Expansion || bringing ;; Bringing
presenting ;; Presence
looked ;; Perception_active || projected ;; Arranging
changes ;; Change || causes ;; Causation || addressing ;; Expressing_publicly || consisted ;; Forming_relationships
commending ;; Judgment_communication || concert ;; Social_event
playing ;; Competition
concert ;; Social_event
released ;; Publishing || titled ;; Name_conferral || broadcast ;; Expressing_publicly
reach ;; Arriving || commented ;; Statement
marked ;; Recording
noted ;; Know || given rise to ;; Causation
erected ;; Building || wearing ;; Wearing || used ;; Using
shifted ;; Change || battles ;; Hostile_encounter
ways ;; Action || practiced ;; Practice || combining ;; Cause_to_amalgamate || resumed ;; Process_start || conquests ;; Conquering || initiated ;; Process_start
Battle ;; Hostile_encounter || overrunning ;; Destroying || seen ;; Perception_active || lost ;; Earnings_and_losses
end ;; Process_end || control ;; Control || levied ;; Imposing_obligation || took ;; Conquering
attacked ;; Attack || capturing ;; Conquering || catalyzing ;; Process_start
looting ;; Robbery || gains ;; Getting || mixed ;; Cause_to_amalgamate || made ;; Getting
extended ;; Expansion || driven ;; Motion
extract ;; Getting || led to ;; Causation
taken over ;; Conquering || wars ;; Hostile_encounter || weakened ;; Cause_change_of_strength || rise ;; Cause_change_of_position_on_a_scale || conflict ;; Hostile_encounter || leading to ;; Causation
Tour ;; Traveling
began ;; Process_start
scheduled ;; Arranging || shared ;; Communication
told ;; Telling || practice ;; Practice || ready ;; GetReady || drop ;; Motion_directional || looked ;; Change_sentiment || falling ;; Motion_directional
dropped ;; Motion_directional
get ;; Getting
finish ;; Process_end || rehearsed ;; Practice || show ;; Social_event
run off ;; Self_motion || know ;; Know || run away ;; Self_motion
described ;; Statement
mixed ;; Cause_to_amalgamate || descended on ;; Motion_directional
targeted ;; Aiming
exercised ;; Practice
committed ;; Commitment
restrain ;; Hindering || permitted ;; Preventing_or_letting
achieved ;; Achieve
minimize ;; Cause_change_of_position_on_a_scale || took place ;; Process_start
given ;; Giving || ordered ;; Arranging || restrain ;; Hindering
contributed ;; Giving || drove ;; Motion
conducted ;; Action || preventing ;; Preventing_or_letting || campaign ;; Military_operation || operations ;; Military_operation || travelling ;; Traveling
involved ;; Cause_to_be_included || raiding ;; Attack || claimed ;; Statement || campaign ;; Military_operation || engagements ;; Hostile_encounter || victory ;; Earnings_and_losses || forces ;; Causation || culminating ;; Process_end || mauled ;; Bodily_harm
lost ;; Earnings_and_losses || reach ;; Arriving || battle ;; Hostile_encounter || distracted ;; Hindering
war ;; Military_operation
provide ;; Supply || supported ;; Supporting || famine ;; Catastrophe || looked ;; Perception_active || convoyed ;; Patrolling
devastated ;; Destroying || reduced ;; Cause_change_of_position_on_a_scale || upheaval ;; Hindering || fight ;; Hostile_encounter || shortages ;; Confronting_problem || supply ;; Supply || weakening ;; Cause_change_of_strength
shortage ;; Confronting_problem || command ;; Control || trained ;; Education_teaching || suffering ;; Confronting_problem
reach ;; Arriving || tasked ;; Confronting_problem || keeping ;; Preserving || occupied ;; Placing
dispatched ;; Sending || protect ;; Defending || knew ;; Know || convoy ;; Patrolling || commanded ;; Control || pursuing ;; Aiming
convoy ;; Patrolling || manoeuvred ;; Practice || drawing ;; Bringing
seized ;; Conquering || choose ;; Choosing || attack ;; Attack || granting ;; Giving
became ;; Becoming || culminating ;; Process_end || took place ;; Process_start
attack ;; Attack || engagement ;; Hostile_encounter || fight ;; Hostile_encounter || battle ;; Hostile_encounter
inflicted ;; Causation || fighting ;; Hostile_encounter || contested ;; Quarreling || battle ;; Hostile_encounter || defeat ;; Conquering
retreat ;; Escaping || captured ;; Conquering || inflicted ;; Causation || Forcing ;; Preventing_or_letting || sank ;; Motion_directional || casualties ;; Death
claimed ;; Statement || success ;; Earnings_and_losses || reach ;; Arriving || delaying ;; Change_event_time || convoy ;; Patrolling || bought ;; Supply
defeats ;; Earnings_and_losses || war ;; Military_operation || bred ;; Creating || engage ;; Hostile_encounter || battle ;; Hostile_encounter || suffered ;; Confronting_problem
invasion ;; Attack
carried out ;; Military_operation
support ;; Supporting
capture ;; Conquering || relieved ;; Cure || landed ;; Self_motion
give ;; Giving || capture ;; Conquering || speed ;; Cause_change_of_position_on_a_scale || advance ;; Motion || lead to ;; Causation || defeat ;; Conquering
shot down ;; Use_firearm || damaged ;; Damaging || carrying ;; Carry_goods || turned ;; Becoming
scattered ;; Dispersal || landed ;; Self_motion
captured ;; Conquering || held ;; Defending || defence ;; Defending || repulsed ;; Defending || attacks ;; Attack || increasing ;; Cause_change_of_position_on_a_scale
halted ;; Process_end || relief ;; Assistance || transport ;; Sending
control ;; Control || relinquished ;; Surrendering || running ;; Self_motion
joined ;; Becoming_a_member || recapture ;; Conquering || support ;; Supporting
established ;; Building || start ;; Process_start || secured ;; Getting
advance ;; Cause_to_make_progress || established ;; Building || lead to ;; Causation || gathered ;; Come_together
captured ;; Conquering
withdrawn ;; Escaping
learned ;; Education_teaching || practice ;; Practice || put ;; Bringing
conducted ;; Action || test ;; Testing || operation ;; Military_operation || test ;; Testing
preceded ;; Process_start
exercise ;; Practice || shot ;; Use_firearm
fired ;; Use_firearm || viewed ;; Perception_active
fired ;; Use_firearm || test ;; Testing || used ;; Using || testing out ;; Testing
testing ;; Testing || examine ;; Testing || tested ;; Testing
used ;; Using
employed ;; Employment || test ;; Testing || devices ;; Using
played ;; Competition
held ;; Hold
reached ;; Achieve
eliminated ;; Removing
resulted in ;; Causation || falling ;; Cause_change_of_position_on_a_scale
testing ;; Testing || received ;; Receiving
ended up ;; Process_end || becoming ;; Becoming || defeating ;; Conquering
became ;; Becoming || win ;; Getting
defended ;; Defending || defeating ;; Conquering
join ;; Becoming_a_member || allowed ;; Causation
become ;; Becoming
participate ;; Participation || leaving ;; GiveUp
defeating ;; Conquering
teaming up ;; Cause_to_amalgamate || teaming up ;; Cause_to_amalgamate || separated ;; Forming_relationships
fell ;; Earnings_and_losses || win ;; Getting || defeating ;; Conquering
competing ;; Competition
defeating ;; Conquering
winning ;; Getting
teaming up ;; Cause_to_amalgamate || defeated ;; Conquering || defeat ;; Conquering
teamed up ;; Cause_to_amalgamate || defeated ;; Conquering || defeat ;; Conquering
win ;; Getting
tests ;; Testing || occurred ;; Presence
tests ;; Testing || yields ;; Creating || performed ;; Action
conducted ;; Action || test ;; Testing
investigate ;; Research || trials ;; Testing || explosions ;; Use_firearm || used ;; Using || effects ;; Influence
cleanup ;; Emptying || contaminated ;; Destroying
found ;; Know || existed ;; Presence || tests ;; Testing || commission ;; Research || report ;; Reporting || test ;; Research || delivered ;; Sending || examination ;; Scrutiny || effects ;; Influence
recommended ;; Convincing || cost ;; Cost || completed ;; Process_end || cleanup ;; Emptying
Debate ;; Quarreling || effects ;; Influence
paid ;; Commerce_pay
change ;; Change || covered ;; Hiding_objects || tests ;; Testing || tests ;; Testing
scrutiny ;; Scrutiny || became ;; Becoming || investigated ;; Criminal_investigation
ran ;; Action
wrote ;; Writing || titled ;; Name_conferral
clean-up ;; Emptying || documented ;; Statement
written ;; Writing
killing ;; Killing || aviation ;; Traveling || crashed ;; Catastrophe
crash ;; Catastrophe
crash ;; Catastrophe || incident ;; Catastrophe
dead ;; Death
attributed ;; Protest || cause ;; Causation || fire ;; Catastrophe || originated ;; Coming_to_be
advanced ;; Cause_to_make_progress || suppressed ;; Hindering || strike ;; Destroying || bomb ;; Catastrophe || accident ;; Catastrophe || theory ;; Statement || cause ;; Causation
alleged ;; Statement || caused ;; Causation || crash ;; Catastrophe || saw ;; Self_motion || indicating ;; Statement || typist ;; Employment || report ;; Reporting || missile ;; Bearing_arms || test ;; Testing
disappeared ;; Departing || crash ;; Catastrophe || reported ;; Reporting
torn ;; Destroying || missile ;; Bearing_arms
damaged ;; Damaging || recording ;; Recording || unreadable ;; Having_or_lacking_access || recorded ;; Recording || said ;; Statement
seized ;; Hold || recovered ;; Recovering
started ;; Process_start || investigation ;; Scrutiny || theory ;; Statement || fire ;; Use_firearm || started ;; Process_start
killed ;; Killing || missile ;; Bearing_arms || member ;; Participation || judge ;; Legal_rulings || said ;; Statement || said ;; Statement || hit ;; Attack
waiting ;; Motion
asked ;; Request || crash ;; Catastrophe || wrote ;; Communication || begin ;; Process_start || declassified ;; Removing || family ;; Forming_relationships || affair ;; Catastrophe || releasing ;; Publishing || victim ;; Death || saying ;; Statement
transport ;; Bringing || report ;; Reporting
published ;; Publishing || accident ;; Catastrophe
examination ;; Scrutiny || launching ;; Motion || declaration ;; Expressing_publicly || strike ;; Attack || suggestion ;; Statement || findings ;; Presence || recovered ;; Recovering || knowledge ;; Education_teaching || rejected ;; Agree_or_refuse_to_act || basing ;; Causation
surmised ;; Coming_to_believe || loss ;; Earnings_and_losses || discarded ;; Removing || caused ;; Causation || caused ;; Causation || report ;; Reporting || fire ;; Catastrophe
visitors ;; Traveling || festival ;; Social_event || attracting ;; Influence || Festival ;; Social_event
festival ;; Social_event || known ;; Know || dancing ;; Social_event
beginners ;; Process_start || dancing ;; Social_event
taken place ;; Process_start
festival ;; Social_event
provides ;; Supply
festival ;; Social_event || organised ;; Arranging || dance ;; Social_event
festival ;; Social_event || supported ;; Supporting
festival ;; Social_event || dancing ;; Social_event || dancers ;; Social_event
ends ;; Process_end || participants ;; Participation || festival ;; Social_event
beginners ;; Process_start
presents ;; Presence || lighting ;; Lighting
voted ;; Choosing || limit ;; Limiting
flown ;; Motion
changed ;; Change
limit ;; Limiting || move ;; Motion || party ;; Social_event || opposed ;; Agree_or_refuse_to_act
response ;; Response || held ;; Defending || protests ;; Attack
decision ;; Deciding || war ;; Military_operation
storm ;; Attack
involved ;; Cause_to_be_included || blocking ;; Hindering || carrying ;; Carry_goods || held ;; Defending
clashes ;; Violence || sparking ;; Lighting || led to ;; Causation
responded ;; Response || attacked ;; Attack
sent ;; Sending || attacked ;; Attack || party ;; Social_event || party ;; Social_event
orchestrated ;; Arranging || violence ;; Violence || force ;; Causation
tension ;; Violence || put up ;; Placing || heightened ;; Cause_change_of_position_on_a_scale
restrictions ;; Limiting || issues ;; Creating || protests ;; Attack
caused ;; Causation || damage ;; Damaging || Hurricane ;; Catastrophe
named ;; Name_conferral || storm ;; Catastrophe || hurricane ;; Catastrophe || originated ;; Coming_to_be
gaining ;; Getting || storm ;; Catastrophe || strength ;; Cause_change_of_strength || meandered ;; Motion_directional
storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || indicated ;; Change_sentiment || attained ;; Achieve
caused ;; Causation || storm ;; Catastrophe || upwelling ;; Motion || decrease ;; Cause_change_of_position_on_a_scale || leading to ;; Causation || weakening ;; Cause_change_of_strength
moved ;; Motion || damaging ;; Damaging || storm ;; Catastrophe || producing ;; Creating
rescued ;; Rescuing || died ;; Death || recovered ;; Cure || cause ;; Causation
killed ;; Killing || lightning ;; Lighting || struck ;; Bodily_harm
performed ;; Hold
decided ;; Deciding || use ;; Using
form ;; Coming_to_be || giving ;; Giving
flashed ;; Motion
surrounded ;; Surrounding || prompting ;; Causation || appeared ;; Presence
said ;; Statement
visited ;; Traveling || rolled ;; Motion
awarded ;; Award
given ;; Giving
Hurricane ;; Catastrophe || Hurricane ;; Catastrophe || cyclones ;; Catastrophe || destructive ;; Destroying || strike ;; Destroying
hurricane ;; Catastrophe || hurricane ;; Catastrophe || formed ;; Coming_to_be || becoming ;; Becoming || landfall ;; Arriving || Category ;; Coming_to_be || storm ;; Catastrophe
caused ;; Causation || destroyed ;; Destroying || hurricane ;; Catastrophe || killed ;; Killing || damaged ;; Damaging || losses ;; Earnings_and_losses
affected ;; Influence || seen ;; Action || Damaged ;; Damaging
hurricane ;; Catastrophe || powerful ;; Change || deadliest ;; Motion || eclipsed ;; Lighting
existence ;; Presence
developed ;; Coming_to_be || storm ;; Catastrophe || depression ;; Catastrophe || cyclone ;; Catastrophe || hurricane ;; Catastrophe || concluded ;; Coming_to_believe || tracked ;; Self_motion
hurricane ;; Catastrophe || strengthen ;; Cause_change_of_strength
storm ;; Catastrophe || hurricane ;; Catastrophe || landfalls ;; Arriving || propelled ;; Cause_change_of_position_on_a_scale
dissipated ;; Change || moving ;; Motion || transitioned ;; Change || cyclone ;; Catastrophe
explosion ;; Catastrophe
died ;; Death || injured ;; Bodily_harm
explosion ;; Catastrophe
recovered ;; Recovering || extinguished ;; Removing
explosion ;; Catastrophe
transported ;; Sending
allowed ;; Preventing_or_letting
parked ;; Placing || illuminated ;; Lighting
said ;; Communication
announced ;; Expressing_publicly || removed ;; Removing || appointed ;; Request
took place ;; Process_start
told ;; Telling || piled ;; Placing
carried out ;; Military_operation
ruled ;; Legal_rulings
accused ;; Judgment_communication
found ;; Know || terrorism ;; Terrorism || stated ;; Statement
disaster ;; Catastrophe || referred ;; Name_conferral
industrial ;; Manufacturing || disaster ;; Catastrophe || considered ;; Deciding
exposed ;; Reveal_secret
death ;; Death
death ;; Death
confirmed ;; Check || deaths ;; Death
injuries ;; Bodily_harm || injuries ;; Bodily_harm || injuries ;; Bodily_harm || stated ;; Statement || caused ;; Causation || disabling ;; Preventing_or_letting
estimate ;; Statement || died ;; Death || died ;; Death || diseases ;; Catastrophe
cause ;; Causation || disaster ;; Catastrophe || remains ;; Presence || debate ;; Quarreling
caused ;; Causation || argue ;; Quarreling || deferred ;; Change_event_time || created ;; Creating || triggering ;; Causation || disaster ;; Catastrophe
argues ;; Quarreling || entered ;; Arriving
holding ;; Hold
paid ;; Commerce_pay || settle ;; Resolve_problem || stemming ;; Preventing_or_letting || disaster ;; Catastrophe
ended ;; Process_end || turned ;; Becoming || merged ;; Cause_to_amalgamate || terminated ;; Process_end || sold ;; Commerce_sell || lease ;; Renting
purchased ;; Commerce_buy || disaster ;; Catastrophe
filed ;; Submitting_documents || focused ;; Aiming
filed ;; Submitting_documents || involving ;; Cause_to_be_included
allowed ;; Preventing_or_letting || employees ;; Employment || causing ;; Causation || death ;; Death || convicted ;; Legal_rulings || sentenced ;; Rewards_and_punishments
released ;; Releasing
died ;; Death || employee ;; Employment || convicted ;; Legal_rulings || passed ;; Process_end
cyclone ;; Catastrophe || storm ;; Catastrophe || cyclone ;; Catastrophe || referred ;; Name_conferral || evacuation ;; Emptying || prompted ;; Cause_change_of_position_on_a_scale
Originating ;; Coming_to_be || classified ;; Check
passing ;; Motion_directional || striking ;; Attack || storm ;; Catastrophe || Tracking ;; Scrutiny || organized ;; Causation
intensified ;; Cause_change_of_strength || turned ;; Motion
cyclone ;; Catastrophe || reached ;; Arriving || hurricane ;; Catastrophe
cyclone ;; Catastrophe || turned ;; Change
landfall ;; Arriving || made ;; Causation
deaths ;; Death || resulted ;; Causation || crash ;; Catastrophe || killed ;; Killing
damaged ;; Damaging || destroyed ;; Destroying || killed ;; Killing || storm ;; Catastrophe || produced ;; Creating
losses ;; Earnings_and_losses || hit ;; Attack
cyclone ;; Catastrophe || reached ;; Arriving || repeat ;; Change || take place ;; Process_start
result ;; Causation || plans ;; Arranging || plans ;; Arranging || enacted ;; Statement || evacuation ;; Emptying || relocate ;; Motion
evacuation ;; Emptying || storm ;; Catastrophe || turned ;; Becoming
deaths ;; Death || damage ;; Damaging || damaged ;; Damaging || recorded ;; Recording
starting ;; Process_start || involved ;; Cause_to_be_included || Wars ;; Hostile_encounter || Wars ;; Hostile_encounter || viewed ;; Perception_active
control ;; Control || disputes ;; Quarreling || governance ;; Control || originated ;; Coming_to_be
came to ;; Motion || impose ;; Imposing_obligation
favoured ;; Choosing || advocated ;; Convincing
pledged ;; Commitment || expel ;; Removing || oppose ;; Agree_or_refuse_to_act || voted ;; Choosing || assembly ;; Building || covenant ;; Sign_agreement
leaving ;; Departing || control ;; Control || forces ;; Causation || defeated ;; Conquering
kidnapped ;; Kidnapping
kidnappings ;; Kidnapping || terrorist ;; Terrorism || claimed ;; Statement
conferences ;; Communication || escape ;; Escaping || described ;; Statement || capture ;; Conquering
released ;; Releasing || believed ;; Know
raised ;; Cause_change_of_position_on_a_scale
suggested ;; Statement || exchange ;; Exchange || use ;; Using || reports ;; Reporting
found ;; Know
claimed ;; Statement || died ;; Death
rescued ;; Rescuing || freed ;; Releasing
found ;; Know
freed ;; Releasing
rescued ;; Rescuing
cut off ;; Hindering || crisis ;; Quarreling || dispute ;; Quarreling || broke out ;; Process_start
expelled ;; Removing || prompting ;; Causation || dispatch ;; Sending
ship ;; Sending || orders ;; Arranging || intercept ;; Preventing_or_letting || sailing ;; Self_motion
captured ;; Conquering
interned ;; Arrest
enforced ;; Action || blockade ;; Besieging
left ;; Departing
seized ;; Hold || ended ;; Process_end || support ;; Supporting || war ;; Hostile_encounter || installed ;; Change_of_leadership
occurred ;; Coming_to_be || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || War ;; Hostile_encounter
Fought ;; Hostile_encounter || prevented ;; Preventing_or_letting || losses ;; Earnings_and_losses || commanded ;; Control || command ;; Control || Siege ;; Besieging || relieving ;; Assistance || defeat ;; Earnings_and_losses
battle ;; Hostile_encounter || link ;; Having_or_lacking_access || defeat ;; Earnings_and_losses || Battle ;; Hostile_encounter
ordered ;; Request || disruptive ;; Destroying
marched ;; Self_motion || ordering ;; Request || command ;; Control || relieving ;; Assistance || informed ;; Know || siege ;; Besieging || travelled ;; Arriving
attack ;; Attack || believing ;; Know || follow ;; Motion
attack ;; Attack || moved ;; Motion
sent ;; Sending || drove ;; Motion || forced ;; Causation || retreat ;; Escaping
prevented ;; Preventing_or_letting || attacked ;; Attack || joining ;; Becoming_a_member
regroup ;; Cause_to_amalgamate || driven ;; Motion || retreated ;; Escaping
killed ;; Killing || casualties ;; Killing || taken ;; Bringing
defeat ;; Earnings_and_losses || prevented ;; Preventing_or_letting || relieving ;; Assistance
ambush ;; Attack || withdrew ;; Escaping || destroyed ;; Destroying
played ;; Competition || supported ;; Supporting || match ;; Competition || Shield ;; Competition
match ;; Competition || contested ;; Competition
held ;; Hold
Watched ;; Perception_active || match ;; Competition || crowd ;; Filling
appearance ;; Presence
debut ;; Process_start || made ;; Action || named ;; Name_conferral
start ;; Process_start || started ;; Process_start || passed ;; Process_end
match ;; Competition || past ;; Motion || scored ;; Competition || came ;; Arriving || collected ;; Come_together
shake ;; Body_movement || game ;; Competition || drew ;; Getting
victory ;; Process_end
festival ;; Social_event || held ;; Hold
signed ;; Sign_agreement || play ;; Competition
include ;; Cause_to_be_included || labels ;; Labeling
Festival ;; Social_event || ran ;; Action || replaced ;; Change_of_leadership
replaced ;; Change_of_leadership || held ;; Hold
concert ;; Social_event || worldwide ;; Dispersal || tours ;; Traveling || embarked ;; Process_start
released ;; Publishing
released ;; Publishing
released ;; Publishing || includes ;; Cause_to_be_included
viewing ;; Perception_active || available ;; Having_or_lacking_access
released ;; Publishing || reached ;; Achieve
released ;; Publishing || entitled ;; Name_conferral || self-titled ;; Labeling
released ;; Publishing
riots ;; Violence || response ;; Response || riots ;; Violence || assassination ;; Killing
riots ;; Violence
killed ;; Killing || deaths ;; Death || estimate ;; Statement
Violence ;; Violence
evade ;; Escaping || arrest ;; Arrest || invited ;; Request
killed ;; Killing || violent ;; Violence || leading ;; Causation || opposed ;; Agree_or_refuse_to_act || killed ;; Killing
deaths ;; Death || incidents ;; Catastrophe || violent ;; Violence || riots ;; Violence || injured ;; Bodily_harm
operation ;; Military_operation || armed ;; Bearing_arms || carried out ;; Military_operation || ordered ;; Arranging || remove ;; Removing
died ;; Death || removed ;; Removing
criticized ;; Judgment_communication || assault ;; Attack
assassinated ;; Killing || operation ;; Military_operation
led to ;; Causation || riots ;; Violence || death ;; Death || killings ;; Killing
reported ;; Reporting || fled ;; Escaping || riots ;; Violence || reported ;; Reporting
believed ;; Know || massacre ;; Killing || organised ;; Arranging
increased ;; Cause_change_of_position_on_a_scale || violence ;; Violence || collusion ;; Collaboration || penalise ;; Rewards_and_punishments || support ;; Supporting || killings ;; Killing || genocide ;; Killing
reported ;; Reporting || prosecute ;; Legality || killings ;; Killing
convinced ;; Convincing || riots ;; Violence || leaks ;; Catastrophe || called ;; Labeling
riots ;; Violence || identified ;; Coming_to_believe || genocide ;; Killing || occurred ;; Presence || acknowledged ;; Statement
discovered ;; Know || reported ;; Reporting || attacks ;; Attack || attacks ;; Attack || revenge ;; Revenge
violence ;; Violence || organised ;; Arranging || support ;; Supporting || believes ;; Know
wrestling ;; Competition || Wrestling ;; Competition || event ;; Competition || produced ;; Creating
took place ;; Process_start
included ;; Cause_to_be_included || brands ;; Labeling || matches ;; Competition || match ;; Competition
matches ;; Competition || matches ;; Competition || contested ;; Competition
match ;; Competition || match ;; Competition || choosing ;; Choosing || match ;; Competition || earn ;; Getting
match ;; Competition || interference ;; Hindering || retained ;; Getting
retaining ;; Preserving || retaining ;; Preserving
lose it ;; Competition || defeat ;; Conquering || defeated ;; Conquering || retain ;; Preserving
received ;; Receiving
organised ;; Arranging || Festivals ;; Social_event
billed ;; Statement
end ;; Process_end || attend ;; Participation
recording ;; Recording || showcase ;; Social_event
developing ;; Coming_to_be || brands ;; Labeling || campaigns ;; Military_operation || focused ;; Aiming
concert ;; Social_event || tour ;; Traveling
visited ;; Traveling
marked ;; Recording
planned ;; Arranging || apologized ;; Judgment_communication || cancelled ;; Change_event_time || reach ;; Arriving
described ;; Statement
performed ;; Hold || performed ;; Hold || bring ;; Bringing || labeled ;; Labeling
received ;; Receiving || reception ;; Judgment_communication
threatened ;; Warning || Lawsuits ;; Legality
sold ;; Commerce_sell || created ;; Creating
dubbed ;; Name_conferral
becoming ;; Becoming || surpassing ;; Cause_change_of_position_on_a_scale || grossed ;; Earnings_and_losses || shows ;; Social_event
record ;; Recording || occupying ;; Control || broke ;; Destroying
accepted ;; Receiving
aired ;; Expressing_publicly || concert ;; Social_event || titled ;; Name_conferral
included ;; Cause_to_be_included || documented ;; Recording
released ;; Publishing
nicknamed ;; Name_conferral
participated ;; Participation
joined ;; Connect || competition ;; Competition || rebranded ;; Labeling
held ;; Defending
shortened ;; Change_event_time || include ;; Cause_to_be_included
played ;; Competition
series ;; Competition
tournament ;; Competition || rebranded ;; Labeling
competition ;; Competition || beat ;; Competition
title ;; Name_conferral || title ;; Name_conferral || losing ;; Earnings_and_losses
winning ;; Getting || games ;; Competition || eliminate ;; Removing
defeated ;; Conquering || final ;; Competition
clash ;; Competition || final ;; Competition || final ;; Competition || losing ;; Earnings_and_losses || lost ;; Earnings_and_losses || lost ;; Earnings_and_losses || final ;; Competition
defending ;; Defending || semi-finals ;; Competition || eliminated ;; Removing
occurred ;; Coming_to_be
occurred ;; Coming_to_be
record ;; Recording || records ;; Recording
received ;; Receiving
began ;; Process_start
dumped ;; Removing || peaking ;; Self_motion
rescue ;; Rescuing || described ;; Statement
described ;; Statement || exceed ;; Cause_change_of_position_on_a_scale
Championships ;; Competition
participated ;; Participation || joining ;; Becoming_a_member || competition ;; Competition || competition ;; Competition || gain ;; Getting || competed ;; Competition
competition ;; Competition || competition ;; Competition
Championship ;; Competition || matches ;; Competition || took place ;; Process_start || played ;; Competition
took part ;; Dispersal || going to ;; Arriving
became ;; Becoming || Championships ;; Competition || final ;; Process_end || game ;; Competition || games ;; Competition || beat ;; Competition
ousted ;; Change_of_leadership
took ;; Conquering || seized ;; Hold || arrested ;; Process_end
overthrow ;; Change_of_leadership || Distrusted ;; Judgment_communication || assigned ;; Deciding
joined ;; Connect || overthrow ;; Change_of_leadership || felt ;; Change_sentiment
defeat ;; Conquering || attempted ;; Hold
pursued ;; Cause_to_make_progress || integrate ;; Cause_change_of_position_on_a_scale || felt ;; Change_sentiment
armed ;; Bearing_arms
withdrawal ;; Escaping
decided ;; Deciding || maintained ;; Statement
promoting ;; Cause_change_of_position_on_a_scale || pay ;; Commerce_pay
accused ;; Judgment_communication
brought ;; Bringing
allowed ;; Preventing_or_letting
forced ;; Motion
raid ;; Attack
command ;; Control || carried out ;; Military_operation || awarded ;; Award
form ;; Coming_to_be
paddle ;; Motion
attack ;; Attack || escape ;; Escaping || arrival ;; Arriving
selected ;; Choosing
deployed ;; Arranging || damaged ;; Damaging
raid ;; Attack || launched ;; Process_start
died ;; Death
Festival ;; Social_event || contest ;; Competition || awards ;; Award || consisting ;; Forming_relationships || held ;; Hold
referred ;; Adducing
broadcast ;; Expressing_publicly || held ;; Hold
Starting ;; Process_start || broadcast ;; Expressing_publicly
starting ;; Process_start || held ;; Hold || held ;; Hold || took place ;; Process_start
using ;; Using || illustrate ;; Statement || sung ;; Perception_active
debut ;; Process_start || hits ;; Bodily_harm || performed ;; Hold || performed ;; Hold
choosing ;; Choosing || launched ;; Process_start
took place ;; Process_start
hosted ;; Come_together || contested ;; Competition
looking ;; Perception_active
named ;; Name_conferral || defeated ;; Conquering || scored ;; Getting || clinching ;; Body_movement
included ;; Cause_to_be_included || scored ;; Getting || competition ;; Competition || scored ;; Getting || awarded ;; Award
show ;; Social_event
show ;; Social_event
show ;; Social_event
celebrated ;; Social_event
given ;; Giving || voted ;; Choosing || award ;; Award || show ;; Social_event || show ;; Social_event || shows ;; Social_event
selection ;; Choosing || includes ;; Cause_to_be_included || show ;; Social_event
recruiting ;; Becoming_a_member
includes ;; Cause_to_be_included || schedule ;; Arranging || show ;; Social_event || show ;; Social_event
includes ;; Cause_to_be_included || fire ;; Use_firearm || show ;; Social_event
canceled ;; Change_event_time || show ;; Social_event
confirmed ;; Coming_to_believe || canceled ;; Change_event_time || show ;; Social_event
takes place ;; Process_start
began ;; Process_start || went away ;; Death
grew ;; Expansion
expanded ;; Expansion
help out ;; Assistance
nominated ;; Choosing
include ;; Cause_to_be_included
awarded ;; Award
placed ;; Placing || made ;; Manufacturing
championship ;; Competition || association ;; Collaboration || Championship ;; Competition
took place ;; Process_start
found ;; Know || selected ;; Deciding || governing ;; Control || host ;; Social_event
matches ;; Competition || played ;; Competition
entered ;; Arriving || tournament ;; Competition
match ;; Competition || took place ;; Process_start || defeated ;; Conquering
scored ;; Getting || champions ;; Competition || tournament ;; Competition || posted ;; Sending
final ;; Process_end || qualify ;; Earnings_and_losses
finished ;; Process_end || final ;; Process_end
decided ;; Deciding || shoot-out ;; Hold
received ;; Receiving || received ;; Receiving
beat ;; Competition || claim ;; Statement
received ;; Receiving || awarded ;; Award
violence ;; Attack
massacre ;; Killing || arson ;; Committing_crime || looting ;; Robbery
violence ;; Violence || violence ;; Violence
killed ;; Killing || injured ;; Bodily_harm || lost ;; Earnings_and_losses
deaths ;; Death || claim ;; Statement
losses ;; Earnings_and_losses || suffered ;; Bodily_harm
deaths ;; Death
destroyed ;; Destroying
violence ;; Violence || attack ;; Attack
riots ;; Violence
blamed ;; Judgment_communication || violence ;; Violence
causes ;; Causation
violence ;; Violence || attacked ;; Attack || caused ;; Causation
attacked ;; Attack || attacked ;; Attack
spread ;; Dispersal
violence ;; Violence
match ;; Competition || Final ;; Competition || played ;; Competition
match ;; Competition
tournament ;; Competition || administered ;; Control
scored ;; Getting || defeating ;; Conquering
tournament ;; Competition
final ;; Competition || semi-final ;; Competition || qualified ;; Becoming_a_member || semi-finals ;; Competition || qualified ;; Becoming_a_member
Sporting ;; Wearing || final ;; Competition || met ;; Come_together || match ;; Competition || winning ;; Getting
final ;; Competition || televised ;; Communication
played ;; Competition || Final ;; Competition || took place ;; Hold
met ;; Come_together || happened ;; Presence || finals ;; Competition || replay ;; Competition || Final ;; Competition
used ;; Using || clubs ;; Bodily_harm || match ;; Competition || match ;; Competition
wore ;; Wearing || retained ;; Containing || replay ;; Competition || final ;; Competition || finals ;; Competition
wearing ;; Wearing || came ;; Arriving || renamed ;; Name_conferral || matches ;; Competition
became ;; Becoming
game ;; Competition || scored ;; Getting
parade ;; Motion || pick up ;; Choosing || hit ;; Body_movement || match ;; Competition || celebrations ;; Social_event || slipped ;; Self_motion
rushed ;; Motion || broke ;; Bodily_harm
presented ;; Presence || Final ;; Competition || receive ;; Receiving || start ;; Process_start
attacked ;; Attack
dressed ;; Wearing || armed ;; Body_movement
took over ;; Conquering || present ;; Presence || parade ;; Social_event || unarmed ;; Causation
take ;; Conquering || arrived ;; Arriving
killed ;; Killing
captured ;; Arrest
arrest ;; Arrest || escape ;; Escaping || avoid ;; Preventing_or_letting || blew ;; Motion || taken ;; Bringing
undisclosed ;; Reveal_secret || taken ;; Bringing
concert ;; Social_event || rock ;; Social_event || tour ;; Traveling
support ;; Supporting || Launched ;; Process_start
tour ;; Traveling || took place ;; Process_start
taking ;; Conquering
wore ;; Wearing
included ;; Cause_to_be_included || covers ;; Hiding_objects || set ;; Placing
received ;; Receiving || mixed ;; Cause_to_amalgamate
operation ;; Military_operation
saw ;; Self_motion
conducted ;; Action || damage ;; Damaging
lost ;; Earnings_and_losses || storage ;; Preserving || carrying ;; Carry_goods || suffered ;; Bodily_harm
damaged ;; Damaging || scrapped ;; Destroying
celebrates ;; Social_event
took place ;; Hold
rowing ;; Self_motion || stretch ;; Body_movement || race ;; Competition
changed ;; Change || race ;; Competition
suggested ;; Convincing || see ;; Perception_active || described ;; Statement
race ;; Competition || scheduled ;; Process_start
race ;; Competition || replaced ;; Change
reserve ;; Preserving
aimed ;; Aiming
began ;; Process_start
weakened ;; Cause_change_of_strength
formed ;; Coming_to_be || planning ;; Arranging
conducted ;; Action || creating ;; Creating
linked ;; Connect
opened ;; Openness
built ;; Building || reducing ;; Cause_change_of_position_on_a_scale || allowed ;; Preventing_or_letting
aimed ;; Aiming || led to ;; Causation
achieving ;; Achieve
made ;; Manufacturing || renew ;; Recovering
remained ;; Preserving
competition ;; Competition
regarded ;; Change_sentiment
death ;; Death || regarded ;; Change_sentiment
determine ;; Deciding || created ;; Creating
winning ;; Getting || contested ;; Competition
send ;; Sending || compete ;; Competition || invited ;; Becoming_a_member
winning ;; Getting || keep ;; Preserving
produce ;; Creating
play ;; Competition
competition ;; Competition || sent ;; Sending || inviting ;; Participation
assembly ;; Come_together || served ;; Assistance
election ;; Change_of_leadership || assembly ;; Come_together || result ;; Causation || formed ;; Coming_to_be || held ;; Hold
writing ;; Writing || assembly ;; Come_together || tasked ;; Request || acting ;; Action
reserved ;; Preserving || elected ;; Choosing || elected ;; Choosing
declared ;; Expressing_publicly || meeting ;; Communication || abolishing ;; Reforming_a_system
agreed ;; Agree_or_refuse_to_act || election ;; Change_of_leadership || receive ;; Receiving || receive ;; Receiving || receive ;; Receiving || receive ;; Receiving || placed ;; Arriving || assembly ;; Come_together
drafting ;; Writing || tenure ;; Employment || failure ;; Causation || dissolved ;; Dispersal
held ;; Hold || postponed ;; Change_event_time || elections ;; Change_of_leadership || slated ;; Hold || assembly ;; Come_together
declaration ;; Expressing_publicly || opening ;; Process_start || assembly ;; Come_together
held ;; Hold || held ;; Hold || meeting ;; Communication || publication ;; Publishing || elected ;; Change_of_leadership || released ;; Releasing
announced ;; Expressing_publicly || session ;; Communication || held ;; Hold
sworn ;; Commitment
declare ;; Expressing_publicly || abolishing ;; Removing || voted ;; Choosing || session ;; Communication || assembly ;; Come_together || looked ;; Perception_active
voted ;; Choosing || opposed ;; Agree_or_refuse_to_act || favor ;; Agree_or_refuse_to_act
opposed ;; Agree_or_refuse_to_act || assembly ;; Come_together
declared ;; Expressing_publicly || said ;; Statement || entering ;; Process_start || come ;; Arriving || took place ;; Process_start
decided ;; Deciding || leave ;; Departing || assembly ;; Come_together
agreed ;; Agree_or_refuse_to_act || become ;; Becoming || delay ;; Change_event_time || reached ;; Achieve || agreement ;; Resolve_problem || led to ;; Causation || assembly ;; Come_together || opening ;; Process_start || creation ;; Creating || hold ;; Hold
removed ;; Removing
decision ;; Deciding || said ;; Statement || accepted ;; Agree_or_refuse_to_act || assembly ;; Come_together
renamed ;; Name_conferral || organised ;; Arranging || Final ;; Competition || match ;; Competition || season ;; Competition || tournament ;; Competition
played ;; Competition
match ;; Competition
play ;; Competition || earned ;; Getting
meaning ;; Causation || League ;; Competition || reserved ;; Preserving || qualified ;; Becoming_a_member || qualifying ;; Becoming_a_member
reserved ;; Preserving || guaranteed ;; Commitment || qualified ;; Participation || enter ;; Participation || stage ;; Competition || stage ;; Competition || stage ;; Competition || used ;; Using
trading ;; Exchange || expedition ;; Attack || raids ;; Attack
war ;; Hostile_encounter || sail ;; Motion || expedition ;; Attack || Setting ;; Process_start
arrived at ;; Arriving || sailed ;; Motion || Operating ;; Being_in_operation
surrendered ;; Surrendering || evaded ;; Escaping || stored ;; Preserving
allowed ;; Preventing_or_letting || sail ;; Motion || put on ;; Placing
suffered ;; Bodily_harm || sailed ;; Motion || hardships ;; Confronting_problem
raid ;; Attack || suffered ;; Bodily_harm
operation ;; Military_operation || defense ;; Defending || undertaken ;; Action
began ;; Process_start || ended ;; Process_end || signed ;; Writing || implement ;; Action
aimed ;; Aiming || launched ;; Motion
met ;; Come_together || captured ;; Conquering || captured ;; Conquering || took effect ;; Change
retained ;; Preserving
assessments ;; Testing || ceasefire ;; Process_end || opposing ;; Agree_or_refuse_to_act || led to ;; Causation
stocks ;; Preserving || claims ;; Statement || estimates ;; Cause_change_of_position_on_a_scale || permit ;; Preventing_or_letting
settled ;; Resolve_problem || launched ;; Process_start || led to ;; Causation
conflict ;; Quarreling
pitted ;; Hostile_encounter
called ;; Name_conferral
debate ;; Protest
placed ;; Placing
became ;; Becoming || forced ;; Causation || embraced ;; Choosing || leave ;; Departing
discussions ;; Communication || influential ;; Influence || hosted ;; Social_event
included ;; Cause_to_be_included || gatherings ;; Come_together
criticized ;; Judgment_communication || accusing ;; Judgment_communication || meetings ;; Communication || preaching ;; Expressing_publicly || opposed ;; Agree_or_refuse_to_act
meetings ;; Communication || held ;; Hold
consensus ;; Agree_or_refuse_to_act || reached ;; Achieve
repentance ;; Rite || ease ;; Cause_change_of_position_on_a_scale || called ;; Request
services ;; Rite || speak ;; Expressing_publicly || deepened ;; Cause_change_of_position_on_a_scale || growing ;; Expansion || invited ;; Request || created ;; Creating
sentenced ;; Legal_rulings || accused ;; Judgment_communication
circulated ;; Come_together || petition ;; Request
election ;; Change_of_leadership || defeated ;; Conquering || supported ;; Supporting || suffered ;; Confronting_problem || setbacks ;; Confronting_problem || voted ;; Choosing
returned ;; Arriving
sentenced ;; Legal_rulings || banishment ;; Dispersal || brought ;; Bringing || trial ;; Legal_rulings
defended ;; Defending || prophesied ;; Statement || prosecution ;; Legality || hearing ;; Criminal_investigation || revelation ;; Reveal_secret || ruin ;; Destroying || claimed ;; Statement
banished ;; Removing || departure ;; Departing || brought ;; Causation || charged ;; Criminal_investigation
events ;; Social_event
played ;; Participation || portrayed ;; Publishing
War ;; Hostile_encounter || War ;; Hostile_encounter || war ;; Hostile_encounter || fought ;; Hostile_encounter || forces ;; Military_operation || forces ;; Military_operation
warfare ;; Hostile_encounter || revolutionary ;; Change_of_leadership
launched ;; Process_start || rising ;; Change_of_leadership || proclaimed ;; Statement
support ;; Supporting || response ;; Response || fighting ;; Hostile_encounter || led to ;; Causation || crushed ;; Destroying
party ;; Participation || election ;; Choosing
formed ;; Coming_to_be || declared ;; Expressing_publicly
shot ;; Use_firearm || dead ;; Death || initiative ;; Participation || ambush ;; Attack
conflict ;; Hostile_encounter || developed ;; Cause_to_make_progress
involved ;; Cause_to_be_included || set about ;; Process_start || state ;; Statement || capturing ;; Conquering || building ;; Building
conflict ;; Hostile_encounter || intensified ;; Cause_change_of_position_on_a_scale
began ;; Process_start || attacking ;; Attack || abandoned ;; GiveUp || patrols ;; Patrolling || ambushing ;; Attack
became ;; Becoming || attacks ;; Attack || bolstered ;; Cause_change_of_strength || authorised ;; Preventing_or_letting
conflict ;; Hostile_encounter || called ;; Name_conferral || War ;; Hostile_encounter
supplies ;; Supply || involved ;; Cause_to_be_included || conflict ;; Hostile_encounter || refusal ;; Agree_or_refuse_to_act || forces ;; Military_operation
control ;; Control || collapsed ;; Destroying || forcing ;; Causation
killed ;; Killing || escalated ;; Cause_change_of_strength || conflict ;; Hostile_encounter
killing ;; Killing || assassinated ;; Killing || wounding ;; Bodily_harm || match ;; Competition || opened ;; Openness
killed ;; Killing
declared ;; Expressing_publicly
burnt out ;; Destroying || forces ;; Military_operation
escalate ;; Cause_change_of_strength || killed ;; Killing || Violence ;; Violence || interned ;; Arrest
fighting ;; Hostile_encounter || conflict ;; Hostile_encounter || took place ;; Process_start
conflict ;; Hostile_encounter
formed ;; Coming_to_be
attacked ;; Attack || killed ;; Killing || conflict ;; Hostile_encounter
created ;; Creating
agreed ;; Agree_or_refuse_to_act || ceasefire ;; Process_end
signing ;; Sign_agreement || led to ;; Causation || talks ;; Telling
ended ;; Process_end || created ;; Creating
violence ;; Violence || fighting ;; Hostile_encounter || ceasefire ;; Process_end
war ;; Hostile_encounter || disagreement ;; Quarreling
flying ;; Self_motion || War ;; Hostile_encounter || awarded ;; Award || service ;; Rite || issued ;; Creating
War ;; Hostile_encounter || conflict ;; Hostile_encounter || took place ;; Process_start
became ;; Becoming || annexation ;; Attack || resulted in ;; Causation
sent ;; Sending || murdered ;; Killing || open ;; Openness || service ;; Rite || joined ;; Becoming_a_member
agreed ;; Agree_or_refuse_to_act || supplies ;; Supply || prepared ;; GetReady || equipped ;; Supply || take ;; Conquering
foresaw ;; Coming_to_believe || spread ;; Dispersal || arise ;; Coming_to_be || capture ;; Conquering
organized ;; Arranging || delayed ;; Change_event_time || strike ;; Attack || proceeded ;; Self_motion
gained ;; Getting
taken ;; Conquering || set ;; Placing || join ;; Becoming_a_member
stop ;; Preventing_or_letting || fighting ;; Hostile_encounter || chased ;; Self_motion
victory ;; Getting
received ;; Receiving || services ;; Rite
took place ;; Process_start || recorded ;; Recording || Battle ;; Hostile_encounter
chronicle ;; Recording || took place ;; Process_start || losses ;; Earnings_and_losses || suffered ;; Catastrophe || battle ;; Hostile_encounter || battle ;; Hostile_encounter
fought ;; Hostile_encounter || commanded ;; Control
chronicle ;; Recording || tells ;; Telling || put ;; Placing || slaughter ;; Killing
fallen ;; Motion_directional
battles ;; Hostile_encounter || died ;; Death || known ;; Know || reported ;; Reporting || fought ;; Defending
died ;; Death || suffered ;; Catastrophe || battle ;; Hostile_encounter || battle ;; Hostile_encounter
battle ;; Hostile_encounter
include ;; Cause_to_be_included
favoured ;; Protest || buried ;; Rite
battle ;; Hostile_encounter || providing ;; Supply || journey ;; Traveling
situated ;; Placing
festival ;; Social_event || taking place ;; Process_start
began ;; Process_start || took place ;; Process_start
beginning ;; Process_start || ending ;; Process_end
revival ;; Recovering
acts ;; Action
held ;; Giving
held ;; Giving
set up ;; Being_in_operation
featured ;; Presence
featured ;; Coming_to_be
alleviate ;; Cure || moved ;; Motion
organized ;; Arranging
services ;; Rite
tarnished ;; Bodily_harm || suffered ;; Bodily_harm
acts ;; Action || canceled ;; Change_event_time
refused ;; Agree_or_refuse_to_act || existing ;; Presence || refund ;; Commerce_pay
tickets ;; Rewards_and_punishments || refused ;; Agree_or_refuse_to_act || sold ;; Commerce_sell || rained ;; Motion || refund ;; Commerce_pay
sold ;; Commerce_sell || canceled ;; Change_event_time
deemed ;; Coming_to_believe
returned ;; Arriving || cancelled ;; Change_event_time
moved ;; Motion || acts ;; Action
competition ;; Competition || placed ;; Placing || blame ;; Judgment_communication || drew ;; Getting || attended ;; Participation
improves ;; Cause_to_make_progress || retool ;; Reforming_a_system || recreate ;; Creating || announced ;; Expressing_publicly || increases ;; Cause_change_of_position_on_a_scale
held ;; Defending || Final ;; Competition
match ;; Competition
game ;; Competition
match ;; Competition
finals ;; Process_end || clubs ;; Bodily_harm || finals ;; Process_end
service ;; Rite || die ;; Death
attacks ;; Attack || incidents ;; Attack || occurred ;; Presence || War ;; Military_operation
incident ;; Attack || killing ;; Killing || concluded ;; Coming_to_believe || war ;; Hostile_encounter || attributed ;; Judgment_communication || planned ;; Arranging || join ;; Becoming_a_member
incident ;; Attack || attacked ;; Attack || raiding ;; Attack || estimated ;; Scrutiny || heard ;; Perception_active || occurred ;; Presence || investigating ;; Criminal_investigation
killed ;; Killing || attack ;; Attack || mutilated ;; Bodily_harm || stationed ;; Placing
released ;; Releasing || attacks ;; Attack || increase ;; Cause_change_of_position_on_a_scale
kidnapped ;; Kidnapping || massacre ;; Killing || brought ;; Bringing
released ;; Releasing || increased ;; Cause_change_of_position_on_a_scale || suspicious ;; Criminal_investigation || prisoner ;; Prison || became ;; Becoming || took ;; Bringing
attacks ;; Attack || lent ;; Causation || set ;; Placing || join ;; Becoming_a_member || war ;; Military_operation
attacks ;; Attack || happened ;; Presence || war ;; Military_operation
rallies ;; Social_event || support ;; Supporting || organized ;; Arranging || labor ;; Employment || took place ;; Process_start
march ;; Motion_directional
leading ;; Causation || march ;; Motion_directional
walked ;; Self_motion
vowed ;; Commitment || return ;; Arriving || work ;; Employment
strike ;; Attack
striking ;; Attack
demanding ;; Request || increases ;; Cause_change_of_position_on_a_scale || working ;; Employment || end ;; Process_end
employees ;; Employment || striking ;; Attack
reported ;; Reporting || strike ;; Attack || reported ;; Reporting
arrested ;; Arrest || jailed ;; Prison || fined ;; Judgment_communication
lost ;; Earnings_and_losses
strike ;; Attack || fined ;; Judgment_communication
attack ;; Attack || denounced ;; Judgment_communication
sent ;; Sending || taking ;; Creating || discouraging ;; Preventing_or_letting
firing ;; Use_firearm || visited ;; Traveling || support ;; Supporting || strike ;; Attack
refused ;; Agree_or_refuse_to_act || party ;; Social_event || received ;; Receiving || election ;; Choosing
struggle ;; Hostile_encounter || election ;; Choosing || refusal ;; Agree_or_refuse_to_act || endorse ;; Agree_or_refuse_to_act
killed ;; Killing || shot ;; Use_firearm
stated ;; Statement || arrest ;; Arrest || possession ;; Getting
died ;; Death || gunshot ;; Killing
death ;; Death || conflict ;; Hostile_encounter || riots ;; Hostile_encounter || resulted in ;; Causation || led to ;; Causation || escalated ;; Cause_change_of_strength
investigation ;; Criminal_investigation
given ;; Giving || known ;; Know || shot ;; Use_firearm || possession ;; Getting
reach ;; Achieve
supplying ;; Supply || convicted ;; Legal_rulings || jailed ;; Prison
investigating ;; Criminal_investigation || delayed ;; Change_event_time || independent ;; Change_of_leadership || release ;; Publishing || report ;; Reporting
began ;; Process_start || ended ;; Process_end || killing ;; Killing || death ;; Death || death ;; Death || concluding ;; Statement
changes ;; Change || death ;; Death || criticism ;; Judgment_communication || attracting ;; Influence || suspicion ;; Judgment_communication
death ;; Death || accuse ;; Judgment_communication || misconduct ;; Action || cooperate ;; Collaboration
raids ;; Attack || resulted in ;; Causation || arrest ;; Arrest || case ;; Committing_crime || refers ;; Adducing || terrorism ;; Terrorism || plotting ;; Committing_crime || attacks ;; Terrorism
inspired ;; Influence || characterized ;; Judgment_communication
planning ;; Arranging || storm ;; Attack || behead ;; Killing || bombs ;; Attack || service ;; Assistance || accused ;; Judgment_communication || detonate ;; Attack || open ;; Process_start || take ;; Conquering
obtained ;; Getting || given ;; Giving || trial ;; Criminal_investigation || case ;; Committing_crime || presentation ;; Communication
contained ;; Containing || convictions ;; Legal_rulings || pleas ;; Request || charges ;; Legality
decision ;; Criminal_investigation
revoked ;; Removing || pleaded ;; Request || sentenced ;; Prison || received ;; Receiving || sentence ;; Prison
received ;; Receiving || sentences ;; Prison
convicted ;; Legal_rulings || imprisonment ;; Prison || sentenced ;; Prison || received ;; Receiving || sentences ;; Prison
released ;; Releasing || charges ;; Criminal_investigation || stayed ;; Temporary_stay
fought ;; Hostile_encounter || war ;; Military_operation
associated ;; Collaboration || receiving ;; Receiving || war ;; Military_operation
called ;; Name_conferral
transferred ;; Giving || control ;; Control || signed ;; Sign_agreement || establishing ;; Creating
include ;; Cause_to_be_included
marked ;; Recording
made ;; Action || seated ;; Placing || establish ;; Creating
struggle ;; Hostile_encounter || involved ;; Cause_to_be_included || control ;; Control
use ;; Using || occupied ;; Control
emerged ;; Coming_to_be
fled ;; Escaping
took ;; Conquering || fought ;; Hostile_encounter
persecuted ;; Bodily_harm || carrying out ;; Military_operation
receiving ;; Receiving || accused ;; Judgment_communication
died ;; Death
sent ;; Sending || arrested ;; Arrest || died ;; Death || war ;; Military_operation
known ;; Know || referred ;; Name_conferral || revolution ;; Social_event || revolution ;; Social_event
took place ;; Process_start
overthrew ;; Change_of_leadership || transfer ;; Giving || proclaimed ;; Statement || resulted in ;; Causation || revolution ;; Social_event || declined ;; Agree_or_refuse_to_act
began ;; Process_start || criticized ;; Judgment_communication || organize ;; Arranging
elected ;; Choosing
initiated ;; Process_start
executed ;; Rewards_and_punishments
used ;; Using || organize ;; Arranging
began ;; Process_start
captured ;; Conquering
elected ;; Choosing
replaced ;; Change_of_leadership || supported ;; Supporting || entrenching ;; Cause_change_of_strength || observed ;; Perception_active || composed ;; Come_together
disputed ;; Quarreling || resulted in ;; Causation || dissipated ;; Dispersal || appointed ;; Choosing || elections ;; Choosing
elections ;; Choosing
coming ;; Self_motion || existed ;; Presence || gone ;; Change || returned ;; Giving
delayed ;; Change_event_time || meet ;; Come_together
came ;; Self_motion || resulting in ;; Causation || rejected ;; Agree_or_refuse_to_act
recognized ;; Know || struggles ;; Hostile_encounter
held ;; Hold
marked ;; Recording || competed ;; Competition
participated ;; Participation || debut ;; Process_start || gaining ;; Getting || independence ;; Creating || made ;; Action
appearance ;; Presence || transfer ;; Giving
given ;; Giving || nickname ;; Name_conferral
recorded ;; Recording || hurricane ;; Catastrophe
developing ;; Coming_to_be
devastated ;; Destroying || storm ;; Catastrophe
concentrated ;; Aiming || felt ;; Change_sentiment || felt ;; Change_sentiment
develop ;; Coming_to_be || storm ;; Catastrophe || reaching ;; Arriving || tracked ;; Self_motion
reached ;; Arriving || storm ;; Catastrophe || Moving ;; Motion
winds ;; Motion || reached ;; Arriving || cyclone ;; Catastrophe
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || recurving ;; Self_motion || moving ;; Motion
weakened ;; Cause_change_of_strength || traversing ;; Motion_directional || crossed ;; Motion_directional
cyclone ;; Catastrophe || transitioned ;; Change || dissipated ;; Dispersal
storm ;; Catastrophe || causing ;; Causation || hurricane ;; Catastrophe || intensifying ;; Cause_change_of_strength || moved ;; Motion
resulted in ;; Causation || damaging ;; Damaging
storm ;; Catastrophe || hampering ;; Hindering || lashed ;; Destroying || disrupting ;; Damaging
damage ;; Damaging || sustained ;; Preserving
toppled ;; Destroying || damage ;; Damaging || reported ;; Reporting
damaging ;; Damaging || storm ;; Catastrophe || brought ;; Bringing || disrupting ;; Damaging
elections ;; Change_of_leadership
elections ;; Change_of_leadership || held ;; Hold
elections ;; Choosing
announced ;; Expressing_publicly || change ;; Change || promised ;; Commitment
announced ;; Expressing_publicly || run ;; Self_motion
secure ;; Getting
votes ;; Choosing || re-elected ;; Change_of_leadership
promised ;; Commitment || stated ;; Statement || take ;; Creating || fulfill ;; Process_end || promises ;; Commitment || stayed ;; Temporary_stay
release ;; Releasing || election ;; Choosing || election ;; Choosing || reform ;; Reforming_a_system || election ;; Choosing || held ;; Hold
referendum ;; Change_of_leadership || reform ;; Reforming_a_system || voted ;; Choosing
deemed ;; Coming_to_believe
announced ;; Expressing_publicly || assembly ;; Building || reduced ;; Cause_change_of_position_on_a_scale || tendered ;; Giving
accepted ;; Agree_or_refuse_to_act
resigned ;; Change_of_leadership
secure ;; Getting || becoming ;; Becoming
election ;; Choosing
raids ;; Attack || conducted ;; Action
attacks ;; Attack || made ;; Action || damage ;; Damaging || eliminate ;; Removing || posed ;; Causation
attacks ;; Attack || raids ;; Attack || attack ;; Attack || conducted ;; Action || made ;; Action || involved ;; Cause_to_be_included
raids ;; Attack
launched ;; Attack || departed ;; Departing
attack ;; Attack || raid ;; Attack || failed ;; Agree_or_refuse_to_act || inflicted ;; Causation
Attacks ;; Attack || conducted ;; Action
raid ;; Attack || hit ;; Attack || caused ;; Causation
causes ;; Causation || damaged ;; Damaging || losses ;; Earnings_and_losses || sunk ;; Motion
suffered ;; Bodily_harm
transferred ;; Giving
raids ;; Attack || conducted ;; Action || crippled ;; Destroying
results ;; Causation || regard ;; Judgment_communication
raid ;; Attack || conducted ;; Action
attack ;; Attack || determined ;; Deciding || initiated ;; Process_start || raid ;; Attack || launched ;; Motion || inflicted ;; Causation || strikes ;; Attack
detected ;; Know || arrived ;; Arriving || protected ;; Defending
attacks ;; Attack || spot ;; Know || inflict ;; Causation || damage ;; Damaging
limited ;; Limiting || destroyed ;; Destroying || damaged ;; Damaging || losses ;; Earnings_and_losses
returned ;; Arriving || intercept ;; Preventing_or_letting
damaged ;; Damaging
raids ;; Attack || conducted ;; Action || failed ;; Agree_or_refuse_to_act || transferred ;; Giving
accident ;; Catastrophe || occurred ;; Presence
forced ;; Request || carry out ;; Action || landing ;; Arriving || abandon ;; GiveUp || carrying ;; Bringing
killed ;; Killing || ejected ;; Departing
detonate ;; Attack || rupture ;; Destroying || disperse ;; Dispersal || contamination ;; Bodily_harm || resulted in ;; Causation || causing ;; Causation || crashed ;; Destroying
launched ;; Process_start || completed ;; Process_end
accident ;; Catastrophe || discontinued ;; Process_end
reviewed ;; Testing || developed ;; Cause_to_make_progress
resulted in ;; Causation || permission ;; Preventing_or_letting || given ;; Giving || revealed ;; Reveal_secret
compensation ;; Rewards_and_punishments || illnesses ;; Bodily_harm
tour ;; Competition
participated ;; Participation
retained ;; Getting
scored ;; Getting || win ;; Earnings_and_losses
scored ;; Competition
gave ;; Giving || test ;; Testing || made ;; Action
played ;; Competition || remembered ;; Know
capture ;; Arrest
went ;; Motion
called ;; Name_conferral || landed ;; Being_in_operation
put ;; Placing || followed ;; Motion
test ;; Testing || set ;; Self_motion
surrendered ;; Surrendering || recaptured ;; Conquering
accepted ;; Receiving || arrived ;; Arriving
landed ;; Motion
reached ;; Arriving
suffered ;; Bodily_harm || failure ;; Earnings_and_losses
occurred ;; Presence || takeoff ;; Self_motion
assess ;; Testing
injuries ;; Bodily_harm || falling ;; Motion_directional
found ;; Know
controls ;; Control || controls ;; Control || causing ;; Causation || landing ;; Self_motion || disintegrated ;; Change || damage ;; Damaging
manufactured ;; Manufacturing || caused ;; Causation || breaking ;; Bodily_harm || concluded ;; Coming_to_believe
operating ;; Being_in_operation
led to ;; Causation
powered ;; Influence || powered ;; Influence || led to ;; Causation
test ;; Testing
formed ;; Coming_to_be
losing ;; Earnings_and_losses || test ;; Testing
win ;; Earnings_and_losses
win ;; Earnings_and_losses
seen ;; Perception_active
fought ;; Hostile_encounter
competing ;; Competition
united ;; Cause_to_amalgamate
gave ;; Giving || began ;; Process_start || gain ;; Getting || taking over ;; Conquering
consolidated ;; Cause_to_amalgamate || overthrowing ;; Change_of_leadership
invaded ;; Attack || conquered ;; Conquering
united ;; Cause_to_amalgamate || invaded ;; Attack
negotiated ;; Communication || submitted ;; Submitting_documents || invade ;; Attack
shootout ;; Hostile_encounter || took place ;; Process_start
shootout ;; Hostile_encounter
feud ;; Competition
killed ;; Killing
ran ;; Motion
wounded ;; Bodily_harm
shootout ;; Hostile_encounter
represent ;; Change_sentiment
published ;; Publishing || well known ;; Know
became ;; Becoming || shootout ;; Hostile_encounter || known ;; Know
conflict ;; Hostile_encounter || become ;; Becoming || portrayed ;; Change_sentiment
gunfight ;; Hostile_encounter || take place ;; Process_start
fronted ;; Placing
took place ;; Process_start || shootout ;; Hostile_encounter
fired ;; Use_firearm
filed ;; Submitting_documents
hearing ;; Criminal_investigation
conflict ;; Hostile_encounter
ambushed ;; Attack || maimed ;; Bodily_harm
fired ;; Use_firearm
furnished ;; Supply || indicted ;; Criminal_investigation
appointed ;; Change_of_leadership
took ;; Conquering
received ;; Receiving
wounded ;; Bodily_harm || gunshot ;; Use_firearm || walking ;; Self_motion
wounded ;; Bodily_harm || shot ;; Use_firearm
identified ;; Check || convicted ;; Legal_rulings || convicted ;; Legal_rulings || murder ;; Killing
appeal ;; Protest || acquitted ;; Legal_rulings
filed ;; Submitting_documents || petition ;; Submitting_documents || denied ;; Agree_or_refuse_to_act
declared ;; Expressing_publicly || died ;; Death || assassination ;; Killing
unsolved ;; Resolve_problem || given rise to ;; Causation || remains ;; Temporary_stay
took place ;; Process_start
shot ;; Use_firearm || killed ;; Killing
said ;; Telling || took place ;; Process_start
fired ;; Use_firearm || turned ;; Becoming || believed ;; Know
murders ;; Killing || told ;; Telling || implicated ;; Collaboration
shot ;; Use_firearm || placed ;; Placing || make it ;; Causation || argued ;; Quarreling
argued ;; Quarreling || said ;; Telling || reach ;; Arriving || made it ;; Causation
informed ;; Telling || released ;; Releasing || convicted ;; Legal_rulings || sentenced ;; Legal_rulings
extended ;; Change_event_time
submitted ;; Submitting_documents || review ;; Statement
taken ;; Creating || used ;; Using || killings ;; Killing || damaged ;; Damaging || reconstructed ;; Building || argued ;; Quarreling
received ;; Receiving || say ;; Telling
fired ;; Use_firearm || said ;; Communication || alerted ;; Warning || standing ;; Placing
became ;; Becoming || made ;; Manufacturing || known ;; Know || alerted ;; Warning
began ;; Process_start || accident ;; Catastrophe || accident ;; Catastrophe
rescued ;; Rescuing
found ;; Know || pulled ;; Motion || took over ;; Getting || accident ;; Catastrophe || drilled ;; Action
cooperated ;; Collaboration || rescue ;; Rescuing
winched ;; Motion || watched ;; Perception_active
cost ;; Cost || rescue ;; Rescuing
resulted in ;; Causation || deaths ;; Death || accident ;; Catastrophe || accidents ;; Catastrophe
filed ;; Submitting_documents || collapse ;; Catastrophe || lawsuits ;; Legality
fought ;; Hostile_encounter
Starting ;; Process_start || changed ;; Change || deal ;; Resolve_problem || expand ;; Expansion
realized ;; Coming_to_believe || escalated ;; Cause_change_of_position_on_a_scale || ambush ;; Attack || war ;; Military_operation
decided ;; Deciding || deploy ;; Arranging || conquer ;; Conquering || situated ;; Placing || expel ;; Removing
war ;; Hostile_encounter || progressed ;; Cause_to_make_progress
varied ;; Change
shift ;; Change || gained ;; Getting
war ;; Hostile_encounter || allowed ;; Preventing_or_letting || prevailed ;; Conquering || expanded ;; Expansion || influence ;; Influence
deteriorated ;; Change || weakened ;; Cause_change_of_strength
submitted ;; Submitting_documents || resist ;; Defending || evicted ;; Removing
involving ;; Cause_to_be_included || Marked ;; Recording || control ;; Control || war ;; Military_operation || resulted in ;; Causation
battle ;; Hostile_encounter || took place ;; Process_start || Battle ;; Hostile_encounter
captured ;; Conquering || burned ;; Destroying
found ;; Know
prevent ;; Preventing_or_letting || getting to ;; Arriving
chose ;; Choosing || escape ;; Escaping || falling ;; Motion_directional || prevent ;; Preventing_or_letting || fled ;; Escaping || burn ;; Destroying
negotiations ;; Communication || submitted ;; Submitting_documents || planned ;; Arranging || attack ;; Attack || decided ;; Deciding || pending ;; Criminal_investigation
hurricane ;; Catastrophe || hit ;; Attack
maintained ;; Preserving
passed ;; Motion_directional
becoming ;; Becoming || attained ;; Getting || reached ;; Arriving
dropped ;; Motion_directional || causing ;; Causation || flooding ;; Motion
caused ;; Causation || wrecking ;; Destroying
damage ;; Damaging
festival ;; Social_event
held ;; Defending
held ;; Defending || celebrate ;; Social_event || festival ;; Social_event
competitions ;; Competition || coming ;; Motion || festival ;; Social_event
recorded ;; Recording || festivals ;; Social_event
attracted ;; Influence
holds ;; Defending
events ;; Social_event
held ;; Defending || elimination ;; Cause_change_of_position_on_a_scale
called ;; Name_conferral
party ;; Social_event || celebrates ;; Social_event
perform ;; Hold || performed ;; Hold || witnessed ;; Know
bushfire ;; Catastrophe || area ;; Surrounding || affected ;; Influence
began ;; Process_start || fire ;; Catastrophe
began ;; Process_start || locality ;; Placing || fire ;; Catastrophe || area ;; Surrounding
mount ;; Motion_directional || travelled ;; Motion
destroyed ;; Destroying || accommodation ;; Temporary_stay || families ;; Forming_relationships
injuries ;; Bodily_harm || increased ;; Cause_change_of_position_on_a_scale || count ;; Damaging
destroyed ;; Destroying || area ;; Surrounding || hit ;; Attack
destroyed ;; Destroying || revised ;; Change || area ;; Surrounding || number ;; Cause_change_of_position_on_a_scale || affected ;; Influence
cost ;; Cost || bushfires ;; Catastrophe || fatalities ;; Killing
threat ;; Warning || posed ;; Coming_to_be
attack ;; Attack || Battle ;; Military_operation || led ;; Bringing
overcome ;; Conquering || retreat ;; Escaping || repelled ;; Defending || assaults ;; Attack
conducted ;; Action || ended ;; Process_end || became ;; Becoming || withdrawal ;; Escaping || scramble ;; Hold
engagement ;; Hostile_encounter || end ;; Process_end || War ;; Military_operation || occurred ;; Presence
Moving ;; Motion || drove ;; Motion
surrounded ;; Surrounding || pause ;; Change_event_time || campaign ;; Military_operation || isolated ;; Having_or_lacking_access
drive ;; Motion || offered ;; Giving || Resuming ;; Process_start || battle ;; Hostile_encounter || pushed ;; Motion
abandon ;; GiveUp || operations ;; Military_operation || decided ;; Deciding || fall ;; Motion_directional
Battle ;; Hostile_encounter
contested ;; Competition || held ;; Placing
attended ;; Participation || marking ;; Recording
competing ;; Competition || came ;; Motion
finished ;; Process_end || upset ;; Perception_active || end ;; Process_end
coming ;; Motion
dominated ;; Control || playing ;; Participation
kicked ;; Motion
appear ;; Presence
formed ;; Creating || include ;; Cause_to_be_included || first ;; Process_start
defending ;; Defending || Battle ;; Military_operation || clash ;; Hostile_encounter
attacked ;; Attack || Leading ;; Control || War ;; Military_operation || drove ;; Motion
assault ;; Attack || crossing ;; Motion_directional
expanded ;; Expansion || sending ;; Sending
clash ;; Hostile_encounter
joined ;; Becoming_a_member
determined ;; Deciding || Battle ;; Military_operation || continue ;; Hold || invasion ;; Military_operation
held ;; Hold || tournament ;; Competition
participated ;; Participation
participate ;; Participation || tournament ;; Competition
competition ;; Competition || inception ;; Coming_to_be || compete ;; Competition
tournament ;; Competition || defeated ;; Conquering
conducted ;; Action || Festival ;; Social_event || event ;; Social_event
organized ;; Arranging || festival ;; Social_event || charitable ;; Giving || established ;; Building || promote ;; Cause_to_make_progress || encourage ;; Cause_change_of_position_on_a_scale || heighten ;; Cause_change_of_position_on_a_scale
festival ;; Social_event
increased ;; Cause_change_of_position_on_a_scale || festival ;; Social_event || stalls ;; Commerce_sell
referred ;; Name_conferral || Festival ;; Social_event
conducted ;; Action || started ;; Process_start || festival ;; Social_event
occurred ;; Coming_to_be || coup ;; Change_of_leadership || conflicts ;; Hostile_encounter
resulted in ;; Causation || preventing ;; Preventing_or_letting || left ;; Departing || removal ;; Removing
accusing ;; Judgment_communication || orchestrating ;; Arranging || coup ;; Change_of_leadership || departure ;; Departing || kidnapping ;; Kidnapping || claims ;; Statement
exile ;; Escaping || flown ;; Escaping || forced ;; Request
installed ;; Change_of_leadership || led ;; Bringing
Events ;; Social_event
election ;; Choosing
accused ;; Judgment_communication || fraud ;; Committing_crime || election ;; Choosing || election ;; Choosing
killing ;; Killing || abduction ;; Kidnapping
started ;; Process_start
stated ;; Statement
push ;; Motion
airstrikes ;; Attack || led to ;; Causation || reacted ;; Response
fled ;; Escaping || evacuated ;; Emptying || enabled ;; Preventing_or_letting
started ;; Process_start
broke ;; Destroying
kidnappings ;; Kidnapping || kidnapped ;; Kidnapping || took place ;; Process_start
escaped ;; Escaping || contacted ;; Communication || given ;; Giving || imprisoned ;; Kidnapping
rescued ;; Rescuing || arrested ;; Arrest
kidnapping ;; Kidnapping || rape ;; Committing_crime || charged ;; Legality
kidnapping ;; Kidnapping || murder ;; Killing || pleaded ;; Protest
sentenced ;; Legal_rulings
sentence ;; Legal_rulings || suicide ;; Killing || committed ;; Killing
reported ;; Reporting || flight ;; Self_motion
carried ;; Carry_goods
results ;; Achieve || Searches ;; Scrutiny
speculated ;; Coming_to_believe || crash ;; Catastrophe || hijacking ;; Kidnapping
found ;; Know || exploring ;; Scrutiny || summit ;; Social_event
report ;; Reporting
confirmed ;; Check || found ;; Know || discovery ;; Know || went ;; Self_motion || film ;; Create_artwork
began ;; Process_start || research ;; Research || took ;; Conquering
moved ;; Motion
released ;; Releasing || remained ;; Presence || comprising ;; Cause_to_be_included
negotiations ;; Communication || release ;; Releasing
removed ;; Removing || stated ;; Statement || engage ;; Participation || combat ;; Hostile_encounter
moved ;; Motion || found ;; Know
killed ;; Killing || crash ;; Catastrophe
confirmed ;; Check || observation ;; Perception_active || hostages ;; Kidnapping || returned ;; Arriving || made ;; Manufacturing
killed ;; Killing || effected ;; Influence || release ;; Releasing
killed ;; Killing || loss ;; Earnings_and_losses || save ;; Rescuing || crash ;; Catastrophe || captured ;; Conquering
use ;; Using
began ;; Process_start || invasion ;; Military_operation
announced ;; Expressing_publicly || war ;; Hostile_encounter || came ;; Motion || uphold ;; Response
proclaimed ;; Statement || armed ;; Bearing_arms || state ;; Statement || heightened ;; Cause_change_of_position_on_a_scale || alert ;; Warning || mobilised ;; Using
sent ;; Sending || invaded ;; Attack || demanding ;; Request
refused ;; Agree_or_refuse_to_act || demands ;; Request || support ;; Supporting || guaranteed ;; Commitment
declared ;; Expressing_publicly || began ;; Process_start || crossed ;; Motion_directional || Battle ;; Hostile_encounter
bring ;; Bringing || invade ;; Attack || sieges ;; Besieging || surrender ;; Surrendering || fall ;; Motion_directional || led to ;; Causation
abandoned ;; GiveUp || fighting ;; Hostile_encounter || withdrew ;; Escaping
began ;; Process_start || Siege ;; Besieging || occupied ;; Control
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || marched ;; Self_motion || leaving ;; Departing
fortified ;; Cause_change_of_strength || advanced ;; Cause_to_make_progress || took over ;; Military_operation
assist ;; Assistance || force ;; Hostile_encounter || harass ;; Hindering || made ;; Creating || keeping ;; Temporary_stay
postponed ;; Change_event_time || retained ;; Preserving || withdrawals ;; Departing || reinforce ;; Cause_change_of_strength || transit ;; Motion
took place ;; Process_start || became ;; Becoming || implement ;; Action || massacres ;; Killing || executions ;; Rewards_and_punishments || fear ;; Change_sentiment || terror ;; Terrorism || hostage ;; Kidnapping || burning ;; Bodily_harm || led ;; Bringing || invasion ;; Military_operation
ended ;; Process_end || began ;; Process_start || Battle ;; Hostile_encounter || fought ;; Hostile_encounter
flew ;; Motion || conduct ;; Action || reconnaissance ;; Scrutiny || service ;; Supply
began ;; Process_start || created ;; Creating || scouting ;; Scrutiny || landed ;; Arriving
moved ;; Motion || rest ;; Placing
collected ;; Come_together || forces ;; Hostile_encounter || landed ;; Arriving || formed ;; Creating || operate ;; Being_in_operation
ended ;; Process_end || destroyed ;; Destroying || defensive ;; Defending
abandoned ;; GiveUp || withdrew ;; Escaping || forces ;; Hostile_encounter
began ;; Process_start || end ;; Process_end || outflank ;; Besieging || retreat ;; Escaping || extending ;; Change_event_time
moved ;; Motion || withdrew ;; Escaping
attacked ;; Attack || defensive ;; Defending || Battle ;; Hostile_encounter || fought ;; Defending || fought ;; Hostile_encounter
occupation ;; Control
rule ;; Control || overseen ;; Scrutiny || established ;; Creating
lasted ;; Process_end
bombing ;; Attack || operation ;; Attack || war ;; Hostile_encounter
strikes ;; Attack
called ;; Name_conferral || operation ;; Attack || Operation ;; Attack || Operation ;; Attack || operation ;; Attack
withdrawal ;; Escaping || bombings ;; Attack || agreement ;; Sign_agreement || establishment ;; Creating || reached ;; Achieve
provoked ;; Causation || destabilize ;; Cause_change_of_strength || cleansing ;; Emptying || driving ;; Change
gain ;; Getting || authorisation ;; Preventing_or_letting || veto ;; Agree_or_refuse_to_act || proposal ;; Arranging || opposed ;; Agree_or_refuse_to_act || indicated ;; Statement
described ;; Statement || authorisation ;; Preventing_or_letting || campaign ;; Military_operation || launched ;; Process_start
illegal ;; Legality || support ;; Supporting || described ;; Statement || campaign ;; Military_operation || war ;; Hostile_encounter || aggression ;; Hostile_encounter || violation ;; Legality
bombing ;; Attack || killed ;; Killing || destroyed ;; Destroying
collaborators ;; Collaboration || beatings ;; Bodily_harm || remaining ;; Presence || victims ;; Catastrophe || left ;; Departing || perceived ;; Perception_active || included ;; Cause_to_be_included || abductions ;; Kidnapping || murders ;; Killing || withdrew ;; Escaping
became ;; Becoming || Wars ;; Hostile_encounter
bombing ;; Attack || combat ;; Hostile_encounter || marked ;; Recording || bombing ;; Attack || campaign ;; Military_operation || operation ;; Military_operation
used ;; Using || approval ;; Preventing_or_letting
kidnapping ;; Kidnapping || kidnapping ;; Kidnapping || murder ;; Killing
murder ;; Killing || involved ;; Participation
kidnapped ;; Kidnapping || killed ;; Killing || learning ;; Education_teaching
confirm ;; Check || murdered ;; Killing
kidnappers ;; Kidnapping || retracted ;; Agree_or_refuse_to_act
kidnappers ;; Kidnapping || convicted ;; Legal_rulings || sentenced ;; Legal_rulings
aired ;; Expressing_publicly
broke out ;; Process_start
led ;; Causation
refer ;; Statement
released ;; Releasing || began ;; Process_start
rose ;; Cause_to_make_progress || used ;; Using || seize ;; Hold || control ;; Control
killed ;; Killing
retaliated ;; Revenge || attack ;; Attack
fled ;; Escaping || captured ;; Arrest || destroyed ;; Destroying || causing ;; Causation
completed ;; Process_end
controlled ;; Control || conflict ;; Hostile_encounter || control ;; Control
massacre ;; Killing || killing ;; Killing
killing ;; Killing || attack ;; Attack || triggered ;; Causation || hit ;; Bodily_harm
attacked ;; Attack || retaliation ;; Revenge
attacked ;; Attack
incident ;; Killing || appointed ;; Choosing
killed ;; Killing
incident ;; Killing || killed ;; Killing
bombing ;; Attack || strike ;; Attack || war ;; Hostile_encounter || independence ;; Change_of_leadership
airstrike ;; Attack || attack ;; Attack || occurred ;; Presence
killed ;; Killing || reported ;; Reporting || injured ;; Bodily_harm
attack ;; Attack || attack ;; Attack || building ;; Building || meeting ;; Communication || injured ;; Bodily_harm
remarked ;; Statement || destroy ;; Destroying || attack ;; Attack
blamed ;; Judgment_communication || suggested ;; Convincing
prompted ;; Causation || sanctions ;; Revenge || attack ;; Attack
moved ;; Motion
started ;; Process_start || damage ;; Damaging
became ;; Becoming || building ;; Building
revolution ;; Change_of_leadership || battle ;; Hostile_encounter || event ;; Social_event
killing ;; Killing || reclaimed ;; Statement
legal ;; Legality || illegal ;; Legality || battle ;; Hostile_encounter || inspired ;; Influence || join ;; Participation
revenge ;; Revenge || Battle ;; Hostile_encounter || rebellion ;; Change_of_leadership || defeated ;; Conquering || ending ;; Process_end
driven ;; Motion
garrisoned ;; Besieging
grew ;; Cause_to_make_progress || arrival ;; Arriving || led ;; Causation
marched ;; Self_motion || retake ;; Control || campaign ;; Military_operation
casualties ;; Catastrophe || skirmishes ;; Hostile_encounter || engaged ;; Participation
supplies ;; Supply || reinforced ;; Cause_change_of_strength || supplying ;; Supply || pleading ;; Request || withstand ;; Defending || war ;; Hostile_encounter || attack ;; Attack || wrote ;; Writing
advanced ;; Cause_to_make_progress
repelling ;; Defending || attacks ;; Attack || attack ;; Attack || fend ;; Defending
buildings ;; Building || scaled ;; Cause_change_of_position_on_a_scale || withdrew ;; Escaping
slain ;; Killing || reach ;; Arriving || escape ;; Escaping
surrendered ;; Surrendering || executed ;; Rewards_and_punishments
agree ;; Agree_or_refuse_to_act || died ;; Death || killed ;; Killing || reported ;; Reporting || wounded ;; Bodily_harm
sent ;; Sending || defeat ;; Earnings_and_losses || spread ;; Dispersal
fled ;; Escaping || sparked ;; Causation || rush ;; Motion || self-proclaimed ;; Statement || advancing ;; Cause_to_make_progress || join ;; Participation
battle ;; Hostile_encounter || War ;; Hostile_encounter
became ;; Becoming || battle ;; Hostile_encounter
purchased ;; Commerce_buy || buildings ;; Building || designated ;; Choosing
beginning ;; Process_start
spread ;; Dispersal
Battle ;; Hostile_encounter || fought ;; Hostile_encounter
battle ;; Hostile_encounter || victory ;; Earnings_and_losses || campaigns ;; Military_operation || resulted in ;; Causation
making ;; Manufacturing || brought ;; Bringing || War ;; Hostile_encounter || ending ;; Process_end || conflict ;; Hostile_encounter || concluding ;; Process_end
expanded ;; Expansion || made ;; Causation
break ;; Destroying || influence ;; Influence
sanction ;; Revenge || besieging ;; Besieging || convinced ;; Convincing
declaration ;; Expressing_publicly || war ;; Hostile_encounter || amounted ;; Causation
opposed ;; Agree_or_refuse_to_act || formation ;; Coming_to_be || prompting ;; Cause_change_of_position_on_a_scale
march ;; Self_motion || advanced ;; Cause_to_make_progress
size ;; Conquering || Opposing ;; Preventing_or_letting || blocking ;; Hindering || occupying ;; Control
dissolved ;; Dispersal || battle ;; Hostile_encounter || fight ;; Hostile_encounter || rout ;; Earnings_and_losses || crushed ;; Attack
battle ;; Hostile_encounter || described ;; Statement
destroyed ;; Destroying || forces ;; Causation || war ;; Hostile_encounter || resistance ;; Defending || end ;; Process_end
impose ;; Action || settlement ;; Statement || accepted ;; Receiving
participants ;; Participation || formed ;; Coming_to_be || made ;; Manufacturing
planned ;; Arranging || war ;; Hostile_encounter || voted ;; Choosing
campaign ;; Military_operation || assassinated ;; Killing || war ;; Hostile_encounter || passed ;; Giving
kill ;; Killing || operation ;; Military_operation || Vengeance ;; Revenge
killed ;; Killing || shot down ;; Use_firearm || operating ;; Being_in_operation
kill ;; Killing
blamed ;; Judgment_communication || damaged ;; Damaging || attack ;; Attack || initiated ;; Process_start || death ;; Death || raised ;; Cause_change_of_position_on_a_scale || state ;; Statement || revenge ;; Revenge
shot down ;; Use_firearm || shot down ;; Use_firearm || claimed ;; Statement
shot down ;; Use_firearm || controversy ;; Quarreling
event ;; Social_event
started ;; Process_start || construction ;; Building || work ;; Action || completed ;; Process_end
approached ;; Arriving || consisting ;; Forming_relationships || threw ;; Self_motion
captured ;; Conquering || killed ;; Killing
evacuated ;; Escaping || withdrew ;; Escaping
siege ;; Besieging || arrived ;; Arriving
occupied ;; Control
hold ;; Defending || defected ;; Escaping || coming to ;; Motion || managed ;; Control || commanding ;; Control || intercept ;; Preventing_or_letting || lift ;; Process_end
defeat ;; Conquering || inflicted ;; Causation || recaptured ;; Conquering || reverse ;; Change || took ;; Conquering
siege ;; Besieging || published ;; Publishing || recorded ;; Recording || engraving ;; Create_artwork
concert ;; Social_event || tour ;; Traveling || support ;; Supporting
consisted ;; Cause_to_be_included || ran ;; Process_start || shows ;; Social_event
concert ;; Social_event || show ;; Social_event || filmed ;; Create_artwork
appeared ;; Presence
show ;; Social_event || performed ;; Hold
performed ;; Hold
depicted ;; Create_artwork || presented ;; Presence
presented ;; Presence
Narrated ;; Create_artwork || represented ;; Change_sentiment
leads ;; Causation
occur ;; Coming_to_be || terminating ;; Process_end
carried out ;; Military_operation
attract ;; Influence
change ;; Change || evolved ;; Coming_to_be || uprising ;; Change_of_leadership || took place ;; Process_start
marked ;; Recording || ending ;; Process_end || armed ;; Bearing_arms || taking place ;; Process_start
expansion ;; Expansion || culminating ;; Self_motion
made ;; Manufacturing || adoption ;; Choosing || suzerain ;; Surrendering
published ;; Publishing || revolution ;; Change_of_leadership
marked ;; Recording
represented ;; Presence
demand ;; Request || called ;; Name_conferral || drawing ;; Create_artwork
paying ;; Commerce_pay || called ;; Name_conferral || deemed ;; Coming_to_believe
revolution ;; Change_of_leadership
existence ;; Presence || consolidated ;; Cause_change_of_strength
paying ;; Commerce_pay || recognized ;; Know
withdrawal ;; Escaping || recognized ;; Know || independence ;; Change_of_leadership
festival ;; Social_event || Festival ;; Social_event || held ;; Hold
founded ;; Creating || become ;; Becoming || grown ;; Expansion
showcases ;; Create_artwork
festival ;; Social_event || held ;; Hold
run ;; Control
produces ;; Creating
copy ;; Motion || inspired ;; Influence
using ;; Using || runs ;; Control
runs ;; Self_motion
find ;; Know || competitions ;; Competition
compete ;; Competition || apply ;; Using
combination ;; Cause_to_amalgamate || cultivating ;; Education_teaching || volunteering ;; Supporting
challenged ;; Competition || donates ;; Giving
produced ;; Manufacturing
festival ;; Social_event || held ;; Hold
fair ;; Social_event || curated ;; Create_artwork
donations ;; Giving || restructured ;; Reforming_a_system || pressure ;; Convincing || collect ;; Come_together || raise ;; Cause_change_of_position_on_a_scale || partnered ;; Collaboration
founded ;; Creating
attracted ;; Motion
grew ;; Cause_change_of_position_on_a_scale || reached ;; Achieve
helped ;; Assistance || promote ;; Cause_change_of_position_on_a_scale || visitors ;; Traveling || growing ;; Expansion
fairs ;; Social_event
takes place ;; Process_start
makes it ;; Causation
marked ;; Recording || fall ;; Motion_directional
besieging ;; Besieging || included ;; Cause_to_be_included
evaded ;; Escaping || robbed ;; Robbery || pay ;; Commerce_pay || fall ;; Motion_directional || paying ;; Giving || pillaged ;; Committing_crime
ensured ;; Preventing_or_letting || remained ;; Presence || cut off ;; Having_or_lacking_access
promised ;; Commitment || aid ;; Assistance || disastrous ;; Catastrophe || hand over ;; Giving
summoned ;; Request || ordered ;; Arranging || promise ;; Commitment || besieging ;; Besieging || accompany ;; Motion || retracted ;; Agree_or_refuse_to_act
taken ;; Conquering
earthquake ;; Catastrophe || earthquake ;; Catastrophe
occurred ;; Coming_to_be || earthquake ;; Catastrophe
aftershocks ;; Catastrophe || recorded ;; Control
quake ;; Catastrophe || affected ;; Catastrophe
dispute ;; Protest || estimates ;; Statement
estimated ;; Statement || damaged ;; Damaging || collapsed ;; Destroying
intervention ;; Assistance || contributed ;; Causation || increased ;; Causation
earthquake ;; Catastrophe || caused ;; Causation
damaged ;; Damaging || destroyed ;; Destroying || assembly ;; Building
killed ;; Killing
killing ;; Killing || collapsed ;; Damaging
responded ;; Response || pledging ;; Commitment || dispatching ;; Killing || appeals ;; Statement
aired ;; Expressing_publicly || raising ;; Bringing
damaged ;; Damaging || earthquake ;; Catastrophe || hampered ;; Hindering
overwhelmed ;; Hindering
buried ;; Filling
became ;; Becoming || rescues ;; Rescuing || supplies ;; Supply
violence ;; Violence || looting ;; Robbery || led to ;; Causation || observed ;; Perception_active
called ;; Request || drawing to a close ;; Process_end || noted ;; Telling
attacked ;; Attack || occurred ;; Coming_to_be || plundered ;; Robbery
resist ;; Defending || defeat ;; Conquering || put ;; Placing || allowed ;; Preventing_or_letting
attacked ;; Attack || destroyed ;; Destroying || came back ;; Arriving || occupy ;; Control
bombarded ;; Attack || intervene ;; Hindering
attacked ;; Attack
reestablish ;; Recovering
consisted ;; Cause_to_be_included
besieged ;; Besieging || built ;; Building
agreed ;; Agree_or_refuse_to_act || surrender ;; Surrendering || taken ;; Conquering
captured ;; Conquering || destroyed ;; Destroying
departed ;; Departing
control ;; Control || facilitate ;; Cause_to_make_progress || occupy ;; Control || block ;; Hindering
attacked ;; Attack
assisted ;; Assistance || agreement ;; Agree_or_refuse_to_act
captured ;; Arrest
attacked ;; Attack || bring ;; Bringing
escape ;; Escaping || occupy ;; Control
fought ;; Hostile_encounter
triggered ;; Causation
turned ;; Change || defeated ;; Conquering
sent ;; Sending || defeated ;; Conquering || swarmed ;; Come_together || pillaged ;; Robbery
defeated ;; Conquering || established ;; Creating
recommenced ;; Process_start || defeated ;; Conquering
pressing ;; Self_motion
attacked ;; Attack || rebuilt ;; Recovering
marched ;; Self_motion || besieging ;; Besieging || razing ;; Destroying
invaded ;; Attack || expanding ;; Expansion
governed ;; Control || leading to ;; Causation
killing ;; Killing || planning ;; Arranging || refers ;; Name_conferral || seize ;; Getting || violence ;; Violence
left ;; GiveUp || wounded ;; Bodily_harm || maimed ;; Bodily_harm
pillaged ;; Robbery || ransacked ;; Robbery
hidden ;; Hiding_objects || debated ;; Communication
evacuated ;; Escaping
returned ;; Arriving || evacuated ;; Escaping
killed ;; Killing || massacre ;; Killing || riots ;; Violence || brought ;; Causation || formed ;; Coming_to_be
massacre ;; Killing || sent ;; Causation
became ;; Becoming || development ;; Cause_change_of_position_on_a_scale || led to ;; Causation
Cyclone ;; Catastrophe || storm ;; Catastrophe
named ;; Name_conferral || storm ;; Catastrophe || disturbed ;; Change_sentiment || cyclone ;; Catastrophe || formed ;; Coming_to_be
storm ;; Catastrophe || strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || attained ;; Getting
starting ;; Process_start || deepening ;; Cause_change_of_strength || attained ;; Getting
cyclone ;; Catastrophe || attained ;; Getting
began ;; Process_start || weaken ;; Cause_change_of_strength
storm ;; Catastrophe || replacement ;; Change || brushed ;; Damaging
weakened ;; Cause_change_of_strength || storm ;; Catastrophe || cyclone ;; Catastrophe
cyclone ;; Catastrophe || cyclone ;; Catastrophe || transitioned ;; Change || dissipated ;; Dispersal
brought ;; Bringing || damage ;; Damaging || Cyclone ;; Catastrophe
damaged ;; Damaging
damage ;; Damaging || took place ;; Process_start || washed ;; Damaging
occurred ;; Presence || delaying ;; Change_event_time || outages ;; Destroying || access ;; Having_or_lacking_access
contaminated ;; Destroying || destroyed ;; Destroying
deaths ;; Death || reported ;; Reporting
conducted ;; Action || attacks ;; Attack
began ;; Process_start || disclosed ;; Reveal_secret || attacks ;; Attack
confirmed ;; Check || aimed ;; Aiming || attack ;; Committing_crime || targeted ;; Aiming
targets ;; Aiming || reports ;; Reporting
stated ;; Statement || attack ;; Attack || operate ;; Using || acknowledged ;; Statement || close ;; Openness
claimed ;; Statement
named ;; Name_conferral || attack ;; Committing_crime
included ;; Cause_to_be_included || associated ;; Collaboration || discovered ;; Coming_to_believe
gave ;; Giving || said ;; Statement
gain ;; Getting || attack ;; Attack || access ;; Having_or_lacking_access
says ;; Communication || open ;; Openness
spend ;; Expend_resource || protecting ;; Defending || thought ;; Know
held ;; Hold
Run ;; Action
drawing ;; Connect
access ;; Having_or_lacking_access || visits ;; Traveling || selected ;; Choosing
used ;; Using || Exchange ;; Exchange
Championship ;; Competition || held ;; Hold
tournament ;; Competition || took ;; Getting || finished ;; Process_end
became ;; Becoming || possess ;; Getting || defeating ;; Conquering
lost ;; Earnings_and_losses
finished ;; Process_end || moved up ;; Cause_change_of_position_on_a_scale || secured ;; Having_or_lacking_access
take ;; Getting || triumphed ;; Achieve || knocked out ;; Competition
hurricane ;; Catastrophe || formed ;; Coming_to_be
formed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || cyclone ;; Catastrophe || took ;; Conquering || turning ;; Becoming
fluctuations ;; Cause_change_of_position_on_a_scale || reached ;; Arriving
bypassed ;; Having_or_lacking_access || brushing ;; Motion
dissipated ;; Dispersal
researched ;; Research || effect ;; Influence || experiment ;; Research || seeded ;; Giving
killed ;; Killing || collision ;; Catastrophe
struck ;; Destroying || stopped ;; Preventing_or_letting
crash ;; Catastrophe || involved ;; Cause_to_be_included
coordinated ;; Collaboration || connected ;; Having_or_lacking_access
received ;; Receiving || told ;; Telling || warnings ;; Warning || provided ;; Supply || clear ;; Emptying
found ;; Know || investigation ;; Scrutiny || proceed ;; Self_motion || identified ;; Check || approached ;; Arriving || moved ;; Motion
help ;; Assistance || prevent ;; Preventing_or_letting || combined ;; Cause_to_amalgamate || Legislation ;; Legality || re-engineering ;; Change || resulted in ;; Causation
added ;; Cause_to_be_included || advising ;; Telling
training ;; Education_teaching || incorporated ;; Cause_to_be_included || embraced ;; Response
protects ;; Preventing_or_letting || installed ;; Creating
memorial ;; Social_event
produced ;; Creating
took place ;; Process_start
purchased ;; Commerce_buy || produced ;; Creating
used ;; Using || produced ;; Creating
leave ;; Departing || continued ;; Process_start || forced ;; Request
included ;; Cause_to_be_included
available ;; Having_or_lacking_access || made ;; Creating
event ;; Social_event
begins ;; Process_start || lasts ;; Being_in_operation || lasts ;; Being_in_operation
drive ;; Motion || earned ;; Earnings_and_losses || celebrated ;; Social_event || held ;; Hold
include ;; Cause_to_be_included || concerts ;; Social_event
included ;; Cause_to_be_included || included ;; Cause_to_be_included
criticism ;; Judgment_communication || purchase ;; Commerce_buy || acquire ;; Getting || distribution ;; Dispersal || held ;; Hold || required ;; Imposing_obligation
asked ;; Request || responded ;; Response || avoid ;; Preventing_or_letting || issues ;; Creating || control ;; Control
renamed ;; Name_conferral
held ;; Hold
shifted ;; Change
festival ;; Social_event
ranked ;; Placing || Shopping ;; Commerce_buy || festival ;; Social_event
Festival ;; Social_event
ranked ;; Placing || grew ;; Cause_to_make_progress || festival ;; Social_event
festival ;; Social_event || playing ;; Competition || Spread ;; Dispersal
shopping ;; Commerce_buy || took place ;; Process_start
exploded ;; Attack || carried ;; Carry_goods
died ;; Death
injured ;; Bodily_harm
required ;; Imposing_obligation || treated ;; Cure || released ;; Releasing
shopping ;; Commerce_buy || come to ;; Motion || crowded ;; Come_together
murder ;; Killing || investigated ;; Criminal_investigation || closed ;; Self_motion || suspect ;; Committing_crime
determined ;; Deciding
captured ;; Arrest
moved ;; Motion
murdered ;; Killing || took ;; Conquering
murdered ;; Killing || destroyed ;; Destroying || established ;; Building
bought ;; Commerce_buy
settled ;; Resolve_problem
returned ;; Arriving
event ;; Social_event || wrestling ;; Hostile_encounter || produced ;; Creating
took place ;; Process_start
match ;; Competition || scheduled ;; Arranging || wrestling ;; Hostile_encounter || streamed ;; Motion
included ;; Cause_to_be_included
matches ;; Competition || comprised ;; Cause_to_be_included
defeated ;; Conquering
falls ;; Motion_directional || defeated ;; Conquering || defeated ;; Conquering
drawing ;; Motion || received ;; Receiving
received ;; Receiving || received ;; Receiving || buys ;; Commerce_buy || buys ;; Commerce_buy || buys ;; Commerce_buy || drop ;; Motion_directional
Championships ;; Competition
games ;; Competition || played ;; Competition
teams ;; Participation || playing ;; Competition
carrying ;; Bringing || play ;; Competition || played ;; Competition || determine ;; Deciding || teams ;; Participation || results ;; Causation || teams ;; Participation
Championships ;; Competition || Championships ;; Competition
became ;; Becoming || tying ;; Connect || title ;; Name_conferral
championship ;; Competition || Promotion ;; Cause_change_of_position_on_a_scale || run ;; Control
participate ;; Participation || invited ;; Participation || compete ;; Competition || Nations ;; Collaboration
entered ;; Arriving || flying ;; Self_motion
reach ;; Arriving || making ;; Manufacturing || ran out of ;; Expend_resource
died ;; Death || injuries ;; Bodily_harm
rescued ;; Rescuing
concert ;; Social_event || support ;; Supporting
played ;; Competition
recorded ;; Recording || released ;; Publishing
tour ;; Traveling
included ;; Cause_to_be_included
refused ;; Agree_or_refuse_to_act || joined ;; Becoming_a_member || sing ;; Perception_active
agreed ;; Agree_or_refuse_to_act || established ;; Building
performed ;; Hold || marked ;; Recording
played ;; Competition
touring ;; Traveling || managed ;; Control
festivals ;; Social_event || headquartered ;; Placing
operates ;; Being_in_operation || Product ;; Manufacturing || Founded ;; Creating || run ;; Control
grown ;; Expansion || include ;; Cause_to_be_included
originate ;; Coming_to_be || launch ;; Motion || teamed up ;; Cause_to_amalgamate
crashed ;; Catastrophe || killing ;; Killing || flown ;; Self_motion || operating ;; Being_in_operation
malfunction ;; Catastrophe || found ;; Know
revealed ;; Reveal_secret || presence ;; Presence
controls ;; Control || seated ;; Placing || control ;; Control || disengaged ;; Removing
causing ;; Causation || roll into ;; Motion || dive ;; Motion_directional || disengaged ;; Escaping
pulling ;; Motion || causing ;; Causation || lost ;; Earnings_and_losses || crashed ;; Catastrophe || level ;; Cause_change_of_position_on_a_scale || stall ;; Cause_change_of_position_on_a_scale || enter ;; Arriving || dive ;; Motion_directional || level ;; Cause_change_of_position_on_a_scale || managing ;; Control || recover ;; Recovering
incident ;; Catastrophe || occurred ;; Presence
misidentified ;; Check
fired ;; Use_firearm || killing ;; Killing || destroyed ;; Destroying
blamed ;; Judgment_communication
faulted ;; Judgment_communication
blamed ;; Judgment_communication
functioned ;; Being_in_operation
integrate ;; Collaboration || failed ;; Agree_or_refuse_to_act
received ;; Receiving
conducted ;; Action || investigations ;; Scrutiny || response ;; Communication || failing ;; Agree_or_refuse_to_act || hold ;; Self_motion
conducted ;; Action
receiving ;; Receiving || led to ;; Causation
refused ;; Agree_or_refuse_to_act || defense ;; Defending || interviewed ;; Communication
found ;; Know || operated ;; Being_in_operation || refused ;; Agree_or_refuse_to_act || designed ;; Creating || noted ;; Telling
operation ;; Military_operation || invaded ;; Attack
began ;; Process_start || ended ;; Process_end || invasion ;; Attack
begin ;; Process_start || warfare ;; Hostile_encounter || shut down ;; Having_or_lacking_access || blockade ;; Besieging
created ;; Creating || designed ;; Creating
strike ;; Attack || invading ;; Attack || cut off ;; Removing
came to ;; Self_motion
punctuated ;; Destroying
withdrew ;; Escaping || attacked ;; Attack
assembled ;; Come_together || ended ;; Process_end || attacked ;; Attack || delayed ;; Change_event_time
caused ;; Causation || invasion ;; Attack || assembled ;; Come_together || redirected ;; Motion
Fighting ;; Hostile_encounter || evacuated ;; Escaping || ceding ;; GiveUp
attacks ;; Attack || sinking ;; Motion_directional
began ;; Process_start || raids ;; Attack
raids ;; Attack || reconnaissance ;; Scrutiny
festival ;; Social_event || held ;; Hold
produced ;; Creating
event ;; Social_event || coordinated ;; Creating || concert ;; Social_event || campaign ;; Social_event || booked ;; Employment
worked ;; Employment || responsible ;; Being_in_operation || concert ;; Social_event
paying ;; Commerce_pay || attracted ;; Causation
festival ;; Social_event || records ;; Recording || installed ;; Giving || paid ;; Commerce_pay || set ;; Causation || attendance ;; Participation || gross ;; Earnings_and_losses
festivals ;; Social_event
retreat ;; Escaping || conceived ;; Creating || relieve ;; Cure || causing ;; Causation || Offensive ;; Attack || War ;; Hostile_encounter || offensive ;; Attack || pressure ;; Influence || offensive ;; Attack || collapse ;; Destroying || resulted in ;; Causation
starting ;; Process_start || ending ;; Process_end || campaigning ;; Social_event || due ;; Causation
cause ;; Causation || victory ;; Earnings_and_losses
devastated ;; Destroying || counterattacks ;; Attack || suppressed ;; Control
observation ;; Perception_active || improved ;; Cause_to_make_progress
siege ;; Besieging || reduced ;; Cause_change_of_position_on_a_scale
devastating ;; Destroying || supported ;; Supporting || fire ;; Use_firearm
tour ;; Traveling
visited ;; Traveling || support ;; Supporting || tour ;; Traveling
conceived ;; Creating
became ;; Becoming || engaged ;; Participation
consisted ;; Cause_to_be_included
became ;; Becoming || spawned ;; Coming_to_be || amends ;; Change
Summit ;; Social_event || held ;; Hold
meeting ;; Communication || summit ;; Social_event
brought together ;; Come_together || starting ;; Process_start || heads ;; Self_motion
formed ;; Coming_to_be || meeting ;; Communication
included ;; Cause_to_be_included || summits ;; Social_event
linked ;; Connect || conceived ;; Creating || meetings ;; Communication || rebellion ;; Change_of_leadership || summits ;; Social_event || summit ;; Social_event
earthquake ;; Catastrophe || occurred ;; Coming_to_be
observed ;; Know || assessed ;; Check || shaking ;; Motion
reported ;; Reporting || reported ;; Reporting || shaking ;; Motion
damage ;; Damaging || limited ;; Limiting || made ;; Action || mainshock ;; Catastrophe || aftershocks ;; Catastrophe
determined ;; Deciding || earthquake ;; Catastrophe || shock ;; Catastrophe || reports ;; Reporting || occurred ;; Coming_to_be || operating ;; Using || studied ;; Research || proposed ;; Creating || event ;; Confronting_problem || event ;; Confronting_problem
study ;; Research || proposed ;; Statement || presented ;; Creating
indicated ;; Statement || shock ;; Catastrophe || study ;; Research || placing ;; Placing || event ;; Confronting_problem
festival ;; Social_event || took place ;; Process_start
conceived ;; Creating || record ;; Recording
postpone ;; Change_event_time || decide ;; Deciding || cancel ;; Change_event_time || forced ;; Hostile_encounter || eliminated ;; Cause_change_of_position_on_a_scale
attended ;; Perception_active || move ;; Motion || made ;; Manufacturing
present ;; Presence || promote ;; Cause_to_make_progress || promoting ;; Cause_to_make_progress
performed ;; Hold
included ;; Cause_to_be_included
included ;; Cause_to_be_included
agree ;; Agree_or_refuse_to_act || overthrow ;; Change_of_leadership || coup ;; Change_of_leadership || install ;; Change_of_leadership
implemented ;; Action || coup ;; Change_of_leadership || carry out ;; Action
designed ;; Creating || free ;; Releasing || coup ;; Change_of_leadership
assistance ;; Assistance
cooperation ;; Cause_change_of_strength || thwarted ;; Hindering
intensified ;; Cause_change_of_strength || investigation ;; Scrutiny || uncovered ;; Reveal_secret || coup ;; Change_of_leadership || Uprising ;; Change_of_leadership || revolt ;; Change_of_leadership
destabilized ;; Cause_change_of_strength || arrested ;; Arrest || prevent ;; Preventing_or_letting || attempt ;; Creating
dissolved ;; Dispersal || obtained ;; Getting
strained ;; Cause_change_of_strength || coup ;; Change_of_leadership
referred ;; Name_conferral || produced ;; Creating
noted ;; Know
hand ;; Giving || introduce ;; Statement
conceived ;; Creating || marketing ;; Commerce_sell
order ;; Arranging || known ;; Know || order ;; Arranging || listed ;; Writing
leading ;; Causation || overthrow ;; Change_of_leadership || took place ;; Process_start || revolution ;; Change_of_leadership || Incident ;; Hostile_encounter
used ;; Using || monitor ;; Scrutiny || supporters ;; Supporting
sent ;; Sending || boarded ;; Temporary_stay || capture ;; Conquering
exchange ;; Exchange || fire ;; Use_firearm
undermanned ;; Supply || fired ;; Use_firearm
damaged ;; Damaging
fired ;; Use_firearm || carried ;; Carry_goods || shots ;; Use_firearm
hit ;; Attack || defeated ;; Conquering
stop ;; Process_end || escape ;; Escaping || find ;; Know
surrender ;; Surrendering
invented ;; Creating || saw ;; Self_motion || battle ;; Hostile_encounter || use ;; Using || entered ;; Arriving
dodged ;; Preventing_or_letting || monitor ;; Scrutiny
shooting ;; Use_firearm || massacre ;; Killing || occurred ;; Coming_to_be || bombing ;; Attack
murdered ;; Killing || suicide ;; Killing || committing ;; Committing_crime
suicide ;; Killing || killed ;; Killing || commit ;; Killing
injured ;; Bodily_harm || exchanged ;; Exchange
injured ;; Bodily_harm || escape ;; Escaping
shooting ;; Use_firearm
crime ;; Violence || inspired ;; Influence || become ;; Becoming || impact ;; Influence || shootings ;; Use_firearm
shootings ;; Use_firearm || attack ;; Attack || involved ;; Cause_to_be_included
placed ;; Placing || detonate ;; Attack || kill ;; Killing || injure ;; Bodily_harm || failed ;; Confronting_problem
set up ;; Placing || failed ;; Confronting_problem || detonate ;; Attack || detonated ;; Attack
planned ;; Arranging || massacre ;; Killing
become ;; Becoming || revolution ;; Change_of_leadership || said ;; Statement
causing ;; Causation || massacre ;; Killing || deaths ;; Death || bombing ;; Attack || exceed ;; Cause_change_of_position_on_a_scale
attack ;; Attack || implemented ;; Action || terrorist ;; Terrorism || bombing ;; Attack || planned ;; Arranging
criticized ;; Judgment_communication || enter ;; Arriving || intervening ;; Preventing_or_letting || shooting ;; Use_firearm
used ;; Using || incident ;; Catastrophe || introduction ;; Coming_to_be || resulted in ;; Causation
increased ;; Cause_change_of_position_on_a_scale || resulted in ;; Causation
Debates ;; Quarreling || control ;; Control || bullying ;; Violence || sparked ;; Process_start
discussed ;; Communication || violence ;; Violence || use ;; Using
massacre ;; Killing || memorials ;; Social_event || created ;; Creating
erected ;; Building
caused ;; Causation || removed ;; Removing || controversy ;; Quarreling
planning ;; Arranging || memorial ;; Social_event || began ;; Process_start
Designing ;; Creating || included ;; Cause_to_be_included || took ;; Conquering
memorial ;; Social_event || occurred ;; Presence
opened ;; Openness
replaced ;; Change_of_leadership || installed ;; Change_of_leadership
conducted ;; Action || planned ;; Arranging || associated ;; Collaboration
overthrow ;; Change_of_leadership
support ;; Supporting || occurred ;; Coming_to_be || aware ;; Know || launched ;; Process_start || moved ;; Motion
contribution ;; Achieve || occurred ;; Coming_to_be
deposed ;; Change_of_leadership
signed ;; Sign_agreement
encouraged ;; Influence || planned ;; Arranging || carry ;; Imposing_obligation
declared ;; Expressing_publicly || brought ;; Bringing || formed ;; Creating
invasion ;; Attack
disputed ;; Quarreling || impact ;; Influence
fought ;; Hostile_encounter || operation ;; Military_operation || Operation ;; Military_operation
planned ;; Arranging
operation ;; Military_operation
create ;; Creating || creating ;; Creating
achieved ;; Achieve || moving ;; Motion
creating ;; Creating
halting ;; Process_end || failed ;; Earnings_and_losses
attack ;; Attack
encircle ;; Besieging
giving ;; Giving || access ;; Getting || enabling ;; Preventing_or_letting
establish ;; Creating
ready ;; GetReady || close ;; Openness || consolidate ;; Cause_change_of_strength
allow ;; Preventing_or_letting || secure ;; Getting || consolidate ;; Cause_change_of_strength || made ;; Manufacturing
required ;; Imposing_obligation
moving ;; Motion || forces ;; Military_operation
took ;; Conquering
captured ;; Conquering
delayed ;; Change_event_time
demolished ;; Destroying || captured ;; Conquering || built ;; Building
delayed ;; Change_event_time || reaching ;; Arriving || made up ;; Change_event_time
failure ;; Earnings_and_losses || capture ;; Conquering
seize ;; Conquering || holding ;; Defending
encountered ;; Confronting_problem
gave ;; Giving || capturing ;; Conquering || organise ;; Arranging || constructing ;; Building
capture ;; Conquering || denying ;; Agree_or_refuse_to_act
relieve ;; Cure || failed ;; Earnings_and_losses
moved ;; Motion || hold ;; Defending || capitulating ;; Surrendering
evacuated ;; Escaping
failed ;; Earnings_and_losses || cross ;; Motion_directional
ended ;; Process_end || form ;; Coming_to_be
Battle ;; Hostile_encounter || campaign ;; Hostile_encounter || War ;; Hostile_encounter || War ;; Hostile_encounter || defeat ;; Conquering
announced ;; Expressing_publicly || blockade ;; Besieging || declaration ;; Expressing_publicly || war ;; Hostile_encounter
end ;; Process_end
battle ;; Hostile_encounter || allied ;; Collaboration || shipping ;; Sending || pitted ;; Hostile_encounter
protected ;; Preventing_or_letting || going to ;; Self_motion || coming ;; Self_motion
aided ;; Assistance || beginning ;; Process_start || forces ;; Causation
joined ;; Becoming_a_member || war ;; Hostile_encounter || entered ;; Arriving
fight ;; Hostile_encounter
enabled ;; Preventing_or_letting || Battle ;; Hostile_encounter || supply ;; Supply || fighting ;; Hostile_encounter || shipping ;; Sending || keep ;; Temporary_stay || war ;; Hostile_encounter || stem ;; Preventing_or_letting || struggle ;; Resolve_problem
prevent ;; Preventing_or_letting || invasion ;; Attack || supplies ;; Supply || preparation ;; GetReady || occupied ;; Hold
pushing ;; Motion || defeat ;; Conquering
cost ;; Cost || battle ;; Hostile_encounter || loss ;; Earnings_and_losses || victory ;; Earnings_and_losses || sunk ;; Earnings_and_losses
destroyed ;; Destroying || destroyed ;; Destroying || end ;; Process_end || forces ;; Causation || forces ;; Causation || scuttled ;; Destroying || war ;; Hostile_encounter || sunk ;; Motion
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || called ;; Name_conferral
began ;; Process_start || started ;; Process_start || campaign ;; Hostile_encounter || War ;; Hostile_encounter || War ;; Hostile_encounter || Surrender ;; Surrendering
battles ;; Hostile_encounter || convoy ;; Patrolling || theatre ;; Social_event || covering ;; Filling || involved ;; Participation || encounters ;; Hostile_encounter
surrendered ;; Surrendering || developed ;; Coming_to_be || participating ;; Participation || gaining ;; Getting || equipment ;; Supply || war ;; Hostile_encounter || changed ;; Change || joined ;; Becoming_a_member || changed ;; Change
overcoming ;; Conquering || gained ;; Getting || losses ;; Earnings_and_losses || war ;; Hostile_encounter || end ;; Process_end || end ;; Process_end || defeating ;; Conquering
landings ;; Self_motion
force ;; Causation || operation ;; Military_operation || carried out ;; Action
Operation ;; Military_operation || invasion ;; Military_operation
landed ;; Self_motion || reaching ;; Arriving || landing ;; Self_motion || preventing ;; Preventing_or_letting
landings ;; Self_motion
landed ;; Self_motion || set up ;; Building || landed ;; Self_motion
forced ;; Causation || return ;; Self_motion || turn ;; Self_motion
captured ;; Conquering || given ;; Giving || forces ;; Causation
became ;; Becoming || landings ;; Self_motion || linked ;; Connect || operation ;; Military_operation
withdrawal ;; Escaping || landing ;; Self_motion || boarded ;; Self_motion
hinder ;; Hindering || mission ;; Military_operation || operation ;; Military_operation || cost ;; Earnings_and_losses || casualties ;; Killing
required ;; Imposing_obligation || bring ;; Bringing
invaded ;; Attack || conquered ;; Conquering || Invasion ;; Attack || operation ;; Military_operation || ruled ;; Control
initiate ;; Process_start
bombarded ;; Attack || determined ;; Deciding || required ;; Imposing_obligation
culminated ;; Process_end
began ;; Process_start
widespread ;; Dispersal || defeated ;; Conquering
root ;; Removing || resulted in ;; Causation
marked ;; Recording
marked ;; Recording
conquered ;; Conquering || organised ;; Arranging
devastated ;; Destroying || cyclone ;; Catastrophe
storm ;; Catastrophe || cyclone ;; Catastrophe
storm ;; Catastrophe || passing ;; Motion_directional || observed ;; Perception_active || turned ;; Motion
became ;; Becoming || reached ;; Arriving
alerted ;; Warning || passed ;; Motion_directional || acknowledge ;; Know || cyclone ;; Catastrophe
duty ;; Imposing_obligation
caused ;; Causation || killed ;; Killing
destroyed ;; Destroying
required ;; Imposing_obligation || left ;; Causation || evacuation ;; Emptying || returned ;; Arriving
rebuilt ;; Building || passed ;; Motion_directional
referred ;; Name_conferral || conflict ;; Hostile_encounter
fired ;; Use_firearm || preceded ;; Process_start || led to ;; Causation
establishing ;; Creating
freezing ;; Change || passed ;; Motion_directional || restricting ;; Hindering
pushed ;; Motion
turned ;; Becoming || establish ;; Building || use ;; Using || prevent ;; Preventing_or_letting || enforce ;; Preventing_or_letting || authorised ;; Imposing_obligation
announced ;; Expressing_publicly
include ;; Cause_to_be_included || set ;; Placing || rejected ;; Agree_or_refuse_to_act || end ;; Process_end
taking ;; Conquering || evaded ;; Escaping
recognised ;; Know || replacing ;; Change_of_leadership
captured ;; Conquering || killed ;; Killing || evaded ;; Escaping
declared ;; Expressing_publicly || end ;; Process_end
established ;; Building || recognized ;; Know || leading to ;; Causation
fought ;; Hostile_encounter
refused ;; Agree_or_refuse_to_act || integrate ;; Cause_to_amalgamate || leading to ;; Causation
led ;; Causation
injuring ;; Bodily_harm || destroyed ;; Destroying || killing ;; Killing || fire ;; Catastrophe || fire ;; Catastrophe || injured ;; Bodily_harm || reporting ;; Reporting
blaze ;; Catastrophe || fire ;; Catastrophe || investigation ;; Criminal_investigation || cause ;; Causation || announced ;; Expressing_publicly || determine ;; Scrutiny
destruction ;; Destroying || concluded ;; Coming_to_believe || led to ;; Causation
illegal ;; Legality || detained ;; Arrest || blame ;; Judgment_communication || placed ;; Action
accused ;; Judgment_communication || arrested ;; Arrest || fire ;; Catastrophe
fire ;; Catastrophe || fire ;; Catastrophe || announced ;; Expressing_publicly
prompted ;; Causation || increased ;; Cause_change_of_position_on_a_scale || fire ;; Catastrophe || inspections ;; Scrutiny || pass ;; Motion_directional || regulations ;; Imposing_obligation
criticized ;; Judgment_communication || accusing ;; Judgment_communication || providing ;; Giving || fire ;; Catastrophe || response ;; Response
decided ;; Deciding || withdraw ;; Escaping || concede ;; Giving
use ;; Using || recapture ;; Conquering || repulsed ;; Defending
reduced ;; Cause_change_of_position_on_a_scale || casualties ;; Catastrophe || battle ;; Hostile_encounter
resupply ;; Supply || losses ;; Earnings_and_losses || suffering ;; Bodily_harm
affecting ;; Influence || losses ;; Earnings_and_losses || recapture ;; Conquering
decision ;; Deciding || withdraw ;; Escaping || endorsed ;; Agree_or_refuse_to_act
began ;; Process_start || operation ;; Military_operation || delivery ;; Military_operation
began ;; Process_start
Battle ;; Hostile_encounter || sunk ;; Motion_directional
sank ;; Motion_directional
carried out ;; Action
evacuated ;; Emptying || cost ;; Cost || damaged ;; Damaging
died ;; Death || required ;; Imposing_obligation
declared ;; Expressing_publicly || gone ;; Self_motion || ending ;; Process_end || realized ;; Coming_to_believe
crash ;; Catastrophe
injuring ;; Bodily_harm || derailed ;; Self_motion || crossing ;; Motion_directional || removed ;; Removing || killing ;; Killing || works ;; Being_in_operation
found ;; Know || placed ;; Placing || report ;; Reporting || stop ;; Process_end || required ;; Imposing_obligation
travelling ;; Traveling
died ;; Death
lost ;; Earnings_and_losses || travelling ;; Traveling || affected ;; Influence || using ;; Using
said ;; Communication || died ;; Death || recovered ;; Recovering
conflict ;; Hostile_encounter
called ;; Name_conferral
cause ;; Causation || overthrown ;; Change_of_leadership || replaced ;; Change_of_leadership
supported ;; Supporting || involved ;; Participation || retained ;; Preserving || address ;; Resolve_problem
supported ;; Supporting || established ;; Building || fought ;; Hostile_encounter || fought ;; Hostile_encounter
viewed ;; Perception_active
provided ;; Supply || support ;; Supporting || control ;; Control || aware ;; Know || driven ;; Motion
began ;; Process_start || stayed ;; Temporary_stay || raised ;; Cause_change_of_position_on_a_scale || failed ;; Earnings_and_losses || regain ;; Control || culminated ;; Process_end
negotiated ;; Communication || left ;; Departing || defeated ;; Conquering
claimed ;; Statement
confirming ;; Check || confined ;; Hindering
offered ;; Supply || erode ;; Cause_change_of_strength
celebrated ;; Social_event
joined ;; Becoming_a_member || fought ;; Hostile_encounter || armed ;; Bearing_arms || conflict ;; Hostile_encounter || War ;; Military_operation
using ;; Using || inflicted ;; Bodily_harm || captured ;; Conquering
surrendered ;; Surrendering || involve ;; Participation || intervention ;; Hostile_encounter || landing ;; Self_motion || taken ;; Bringing
led to ;; Causation || confronted ;; Confronting_problem
began ;; Process_start || operations ;; Military_operation
worked ;; Being_in_operation || lasted ;; Process_end || pacify ;; Recovering || consolidation ;; Cause_to_make_progress || War ;; Military_operation
signing ;; Sign_agreement || adjudicated ;; Legal_rulings || treaty ;; Writing || protectorate ;; Change_of_leadership
resisted ;; Defending || conflict ;; Hostile_encounter || last ;; Process_end || unleashing ;; Process_start
led ;; Causation || rebellion ;; Change_of_leadership || suffered ;; Confronting_problem
retreated ;; Escaping || created ;; Creating
conflict ;; Hostile_encounter || campaign ;; Military_operation
collaboration ;; Collaboration || turning ;; Change || Battle ;; Hostile_encounter || intervened ;; Response || established ;; Creating
surrendered ;; Surrendering || regained ;; Getting || pacified ;; Recovering
controversial ;; Quarreling || War ;; Military_operation
see ;; Perception_active
consider ;; Convincing || wars ;; Military_operation || conquer ;; Conquering
left ;; Bringing || War ;; Military_operation
war ;; Military_operation || took place ;; Process_start || interpreted ;; Communication
began ;; Process_start || attacked ;; Attack || coup ;; Change_of_leadership || rebellion ;; Violence
declared ;; Expressing_publicly || overthrown ;; Change_of_leadership || Restoration ;; Recovering || hiding ;; Escaping || said ;; Telling || formed ;; Creating || forcing ;; Causation
leading ;; Causation || coup ;; Change_of_leadership || coup ;; Change_of_leadership || condemnation ;; Judgment_communication || loss ;; Earnings_and_losses || followed ;; Bringing || describe ;; Communication
agreed ;; Agree_or_refuse_to_act || end ;; Process_end || giving ;; Giving || step down ;; Change_of_leadership || sanctions ;; Legality
resigned ;; Change_of_leadership || coup ;; Change_of_leadership || maintained ;; Cause_change_of_strength
discovered ;; Know || went ;; Change
concert ;; Social_event || taking place ;; Process_start
expressed ;; Communication || leave ;; Departing
announced ;; Expressing_publicly || announced ;; Expressing_publicly || plans ;; Arranging || disbanding ;; Response
announced ;; Expressing_publicly || revealed ;; Reveal_secret || take place ;; Process_start
commented ;; Statement || due ;; Causation || commitments ;; Commitment || touring ;; Traveling || touring ;; Traveling
announced ;; Expressing_publicly || sold ;; Commerce_sell || added ;; Cause_to_be_included
became ;; Becoming || ends ;; Process_end || meet ;; Come_together || recording ;; Communication || toured ;; Traveling || performing ;; Social_event || developed ;; Cause_to_make_progress
release ;; Publishing || toured ;; Traveling || battle ;; Confronting_problem
enjoined ;; Preventing_or_letting || recording ;; Recording || battles ;; Confronting_problem || plunged ;; Placing
release ;; Publishing || resolved ;; Resolve_problem || stop ;; Process_end || make ;; Earnings_and_losses || issues ;; Legality || toured ;; Traveling
tours ;; Traveling
skirmish ;; Hostile_encounter || Battle ;; Hostile_encounter || called ;; Name_conferral
Fought ;; Hostile_encounter
skirmish ;; Hostile_encounter || victory ;; Earnings_and_losses || resulted in ;; Causation
started ;; Process_start || uprising ;; Change_of_leadership || engagement ;; Hostile_encounter || marching ;; Motion_directional
approached ;; Arriving || ordered ;; Arranging || negotiate ;; Communication || take ;; Conquering
refused ;; Agree_or_refuse_to_act || ordered ;; Arranging || negotiate ;; Communication
equipped ;; Supply || killed ;; Killing || fight ;; Hostile_encounter || dispersed ;; Dispersal
losses ;; Earnings_and_losses || lost ;; Earnings_and_losses || wounded ;; Bodily_harm
armed ;; Bearing_arms || recurred ;; Coming_to_be
began ;; Process_start || crackdown ;; Process_end || campaign ;; Social_event
violence ;; Violence || spread ;; Dispersal || support ;; Supporting || took place ;; Process_start
took ;; Conquering || attacked ;; Attack || control ;; Control
sent ;; Sending || imposed ;; Action
tragedy ;; Catastrophe || suffocated ;; Death
extended ;; Change_event_time
imprisoned ;; Arrest || rebels ;; Hostile_encounter || lost ;; Earnings_and_losses
imprisoned ;; Arrest || deported ;; Removing || went ;; Self_motion
rebellion ;; Violence
debating ;; Quarreling || triggered ;; Causation
repudiated ;; Agree_or_refuse_to_act || remained ;; Presence
view ;; Perception_active || upheaval ;; Hostile_encounter
magnitude ;; Catastrophe || seen ;; Perception_active
giving ;; Giving || uprising ;; Terrorism
freedom ;; Change_of_leadership || recognised ;; Know
promoted ;; Creating
visited ;; Traveling
marks ;; Recording
serves ;; Giving
joined ;; Becoming_a_member
announced ;; Expressing_publicly || joined ;; Becoming_a_member
moved ;; Motion
filmed ;; Communication || released ;; Publishing
titled ;; Name_conferral
contains ;; Containing || made ;; Manufacturing
Operation ;; Military_operation
planned ;; Arranging || led to ;; Causation
supply ;; Supply || reconsidered ;; Deciding || forces ;; Military_operation
pushed ;; Motion || include ;; Cause_to_be_included
approved ;; Agree_or_refuse_to_act
opening ;; Openness || goal ;; Aiming || forces ;; Military_operation || increase ;; Cause_change_of_position_on_a_scale
followed ;; Motion || landed ;; Motion
weakened ;; Cause_change_of_strength || opposed ;; Agree_or_refuse_to_act || equipment ;; Supply || forces ;; Military_operation
hindered ;; Hindering || uprising ;; Change_of_leadership || forces ;; Military_operation || resistance ;; Hostile_encounter || defeated ;; Conquering
withdrew ;; Escaping || defense ;; Defending || establish ;; Creating
overtake ;; Change_of_leadership || block ;; Hindering
achieve ;; Achieve || led to ;; Causation || withdrawal ;; Escaping || retreat ;; Escaping || complete ;; Process_end
capture ;; Conquering || retreating ;; Escaping || putting ;; Placing
hold ;; Defending || withdrawal ;; Escaping || ordered ;; Arranging
retreated ;; Escaping || forces ;; Military_operation || pursued ;; Aiming
came to ;; Motion || defense ;; Defending || establish ;; Creating
meeting with ;; Come_together || halted ;; Process_end || facing ;; Confronting_problem || forces ;; Military_operation
inflicting ;; Causation || escape ;; Escaping || forces ;; Military_operation || casualties ;; Death || liberate ;; Conquering
put ;; Placing || allowing ;; Preventing_or_letting || solve ;; Resolve_problem
movement ;; Social_event || demonstrations ;; Arranging || strikes ;; Attack || took place ;; Process_start
began ;; Process_start || solving ;; Resolve_problem || provoked ;; Causation || raise ;; Change || exchange ;; Exchange || crisis ;; Catastrophe || cut ;; Change
killed ;; Killing || demonstrations ;; Protest
organised ;; Arranging
started ;; Process_start || movement ;; Social_event || Movement ;; Social_event
began ;; Process_start
turned ;; Becoming || events ;; Social_event
nickname ;; Name_conferral || organised ;; Arranging || protests ;; Preventing_or_letting || earned ;; Receiving
ended ;; Process_end || removed ;; Removing
occurred ;; Presence || clashes ;; Bearing_arms || accept ;; Agree_or_refuse_to_act || voted ;; Choosing
brutality ;; Violence || reported ;; Reporting
agreed ;; Agree_or_refuse_to_act || alleged ;; Statement || investigation ;; Scrutiny || accusations ;; Statement
way ;; Action || investigation ;; Scrutiny
massacre ;; Killing || took place ;; Process_start || strike ;; Attack
costs ;; Cost || agreed ;; Agree_or_refuse_to_act || observe ;; Perception_active || ship ;; Bringing || strike ;; Attack || went ;; Self_motion || hired ;; Employment || produce ;; Creating
armed ;; Bearing_arms || killed ;; Killing || exchange ;; Exchange || gunfire ;; Use_firearm
shot ;; Use_firearm || going to ;; Self_motion || armed ;; Bearing_arms || agreement ;; Sign_agreement || disregarded ;; Agree_or_refuse_to_act || work ;; Being_in_operation
killed ;; Killing || shot ;; Use_firearm || exchange ;; Exchange || siege ;; Besieging || gunfire ;; Use_firearm || striking ;; Attack
killed ;; Killing
murdered ;; Killing || death ;; Death || bringing ;; Bringing
began ;; Process_start || ended ;; Process_end || invasion ;; Attack
expelled ;; Removing || deemed ;; Coming_to_believe
began ;; Process_start || obtain ;; Getting || demanding ;; Request || exchange ;; Exchange || claiming ;; Statement || cede ;; Surrendering
refused ;; Agree_or_refuse_to_act || invaded ;; Attack
use ;; Using || conclude ;; Coming_to_believe || argue ;; Agree_or_refuse_to_act
repelled ;; Defending || inflicted ;; Causation
overcame ;; Conquering || reorganised ;; Reforming_a_system || adopted ;; Choosing || renewed ;; Recovering
ceased ;; Process_end
ceded ;; Surrendering
suffered ;; Bodily_harm
received ;; Receiving || exceeded ;; Cause_change_of_position_on_a_scale
enhanced ;; Cause_change_of_position_on_a_scale || retained ;; Preserving
confirmed ;; Check
began ;; Process_start || commenced ;; Process_start
carried out ;; Attack || attack ;; Attack
fired ;; Use_firearm || killing ;; Killing || crossed ;; Motion_directional || ambushed ;; Attack || capturing ;; Conquering
killed ;; Killing || rescue ;; Rescuing
demanded ;; Request || held ;; Conquering
refused ;; Agree_or_refuse_to_act || campaign ;; Military_operation || response ;; Response || launched ;; Process_start || raid ;; Attack
start ;; Process_start
exchange ;; Exchange || returned ;; Arriving
named ;; Name_conferral || operation ;; Military_operation || shortened ;; Cause_change_of_position_on_a_scale
conflicts ;; Hostile_encounter || took place ;; Process_start
toppled ;; Change_of_leadership
rise ;; Cause_change_of_position_on_a_scale
demanded ;; Request || drive ;; Motion
refused ;; Agree_or_refuse_to_act || sent ;; Sending || demanding ;; Request
rejected ;; Agree_or_refuse_to_act
permitted ;; Preventing_or_letting || escape ;; Escaping || limited ;; Limiting || ordered ;; Arranging || secure ;; Preventing_or_letting || contested ;; Quarreling
driven ;; Motion || contest ;; Competition
demanding ;; Request || turned ;; Motion
refused ;; Agree_or_refuse_to_act || invasion ;; Attack || ordered ;; Arranging
flee ;; Escaping || occupied ;; Control
organized ;; Arranging
began ;; Process_start || ended ;; Process_end || agreeing ;; Agree_or_refuse_to_act
received ;; Receiving || agreed ;; Agree_or_refuse_to_act || exchange ;; Exchange || acknowledged ;; Statement
emerged ;; Coming_to_be || assassinated ;; Killing
order ;; Arranging || command ;; Control || deemed ;; Coming_to_believe || ordered ;; Arranging
reached ;; Arriving || suffered ;; Bodily_harm || returned ;; Arriving
ensued ;; Process_start
preferred ;; Choosing || restore ;; Recovering
accepted ;; Receiving || controlled ;; Control
overthrown ;; Change_of_leadership || forced ;; Hostile_encounter || leading to ;; Causation
invasions ;; Attack || reported ;; Reporting || travelogue ;; Traveling
referred ;; Name_conferral || made up ;; Becoming_a_member
remembered ;; Change_sentiment
conflicts ;; Hostile_encounter
declared ;; Expressing_publicly || rebelled ;; Change_of_leadership
promised ;; Commitment || control ;; Control || fallen ;; Motion_directional
agreed ;; Agree_or_refuse_to_act || combined ;; Cause_to_amalgamate || defeat ;; Conquering || made ;; Manufacturing
attacked ;; Attack || dispatching ;; Sending
retreated ;; Escaping || captured ;; Conquering || besieged ;; Besieging || invasion ;; Attack
fled ;; Escaping || approached ;; Arriving || abdicated ;; Change_of_leadership
enthroned ;; Change_of_leadership
negotiated ;; Communication || retreat ;; Escaping || agreeing ;; Agree_or_refuse_to_act || siege ;; Besieging
defend ;; Defending || fortifying ;; Cause_change_of_strength || deal ;; Resolve_problem || ordered ;; Arranging
besieged ;; Besieging || resumed ;; Process_start
captured ;; Conquering
separated ;; Forming_relationships
retreated ;; Escaping || relocated ;; Removing
retreated ;; Escaping || supported ;; Supporting || orders ;; Arranging || regained ;; Recovering
set ;; Placing || conflicts ;; Hostile_encounter || fall ;; Motion_directional
begun ;; Process_start
assassinated ;; Killing || lost ;; Earnings_and_losses
invasion ;; Attack
replace ;; Change_of_leadership || lost ;; Earnings_and_losses || invading ;; Attack
captured ;; Conquering || allied ;; Collaboration
collapsed ;; Destroying
became ;; Becoming || collapsed ;; Catastrophe
changes ;; Change
brought about ;; Causation || Battles ;; Hostile_encounter
recorded ;; Recording || siege ;; Besieging
fought ;; Hostile_encounter || reports ;; Reporting || exploding ;; Attack
ruling ;; Legal_rulings
assimilated ;; Cause_to_amalgamate || conquered ;; Conquering
basing ;; Supporting || instituted ;; Action
diminished ;; Cause_change_of_position_on_a_scale
trade ;; Exchange || returned ;; Arriving
expanded ;; Expansion
reaching ;; Arriving || attacked ;; Attack || Operation ;; Military_operation || control ;; Control
named ;; Name_conferral
expelling ;; Removing || fighting ;; Hostile_encounter
ended ;; Process_end || accusations ;; Judgment_communication || due ;; Causation || crimes ;; Committing_crime
became ;; Becoming || pressure ;; Convincing || outcome ;; Coming_to_believe || forced ;; Causation || battle ;; Hostile_encounter || withdrawal ;; Escaping
exchanged ;; Exchange || driving ;; Self_motion || resulting in ;; Causation
fought ;; Hostile_encounter || battle ;; Hostile_encounter || considered ;; Convincing
concert ;; Social_event || launched ;; Process_start || pay ;; Commerce_pay || promote ;; Cause_to_make_progress
swapped out ;; Exchange
included ;; Cause_to_be_included || play ;; Competition || assembled ;; Come_together
performed ;; Hold
paid ;; Commerce_pay
opening ;; Openness || Open ;; Openness
beginning ;; Process_start || lifted ;; Motion
died ;; Death || last ;; Process_end
Conference ;; Social_event || meeting ;; Social_event
conferences ;; Social_event
taken place ;; Process_start || preceded ;; Process_start
arrived ;; Arriving || open ;; Openness
addressed ;; Expressing_publicly || envisaged ;; Arranging
signed ;; Sign_agreement || recognize ;; Know
hurricane ;; Catastrophe
Forming ;; Coming_to_be || intensified ;; Cause_change_of_strength
hurricanes ;; Catastrophe || known ;; Know || reached ;; Arriving
damages ;; Damaging || casualties ;; Catastrophe || hurricane ;; Catastrophe || reach ;; Achieve || reported ;; Reporting
moved ;; Motion || began ;; Process_start || moved ;; Motion || weaken ;; Cause_change_of_strength
dissipated ;; Dispersal || issued ;; Expressing_publicly
caused ;; Causation || weakening ;; Cause_change_of_strength || reaching ;; Arriving || hurricane ;; Catastrophe
better ;; Cause_to_make_progress
protests ;; Protest || protests ;; Protest || triggered ;; Causation
criticized ;; Judgment_communication
became ;; Becoming || injuries ;; Bodily_harm || protests ;; Protest
protests ;; Protest || announced ;; Expressing_publicly || resignation ;; Change_of_leadership
said ;; Communication
Protests ;; Protest
demanded ;; Request || elections ;; Choosing
disagreements ;; Quarreling || protests ;; Protest
meeting ;; Social_event || summit ;; Social_event
took place ;; Process_start
marked ;; Recording || sitting ;; Placing || visited ;; Traveling
summit ;; Social_event || taken place ;; Process_start
attending ;; Participation
trip ;; Traveling || summit ;; Social_event
travelled ;; Traveling || summit ;; Social_event || met with ;; Come_together
gave ;; Giving || directed ;; Aiming || met with ;; Come_together || speech ;; Expressing_publicly
travelled ;; Traveling || met with ;; Come_together
spent ;; Expend_resource || visiting ;; Traveling || summit ;; Social_event
invasion ;; Hostile_encounter
took place ;; Process_start
turning ;; Change || defeat ;; Conquering || changed ;; Change
weakened ;; Cause_change_of_strength || made ;; Manufacturing || gains ;; Getting
conflict ;; Hostile_encounter
prevent ;; Preventing_or_letting || spread ;; Dispersal
killed ;; Killing || established ;; Building || defeat ;; Conquering
recognized ;; Know || establishment ;; Reforming_a_system
made ;; Manufacturing || rolled ;; Self_motion
confirmed ;; Check || constitution ;; Legal_rulings
emphasising ;; Expressing_publicly || include ;; Cause_to_be_included
made it ;; Causation
blocked ;; Hiding_objects || occurring ;; Coming_to_be
occur ;; Coming_to_be
conflict ;; Hostile_encounter
originated ;; Coming_to_be
causes ;; Causation
agreement ;; Agree_or_refuse_to_act
established ;; Building || allowed ;; Preventing_or_letting || expel ;; Removing
move ;; Motion || recognized ;; Know || permitted ;; Preventing_or_letting
ruled ;; Legal_rulings || placed ;; Placing || converted ;; Change || resign ;; Process_end
erupt ;; Process_start
agreed ;; Agree_or_refuse_to_act || adhered ;; Connect || conflicts ;; Hostile_encounter
says ;; Communication || gained ;; Getting || offering ;; Supply
cessation ;; Process_end
declared ;; Expressing_publicly || contained ;; Containing || become ;; Becoming || meeting ;; Social_event
happen ;; Presence
convert ;; Change || drive ;; Self_motion
convert ;; Change || turn ;; Change
understood ;; Know || preserve ;; Preserving || unite ;; Collaboration
held ;; Defending
understood ;; Know || acquired ;; Getting || harm ;; Damaging
established ;; Building || relieve ;; Confronting_problem || kept ;; Temporary_stay
maintained ;; Preserving
arose ;; Coming_to_be || dispute ;; Quarreling
forced ;; Hostile_encounter
make ;; Manufacturing || ceded ;; Surrendering
revolted ;; Change_of_leadership
granted ;; Giving || controlled ;; Control || included ;; Cause_to_be_included
requested ;; Request || sent ;; Sending
seized ;; Hold || request ;; Request || releasing ;; Releasing || abdicated ;; Change_of_leadership
died ;; Death || acquired ;; Getting
elected ;; Choosing
become ;; Becoming || forced ;; Hostile_encounter
received ;; Receiving
allow ;; Preventing_or_letting || acceded ;; Agree_or_refuse_to_act || constructed ;; Building
dissolved ;; Destroying || reverse ;; Change || protested ;; Agree_or_refuse_to_act
trigger ;; Causation
gathered ;; Come_together
stripped ;; Emptying || seized ;; Hold || stormed ;; Catastrophe
held ;; Defending || found ;; Know || threw ;; Self_motion
illegal ;; Legality
case ;; Protest || jurisdiction ;; Legality
saved ;; Rescuing
started ;; Process_start || support ;; Supporting
stood ;; Placing
made ;; Manufacturing
formed ;; Coming_to_be
made ;; Manufacturing || refusal ;; Agree_or_refuse_to_act
aid ;; Assistance || married ;; Forming_relationships
revolt ;; Change_of_leadership || chosen ;; Choosing
refused ;; Agree_or_refuse_to_act || revolt ;; Change_of_leadership || election ;; Choosing
called ;; Name_conferral || met ;; Arriving || conflict ;; Hostile_encounter
sent ;; Sending || offer ;; Supply || informing ;; Reporting || accepted ;; Receiving
crashed ;; Catastrophe || death ;; Death
crashed ;; Catastrophe || flown ;; Motion || crossing ;; Motion_directional
broke ;; Destroying || impact ;; Influence
catch fire ;; Damaging
caused ;; Causation || triggered ;; Causation
caused ;; Causation || decrease ;; Cause_change_of_position_on_a_scale || approach ;; Arriving
noticed ;; Know || crashed ;; Catastrophe || increase ;; Cause_change_of_position_on_a_scale || recover ;; Recovering || take ;; Motion
remind ;; Telling || advising ;; Convincing || use ;; Using || landing ;; Self_motion || issued ;; Expressing_publicly
attached ;; Connect || crashed ;; Catastrophe || collided ;; Motion
died ;; Death
treated ;; Cure || taken ;; Bringing
diverted ;; Motion
deaths ;; Death || concluded ;; Process_end
flying ;; Self_motion
began ;; Process_start || disaster ;; Catastrophe
occurred ;; Coming_to_be
present ;; Presence
treated ;; Cure || released ;; Releasing || walk ;; Self_motion
believed ;; Know
believed ;; Know || explosion ;; Catastrophe
command ;; Control || said ;; Telling || believed ;; Know
occurred ;; Coming_to_be || occurred ;; Coming_to_be
died ;; Death || recovered ;; Cure
ranks ;; Placing
resulted in ;; Causation || caused ;; Causation || surpassed ;; Cause_change_of_position_on_a_scale
said ;; Telling || apologise ;; Judgment_communication
established ;; Creating || elected ;; Choosing
reports ;; Reporting
cost ;; Cost
took over ;; Getting || entered ;; Arriving
constructing ;; Building || return ;; Arriving
crash ;; Catastrophe || named ;; Name_conferral
killed ;; Killing
crash ;; Catastrophe || occurred ;; Coming_to_be
dead ;; Death
survivor ;; Cure
win ;; Getting || elections ;; Choosing
derailed ;; Motion || wrecked ;; Destroying
killed ;; Killing || injured ;; Bodily_harm
derailed ;; Motion || traveling ;; Self_motion
extricated ;; Body_movement
helped ;; Assistance || rescue ;; Rescuing
treated ;; Cure
derailment ;; Motion || disrupted ;; Hindering
caused ;; Causation || becoming ;; Becoming || derailment ;; Catastrophe || activation ;; Being_in_operation || delayed ;; Hindering || due ;; Causation || prevented ;; Preventing_or_letting || ruled ;; Legal_rulings || losing ;; Earnings_and_losses || said ;; Statement || operational ;; Being_in_operation
derailment ;; Catastrophe || equipped ;; Supply || operational ;; Being_in_operation || limited ;; Limiting
derailment ;; Catastrophe || completed ;; Process_end || installation ;; Building
died ;; Death || wreck ;; Catastrophe
landfall ;; Self_motion
developed ;; Coming_to_be
tracked ;; Self_motion || decreasing ;; Cause_change_of_position_on_a_scale || acquired ;; Getting
transitioned ;; Change
moved ;; Motion
weakened ;; Cause_change_of_strength || moving ;; Motion || dropping ;; Motion_directional
became ;; Becoming || turned ;; Becoming
produced ;; Manufacturing || causing ;; Causation || deaths ;; Death || flooding ;; Motion
affected ;; Influence
produced ;; Manufacturing || leaving ;; Departing
produced ;; Manufacturing || landfall ;; Self_motion || left ;; Departing
alleviated ;; Cure || put out ;; Placing
killed ;; Killing
snapped ;; Destroying || damaged ;; Damaging || spawned ;; Coming_to_be
aim ;; Aiming || wars ;; Hostile_encounter || took place ;; Process_start
research ;; Research || aim ;; Aiming
restoration ;; Recovering || rejection ;; Agree_or_refuse_to_act || hardened ;; Change
started ;; Process_start || opposing ;; Agree_or_refuse_to_act || established ;; Creating
fell ;; Motion_directional || appeared ;; Presence
formed ;; Coming_to_be || support ;; Supporting
believed ;; Know || supported ;; Supporting
convinced ;; Convincing || establish ;; Creating
fought ;; Hostile_encounter || warfare ;; Hostile_encounter || warfare ;; Hostile_encounter || wars ;; Hostile_encounter || wars ;; Hostile_encounter || conflicts ;; Hostile_encounter
resulted in ;; Causation
remained ;; Presence
abolished ;; Removing
ended ;; Process_end || abolished ;; Removing
replaced ;; Change
retained ;; Preserving || remained ;; Presence
changes ;; Change || rebellions ;; Change_of_leadership || fought ;; Hostile_encounter
wars ;; Hostile_encounter
triggered ;; Causation || conflicts ;; Hostile_encounter
emerged ;; Coming_to_be || influenced ;; Influence || took place ;; Process_start
wars ;; Hostile_encounter || cause ;; Causation || occurring ;; Coming_to_be
Battle ;; Hostile_encounter || engagement ;; Hostile_encounter || War ;; Military_operation || fought ;; Hostile_encounter
aligned ;; Placing || rising ;; Cause_change_of_position_on_a_scale
research ;; Research || battlefield ;; Hostile_encounter || inventoried ;; Scrutiny || protected ;; Defending
murder ;; Killing || crowned ;; Change_of_leadership || involved ;; Cause_to_be_included
led to ;; Causation || war ;; Military_operation
Battle ;; Hostile_encounter || crowned ;; Change_of_leadership || Battle ;; Hostile_encounter || defeat ;; Conquering
made ;; Manufacturing || recruited ;; Becoming_a_member || landing ;; Self_motion
support ;; Supporting || command ;; Control || came ;; Arriving
well known ;; Know || cover ;; Hiding_objects || protection ;; Preserving
forces ;; Causation || held ;; Defending
landing ;; Self_motion || met with ;; Come_together || disaster ;; Catastrophe
revolution ;; Change_of_leadership || occurred ;; Coming_to_be || revolution ;; Change_of_leadership || insurrection ;; Violence || armed ;; Bearing_arms || referred ;; Statement
revolution ;; Change_of_leadership || occurred ;; Coming_to_be || revolutions ;; Change_of_leadership || overthrow ;; Change_of_leadership || rule ;; Control || proclamation ;; Statement || led to ;; Causation
revolution ;; Change_of_leadership || emerged ;; Coming_to_be || reforms ;; Reforming_a_system || opposition ;; Participation
perceived ;; Know || attack ;; Attack
focused ;; Aiming || Resistance ;; Response || grew ;; Cause_to_make_progress
Revolution ;; Change_of_leadership || rioting ;; Violence || disruption ;; Damaging || formed ;; Coming_to_be
crossed ;; Motion_directional || revolutions ;; Change_of_leadership || outbreak ;; Process_start || Battle ;; Military_operation || defeated ;; Conquering
took ;; Conquering || supported ;; Supporting || proclaimed ;; Statement
received ;; Receiving || became ;; Becoming || support ;; Supporting || established ;; Creating || divided ;; Destroying
advocated ;; Convincing || supported ;; Supporting
support ;; Supporting || exile ;; Escaping || terror ;; Attack || drove ;; Motion
ended ;; Process_end || revolutionaries ;; Change_of_leadership || war ;; Military_operation || prepared ;; GetReady || suppress ;; Hindering
proposed ;; Statement || amnesty ;; Legal_rulings
Battle ;; Military_operation || revolution ;; Change_of_leadership || overcome ;; Resolve_problem || overrun ;; Conquering || defeated ;; Conquering
Wars ;; Hostile_encounter || reestablishment ;; Recovering || overrun ;; Conquering
revolution ;; Change_of_leadership || revolution ;; Change_of_leadership || used ;; Using || comparisons ;; Research
revolution ;; Change_of_leadership || formation ;; Coming_to_be || influence ;; Influence || seen ;; Change_sentiment
revolution ;; Change_of_leadership || triggered ;; Process_start || chain ;; Connect
influenced ;; Influence || withdrawal ;; Removing || investigate ;; Research || elections ;; Choosing
demanded ;; Request
start ;; Process_start || maintaining ;; Preserving
withdrew ;; Escaping
achieved ;; Achieve || disbanding ;; Dispersal
see ;; Perception_active
taken ;; Conquering
igniting ;; Process_start || fires ;; Catastrophe || wildfires ;; Catastrophe || wildfires ;; Catastrophe || burning ;; Damaging
drought ;; Catastrophe || drought ;; Catastrophe
wildfires ;; Catastrophe || ignited ;; Process_start || burned ;; Destroying
caused ;; Causation || damage ;; Damaging || fatalities ;; Death || wildfires ;; Catastrophe || causing ;; Causation || injuries ;; Bodily_harm
began ;; Process_start || wildfire ;; Catastrophe || ignited ;; Process_start || fires ;; Catastrophe || igniting ;; Process_start
broke out ;; Process_start || wildfires ;; Catastrophe || wildfires ;; Catastrophe
said ;; Statement || dealt ;; Arranging || described ;; Statement || wildfires ;; Catastrophe
caused ;; Causation || estimated ;; Statement || wildfires ;; Catastrophe || damage ;; Damaging
wildfires ;; Catastrophe || ignited ;; Process_start || reached ;; Arriving
erupted ;; Process_start || wildfires ;; Catastrophe || becoming ;; Becoming || wildfires ;; Catastrophe
became ;; Becoming || Fire ;; Catastrophe || wildfire ;; Catastrophe
killed ;; Killing || sparked ;; Causation || Fire ;; Catastrophe || wildfire ;; Catastrophe || crashed ;; Damaging
wildfires ;; Catastrophe || extinguished ;; Destroying
present ;; Presence || wildfires ;; Catastrophe || extinguished ;; Destroying
wildfires ;; Catastrophe || sparked ;; Process_start || extinguished ;; Removing
found ;; Know
published ;; Publishing || titled ;; Name_conferral
stating ;; Statement || projected ;; Research || showed ;; Statement || fires ;; Catastrophe || increase ;; Cause_change_of_position_on_a_scale || exceed ;; Cause_change_of_position_on_a_scale || warming ;; Cause_change_of_position_on_a_scale || influence ;; Influence || increases ;; Cause_change_of_position_on_a_scale
conducted ;; Action || test ;; Research
conduct ;; Action || announced ;; Expressing_publicly || test ;; Research
blast ;; Catastrophe || detected ;; Check
suggested ;; Convincing
told ;; Telling
test ;; Research || test ;; Research || required ;; Preventing_or_letting || failed ;; Confronting_problem
paper ;; Statement
given ;; Giving || warning ;; Warning || test ;; Research || reported ;; Reporting || occur ;; Coming_to_be
sent ;; Sending || told ;; Telling || alert ;; Warning || test ;; Research
occurred ;; Presence || fought ;; Hostile_encounter || Fall ;; Conquering || Campaign ;; Military_operation || War ;; Hostile_encounter
Arrival ;; Arriving
overhaul ;; Reforming_a_system || surrender ;; Surrendering
given ;; Giving || restoring ;; Recovering
spent ;; Expend_resource || rebuilding ;; Recovering
sent ;; Sending || recruited ;; Becoming_a_member
built ;; Building || trained ;; Education_teaching
obtained ;; Getting || supply ;; Supply
campaign ;; Military_operation || launched ;; Process_start
forming ;; Cause_to_amalgamate || well-trained ;; Education_teaching || well-equipped ;; Supply
included ;; Cause_to_be_included || called ;; Request
command ;; Control
perceived ;; Perception_active || insurrection ;; Hostile_encounter
revolt ;; Change_of_leadership || existed ;; Presence || emboldened ;; Influence
began ;; Process_start || became ;; Becoming || emerged ;; Coming_to_be || defeated ;; Conquering || lingered ;; Temporary_stay || support ;; Supporting || launched ;; Process_start
revolution ;; Change_of_leadership || struggles ;; Hostile_encounter || viewed ;; Perception_active || revolution ;; Change_of_leadership || suggest ;; Convincing || revolutions ;; Change_of_leadership
struggled ;; Hostile_encounter || inspired ;; Influence || differed ;; Change
reformed ;; Reforming_a_system || led ;; Causation || leading to ;; Causation
championship ;; Competition || played ;; Competition || Cup ;; Competition
cup ;; Competition || overhauled ;; Reforming_a_system
compete ;; Competition || championship ;; Competition || playoffs ;; Competition || qualify ;; Becoming_a_member
joined ;; Becoming_a_member || round ;; Process_start || field ;; Competition
ended up ;; Process_end || cup ;; Competition || winning ;; Getting
competing ;; Competition || tournament ;; Competition
freedom ;; Change_of_leadership || held ;; Defending || march ;; Self_motion
organized ;; Arranging
issues ;; Creating || proposals ;; Convincing || spending ;; Expend_resource
serving ;; Assistance
included ;; Cause_to_be_included || reform ;; Reforming_a_system || party ;; Social_event
called ;; Name_conferral || following ;; Self_motion || promoted ;; Cause_change_of_position_on_a_scale || attacks ;; Attack
gathering ;; Come_together || held ;; Defending || demonstration ;; Social_event
suggested ;; Convincing
released ;; Statement || stated ;; Statement || saying ;; Communication || freedom ;; Change_of_leadership || participants ;; Participation || attributed ;; Causation
reported ;; Reporting || organizers ;; Arranging
suggested ;; Convincing || said ;; Communication || participants ;; Participation
begun ;; Process_start || wars ;; Hostile_encounter || occurring ;; Coming_to_be
confronted ;; Confronting_problem || combined ;; Cause_to_amalgamate || opposition ;; Hostile_encounter || end ;; Process_end
started ;; Process_start || increase ;; Cause_change_of_position_on_a_scale || dispute ;; Quarreling
check ;; Scrutiny || pushed ;; Self_motion || questioned ;; Communication
campaigns ;; Military_operation
ended ;; Process_end || outbreak ;; Process_start
operate ;; Being_in_operation || war ;; Hostile_encounter || hardened ;; Cause_change_of_strength
impressed ;; Change_sentiment || reorganize ;; Reforming_a_system || fighting ;; Hostile_encounter || forces ;; Hostile_encounter
end ;; Process_end || crisis ;; Hostile_encounter
final ;; Process_end || battle ;; Hostile_encounter || humiliated ;; Judgment_communication
strengthen ;; Cause_change_of_strength || served ;; Assistance || weakened ;; Cause_change_of_position_on_a_scale
facilitated ;; Cause_to_make_progress || emergence ;; Coming_to_be
incident ;; Catastrophe || Disaster ;; Catastrophe || demonstration ;; Social_event || happened ;; Presence || riot ;; Violence
visit ;; Traveling || protesting ;; Agree_or_refuse_to_act
became ;; Becoming || turned ;; Becoming
killed ;; Killing || end ;; Process_end || buildings ;; Building || destroyed ;; Destroying
caused ;; Causation || changes ;; Change
enacted ;; Action || reforms ;; Reforming_a_system || armed ;; Bearing_arms || repressive ;; Hindering || enforced ;; Action || order ;; Arranging || improve ;; Cause_to_make_progress || forced ;; Causation
fought ;; Hostile_encounter || Battle ;; Hostile_encounter || battle ;; Hostile_encounter || War ;; Hostile_encounter
force ;; Hostile_encounter || battle ;; Hostile_encounter || battle ;; Hostile_encounter || fought ;; Hostile_encounter
began ;; Process_start || end ;; Process_end || battles ;; Hostile_encounter || advance ;; Cause_to_make_progress
fighting ;; Hostile_encounter || Battle ;; Hostile_encounter
Allied ;; Cause_to_amalgamate || operations ;; Military_operation || fighting ;; Hostile_encounter || battle ;; Hostile_encounter || Battle ;; Hostile_encounter || battles ;; Hostile_encounter || attacks ;; Attack || Battle ;; Hostile_encounter || culminated ;; Process_end || Battle ;; Hostile_encounter || faded ;; Change
merged ;; Cause_to_amalgamate || understood ;; Know || Battles ;; Hostile_encounter || War ;; Hostile_encounter || battles ;; Hostile_encounter || offensive ;; Attack || wrote in ;; Writing || battle ;; Hostile_encounter || battle ;; Hostile_encounter || retired ;; Process_end || offensive ;; Judgment_communication || battles ;; Hostile_encounter || took place ;; Process_start || taken ;; Conquering
progress ;; Cause_to_make_progress || Attacks ;; Attack
took ;; Conquering || cost ;; Cost || Battle ;; Hostile_encounter
capture ;; Conquering || offensive ;; Judgment_communication
obtain ;; Getting || exhausted ;; Expend_resource || forces ;; Hostile_encounter || moved ;; Motion
refused ;; Agree_or_refuse_to_act || orders ;; Arranging || suffering ;; Bodily_harm
operations ;; Military_operation || battles ;; Hostile_encounter || become ;; Becoming || battles ;; Hostile_encounter
repulsed ;; Defending || attacks ;; Attack
losses ;; Earnings_and_losses || effect ;; Influence || attacks ;; Attack
armies ;; Hostile_encounter || warfare ;; Hostile_encounter || developments ;; Cause_to_make_progress || neutralised ;; Destroying || equipped ;; Supply || revolution ;; Change_of_leadership || proved ;; Know
supply ;; Supply || battlefield ;; Hostile_encounter || dominated ;; Control || use ;; Using || defensive ;; Defending
fought ;; Hostile_encounter || battles ;; Hostile_encounter
reconsidered ;; Deciding
coalition ;; Cause_to_amalgamate || detach ;; Removing
war ;; Hostile_encounter || attrition ;; Hostile_encounter || make ;; Causation || cost ;; Cost
obtain ;; Getting || negotiate ;; Communication || belligerents ;; Hostile_encounter || victory ;; Earnings_and_losses || face ;; Confronting_problem
took place ;; Process_start
use ;; Using
began ;; Process_start
crossed ;; Motion_directional
initiated ;; Process_start || led to ;; Causation
began ;; Process_start
crossed ;; Motion_directional || advanced ;; Cause_to_make_progress
mobilized ;; Arranging || halted ;; Process_end
made ;; Getting
pushed ;; Influence
launched ;; Process_start
began ;; Process_start
repulsed ;; Defending || ordered ;; Request || believed ;; Know
began ;; Process_start || counter-attacked ;; Attack || crossed ;; Motion_directional
improved ;; Cause_to_make_progress || completed ;; Process_end
imposed ;; Action || led to ;; Causation
felt ;; Change_sentiment
led ;; Causation || dominate ;; Control
paved ;; Influence
led to ;; Causation || normalized ;; Change
left ;; Departing
involving ;; Cause_to_be_included
send ;; Sending || prevent ;; Preventing_or_letting || captured ;; Conquering || overrun ;; Conquering
sent ;; Sending || command ;; Control
scattered ;; Dispersal || retreat ;; Escaping || reached ;; Arriving || withering ;; Change || ended ;; Process_end
Charge ;; Judgment_communication || published ;; Publishing
carrying out ;; Action
delivered ;; Sending || died ;; Death || assault ;; Attack || order ;; Arranging
cyclone ;; Catastrophe || affect ;; Influence
formed ;; Coming_to_be || moved ;; Self_motion || intensifying ;; Cause_change_of_strength
storm ;; Catastrophe || intensified ;; Cause_change_of_strength
reached ;; Achieve || landfall ;; Arriving
entering ;; Arriving || re-intensified ;; Cause_change_of_strength || weakened ;; Cause_change_of_strength
passing ;; Motion_directional || turned ;; Self_motion || striking ;; Connect
crossed ;; Motion_directional
emerging ;; Coming_to_be || dissipating ;; Change
destroyed ;; Destroying || produced ;; Creating
left ;; Causation || deaths ;; Death
causing ;; Causation || damaged ;; Damaging || dropped ;; Motion_directional
destroyed ;; Destroying
caused ;; Causation
injured ;; Bodily_harm || killed ;; Killing
rushed ;; Motion || storm ;; Catastrophe || supplies ;; Supply
destroyed ;; Destroying || death ;; Death || damage ;; Damaging || reported ;; Reporting
Accident ;; Catastrophe || crashed ;; Catastrophe || occurred ;; Coming_to_be
passing ;; Motion_directional || started ;; Process_start || experiencing ;; Know || accelerating ;; Cause_change_of_strength
lost ;; Earnings_and_losses || Combined ;; Cause_to_amalgamate || lift ;; Cause_change_of_position_on_a_scale
turn ;; Change || hit ;; Attack
killed ;; Killing || impact ;; Influence || died ;; Death
fatal ;; Killing || accident ;; Catastrophe || crash ;; Attack
host ;; Come_together
hindered ;; Hindering || search ;; Scrutiny || rescue ;; Rescuing || operation ;; Military_operation || blizzard ;; Catastrophe
left ;; Departing || party ;; Social_event || rescue ;; Rescuing || met ;; Come_together
caused ;; Causation || icing ;; Change || investigation ;; Scrutiny || concluded ;; Coming_to_believe
released ;; Publishing || support ;; Supporting || tour ;; Traveling
see ;; Perception_active || playing ;; Participation
commenced ;; Process_start || finished ;; Process_end || passing ;; Motion_directional || selected ;; Choosing
helped ;; Assistance || solidifying ;; Change || marked ;; Recording
opted ;; Choosing
concert ;; Social_event
released ;; Publishing || let ;; Preventing_or_letting
caused ;; Causation || damage ;; Damaging || hurricane ;; Catastrophe
cyclone ;; Catastrophe || originated ;; Coming_to_be
storm ;; Catastrophe || cyclone ;; Catastrophe || curved ;; Motion_directional || intensified ;; Cause_change_of_strength || becoming ;; Becoming || hurricane ;; Catastrophe
storm ;; Catastrophe || storm ;; Catastrophe || ensued ;; Coming_to_be || approached ;; Arriving || striking ;; Motion
made ;; Manufacturing || storm ;; Catastrophe || landfall ;; Arriving
weakened ;; Cause_change_of_strength || moving ;; Motion || cyclone ;; Catastrophe || falling ;; Cause_change_of_position_on_a_scale
weakened ;; Cause_change_of_strength || curved ;; Motion_directional || entered ;; Arriving || storm ;; Catastrophe
became ;; Becoming || storm ;; Catastrophe || accelerated ;; Cause_change_of_strength
traversed ;; Self_motion || dissipating ;; Change
hurricane ;; Catastrophe || produced ;; Causation
stretching ;; Expansion || recorded ;; Recording
observed ;; Perception_active || landfall ;; Arriving
impacted ;; Influence || destroyed ;; Destroying || damaged ;; Damaging
received ;; Receiving || impact ;; Influence || damage ;; Damaging
destruction ;; Destroying || hurricane ;; Catastrophe || damaged ;; Damaging || suffered ;; Earnings_and_losses || damage ;; Damaging
damage ;; Damaging || occurred ;; Presence || damage ;; Damaging
Damage ;; Damaging || deaths ;; Death || occurred ;; Presence
conflict ;; Hostile_encounter
named ;; Name_conferral
protected ;; Defending || killing ;; Killing || service ;; Supply || forced ;; Hostile_encounter
ended ;; Process_end || signed ;; Sign_agreement || conference ;; Social_event
violence ;; Violence || deteriorated ;; Change
caused ;; Causation || resulting in ;; Causation
death ;; Death || say ;; Telling || put ;; Placing || lost ;; Earnings_and_losses
struck ;; Attack || Hurricane ;; Catastrophe || destructive ;; Destroying || hurricane ;; Catastrophe
hurricane ;; Catastrophe || hit ;; Attack || damaged ;; Damaging || destroyed ;; Destroying || destructive ;; Destroying || Hurricane ;; Catastrophe || surpassed ;; Motion_directional
hurricane ;; Catastrophe || landfalling ;; Self_motion || hurricane ;; Catastrophe || landfall ;; Arriving || surpassed ;; Motion_directional
caused ;; Causation || damage ;; Damaging || impact ;; Influence || storm ;; Catastrophe || hurricane ;; Catastrophe || made ;; Manufacturing || landfall ;; Arriving || felt ;; Change_sentiment
Passing ;; Motion_directional || stripped ;; Removing
destroyed ;; Destroying || damaged ;; Damaging || caused ;; Causation || damage ;; Damaging || dead ;; Death
began ;; Process_start || depression ;; Catastrophe
strengthening ;; Cause_change_of_strength || moving ;; Motion || intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe || spending ;; Expend_resource
weakened ;; Cause_change_of_strength || traversing ;; Motion_directional || regained ;; Recovering || landfall ;; Arriving
hurricane ;; Catastrophe || landfall ;; Arriving || strike ;; Attack
hurricane ;; Catastrophe || strength ;; Cause_change_of_strength || emerged ;; Presence
weakening ;; Cause_change_of_strength || moved ;; Motion || storm ;; Catastrophe || turning ;; Becoming
moving ;; Motion || hurricane ;; Catastrophe || lost ;; Earnings_and_losses || merging ;; Cause_to_amalgamate || curved ;; Motion_directional
moved ;; Motion || Hurricane ;; Catastrophe || inflicted ;; Causation || damage ;; Damaging || storm ;; Catastrophe || winds ;; Motion || tornadoes ;; Catastrophe || surge ;; Motion_directional || lashing ;; Attack
destroyed ;; Destroying || damage ;; Damaging || transport ;; Sending || fishing ;; Self_motion
dead ;; Death || damage ;; Damaging
gust ;; Motion || produced ;; Creating || observed ;; Perception_active
received ;; Receiving || storm ;; Catastrophe || brunt ;; Attack
storm ;; Catastrophe
began ;; Process_start || destroyed ;; Destroying || downed ;; Motion
Rainfall ;; Motion || peaking ;; Motion_directional
killed ;; Killing || damage ;; Damaging || record ;; Recording || left ;; GiveUp
caused ;; Causation || losses ;; Earnings_and_losses || damage ;; Damaging || landfall ;; Arriving || leading to ;; Causation
damaging ;; Damaging || stretches ;; Body_movement || winds ;; Motion || produced ;; Creating
devastated ;; Destroying || downed ;; Motion
hurricane ;; Catastrophe || killed ;; Killing
damaged ;; Damaging || destroyed ;; Destroying || demolished ;; Destroying || losses ;; Earnings_and_losses || exceeded ;; Cause_change_of_position_on_a_scale
caused ;; Causation || hurricane ;; Catastrophe || deaths ;; Death || state ;; Statement
tornadoes ;; Catastrophe || spawned ;; Creating
caused ;; Causation || dead ;; Death || damage ;; Damaging || left ;; GiveUp
hurricane ;; Catastrophe || hurricane ;; Catastrophe || hit ;; Attack
hurricane ;; Catastrophe || hit ;; Attack
battle ;; Hostile_encounter
started ;; Process_start || victory ;; Earnings_and_losses
withdraw ;; Escaping || forces ;; Hostile_encounter || pressure ;; Convincing || cross ;; Motion_directional || ordered ;; Arranging
attacked ;; Attack || defenses ;; Defending || breaching ;; Damaging
forces ;; Hostile_encounter || command ;; Control || prevent ;; Preventing_or_letting || evacuated ;; Escaping
captured ;; Conquering || abandoned ;; GiveUp || command ;; Control || support ;; Supporting || galvanized ;; Influence
exhausted ;; Expend_resource || allowing ;; Preventing_or_letting || fighting ;; Hostile_encounter || regain ;; Recovering
final ;; Competition || match ;; Competition
competition ;; Competition || final ;; Competition
final ;; Process_end || reached ;; Arriving || defeated ;; Earnings_and_losses
scoring ;; Getting || conceding ;; Earnings_and_losses || final ;; Competition || reached ;; Becoming_a_member
included ;; Cause_to_be_included
decided ;; Deciding || final ;; Competition
spent ;; Expend_resource || training ;; Education_teaching || thwarted ;; Preventing_or_letting
said ;; Communication || missed ;; Earnings_and_losses
posted ;; Sending
conference ;; Social_event || discuss ;; Communication || conference ;; Social_event || conference ;; Social_event || conference ;; Social_event
held ;; Defending
abandon ;; GiveUp || conference ;; Social_event || admit ;; Know
meeting ;; Social_event || rest ;; Placing || worked ;; Employment || limited ;; Limiting || establish ;; Creating
producers ;; Manufacturing || producers ;; Manufacturing || producers ;; Manufacturing || called ;; Request
result ;; Causation || conference ;; Social_event || agreements ;; Sign_agreement
open ;; Openness || led to ;; Causation || abandonment ;; GiveUp || left ;; Departing || remained ;; Temporary_stay
increasing ;; Cause_change_of_position_on_a_scale || expanding ;; Expansion || supply ;; Supply || spending ;; Expend_resource || conference ;; Social_event || adoption ;; Using || lowering ;; Cause_change_of_position_on_a_scale
annoyed ;; Judgment_communication || affected ;; Influence
participated ;; Participation || Conference ;; Social_event || attended ;; Participation
conference ;; Social_event || attended ;; Perception_active || services ;; Supply
played ;; Competition
leg ;; Social_event || played ;; Competition
winning ;; Getting || qualified ;; Competition
beating ;; Competition
qualified ;; Getting || defeated ;; Preventing_or_letting
meeting ;; Social_event
watch ;; Perception_active || stabbed ;; Bodily_harm
abandoned ;; GiveUp || arrived ;; Arriving || suspended ;; Change_event_time
paid ;; Giving
play ;; Competition || marked ;; Recording || return ;; Arriving || spent ;; Expend_resource
win ;; Getting || dominate ;; Control
engagement ;; Hostile_encounter || Wars ;; Hostile_encounter || fought ;; Hostile_encounter || met ;; Come_together
campaigns ;; Military_operation || encounter ;; Hostile_encounter || participating ;; Participation || aware ;; Know
destroying ;; Destroying || departed ;; Departing || raiding ;; Attack || campaign ;; Military_operation || tasked ;; Request || led ;; Conquering || hunting ;; Military_operation
sailed ;; Self_motion
raided ;; Attack || Battle ;; Hostile_encounter || achieving ;; Achieve || withdrawing ;; Escaping
decided ;; Deciding || replenishment ;; Supply || sailing ;; Self_motion || offered ;; Giving || eliminated ;; Removing || exhausted ;; Expend_resource || return ;; Arriving
engage ;; Hostile_encounter || failed ;; Agree_or_refuse_to_act || capture ;; Conquering
decided ;; Deciding || prove ;; Know || investigate ;; Scrutiny
realised ;; Coming_to_believe || outrun ;; Escaping
engaged ;; Hostile_encounter || escape ;; Escaping || battle ;; Hostile_encounter || brought ;; Bringing || run ;; Motion
losing ;; Earnings_and_losses || surrendering ;; Surrendering || engagements ;; Hostile_encounter || bloody ;; Bodily_harm || lasted ;; Being_in_operation
battle ;; Hostile_encounter || campaign ;; Military_operation || campaign ;; Military_operation || Battle ;; Hostile_encounter || marked ;; Recording
returned ;; Arriving || destroyed ;; Destroying
encountered ;; Hostile_encounter || fought ;; Hostile_encounter
refused ;; Agree_or_refuse_to_act || employ ;; Employment || campaign ;; Military_operation || criticised ;; Judgment_communication
battle ;; Hostile_encounter || battle ;; Hostile_encounter || battle ;; Hostile_encounter || called ;; Request || defeated ;; Conquering
fought ;; Hostile_encounter
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || shattered ;; Destroying || defeated ;; Conquering
battle ;; Hostile_encounter || mentioned ;; Statement || housed ;; Containing
killed ;; Killing || exhausted ;; Expend_resource || returned ;; Arriving
present ;; Presence || mentions ;; Statement
dealing ;; Resolve_problem || rebellions ;; Change_of_leadership
occurred ;; Coming_to_be
passes ;; Motion_directional
use ;; Using || met ;; Come_together
saw ;; Motion || managed ;; Control
killed ;; Killing || injured ;; Bodily_harm || injured ;; Bodily_harm
named ;; Name_conferral || raise ;; Cause_change_of_position_on_a_scale || drive ;; Motion
injured ;; Bodily_harm || arrived ;; Arriving || use ;; Using || bringing ;; Bringing || dead ;; Death || clear ;; Emptying
prevent ;; Preventing_or_letting || escort ;; Patrolling
attached ;; Connect || used ;; Using
approaching ;; Arriving || let ;; Preventing_or_letting
campaign ;; Social_event || given ;; Giving || defensive ;; Defending || fought ;; Hostile_encounter
prevent ;; Preventing_or_letting || aim ;; Aiming || destruction ;; Destroying
bombing ;; Attack || battles ;; Hostile_encounter || war ;; Hostile_encounter || involved ;; Participation || counter ;; Hostile_encounter
Battle ;; Hostile_encounter || warfare ;; Hostile_encounter
defended ;; Defending || attack ;; Attack || command ;; Causation
defeats ;; Conquering || inflict ;; Causation
flying ;; Motion || forced ;; Request || operate ;; Military_operation || due ;; Causation || losses ;; Earnings_and_losses
suffered ;; Bodily_harm || called ;; Request
enabled ;; Preventing_or_letting || stream ;; Motion || loss ;; Earnings_and_losses || mount ;; Self_motion || built ;; Creating || introduced ;; Choosing || attacks ;; Attack
targets ;; Aiming || introduced ;; Choosing || escorting ;; Patrolling
gained ;; Getting
raids ;; Attack || bombing ;; Attack || production ;; Manufacturing || brought ;; Causation || damaged ;; Damaging || halt ;; Process_end || dropped ;; Cause_change_of_position_on_a_scale
production ;; Manufacturing || attacks ;; Attack || bring ;; Bringing
produced ;; Manufacturing
dropped ;; Cause_change_of_position_on_a_scale
flying ;; Motion || training ;; Education_teaching || fighting ;; Hostile_encounter || decreased ;; Cause_change_of_position_on_a_scale
destroyed ;; Destroying || destroyed ;; Destroying || claimed ;; Statement || claimed ;; Statement || claimed ;; Statement
dropped ;; Motion_directional || dropped ;; Motion_directional || dropped ;; Motion_directional
damages ;; Damaging || desperate ;; Change_sentiment || forced ;; Request || use ;; Using || halt ;; Process_end
attack ;; Attack || halt ;; Process_end || grew ;; Cause_change_of_position_on_a_scale || brought ;; Bringing || production ;; Manufacturing
bombing ;; Attack || bombing ;; Attack || meeting ;; Social_event || planned ;; Arranging || result ;; Causation || noted ;; Statement || began ;; Process_start || produced ;; Manufacturing
bombing ;; Attack || fighting ;; Hostile_encounter || assign ;; Request || switch ;; Change || combat ;; Hostile_encounter
added ;; Cause_to_be_included || caused ;; Causation || bombing ;; Attack || attacks ;; Attack || destruction ;; Destroying || collapse ;; Destroying
became ;; Becoming || reached ;; Arriving || fused ;; Connect
ended ;; Process_end || bombing ;; Attack || continued ;; Motion
captured ;; Conquering || preceded ;; Process_start || cut off ;; Hindering
improvised ;; Hold || dislodge ;; Removing || regain ;; Recovering || made ;; Creating
arrived in ;; Arriving || diverted ;; Removing
joined ;; Becoming_a_member || training ;; Education_teaching || organised ;; Arranging
sent ;; Sending || build ;; Building || make ;; Creating || eliminate ;; Destroying
recaptured ;; Conquering || reinforced ;; Cause_change_of_strength || lost ;; Earnings_and_losses || running ;; Self_motion
began ;; Process_start || arrived ;; Arriving || pushed ;; Hold
entered ;; Arriving || cutting off ;; Removing
decided ;; Deciding || make ;; Manufacturing || guard ;; Patrolling
found ;; Know || advanced ;; Cause_to_make_progress || prepared ;; GetReady
retreated ;; Escaping || rescued ;; Rescuing
taken ;; Conquering
came to ;; Self_motion || evacuated ;; Escaping
embarked ;; Process_start
fought ;; Hostile_encounter || battle ;; Hostile_encounter
advancing ;; Self_motion || siege ;; Besieging || blocked ;; Hindering
fought ;; Quarreling || battles ;; Hostile_encounter || advance ;; Self_motion || halted ;; Preventing_or_letting
failed ;; Agree_or_refuse_to_act || reconnaissance ;; Patrolling || battle ;; Hostile_encounter
allowed ;; Preventing_or_letting || failed ;; Agree_or_refuse_to_act || bombardment ;; Attack || advance ;; Self_motion || survive ;; Preserving
destroyed ;; Destroying || suffered ;; Confronting_problem
attained ;; Achieve || advance ;; Self_motion || holding ;; Defending
became ;; Becoming || battle ;; Hostile_encounter || battles ;; Hostile_encounter || War ;; Military_operation
delayed ;; Hindering || brought ;; Bringing
appointed ;; Change_of_leadership || moved ;; Self_motion
lifted ;; Process_end || Siege ;; Besieging || surrender ;; Surrendering || Battle ;; Hostile_encounter
engagement ;; Hostile_encounter || fought ;; Hostile_encounter || campaign ;; Military_operation || Wars ;; Hostile_encounter
conducted ;; Action || operations ;; Military_operation || contest ;; Competition
surrounded ;; Surrounding || supplies ;; Supply || disruptive ;; Destroying || interference ;; Hindering
achieved ;; Achieve || Battle ;; Hostile_encounter || engagement ;; Hostile_encounter
escaping ;; Escaping || convoy ;; Patrolling || traveling ;; Traveling || resulted in ;; Causation
change ;; Change || planned ;; Arranging || suggested ;; Statement
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || formed ;; Coming_to_be || Battle ;; Hostile_encounter || fought ;; Hostile_encounter
captured ;; Conquering || captured ;; Conquering || Battle ;; Hostile_encounter || commanded ;; Control
deployed ;; Arranging || guarding ;; Patrolling || supply ;; Supply || ordered ;; Arranging || attack ;; Attack || capture ;; Conquering
fight ;; Hostile_encounter || ordered ;; Request
stretching ;; Expansion || retreat ;; Escaping || cover ;; Filling
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || delay ;; Change_event_time
retreat ;; Escaping || attack ;; Attack || forced ;; Motion || battles ;; Hostile_encounter || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter
ordered ;; Arranging || prepare ;; GetReady
attacked ;; Attack || entrenched ;; Hindering
began ;; Process_start || ended ;; Process_end || fighting ;; Hostile_encounter
captured ;; Conquering || fighting ;; Hostile_encounter
captured ;; Conquering || captured ;; Conquering || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || concluded ;; Process_end || opened ;; Process_start
captured ;; Conquering || signed ;; Sign_agreement || fighting ;; Hostile_encounter || progress ;; Cause_to_make_progress
turned ;; Becoming || killing ;; Killing
occurred ;; Coming_to_be
war ;; Hostile_encounter || crime ;; Committing_crime
negotiated ;; Communication || monitor ;; Scrutiny
refused ;; Agree_or_refuse_to_act || removed ;; Removing
fought ;; Quarreling || transported ;; Sending || consisting ;; Cause_to_be_included
identified ;; Check || removed ;; Removing || transported ;; Sending || returned ;; Arriving
beaten ;; Bodily_harm || pulled ;; Motion || leaving ;; Departing
shot ;; Use_firearm || buried ;; Placing || taken ;; Conquering
discovered ;; Know || deployed ;; Arranging || guarded ;; Patrolling
buried ;; Placing || believe ;; Know
convicted ;; Legal_rulings
completed ;; Process_end
convicted ;; Legal_rulings
ruled ;; Control || committed ;; Committing_crime
marked ;; Recording || used ;; Using || rebuilt ;; Building || hold ;; Hold
visited ;; Traveling
saw ;; Self_motion || fighting ;; Hostile_encounter
indicated ;; Statement || deployed ;; Arranging || reports ;; Reporting || response ;; Response
established ;; Building || support ;; Supporting || destroy ;; Destroying
clashed ;; Hostile_encounter || patrols ;; Patrolling
mounted ;; Connect || prevent ;; Preventing_or_letting || limiting ;; Limiting || withdrawal ;; Escaping || casualties ;; Catastrophe || attacks ;; Attack || disrupted ;; Hindering || attack ;; Attack || causing ;; Causation
set ;; Placing || deployment ;; Arranging
repelled ;; Defending || attacks ;; Attack
Operation ;; Military_operation || attacks ;; Attack
operation ;; Military_operation
escorted ;; Patrolling
provided ;; Supply || operations ;; Military_operation || command ;; Control
participated ;; Participation
command ;; Control
used ;; Using
deployments ;; Arranging || operated ;; Being_in_operation
operation ;; Military_operation || involved ;; Cause_to_be_included || working together ;; Collaboration
fought ;; Hostile_encounter || Wars ;; Hostile_encounter
disrupt ;; Hindering || intercepting ;; Know || capturing ;; Conquering || countered ;; Response
sighted ;; Know || approaching ;; Arriving || chase ;; Self_motion || gave ;; Giving || escorts ;; Patrolling
separated ;; Forming_relationships || following ;; Self_motion
escaped ;; Escaping || pursuit ;; Self_motion || attempting ;; Hold || drive ;; Motion
chase ;; Self_motion || defence ;; Defending || surrender ;; Surrendering || forced ;; Causation || casualties ;; Bodily_harm || came ;; Self_motion || suffering ;; Bodily_harm
surrendered ;; Surrendering || defending ;; Defending || praised ;; Judgment_communication
escaped ;; Escaping || engagement ;; Hostile_encounter || campaign ;; Military_operation || formed ;; Coming_to_be || developing ;; Coming_to_be || aftermath ;; Catastrophe || battle ;; Hostile_encounter || pursued ;; Self_motion
taken ;; Bringing
caused ;; Causation || hurricane ;; Catastrophe || flooding ;; Catastrophe
caused ;; Causation || attacks ;; Attack || canceled ;; Change_event_time || struck ;; Connect || cancellations ;; Change_event_time || developed ;; Cause_to_make_progress
moved ;; Self_motion
combination ;; Cause_to_amalgamate || left ;; Causation
caused ;; Causation || damage ;; Damaging
deaths ;; Death || death ;; Death || contributed ;; Causation || flooding ;; Catastrophe
crossing ;; Motion_directional || decreased ;; Cause_change_of_position_on_a_scale || cyclone ;; Catastrophe || appearance ;; Presence
became ;; Becoming || re-intensified ;; Cause_change_of_strength || passed ;; Motion_directional || hurricane ;; Catastrophe
weakened ;; Cause_change_of_strength || reached ;; Arriving
transitioned ;; Change || cyclone ;; Catastrophe || passed ;; Motion_directional
caused ;; Causation || record ;; Recording || described ;; Statement || flooding ;; Catastrophe
flooded ;; Destroying
dissipated ;; Dispersal
hurricane ;; Catastrophe || affected ;; Influence
affect ;; Influence
became ;; Becoming || originated ;; Coming_to_be
intensify ;; Cause_change_of_strength || upgraded ;; Cause_to_make_progress || moved ;; Self_motion
hurricane ;; Catastrophe || intensifying ;; Cause_change_of_strength || strengthened ;; Cause_change_of_strength || struck ;; Attack
damaged ;; Damaging || damaged ;; Damaging || leaving ;; Causation
entering ;; Arriving || weakened ;; Cause_change_of_strength || attaining ;; Achieve
hurricane ;; Catastrophe || reported ;; Reporting
hurricane ;; Catastrophe || landfall ;; Arriving
destroyed ;; Destroying || flooded ;; Destroying
hurricane ;; Catastrophe || affecting ;; Influence || struck ;; Attack
killed ;; Killing || left ;; Causation
send ;; Sending || prompting ;; Causation
hurricane ;; Catastrophe || weakened ;; Cause_change_of_strength || striking ;; Connect
evacuate ;; Removing
hurricane ;; Catastrophe || emerging ;; Coming_to_be || moved ;; Self_motion
stalled ;; Temporary_stay
killed ;; Killing || caused ;; Causation || left ;; Causation
damaged ;; Damaging || occurred ;; Presence
damaged ;; Damaging
killing ;; Killing
crashed ;; Catastrophe || carrying ;; Carry_goods || killing ;; Killing
caused ;; Causation || hurricane ;; Catastrophe || threatened ;; Warning
cut ;; Removing || landfall ;; Arriving || flooded ;; Destroying
hurricane ;; Catastrophe || destroyed ;; Destroying || left ;; Causation
occurred ;; Coming_to_be || disaster ;; Catastrophe || crashed ;; Damaging || take off ;; Departing
nicknamed ;; Name_conferral
died ;; Death
died ;; Death || injured ;; Bodily_harm || fatalities ;; Death || taken ;; Bringing || resulting in ;; Causation
advance ;; Cause_to_make_progress || competition ;; Competition || semi-finals ;; Competition || match ;; Competition || returning ;; Departing || eliminated ;; Removing
stopped ;; Process_end
take-off ;; Self_motion || surging ;; Motion || left ;; Departing || abandoned ;; GiveUp
rejected ;; Agree_or_refuse_to_act || get ;; Getting || schedule ;; Arranging || stay ;; Temporary_stay || take-off ;; Self_motion
causing ;; Causation || form ;; Coming_to_be || end ;; Process_end || falling ;; Motion_directional
torn ;; Destroying || end ;; Process_end || left ;; Departing || hit ;; Attack || ploughed ;; Motion || hitting ;; Attack
began ;; Process_start || helped ;; Assistance || evacuating ;; Removing || explode ;; Attack
blamed ;; Judgment_communication || statements ;; Statement || investigation ;; Criminal_investigation || saying ;; Statement
crash ;; Catastrophe || caused ;; Causation || established ;; Statement || slowed ;; Change_event_time || take off ;; Departing
incident ;; Catastrophe
become ;; Becoming || games ;; Competition || win ;; Earnings_and_losses
advanced ;; Cause_to_make_progress || semi-finals ;; Competition || held ;; Hold
matches ;; Competition || beaten ;; Competition
crash ;; Catastrophe || destroyed ;; Destroying || promised ;; Commitment || derailed ;; Hindering
rebuilding ;; Building || winning ;; Getting || took ;; Conquering || recover ;; Recovering
operated ;; Being_in_operation || occurred ;; Coming_to_be || crash ;; Damaging
crashed ;; Catastrophe || taking off ;; Departing
captained ;; Control
perished ;; Death || crash ;; Damaging || making it ;; Causation || crash ;; Damaging
verdict ;; Legal_rulings
due ;; Causation || found ;; Know
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || War ;; Hostile_encounter || fought ;; Hostile_encounter
capture ;; Conquering || took place ;; Process_start
siege ;; Besieging || referred ;; Name_conferral || Siege ;; Besieging
organized ;; Arranging || operations ;; Military_operation
reached ;; Arriving || Delayed ;; Change_event_time
decided ;; Deciding || operations ;; Military_operation
called ;; Name_conferral || commanded ;; Control || bombardment ;; Attack || assault ;; Attack || Arriving ;; Arriving || constructed ;; Building || preparation ;; GetReady || landed ;; Arriving
war ;; Hostile_encounter || commanding ;; Control
conducted ;; Action || Operations ;; Military_operation || support ;; Supporting || landings ;; Self_motion
Operation ;; Military_operation || surrounding ;; Surrounding || invasion ;; Attack || operations ;; Military_operation || formed ;; Coming_to_be
approaching ;; Self_motion
belief ;; Coming_to_believe || Allied ;; Cause_to_amalgamate || land ;; Arriving || amplified ;; Cause_change_of_position_on_a_scale
positioned ;; Placing || confuse ;; Influence || landings ;; Arriving
operations ;; Military_operation || complemented ;; Cause_to_amalgamate || Operation ;; Military_operation || confuse ;; Influence
operations ;; Military_operation || response ;; Response
plan ;; Arranging || contributed ;; Causation
siege ;; Besieging || fought ;; Hostile_encounter
besieged ;; Besieging || held ;; Defending
asked ;; Request || contacted ;; Connect || driven ;; Self_motion || control ;; Control || preventing ;; Preventing_or_letting || delivery ;; Sending || assistance ;; Assistance
opened ;; Openness || slaughtered ;; Killing || arrived ;; Arriving || killing ;; Killing
massacred ;; Killing || freeing ;; Releasing
destroyed ;; Destroying || control ;; Control
crashed ;; Catastrophe
operational ;; Being_in_operation || take-off ;; Departing || cancellation ;; Change_event_time || took off ;; Departing || delayed ;; Change_event_time
final ;; Process_end || land ;; Arriving
approach ;; Arriving || crashed ;; Catastrophe || killing ;; Killing
injured ;; Bodily_harm
determined ;; Deciding || cause ;; Causation || accident ;; Catastrophe || judged ;; Criminal_investigation
accident ;; Catastrophe || disaster ;; Catastrophe || Flight ;; Self_motion
accident ;; Catastrophe
War ;; Hostile_encounter || conflict ;; Hostile_encounter || took place ;; Process_start
impact ;; Influence || brought about ;; Causation
placed ;; Placing || touched ;; Connect || forcing ;; Request
response ;; Response || set up ;; Reforming_a_system
overthrew ;; Change_of_leadership
began ;; Process_start
occurred ;; Coming_to_be || issued ;; Creating
touched ;; Connect
spread ;; Dispersal
overthrew ;; Change_of_leadership || formed ;; Coming_to_be || forged ;; Cause_to_amalgamate || maintain ;; Preserving
collapse ;; Destroying || ended ;; Process_end
inspired ;; Influence
begun ;; Process_start || educated ;; Education_teaching
caused ;; Causation
influenced ;; Influence
abdicate ;; Change_of_leadership || forced ;; Request || made ;; Manufacturing
claimed ;; Statement || supported ;; Supporting
jailed ;; Arrest || led to ;; Causation || suppressed ;; Hindering
met ;; Motion || raise ;; Cause_change_of_position_on_a_scale
discovered ;; Know || chose ;; Choosing
action ;; Response || called ;; Request
imprisoned ;; Arrest || executed ;; Killing
disputed ;; Quarreling || recognizing ;; Know
took ;; Conquering || rejecting ;; Agree_or_refuse_to_act
legal ;; Legality || form ;; Coming_to_be || called ;; Request
supported ;; Supporting || resulted in ;; Causation
decided ;; Deciding || support ;; Supporting || affected ;; Influence
met ;; Come_together
proclaimed ;; Statement || called ;; Request
supported ;; Supporting
called ;; Request
achieved ;; Achieve
organized ;; Arranging
separation ;; Forming_relationships || changed ;; Change || due ;; Causation || conflicts ;; Hostile_encounter
recognized ;; Know
impact ;; Influence || strike ;; Connect
storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || originated ;; Coming_to_be
caused ;; Causation || tracked ;; Self_motion
intensify ;; Cause_change_of_strength || allowed ;; Preventing_or_letting
landfall ;; Arriving || emerging ;; Coming_to_be
began ;; Process_start || strengthen ;; Cause_change_of_strength || becoming ;; Becoming || reaching ;; Arriving
hurricane ;; Catastrophe || came ;; Self_motion || resulted in ;; Causation
exiting ;; Departing || weakening ;; Cause_change_of_strength || ensued ;; Process_start || moved ;; Self_motion || transitioned ;; Change
prompted ;; Causation
caused ;; Causation || killing ;; Killing || leading to ;; Causation
resulted in ;; Causation
hurricane ;; Catastrophe || Damage ;; Damaging || making it ;; Causation || strike ;; Connect
warnings ;; Warning || arrival ;; Arriving || put ;; Placing
declared ;; Expressing_publicly || impacts ;; Influence || cancelled ;; Change_event_time || evacuated ;; Removing
opened ;; Openness
became ;; Becoming || hurricane ;; Catastrophe || struck ;; Connect || make ;; Self_motion
causing ;; Causation || destroyed ;; Destroying
contributed ;; Giving
led to ;; Causation
resulted in ;; Causation
occurred ;; Coming_to_be
attack ;; Attack || hired ;; Employment
captured ;; Arrest
demanded ;; Request || stopped ;; Process_end
declare ;; Expressing_publicly
negotiations ;; Communication
ended ;; Process_end || paid ;; Commerce_pay || negotiating ;; Communication
negotiation ;; Communication || compensated ;; Rewards_and_punishments
implemented ;; Action || opting ;; Choosing
started ;; Process_start
hosted ;; Come_together || Fair ;; Social_event || festival ;; Social_event || centering ;; Placing
fair ;; Social_event || celebrated ;; Social_event
visits ;; Traveling || tallied ;; Recording
fair ;; Social_event || fair ;; Social_event || decline ;; Cause_change_of_position_on_a_scale || marked ;; Recording
fair ;; Social_event || held ;; Hold
Fair ;; Social_event || held ;; Hold || admission ;; Arriving || charged ;; Commerce_pay
formed ;; Coming_to_be || Tour ;; Traveling || tour ;; Traveling || concert ;; Social_event
tour ;; Traveling || visited ;; Traveling
added ;; Cause_to_be_included
earning ;; Earnings_and_losses || tour ;; Traveling || Tour ;; Traveling || ranked ;; Placing
placed ;; Placing || earning ;; Earnings_and_losses || tours ;; Traveling || tour ;; Traveling || shows ;; Social_event
pay ;; Commerce_pay || concert ;; Social_event || select ;; Choosing || view ;; Change_sentiment
bushfires ;; Catastrophe || bushfires ;; Catastrophe || known ;; Know || occurred ;; Coming_to_be
caused ;; Causation || destruction ;; Destroying || fanned ;; Motion || fires ;; Catastrophe
combined ;; Cause_to_amalgamate || create ;; Creating || fire ;; Catastrophe || drought ;; Catastrophe
became ;; Becoming || bushfire ;; Catastrophe || bushfires ;; Catastrophe || fires ;; Catastrophe
died ;; Death
deaths ;; Death
included ;; Cause_to_be_included || died ;; Death
change ;; Change || changed ;; Change || caused ;; Causation || fatalities ;; Death || fire ;; Catastrophe
aided ;; Assistance || impossible ;; Confronting_problem || suppression ;; Hindering || fuels ;; Supply || immersed ;; Placing || made ;; Causation
fires ;; Hindering || supplies ;; Supply || escape ;; Escaping || fended ;; Defending || communications ;; Communication || severed ;; Destroying || cut off ;; Having_or_lacking_access
declared ;; Expressing_publicly || evacuated ;; Emptying
burned ;; Bodily_harm
disasters ;; Catastrophe
destroyed ;; Destroying || damaged ;; Damaging || lost ;; Earnings_and_losses
losses ;; Earnings_and_losses || dead ;; Death || destroyed ;; Destroying
paid ;; Commerce_pay || cost ;; Cost || claims ;; Statement || adjusted ;; Cause_change_of_position_on_a_scale
called ;; Request || relief ;; Assistance || support ;; Supporting
defense ;; Defending || arising ;; Coming_to_be || storm ;; Catastrophe || combat ;; Hostile_encounter || response ;; Response || invasion ;; Attack
known ;; Know || became ;; Becoming
earned ;; Earnings_and_losses || nickname ;; Name_conferral
brought ;; Bringing || invaded ;; Attack || occupied ;; Control || met with ;; Come_together
deployed ;; Arranging || resisted ;; Defending || urged ;; Convincing || send ;; Sending
joined ;; Becoming_a_member || forming ;; Coming_to_be
order ;; Arranging
paid ;; Commerce_pay || cost ;; Cost
battle ;; Hostile_encounter || marked ;; Recording
began ;; Process_start || conflict ;; Hostile_encounter
assault ;; Attack
advanced ;; Cause_to_make_progress
ceased ;; Process_end || declared ;; Expressing_publicly || started ;; Process_start
combat ;; Hostile_encounter
launched ;; Military_operation
conducted ;; Action || resulted in ;; Causation
withdraw ;; Escaping || led to ;; Causation
resulted in ;; Causation
attack ;; Attack || indicated ;; Statement
pay ;; Commerce_pay
started ;; Process_start || fell ;; Motion_directional
announced ;; Expressing_publicly || rule ;; Control || set up ;; Reforming_a_system
Battle ;; Hostile_encounter || called ;; Name_conferral || took place ;; Process_start
besieged ;; Besieging || forces ;; Military_operation
rebelled ;; Hostile_encounter || destroyed ;; Destroying
raided ;; Attack || forces ;; Military_operation
rebelled ;; Hostile_encounter
began ;; Process_start || surrendered ;; Surrendering
pay ;; Commerce_pay || killed ;; Killing
began ;; Process_start
murder ;; Killing || ordered ;; Request || burn ;; Destroying
record ;; Recording || hurricane ;; Catastrophe
storm ;; Catastrophe || hurricane ;; Catastrophe || developed ;; Coming_to_be
upgraded ;; Cause_to_make_progress
curved ;; Motion_directional || strengthen ;; Cause_change_of_strength || continued ;; Process_start
became ;; Becoming
reached ;; Arriving
slowed ;; Change_event_time || became ;; Becoming
curving ;; Motion_directional || weakened ;; Cause_change_of_strength
re-strengthened ;; Cause_change_of_strength
became ;; Becoming || Approaching ;; Arriving || making ;; Manufacturing
weakened ;; Cause_change_of_strength
slowed ;; Change_event_time || storm ;; Catastrophe
turned ;; Change || drifted ;; Self_motion
re-emerged ;; Coming_to_be || transitioned ;; Change
moved ;; Motion || dissipated ;; Removing
reached ;; Arriving
lashed ;; Bodily_harm || observed ;; Perception_active
left ;; Departing
damage ;; Damaging || occurred ;; Coming_to_be
flooding ;; Motion
damaged ;; Damaging || inundated ;; Destroying
received ;; Receiving || destroyed ;; Destroying || impact ;; Influence || damage ;; Damaging || suffered ;; Bodily_harm || flattened ;; Damaging
damaged ;; Damaging || demolished ;; Destroying
occurred ;; Coming_to_be || deaths ;; Death || damage ;; Damaging
damaged ;; Damaging || obliterated ;; Destroying
destroyed ;; Destroying || damage ;; Damaging || suffered ;; Bodily_harm
death ;; Death || damage ;; Damaging
storm ;; Catastrophe || impact ;; Influence || affected ;; Influence
death ;; Death || occurred ;; Coming_to_be
caused ;; Causation || damage ;; Damaging || deaths ;; Death
prolonged ;; Change_event_time || war ;; Hostile_encounter || unrest ;; Hostile_encounter
took ;; Conquering || violence ;; Violence || perished ;; Death || war ;; Hostile_encounter || surpassed ;; Cause_change_of_position_on_a_scale
took place ;; Hold || conflict ;; Hostile_encounter
involved ;; Cause_to_be_included || armed ;; Bearing_arms || struggle ;; Hostile_encounter || forces ;; Hostile_encounter || claimed ;; Statement
provided ;; Supply || assistance ;; Assistance || supporting ;; Supporting || supporting ;; Supporting
associated ;; Collaboration || open ;; Openness || avoid ;; Preventing_or_letting
maintain ;; Preserving || put ;; Placing
began ;; Process_start
hardened ;; Cause_change_of_strength
involved ;; Cause_to_be_included || massacres ;; Killing || killed ;; Killing || control ;; Control || resulting in ;; Causation
crowned ;; Change_of_leadership || converted ;; Change || conflict ;; Hostile_encounter
granted ;; Giving
wars ;; Hostile_encounter || threatened ;; Warning
changed ;; Change
revoked ;; Removing
gained ;; Getting || leave ;; Departing || earning ;; Earnings_and_losses || selection ;; Choosing
broke out ;; Process_start
erupted ;; Process_start || started ;; Process_start
conducted ;; Action
determine ;; Deciding || determine ;; Deciding || reproduced ;; Creating
determined ;; Deciding || started ;; Process_start
increased ;; Cause_change_of_position_on_a_scale || prolonged ;; Change_event_time || seemed ;; Change_sentiment || shot ;; Use_firearm
discovered ;; Know || confirmed ;; Check
criticised ;; Judgment_communication
given ;; Giving
published ;; Publishing
competition ;; Competition || open ;; Openness
Open ;; Openness || contested ;; Competition
shots ;; Motion
winning ;; Getting || gained ;; Getting
took place ;; Hold
given ;; Giving || read ;; Statement || open ;; Openness || competed ;; Competition
record ;; Recording
gave ;; Giving
held ;; Hold
extended ;; Change_event_time || final ;; Process_end
score ;; Getting || record ;; Recording
returned ;; Arriving
returned ;; Arriving
meet ;; Come_together || carried ;; Sending
Battle ;; Hostile_encounter || engagement ;; Hostile_encounter || fleet ;; Military_operation || fleet ;; Military_operation || arranged ;; Arranging || took place ;; Process_start
met ;; Come_together || fleet ;; Military_operation || forces ;; Military_operation || sailing ;; Motion || sailing ;; Motion
financed ;; Supply
engagement ;; Hostile_encounter || fought ;; Hostile_encounter || warfare ;; Hostile_encounter
battle ;; Hostile_encounter || battle ;; Hostile_encounter
battle ;; Hostile_encounter
beginning ;; Process_start || battle ;; Hostile_encounter || marking ;; Recording
expansion ;; Expansion || military ;; Military_operation || victory ;; Getting || marking ;; Recording || wars ;; Military_operation
Battle ;; Hostile_encounter || expansion ;; Expansion || defense ;; Defending
torn ;; Destroying || Reformation ;; Change_of_leadership || wars ;; Military_operation
strengthen ;; Cause_change_of_strength || Victory ;; Getting || used ;; Using || instituted ;; Coming_to_be
military ;; Military_operation
caused ;; Causation
exemplified ;; Change_event_time || deterioration ;; Damaging || setback ;; Hindering || defeat ;; Conquering
battle ;; Hostile_encounter || heartened ;; Recovering || tarnished ;; Destroying
strike ;; Attack || record ;; Recording || record ;; Recording || strike ;; Attack
identified ;; Check || storm ;; Catastrophe || originated ;; Coming_to_be
moved ;; Motion
become ;; Becoming || storm ;; Catastrophe || issue ;; Statement
passed ;; Motion_directional || storm ;; Catastrophe || hurricane ;; Catastrophe || resulting in ;; Causation || killed ;; Killing
became ;; Becoming || storm ;; Catastrophe || tracked ;; Self_motion
became ;; Becoming || storm ;; Catastrophe
intensified ;; Cause_change_of_strength || reached ;; Arriving
slowed ;; Change_event_time || weakened ;; Cause_change_of_strength
influenced ;; Influence || became ;; Becoming || causing ;; Causation || accelerate ;; Cause_change_of_position_on_a_scale
passed ;; Motion_directional || hurricane ;; Catastrophe
maintained ;; Preserving || transitioned ;; Change
took place ;; Process_start || deepened ;; Cause_change_of_strength
become ;; Becoming || confirmed ;; Check
turned ;; Becoming || storm ;; Catastrophe || dissipating ;; Dispersal
brought ;; Bringing || Striking ;; Attack || storm ;; Catastrophe || record ;; Recording
caused ;; Causation || damage ;; Damaging || downing ;; Body_movement
damage ;; Damaging || destroyed ;; Destroying
losses ;; Earnings_and_losses || experienced ;; Earnings_and_losses
killed ;; Killing
caused ;; Causation || damage ;; Damaging
battered ;; Damaging
storm ;; Catastrophe
originated ;; Coming_to_be
intensified ;; Cause_change_of_strength
attained ;; Getting || entered ;; Arriving
slowed ;; Change_event_time || attained ;; Getting
began ;; Process_start || maintained ;; Preserving
re-intensified ;; Cause_change_of_strength || approached ;; Arriving || weaken ;; Cause_change_of_strength || entering ;; Arriving
struck ;; Attack || dissipated ;; Dispersal
damage ;; Damaging
suffered ;; Bodily_harm || effects ;; Influence
damaged ;; Damaging
destroyed ;; Destroying
forcing ;; Hostile_encounter || flee ;; Departing || flooded ;; Catastrophe
damaged ;; Damaging || leveled ;; Destroying
hurt ;; Bodily_harm
passed ;; Motion_directional || tracking ;; Recording
suffered ;; Bodily_harm || injuries ;; Bodily_harm || occurred ;; Presence
destroyed ;; Destroying
deaths ;; Death || damage ;; Damaging
struck ;; Attack || storm ;; Catastrophe
lasted ;; Process_start || spanning ;; Change_event_time
deaths ;; Death || reported ;; Reporting
died ;; Death
injuries ;; Bodily_harm || deaths ;; Death || reported ;; Reporting
deaths ;; Death || destroyed ;; Destroying || crushed ;; Bodily_harm
destroyed ;; Destroying || damaged ;; Damaging
damaged ;; Damaging || storm ;; Catastrophe
storm ;; Catastrophe || left ;; GiveUp || affected ;; Influence
sanctioned ;; Arranging || Championships ;; Competition || Championships ;; Competition
participated ;; Participation || competition ;; Competition
competition ;; Competition || competition ;; Competition
tournament ;; Competition || took place ;; Process_start || played ;; Competition
playing ;; Competition
played ;; Competition
became ;; Becoming || title ;; Name_conferral
Championships ;; Competition
championships ;; Competition
awarded ;; Giving || captured ;; Getting || title ;; Name_conferral
began ;; Process_start || inherited ;; Giving || change ;; Change || tournament ;; Competition || tournaments ;; Competition
promote ;; Cause_change_of_position_on_a_scale || contested ;; Participation || expand ;; Expansion || beginning ;; Process_start
battle ;; Competition
advanced ;; Cause_to_make_progress || defeating ;; Conquering
defeated ;; Conquering || involved ;; Participation
tied ;; Connect || ensuring ;; Preventing_or_letting || tied ;; Connect
got ;; Getting || playing ;; Competition
pulled ;; Motion || claiming ;; Statement || win ;; Getting || score ;; Getting
change ;; Change || finishes ;; Process_end || tournament ;; Competition || hastened ;; Change_event_time
given ;; Giving
capture ;; Getting || scored ;; Getting || held on to ;; Defending
held ;; Hold || tournament ;; Competition
held ;; Hold || held ;; Hold || moved ;; Motion
winning ;; Getting || qualifying ;; Becoming_a_member || defeated ;; Hostile_encounter
ended ;; Process_end || abandoned ;; GiveUp || impacted ;; Influence || shortened ;; Change_event_time || Played ;; Competition
led ;; Bringing
withdrawal ;; Escaping || attacks ;; Attack || hit ;; Attack || insurgency ;; Military_operation || assault ;; Attack || end ;; Process_end || bombings ;; Use_firearm
spread ;; Dispersal || killed ;; Killing || attacks ;; Attack || wounded ;; Bodily_harm
attack ;; Attack || dead ;; Death || injured ;; Bodily_harm || left ;; Bringing || deadliest ;; Death || took place ;; Process_start || bombings ;; Attack
parked ;; Placing || killing ;; Killing || exploded ;; Attack || wounded ;; Bodily_harm
suicide ;; Killing || killed ;; Killing || rocked ;; Motion || attacks ;; Attack || attack ;; Attack || explosions ;; Attack || blast ;; Attack || wounded ;; Bodily_harm
bombings ;; Use_firearm || shootings ;; Use_firearm || took place ;; Process_start
killed ;; Killing || killed ;; Killing || car bomb ;; Terrorism || injured ;; Bodily_harm || shot ;; Use_firearm
discovered ;; Know || disarmed ;; Emptying
took place ;; Process_start || running ;; Process_start
race ;; Competition
race ;; Competition || televised ;; Communication
race ;; Competition || considered ;; Coming_to_believe || seen ;; Perception_active
race ;; Competition || finished ;; Process_end || ridden ;; Motion
race ;; Competition || track ;; Motion
cyclone ;; Catastrophe || landfall ;; Catastrophe
developed ;; Coming_to_be
strengthened ;; Cause_change_of_strength || moving ;; Motion
curved ;; Motion_directional || deepen ;; Cause_change_of_strength
landfall ;; Catastrophe || turned ;; Motion || made ;; Creating
occurred ;; Coming_to_be
destruction ;; Destroying || reported ;; Reporting || observed ;; Perception_active
destroyed ;; Destroying
damage ;; Damaging || occurred ;; Coming_to_be || deaths ;; Death
emerging ;; Coming_to_be || striking ;; Attack
occurred ;; Coming_to_be
reported ;; Reporting || damage ;; Damaging || winds ;; Motion
storm ;; Catastrophe
demolished ;; Destroying || impact ;; Influence || damaged ;; Damaging || destroyed ;; Destroying || suffered ;; Bodily_harm
damage ;; Damaging || inflicted ;; Causation || fatalities ;; Killing
weakened ;; Cause_change_of_strength || emerged ;; Coming_to_be || crossing ;; Motion_directional || storm ;; Catastrophe
became ;; Becoming || occurred ;; Coming_to_be
sent ;; Sending || fought ;; Hostile_encounter || protect ;; Defending || augmented ;; Cause_change_of_strength
expansion ;; Expansion || reacted ;; Response || developed ;; Coming_to_be
spread ;; Dispersal
joined ;; Becoming_a_member || began ;; Process_start || losses ;; Earnings_and_losses
deployed ;; Arranging || maintained ;; Statement || armed ;; Bearing_arms || raised ;; Cause_change_of_position_on_a_scale || drive ;; Self_motion
beaten ;; Bodily_harm || arrival ;; Arriving || capturing ;; Conquering || attack ;; Attack || success ;; Achieve
ended ;; Process_end || fought ;; Hostile_encounter || receiving ;; Receiving
attacked ;; Attack || capturing ;; Conquering || returning ;; Arriving || turned ;; Motion
ambush ;; Attack || took place ;; Process_start
dead ;; Death || ambush ;; Attack || shot ;; Use_firearm
gunmen ;; Bearing_arms || taken ;; Conquering || conference ;; Communication || returning ;; Arriving || flagged ;; Action || fired ;; Use_firearm
suffering ;; Bodily_harm || killed ;; Killing || wounds ;; Bodily_harm || left ;; Departing || waving ;; Motion || shot ;; Use_firearm
killed ;; Killing
charged ;; Judgment_communication || killing ;; Killing
attack ;; Attack || result ;; Causation
published ;; Publishing || killings ;; Killing || judge ;; Legal_rulings || findings ;; Know || report ;; Reporting || investigated ;; Criminal_investigation
published ;; Publishing || led to ;; Causation || inquiry ;; Criminal_investigation || killings ;; Killing || report ;; Reporting || setting up ;; Process_start || final ;; Process_end || opened ;; Process_start
stated ;; Statement || passed ;; Giving || report ;; Reporting
caused ;; Causation || Hurricane ;; Catastrophe || damage ;; Damaging
emerged ;; Coming_to_be
Traversing ;; Motion_directional || consolidated ;; Cause_to_amalgamate
acquired ;; Getting || storm ;; Catastrophe || hurricane ;; Catastrophe || Remaining ;; Presence || strengthened ;; Cause_change_of_strength
reached ;; Arriving
hurricane ;; Catastrophe || turned ;; Motion || accelerated ;; Cause_change_of_position_on_a_scale
made ;; Manufacturing || hurricane ;; Catastrophe || crossing ;; Motion_directional || storm ;; Catastrophe || landfall ;; Damaging || hitting ;; Attack
transitioned ;; Change || moving ;; Motion
turned ;; Motion || dissipated ;; Dispersal
issued ;; Creating || warnings ;; Warning || hurricane ;; Catastrophe || Threatening ;; Warning
evacuated ;; Removing
attributed ;; Causation || impacts ;; Influence || flooding ;; Motion
due ;; Causation || lost ;; Earnings_and_losses
hurricane ;; Catastrophe || damage ;; Damaging || lost ;; Death || reached ;; Earnings_and_losses
declared ;; Expressing_publicly || disaster ;; Catastrophe
damage ;; Damaging || took place ;; Process_start
caused ;; Causation || hurricane ;; Catastrophe || flooding ;; Catastrophe
hurricane ;; Catastrophe || cyclone ;; Catastrophe || formed ;; Coming_to_be
disturbance ;; Self_motion || gaining ;; Getting || moved ;; Motion
storm ;; Catastrophe || depression ;; Catastrophe || attained ;; Cause_change_of_position_on_a_scale
storm ;; Catastrophe || cyclone ;; Catastrophe || reported ;; Reporting || suggested ;; Convincing
storm ;; Catastrophe || hurricane ;; Catastrophe || reaching ;; Cause_change_of_position_on_a_scale || strengthened ;; Cause_change_of_position_on_a_scale
hurricane ;; Catastrophe || moved ;; Motion
storm ;; Catastrophe || storm ;; Catastrophe || degenerating ;; Change || curve ;; Motion || weakened ;; Cause_change_of_strength || dissipating ;; Change
storm ;; Catastrophe || forced ;; Causation || Reports ;; Statement || hurricane ;; Catastrophe || evacuate ;; Escaping || moving ;; Motion
generated ;; Creating || storm ;; Catastrophe || hurricane ;; Catastrophe
flooded ;; Catastrophe
winds ;; Motion || damage ;; Damaging || disrupted ;; Damaging || caused ;; Causation
produced ;; Creating || hurricane ;; Catastrophe || damage ;; Damaging || setting off ;; Causation || floods ;; Catastrophe
cyclone ;; Catastrophe
hurricane ;; Catastrophe || recorded ;; Recording
damaged ;; Damaging
altered ;; Change
damages ;; Damaging || caused ;; Damaging || storm ;; Catastrophe || fatalities ;; Killing
hurricane ;; Catastrophe || hurricane ;; Catastrophe || strike ;; Attack || formed ;; Coming_to_be
noted ;; Know || hurricane ;; Catastrophe || intensifying ;; Cause_change_of_strength || moved ;; Motion
storm ;; Catastrophe
became ;; Becoming || moved up ;; Motion || hurricane ;; Catastrophe || struck ;; Attack || bringing ;; Bringing || weakened ;; Cause_change_of_strength
centered ;; Placing
moving ;; Motion || became ;; Becoming || cyclone ;; Catastrophe
formed ;; Coming_to_be
killed ;; Killing
started ;; Process_start || exited ;; Departing
turned ;; Becoming || predicted ;; Coming_to_believe
cancelled ;; Change_event_time || suspended ;; Process_end
evacuated ;; Emptying
caused ;; Causation || claimed ;; Statement
strengthened ;; Cause_change_of_strength
affected ;; Influence
poured ;; Motion
evacuated ;; Emptying || reported ;; Reporting
contributed ;; Giving || refers ;; Adducing
earthquake ;; Catastrophe
affected ;; Influence || originated ;; Coming_to_be
killed ;; Killing
caused ;; Causation
left ;; Departing
revolution ;; Change_of_leadership
described ;; Statement || distributed ;; Dispersal
provided ;; Supply
studied ;; Education_teaching
cited ;; Adducing
tour ;; Traveling
referred ;; Adducing || support ;; Supporting || launched ;; Process_start
visited ;; Traveling
earning ;; Earnings_and_losses || ranked ;; Placing
placed ;; Placing || earning ;; Earnings_and_losses
made it ;; Causation
refers ;; Adducing || take-off ;; Motion
arrived at ;; Arriving
Filled ;; Filling || departed ;; Departing
destroying ;; Destroying || moving ;; Motion
lost ;; Damaging
Leaving ;; Departing || planned ;; Arranging || gain ;; Getting || going ;; Self_motion || travel ;; Traveling
veered ;; Self_motion || planned ;; Arranging || takeoff ;; Motion
asked ;; Request || departure ;; Departing || setting ;; Placing || permission ;; Preventing_or_letting || takeoff ;; Motion
flight ;; Motion
crashed ;; Catastrophe
killed ;; Killing
killed ;; Killing || injured ;; Bodily_harm
determined ;; Deciding || cause ;; Causation || rising ;; Cause_change_of_position_on_a_scale || climb ;; Self_motion || deviated ;; Change || affected ;; Influence
hosted ;; Social_event
hosted ;; Social_event
awarded ;; Award || tournament ;; Competition
confirmed ;; Check || tournament ;; Competition
confirmed ;; Check || uses ;; Using
held ;; Hold || tournament ;; Competition || competition ;; Competition
advance ;; Cause_to_make_progress
match ;; Competition || abandoned ;; GiveUp || rain ;; Motion
moving ;; Motion || looked ;; Change_sentiment
confirmed ;; Check
issues ;; Creating || cost ;; Cost || cited ;; Adducing
wins ;; Getting || qualified ;; Becoming_a_member
beat ;; Competition || wins ;; Getting || qualified ;; Becoming_a_member
hosts ;; Social_event || progressed ;; Cause_to_make_progress || tournament ;; Competition
faced ;; Hostile_encounter || playing ;; Competition
beat ;; Competition || progress ;; Cause_to_make_progress || final ;; Competition || beat ;; Competition
final ;; Process_end || beating ;; Competition
said ;; Statement
proud ;; Perception_active || said ;; Statement
tournament ;; Competition || named ;; Name_conferral
arrested ;; Arrest
responding ;; Response
arrest ;; Arrest || initiated ;; Causation
occurred ;; Presence || arrest ;; Arrest || returned ;; Arriving
found ;; Know || help ;; Assistance || force ;; Motion
reported ;; Reporting
arrested ;; Arrest || differ ;; Change
charged ;; Judgment_communication
dropped ;; Removing
arrest ;; Arrest || generated ;; Creating || debate ;; Quarreling
said ;; Statement
arresting ;; Arrest || know ;; Know || proof ;; Protest || stopped ;; Preventing_or_letting
criticized ;; Judgment_communication
stated ;; Statement || become ;; Becoming
became ;; Becoming || discuss ;; Communication || joined ;; Becoming_a_member || meeting ;; Social_event || invited ;; Request
published ;; Publishing || states ;; Statement
respond ;; Response || explained ;; Statement
understand ;; Coming_to_believe || spoken ;; Statement
feel ;; Change_sentiment || cites ;; Adducing || depend ;; Influence
took place ;; Process_start
decided ;; Deciding || led ;; Conquering
fighting ;; Hostile_encounter || Revolution ;; Change_of_leadership || demands ;; Request
crisis ;; Catastrophe || reached ;; Arriving || abdication ;; Change_of_leadership
weakened ;; Cause_change_of_strength || giving ;; Giving
revolutionary ;; Change_of_leadership || death ;; Death || contributing ;; Giving || promoted ;; Cause_to_make_progress
became ;; Becoming || victims ;; Catastrophe || murder ;; Committing_crime
opposed ;; Agree_or_refuse_to_act || fighting ;; Hostile_encounter || giving ;; Giving || fight ;; Hostile_encounter || fulfilling ;; Achieve
strengthening ;; Cause_change_of_strength || restore ;; Recovering || referred ;; Adducing || gain ;; Getting
Starting ;; Process_start || pushing ;; Self_motion || attacked ;; Attack
involved ;; Cause_to_be_included
refused ;; Agree_or_refuse_to_act || halted ;; Process_end || mutinied ;; Change_of_leadership || fight ;; Hostile_encounter
collapsed ;; Destroying
meeting ;; Social_event || advancing ;; Cause_to_make_progress || counterattacked ;; Defending
broken ;; Destroying || retreated ;; Escaping
weakened ;; Cause_change_of_strength || became ;; Becoming
strengthening ;; Cause_change_of_strength || existed ;; Presence || proved ;; Coming_to_believe
command ;; Control || ordered ;; Arranging || count on ;; Coming_to_believe
helped ;; Assistance || start ;; Process_start
took place ;; Process_start || fight ;; Hostile_encounter
captured ;; Conquering || attacked ;; Attack
refused ;; Agree_or_refuse_to_act || fled ;; Escaping || defending ;; Defending || fight ;; Hostile_encounter
led to ;; Causation
referred ;; Adducing
seen ;; Perception_active
took place ;; Process_start
involved ;; Cause_to_be_included
culminated ;; Process_end
installed ;; Change_of_leadership
conflict ;; Hostile_encounter || disputed ;; Quarreling || conflict ;; Hostile_encounter || referred ;; Adducing
went into effect ;; Process_start
started ;; Process_start || capture ;; Conquering
control ;; Control
controls ;; Control
gained ;; Getting || operations ;; Military_operation
revolts ;; Change_of_leadership
conflict ;; Hostile_encounter || fought ;; Hostile_encounter || restore ;; Recovering || state ;; Statement || rebellions ;; Change_of_leadership || revolt ;; Change_of_leadership
use ;; Using || include ;; Cause_to_be_included || refer ;; Adducing || revolt ;; Change_of_leadership
turning ;; Becoming || scattered ;; Dispersal || impact ;; Influence
cited ;; Adducing
impact ;; Influence || destroyed ;; Destroying
limited ;; Limiting || establishing ;; Building || regained ;; Recovering || founding ;; Creating
carried out ;; Action
brought ;; Bringing || end ;; Process_end
ceded ;; Surrendering || resettled ;; Placing
named ;; Name_conferral
condemned ;; Judgment_communication || described ;; Statement
pointed out ;; Know || regroup ;; Cause_to_amalgamate
received ;; Receiving || took over ;; Conquering
received ;; Receiving
dubbed ;; Name_conferral || carried out ;; Action
coordinated ;; Arranging
conducted ;; Action || targeted ;; Aiming
deported ;; Removing || forced ;; Causation
deported ;; Removing || sent ;; Sending
received ;; Receiving
resigned ;; Process_end || succeeded ;; Change_of_leadership
described ;; Statement || view ;; Perception_active
withdrawn ;; Removing
said ;; Telling
divided ;; Change || noted ;; Statement
calls ;; Request
ignited ;; Process_start
delivered ;; Giving
referred ;; Adducing
decided ;; Deciding
stated ;; Statement
defected ;; Escaping
transmitted ;; Sending
caused ;; Causation
opened ;; Openness
began ;; Process_start
received ;; Receiving || covered ;; Filling
decide ;; Deciding || conference ;; Social_event || conference ;; Social_event || game ;; Social_event
defeated ;; Conquering
played ;; Competition || game ;; Social_event
watched ;; Perception_active
making ;; Manufacturing || record ;; Recording || appearance ;; Presence || posting ;; Sending
winning ;; Getting || made it ;; Causation || made ;; Creating
record ;; Recording || posting ;; Sending
consider ;; Statement || game ;; Social_event
ended up ;; Process_end || combined ;; Cause_to_amalgamate || score ;; Getting
combined ;; Cause_to_amalgamate || record ;; Recording || game ;; Social_event
combined ;; Cause_to_amalgamate || record ;; Recording
decided ;; Deciding || contest ;; Competition || made ;; Creating
named ;; Name_conferral
exposed ;; Reveal_secret || known ;; Know || game ;; Social_event || referred ;; Adducing
debate ;; Communication || perceived ;; Know || led to ;; Causation
fought ;; Hostile_encounter || conflict ;; Violence
began ;; Process_start
beginning ;; Process_start || invasion ;; Attack
resistance ;; Defending
resistance ;; Defending
fought ;; Hostile_encounter
merged ;; Cause_to_amalgamate || attack ;; Attack
start ;; Process_start || consider ;; Convincing
expand ;; Expansion
brought about ;; Causation
affecting ;; Influence || production ;; Manufacturing
brought about ;; Causation
rise ;; Cause_change_of_position_on_a_scale
helped ;; Assistance || invasion ;; Attack
cite ;; Adducing || beginning ;; Process_start || defeated ;; Conquering || created ;; Creating
adopted ;; Choosing
capturing ;; Conquering || scored ;; Getting
stop ;; Process_end || relocated ;; Removing
stretched ;; Expansion || reached ;; Achieve
defeat ;; Conquering
control ;; Control || ruled ;; Control
launched ;; Process_start || launched ;; Process_start
declared ;; Expressing_publicly || attacked ;; Attack
began ;; Process_start || defeat ;; Conquering || airlifting ;; Bringing
conquered ;; Conquering || invasion ;; Attack
surrender ;; Surrendering || failed ;; Earnings_and_losses
completed ;; Process_end || resumed ;; Recovering
retook ;; Conquering
surrendered ;; Surrendering || bombings ;; Attack || invasion ;; Attack
surrendered ;; Surrendering || convened ;; Come_together
decided ;; Deciding || punish ;; Rewards_and_punishments
became ;; Becoming || recognized ;; Know
directed ;; Aiming
orchestrated ;; Arranging
triggered ;; Causation || bombed ;; Attack
confessed ;; Reveal_secret || planted ;; Placing || arrested ;; Arrest
set off ;; Placing
assaulted ;; Attack
died ;; Death
harmed ;; Bodily_harm
declared ;; Expressing_publicly || remained ;; Presence || called in ;; Request || put ;; Placing
declined ;; Cause_change_of_position_on_a_scale
decreased ;; Cause_change_of_position_on_a_scale
attacks ;; Attack || started ;; Process_start
adduce ;; Adducing
met ;; Come_together || written ;; Writing
fought ;; Hostile_encounter
cooperated ;; Collaboration || started ;; Process_start
siege ;; Besieging || prevented ;; Preventing_or_letting || move ;; Motion
gave ;; Giving || allowing ;; Preventing_or_letting || reinforce ;; Cause_change_of_strength || end ;; Process_end
fighting ;; Hostile_encounter || defeat ;; Conquering
ended ;; Process_end || conference ;; Social_event || invited ;; Request
captured ;; Conquering || betrayed ;; Reveal_secret
cost ;; Cost
storm ;; Catastrophe || produced ;; Causation
formed ;; Coming_to_be
resulting in ;; Causation || moved up ;; Motion || broke ;; Destroying
caused ;; Causation || arrived ;; Arriving || damage ;; Damaging || damage ;; Damaging || storm ;; Catastrophe || Hurricane ;; Catastrophe || causing ;; Causation || outages ;; Damaging
increased ;; Cause_change_of_position_on_a_scale || uprooted ;; Removing || dumped ;; Bringing
caused ;; Causation || costs ;; Cost || damage ;; Damaging || storm ;; Catastrophe || collapsed ;; Destroying
impacting ;; Influence || storm ;; Catastrophe || outages ;; Damaging
postpone ;; Change_event_time || get ;; Getting || cancel ;; Process_end || outages ;; Damaging || chose ;; Choosing
Delays ;; Change_event_time || operating ;; Being_in_operation || resignation ;; Change_of_leadership || restoring ;; Recovering || led to ;; Causation
launched ;; Process_start
driven ;; Motion
decided ;; Deciding
began ;; Process_start
conquered ;; Conquering
achieved ;; Achieve || cost ;; Cost
Cyclone ;; Catastrophe || records ;; Recording || dropping ;; Motion_directional
developed ;; Coming_to_be || storm ;; Catastrophe
drifted ;; Motion || strengthening ;; Cause_change_of_strength || assigned ;; Choosing || storm ;; Catastrophe
deepened ;; Cause_change_of_strength || approached ;; Arriving || storm ;; Catastrophe || attained ;; Achieve
drifted ;; Motion || weakening ;; Cause_change_of_strength || making ;; Creating || dissipating ;; Cause_change_of_strength
dropping ;; Motion || inflicted ;; Causation || damage ;; Damaging || forcing ;; Causation || closure ;; Process_end
damage ;; Damaging || storm ;; Catastrophe || destruction ;; Destroying || suffered ;; Bodily_harm
tore ;; Destroying || smashed ;; Destroying || knocked out ;; Destroying || unroofed ;; Destroying || toppled ;; Destroying
flooding ;; Catastrophe || cleared ;; Removing
disrupted ;; Hindering || costing ;; Cost
restored ;; Recovering || involved ;; Cause_to_be_included || cleanup ;; Emptying || recovery ;; Rescuing
caused ;; Causation || damage ;; Damaging || cyclone ;; Catastrophe || deaths ;; Death
took place ;; Process_start || Concert ;; Social_event || concert ;; Social_event
devastated ;; Destroying || response ;; Response || concert ;; Social_event || Hurricane ;; Catastrophe || damage ;; Damaging || cost ;; Cost
concert ;; Social_event || broadcast ;; Expressing_publicly || released ;; Publishing
made ;; Manufacturing
collected ;; Come_together || benefit ;; Supply
performed ;; Participation
assault ;; Attack || combined ;; Cause_to_amalgamate
killing ;; Killing || capturing ;; Conquering || destroying ;; Destroying || aimed ;; Aiming || support ;; Supporting || launched ;; Process_start
associated ;; Collaboration
operation ;; Military_operation || involving ;; Participation || combined ;; Cause_to_amalgamate || converging ;; Come_together
bombing ;; Attack || preceded ;; Process_start
included ;; Cause_to_be_included
escaped ;; Escaping
limited ;; Limiting || planning ;; Arranging
caused ;; Causation || damage ;; Damaging || Hurricane ;; Catastrophe
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe || cyclone ;; Catastrophe || hurricane ;; Catastrophe
acquired ;; Getting || tracked ;; Motion
re-classified ;; Check || cyclone ;; Catastrophe
intensified ;; Cause_change_of_strength || storm ;; Catastrophe || transition ;; Change || upgraded ;; Cause_change_of_strength
strengthened ;; Cause_change_of_strength || curved ;; Motion_directional || storm ;; Catastrophe
became ;; Becoming || weakened ;; Cause_change_of_strength || hurricane ;; Catastrophe || storm ;; Catastrophe
re-intensified ;; Cause_change_of_strength || hurricane ;; Catastrophe || hurricane ;; Catastrophe
weakening ;; Cause_change_of_strength || made ;; Manufacturing || cyclone ;; Catastrophe || transitioning ;; Change
impacted ;; Influence || reported ;; Reporting
rescues ;; Rescuing || caused ;; Causation || drowning ;; Death || fatalities ;; Death
drowning ;; Death || occurred ;; Presence || deaths ;; Death
limited ;; Limiting || storm ;; Catastrophe || impact ;; Influence || occurred ;; Presence
caused ;; Causation || damage ;; Damaging || collapsed ;; Destroying
caused ;; Causation || flooding ;; Catastrophe
caused ;; Causation || damage ;; Damaging || fatalities ;; Death
began ;; Process_start || advanced ;; Self_motion
attacked ;; Attack || Battle ;; Hostile_encounter
began ;; Process_start || capture ;; Conquering || advanced ;; Self_motion
overrun ;; Conquering
became ;; Becoming || rise ;; Cause_change_of_position_on_a_scale || captured ;; Conquering || limit ;; Limiting
lost ;; Change_of_leadership || recaptured ;; Conquering
terrorist ;; Terrorism
become ;; Becoming || allied ;; Collaboration
began ;; Process_start || freedom ;; Change_of_leadership || assistance ;; Assistance || operation ;; Military_operation || affected ;; Influence || offered ;; Supply
presented ;; Presence || put ;; Placing || warfare ;; Hostile_encounter || increasing ;; Cause_change_of_position_on_a_scale || engage ;; Participation
intervention ;; Preventing_or_letting || captured ;; Conquering || fought ;; Hostile_encounter
control ;; Control || limited ;; Limiting || war ;; Hostile_encounter
fought ;; Hostile_encounter || Battle ;; Hostile_encounter || forces ;; Causation || War ;; Military_operation
losses ;; Earnings_and_losses || battle ;; Hostile_encounter || came ;; Arriving || war ;; Military_operation || defeat ;; Conquering
killed ;; Killing || wounded ;; Bodily_harm || taken ;; Conquering || prisoner ;; Arrest
suffered ;; Bodily_harm
captured ;; Conquering || became ;; Becoming || made ;; Manufacturing || trek ;; Self_motion
escaped ;; Escaping || attacking ;; Attack || arrived ;; Arriving || limited ;; Limiting || bolster ;; Cause_change_of_strength
reduce ;; Cause_change_of_position_on_a_scale || attack ;; Attack || conceal ;; Hiding_objects || made ;; Manufacturing
forces ;; Causation || converge ;; Come_together
battle ;; Hostile_encounter || killed ;; Killing || turned ;; Becoming || penetrated ;; Attack
attack ;; Attack || became ;; Becoming || forced ;; Motion || assault ;; Attack || surrender ;; Surrendering || injured ;; Bodily_harm
arrived ;; Arriving || blockade ;; Besieging
celebrate ;; Social_event || abuse ;; Committing_crime || rally ;; Social_event || address ;; Statement || issues ;; Creating
planned ;; Arranging || drive ;; Motion || participants ;; Participation || vote ;; Choosing || encouraged ;; Influence || elections ;; Choosing
sponsored ;; Supply
organized ;; Arranging || held ;; Hold
asked ;; Request || answered ;; Communication
builds ;; Building
told ;; Telling
say ;; Communication
told ;; Telling || teach ;; Education_teaching || know ;; Know
wrote ;; Writing || saying ;; Communication
knew ;; Know || marriage ;; Forming_relationships || ban ;; Preventing_or_letting || push ;; Self_motion
limited ;; Limiting || revival ;; Recovering
called ;; Communication
cut ;; Removing || concert ;; Social_event || presenting ;; Presence
participated ;; Participation
performed ;; Hold || show ;; Social_event
composed ;; Writing
composed ;; Writing
added ;; Cause_to_be_included
included ;; Cause_to_be_included || include ;; Cause_to_be_included
conference ;; Social_event || summit ;; Social_event || held ;; Hold
Conference ;; Social_event || conference ;; Social_event || included ;; Cause_to_be_included || Meeting ;; Social_event
agreed ;; Agree_or_refuse_to_act || change ;; Change
conference ;; Social_event || disarray ;; Quarreling || reported ;; Reporting
conference ;; Social_event || conclusion ;; Process_end || reported ;; Reporting || statement ;; Statement
agreement ;; Commitment || drafted ;; Writing || judged ;; Legal_rulings
participating ;; Participation || adopted ;; Choosing || debate ;; Quarreling
increases ;; Cause_change_of_position_on_a_scale || keep ;; Preventing_or_letting || challenges ;; Confronting_problem || recognised ;; Know || change ;; Change || taken ;; Self_motion
contain ;; Containing || commitments ;; Commitment || reducing ;; Cause_change_of_position_on_a_scale
siege ;; Besieging || took place ;; Process_start
visited ;; Traveling || orders ;; Arranging
besieged ;; Besieging
fell ;; Motion_directional
consisted ;; Cause_to_be_included || imprisoned ;; Arrest || allowed ;; Preventing_or_letting
moved ;; Motion
contributing ;; Giving || mentioned ;; Statement || includes ;; Cause_to_be_included || written ;; Writing
published ;; Publishing || written in ;; Writing || reads ;; Perception_active
found ;; Know || surrendered ;; Surrendering || laid ;; Placing || fortified ;; Cause_change_of_strength
recorded ;; Recording || conflict ;; Hostile_encounter
know ;; Know || walk ;; Self_motion || returned ;; Arriving
held ;; Defending || Celebration ;; Social_event
celebration ;; Social_event || Running ;; Self_motion || based ;; Causation
changes ;; Change || spawned ;; Coming_to_be
erected ;; Building || created ;; Creating || called ;; Name_conferral || written ;; Writing
produced ;; Manufacturing || created ;; Creating || called ;; Name_conferral || become ;; Becoming || promoted ;; Cause_to_make_progress
released ;; Publishing || celebration ;; Social_event || Celebrate ;; Social_event
contained ;; Containing
race ;; Competition || held ;; Hold
took place ;; Process_start
due ;; Causation || earthquake ;; Catastrophe || marathon ;; Competition || cancelled ;; Removing
takes place ;; Process_start
altered ;; Change || allow ;; Preventing_or_letting
host ;; Social_event
held ;; Hold
written ;; Writing
festival ;; Social_event || Festival ;; Social_event || educational ;; Education_teaching || concerts ;; Social_event
festival ;; Social_event
promotes ;; Cause_to_make_progress || exploring ;; Scrutiny || informed ;; Telling || supports ;; Supporting || performing ;; Action || writing ;; Writing
festival ;; Social_event || established ;; Creating
programmed ;; Arranging || managed ;; Control || stepping down ;; Change_of_leadership
became ;; Becoming || festival ;; Social_event || charity ;; Giving || constituted ;; Response || set up ;; Reforming_a_system
festival ;; Social_event || supported ;; Supporting || festival ;; Social_event || managing ;; Control
Festival ;; Social_event || takes place ;; Process_start
festival ;; Social_event || concerts ;; Social_event
ferry ;; Bringing || party ;; Participation || battle ;; Hostile_encounter || saw ;; Change
defended ;; Defending || bombardment ;; Attack || storm ;; Attack || failed ;; Earnings_and_losses || position ;; Placing
kept ;; Getting || action ;; Response || use ;; Using || drove ;; Preventing_or_letting
inspired ;; Influence || entitled ;; Name_conferral || clash ;; Hostile_encounter || write ;; Writing
fought ;; Hostile_encounter || war ;; Hostile_encounter || ferry ;; Bringing
raids ;; Attack || occurred ;; Coming_to_be || conflict ;; Hostile_encounter
Battle ;; Hostile_encounter || took place ;; Process_start
War ;; Military_operation
war ;; Military_operation || battalions ;; Hostile_encounter || finish ;; Process_end || neutralizing ;; Defending
deserted ;; Escaping || command ;; Control || fell ;; Being_in_operation
decided ;; Deciding || arrived ;; Arriving || force ;; Request || dispense ;; Dispersal || catch ;; Self_motion || contain ;; Containing || encountered ;; Hostile_encounter
command ;; Control || opposing ;; Agree_or_refuse_to_act || commanded ;; Control
supported ;; Supporting
made ;; Action || laid ;; Placing || standing ;; Placing
suffered ;; Bodily_harm
captured ;; Conquering
wrote ;; Writing || fought ;; Hostile_encounter
casualties ;; Killing
lost ;; Earnings_and_losses
killed ;; Killing
signed ;; Writing || gave up ;; Process_end || battle ;; Military_operation
disaster ;; Catastrophe || occurred ;; Coming_to_be
killed ;; Killing || used ;; Using || injured ;; Bodily_harm || transport ;; Sending
found ;; Know || use ;; Using || cause ;; Causation || accident ;; Catastrophe || attributed ;; Causation || investigation ;; Criminal_investigation
contributing ;; Giving || concluded ;; Process_end
criticize ;; Judgment_communication
accident ;; Catastrophe || hit ;; Attack || said ;; Statement
transport ;; Sending || following ;; Self_motion || occurred ;; Coming_to_be || crashed ;; Catastrophe
take-off ;; Self_motion
shut down ;; Being_in_operation
respond ;; Response
struck ;; Attack || lifting off ;; Motion_directional
killed ;; Killing
killed ;; Killing
found ;; Know || installed ;; Placing || starved ;; Death || causing ;; Causation
found ;; Know
installed ;; Placing
refusing ;; Agree_or_refuse_to_act || praised ;; Judgment_communication || took part ;; Dispersal || rescue ;; Rescuing
beginning ;; Process_start || ending ;; Process_end || offensive ;; Attack || offensive ;; Attack
began ;; Process_start || killing ;; Killing || offensive ;; Attack || attack ;; Attack || forces ;; Military_operation
began ;; Process_start || bombard ;; Attack || forces ;; Military_operation
blamed ;; Judgment_communication || armed ;; Bearing_arms
launched ;; Military_operation || restore ;; Recovering || forces ;; Request
sent ;; Sending
cut off ;; Having_or_lacking_access
sent ;; Sending
retreat ;; Escaping || forces ;; Military_operation
associate ;; Collaboration || Cup ;; Competition || tournament ;; Competition || Cup ;; Competition
tournament ;; Competition || took place ;; Process_start
ran ;; Action
process ;; Participation || replaced ;; Becoming || event ;; Competition || continued ;; Hold || play ;; Participation || event ;; Competition || qualified ;; Becoming_a_member
changes ;; Change || announced ;; Expressing_publicly || scheduled ;; Arranging || play ;; Participation || matches ;; Competition || matches ;; Competition || event ;; Competition || challenge ;; Confronting_problem
became ;; Becoming || win ;; Earnings_and_losses || challenge ;; Confronting_problem
changes ;; Change || agreed ;; Agree_or_refuse_to_act || following ;; Bringing || expand ;; Cause_change_of_position_on_a_scale || meeting ;; Social_event
granted ;; Receiving || proposed ;; Arranging || challenge ;; Confronting_problem
making ;; Action || play ;; Participation || match ;; Competition
challenge ;; Confronting_problem || scrapped ;; GiveUp
competition ;; Competition || game ;; Competition || final ;; Process_end || beating ;; Competition
finished ;; Process_end
match ;; Competition || fixture ;; Competition || said ;; Statement || winning ;; Earnings_and_losses || preparation ;; GetReady || cricket ;; Competition
hurricane ;; Catastrophe || struck ;; Attack
storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
triggered ;; Causation || approached ;; Arriving || ordered ;; Request
formed ;; Coming_to_be || becoming ;; Becoming || peaked ;; Cause_change_of_position_on_a_scale
recorded ;; Recording
strike ;; Attack || turned ;; Becoming
causing ;; Causation || damage ;; Damaging || struck ;; Attack
evacuations ;; Escaping || causing ;; Causation || preparations ;; GetReady || moved ;; Motion
caused ;; Causation || damage ;; Damaging || weakened ;; Cause_change_of_strength || striking ;; Attack || traveled ;; Traveling
produced ;; Manufacturing || hit ;; Attack
caused ;; Causation || exceeded ;; Cause_change_of_position_on_a_scale || flooding ;; Catastrophe
damage ;; Damaging || fatalities ;; Death
destruction ;; Destroying || Due ;; Causation || retired ;; GiveUp
held ;; Hold
held ;; Hold
races ;; Competition
race ;; Competition
set ;; GetReady || taking part ;; Dispersal
began ;; Process_start || Set ;; GetReady || built ;; Building
hear ;; Perception_active
played ;; Competition
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Invasion ;; Attack || War ;; Military_operation
defended ;; Defending
meet ;; Come_together || ready ;; GetReady || fighting ;; Hostile_encounter || defeated ;; Conquering
battle ;; Hostile_encounter || ceased ;; Process_end || exist ;; Preserving
joined ;; Becoming_a_member
fall ;; Motion_directional || set out ;; Departing || promote ;; Cause_to_make_progress
reaching ;; Arriving
ended ;; Process_end || started ;; Process_start
started ;; Process_start || interrupted ;; Hindering
completing ;; Process_end || ending ;; Process_end || tour ;; Traveling || returned ;; Arriving || returning ;; Arriving
Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || battle ;; Hostile_encounter || War ;; Military_operation || took place ;; Process_start
combatants ;; Hostile_encounter
reinforcing ;; Cause_change_of_strength || battle ;; Hostile_encounter || prevented ;; Preventing_or_letting || besieged ;; Besieging || evacuating ;; Emptying || forces ;; Causation
provided ;; Supply || siege ;; Besieging || control ;; Control || achieve ;; Achieve || reinforcements ;; Cause_change_of_strength
Siege ;; Besieging || independence ;; Change_of_leadership || securing ;; Getting
opted ;; Choosing || attack ;; Attack || arriving at ;; Arriving || forces ;; Causation
learned ;; Education_teaching || sailed ;; Self_motion || sailed ;; Self_motion
concluded ;; Coming_to_believe || join ;; Cause_to_amalgamate
arrived at ;; Arriving || see ;; Perception_active || anchor ;; Temporary_stay || sailed ;; Self_motion
meet ;; Come_together || prepared ;; GetReady || sailed ;; Self_motion
engagement ;; Hostile_encounter || took place ;; Process_start || maneuvering ;; Motion
meet ;; Come_together || engaged ;; Hostile_encounter
battle ;; Hostile_encounter || damage ;; Damaging || set ;; Placing || suffered ;; Confronting_problem || broke ;; Bodily_harm
debate ;; Quarreling
siege ;; Besieging || view ;; Perception_active || preferred ;; Choosing || carrying ;; Carry_goods || arrive ;; Arriving || sailed ;; Self_motion || lure ;; Influence
arrived ;; Arriving || broke ;; Self_motion || returned ;; Arriving
surrendered ;; Surrendering || organize ;; Arranging || sail ;; Self_motion || returned ;; Arriving
match ;; Competition || took place ;; Hold
decide ;; Deciding || match ;; Competition
losing ;; Competition || lost ;; Earnings_and_losses || appearance ;; Presence
lost ;; Earnings_and_losses || appearing ;; Presence
final ;; Competition || held ;; Hold
progressed ;; Achieve || finishing ;; Process_end
beat ;; Competition || reach ;; Achieve
took ;; Getting || missed ;; Earnings_and_losses || take ;; Causation || penalty ;; Rewards_and_punishments || saving ;; Rescuing
match ;; Competition || went to ;; Change || shoot-out ;; Hostile_encounter || clinch ;; Getting
became ;; Becoming || tournament ;; Competition || win ;; Earnings_and_losses
losing ;; Earnings_and_losses
competition ;; Competition || allowed ;; Preventing_or_letting || failed ;; Confronting_problem || finished ;; Process_end || enter ;; Participation || entered ;; Participation
beaten ;; Competition || represented ;; Presence || entering ;; Participation
competitions ;; Competition || match ;; Competition || played ;; Participation || sponsorship ;; Earnings_and_losses
match ;; Competition || played ;; Participation || resulted in ;; Causation || win ;; Getting
scored ;; Getting || goals ;; Rewards_and_punishments
sent ;; Sending || sent ;; Sending || challenge ;; Confronting_problem
record ;; Recording || scoring ;; Getting || signing ;; Sign_agreement || deflected ;; Motion
hitting ;; Motion || shot ;; Motion || got ;; Getting
signings ;; Sign_agreement
game ;; Competition || Goalkeeper ;; Defending
killing ;; Killing || collaborators ;; Collaboration || massacre ;; Killing
kill ;; Killing || moves ;; Motion || directed ;; Aiming || ordered ;; Request
captured ;; Conquering || organised ;; Arranging || executions ;; Rewards_and_punishments || murdered ;; Killing
perished ;; Death
killing ;; Killing || murdered ;; Killing
killed ;; Killing
check ;; Scrutiny || piled ;; Placing
killed ;; Killing || found ;; Know
fires ;; Use_firearm || burning ;; Bodily_harm || covered ;; Filling
killed ;; Killing
murdered ;; Committing_crime || watch ;; Perception_active || killed ;; Killing || protect ;; Defending
uprising ;; Change_of_leadership || end ;; Process_end || fight ;; Hostile_encounter
took ;; Creating || control ;; Control || fighting ;; Hostile_encounter || stiffened ;; Body_movement || regain ;; Getting
Battle ;; Hostile_encounter || attacked ;; Attack || liberation ;; Change_of_leadership
marched ;; Self_motion || violence ;; Violence || army ;; Use_firearm || looting ;; Attack || departure ;; Departing || ceased ;; Process_end
restored ;; Recovering || dragged ;; Motion || Order ;; Arranging || hiding ;; Hiding_objects || massacred ;; Catastrophe
demolished ;; Destroying || target ;; Aiming || becoming ;; Becoming
regains ;; Getting
arrival ;; Arriving
received ;; Receiving || reached ;; Arriving || rushing ;; Self_motion || dissuade ;; Convincing || walk ;; Self_motion || expressed ;; Expressing_publicly || reward ;; Rewards_and_punishments || service ;; Supply
return ;; Arriving || jubilation ;; Self_motion
reduced ;; Cause_change_of_position_on_a_scale || tears ;; Destroying
bombing ;; Attack || terrorist ;; Terrorism || attack ;; Attack || bombing ;; Terrorism
killed ;; Killing || died ;; Death || attack ;; Attack || injured ;; Bodily_harm
committed ;; Committing_crime || bombings ;; Terrorism
discovered ;; Know || cleared ;; Emptying
pack ;; Placing || detonated ;; Attack
focused ;; Aiming || bombings ;; Terrorism || investigated ;; Criminal_investigation || Investigation ;; Criminal_investigation
declared ;; Expressing_publicly || exonerated ;; Legality
identified ;; Check || bombings ;; Terrorism
arrested ;; Arrest
crimes ;; Committing_crime || sentenced ;; Rewards_and_punishments
sponsored ;; Supply
tournament ;; Competition || played ;; Competition
match ;; Competition || played ;; Competition
began ;; Process_start || threatened ;; Warning
tournament ;; Competition || get ;; Getting || argued ;; Statement || sanctioned ;; Rewards_and_punishments
started ;; Process_start || took place ;; Hold
aired ;; Expressing_publicly
decided ;; Deciding || match ;; Competition || played ;; Participation
received ;; Receiving || Champion ;; Competition
tournament ;; Competition || Champions ;; Competition
held ;; Hold || contested ;; Competition || contested ;; Competition
match ;; Competition || kicked off ;; Process_start
match ;; Competition || referee ;; Rewards_and_punishments
hosted ;; Social_event || opened ;; Process_start
hosted ;; Social_event
competition ;; Competition || entered ;; Participation
reach ;; Arriving || beat ;; Competition || beat ;; Competition
champions ;; Competition || entered ;; Participation
dominated ;; Control || securing ;; Getting || match ;; Competition || Champions ;; Competition || winning ;; Earnings_and_losses
score ;; Causation || scored ;; Getting
play ;; Participation || qualified ;; Getting || earned ;; Receiving
balance ;; Cause_change_of_strength
provided ;; Supply || governed ;; Control
applied ;; Using || convert ;; Change || ruled ;; Legal_rulings
converted ;; Change || prevent ;; Preventing_or_letting || forfeit ;; Rewards_and_punishments || replaced ;; Change_of_leadership
supported ;; Supporting
chose ;; Choosing || gained ;; Getting || applied ;; Using || protected ;; Defending || veto ;; Agree_or_refuse_to_act
contested ;; Competition || converted ;; Change
supported ;; Supporting || supported ;; Supporting || recovered ;; Recovering || defeated ;; Conquering
bring ;; Bringing || dissolve ;; Destroying
existence ;; Presence || voted ;; Choosing
died ;; Death
shot ;; Use_firearm
killed ;; Killing
admitted ;; Reveal_secret || accused ;; Rewards_and_punishments
said ;; Communication
shot ;; Use_firearm
claimed ;; Statement
organized ;; Arranging || festival ;; Social_event || reach ;; Arriving
walking ;; Self_motion || stands ;; Placing || consists ;; Cause_to_be_included
collected ;; Come_together || used ;; Using || assist ;; Assistance || represent ;; Presence
aims ;; Aiming || raising ;; Cause_change_of_position_on_a_scale || highlighting ;; Judgment_communication
organizes ;; Arranging || event ;; Social_event
contribute ;; Giving || plan ;; Arranging || arranged ;; Arranging || festival ;; Social_event
ended ;; Process_end || standoff ;; Hostile_encounter || started ;; Process_start || conflict ;; Hostile_encounter || incursion ;; Attack
began ;; Process_start || conflict ;; Hostile_encounter || arrived ;; Arriving || armed ;; Giving
sent ;; Sending
stated ;; Statement || objective ;; Aiming || assert ;; Statement || claim ;; Statement
surrounded ;; Surrounding || negotiations ;; Communication || killing ;; Killing || flush ;; Emptying || withdraw ;; Removing || launched ;; Process_start || operation ;; Military_operation || gathered ;; Come_together
killed ;; Killing || standoff ;; Hostile_encounter
captured ;; Conquering || escaped ;; Escaping
festival ;; Social_event || gathers ;; Come_together
founded ;; Creating || sponsored ;; Supply
playing ;; Competition || shows ;; Social_event
initiated ;; Process_start
starting ;; Process_start || beginning ;; Process_start || includes ;; Cause_to_be_included
contributing ;; Giving || awarded ;; Award
received ;; Receiving || gained ;; Achieve
performed ;; Hold
became ;; Becoming || Forming ;; Coming_to_be || organized ;; Come_together
strengthen ;; Cause_change_of_strength || hurricane ;; Catastrophe || peaked ;; Self_motion
weakened ;; Cause_change_of_strength || making ;; Causation
dissipated ;; Dispersal
killed ;; Killing || hurricane ;; Catastrophe || evacuate ;; Escaping
destroyed ;; Destroying || left ;; Causation
suffered ;; Bodily_harm || damage ;; Damaging || totaled ;; Earnings_and_losses
race ;; Competition || National ;; Competition || Grand ;; Competition || took place ;; Process_start
start ;; Process_start || delayed ;; Change_event_time || caused ;; Causation || race ;; Competition
start ;; Process_start || started ;; Process_start || Grand ;; Competition || National ;; Competition || introduced ;; Change
race ;; Competition || trained ;; Education_teaching || ridden ;; Motion || collected ;; Come_together
finished ;; Process_end
ran ;; Self_motion || returned ;; Arriving
deployed ;; Arranging || record ;; Recording || race ;; Competition || capture ;; Getting
celebrations ;; Social_event || made ;; Manufacturing || took place ;; Process_start || victory ;; Earnings_and_losses || leaping ;; Self_motion
held ;; Hold
attend ;; Participation
programmed ;; Arranging || featuring ;; Cause_to_be_included
takes place ;; Process_start
offers ;; Supply
supplies ;; Supply
gather ;; Come_together
takes place ;; Process_start
listen ;; Perception_active || guest ;; Traveling
culminates ;; Process_end
announced ;; Expressing_publicly
Celebration ;; Social_event || gathering ;; Come_together
founded ;; Creating || event ;; Social_event || brings together ;; Come_together
raises ;; Cause_change_of_position_on_a_scale
distributed ;; Dispersal
Celebration ;; Social_event || held ;; Hold
Celebration ;; Social_event || event ;; Social_event || Jamboree ;; Social_event || held ;; Hold
took place ;; Process_start || strike ;; Attack
celebrated ;; Social_event || gave ;; Giving || passing ;; Motion_directional
sponsored ;; Supply
gathered ;; Come_together
gathering ;; Come_together
strike ;; Attack || demand ;; Request || spearheaded ;; Self_motion
advocated ;; Convincing || social ;; Social_event || marriage ;; Forming_relationships
refers ;; Name_conferral || wedding ;; Forming_relationships
celebrating ;; Social_event || wedding ;; Forming_relationships
accused ;; Judgment_communication || wedding ;; Forming_relationships
found ;; Know || killed ;; Killing || said ;; Communication
Wedding ;; Forming_relationships
injured ;; Bodily_harm
gathered ;; Come_together || destroyed ;; Destroying
responded ;; Response || elected ;; Choosing
win ;; Getting
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || fought ;; Hostile_encounter
battles ;; Hostile_encounter || fought ;; Hostile_encounter
battle ;; Hostile_encounter || fought ;; Hostile_encounter || resolve ;; Resolve_problem || competing ;; Competition || claims ;; Statement
control ;; Control || taken ;; Getting
claim ;; Statement || married ;; Forming_relationships || contested ;; Hostile_encounter
invaded ;; Attack || intention ;; Aiming || seizing ;; Conquering
Battle ;; Hostile_encounter || defeated ;; Conquering
captured ;; Conquering || advanced ;; Self_motion
assembled ;; Come_together || met ;; Come_together
fighting ;; Hostile_encounter || lost ;; Earnings_and_losses || retreating ;; Escaping || lost ;; Earnings_and_losses
forced ;; Causation || surrender ;; Surrendering || recaptured ;; Conquering || claim ;; Statement || victory ;; Earnings_and_losses || saved ;; Rescuing
awarded ;; Award
gave ;; Giving || nickname ;; Name_conferral || battle ;; Hostile_encounter
commemorated ;; Social_event
took place ;; Process_start || Battle ;; Hostile_encounter
sent ;; Sending || forces ;; Causation || led ;; Conquering || gathering ;; Come_together
attacked ;; Attack
put up ;; Placing
sent ;; Sending || attacked ;; Attack
broke ;; Destroying
occurred ;; Coming_to_be || fire ;; Catastrophe || fire ;; Catastrophe
criticism ;; Judgment_communication || fire ;; Catastrophe || broke out ;; Process_start || generated ;; Causation
escape ;; Escaping || claimed ;; Statement || shut ;; Body_movement || fire ;; Catastrophe
response ;; Communication || criticism ;; Judgment_communication || response ;; Communication || accident ;; Catastrophe
launched ;; Process_start || separate ;; Forming_relationships || investigations ;; Scrutiny
operation ;; Using || focused ;; Aiming || ascertain ;; Check || disaster ;; Catastrophe || illegal ;; Legality
departed ;; Departing || stopover ;; Temporary_stay
collided ;; Catastrophe || descending ;; Motion_directional || taken off ;; Departing
occurred ;; Presence || collision ;; Catastrophe
crashed ;; Catastrophe || lost ;; Earnings_and_losses || control ;; Control || killing ;; Killing
leading to ;; Causation || involved ;; Cause_to_be_included || violations ;; Legality
disaster ;; Catastrophe
occurred ;; Coming_to_be
block ;; Hiding_objects || situated ;; Placing
used ;; Using || tickets ;; Preventing_or_letting || arriving in ;; Arriving || race ;; Competition || meeting ;; Social_event
pass ;; Motion_directional || block ;; Hiding_objects || ruling ;; Legal_rulings || used ;; Using || control ;; Control
stood ;; Placing || waiting ;; Temporary_stay
moved ;; Motion || moved ;; Motion || checked ;; Scrutiny || tickets ;; Preventing_or_letting
understood ;; Know
came ;; Arriving
rose ;; Cause_change_of_position_on_a_scale || signals ;; Warning || lowered ;; Cause_change_of_position_on_a_scale || assumed ;; Deciding
gave ;; Giving || gave ;; Giving || seen ;; Perception_active || said ;; Telling || understood ;; Coming_to_believe
reported ;; Reporting || travelling ;; Motion
rounded ;; Motion
applied ;; Using || brake ;; Self_motion || threw ;; Motion || stop ;; Process_end
legal ;; Legality || formed ;; Coming_to_be || case ;; Criminal_investigation
defend ;; Defending
existence ;; Presence || break down ;; Destroying || blame ;; Judgment_communication || known ;; Know || use ;; Using || returned ;; Giving || think ;; Coming_to_believe || said ;; Communication
skinned ;; Emptying || exonerated ;; Judgment_communication || misfortune ;; Catastrophe || said ;; Communication
sounded ;; Change_sentiment || thinking ;; Perception_active || disaster ;; Catastrophe || death ;; Death || followed ;; Motion
began ;; Process_start
conducted ;; Action || search ;; Scrutiny
conducted ;; Action || provided ;; Supply || operating ;; Being_in_operation || support ;; Supporting
netted ;; Earnings_and_losses || illegal ;; Legality
blocking ;; Hiding_objects || searching ;; Scrutiny || established ;; Building
terrorists ;; Terrorism || said ;; Communication || hinder ;; Hindering || going ;; Self_motion || protecting ;; Defending || criminals ;; Committing_crime || create ;; Creating
reported ;; Reporting || deaths ;; Death
organized ;; Arranging || championship ;; Competition
tournament ;; Competition
hosted ;; Social_event
defeating ;; Conquering
defeating ;; Conquering || claim ;; Statement
participation ;; Participation || hosted ;; Social_event
host ;; Social_event
opened ;; Openness
opening ;; Openness
built ;; Building || named ;; Name_conferral || called ;; Statement
used ;; Using || converted ;; Change || housing ;; Building
final ;; Process_end || host ;; Come_together
shelling ;; Attack || targeting ;; Aiming || carried out ;; Action
occurred ;; Coming_to_be
killed ;; Killing || occurred ;; Coming_to_be || wounded ;; Bodily_harm
wounded ;; Bodily_harm || killed ;; Killing || occurred ;; Coming_to_be
attack ;; Attack || end ;; Process_end || lead to ;; Causation
shelling ;; Attack
establish ;; Building || led to ;; Causation
shelled ;; Attack || claimed ;; Statement
fired ;; Use_firearm || summarized ;; Statement || ruled ;; Legal_rulings || found ;; Know
ambushed ;; Attack || annihilated ;; Killing
attacked ;; Attack
achieved ;; Achieve
comprised ;; Cause_to_be_included
crossed ;; Motion_directional || following ;; Self_motion
moved ;; Motion || ambushed ;; Killing
made ;; Manufacturing || broke out ;; Process_start
flood ;; Motion || involved ;; Participation || risen ;; Cause_change_of_position_on_a_scale
fighting ;; Hostile_encounter || killing ;; Killing
elevated ;; Cause_change_of_position_on_a_scale
became ;; Becoming || battle ;; Hostile_encounter
produced ;; Manufacturing || released ;; Publishing || depicting ;; Statement
given ;; Giving || claimed ;; Statement || stop ;; Process_end
found ;; Know || accepting ;; Receiving || keeping ;; Temporary_stay || passing ;; Motion_directional || overturned ;; Self_motion
conflict ;; Violence || tension ;; Confronting_problem || conflicts ;; Hostile_encounter || disputes ;; Quarreling
attributed ;; Causation || conflict ;; Violence || movements ;; Social_event || rise ;; Cause_change_of_position_on_a_scale || clashed ;; Hostile_encounter
dispute ;; Quarreling || conflicting ;; Hostile_encounter || claims ;; Statement
regarded ;; Statement || regarded ;; Statement
emerged ;; Coming_to_be || declaration ;; Expressing_publicly || conflict ;; Violence || war ;; Military_operation || war ;; Military_operation || independence ;; Change_of_leadership || peaking ;; Cause_change_of_position_on_a_scale || transforming ;; Change
ended ;; Process_end || War ;; Military_operation
withdrawal ;; Escaping || signed ;; Sign_agreement || resulting in ;; Causation || abolishment ;; GiveUp || annexation ;; Change_of_leadership
shifted ;; Change || conflict ;; Violence || War ;; Military_operation || peaked ;; Cause_change_of_position_on_a_scale
decline ;; Cause_change_of_position_on_a_scale || led to ;; Causation || creation ;; Creating
reached ;; Achieve || accord ;; Statement
cease-fire ;; Process_end || maintained ;; Preserving
existing ;; Presence || agreements ;; Sign_agreement || accords ;; Statement
reshuffled ;; Change_of_leadership || warfare ;; Hostile_encounter || emerging ;; Coming_to_be || War ;; Military_operation || opposition ;; Competition || putting ;; Placing
attributed ;; Causation || conflict ;; Violence || conflict ;; Violence
formed ;; Coming_to_be || confront ;; Confronting_problem || move ;; Motion || marked ;; Recording || fading ;; Cause_change_of_position_on_a_scale
fought ;; Hostile_encounter || consisting ;; Cause_to_be_included
dispute ;; Quarreling || influence ;; Influence || war ;; Military_operation
took place ;; Process_start
leading to ;; Causation
gained ;; Getting || plagued ;; Bodily_harm
started ;; Process_start || gaining ;; Getting || war ;; Military_operation || struggled ;; Confronting_problem
extend ;; Expansion || occupied ;; Control
asserting ;; Statement
included ;; Cause_to_be_included || incorporated ;; Cause_to_amalgamate || threatened ;; Warning
pursued ;; Aiming || eliminate ;; Removing
allied ;; Collaboration
signing ;; Sign_agreement
declared ;; Expressing_publicly
advanced ;; Cause_to_make_progress || defeating ;; Conquering
advancing ;; Cause_to_make_progress || engage ;; Participation || launching ;; Process_start
ended ;; Process_end || establishing ;; Creating
war ;; Military_operation
began ;; Process_start
resolve ;; Resolve_problem || war ;; Military_operation
war ;; Military_operation || continued ;; Process_start || asserting ;; Statement
sparked ;; Causation || wars ;; Military_operation
went off ;; Process_end || bombing ;; Attack || occurred ;; Presence
died ;; Death || placed ;; Placing || bomb ;; Attack
found ;; Know
injuries ;; Bodily_harm || claimed ;; Statement || attack ;; Attack || exploded ;; Attack
killed ;; Killing || kill ;; Killing || bombing ;; Attack || attack ;; Attack || attack ;; Attack
came ;; Motion || founding ;; Creating || attack ;; Attack
tournament ;; Competition || took place ;; Process_start || involving ;; Cause_to_be_included
matches ;; Competition || played ;; Competition || played ;; Competition || matches ;; Competition
rank ;; Choosing || tournament ;; Competition || played ;; Competition || playing ;; Competition
competed ;; Competition || defeating ;; Conquering
losing ;; Earnings_and_losses || win ;; Earnings_and_losses || represented ;; Change_sentiment
tournament ;; Competition || played ;; Competition
contest ;; Competition || progressed ;; Cause_to_make_progress || leaving ;; Causation
claiming ;; Statement || defeated ;; Hostile_encounter
event ;; Competition || marred ;; Hindering || withdrawal ;; Escaping || decision ;; Deciding || repercussions ;; Hindering
gained ;; Getting
gained ;; Getting || opposing ;; Agree_or_refuse_to_act
ended ;; Process_end || match ;; Competition
give ;; Giving
reach ;; Achieve
included ;; Cause_to_be_included
uploaded ;; Dispersal
nickname ;; Name_conferral || claims ;; Statement
fought ;; Hostile_encounter
capture ;; Conquering
allied ;; Collaboration || supporting ;; Supporting || claim ;; Statement || prevent ;; Preventing_or_letting
opposed ;; Agree_or_refuse_to_act || ruling ;; Legal_rulings
began ;; Process_start || contained ;; Containing || joined ;; Becoming_a_member
focused ;; Aiming || use ;; Using
outcome ;; Process_end
start ;; Process_start
hold off ;; Defending
undermined ;; Destroying || led to ;; Causation
prevent ;; Preventing_or_letting || proved ;; Convincing
fought ;; Hostile_encounter || abandoned ;; GiveUp || resulted in ;; Causation
said ;; Telling || lost ;; Earnings_and_losses
bombings ;; Attack
killed ;; Killing || injured ;; Bodily_harm
claimed ;; Statement || bombings ;; Attack || attacks ;; Attack
bombing ;; Attack || associates ;; Collaboration || perpetrated ;; Committing_crime || provoke ;; Causation || carried out ;; Action || service ;; Supply
incurred ;; Causation || claimed ;; Statement || carried out ;; Action
separate ;; Forming_relationships || exploded ;; Attack
killed ;; Killing || wounded ;; Bodily_harm || voted ;; Choosing
bombings ;; Attack
commissioned ;; Request
published ;; Publishing
concert ;; Social_event
supported ;; Supporting
performed ;; Hold
announced ;; Expressing_publicly
performed ;; Hold
began ;; Process_start || concluded ;; Process_end
divided ;; Change
included ;; Cause_to_be_included
began ;; Process_start || rising ;; Cause_change_of_position_on_a_scale || opening ;; Openness || perform ;; Action
included ;; Cause_to_be_included || extended ;; Expansion
received ;; Receiving || praised ;; Judgment_communication || reviews ;; Statement
announcement ;; Expressing_publicly || success ;; Achieve || leading to ;; Causation
titled ;; Name_conferral || recording ;; Recording || released ;; Publishing || release ;; Publishing
cyclone ;; Catastrophe || develop ;; Coming_to_be
noted ;; Know
moved ;; Motion || named ;; Name_conferral || developed ;; Coming_to_be
developed ;; Coming_to_be || moved ;; Motion || intensified ;; Cause_change_of_strength
started ;; Process_start || peaked ;; Cause_change_of_position_on_a_scale || weaken ;; Cause_change_of_strength || passed ;; Motion_directional
passed ;; Motion_directional || passed ;; Motion_directional
weakened ;; Cause_change_of_strength || became ;; Becoming || advancing ;; Motion
caused ;; Causation || damages ;; Damaging || cyclone ;; Catastrophe || affected ;; Influence
evacuated ;; Emptying || affecting ;; Influence
closure ;; Process_end || forced ;; Causation
moved ;; Motion || destroyed ;; Destroying || damaged ;; Damaging || bringing ;; Bringing || uprooting ;; Destroying
go ;; Self_motion || used ;; Using || washed ;; Motion || take ;; Removing
refused ;; Agree_or_refuse_to_act || attacked ;; Attack
damage ;; Damaging || recorded ;; Recording
damage ;; Damaging || reported ;; Publishing
election ;; Change_of_leadership || surrounding ;; Surrounding || following ;; Motion || formation ;; Coming_to_be || produce ;; Creating || took place ;; Process_start
election ;; Change_of_leadership || held ;; Defending || negotiations ;; Communication || resulted in ;; Causation || sparking ;; Causation || form ;; Coming_to_be
election ;; Change_of_leadership || lost ;; Earnings_and_losses || defeated ;; Hostile_encounter
aimed ;; Aiming || forming ;; Coming_to_be || embarked ;; Process_start || meetings ;; Social_event
held ;; Defending || party ;; Social_event || party ;; Social_event || enter ;; Arriving || announced ;; Expressing_publicly
began ;; Process_start || election ;; Change_of_leadership || announced ;; Expressing_publicly || meetings ;; Social_event
held ;; Defending || party ;; Social_event || negotiations ;; Communication
signalled ;; Communication || became ;; Becoming || party ;; Social_event || join ;; Becoming_a_member || coalition ;; Collaboration || presence ;; Presence || seen ;; Perception_active || formulating ;; Creating
party ;; Social_event || agreement ;; Agree_or_refuse_to_act || relinquished ;; GiveUp
took ;; Conquering || resigned ;; Change_of_leadership
emerged ;; Coming_to_be || published ;; Publishing || meeting ;; Social_event || party ;; Social_event || coalition ;; Collaboration || announce ;; Expressing_publicly || approved ;; Preventing_or_letting
became ;; Becoming
concert ;; Social_event || Tour ;; Traveling || tour ;; Traveling
follows ;; Motion || release ;; Publishing
followed ;; Motion
began ;; Process_start || tour ;; Traveling || tour ;; Traveling || announced ;; Expressing_publicly
decision ;; Deciding || retracted ;; Agree_or_refuse_to_act
run ;; Self_motion || tour ;; Traveling
air ;; Expressing_publicly || crash ;; Catastrophe || crashed ;; Motion || occurred ;; Coming_to_be
recovered ;; Recovering || moved ;; Bringing
injured ;; Bodily_harm
ending ;; Process_end || released ;; Publishing
states ;; Statement || pulled ;; Self_motion || lost ;; Earnings_and_losses
given ;; Giving || codename ;; Name_conferral
raid ;; Attack || carried out ;; Military_operation
supported ;; Supporting
taking place ;; Process_start
draw ;; Motion || seen ;; Change_sentiment
described ;; Statement || crisis ;; Hostile_encounter
dismissal ;; Change_of_leadership || culminated ;; Filling
elected ;; Change_of_leadership || held ;; Hold
election ;; Change_of_leadership || change ;; Change || resulted in ;; Causation
introduced ;; Creating || rocked ;; Bodily_harm
used ;; Using || control ;; Control || defer ;; Change_event_time
stated ;; Statement || urged ;; Request || continue ;; Self_motion || opposition ;; Judgment_communication || stance ;; Statement || dismiss ;; Judgment_communication || election ;; Choosing
believed ;; Know
break ;; Destroying || call ;; Request || attempt ;; Self_motion || election ;; Choosing
installed ;; Change_of_leadership || went to ;; Motion || dismissed ;; Judgment_communication || approval ;; Supporting || election ;; Choosing
became ;; Becoming || change ;; Change || aware ;; Coming_to_believe || dissolved ;; Dispersal || election ;; Choosing
election ;; Change_of_leadership || held ;; Hold || returned ;; Motion
change ;; Change || Dismissal ;; Change_of_leadership || led to ;; Causation || events ;; Social_event
supply ;; Supply || retained ;; Preserving || block ;; Preventing_or_letting || dismiss ;; Judgment_communication
used ;; Using || force ;; Request
resigned ;; Change_of_leadership || criticised ;; Judgment_communication
Games ;; Competition || hosted ;; Social_event || Games ;; Competition
Games ;; Competition || hosts ;; Social_event || designated ;; Deciding
host ;; Social_event || games ;; Competition
hosted ;; Social_event || games ;; Competition
games ;; Competition
awarded ;; Giving || Games ;; Competition || hosting ;; Social_event || pulled out ;; GiveUp || due ;; Causation || reasons ;; Causation
host ;; Social_event || games ;; Competition || withdrawal ;; Escaping
hosting ;; Social_event || games ;; Competition || support ;; Supporting || use ;; Using || occupied ;; Hold || withdrawal ;; Escaping
expressed ;; Expressing_publicly || host ;; Social_event
accepted ;; Receiving || hosting ;; Social_event || reversed ;; Change || withdrawal ;; Escaping || Games ;; Competition || support ;; Supporting || announced ;; Expressing_publicly
hosting ;; Social_event || host ;; Social_event || Games ;; Competition || Games ;; Competition
held ;; Defending
expand ;; Expansion || host ;; Come_together || joined ;; Becoming_a_member || let ;; Preventing_or_letting
expansion ;; Expansion || keeping ;; Temporary_stay
hosted ;; Come_together
opened ;; Openness || closed ;; Process_end
conducted ;; Action || Operation ;; Military_operation || forces ;; Military_operation
controlled ;; Control || objective ;; Aiming || re-establish ;; Creating
prompted ;; Causation || blocked ;; Hiding_objects || capture ;; Conquering
recaptured ;; Conquering || capturing ;; Conquering || pushed ;; Motion
reached ;; Arriving || destroyed ;; Destroying || held ;; Defending
involved ;; Cause_to_be_included || resulted in ;; Causation || casualties ;; Killing
blamed ;; Judgment_communication || report ;; Reporting || conflicts ;; Hostile_encounter || command ;; Control
caused ;; Causation || rejected ;; Response || secure ;; Getting || allegations ;; Statement
supported ;; Supporting
covered ;; Hiding_objects || started ;; Process_start || culminating ;; Self_motion
supported ;; Supporting || opening ;; Openness
toured ;; Traveling || beginning ;; Process_start
united ;; Cause_to_amalgamate
bombings ;; Terrorism || believed ;; Know || took place ;; Process_start || insurgency ;; Change_of_leadership
killed ;; Killing || injured ;; Bodily_harm
injured ;; Bodily_harm || started ;; Process_start || explosions ;; Attack || blasts ;; Attack
hidden ;; Hiding_objects || starting ;; Process_start || detonated ;; Attack
hurled ;; Attack || exploded ;; Attack
exploded ;; Attack
hidden ;; Hiding_objects || exploded ;; Attack
destroyed ;; Destroying || damaged ;; Damaging || blast ;; Attack
terrorist ;; Terrorism || attacks ;; Terrorism || reignited ;; Recovering || insurgency ;; Change_of_leadership
occurred ;; Coming_to_be
entered ;; Arriving
comprised ;; Cause_to_be_included
causing ;; Causation || covering ;; Hiding_objects || travelling ;; Traveling || saturated ;; Influence
running ;; Self_motion || causing ;; Causation || derailed ;; Self_motion
crushed ;; Bodily_harm
died ;; Death || died ;; Death || injured ;; Bodily_harm
reported ;; Reporting
conflict ;; Hostile_encounter || insurgency ;; Military_operation
began ;; Process_start || declared ;; Expressing_publicly || creating ;; Creating
formed ;; Coming_to_be || combatting ;; Hostile_encounter
conflict ;; Hostile_encounter
helped ;; Assistance || gained ;; Getting
clashed ;; Hostile_encounter || driven ;; Self_motion
lost ;; Earnings_and_losses
hiding ;; Hiding_objects || gaining ;; Getting || went ;; Self_motion || strength ;; Cause_change_of_strength
start ;; Process_start || brought ;; Bringing || beginning ;; Process_start || led ;; Causation || ousted ;; Change_of_leadership
supported ;; Supporting
refused ;; Agree_or_refuse_to_act || hand ;; Giving || recaptured ;; Conquering
attacks ;; Attack || sponsored ;; Supply
targeted ;; Aiming || targeted ;; Aiming || suicide bombings ;; Killing
reached ;; Arriving || let ;; Preventing_or_letting
violence ;; Violence || murder ;; Killing
responded ;; Response || create ;; Creating || murdering ;; Killing
massacring ;; Killing || stepped up ;; Cause_change_of_strength
battling ;; Hostile_encounter
known ;; Know
debated ;; Communication
constructed ;; Building
concealed ;; Hiding_objects || gathered ;; Come_together || attack ;; Attack
asked ;; Request || advised ;; Convincing || travel ;; Traveling || met ;; Come_together
sent ;; Sending
asked ;; Request || arrived ;; Arriving || leave ;; Departing
emerged ;; Coming_to_be || began ;; Process_start || hidden ;; Hiding_objects || broke out ;; Process_start
fled ;; Escaping || leaving ;; Departing
called ;; Name_conferral
effects ;; Influence || cyclone ;; Catastrophe
developed ;; Coming_to_be
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe
intensified ;; Cause_change_of_strength
curved ;; Motion_directional
caused ;; Causation || drift ;; Motion || blocking ;; Hiding_objects
upgraded ;; Cause_to_make_progress || hurricane ;; Catastrophe
causing ;; Causation || decreased ;; Cause_change_of_position_on_a_scale || due ;; Causation || weakening ;; Cause_change_of_strength
passing ;; Motion_directional || drifted ;; Self_motion || accelerated ;; Cause_change_of_position_on_a_scale
caused ;; Causation || knocked down ;; Damaging
becoming ;; Becoming || took place ;; Process_start || transitioning ;; Change
brought ;; Bringing
occurred ;; Coming_to_be
ripped ;; Destroying || destroyed ;; Destroying || left ;; Temporary_stay
built ;; Building || destroyed ;; Destroying || damaged ;; Damaging
caused ;; Causation || damage ;; Damaging
war ;; Hostile_encounter || landing ;; Self_motion || invasion ;; Attack
advance ;; Cause_to_make_progress || support ;; Supporting || seize ;; Control
capture ;; Conquering || guarded ;; Defending
landing ;; Self_motion || landing ;; Self_motion || landing ;; Self_motion
made ;; Causation || demonstration ;; Supporting
landing ;; Self_motion
became ;; Becoming || defences ;; Defending || landings ;; Self_motion || exploited ;; Reveal_secret
gain ;; Getting || managed ;; Control
battles ;; Hostile_encounter || fought ;; Hostile_encounter || reach ;; Arriving || defeated ;; Conquering
known ;; Know
impacts ;; Influence
became ;; Becoming || impact ;; Influence
formed ;; Coming_to_be || drifted ;; Motion || come together ;; Come_together
moved ;; Motion || passed ;; Motion_directional || turned ;; Becoming
threat ;; Warning || landfall ;; Self_motion || reach ;; Achieve
pass ;; Motion_directional || storm ;; Catastrophe || landfall ;; Self_motion || landfall ;; Self_motion || followed ;; Motion
declared ;; Expressing_publicly || upgraded ;; Cause_to_make_progress
revealed ;; Reveal_secret || passing ;; Motion_directional
stated ;; Statement || kept ;; Preventing_or_letting
landfall ;; Self_motion
received ;; Receiving
received ;; Receiving
impact ;; Influence
predicted ;; Coming_to_believe
focusing ;; Aiming
warned ;; Warning
stranded ;; Motion
stranded ;; Motion
neared ;; Placing || cancelled ;; Change_event_time || evacuated ;; Escaping
said ;; Communication || prepared ;; GetReady
claimed ;; Statement || prepared ;; GetReady
declared ;; Expressing_publicly || landfall ;; Self_motion
passed ;; Motion_directional
reported ;; Reporting
caused ;; Causation
stranded ;; Motion
weakened ;; Cause_change_of_strength || maintained ;; Preserving || intensified ;; Cause_change_of_strength
run ;; Action
contested ;; Competition
separated ;; Forming_relationships
contested ;; Competition
played ;; Competition
finished ;; Process_end || gaining ;; Getting
finished ;; Process_end || relegated ;; Judgment_communication
concert ;; Social_event || tour ;; Traveling
contained ;; Containing || shows ;; Social_event
began ;; Process_start || concluded ;; Process_end
marriage ;; Forming_relationships
collaborated ;; Collaboration
included ;; Cause_to_be_included || set ;; Placing || shows ;; Social_event
release ;; Publishing || changed ;; Change
praised ;; Judgment_communication || dancing ;; Self_motion || lauded ;; Judgment_communication
announcement ;; Expressing_publicly || prompted ;; Causation || added ;; Cause_change_of_position_on_a_scale || tour ;; Traveling || shows ;; Social_event
bringing ;; Bringing || grossed ;; Earnings_and_losses
tour ;; Traveling
released ;; Publishing || tour ;; Traveling || broadcast ;; Expressing_publicly
Contention ;; Competition
collapse ;; Process_end || split ;; Destroying
emerged ;; Coming_to_be || engaged ;; Hostile_encounter || struggle ;; Hostile_encounter
led ;; Bringing
independent ;; Forming_relationships || conflict ;; Hostile_encounter || warred ;; Hostile_encounter
ended ;; Process_end || proclaiming ;; Statement || war ;; Military_operation || victory ;; Earnings_and_losses
hurricane ;; Catastrophe || Hurricane ;; Catastrophe || cyclone ;; Catastrophe
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
passing ;; Motion_directional || moved ;; Motion
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || struck ;; Attack
strengthened ;; Cause_change_of_strength || landfall ;; Arriving
weakened ;; Cause_change_of_strength || final ;; Process_end || re-strengthened ;; Cause_change_of_strength || made ;; Manufacturing || landfall ;; Arriving
caused ;; Causation || deaths ;; Death
damages ;; Damaging || disaster ;; Catastrophe || causing ;; Causation || deaths ;; Death || hurricane ;; Catastrophe || produced ;; Causation
caused ;; Causation || described ;; Statement || surrounding ;; Surrounding || hurricane ;; Catastrophe || cyclone ;; Catastrophe || fatalities ;; Death
recovered ;; Recovering || flooding ;; Catastrophe
suggesting ;; Convincing || hurricane ;; Catastrophe || hurricane ;; Catastrophe || landfall ;; Arriving || produced ;; Causation
end ;; Process_end || took place ;; Process_start
surrounded ;; Surrounding || captured ;; Conquering || overthrowing ;; Change_of_leadership || installing ;; Placing
participate ;; Participation || thrown off ;; GiveUp || established ;; Building
led to ;; Causation || fight ;; Hostile_encounter || defeats ;; Conquering || concluding ;; Process_end || concluded ;; Process_end
ordered ;; Arranging || return ;; Arriving
resisted ;; Defending || employ ;; Employment || keep ;; Temporary_stay
became ;; Becoming || surrendered ;; Surrendering || death ;; Death || capture ;; Conquering || forces ;; Military_operation
derailed ;; Self_motion || placed ;; Placing || hitting ;; Attack || smashed ;; Destroying || opened ;; Openness
involved ;; Cause_to_be_included
placed ;; Placing || climbed ;; Self_motion
travelling ;; Traveling || going to ;; Self_motion
derailed ;; Self_motion || seated ;; Placing || structure ;; Building || crushing ;; Bodily_harm || killing ;; Killing || killing ;; Killing
reduce ;; Cause_change_of_position_on_a_scale || injury ;; Bodily_harm
carrying ;; Carry_goods
surrounding ;; Surrounding || Following ;; Self_motion
imprisoned ;; Arrest
launched ;; Process_start
massacre ;; Killing || killing ;; Killing
massacre ;; Killing || carried out ;; Action
ordered ;; Request
received ;; Receiving || massacre ;; Killing || reports ;; Reporting || stop ;; Preventing_or_letting || prevent ;; Preventing_or_letting
presented ;; Presence || massacre ;; Killing || elected ;; Choosing
assumed ;; Choosing || carried out ;; Action
invaded ;; Attack
massacre ;; Killing || battles ;; Hostile_encounter || took place ;; Process_start || withdrew ;; Escaping
removed ;; Removing || taking ;; Action || encircled ;; Attack || kept ;; Temporary_stay
enabled ;; Preventing_or_letting || withdrawal ;; Action || raid ;; Attack || ceasefire ;; Process_end || considered ;; Agree_or_refuse_to_act
surrounded ;; Surrounding || request ;; Request || prevent ;; Preventing_or_letting || leaving ;; Departing || fired ;; Use_firearm || stationed ;; Motion
expelled ;; Removing || killings ;; Killing
killings ;; Killing || taken place ;; Process_start
murdered ;; Killing || massacre ;; Killing || massacre ;; Killing || response ;; Response
became ;; Becoming || served ;; Assistance
involved ;; Participation
involved ;; Participation
concluded ;; Deciding
massacre ;; Killing || concluded ;; Deciding
found ;; Know || aware ;; Know || massacre ;; Killing || stop ;; Preventing_or_letting || investigate ;; Criminal_investigation
forcing ;; Request || deemed ;; Coming_to_believe || resign ;; Change_of_leadership
took place ;; Process_start
take ;; Conquering
retreated ;; Escaping || defeat ;; Conquering
made ;; Manufacturing || set off ;; Departing
surrounded ;; Surrounding
traversed ;; Motion_directional || help ;; Assistance
precluded ;; Hindering
bombings ;; Terrorism || suicide attacks ;; Terrorism || targeted ;; Aiming || referred ;; Name_conferral || travelling ;; Traveling
detonated ;; Attack
bombings ;; Terrorism || occurred ;; Coming_to_be
attacks ;; Terrorism || bombing ;; Terrorism || bombings ;; Terrorism || suicide attack ;; Terrorism || killed ;; Killing || injured ;; Bodily_harm || incident ;; Attack || making it ;; Causation
caused ;; Causation || packed ;; Filling || explosions ;; Use_firearm
attacks ;; Terrorism || damage ;; Damaging || followed ;; Arriving || cause ;; Causation || injury ;; Bodily_harm || bombings ;; Use_firearm
attacks ;; Terrorism || occurred ;; Coming_to_be || Games ;; Competition || bid ;; Arranging || host ;; Social_event
tournament ;; Competition || qualification ;; Participation
held ;; Hold || involved ;; Participation
tournament ;; Competition || format ;; Arranging
tournament ;; Competition || qualifying ;; Participation || filled ;; Filling || divided ;; Change
tournament ;; Competition || followed ;; Action || play ;; Competition || qualifying ;; Becoming_a_member
enter ;; Becoming_a_member || tournament ;; Competition || clubs ;; Becoming_a_member || participating ;; Participation
tournament ;; Competition || tournament ;; Competition || qualification ;; Participation || took place ;; Hold
host ;; Social_event || announced ;; Expressing_publicly
decided ;; Deciding || host ;; Social_event || appoint ;; Change_of_leadership || match ;; Competition || hold ;; Hold
listed ;; Arranging
sinking ;; Catastrophe || take ;; Bringing
built ;; Building
crammed ;; Filling
failed ;; Earnings_and_losses || departure ;; Departing || arrival ;; Arriving
towed ;; Self_motion || leave ;; Departing || enter ;; Arriving
towed ;; Self_motion
killing ;; Killing || torpedoed ;; Attack || disaster ;; Catastrophe || making it ;; Causation
revolt ;; Change_of_leadership || joined ;; Becoming_a_member || sunk ;; Self_motion || laden ;; Bringing
damaging ;; Damaging || struck ;; Attack || typhoon ;; Catastrophe
storm ;; Catastrophe
moved ;; Motion || formed ;; Coming_to_be || turning ;; Becoming || curving ;; Motion_directional
intensified ;; Cause_change_of_strength
becoming ;; Becoming || landfall ;; Self_motion || hit ;; Attack
landfall ;; Self_motion
dissipated ;; Dispersal
affected ;; Influence || bringing ;; Bringing
dropped ;; Motion_directional
evacuated ;; Emptying || effects ;; Influence || heightened ;; Cause_change_of_position_on_a_scale
caused ;; Causation || damage ;; Damaging || flooding ;; Motion
damage ;; Damaging || inundating ;; Motion || reached ;; Arriving
caused ;; Causation || evacuate ;; Removing
dropped ;; Motion_directional || reaching ;; Arriving
caused ;; Causation || killing ;; Killing
wrecked ;; Destroying || flooded ;; Filling
damage ;; Damaging || retired ;; GiveUp
concert ;; Social_event || recording ;; Recording
held ;; Hold || marked ;; Recording
concert ;; Social_event || beginning ;; Process_start || covering ;; Filling || hits ;; Body_movement || performed ;; Action
arranged ;; Arranging || show ;; Social_event || performed ;; Action
concert ;; Social_event || served ;; Giving || perform ;; Action
concert ;; Social_event || concert ;; Social_event || sold ;; Commerce_sell || concert ;; Social_event || concert ;; Social_event || realised ;; Coming_to_believe
received ;; Receiving || concert ;; Social_event || acclaim ;; Judgment_communication || praised ;; Judgment_communication
fought ;; Hostile_encounter || Battle ;; Hostile_encounter || battles ;; Hostile_encounter || Battle ;; Hostile_encounter
defensive ;; Defending || battle ;; Hostile_encounter
defeat ;; Conquering || led ;; Conquering || commanded ;; Control
defensive ;; Defending || defending ;; Defending || eliminated ;; Killing || obliterated ;; Killing
battle ;; Hostile_encounter || eradicate ;; Removing || burnt ;; Filling
committed ;; Commitment
battle ;; Hostile_encounter || fought ;; Hostile_encounter || enter ;; Arriving
Battle ;; Hostile_encounter || caught ;; Self_motion
occurred ;; Presence
cooperated ;; Collaboration || advance ;; Cause_to_make_progress || retreat ;; Escaping
hold off ;; Defending || posted ;; Placing
drew ;; Motion
charged ;; Attack || cut ;; Cause_change_of_position_on_a_scale
surrender ;; Surrendering || forced ;; Motion || trapped ;; Filling || cut ;; Cause_change_of_position_on_a_scale
abandoned ;; GiveUp
hurricane ;; Catastrophe || impacted ;; Influence
strike ;; Attack || hurricanes ;; Catastrophe
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe
Moving ;; Motion || skirted ;; Motion_directional || turning ;; Becoming
strengthened ;; Cause_change_of_strength || storm ;; Catastrophe || reach ;; Arriving || hurricane ;; Catastrophe || attained ;; Getting
intensified ;; Cause_change_of_strength || reached ;; Arriving || weakening ;; Cause_change_of_strength
dissipated ;; Dispersal || transitioned ;; Change
caused ;; Causation || damage ;; Damaging
caused ;; Causation || flooding ;; Motion || brushed ;; Filling
affected ;; Influence
cut off ;; Having_or_lacking_access || resulted in ;; Causation
deaths ;; Death || damage ;; Damaging
evacuations ;; Escaping || struck ;; Attack
caused ;; Causation
became ;; Becoming || caused ;; Causation || hurricane ;; Catastrophe || hurricane ;; Catastrophe || flooding ;; Motion
deaths ;; Death || drownings ;; Bodily_harm
damaged ;; Damaging
hurricane ;; Catastrophe
observed ;; Know
passing ;; Motion_directional || reaching ;; Achieve || tracked ;; Motion
weakening ;; Cause_change_of_strength || passed ;; Motion_directional
regained ;; Recovering || landfall ;; Arriving || emerged ;; Coming_to_be || made ;; Bringing
crossed ;; Motion_directional || Turning ;; Self_motion || entered ;; Arriving
evacuations ;; Escaping || storm ;; Catastrophe || boarded ;; Placing || recommended ;; Convincing
reached ;; Causation || killed ;; Killing
struck ;; Attack
deaths ;; Death || included ;; Cause_to_be_included
characterized ;; Action || brought ;; Causation
storm ;; Catastrophe || flooded ;; Filling
damage ;; Damaging || progressed ;; Self_motion || suffered ;; Bodily_harm
killed ;; Killing
rivalry ;; Competition
Starting ;; Process_start || played ;; Participation
lost ;; Earnings_and_losses
matches ;; Competition || expressed ;; Expressing_publicly
retained ;; Preserving
earning ;; Earnings_and_losses
stifle ;; Hindering || pace ;; Self_motion || struggled ;; Confronting_problem || attack ;; Hostile_encounter
struggling ;; Confronting_problem || collapsing ;; Motion_directional
aided ;; Assistance || series ;; Competition || dropped ;; Earnings_and_losses || bowled ;; Body_movement
lose ;; Earnings_and_losses
competition ;; Competition || Final ;; Competition || Cup ;; Competition || Cup ;; Competition
contested ;; Competition || match ;; Competition || played ;; Competition
entered ;; Becoming_a_member
given ;; Giving || retained ;; Preserving
ended ;; Process_end || finished ;; Process_end || win ;; Getting
preserving ;; Preserving || developing ;; Cause_to_make_progress
started ;; Process_start
inaugurated ;; Social_event
inaugurated ;; Social_event
competition ;; Competition
competition ;; Competition || played ;; Competition
competition ;; Competition || winning ;; Earnings_and_losses
abandoned ;; GiveUp || scheduled ;; Arranging || maintained ;; Preserving
tournament ;; Competition
funded ;; Supply
cyclone ;; Catastrophe
storm ;; Catastrophe
developed ;; Coming_to_be || cyclone ;; Catastrophe || cyclone ;; Catastrophe
became ;; Becoming || became ;; Becoming || Convection ;; Motion || storm ;; Catastrophe
developing ;; Coming_to_be || strengthened ;; Cause_change_of_strength || attained ;; Getting || storm ;; Catastrophe
storm ;; Catastrophe || maintaining ;; Preserving || interacted ;; Communication || weakened ;; Cause_change_of_strength
lost ;; Earnings_and_losses
wrestling ;; Competition || produced ;; Creating || took place ;; Process_start
held ;; Hold || held ;; Hold
schedule ;; Arranging
wrestling ;; Competition
ended up ;; Process_end || winning ;; Getting || retaining ;; Getting
defended ;; Defending
retained ;; Getting
fought ;; Hostile_encounter
contest ;; Competition
defense ;; Defending || promoted ;; Cause_to_make_progress
retaining ;; Preserving
given ;; Giving || given ;; Giving || held ;; Hold || wrestling ;; Competition
became ;; Becoming || Wrestling ;; Competition || stream ;; Motion || demand ;; Request || launch ;; Process_start
killing ;; Killing || crashed ;; Catastrophe || scheduled ;; Hold || take-off ;; Motion
became ;; Becoming || terrorist ;; Terrorism || incidents ;; Catastrophe || accident ;; Catastrophe || accident ;; Catastrophe || disaster ;; Catastrophe || air ;; Expressing_publicly || remains ;; Temporary_stay
marks ;; Recording || death ;; Death || involving ;; Participation
crashed ;; Catastrophe || suffered ;; Bodily_harm
blamed ;; Judgment_communication || inquest ;; Scrutiny || failing ;; Earnings_and_losses
resolved ;; Resolve_problem || noting ;; Statement || take-off ;; Motion || cited ;; Statement
caused ;; Causation || crash ;; Catastrophe || strike ;; Attack || took place ;; Process_start
causing ;; Causation || strike ;; Attack || disrupted ;; Hindering || services ;; Supply
led to ;; Causation || inquiry ;; Scrutiny
allowing ;; Preventing_or_letting || recommendation ;; Convincing || occupy ;; Hold
felt ;; Coming_to_believe
tour ;; Traveling
visited ;; Traveling || supported ;; Supporting || tour ;; Traveling
tour ;; Traveling || finished ;; Process_end || disbanded ;; Dispersal
aired ;; Expressing_publicly
shown ;; Presence
select ;; Choosing
made ;; Creating
earnings ;; Earnings_and_losses
tour ;; Traveling || consisted ;; Cause_to_be_included
sold ;; Commerce_sell
released ;; Publishing || tour ;; Traveling
tour ;; Traveling || produced ;; Manufacturing
produced ;; Manufacturing || season ;; Process_start
trained ;; Education_teaching || participating ;; Participation
coached ;; Education_teaching
kicked off ;; Process_start
fought ;; Hostile_encounter || aired ;; Expressing_publicly
concluded ;; Process_end || premiered ;; Getting
began ;; Process_start
crisis ;; Catastrophe || siege ;; Besieging
rescue ;; Rescuing || broadcast ;; Expressing_publicly
began ;; Process_start || dead ;; Death || incident ;; Catastrophe || purge ;; Killing || broke ;; Bodily_harm || taking ;; Conquering
standoff ;; Hostile_encounter || took place ;; Process_start
enabled ;; Preventing_or_letting || constructed ;; Building
stormed ;; Attack
killed ;; Killing || rescued ;; Rescuing || assault ;; Attack || taken ;; Bringing
decline ;; Cause_change_of_position_on_a_scale || incident ;; Catastrophe || contributed ;; Causation
produced ;; Manufacturing || took place ;; Process_start || wrestling ;; Competition || event ;; Social_event
event ;; Social_event
match ;; Competition || occurred ;; Presence || matches ;; Competition || aired ;; Expressing_publicly
matches ;; Competition || occur ;; Presence || concept ;; Statement
competitors ;; Competition || defending ;; Defending || challengers ;; Competition || match ;; Competition
defended ;; Defending
defended ;; Defending || match ;; Competition
defended ;; Defending || defended ;; Defending || competed ;; Competition || match ;; Competition
received ;; Receiving || earned ;; Earnings_and_losses || increase ;; Cause_change_of_position_on_a_scale || event ;; Social_event
received ;; Receiving || increased ;; Cause_change_of_position_on_a_scale || criticising ;; Judgment_communication || weak ;; Cause_change_of_strength || consisting ;; Cause_to_be_included || event ;; Social_event
founded ;; Creating
complete ;; Process_end || using ;; Using || launched ;; Process_start
offering ;; Supply || played ;; Competition || stand out ;; Action
captured ;; Conquering || following ;; Self_motion || generating ;; Creating
announced ;; Expressing_publicly
premiered ;; Process_start
announced ;; Expressing_publicly || formed ;; Coming_to_be || develop ;; Coming_to_be
Beginning ;; Process_start || broadcast ;; Expressing_publicly
commenced ;; Process_start || called ;; Name_conferral || set ;; Placing || inked ;; Writing
debuted ;; Process_start
marked ;; Recording
besieged ;; Besieging || allowed ;; Preventing_or_letting || enter ;; Arriving
alleged ;; Statement || find ;; Know || attacked ;; Attack || acquire ;; Getting
organized ;; Arranging
died ;; Death
marked ;; Recording
verified ;; Check || destroyed ;; Destroying || claimed ;; Statement
led to ;; Causation || carried out ;; Action
operating ;; Being_in_operation || crashed ;; Catastrophe || suffered ;; Bodily_harm
caused ;; Causation || investigation ;; Scrutiny || concluded ;; Process_end || landing ;; Self_motion
repaired ;; Recovering || compromising ;; Influence
caused ;; Causation || controls ;; Control || causing ;; Causation || expand ;; Expansion || loss ;; Earnings_and_losses || failed ;; Earnings_and_losses || ripped ;; Removing
carrying ;; Carry_goods
included ;; Cause_to_be_included
died ;; Death || injuries ;; Bodily_harm || go to ;; Motion || offer ;; Supply || start ;; Process_start || rescue ;; Rescuing || declining ;; Agree_or_refuse_to_act
carried out ;; Action || airstrike ;; Attack || damaged ;; Damaging
commenced ;; Process_start || War ;; Hostile_encounter
refueled ;; Self_motion
crossing ;; Motion_directional || climbed ;; Self_motion
avoid ;; Preventing_or_letting || detection ;; Scrutiny || peeled off ;; Wearing || dropped ;; Motion_directional
flew ;; Motion
attack ;; Attack
attack ;; Attack
destroyed ;; Destroying || airstrike ;; Attack || called ;; Name_conferral
crashed ;; Catastrophe || route ;; Sending
accident ;; Catastrophe
investigation ;; Scrutiny || calls ;; Request || inquiry ;; Scrutiny || dismissed ;; Agree_or_refuse_to_act || carried out ;; Action
found ;; Know || accident ;; Catastrophe || caused ;; Causation || error ;; Causation
found ;; Know || warning ;; Warning || equipment ;; Supply || failure ;; Causation || monitor ;; Scrutiny || lack ;; Causation
crash ;; Catastrophe || review ;; Scrutiny || recommendations ;; Convincing || sparked ;; Causation
warnings ;; Warning
remained ;; Temporary_stay || accident ;; Catastrophe || fatalities ;; Death || crashed ;; Catastrophe
involved ;; Cause_to_be_included
carrying ;; Carry_goods || landing ;; Self_motion || repel ;; Removing
attacked ;; Attack || sailing ;; Self_motion || unloading ;; Removing
killing ;; Killing || strike ;; Attack
broken up ;; Destroying || damage ;; Damaging || suffered ;; Bodily_harm
gained ;; Getting || taken ;; Conquering
court-martialed ;; Judgment_communication || expelled ;; Removing
attack ;; Attack || took place ;; Process_start
wounded ;; Bodily_harm || attackers ;; Attack || terrorist ;; Terrorism || attack ;; Attack || killed ;; Killing
surveillance ;; Scrutiny || destroyed ;; Destroying
planned ;; Arranging || attack ;; Attack || attack ;; Attack || attack ;; Attack
attack ;; Attack || attack ;; Attack || believed ;; Know
service ;; Supply || counter-attack ;; Attack || carried out ;; Military_operation
started ;; Process_start || conflicts ;; Hostile_encounter
began ;; Process_start || fighting ;; Hostile_encounter || called ;; Name_conferral
fighting ;; Hostile_encounter || control ;; Control
ousted ;; Change_of_leadership || handling ;; Resolve_problem || election ;; Choosing || taken place ;; Process_start
control ;; Control || calling ;; Name_conferral || suspended ;; Process_end
following ;; Self_motion || overrun ;; Control
called ;; Name_conferral || capture ;; Conquering || accomplished ;; Achieve || said ;; Telling || offensive ;; Attack
renaming ;; Name_conferral || proclaimed ;; Statement
began ;; Process_start || driven ;; Self_motion
conflicting ;; Quarreling || struggled ;; Hostile_encounter
began ;; Process_start || fighting ;; Hostile_encounter
control ;; Control || lost ;; Earnings_and_losses
help ;; Assistance || asked ;; Request
began ;; Process_start
deployed ;; Arranging
help ;; Assistance
accused ;; Judgment_communication || fight ;; Hostile_encounter || attacks ;; Attack
commitments ;; Commitment || deal ;; Resolve_problem || signed on ;; Sign_agreement || pulled out ;; GiveUp || claimed ;; Statement
withdrawal ;; Escaping
terrorist ;; Terrorism || attacks ;; Attack || occur ;; Coming_to_be || signed on ;; Sign_agreement
fighting ;; Hostile_encounter || signing ;; Sign_agreement
standoff ;; Hostile_encounter || resulted in ;; Causation
responding ;; Response || buildup ;; Hostile_encounter || initiated ;; Process_start || killed ;; Killing || terrorist ;; Terrorism || attack ;; Terrorism
claimed ;; Statement || attacks ;; Terrorism || said ;; Statement || charge ;; Judgment_communication || carried out ;; Action || fighting ;; Hostile_encounter || denied ;; Agree_or_refuse_to_act
standoff ;; Hostile_encounter || focused ;; Aiming
mediation ;; Resolve_problem || resulted in ;; Causation || withdrawal ;; Removing
Cup ;; Competition
began ;; Process_start || concluded ;; Process_end || held ;; Hold
began ;; Process_start || advanced ;; Cause_to_make_progress || tournament ;; Competition || involved ;; Participation || joined ;; Participation
reduced ;; Cause_change_of_position_on_a_scale || arriving ;; Arriving || attack ;; Terrorism || withdrawal ;; Departing
games ;; Competition || played ;; Competition
beating ;; Competition || tournament ;; Competition
massacre ;; Killing || killings ;; Killing || terrorist ;; Terrorism || carried out ;; Military_operation
began ;; Process_start || joint ;; Collaboration || overran ;; Control || attack ;; Attack
killings ;; Killing || culminated ;; Process_end || committed ;; Committing_crime || forced ;; Control
reported ;; Reporting || Fatalities ;; Death
killed ;; Killing || killed ;; Killing || reported ;; Reporting || reported ;; Reporting || fatalities ;; Death || said ;; Statement
claiming ;; Statement || repelled ;; Defending || massacre ;; Killing || fatalities ;; Death || reported ;; Reporting || claim ;; Statement || refuted ;; Judgment_communication || taken place ;; Process_start
destroyed ;; Destroying || reported ;; Reporting || displaced ;; Bodily_harm || drowned ;; Motion
said ;; Statement || claimed ;; Statement || saying ;; Statement || resulted in ;; Causation || massacre ;; Killing || statement ;; Statement || control ;; Control || stop ;; Preventing_or_letting || attacks ;; Attack || extending ;; Cause_change_of_position_on_a_scale
bombings ;; Terrorism || occurred ;; Coming_to_be
killed ;; Killing || injured ;; Bodily_harm
caused ;; Causation || exploded ;; Attack
left ;; Causation
destroyed ;; Destroying || collapsed ;; Destroying
targeted ;; Aiming || wounded ;; Bodily_harm
killing ;; Killing || wounding ;; Bodily_harm || exploded ;; Attack
discovered ;; Know || made ;; Change
summit ;; Social_event
adopted ;; Choosing || following ;; Motion || adopted ;; Choosing
terrorism ;; Terrorism || agreed ;; Agree_or_refuse_to_act || defense ;; Defending || addressed ;; Resolve_problem || accepting ;; Agree_or_refuse_to_act || attacks ;; Attack || develop ;; Coming_to_be
operations ;; Participation || met with ;; Come_together
agreed ;; Agree_or_refuse_to_act || forces ;; Causation || withdraw ;; Removing
reaffirmed ;; Statement || forces ;; Causation || provide ;; Supply || advice ;; Supply
held ;; Hold || deemed ;; Legal_rulings
awarded ;; Giving
hosted ;; Social_event
finished ;; Process_end || granting ;; Preventing_or_letting
opened ;; Process_start
sent ;; Sending || dismissed ;; Response
sent ;; Sending || resulted in ;; Causation || scuffle ;; Hostile_encounter || end ;; Process_end || marred ;; Damaging
launched ;; Process_start || following ;; Action
called ;; Statement || established ;; Creating
ordered ;; Request || powers ;; Influence || dissolving ;; Dispersal || vested ;; Giving || operate ;; Being_in_operation
declared ;; Expressing_publicly || repealed ;; Removing || banned ;; Preventing_or_letting || arrested ;; Arrest || imposed ;; Action || save ;; Placing || detained ;; Arrest || control ;; Control
power ;; Influence || granting ;; Giving
elected ;; Change_of_leadership || legislature ;; Legal_rulings || established ;; Creating
uprising ;; Change_of_leadership || involving ;; Cause_to_be_included || took place ;; Process_start
took place ;; Process_start
reacting ;; Response || widespread ;; Dispersal
involved ;; Cause_to_be_included
implicated ;; Influence
crushed ;; Destroying
killed ;; Killing || fighting ;; Hostile_encounter
death ;; Death || sentenced ;; Legal_rulings
deported ;; Removing || following ;; Motion
died ;; Death || death ;; Death || cause ;; Causation
strengthened ;; Cause_change_of_strength || death ;; Death
declared ;; Expressing_publicly
dedicated ;; Giving
constructed ;; Building
became ;; Becoming || led to ;; Causation
proposed ;; Arranging || generating ;; Creating
impacted ;; Influence || joins ;; Becoming_a_member
listed ;; Cause_to_be_included
building ;; Building || led to ;; Causation
occupied ;; Control || leading to ;; Causation
became ;; Becoming || assisted ;; Assistance || helped ;; Assistance || election ;; Choosing
built ;; Building || promised ;; Commitment || stop ;; Preventing_or_letting
ruling ;; Legal_rulings || resulting in ;; Causation
conflict ;; Hostile_encounter || took place ;; Process_start
started ;; Process_start || viewed ;; Perception_active || war ;; Military_operation
ended ;; Process_end || war ;; Military_operation
supplied ;; Supply || led ;; Causation
breakup ;; Destroying || war ;; Military_operation
inhabited ;; Come_together || referendum ;; Request
rejected ;; Agree_or_refuse_to_act || boycotted ;; Agree_or_refuse_to_act
spread ;; Dispersal || declaration ;; Expressing_publicly || secure ;; Getting || mobilised ;; Arranging
transformed ;; Change || conflict ;; Hostile_encounter
increased ;; Cause_change_of_position_on_a_scale || escalated ;; Process_start
War ;; Military_operation || perpetrated ;; Committing_crime
became ;; Becoming || conflict ;; Hostile_encounter
provided ;; Supply || lost ;; Earnings_and_losses || allied ;; Collaboration
targeting ;; Aiming || defied ;; Agree_or_refuse_to_act || airlifted ;; Supply || intervened ;; Hindering || ending ;; Process_end
signing ;; Sign_agreement || war ;; Military_operation || end ;; Process_end
negotiations ;; Communication || finalised ;; Process_end
war ;; Military_operation || convicted ;; Legal_rulings
killed ;; Killing || war ;; Military_operation
conflict ;; Hostile_encounter
raped ;; Bodily_harm
concert ;; Social_event || take ;; Creating
promoted ;; Cause_to_make_progress
began ;; Process_start || finished ;; Process_end
seen ;; Perception_active || making ;; Creating
became ;; Becoming || selling ;; Commerce_sell || selling ;; Commerce_sell
recording ;; Recording || support ;; Supporting || concert ;; Social_event
confirmed ;; Check || tour ;; Traveling || discussed ;; Communication
decided ;; Deciding || promote ;; Cause_change_of_position_on_a_scale || touring ;; Traveling || tour ;; Traveling || left ;; Departing
started ;; Process_start || set ;; Placing || supporting ;; Supporting
inspired ;; Influence || appearing ;; Presence || incorporated ;; Cause_to_amalgamate
garnered ;; Earnings_and_losses
prompting ;; Causation || add ;; Cause_to_be_included || announced ;; Expressing_publicly || sold ;; Commerce_sell
achieved ;; Achieve || decided ;; Deciding || negotiations ;; Communication || touring ;; Traveling || visit ;; Traveling
released ;; Publishing || concert ;; Social_event || tour ;; Traveling
took ;; Conquering || tour ;; Traveling || suffering ;; Catastrophe
begin ;; Process_start || recover ;; Recovering || traveled ;; Traveling
held ;; Hold
held ;; Hold
offered ;; Giving
offered ;; Giving || competitions ;; Competition
competitions ;; Competition || competed ;; Competition || offered ;; Supply
competition ;; Competition || included ;; Cause_to_be_included || sell ;; Commerce_sell || displayed ;; Perception_active || see ;; Perception_active || hosted ;; Social_event
included ;; Cause_to_be_included
produced ;; Creating
took place ;; Process_start
event ;; Competition || take place ;; Hold
sold ;; Commerce_sell
contested ;; Competition
effect ;; Influence
teamed ;; Cause_to_amalgamate || defeated ;; Conquering
defeat ;; Conquering
win ;; Getting || become ;; Becoming
held ;; Defending || festival ;; Social_event
festival ;; Social_event || includes ;; Cause_to_be_included
festival ;; Social_event || organised ;; Arranging
grown ;; Expansion || festival ;; Social_event
sold ;; Commerce_sell
organised ;; Arranging || festival ;; Social_event || festival ;; Social_event
organised ;; Arranging
called ;; Name_conferral || interviews ;; Communication || festival ;; Social_event || releases ;; Publishing
viewed ;; Perception_active
tour ;; Traveling || concert ;; Social_event
support ;; Supporting
joining ;; Becoming_a_member
include ;; Cause_to_be_included || tour ;; Traveling
selling ;; Commerce_sell || bringing ;; Bringing || tickets ;; Preventing_or_letting || tour ;; Traveling
tour ;; Traveling || tour ;; Traveling
tour ;; Traveling || co-headlined ;; Cause_to_amalgamate
tour ;; Traveling || saw ;; Motion || play ;; Action || return ;; Motion
tour ;; Traveling || included ;; Cause_to_be_included || co-headline ;; Cause_to_amalgamate
tour ;; Traveling
tour ;; Traveling
released ;; Publishing || supported ;; Supporting || promoted ;; Cause_to_make_progress
ended ;; Process_end || started ;; Process_start
included ;; Cause_to_be_included
included ;; Cause_to_be_included
attended ;; Participation
tour ;; Traveling || sold ;; Commerce_sell
concert ;; Social_event || support ;; Supporting || released ;; Publishing
traveled ;; Traveling
began ;; Process_start || starting ;; Process_start || finishing ;; Process_end
commenced ;; Process_start || culminated ;; Process_end
beginning ;; Process_start || played ;; Competition
played ;; Competition
began ;; Process_start || start ;; Process_start
performed ;; Hold || concert ;; Social_event || sold ;; Commerce_sell || concluded ;; Process_end
recorded ;; Recording || release ;; Publishing || issued ;; Creating || released ;; Publishing
included ;; Cause_to_be_included || documenting ;; Recording || tour ;; Traveling || re-released ;; Publishing
show ;; Social_event
show ;; Social_event || held ;; Hold || attended ;; Participation
claims ;; Statement
held ;; Hold || Established ;; Creating
sales ;; Commerce_sell || fair ;; Social_event || attended ;; Participation
starts ;; Process_start || opened ;; Openness
organised ;; Arranging
Festival ;; Social_event || fair ;; Social_event
run ;; Being_in_operation
open ;; Process_start
taking place ;; Process_start
performances ;; Hold
shows ;; Social_event || take place ;; Process_start
cast ;; Motion
selling ;; Commerce_sell
selling ;; Commerce_sell || provide ;; Supply
festival ;; Social_event || selection ;; Choosing
run ;; Self_motion
devised ;; Creating
risen ;; Cause_change_of_position_on_a_scale
become ;; Becoming || advertised ;; Commerce_sell
incorporated ;; Cause_to_amalgamate
holds ;; Hold
bettered ;; Cause_to_make_progress
produced ;; Manufacturing
took place ;; Process_start
produced ;; Manufacturing || held ;; Hold
sold ;; Commerce_sell
held ;; Hold
starting ;; Process_start || finishing ;; Process_end || courses ;; Motion
held ;; Hold
includes ;; Cause_to_be_included || run ;; Self_motion || race ;; Competition
races ;; Competition || attracting ;; Influence || marketed ;; Commerce_sell
participate ;; Participation
released ;; Publishing
preceded ;; Process_start
produced ;; Manufacturing
sold ;; Commerce_sell || certified ;; Check
reached ;; Achieve
debuted ;; Process_start
crashed ;; Catastrophe
killed ;; Killing
ended ;; Process_end || originated ;; Coming_to_be
began ;; Process_start || departed ;; Departing
reached ;; Arriving
departed ;; Departing || delayed ;; Change_event_time
began ;; Process_start || descended ;; Motion_directional || lost ;; Earnings_and_losses || takeoff ;; Departing || turn ;; Motion
descended ;; Motion_directional || pulled ;; Motion || turned ;; Motion || reached ;; Arriving
departed ;; Departing || developed ;; Coming_to_be || determined ;; Deciding
prevented ;; Preventing_or_letting || pinpointing ;; Aiming || found ;; Know
maintained ;; Preserving
provided ;; Supply || leak ;; Motion
inspect ;; Scrutiny || maintain ;; Preserving
recommended ;; Convincing || caused ;; Causation || reduce ;; Cause_change_of_position_on_a_scale || allow ;; Preventing_or_letting || relocated ;; Removing
recommended ;; Convincing || modified ;; Change || allow ;; Preventing_or_letting
occurred ;; Coming_to_be || crashed ;; Catastrophe
befell ;; Self_motion
sold ;; Commerce_sell
used ;; Using
acquired ;; Getting || existed ;; Presence
marked ;; Recording
scheduled ;; Arranging || took place ;; Process_start
making ;; Manufacturing || sold ;; Commerce_sell
breaking ;; Bodily_harm || grossed ;; Earnings_and_losses
topped ;; Placing
released ;; Publishing
preceded ;; Process_start
marked ;; Recording
released ;; Publishing || renew ;; Recovering || deciding ;; Deciding || signed ;; Sign_agreement
produced ;; Manufacturing
becoming ;; Becoming || debuted ;; Process_start || peaked ;; Cause_change_of_position_on_a_scale
sales ;; Commerce_sell || certified ;; Check
losing ;; Earnings_and_losses || crash-landed ;; Damaging
survived ;; Rescuing
crash ;; Catastrophe
causing ;; Causation || overspeed ;; Motion || leading to ;; Causation || loss ;; Earnings_and_losses || found ;; Coming_to_believe || retarded ;; Cause_change_of_position_on_a_scale
prevented ;; Preventing_or_letting || installed ;; Placing
became ;; Becoming || Installation ;; Building
included ;; Cause_to_be_included
take place ;; Process_start
end ;; Process_end || trailed ;; Self_motion
starting ;; Process_start || ending ;; Process_end
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
met ;; Come_together || plan ;; Arranging || presence ;; Presence
decided ;; Deciding || put ;; Placing || allying ;; Collaboration || battle ;; Hostile_encounter || war ;; Hostile_encounter || waged ;; Hold || assert ;; Statement
resolve ;; Resolve_problem || battle ;; Hostile_encounter || descent ;; Motion_directional || defeat ;; Death || ended ;; Process_end || approach ;; Arriving
recognized ;; Know || resulted ;; Causation || dominate ;; Control || giving ;; Giving || putting an end to ;; Process_end || attempt ;; Self_motion
battle ;; Hostile_encounter || victory ;; Earnings_and_losses || reads ;; Statement
mentioned ;; Statement || battle ;; Hostile_encounter
commemorate ;; Social_event || takes place ;; Process_start || battle ;; Hostile_encounter
chosen ;; Choosing
started ;; Process_start
promote ;; Cause_change_of_position_on_a_scale
moved ;; Motion
toured ;; Traveling
end ;; Process_end
performed ;; Hold
made ;; Causation
lifted ;; Process_end || entered ;; Arriving
competition ;; Competition || sparked ;; Causation || reached ;; Arriving || forced ;; Request
eliminated ;; Conquering
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
attacked ;; Attack
left ;; Departing
attack ;; Attack || assembled ;; Come_together
attacked ;; Attack
bringing ;; Motion
flee ;; Escaping
allowed ;; Preventing_or_letting || row ;; Motion
dismasted ;; Process_end || forced ;; Causation || strike ;; Process_end
escape ;; Escaping || enabled ;; Preventing_or_letting
captured ;; Conquering
escaped ;; Escaping || damage ;; Damaging
held ;; Hold || Championship ;; Competition
win ;; Getting
championship ;; Competition
turning ;; Becoming || win ;; Getting
opened ;; Process_start || record ;; Recording
hosted ;; Social_event || Championship ;; Competition
defending ;; Defending || stopped ;; Preventing_or_letting || ending ;; Process_end
shows ;; Presence || held ;; Hold || took place ;; Process_start
cancellation ;; Removing || force ;; Request || hold ;; Hold
matches ;; Competition || shows ;; Presence || show ;; Presence
match ;; Competition || feud ;; Quarreling || conclusion ;; Process_end
matches ;; Competition || matches ;; Competition || appearing ;; Coming_to_be
included ;; Cause_to_be_included
match ;; Competition || matches ;; Competition || featuring ;; Arranging || featuring ;; Arranging || held ;; Hold
shows ;; Presence || appear ;; Presence
change ;; Change || saw ;; Causation
match ;; Competition || announced ;; Expressing_publicly || losing ;; Earnings_and_losses || returning ;; Arriving
used ;; Using || match ;; Competition || doubts ;; Quarreling || appearance ;; Presence || win ;; Getting
flared ;; Process_start || appeared ;; Coming_to_be || feud ;; Quarreling
reached ;; Arriving || match ;; Competition || match ;; Competition || building ;; Creating || feud ;; Quarreling || defeated ;; Preventing_or_letting || forcing ;; Causation
development ;; Coming_to_be || held ;; Hold || appeared ;; Coming_to_be || turning on ;; Attack
teamed up ;; Cause_to_amalgamate || show ;; Presence || walked ;; Motion
claims ;; Statement
tournament ;; Competition || saw ;; Causation || host ;; Response
tournaments ;; Competition || split ;; Change
match ;; Competition || final ;; Process_end || qualify ;; Earnings_and_losses || defeated ;; Conquering
become ;; Becoming || qualify ;; Earnings_and_losses || defeated ;; Conquering
tournament ;; Competition
matches ;; Competition || protested ;; Judgment_communication || played ;; Competition
banned ;; Preventing_or_letting
matches ;; Competition || played ;; Competition
tournament ;; Competition || due ;; Causation || withdrew ;; Removing
withdrew ;; Removing || stayed ;; Temporary_stay
withdrawal ;; Removing
tournament ;; Competition || tournament ;; Competition || opposed ;; Agree_or_refuse_to_act || entered ;; Becoming_a_member
tournament ;; Competition || entered ;; Becoming_a_member
meet ;; Come_together || occurred ;; Coming_to_be || enabling ;; Preventing_or_letting || final ;; Competition
knockout ;; Competition || topped ;; Placing || progressed ;; Achieve
meet ;; Come_together || beat ;; Competition || beat ;; Competition || final ;; Competition
final ;; Competition
match ;; Competition || finished ;; Process_end || finished ;; Process_end || beating ;; Competition
bushfire ;; Catastrophe || bushfire ;; Catastrophe
fires ;; Catastrophe || bushfire ;; Catastrophe || burning ;; Destroying || burning ;; Destroying
Bushfires ;; Catastrophe
included ;; Cause_to_be_included || bushfires ;; Catastrophe || Fire ;; Catastrophe
caused ;; Causation || merged ;; Cause_to_amalgamate || become ;; Becoming || fires ;; Catastrophe || burned ;; Destroying
contained ;; Containing || fires ;; Catastrophe || burnt ;; Bodily_harm
implemented ;; Action || resulted in ;; Causation || fires ;; Catastrophe || fatality ;; Death
concert ;; Social_event || tour ;; Traveling || Tour ;; Traveling
support ;; Supporting || launched ;; Process_start || debut ;; Process_start
ended ;; Process_end || started ;; Process_start
tour ;; Traveling
made ;; Manufacturing || tour ;; Traveling
revolution ;; Change_of_leadership || revolution ;; Change_of_leadership || tours ;; Traveling || tour ;; Traveling || tour ;; Traveling || tour ;; Traveling || supporting ;; Supporting
intervention ;; Hostile_encounter || War ;; Military_operation || expeditions ;; Attack
supplies ;; Supply || secure ;; Assistance || help ;; Assistance || re-establish ;; Recovering
Overthrow ;; Change_of_leadership
withdrew ;; Escaping || War ;; Military_operation
hampered ;; Hindering || conflict ;; Hostile_encounter || support ;; Supporting
withdraw ;; Escaping || occupied ;; Control || evacuation ;; Escaping || compelled ;; Influence
attacks ;; Attack || attacks ;; Attack || took place ;; Process_start
began ;; Process_start || alleged ;; Statement || attacks ;; Attack || attacks ;; Attack || attacked ;; Attack || blamed ;; Judgment_communication || held ;; Defending
destruction ;; Destroying || attacks ;; Attack || burning ;; Bodily_harm || resulted in ;; Change
attacks ;; Attack || killed ;; Killing || injured ;; Bodily_harm
killed ;; Killing || unidentified ;; Check
known ;; Know || comprises ;; Containing
competition ;; Competition
allows ;; Preventing_or_letting || compete ;; Competition || competitions ;; Competition
ends ;; Process_end || deciding ;; Deciding || season ;; Competition || competition ;; Competition
participating ;; Participation || season ;; Competition
competition ;; Competition
competition ;; Competition
organise ;; Arranging || featuring ;; Cause_to_be_included || scheduled ;; Process_start || take place ;; Process_start || forced ;; Request
take part ;; Dispersal
Tour ;; Competition || Tour ;; Competition
started ;; Process_start || finished ;; Process_end
games ;; Competition || games ;; Competition || playing ;; Participation || played ;; Competition
consists ;; Containing || Tournament ;; Competition
final ;; Process_end || game ;; Competition || replayed ;; Competition
tournament ;; Competition
Carried ;; Carry_goods
prevent ;; Preventing_or_letting || destroy ;; Destroying
employing ;; Employment || captured ;; Conquering || retreat ;; Escaping || destroyed ;; Destroying
opening ;; Openness || failed ;; Earnings_and_losses || regrouped ;; Cause_to_amalgamate || operation ;; Military_operation
helped ;; Assistance || led to ;; Causation
tournament ;; Competition || tournament ;; Competition || held ;; Hold || held ;; Hold
tournament ;; Competition || inspired ;; Influence || tournament ;; Competition
worked ;; Employment || elimination ;; Removing || tournament ;; Competition || tournament ;; Competition || invited ;; Request
match ;; Competition || changed ;; Change || composed ;; Creating
participated ;; Participation
defeated ;; Conquering || win ;; Getting
Battle ;; Hostile_encounter || fought ;; Hostile_encounter || War ;; Hostile_encounter
aim ;; Aiming || drive ;; Motion || positions ;; Placing
assaults ;; Attack || barrages ;; Attack || failed ;; Earnings_and_losses || employed ;; Employment
fighting ;; Hostile_encounter || resist ;; Defending || assault ;; Attack
successes ;; Achieve
took ;; Conquering || took ;; Conquering || captured ;; Conquering
dislodge ;; Removing || launching ;; Motion || Offensive ;; Attack || form ;; Coming_to_be
fighting ;; Hostile_encounter || occurred ;; Presence
created ;; Creating || defenses ;; Defending
fighting ;; Hostile_encounter || combat ;; Hostile_encounter || consisted ;; Cause_to_be_included
get ;; Getting || advance ;; Self_motion || driven ;; Motion
retook ;; Control || retook ;; Control
received ;; Receiving || breaking ;; Bodily_harm
conquered ;; Conquering || conquered ;; Conquering || mount ;; Motion_directional || gains ;; Earnings_and_losses
occurred ;; Coming_to_be || brawl ;; Bodily_harm || fight ;; Hostile_encounter
confirmed ;; Check || killed ;; Killing || injured ;; Bodily_harm
started ;; Process_start || banned ;; Preventing_or_letting || fight ;; Hostile_encounter || fight ;; Hostile_encounter || burst ;; Damaging || triggering ;; Causation || attacking ;; Attack
kill ;; Killing
used ;; Using || massacre ;; Killing
pledged ;; Commitment || work ;; Employment
asked ;; Request || massacre ;; Killing || monitor ;; Scrutiny
using ;; Using || flying ;; Self_motion || arrested ;; Arrest || shoot down ;; Use_firearm || blow up ;; Destroying || concocted ;; Hold
avoid ;; Preventing_or_letting || investigation ;; Scrutiny || working ;; Employment
charged ;; Judgment_communication || paid ;; Earnings_and_losses
use ;; Using || led to ;; Causation || offers ;; Giving || engaged ;; Participation
overturn ;; Change || denied ;; Agree_or_refuse_to_act
cited ;; Judgment_communication
joining ;; Becoming_a_member || said ;; Statement
overturned ;; Change || rejected ;; Agree_or_refuse_to_act || rejected ;; Agree_or_refuse_to_act
hired ;; Employment
earned ;; Earnings_and_losses
died ;; Death || said ;; Telling
became ;; Becoming || returned ;; Arriving
jumped ;; Body_movement
opposing ;; Quarreling || protests ;; Protest
protests ;; Protest || launched ;; Process_start
protests ;; Protest || occurring ;; Coming_to_be
took place ;; Process_start
upheld ;; Action || declined ;; Cause_change_of_position_on_a_scale
election ;; Change_of_leadership || protests ;; Protest || elections ;; Change_of_leadership
attack ;; Attack || attempt ;; Hold || Expedition ;; Self_motion
drowned ;; Killing || wrecked ;; Destroying
proposed ;; Statement || planned ;; Arranging
decided ;; Deciding || mount ;; Process_start
chosen ;; Choosing || kept ;; Temporary_stay
discover ;; Know || warn ;; Warning
provide ;; Supply || provisioned ;; Supply || supplies ;; Supply || arrived ;; Arriving
acquiring ;; Getting
reached ;; Arriving || combined ;; Cause_to_amalgamate || wrecked ;; Destroying || drive ;; Motion
abandoned ;; GiveUp || returned ;; Motion
implement ;; Action
affair ;; Participation
enforcing ;; Action
chasing ;; Self_motion || ran ;; Motion
attacked ;; Attack || boarded ;; Self_motion || torched ;; Damaging
increased ;; Cause_change_of_position_on_a_scale
increase ;; Cause_change_of_position_on_a_scale || control ;; Control
protested ;; Attack || clashed ;; Hostile_encounter
marked ;; Recording || moving ;; Motion || uprising ;; Change_of_leadership || preceding ;; Change_event_time
battle ;; Hostile_encounter
attacked ;; Attack || raid ;; Attack || inflicting ;; Causation
airstrike ;; Attack || attack ;; Attack
protecting ;; Defending
attack ;; Attack || intercepted ;; Preventing_or_letting || suffered ;; Bodily_harm
battle ;; Hostile_encounter || led to ;; Causation
adopted ;; Action
occupied ;; Control
became ;; Becoming || flanked ;; Attack || tasked ;; Request || protecting ;; Defending
build ;; Building
chose ;; Choosing || transforming ;; Change || relaunch ;; Recovering || mining ;; Manufacturing
built ;; Building || strikes ;; Attack
affected ;; Influence
enforced ;; Action || saw ;; Self_motion
decided ;; Deciding || deport ;; Removing
cut off ;; Having_or_lacking_access || worsened ;; Influence || left ;; GiveUp
surrender ;; Surrendering
Cyclone ;; Catastrophe || cyclone ;; Catastrophe || passed ;; Motion_directional
identified ;; Check || impact ;; Influence
moved ;; Motion || becoming ;; Becoming || cyclone ;; Catastrophe || organized ;; Come_together
began ;; Process_start || named ;; Name_conferral || acquired ;; Getting
displayed ;; Action
cyclone ;; Catastrophe || reached ;; Achieve
struck ;; Attack || producing ;; Creating
cyclone ;; Catastrophe || prompted ;; Causation || weakening ;; Cause_change_of_position_on_a_scale
degraded ;; Cause_change_of_position_on_a_scale || peaked ;; Arriving
devastated ;; Destroying || Cyclone ;; Catastrophe
destroyed ;; Destroying || damaged ;; Damaging
left ;; Causation || lost ;; Earnings_and_losses
experienced ;; Change_sentiment || lost ;; Earnings_and_losses
reported ;; Reporting
began ;; Process_start || aiding ;; Assistance
enacted ;; Action || funded ;; Supply
retired ;; Removing || replaced ;; Becoming
massacre ;; Committing_crime
accused ;; Judgment_communication || poisoned ;; Bodily_harm || killed ;; Killing
began ;; Process_start
took place ;; Process_start
helped ;; Assistance || reach ;; Arriving
shocked ;; Change_sentiment || established ;; Creating || massacre ;; Killing
spread ;; Dispersal || killings ;; Killing
deaths ;; Death || held ;; Hold
poisoning ;; Bodily_harm || remains ;; Presence || impose ;; Action || regarded ;; Perception_active
conflict ;; Hostile_encounter
become ;; Becoming || originated ;; Coming_to_be
conquered ;; Conquering || included ;; Cause_to_be_included || governed ;; Control
violence ;; Violence || spilled ;; Motion || occurred ;; Coming_to_be || escalated ;; Process_start
blamed ;; Judgment_communication || occurred ;; Coming_to_be
violence ;; Violence
ousted ;; Change_of_leadership
implemented ;; Action || replacing ;; Change || approach ;; Action
declared ;; Expressing_publicly || violence ;; Violence || improving ;; Cause_to_make_progress || come to ;; Arriving
surpassed ;; Cause_change_of_position_on_a_scale
noted ;; Know || bringing ;; Bringing
violence ;; Violence || increased ;; Cause_change_of_position_on_a_scale
violence ;; Violence || increasing ;; Cause_change_of_position_on_a_scale || solved ;; Resolve_problem || conceded ;; Agree_or_refuse_to_act
demanded ;; Request || demands ;; Request || negotiations ;; Communication
negotiations ;; Communication || talks ;; Telling
aim ;; Aiming
claimed ;; Statement
estimates ;; Communication
brought in ;; Bringing || believed ;; Know || claims ;; Statement || balanced ;; Cause_change_of_position_on_a_scale || suggesting ;; Protest
died ;; Death || taken over ;; Change_of_leadership || support ;; Supporting || injured ;; Bodily_harm || approach ;; Action
began ;; Process_start || strike ;; Attack
negotiations ;; Communication || Amalgamated ;; Cause_to_amalgamate || incident ;; Catastrophe
protect ;; Preventing_or_letting || working ;; Being_in_operation
clash ;; Hostile_encounter || enforcement ;; Action || strikes ;; Attack || escalated ;; Cause_change_of_position_on_a_scale || resulted in ;; Causation
understood ;; Know || confrontation ;; Hostile_encounter || violent ;; Violence || event ;; Social_event
event ;; Social_event || embarking ;; Self_motion
Battle ;; Hostile_encounter || battle ;; Hostile_encounter || War ;; Hostile_encounter || siege ;; Besieging || occurred ;; Presence
reduced ;; Cause_change_of_position_on_a_scale || forces ;; Causation || protected ;; Defending
began ;; Process_start || received ;; Receiving || siege ;; Besieging
chose ;; Choosing || use ;; Using || fortify ;; Cause_change_of_strength || support ;; Supporting || running ;; Self_motion || protected ;; Defending
brought ;; Bringing || storm ;; Attack
sent ;; Sending || supply ;; Supply || seize ;; Conquering
attacked ;; Attack || reached ;; Arriving
Fighting ;; Hostile_encounter
reach ;; Arriving || relief ;; Assistance || fell ;; Earnings_and_losses || marched ;; Self_motion
withdrew ;; Escaping || organized ;; Arranging || battle ;; Hostile_encounter
siege ;; Besieging || loss ;; Earnings_and_losses || brought ;; Bringing
loss ;; Earnings_and_losses || supply ;; Supply || fall ;; Earnings_and_losses
crash ;; Catastrophe || occurred ;; Coming_to_be
accident ;; Catastrophe
ran ;; Motion || killing ;; Killing || injuring ;; Bodily_harm
left ;; Departing
allowing ;; Preventing_or_letting || entrance ;; Arriving || passed ;; Motion_directional || follow ;; Motion || return ;; Arriving
seen ;; Perception_active || end ;; Process_end || waved ;; Body_movement || stop ;; Preventing_or_letting
glimpsed ;; Perception_active || stopped ;; Preventing_or_letting
signalled ;; Communication || end ;; Process_end
crash-landed ;; Self_motion
control ;; Control || final ;; Process_end || approach ;; Arriving || reported ;; Reporting
shut down ;; Being_in_operation || following ;; Motion
approach ;; Arriving || descended ;; Motion_directional || hitting ;; Bodily_harm || stopping ;; Self_motion
killed ;; Killing
scrapped ;; Destroying
held ;; Hold || championship ;; Competition
playoff ;; Competition
final ;; Process_end || sinking ;; Body_movement
became ;; Becoming || win ;; Earnings_and_losses
turned ;; Becoming
tied ;; Connect || shot ;; Motion
tour ;; Traveling || tour ;; Traveling
tour ;; Traveling || date ;; Statement
bodybuilding ;; Body_movement || bulked ;; Cause_change_of_position_on_a_scale
decided ;; Deciding || tours ;; Traveling || recording ;; Recording || tour ;; Traveling || go ;; Motion
appear ;; Presence || tour ;; Traveling || replaced ;; Removing || promote ;; Cause_to_make_progress
tour ;; Traveling
started ;; Process_start || tour ;; Traveling || went through ;; Motion
tour ;; Traveling || went to ;; Motion
tour ;; Traveling || headed ;; Motion || played ;; Motion
finished ;; Process_end || tour ;; Traveling
grossed ;; Earnings_and_losses || tour ;; Traveling
enjoying ;; Perception_active
performed ;; Hold || tour ;; Traveling || tour ;; Traveling || promote ;; Cause_to_make_progress
attendance ;; Participation
held ;; Hold || championship ;; Competition || Championship ;; Competition || Championship ;; Competition
became ;; Becoming || victory ;; Earnings_and_losses || win ;; Getting || passing ;; Body_movement
became ;; Becoming || complete ;; Process_end
achieved ;; Achieve || Championship ;; Competition || broke ;; Destroying
became ;; Becoming || win ;; Earnings_and_losses || joining ;; Becoming_a_member
telecast ;; Communication || telecast ;; Communication
alleged ;; Statement || collaboration ;; Collaboration || demonstrations ;; Protest || protests ;; Protest
began ;; Process_start
began ;; Process_start
established ;; Creating
cooperates ;; Collaboration || movements ;; Social_event
engage ;; Participation || meetings ;; Social_event
bombed ;; Attack
housed ;; Placing || detonated ;; Attack
killed ;; Killing || injured ;; Bodily_harm
planned ;; Arranging || objectives ;; Aiming
decide ;; Deciding || attacking ;; Attack || demonstrate ;; Protest
demonstrate ;; Protest
debate ;; Quarreling
favored ;; Supporting || existing ;; Presence || protect ;; Preventing_or_letting
decided ;; Deciding || take ;; Action || action ;; Response
revolution ;; Change_of_leadership || stormed ;; Attack || took place ;; Process_start || resigned ;; Change_of_leadership
fled ;; Escaping || denouncing ;; Judgment_communication || protests ;; Protest || occupation ;; Attack || resigned ;; Change_of_leadership
attributed ;; Causation || uprising ;; Violence || perceived ;; Know
allowed ;; Preventing_or_letting || seceded ;; Escaping || register ;; Recording || receive ;; Receiving
election ;; Change_of_leadership || revolution ;; Change_of_leadership || led to ;; Causation || called in ;; Process_start
elected ;; Change_of_leadership
Uprising ;; Violence || Uprising ;; Violence || Uprisings ;; Violence || Protests ;; Protest || unrest ;; Violence
Uprising ;; Violence || occurred ;; Coming_to_be
began ;; Process_start || organised ;; Arranging || protests ;; Self_motion
headed ;; Motion || ruled ;; Control
strengthening ;; Cause_change_of_strength || became ;; Becoming || called ;; Request || involved ;; Participation
combined ;; Cause_to_amalgamate || nationalised ;; Cause_to_amalgamate
started ;; Process_start || uprising ;; Violence
spread ;; Dispersal || protests ;; Self_motion
ended ;; Process_end || uprising ;; Violence
uprising ;; Violence || deaths ;; Death || killed ;; Killing || attributed ;; Causation || put ;; Placing
emerged ;; Coming_to_be || crisis ;; Quarreling
election ;; Change_of_leadership || arranged ;; Arranging
refused ;; Agree_or_refuse_to_act || results ;; Causation || continued ;; Being_in_operation
put ;; Placing || arrest ;; Arrest
change ;; Change || party ;; Participation
arrest ;; Arrest || peaked ;; Arriving
crashed ;; Destroying
resulted in ;; Causation
accident ;; Catastrophe
ruled ;; Deciding || establish ;; Creating
stated ;; Statement || flying ;; Motion || overturned ;; Change
proved ;; Convincing
found ;; Know || conducted ;; Action || negligence ;; Judgment_communication
cleared ;; Giving || review ;; Scrutiny || negligence ;; Judgment_communication
operated ;; Being_in_operation || chartered ;; Renting
crashed ;; Catastrophe || making ;; Self_motion
caught ;; Getting || occurred ;; Presence
cause ;; Causation || controversy ;; Communication || crash ;; Catastrophe
flight ;; Motion || demonstration ;; Protest
performed ;; Hold || crashed ;; Catastrophe || shown ;; Protest || take place ;; Process_start || skimmed ;; Motion
impact ;; Influence || died ;; Death || escape ;; Escaping
flew ;; Motion || flew ;; Motion || failed ;; Agree_or_refuse_to_act || concluded ;; Coming_to_believe
disputed ;; Quarreling || claimed ;; Statement || prevented ;; Preventing_or_letting || applying ;; Using || pulling ;; Hold
aftermath ;; Causation || tampered ;; Hindering
crash ;; Catastrophe
attack ;; Attack || launched ;; Attack
strike ;; Attack || targeted ;; Aiming || attack ;; Attack
killed ;; Killing || started ;; Process_start || strike ;; Attack || attacks ;; Attack || reported ;; Reporting
claimed ;; Statement
escaped ;; Escaping || reports ;; Reporting || killed ;; Killing || disputed ;; Quarreling
phoned ;; Communication || prove ;; Convincing
fired ;; Use_firearm || holding ;; Hold || meeting ;; Social_event
engagement ;; Hostile_encounter || Battle ;; Hostile_encounter || Battle ;; Hostile_encounter || War ;; Military_operation || War ;; Military_operation
battle ;; Hostile_encounter || went on ;; Motion
stretched ;; Expansion
Battle ;; Hostile_encounter || casualty ;; Death
called ;; Name_conferral
considered ;; Coming_to_believe || Battle ;; Hostile_encounter || turning ;; Change || War ;; Military_operation
retreat ;; Escaping || described ;; Statement || battle ;; Hostile_encounter || started ;; Process_start || stopped ;; Process_end
tour ;; Traveling
supported ;; Supporting || visiting ;; Traveling
derives ;; Creating || success ;; Achieve || showcasing ;; Social_event
began ;; Process_start || excursion ;; Traveling
ending ;; Process_end || visiting ;; Traveling || traveled ;; Traveling
became ;; Becoming || add ;; Cause_change_of_position_on_a_scale || planned ;; Arranging || prompting ;; Causation || concerts ;; Social_event || reports ;; Reporting
tour ;; Traveling || tour ;; Traveling
ranked ;; Placing || earning ;; Earnings_and_losses
crashed ;; Catastrophe || killing ;; Killing
crashed ;; Catastrophe || named ;; Name_conferral
determined ;; Deciding || crash ;; Catastrophe || cause ;; Causation || investigation ;; Criminal_investigation
proposed ;; Statement || reports ;; Reporting || impact ;; Influence || Causes ;; Causation
crash ;; Catastrophe || uses ;; Using || discontinuing ;; Process_end
operated ;; Being_in_operation
crashed ;; Catastrophe
operated ;; Being_in_operation
approach ;; Arriving || crashed ;; Catastrophe
killed ;; Killing
determined ;; Deciding || investigation ;; Criminal_investigation || cause ;; Causation
collision ;; Catastrophe || occurred ;; Coming_to_be
crash ;; Catastrophe || ensued ;; Coming_to_be || involved ;; Presence
crash ;; Catastrophe || killed ;; Killing || died ;; Death || making ;; Bringing || injured ;; Bodily_harm
organised ;; Participation || operating ;; Being_in_operation || charged ;; Criminal_investigation
charge ;; Judgment_communication || failing ;; Causation || ensure ;; Preventing_or_letting || charges ;; Criminal_investigation || dropped ;; Removing || faced ;; Confronting_problem
charge ;; Judgment_communication || found ;; Coming_to_believe
caused ;; Causation || cause ;; Causation || fog ;; Catastrophe || concluded ;; Statement || accident ;; Catastrophe || incident ;; Catastrophe
killing ;; Killing || took place ;; Process_start
perpetrated ;; Committing_crime || killings ;; Killing
refused ;; Agree_or_refuse_to_act || maintained ;; Statement
killings ;; Killing || use ;; Using
killings ;; Killing || investigated ;; Criminal_investigation
commissioned ;; Deciding || concluded ;; Coming_to_believe
massacre ;; Killing || condemned ;; Judgment_communication
riot ;; Violence || riot ;; Violence || occurred ;; Presence
broke out ;; Process_start || riot ;; Violence || search ;; Scrutiny
left ;; Causation
appointed ;; Deciding || investigate ;; Criminal_investigation
riots ;; Violence
riot ;; Violence || riot ;; Violence || left ;; Causation
began ;; Process_start || involved ;; Cause_to_be_included
ended ;; Process_end
signed ;; Sign_agreement
involved ;; Cause_to_be_included
caused ;; Causation
sought ;; Creating
died ;; Death
driven ;; Motion
began ;; Process_start || unrest ;; Hostile_encounter
choosing ;; Choosing || signing ;; Sign_agreement || sparked ;; Causation || suspend ;; Change_event_time
widened ;; Expansion || calls ;; Request
fueled ;; Cause_change_of_position_on_a_scale || protests ;; Attack
named ;; Name_conferral
dispersal ;; Dispersal || escalated ;; Cause_change_of_strength
revolution ;; Change_of_leadership || protests ;; Attack || led to ;; Causation
clashes ;; Attack || protests ;; Attack
toppled ;; Change_of_leadership
increased ;; Cause_change_of_position_on_a_scale
occupied ;; Control
protests ;; Attack
advanced ;; Cause_to_make_progress || clashed ;; Attack || occupy ;; Control
fired ;; Use_firearm
fighting ;; Hostile_encounter || killed ;; Killing
signed on ;; Sign_agreement
signing ;; Sign_agreement || witnessed ;; Scrutiny
refused ;; Agree_or_refuse_to_act || sign ;; Sign_agreement
fled ;; Escaping || signed ;; Sign_agreement
gained ;; Getting || control ;; Control
released ;; Releasing || replaced ;; Change_of_leadership || ordered ;; Arranging || removed ;; Removing
unrest ;; Hostile_encounter
protests ;; Attack
fought ;; Quarreling || known ;; Know || War ;; Hostile_encounter || war ;; Hostile_encounter
conflicts ;; Hostile_encounter
occupied ;; Hold
supported ;; Supporting || conflict ;; Hostile_encounter || war ;; Hostile_encounter || occurred ;; Presence || affiliated ;; Connect
came into effect ;; Change || agreement ;; Sign_agreement
signed ;; Sign_agreement || withdrew ;; Escaping
dispute ;; Quarreling || signing ;; Sign_agreement || enmity ;; Hostile_encounter || continued ;; Motion || concluded ;; Deciding
occurred ;; Coming_to_be
began ;; Process_start || led to ;; Causation
ended ;; Process_end || signed ;; Sign_agreement || clashed ;; Hostile_encounter
called ;; Name_conferral
taken ;; Conquering || included ;; Cause_to_be_included || remained ;; Presence
killed ;; Killing
fled ;; Escaping
called ;; Name_conferral
War ;; Hostile_encounter || fought ;; Hostile_encounter
expand ;; Expansion || felt ;; Coming_to_believe
cross ;; Motion_directional || assist ;; Assistance || bring about ;; Causation
control ;; Control || taken ;; Conquering || War ;; Hostile_encounter
create ;; Creating
retreat ;; Escaping || defend ;; Defending || strengthen ;; Cause_change_of_strength || began ;; Process_start || started ;; Process_start || causing ;; Causation || advance ;; Cause_to_make_progress
decided ;; Deciding || formed ;; Coming_to_be || end ;; Process_end
escalated ;; Expansion
counter-attack ;; Attack
fled ;; Escaping || pushed ;; Self_motion
increased ;; Cause_change_of_position_on_a_scale || arriving at ;; Arriving || war ;; Hostile_encounter
achieved ;; Achieve || Battle ;; Hostile_encounter || fall ;; Conquering || victory ;; Getting || turned ;; Change
ended ;; Process_end || advance ;; Cause_to_make_progress || war ;; Hostile_encounter || sued ;; Request
signed on ;; Sign_agreement
determined ;; Deciding
gained ;; Getting || defined ;; Statement
became ;; Becoming
War ;; Hostile_encounter || conflict ;; Hostile_encounter
killed ;; Killing || involvement ;; Participation || conflict ;; Hostile_encounter || led to ;; Causation
War ;; Hostile_encounter || broke out ;; Process_start
overthrew ;; Change_of_leadership || elected ;; Choosing || coup ;; Change_of_leadership || elections ;; Change_of_leadership
military ;; Hostile_encounter || coup ;; Change_of_leadership
start ;; Process_start || topple ;; Change_of_leadership || moved ;; Motion || uprising ;; Change_of_leadership
battled ;; Hostile_encounter || control ;; Control || forces ;; Military_operation
executed ;; Killing
negotiations ;; Communication || involvement ;; Participation || ceasefire ;; Process_end || agreement ;; Sign_agreement || elections ;; Change_of_leadership || broken ;; Bodily_harm || led to ;; Causation
elected ;; Change_of_leadership
known ;; Know || fought ;; Hostile_encounter || War ;; Hostile_encounter || War ;; Hostile_encounter || War ;; Hostile_encounter
ended ;; Process_end || war ;; Hostile_encounter
fought ;; Hostile_encounter || battles ;; Hostile_encounter || Battle ;; Hostile_encounter
uprisings ;; Change_of_leadership || sparked ;; Process_start
agreed ;; Agree_or_refuse_to_act || declare ;; Expressing_publicly || war ;; Hostile_encounter
allied ;; Collaboration
battle ;; Hostile_encounter || war ;; Hostile_encounter || Battle ;; Hostile_encounter
battles ;; Hostile_encounter || fought ;; Hostile_encounter
counterattacked ;; Attack || took ;; Conquering
conquered ;; Conquering
ended ;; Process_end || war ;; Hostile_encounter || signed ;; Sign_agreement
increased ;; Cause_change_of_position_on_a_scale || forces ;; Hostile_encounter || sign ;; Sign_agreement || recognising ;; Know
gained ;; Getting
began ;; Process_start || Hurricane ;; Catastrophe
exited ;; Departing || formed ;; Coming_to_be
intensified ;; Cause_change_of_strength || storm ;; Catastrophe
allowed ;; Preventing_or_letting || storm ;; Catastrophe
intensified ;; Cause_change_of_strength || becoming ;; Becoming || due ;; Causation || response ;; Response || improving ;; Cause_to_make_progress || included ;; Cause_to_be_included
became ;; Becoming || developed ;; Coming_to_be || hurricane ;; Catastrophe
intensified ;; Cause_change_of_strength || turning ;; Becoming || hurricane ;; Catastrophe
maintained ;; Preserving
reported ;; Reporting || tied ;; Connect || hurricane ;; Catastrophe
turn ;; Change
turned ;; Becoming || due ;; Causation || observed ;; Perception_active || storm ;; Catastrophe
made ;; Manufacturing || hurricane ;; Catastrophe || weakening ;; Cause_change_of_strength || imparted ;; Dispersal
Turning ;; Becoming || maintained ;; Preserving || weakened ;; Cause_change_of_strength
dissipated ;; Dispersal || moving ;; Motion
reorganized ;; Cause_to_amalgamate || storm ;; Catastrophe || reached ;; Arriving
moving ;; Motion || striking ;; Attack || accelerated ;; Cause_change_of_position_on_a_scale
became ;; Becoming || storm ;; Catastrophe || tracked ;; Self_motion
began ;; Process_start || protest ;; Self_motion || Movement ;; Social_event || protests ;; Self_motion
began ;; Process_start || used ;; Using
demanding ;; Request || demonstrations ;; Social_event || took place ;; Process_start
attempts ;; Hold || Following ;; Response || dialogue ;; Communication
called ;; Request
triggered ;; Causation
molested ;; Bodily_harm || manhandled ;; Bodily_harm || attacked ;; Attack || charge ;; Attack || included ;; Cause_to_be_included || used ;; Using
maintain ;; Statement || insist ;; Statement || ruling ;; Control
applied ;; Using || escort ;; Sending
started ;; Process_start || Demonstrations ;; Social_event
increasing ;; Cause_change_of_position_on_a_scale || marches ;; Social_event
participants ;; Participation || fluctuate ;; Cause_change_of_position_on_a_scale
dancing ;; Self_motion || singing ;; Self_motion || marked ;; Recording || arranging ;; Arranging
link ;; Connect || led ;; Causation
employ ;; Using
deployment ;; Arranging || renamed ;; Name_conferral
dropped ;; Motion_directional
communications ;; Communication || disrupt ;; Hindering || dropped ;; Motion_directional
response ;; Response || invasion ;; Attack
scattered ;; Dispersal || destroying ;; Destroying || Breaking ;; Destroying
tied ;; Connect || passed ;; Motion_directional
joined ;; Participation
win ;; Getting || final ;; Process_end
made ;; Manufacturing || tied ;; Connect
withdrew ;; Escaping
played ;; Participation
shot ;; Use_firearm
fought ;; Hostile_encounter
supported ;; Supporting
included ;; Cause_to_be_included
emerged ;; Coming_to_be
provided ;; Supply
initiated ;; Process_start
increased ;; Cause_change_of_position_on_a_scale || entered ;; Arriving
escalated ;; Cause_change_of_position_on_a_scale
sent ;; Sending
backed ;; Self_motion
gave ;; Giving
engaged ;; Participation
conducted ;; Action
showed ;; Presence || mounted ;; Connect
expanded ;; Expansion
degraded ;; Cause_change_of_position_on_a_scale
increased ;; Cause_change_of_position_on_a_scale || held ;; Hold || dropped ;; Motion_directional
declared ;; Expressing_publicly
began ;; Process_start || invaded ;; Attack || crossed ;; Motion_directional
resulted in ;; Causation
saw ;; Self_motion
became ;; Becoming
stopped ;; Process_end || failed ;; Earnings_and_losses
prohibited ;; Preventing_or_letting
broken ;; Destroying || continued ;; Preventing_or_letting
marked ;; Recording || culminated ;; Process_end || reunified ;; Cause_to_amalgamate
cost ;; Earnings_and_losses
died ;; Death || missing ;; Bodily_harm
begun ;; Process_start || re-emerged ;; Coming_to_be
fought ;; Hostile_encounter
gave rise to ;; Causation
produced ;; Creating
took place ;; Hold
consisting ;; Cause_to_be_included
included ;; Cause_to_be_included
win ;; Getting || pinned ;; Connect
served ;; Assistance
moved ;; Motion
replaced ;; Change
used ;; Using || replaced ;; Change_of_leadership
fought ;; Hostile_encounter || battle ;; Hostile_encounter
attacks ;; Attack || led ;; Conquering || invaded ;; Attack
establish ;; Creating
engaged ;; Participation
abandoned ;; GiveUp || overwhelmed ;; Destroying || defeated ;; Conquering
killed ;; Killing
mounted ;; Connect || commanded ;; Control
dead ;; Death || captured ;; Conquering || suffered ;; Bodily_harm
occupied ;; Hold
led ;; Bringing || defeated ;; Conquering
fought ;; Defending
defeated ;; Conquering || withdrew ;; Escaping
game ;; Competition
played ;; Participation
game ;; Competition
game ;; Competition || played ;; Participation || appearance ;; Presence
tied ;; Connect
run ;; Action || scored ;; Getting
pass ;; Motion || scored ;; Getting || provide ;; Giving
commanded ;; Control || marked ;; Recording
cross ;; Motion_directional || started ;; Process_start || link ;; Connect
destroyed ;; Destroying || advanced ;; Arriving
stopped ;; Preventing_or_letting
anticipated ;; Coming_to_believe || stop ;; Process_end || capture ;; Arrest
allow ;; Preventing_or_letting || withdraw ;; Removing || delay ;; Change_event_time
ordered ;; Request || fight ;; Hostile_encounter || forces ;; Military_operation
cross ;; Motion_directional
lost ;; Earnings_and_losses || became ;; Becoming
casualties ;; Catastrophe || amounted ;; Earnings_and_losses
cyclone ;; Catastrophe || affected ;; Influence
impacted ;; Influence || cyclones ;; Catastrophe
embedded ;; Connect || developed ;; Coming_to_be
Moving ;; Motion || storm ;; Catastrophe || reached ;; Arriving || entering ;; Arriving || intensified ;; Cause_change_of_strength
cyclone ;; Catastrophe || designated ;; Giving || crossed ;; Motion_directional
assigned ;; Deciding
formed ;; Coming_to_be || storm ;; Catastrophe || intensified ;; Cause_change_of_strength
cyclone ;; Catastrophe || upgraded ;; Cause_to_make_progress
maintained ;; Statement || making ;; Motion
weakened ;; Cause_change_of_strength || moving ;; Motion
made ;; Manufacturing || reached ;; Arriving || dissipated ;; Removing
brought ;; Bringing
impacted ;; Influence || affected ;; Influence
reached ;; Arriving
storm ;; Catastrophe || destroyed ;; Destroying
deaths ;; Death || storm ;; Catastrophe
storm ;; Catastrophe || damage ;; Damaging || affected ;; Influence || struck ;; Attack
occurred ;; Coming_to_be || flooding ;; Catastrophe
storm ;; Catastrophe || damaged ;; Damaging || killed ;; Killing || downed ;; Motion
occurred ;; Coming_to_be || stormed ;; Attack
battle ;; Hostile_encounter || Campaign ;; Military_operation || War ;; Military_operation
cross ;; Motion_directional
created ;; Creating
linked ;; Connect
constructed ;; Building
war ;; Military_operation || War ;; Military_operation || War ;; Military_operation || Battle ;; Hostile_encounter || crossed ;; Motion_directional
ability ;; Assistance
offered ;; Giving
forced ;; Causation || acquired ;; Getting || retreat ;; Escaping || control ;; Control || counter-attack ;; Attack || control ;; Control || control ;; Control
prevented ;; Preventing_or_letting || withdraw ;; Escaping || Control ;; Control || crossing ;; Motion_directional || required ;; Imposing_obligation
retreated ;; Escaping || begin ;; Process_start || Battle ;; Hostile_encounter || siege ;; Besieging || moved ;; Motion
sparked ;; Causation || relinquish ;; GiveUp
end ;; Process_end || joint ;; Collaboration || resulted in ;; Causation
proclaimed ;; Statement
pressured ;; Influence
entered ;; Arriving
occupied ;; Control
invaded ;; Attack || occupied ;; Control
used ;; Using || provide ;; Supply
agreed ;; Agree_or_refuse_to_act || withdraw ;; Escaping
came ;; Self_motion || remained ;; Presence
supported ;; Supporting
led to ;; Causation
conquer ;; Conquering
support ;; Supporting
marched ;; Self_motion || defeating ;; Conquering || taking ;; Conquering
lasted ;; Process_end || suffered ;; Bodily_harm || claimed ;; Statement
abandoned ;; GiveUp || siege ;; Besieging || withdrew ;; Escaping
pursued ;; Aiming || led to ;; Causation || fall ;; Cause_change_of_position_on_a_scale || culminated ;; Process_end
named ;; Name_conferral
began ;; Process_start || Forming ;; Coming_to_be || strengthening ;; Cause_change_of_strength
warning ;; Warning || issued ;; Creating
warning ;; Warning || issued ;; Statement
flooding ;; Catastrophe || predicted ;; Coming_to_believe || leading to ;; Causation
warning ;; Warning || issued ;; Statement
disaster ;; Catastrophe
damage ;; Damaging
storms ;; Catastrophe
began ;; Process_start || hit ;; Attack
hit ;; Attack
hit ;; Attack
formed ;; Coming_to_be || struck ;; Attack || followed ;; Self_motion
reduce ;; Cause_change_of_position_on_a_scale || build ;; Building || violence ;; Violence
designated ;; Choosing
objective ;; Aiming || end ;; Process_end || hunt ;; Killing || remaining ;; Temporary_stay
operation ;; Military_operation || included ;; Cause_to_be_included || targeting ;; Aiming
targeted ;; Aiming || designated ;; Choosing
suicide ;; Killing || aims ;; Aiming
violence ;; Violence || escalated ;; Cause_change_of_position_on_a_scale
attacks ;; Attack || destruction ;; Destroying || took ;; Conquering
killed ;; Killing || attacks ;; Attack || killed ;; Killing || injured ;; Bodily_harm || murdered ;; Killing || injured ;; Bodily_harm || killed ;; Killing
enlisted ;; Becoming_a_member || riots ;; Violence
evacuated ;; Removing
found ;; Know || violence ;; Violence || occurred ;; Coming_to_be || riot ;; Violence || menace ;; Commitment
found ;; Know || contributed ;; Supply
described ;; Statement
tournament ;; Competition || held ;; Hold
contested ;; Competition
championships ;; Competition || promoted ;; Cause_to_make_progress
moved up ;; Cause_change_of_position_on_a_scale
tournament ;; Competition || held ;; Hold
play ;; Participation || tournament ;; Competition || wait ;; Temporary_stay
Battle ;; Hostile_encounter || War ;; Military_operation || took place ;; Process_start
retreated ;; Escaping || regrouped ;; Cause_to_amalgamate || Wars ;; Hostile_encounter
arrive ;; Arriving || marching ;; Motion_directional || wait ;; Temporary_stay || encountered ;; Hostile_encounter
destruction ;; Destroying || pushed ;; Motion
entered ;; Arriving
controlled ;; Control
crash ;; Catastrophe || accident ;; Catastrophe || occurred ;; Presence
killing ;; Killing || injuring ;; Bodily_harm || Removal ;; Removing || jumping ;; Motion || led to ;; Causation
derailed ;; Motion || disrupting ;; Hindering
assisted ;; Assistance || arrived ;; Arriving || injured ;; Bodily_harm
accident ;; Catastrophe || injured ;; Bodily_harm || rushed ;; Motion
remaining ;; Temporary_stay || taken ;; Bringing
injuries ;; Bodily_harm || died ;; Death || succumbed ;; Bodily_harm
accident ;; Catastrophe || cause ;; Causation || removal ;; Removing || said ;; Statement
serving ;; Assistance || tournament ;; Competition || Championship ;; Competition || Championships ;; Competition || Olympics ;; Competition
competition ;; Competition
defended ;; Defending || represented ;; Presence
included ;; Cause_to_be_included || contenders ;; Competition
used ;; Using || tournament ;; Competition || discarded ;; GiveUp
final ;; Process_end || placed ;; Placing || opening ;; Openness || advancing ;; Cause_to_make_progress
awarded ;; Giving || final ;; Process_end || record ;; Recording
remain ;; Temporary_stay || use ;; Using || Olympics ;; Competition || tournament ;; Competition || replaced ;; Change
killed ;; Killing || shooting ;; Use_firearm || shooting ;; Use_firearm || occurred ;; Coming_to_be || injured ;; Bodily_harm || opened ;; Process_start
suicide ;; Killing || committed ;; Killing
shooting ;; Use_firearm || took place ;; Process_start
attack ;; Attack
murderer ;; Killing || shooting ;; Use_firearm
consult ;; Communication || find ;; Know
remains ;; Temporary_stay || shooting ;; Use_firearm
cyclone ;; Catastrophe || Cyclone ;; Catastrophe || passed ;; Motion_directional
storm ;; Catastrophe || originated ;; Coming_to_be || named ;; Name_conferral
moved ;; Motion || remained ;; Temporary_stay
storm ;; Catastrophe || cyclone ;; Catastrophe || intensified ;; Cause_change_of_strength || intensify ;; Cause_change_of_strength
cyclone ;; Catastrophe || attained ;; Achieve || reaching ;; Achieve
storm ;; Catastrophe || estimated ;; Statement || estimated ;; Statement
weakened ;; Cause_change_of_strength || turned ;; Becoming || passing ;; Motion_directional
becoming ;; Becoming || storm ;; Catastrophe || turned ;; Motion
caused ;; Causation || flooding ;; Catastrophe || passing ;; Motion_directional || produced ;; Creating
storm ;; Catastrophe || considered ;; Statement
damaged ;; Damaging || destroyed ;; Destroying || leaving ;; Causation
damaged ;; Damaging
storm ;; Catastrophe || killed ;; Killing
sunk ;; Killing
formed ;; Coming_to_be || affected ;; Influence || storm ;; Catastrophe
became ;; Becoming || formed ;; Coming_to_be
storm ;; Catastrophe || paralleled ;; Motion_directional
peaking ;; Cause_change_of_position_on_a_scale || induced ;; Causation || storm ;; Catastrophe
degenerating ;; Change || downgraded ;; Cause_change_of_position_on_a_scale
dissipating ;; Removing || proceeded ;; Self_motion
stayed ;; Temporary_stay
caused ;; Causation
storm ;; Catastrophe || reported ;; Reporting
uprising ;; Change_of_leadership
controlled ;; Control || ruling ;; Control
revolt ;; Change_of_leadership || death ;; Death || occurred ;; Coming_to_be
ruled ;; Control
proclaimed ;; Statement || death ;; Death
raised ;; Cause_change_of_position_on_a_scale
arrived in ;; Arriving
resulted in ;; Causation
elected ;; Choosing
departed ;; Departing
took ;; Conquering
chose ;; Choosing || control ;; Control
rebel ;; Change_of_leadership || rebellions ;; Change_of_leadership || supporting ;; Supporting
struck ;; Attack
beheaded ;; Killing
fell ;; Motion_directional
kept ;; Temporary_stay
revolution ;; Change_of_leadership || debate ;; Communication
revolt ;; Change_of_leadership
perceived ;; Know || control ;; Control
revolt ;; Change_of_leadership
revolt ;; Change_of_leadership || adopted ;; Choosing
grown ;; Expansion || establishment ;; Creating
celebrated ;; Social_event
killing ;; Killing || tragedy ;; Killing || Massacre ;; Killing
help ;; Assistance || orders ;; Arranging || massacre ;; Killing || committed ;; Commitment || command ;; Control
death ;; Death || claimed ;; Statement
became ;; Becoming || conflict ;; Hostile_encounter || massacre ;; Killing
Battle ;; Hostile_encounter || massacre ;; Killing || tragedy ;; Killing || refer ;; Name_conferral
massacre ;; Killing || genocide ;; Killing || tragedy ;; Killing || refer ;; Name_conferral
engagement ;; Hostile_encounter
formed ;; Coming_to_be || campaign ;; Military_operation || culminated ;; Process_end
converted ;; Change || seized ;; Hold
encountered ;; Hostile_encounter || manoeuvered ;; Hold
attacked ;; Attack || surrender ;; Surrendering || discovering ;; Know
taken ;; Bringing || ensued ;; Process_start
became ;; Becoming || served ;; Assistance || harm ;; Damaging || lawsuit ;; Legality
took place ;; Process_start
campaign ;; Military_operation
occurred ;; Coming_to_be || given ;; Giving
invasion ;; Attack || supporting ;; Supporting
caused ;; Causation || dropping ;; Motion_directional || surrender ;; Surrendering
occurred ;; Coming_to_be
walked ;; Self_motion || marched ;; Self_motion
called ;; Name_conferral || seized ;; Conquering
joined ;; Becoming_a_member
surrendered ;; Surrendering || barged ;; Self_motion || armored ;; Use_firearm
arrested ;; Arrest || detained ;; Arrest
released ;; Publishing
invasion ;; Attack || took place ;; Process_start
withdrawn ;; Escaping || taken up position ;; Temporary_stay
withdrew ;; Escaping || Battle ;; Hostile_encounter
became ;; Becoming
concluded ;; Process_end
surrendered ;; Surrendering
caused ;; Causation || incorporated ;; Cause_to_amalgamate
fought ;; Hostile_encounter || changing ;; Change
fought ;; Hostile_encounter || engagement ;; Hostile_encounter || Wars ;; Hostile_encounter
discovered ;; Know || blockading ;; Besieging || approaching ;; Arriving
captured ;; Conquering || recognised ;; Know || came ;; Self_motion
fled ;; Escaping || chase ;; Self_motion || gave ;; Placing
chase ;; Self_motion
arrive ;; Arriving || disabled ;; Destroying || raking ;; Damaging
surrendered ;; Surrendering || taking over ;; Change_of_leadership || bringing ;; Bringing
supporting ;; Supporting || War ;; Hostile_encounter || war ;; Hostile_encounter
beginning ;; Process_start || war ;; Hostile_encounter
surrendered ;; Surrendering || used ;; Using || end ;; Process_end || incorporated ;; Cause_to_amalgamate || defeated ;; Conquering || forces ;; Military_operation
declaration ;; Expressing_publicly || established ;; Creating || recognized ;; Know
wars ;; Hostile_encounter || War ;; Hostile_encounter
support ;; Supporting
ended ;; Process_end || started ;; Process_start || war ;; Hostile_encounter
sieges ;; Besieging
making it ;; Causation
changed ;; Change
besieged ;; Besieging
besieged ;; Besieging || deploying ;; Arranging || force ;; Request || surrender ;; Surrendering
retake ;; Conquering || failed ;; Earnings_and_losses
besieged ;; Besieging
captured ;; Conquering
besieged ;; Besieging || took place ;; Process_start
hurricane ;; Catastrophe
developed ;; Coming_to_be || named ;; Name_conferral
became ;; Becoming
turning ;; Becoming || weakening ;; Cause_change_of_strength || reached ;; Arriving
moved ;; Motion || caused ;; Causation || damage ;; Damaging || due ;; Causation || affected ;; Influence
brought ;; Bringing || dissipated ;; Dispersal
reported ;; Reporting
started ;; Process_start
influenced ;; Influence || inspired ;; Influence
began ;; Process_start
resulted in ;; Causation
responded ;; Response
gained ;; Getting
sponsored ;; Supply
put out ;; Manufacturing || promoted ;; Cause_change_of_position_on_a_scale
sponsored ;; Supply
known ;; Know || struck ;; Attack
originated ;; Coming_to_be
moved ;; Motion || increase ;; Cause_change_of_position_on_a_scale || upgraded ;; Cause_to_make_progress || storm ;; Catastrophe || classified ;; Check
attained ;; Getting || passing ;; Motion_directional || deepen ;; Cause_change_of_strength
reached ;; Arriving || accelerated ;; Cause_change_of_position_on_a_scale
weakened ;; Cause_change_of_strength
moved ;; Motion
emerged ;; Coming_to_be || storm ;; Catastrophe || deteriorated ;; Cause_change_of_position_on_a_scale
began ;; Process_start || weakening ;; Cause_change_of_strength || obtained ;; Getting
brought ;; Bringing || damage ;; Damaging || impacted ;; Influence || flooding ;; Catastrophe
deaths ;; Death || resulting in ;; Causation || noted ;; Know
occurred ;; Coming_to_be || demolished ;; Destroying
killed ;; Killing
affected ;; Influence
destroyed ;; Destroying || damaged ;; Damaging
left ;; Departing
damage ;; Damaging
affect ;; Influence || hurricane ;; Catastrophe
named ;; Name_conferral || developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe
crossed ;; Motion_directional || degenerated ;; Change || due ;; Causation
designated ;; Choosing || observed ;; Perception_active
Moving ;; Motion || entering ;; Arriving
began ;; Process_start || upgraded ;; Cause_to_make_progress || storm ;; Catastrophe
reached ;; Arriving
moving ;; Motion || weakened ;; Cause_change_of_strength || dissipated ;; Dispersal
deaths ;; Death
produced ;; Creating
formed ;; Coming_to_be
organized ;; Arranging || beginning ;; Process_start || Moving ;; Motion
intensified ;; Cause_change_of_strength || producing ;; Creating
intensified ;; Cause_change_of_strength || curved ;; Motion_directional
designated ;; Choosing
weakened ;; Cause_change_of_strength || becoming ;; Becoming || exiting ;; Departing || turned ;; Becoming
affected ;; Influence || dropped ;; Motion_directional
killed ;; Killing || causing ;; Causation
passed ;; Motion_directional || producing ;; Creating
left ;; Causation || left ;; Departing
dropped ;; Motion_directional || flooding ;; Catastrophe
leaving ;; Departing || destroyed ;; Destroying || damaged ;; Damaging
declared ;; Expressing_publicly
causing ;; Causation || damage ;; Damaging
detonated ;; Attack
frequented ;; Traveling
caused ;; Causation || killing ;; Killing || wounding ;; Bodily_harm || collapse ;; Catastrophe
attack ;; Attack
asserted ;; Statement || blame ;; Judgment_communication || exploded ;; Attack
found ;; Know || hindered ;; Hindering || biased ;; Judgment_communication || view ;; Perception_active
spread ;; Dispersal || discredit ;; Judgment_communication
bombing ;; Attack || served ;; Assistance || sentenced ;; Legal_rulings
contributed ;; Giving || conflict ;; Hostile_encounter || sparked ;; Causation || shootings ;; Use_firearm
feud ;; Hostile_encounter || involved ;; Cause_to_be_included
descended ;; Motion_directional || feud ;; Hostile_encounter || involved ;; Cause_to_be_included
feud ;; Hostile_encounter || entered ;; Becoming_a_member
become ;; Becoming || brought ;; Bringing || feud ;; Hostile_encounter
fought ;; Hostile_encounter || fought ;; Hostile_encounter
murdered ;; Killing || feud ;; Hostile_encounter || returned ;; Arriving
confirmed ;; Check
murder ;; Killing || believed ;; Know || committed ;; Committing_crime
owned ;; Earnings_and_losses
involved ;; Cause_to_be_included
battle ;; Hostile_encounter || took place ;; Process_start
attacked ;; Attack || support ;; Supporting || allowed ;; Preventing_or_letting || escape ;; Escaping
died ;; Death || lost ;; Earnings_and_losses
convicted ;; Legal_rulings || shot ;; Use_firearm
entitled ;; Name_conferral || inspired ;; Influence
determine ;; Deciding || Final ;; Competition || match ;; Competition || played ;; Competition
ended ;; Process_end || defeated ;; Conquering
stood ;; Placing || captained ;; Control || led ;; Bringing
defeated ;; Conquering || semi-finals ;; Competition
opted ;; Choosing || field ;; Motion
scored ;; Getting || loss ;; Earnings_and_losses
scored ;; Getting || opening ;; Openness || Batting ;; Self_motion
took ;; Getting
partnership ;; Collaboration || build ;; Creating || opening ;; Process_start
failed ;; Earnings_and_losses || order ;; Arranging || reached ;; Achieve || take ;; Getting || bowled ;; Competition
ended ;; Process_end || match ;; Competition || took ;; Getting
earned ;; Earnings_and_losses || match ;; Competition
named ;; Name_conferral || match ;; Competition
began ;; Process_start || concluded ;; Process_end
started ;; Process_start
started ;; Process_start
opened ;; Openness
culminated ;; Coming_to_be
released ;; Publishing
released ;; Publishing
played ;; Competition || tournament ;; Competition || Open ;; Openness
Open ;; Openness
took place ;; Process_start
announced ;; Expressing_publicly || failing ;; Earnings_and_losses || recover ;; Recovering || defending ;; Defending || injury ;; Bodily_harm || defending ;; Defending || withdrawal ;; Removing
competition ;; Competition || competition ;; Competition || record ;; Recording || tying ;; Connect
became ;; Becoming || winning ;; Getting || open ;; Openness || tying ;; Connect
becoming ;; Becoming || competition ;; Competition || open ;; Openness
winning ;; Getting
Open ;; Openness
holds ;; Hold
takes place ;; Process_start
open ;; Openness || held ;; Hold
draw ;; Getting || exceed ;; Cause_change_of_position_on_a_scale
held ;; Hold
held ;; Hold
competition ;; Competition || contested ;; Competition
scheduled ;; Arranging || celebrations ;; Social_event
tournament ;; Competition || hosted ;; Social_event || match ;; Competition || played ;; Competition
competition ;; Competition || open ;; Openness
played ;; Competition
kicked ;; Motion
match ;; Competition || played ;; Competition
televised ;; Perception_active || final ;; Competition
Games ;; Competition || Games ;; Competition || event ;; Competition || held ;; Hold
games ;; Competition || host ;; Social_event
hosted ;; Social_event
Games ;; Competition || games ;; Competition
commenced ;; Process_start || events ;; Competition || held ;; Hold
participated ;; Participation || event ;; Competition || events ;; Competition
opened ;; Openness
led ;; Placing || final ;; Process_end
broken ;; Destroying || Games ;; Competition || Records ;; Recording
competition ;; Competition || rising ;; Request || games ;; Competition
held ;; Hold
hosted ;; Social_event
host ;; Social_event
participated ;; Participation
opened ;; Openness
broken ;; Destroying
competition ;; Competition || deemed ;; Check
held ;; Hold || tournament ;; Competition
Championship ;; Competition
associate ;; Collaboration || tournament ;; Competition || contested ;; Participation
played ;; Competition || participant ;; Participation || playing ;; Competition
matches ;; Competition
tournament ;; Competition || player ;; Participation
Hurricane ;; Catastrophe || hurricane ;; Catastrophe || Hurricane ;; Catastrophe
formed ;; Coming_to_be || storm ;; Catastrophe || upgraded ;; Cause_change_of_position_on_a_scale
Moving ;; Motion || hurricane ;; Catastrophe || strengthened ;; Cause_change_of_position_on_a_scale
move ;; Motion || paralleling ;; Motion
hurricane ;; Catastrophe || entering ;; Arriving || attaining ;; Getting
moved ;; Motion
dissipated ;; Dispersal
emerged ;; Coming_to_be
lost ;; Earnings_and_losses || damage ;; Damaging || association ;; Collaboration || reported ;; Reporting || produced ;; Creating
used ;; Using || benefit ;; Earnings_and_losses || raise ;; Cause_change_of_position_on_a_scale
started ;; Process_start || included ;; Cause_to_be_included || featuring ;; Cause_to_be_included || shows ;; Social_event
causes ;; Causation || become ;; Becoming || shows ;; Social_event
Co-founded ;; Collaboration || shows ;; Social_event
seen ;; Perception_active || heard ;; Perception_active || shows ;; Social_event || featuring ;; Cause_to_be_included
released ;; Publishing
took place ;; Process_start
show ;; Social_event
killed ;; Killing || injured ;; Bodily_harm || bombing ;; Attack || travelling ;; Traveling
suspected ;; Scrutiny
became ;; Becoming
takes place ;; Process_start || known ;; Know
held ;; Hold
starts ;; Process_start || end ;; Process_end
travels ;; Traveling || finishes ;; Process_end
climb ;; Self_motion
begins ;; Process_start || finishing ;; Process_end || continues ;; Change
Tour ;; Traveling
followed ;; Motion
recorded ;; Recording || tour ;; Traveling
consisted ;; Cause_to_be_included || end ;; Process_end
released ;; Publishing
called ;; Name_conferral || attend ;; Participation || pass ;; Process_end
attend ;; Participation
released ;; Publishing
typhoon ;; Catastrophe || brushed ;; Motion
formed ;; Coming_to_be || storm ;; Catastrophe
storm ;; Catastrophe || classified ;; Check
strengthened ;; Cause_change_of_strength || reaching ;; Arriving || storm ;; Catastrophe
weakened ;; Cause_change_of_strength || began ;; Process_start || causing ;; Causation || storm ;; Catastrophe || recurve ;; Self_motion || parallel ;; Self_motion
weakened ;; Cause_change_of_strength || dissipating ;; Dispersal
effects ;; Influence || estimated ;; Statement
killed ;; Killing || struck ;; Attack || injured ;; Bodily_harm
injuring ;; Bodily_harm || killing ;; Killing || stormed ;; Attack
took ;; Conquering || claimed ;; Statement
took over ;; Conquering || killing ;; Killing || identified ;; Check || freeing ;; Releasing
killed ;; Killing || ended ;; Process_end
arrested ;; Arrest || attack ;; Attack || placed ;; Placing
bombings ;; Attack
conducted ;; Action || raid ;; Attack
attack ;; Attack || happened ;; Process_start
attacked ;; Attack
conducted ;; Action || escaped ;; Escaping || supported ;; Supporting || attack ;; Attack || started ;; Process_start
killed ;; Killing || attack ;; Attack || wounded ;; Bodily_harm
confirmed ;; Check || killed ;; Killing
sent ;; Sending || crashed ;; Catastrophe
reported ;; Reporting || destroyed ;; Destroying || crashed ;; Catastrophe
stated ;; Statement || destroyed ;; Destroying || damaged ;; Damaging || attack ;; Attack || destroyed ;; Destroying
released ;; Statement || attack ;; Attack || calling ;; Communication || preceded ;; Process_start
champions ;; Competition
match ;; Competition || contested ;; Competition
match ;; Competition || played ;; Competition
promote ;; Cause_change_of_position_on_a_scale
match ;; Competition || televised ;; Communication
marked ;; Recording || match ;; Competition || match ;; Competition || contested ;; Participation || final ;; Process_end || played ;; Competition
confirmed ;; Check || match ;; Competition
assisted ;; Assistance
match ;; Competition || finished ;; Process_end || defeating ;; Earnings_and_losses
champions ;; Competition || victory ;; Earnings_and_losses || gives ;; Giving
hosted ;; Social_event || record ;; Recording
tournaments ;; Competition
tournament ;; Competition || played ;; Competition
matches ;; Competition || tournament ;; Competition || played ;; Participation
announce ;; Expressing_publicly || involvement ;; Participation
confirmed ;; Check || tournament ;; Competition
matches ;; Competition || tournament ;; Competition || playing ;; Participation
competitions ;; Competition
tournament ;; Competition || games ;; Competition || played ;; Competition
involved ;; Participation || games ;; Competition || games ;; Competition || played ;; Competition || played ;; Competition
Select ;; Choosing || matches ;; Competition || games ;; Competition || allow ;; Preventing_or_letting
confirm ;; Check || involvement ;; Participation
Festival ;; Social_event || Festival ;; Social_event
called ;; Name_conferral || held ;; Hold
changed ;; Change
moved ;; Motion || search ;; Scrutiny
drawing ;; Getting
hurricane ;; Catastrophe || hurricane ;; Catastrophe || Hurricane ;; Catastrophe
strengthened ;; Cause_change_of_strength || becoming ;; Becoming || disturbance ;; Attack || storm ;; Catastrophe || hurricane ;; Catastrophe || hurricane ;; Catastrophe || Hurricane ;; Catastrophe || Forming ;; Coming_to_be || making ;; Causation || exiting ;; Departing
lost ;; Earnings_and_losses || making ;; Causation
passing ;; Motion_directional || dissipating ;; Dispersal || storm ;; Catastrophe || continued ;; Motion
effects ;; Influence || hurricane ;; Catastrophe || passage ;; Motion_directional || disappeared ;; Departing
found ;; Placing || search ;; Scrutiny
triggered ;; Causation || damaging ;; Damaging || occurred ;; Coming_to_be || responsible ;; Causation
attack ;; Attack || break ;; Destroying
attack ;; Attack || occurred ;; Presence
escaped ;; Escaping || leaving ;; Causation
captured ;; Conquering || escaped ;; Escaping
escaped ;; Escaping || attack ;; Attack || rearrested ;; Arrest
claimed ;; Statement || run away ;; Self_motion || returned ;; Arriving
Battle ;; Hostile_encounter || combined ;; Cause_to_amalgamate || fought ;; Hostile_encounter || March ;; Self_motion || commanded ;; Control || led ;; Causation
killing ;; Killing || capturing ;; Arrest || captured ;; Conquering
deployed ;; Arranging || approaching ;; Arriving || grasping ;; Know
retreat ;; Escaping || organize ;; Arranging || fighting ;; Hostile_encounter || realized ;; Know
battle ;; Hostile_encounter || crushing ;; Bodily_harm || routed ;; Bodily_harm || force ;; Military_operation || struck ;; Attack || losses ;; Earnings_and_losses
fighting ;; Hostile_encounter || struck ;; Attack || died ;; Death
took place ;; Process_start
losing ;; Earnings_and_losses
attacking ;; Attack || struck ;; Attack
escape ;; Escaping || overrun ;; Attack || interned ;; Arrest
dismantled ;; Destroying || replaced ;; Change_of_leadership
create ;; Creating || install ;; Building || acquiesce ;; Preventing_or_letting
coup ;; Change_of_leadership
overthrow ;; Change_of_leadership || attempted ;; Hold
coup ;; Change_of_leadership || included ;; Cause_to_be_included
arresting ;; Arrest
refused ;; Agree_or_refuse_to_act || arrests ;; Arrest || ended ;; Process_end
injuries ;; Bodily_harm
admitted ;; Agree_or_refuse_to_act || membership ;; Participation
Battle ;; Hostile_encounter || known ;; Know || took place ;; Process_start
took place ;; Process_start
threw ;; Motion
supported ;; Supporting || led ;; Causation
seized ;; Arrest || Battle ;; Hostile_encounter
died ;; Death
suicide attack ;; Terrorism
provide ;; Supply || supporting ;; Supporting || attacks ;; Judgment_communication || stationed ;; Placing
detonated ;; Attack || killed ;; Killing
wounded ;; Bodily_harm
attack ;; Attack
detained ;; Arrest || interrogated ;; Arrest
turned ;; Becoming || provide ;; Supply
used ;; Using || gain ;; Getting || attack ;; Attack
helped ;; Assistance || attack ;; Attack || saying ;; Communication
hurricanes ;; Catastrophe || record ;; Recording
developed ;; Coming_to_be || named ;; Name_conferral || hurricane ;; Catastrophe || storm ;; Catastrophe
intensified ;; Cause_change_of_strength || storm ;; Catastrophe
strengthening ;; Cause_change_of_strength || turned ;; Becoming || became ;; Becoming || upgraded ;; Cause_to_make_progress || hurricane ;; Catastrophe || hurricane ;; Catastrophe || ensued ;; Coming_to_be || curved ;; Motion_directional
storm ;; Catastrophe || weakened ;; Cause_change_of_strength || reemerged ;; Cause_to_amalgamate
hurricane ;; Catastrophe || headed ;; Self_motion || weaken ;; Cause_change_of_strength
made ;; Manufacturing || weakened ;; Cause_change_of_strength
dissipated ;; Dispersal || entering ;; Arriving
hurricane ;; Catastrophe || record ;; Recording
dropped ;; Motion_directional
produced ;; Manufacturing || caused ;; Causation || flooding ;; Motion
reported ;; Reporting
casualties ;; Catastrophe || due ;; Causation || flooding ;; Motion
destroyed ;; Destroying
storm ;; Catastrophe || reported ;; Reporting
damaged ;; Damaging
caused ;; Causation || contained ;; Containing
took ;; Conquering
storm ;; Catastrophe
caused ;; Causation || damage ;; Damaging
formed ;; Coming_to_be || Cyclone ;; Catastrophe || Cyclone ;; Catastrophe || windstorm ;; Catastrophe
began ;; Process_start || storm ;; Catastrophe || producing ;; Creating
damage ;; Damaging || reported ;; Reporting
named ;; Name_conferral || storm ;; Catastrophe || service ;; Supply
storm ;; Catastrophe || hit ;; Attack || called ;; Request
storm ;; Catastrophe || issue ;; Statement || service ;; Supply
referred ;; Name_conferral || took place ;; Process_start
pulled ;; Motion || driving ;; Self_motion
broke out ;; Process_start || escalated ;; Cause_change_of_strength
hurt ;; Bodily_harm || reported ;; Reporting || followed ;; Self_motion
helped ;; Assistance || damage ;; Damaging
unrest ;; Violence
known ;; Know || alleged ;; Statement || set up ;; Placing
included ;; Cause_to_be_included
started ;; Process_start || returned ;; Arriving || establish ;; Creating
reported ;; Reporting
reported ;; Reporting || commented ;; Statement
engage ;; Participation
generated ;; Creating
named ;; Name_conferral || formed ;; Coming_to_be || originated ;; Coming_to_be
bypassing ;; Self_motion || developed ;; Cause_to_make_progress
turned ;; Becoming || moving ;; Motion
intensified ;; Cause_change_of_strength
peaked ;; Self_motion
turned ;; Becoming || weakened ;; Cause_change_of_strength
storm ;; Catastrophe || weakened ;; Cause_change_of_strength
dissipated ;; Dispersal
brought ;; Bringing
produced ;; Manufacturing || caused ;; Causation || contributing ;; Giving
reported ;; Reporting
brought ;; Bringing || damage ;; Damaging
coupled ;; Cause_to_amalgamate || making ;; Causation || accrue ;; Cause_change_of_position_on_a_scale
felt ;; Perception_active
began ;; Process_start || moved ;; Motion || strengthened ;; Cause_change_of_strength
move ;; Motion || causing ;; Causation || proceeded ;; Motion
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe || emerging ;; Coming_to_be || reached ;; Achieve
weaken ;; Cause_change_of_strength || degenerating ;; Destroying
reported ;; Reporting || developing ;; Coming_to_be || producing ;; Creating || tracked ;; Self_motion
moving ;; Motion || strengthened ;; Cause_change_of_strength || tracking ;; Self_motion
occurred ;; Coming_to_be
hurricane ;; Catastrophe || impacted ;; Influence
ensued ;; Coming_to_be
occurred ;; Coming_to_be || amounted ;; Cause_change_of_position_on_a_scale
hurricane ;; Catastrophe || tracked ;; Self_motion
causing ;; Causation || inundated ;; Destroying
cut off ;; Having_or_lacking_access
caused ;; Causation
hurricane ;; Catastrophe || felt ;; Perception_active
inundating ;; Destroying || propelled ;; Causation || breaching ;; Damaging
caused ;; Causation
extended ;; Expansion || caused ;; Causation || effects ;; Influence
impacted ;; Influence
caused ;; Causation
moved ;; Motion || lessened ;; Cause_change_of_position_on_a_scale || resulting ;; Causation
hurricane ;; Catastrophe || making it ;; Causation
caused ;; Causation || hurricane ;; Catastrophe
retired ;; Removing
occur ;; Presence
developed ;; Coming_to_be
moved ;; Motion || curved ;; Motion_directional || attaining ;; Achieve
intensified ;; Cause_change_of_strength || reached ;; Achieve || reported ;; Reporting || reported ;; Reporting
merged ;; Cause_to_amalgamate || accelerated ;; Cause_change_of_position_on_a_scale || turning ;; Change
caused ;; Causation
dropped ;; Motion_directional
damage ;; Damaging || damage ;; Damaging
deaths ;; Death || reported ;; Reporting
caused ;; Causation || storm ;; Catastrophe || flooding ;; Catastrophe
storm ;; Catastrophe || developed ;; Cause_to_make_progress
crossing ;; Motion_directional || Forming ;; Creating || remained ;; Presence
storm ;; Catastrophe || intensified ;; Cause_change_of_strength
came ;; Motion || tracked ;; Self_motion
weakened ;; Cause_change_of_strength || weakened ;; Cause_change_of_strength
tracked ;; Self_motion || dissipating ;; Dispersal
caused ;; Causation || produced ;; Creating
caused ;; Causation || damaged ;; Damaging || occurred ;; Coming_to_be
reported ;; Reporting || damage ;; Damaging || fell ;; Motion_directional
damage ;; Damaging || occurred ;; Coming_to_be
caused ;; Causation || destroyed ;; Destroying || resulted in ;; Causation
reported ;; Reporting || Flooding ;; Catastrophe
occurred ;; Coming_to_be || tornado ;; Catastrophe
reported ;; Reporting || falling ;; Motion_directional || deaths ;; Death || drowning ;; Death
left ;; Departing
produced ;; Manufacturing || storm ;; Catastrophe
losses ;; Earnings_and_losses || storm ;; Catastrophe || attributed ;; Causation
struck ;; Attack
developed ;; Coming_to_be
strengthened ;; Cause_change_of_strength || intensified ;; Cause_change_of_strength
intensify ;; Cause_change_of_strength || strength ;; Cause_change_of_strength || peaked ;; Motion_directional || attained ;; Getting
landfall ;; Self_motion || dissipated ;; Dispersal
reported ;; Reporting
developed ;; Coming_to_be
tracked ;; Motion || curved ;; Motion_directional
intensified ;; Cause_change_of_strength
peaked ;; Cause_change_of_position_on_a_scale
re-curved ;; Motion
produced ;; Creating
reported ;; Reporting || left ;; Departing
caused ;; Causation
developed ;; Coming_to_be || storm ;; Catastrophe || hurricane ;; Catastrophe
acquired ;; Getting
strengthened ;; Cause_change_of_strength || hurricane ;; Catastrophe
hurricane ;; Catastrophe || peaked ;; Cause_change_of_position_on_a_scale
weakened ;; Cause_change_of_strength || became ;; Becoming || making ;; Self_motion
reported ;; Reporting
reported ;; Reporting || peaked ;; Cause_change_of_position_on_a_scale
caused ;; Causation || confined ;; Destroying || unroofed ;; Damaging
reported ;; Reporting
reporting ;; Reporting
earthquake ;; Catastrophe || occurred ;; Presence
reached ;; Arriving || felt ;; Perception_active
earthquake ;; Catastrophe || rains ;; Motion || temblor ;; Catastrophe || intensified ;; Cause_change_of_strength
dead ;; Death || reported ;; Reporting
suffered ;; Bodily_harm
earthquake ;; Catastrophe
known ;; Know || produced ;; Creating
intensified ;; Cause_change_of_strength || moving ;; Motion || developed ;; Coming_to_be
became ;; Becoming || formed ;; Coming_to_be || approached ;; Arriving
weakening ;; Cause_change_of_strength || attained ;; Getting
moved ;; Motion || landfall ;; Self_motion
became ;; Becoming || weakening ;; Cause_change_of_strength || dissipated ;; Dispersal
caused ;; Causation || passing ;; Motion_directional
caused ;; Causation || crossed ;; Motion_directional
struck ;; Attack || reported ;; Reporting
dropped ;; Motion
caused ;; Causation
killed ;; Killing || caused ;; Causation || destroyed ;; Destroying
conference ;; Social_event
conference ;; Social_event || took place ;; Hold
conferences ;; Social_event
revived ;; Recovering
begun ;; Process_start || stopped ;; Process_end || conference ;; Social_event
extended ;; Expansion
inspired ;; Influence
crash-landed ;; Self_motion
slammed ;; Motion || burst ;; Destroying
recovered ;; Recovering
occur ;; Coming_to_be || crashed ;; Catastrophe || killing ;; Killing || disaster ;; Catastrophe
accident ;; Catastrophe
took place ;; Process_start || rounds ;; Competition
win ;; Earnings_and_losses || advanced ;; Cause_to_make_progress || chance ;; Getting
played ;; Competition
used ;; Using || marks ;; Recording
tournament ;; Competition || marks ;; Recording
decided ;; Deciding || revert ;; Recovering || take place ;; Process_start || tournament ;; Competition
took place ;; Process_start
crisis ;; Catastrophe
removed ;; Removing
reinstated ;; Recovering
ousted ;; Change_of_leadership || replaced ;; Change_of_leadership
ouster ;; Departing
competitions ;; Competition || scheduled ;; Arranging || held ;; Hold
start ;; Process_start || finish ;; Process_end || race ;; Competition
competitions ;; Competition
competitions ;; Competition || race ;; Competition || contests ;; Competition || arrive at ;; Arriving || include ;; Cause_to_be_included || cycling ;; Competition || evolved ;; Coming_to_be
announced ;; Expressing_publicly || stated ;; Statement || cycling ;; Competition || extend ;; Expansion
changes ;; Change || made ;; Action
put out ;; Removing || expanded ;; Expansion
affected ;; Influence
death ;; Death || stood ;; Placing
lost ;; Death
believed ;; Know
rendered ;; Causation
recorded ;; Recording
killed ;; Killing
destroyed ;; Destroying
burnt ;; Destroying
destroyed ;; Destroying
damaged ;; Damaging || destroyed ;; Destroying
festival ;; Social_event
organised ;; Arranging
began ;; Process_start || voted ;; Choosing || voted ;; Choosing
described ;; Statement
calling ;; Name_conferral || describing ;; Statement
described ;; Statement
services ;; Supply || differs ;; Change
began ;; Process_start || growing ;; Expansion
festival ;; Social_event || incorporates ;; Cause_to_amalgamate || offering ;; Supply
celebrate ;; Social_event || festival ;; Social_event
bring ;; Bringing || described ;; Statement
began ;; Process_start || invaded ;; Attack
established ;; Building
launched ;; Process_start
growing ;; Expansion || ensured ;; Preventing_or_letting
prevented ;; Preventing_or_letting || occupied ;; Control || resulted in ;; Causation
cited ;; Statement
occurred ;; Coming_to_be || blasts ;; Attack
killed ;; Killing || explosions ;; Attack
said ;; Statement
dead ;; Death || construction ;; Building
injuries ;; Bodily_harm || said ;; Statement || wounded ;; Bodily_harm || due ;; Causation || overflowing ;; Expansion || lying ;; Placing
injuries ;; Bodily_harm || exceeded ;; Cause_change_of_position_on_a_scale
left ;; Causation
described ;; Statement || described ;; Statement || tsunami ;; Catastrophe || earthquake ;; Catastrophe
organized ;; Arranging || Show ;; Social_event || show ;; Social_event || Exhibition ;; Social_event
held ;; Hold || exhibition ;; Social_event || exhibitions ;; Social_event
started ;; Process_start || exhibition ;; Social_event
exhibition ;; Social_event || removed ;; Removing || went on ;; Motion
became ;; Becoming || show ;; Social_event || event ;; Social_event
became ;; Becoming || created ;; Creating || show ;; Social_event || served ;; Giving
challenged ;; Competition || enlarging ;; Expansion || show ;; Social_event || emergence ;; Coming_to_be || exhibitions ;; Social_event || expanding ;; Expansion || enhancing ;; Cause_to_make_progress
grown ;; Expansion
presents ;; Presence || festival ;; Social_event || event ;; Social_event
aimed ;; Aiming || festival ;; Social_event || expand ;; Expansion
focused ;; Aiming || reassembling ;; Creating || played ;; Participation
included ;; Cause_to_be_included || singing ;; Self_motion
include ;; Cause_to_be_included || played ;; Participation
took place ;; Process_start || festival ;; Social_event || celebrate ;; Social_event
organized ;; Arranging || founded ;; Creating || Festival ;; Social_event || festival ;; Social_event
festival ;; Social_event
festival ;; Social_event || festival ;; Social_event || led to ;; Causation || expanded ;; Expansion || attended ;; Participation
festival ;; Social_event || comprised ;; Cause_to_be_included
led to ;; Causation
festival ;; Social_event || scaled ;; Cause_change_of_position_on_a_scale
Festival ;; Social_event || Festival ;; Social_event || took place ;; Hold
fought ;; Hostile_encounter || Battle ;; Hostile_encounter
crossed ;; Motion_directional || command ;; Control || threaten ;; Warning
extended ;; Expansion
moved ;; Motion
drove ;; Motion
returned ;; Arriving
achieved ;; Achieve
charity ;; Assistance || occurs ;; Presence || race ;; Competition
reached ;; Arriving || taking part ;; Dispersal || entering ;; Arriving || record ;; Recording || event ;; Competition || event ;; Competition
grown ;; Expansion || event ;; Competition
entered ;; Arriving
charities ;; Assistance || involved ;; Cause_to_be_included
raised ;; Cause_change_of_position_on_a_scale || bringing ;; Bringing || raised ;; Cause_change_of_position_on_a_scale || race ;; Competition || started ;; Process_start || estimated ;; Statement
causes ;; Causation || decide ;; Deciding || gain ;; Getting || support ;; Supporting
charity ;; Assistance || event ;; Competition
election ;; Change_of_leadership || demanded ;; Request || movement ;; Social_event || movement ;; Social_event || created ;; Creating
originated ;; Coming_to_be
revolution ;; Change_of_leadership || involved ;; Cause_to_be_included || documenting ;; Recording
given ;; Giving || tear ;; Destroying || protects ;; Preventing_or_letting || Use ;; Using || deflect ;; Defending || resistance ;; Defending
extended ;; Expansion || transformed ;; Change
Festival ;; Social_event || festival ;; Social_event
festival ;; Social_event || grown ;; Expansion
programmed ;; Arranging
festival ;; Social_event || consists ;; Cause_to_be_included
festival ;; Social_event || pop ;; Social_event
played ;; Action
festival ;; Social_event || mixed ;; Cause_to_amalgamate || attendees ;; Participation || attracts ;; Influence
Festival ;; Social_event || festival ;; Social_event || Festival ;; Social_event || Awards ;; Achieve
storm ;; Catastrophe || cyclone ;; Catastrophe || cyclone ;; Catastrophe || record ;; Recording || named ;; Name_conferral
developed ;; Coming_to_be || cyclone ;; Catastrophe || named ;; Name_conferral
intensified ;; Cause_change_of_strength || attain ;; Getting
weakened ;; Cause_change_of_strength || encountering ;; Hostile_encounter || becoming ;; Becoming || cyclone ;; Catastrophe || hit ;; Attack
turned ;; Motion || dissipated ;; Dispersal
cyclones ;; Catastrophe || dissipate ;; Expansion || storms ;; Catastrophe
caused ;; Causation || damage ;; Damaging || cyclone ;; Catastrophe || cyclone ;; Catastrophe || deaths ;; Death || disaster ;; Catastrophe
caused ;; Causation || flooding ;; Catastrophe || damage ;; Damaging || reaching ;; Arriving || dropped ;; Motion
caused ;; Causation || deaths ;; Death || damage ;; Damaging || cyclone ;; Catastrophe
held ;; Hold
celebrated ;; Social_event
celebrated ;; Social_event || attracts ;; Influence
renamed ;; Name_conferral || centred ;; Placing
include ;; Cause_to_be_included
ravaged ;; Destroying
disrupted ;; Damaging
became ;; Becoming || made ;; Building
caused ;; Causation
developed ;; Coming_to_be || tracking ;; Motion || reaching ;; Achieve
began ;; Process_start || situated ;; Placing || strengthened ;; Cause_change_of_strength || intensifying ;; Cause_change_of_strength
weakening ;; Cause_change_of_strength || occurred ;; Presence || made ;; Creating
weakened ;; Cause_change_of_strength || reaching ;; Arriving || took ;; Motion
merged ;; Cause_to_amalgamate || weakened ;; Cause_change_of_strength || landfall ;; Motion || dissipated ;; Dispersal
became ;; Becoming || intensified ;; Cause_change_of_strength || moving ;; Motion || striking ;; Attack || Originating ;; Coming_to_be
spread ;; Dispersal
led to ;; Causation
championship ;; Competition || played ;; Competition
take ;; Getting || playoff ;; Competition || defeated ;; Preventing_or_letting
participated ;; Participation || received ;; Receiving || championship ;; Competition || increase ;; Cause_change_of_position_on_a_scale || increase ;; Cause_change_of_position_on_a_scale
using ;; Using
held ;; Hold || hosted ;; Social_event || playoff ;; Competition || end ;; Process_end || Championship ;; Competition
nicknamed ;; Name_conferral || championship ;; Competition
stood ;; Placing || win ;; Getting
playoff ;; Competition || went to ;; Motion
playoff ;; Competition || came ;; Motion || win ;; Getting
accident ;; Catastrophe || crash ;; Catastrophe
accident ;; Catastrophe || occurred ;; Coming_to_be || took off ;; Departing
accident ;; Catastrophe || killed ;; Killing
crash ;; Catastrophe || death ;; Death
attended ;; Participation
flown ;; Motion || buried ;; Placing
determined ;; Deciding || parked ;; Placing || crash ;; Catastrophe || remove ;; Removing || secured ;; Placing
accident ;; Catastrophe || crash ;; Catastrophe
observation ;; Perception_active || began ;; Process_start || hurricane ;; Catastrophe || hurricane ;; Catastrophe
developed ;; Coming_to_be || hurricane ;; Catastrophe
organized ;; Arranging || attain ;; Getting || predicted ;; Statement || remain ;; Presence
hurricane ;; Catastrophe || weakened ;; Cause_change_of_strength || dissipated ;; Removing
produced ;; Manufacturing || hurricane ;; Catastrophe || killing ;; Killing
experienced ;; Know
compounded ;; Cause_to_amalgamate || hurricanes ;; Catastrophe
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || War ;; Military_operation || took place ;; Process_start
retreating ;; Escaping || defeats ;; Hostile_encounter || commanded ;; Control
pursued ;; Self_motion || command ;; Control
stationed ;; Placing || rushed ;; Self_motion
fighting ;; Hostile_encounter || inflicting ;; Causation
arrived at ;; Arriving || left ;; Departing
starts ;; Process_start
attracts ;; Influence
centered ;; Placing || consists ;; Cause_to_be_included
held ;; Hold
participated ;; Participation
hosted ;; Social_event
hosted ;; Social_event
hosted ;; Social_event
closed ;; Process_end || opened ;; Process_start
followed ;; Action || host ;; Social_event
broken ;; Destroying
deemed ;; Response || rising ;; Cause_change_of_position_on_a_scale
organized ;; Arranging
invite ;; Request
challenge ;; Competition || launched ;; Process_start
challenge ;; Competition || launched ;; Process_start
understand ;; Know || highlight ;; Aiming || prevalent ;; Dispersal
documented ;; Recording || produced ;; Creating || reported ;; Reporting || report ;; Reporting
combination ;; Cause_to_amalgamate || occurred ;; Presence
moved ;; Motion || causing ;; Causation || record ;; Recording
brought ;; Bringing
reported ;; Reporting
Damage ;; Damaging || losses ;; Earnings_and_losses || occurred ;; Presence
damage ;; Damaging || losses ;; Earnings_and_losses
occurred ;; Presence
engagement ;; Hostile_encounter || ships ;; Sending || led ;; Conquering || encountered ;; Hostile_encounter
captured ;; Conquering
escorted ;; Patrolling || sailed ;; Self_motion
encountered ;; Hostile_encounter
captured ;; Conquering || made ;; Manufacturing || making it ;; Causation
lost ;; Earnings_and_losses || equipment ;; Supply
helped ;; Assistance || derail ;; Preventing_or_letting || make ;; Manufacturing
took place ;; Process_start
competed ;; Competition || competitors ;; Competition
sent ;; Sending || win ;; Getting
permitted ;; Preventing_or_letting || participate ;; Participation
participated ;; Participation || competition ;; Competition || becoming ;; Becoming
increased ;; Cause_change_of_position_on_a_scale || erupted ;; Process_start
complied ;; Agree_or_refuse_to_act
reducing ;; Cause_change_of_position_on_a_scale
send ;; Sending || competition ;; Competition || competing ;; Competition || defeating ;; Conquering || competed ;; Competition
send ;; Sending
battle ;; Hostile_encounter || Battle ;; Hostile_encounter || Uprising ;; Self_motion
command ;; Control || defeated ;; Conquering || command ;; Control
killed ;; Killing || battle ;; Hostile_encounter
raided ;; Attack || battle ;; Hostile_encounter || took place ;; Process_start || broke ;; Destroying
sent ;; Sending || supported ;; Supporting || halt ;; Preventing_or_letting
camped ;; Placing || approaching ;; Arriving || headed ;; Self_motion
reached ;; Arriving || seized ;; Conquering
attacked ;; Attack || split ;; Destroying
turned ;; Becoming || battle ;; Hostile_encounter || massacre ;; Killing || victims ;; Bodily_harm
fought ;; Hostile_encounter || managed ;; Hold
losses ;; Earnings_and_losses || reached ;; Arriving || dead ;; Death
seizing ;; Conquering || marched ;; Self_motion
retreat ;; Escaping || losing ;; Earnings_and_losses || battle ;; Hostile_encounter
Championship ;; Competition || Championships ;; Competition
hosted ;; Social_event
began ;; Process_start || ended ;; Process_end || game ;; Competition || played ;; Participation
win ;; Getting || defeated ;; Conquering
named ;; Name_conferral || tournament ;; Competition
win ;; Getting || defeated ;; Conquering || sending ;; Sending
promoted ;; Cause_change_of_position_on_a_scale || Championships ;; Competition
competition ;; Competition || took place ;; Process_start || tournament ;; Competition
posted ;; Sending
tournament ;; Competition || held ;; Hold || held ;; Hold
chosen ;; Choosing || tournament ;; Competition
round ;; Competition
announced ;; Expressing_publicly
named ;; Name_conferral || tournament ;; Competition || unveiled ;; Statement
Campaign ;; Military_operation || Battle ;; Military_operation || battle ;; Military_operation || War ;; Military_operation || Battle ;; Military_operation
derived ;; Creating
battle ;; Military_operation || Campaign ;; Military_operation || Offensive ;; Military_operation
Battle ;; Military_operation || Campaign ;; Military_operation || resulted in ;; Causation
sent ;; Sending || battle ;; Military_operation || conflict ;; Hostile_encounter || took place ;; Process_start || infiltrate ;; Attack
commanded ;; Control || surprised ;; Change_sentiment
battle ;; Military_operation
Command ;; Request || command ;; Control || command ;; Control || attacked ;; Attack || ordered ;; Request || nicknamed ;; Name_conferral || destroy ;; Destroying
withdrawal ;; Escaping || casualties ;; Catastrophe || fighting ;; Hostile_encounter || inflicting ;; Damaging || break out ;; Process_start
casualties ;; Catastrophe || supported ;; Supporting || suffered ;; Bodily_harm || offensive ;; Attack
retreat ;; Escaping || withdrawal ;; Escaping || complete ;; Process_end
invaded ;; Attack || reached ;; Arriving
agreed ;; Agree_or_refuse_to_act || become ;; Becoming || become ;; Becoming || choose ;; Choosing || allowing ;; Preventing_or_letting || allow ;; Preventing_or_letting
captured ;; Conquering
negotiations ;; Communication || sent ;; Sending || withdrew ;; Escaping
drew ;; Motion || invasion ;; Hostile_encounter
Beginning ;; Process_start || encroached ;; Bodily_harm
captured ;; Conquering || divided ;; Change || marching ;; Self_motion || move ;; Self_motion
retreat ;; Escaping || keep ;; Temporary_stay
agreed ;; Agree_or_refuse_to_act || reached ;; Arriving || become ;; Becoming || choose ;; Choosing || allowing ;; Preventing_or_letting || become ;; Becoming || allow ;; Preventing_or_letting
played ;; Competition
involved ;; Cause_to_be_included
started ;; Process_start || followed ;; Self_motion
champions ;; Competition
final ;; Process_end || established ;; Building
put ;; Placing
named ;; Name_conferral
explosion ;; Catastrophe || occurred ;; Coming_to_be
incident ;; Catastrophe || occurred ;; Coming_to_be
heard ;; Perception_active || incident ;; Catastrophe || blast ;; Catastrophe
blast ;; Catastrophe || injured ;; Bodily_harm
killed ;; Killing
damaged ;; Damaging || blast ;; Catastrophe
caused ;; Causation || blast ;; Catastrophe || leak ;; Motion
smelled ;; Perception_active || blast ;; Catastrophe
competing ;; Competition
competition ;; Competition
beat ;; Competition || lost ;; Earnings_and_losses
record ;; Recording || established ;; Creating
scored ;; Getting
record ;; Recording
competition ;; Competition || reached ;; Arriving
announced ;; Expressing_publicly || retirement ;; Departing
took place ;; Process_start || War ;; Hostile_encounter
overthrew ;; Change_of_leadership || started ;; Process_start
prompted ;; Causation || organize ;; Arranging || initiating ;; Process_start || coup ;; Change_of_leadership || campaign ;; Military_operation || rebels ;; Hostile_encounter
support ;; Supporting || transformed ;; Change || States ;; Statement || rebels ;; Hostile_encounter || Allegations ;; Judgment_communication || led to ;; Causation || intervention ;; Participation || conflict ;; Hostile_encounter || occupation ;; Control
seat ;; Placing || Elections ;; Change_of_leadership || aftermath ;; Catastrophe || held ;; Hold || elected ;; Change_of_leadership
departed ;; Departing
hurricane ;; Catastrophe || hit ;; Attack
formed ;; Coming_to_be || intensifying ;; Cause_change_of_strength
followed ;; Self_motion || moved ;; Motion || turn ;; Change
reached ;; Arriving || moving ;; Motion
destroyed ;; Destroying || damage ;; Damaging || leaving ;; Departing
killed ;; Killing || drowning ;; Killing
hurricane ;; Catastrophe || storm ;; Catastrophe || produced ;; Causation || rainfall ;; Motion
hurricane ;; Catastrophe || arrived at ;; Arriving
drift ;; Motion || landfall ;; Catastrophe || made ;; Causation || disturbance ;; Damaging
moved ;; Motion || storm ;; Catastrophe || persisting ;; Presence || dissipating ;; Departing
damaged ;; Damaging
killed ;; Killing || suffered ;; Bodily_harm
wrecked ;; Damaging
killed ;; Killing || occurred ;; Coming_to_be || deaths ;; Death || damage ;; Damaging
killed ;; Killing || hurricane ;; Catastrophe || reported ;; Reporting || arriving in ;; Arriving || rainfall ;; Motion || deaths ;; Death || track ;; Motion
Battle ;; Hostile_encounter || battle ;; Hostile_encounter
battle ;; Hostile_encounter || fought ;; Hostile_encounter
dislodge ;; Removing || called ;; Request
left ;; Departing || relocated ;; Removing || enable ;; Preventing_or_letting
retreat ;; Escaping || inflicting ;; Bodily_harm || defeated ;; Conquering || forcing ;; Hostile_encounter
included ;; Cause_to_be_included
takes place ;; Process_start
begin ;; Process_start
named ;; Name_conferral
organizing ;; Arranging
come to ;; Self_motion
helps ;; Assistance || produce ;; Manufacturing
ceremony ;; Social_event || closing ;; Process_end || held ;; Hold
ceremony ;; Social_event || closing ;; Process_end || ceremony ;; Social_event || directed ;; Action
ceremony ;; Social_event || set ;; Placing || performed ;; Hold || joined ;; Participation
ceremony ;; Social_event || passed ;; Giving
congratulated ;; Judgment_communication || speech ;; Statement || closing ;; Process_end
offensives ;; Attack
called ;; Statement || came ;; Motion || loss ;; Earnings_and_losses
attack ;; Attack || involved ;; Participation
Launched ;; Self_motion
took place ;; Process_start
charge ;; Judgment_communication
League ;; Competition || established ;; Creating
commenced ;; Process_start || final ;; Competition || held ;; Hold
started ;; Process_start || competition ;; Competition || played ;; Competition
semi-finals ;; Competition || final ;; Competition || followed ;; Arriving
named ;; Name_conferral || win ;; Getting || defeated ;; Preventing_or_letting || adjudged ;; Judgment_communication
tournament ;; Competition
awarded ;; Giving
show ;; Social_event
match ;; Competition || littered ;; Placing
used ;; Using
matches ;; Competition || littered ;; Placing || pin ;; Judgment_communication || win ;; Getting
used ;; Using
tournament ;; Competition || Games ;; Competition || held ;; Hold
held ;; Hold
used ;; Using || Games ;; Competition
competition ;; Competition || players ;; Competition || participate ;; Participation
defeating ;; Earnings_and_losses
becoming ;; Becoming
lost ;; Competition || lost ;; Earnings_and_losses || game ;; Competition || tournament ;; Competition
match ;; Competition || beating ;; Competition
claims ;; Statement
directed ;; Aiming
described ;; Statement
begun ;; Process_start
prepared ;; GetReady
went ;; Self_motion
led to ;; Causation
defeated ;; Conquering
used ;; Using
taking place ;; Process_start
organised ;; Arranging || took place ;; Process_start
use ;; Using || beginning ;; Process_start
appear ;; Presence
organised ;; Arranging
performing ;; Hold || appearing ;; Presence
took place ;; Hold
running ;; Self_motion
race ;; Competition || race ;; Competition
race ;; Competition
race ;; Competition || race ;; Competition
record ;; Recording || set ;; Causation || running ;; Self_motion
race ;; Competition || participants ;; Participation
championships ;; Competition
held ;; Hold
matches ;; Competition || played ;; Competition
tournament ;; Competition || came ;; Motion
held ;; Hold
tournament ;; Competition || organised ;; Arranging
defeated ;; Conquering || record ;; Recording || set ;; Action
defeated ;; Conquering
earthquakes ;; Catastrophe || struck ;; Attack || deaths ;; Death || damage ;; Damaging || causing ;; Causation
became ;; Becoming || earthquakes ;; Catastrophe
earthquake ;; Catastrophe || struck ;; Attack || registering ;; Recording
epicentre ;; Catastrophe
aftershocks ;; Catastrophe || occurred ;; Coming_to_be || event ;; Process_end || event ;; Process_end
killed ;; Killing
earthquake ;; Catastrophe || earthquake ;; Catastrophe || struck ;; Attack || causing ;; Causation || deaths ;; Death || damage ;; Damaging || weakened ;; Cause_change_of_strength
quake ;; Catastrophe || occurred ;; Coming_to_be || epicentre ;; Catastrophe
conducted ;; Action || command ;; Control
renamed ;; Name_conferral || control ;; Control || marked ;; Recording || took ;; Creating || held ;; Defending
renamed ;; Name_conferral || occupied ;; Control || captured ;; Conquering
capture ;; Conquering
issues ;; Creating
resulted in ;; Causation || occupy ;; Control
held ;; Defending || Festival ;; Social_event
started ;; Process_start
record ;; Recording || issued ;; Creating
record ;; Recording
led to ;; Causation
festival ;; Social_event
running ;; Self_motion || control ;; Control || festival ;; Social_event || due ;; Causation
took place ;; Process_start || wrestling ;; Competition || produced ;; Creating
wrestling ;; Competition
defeated ;; Conquering
match ;; Competition || defeated ;; Conquering
wrestled ;; Hostile_encounter
contest ;; Competition
show up ;; Participation
feast ;; Social_event || match ;; Competition || marks ;; Recording
became ;; Becoming || launch ;; Process_start
takes place ;; Process_start
holds ;; Defending
competition ;; Competition
competition ;; Competition || records ;; Recording || set ;; Placing
attract ;; Influence
wrestling ;; Competition || produced ;; Creating || took place ;; Process_start
schedule ;; Arranging
wrestling ;; Competition
pitting ;; Motion
defended ;; Defending || retain ;; Preserving
retain ;; Preserving || defeated ;; Conquering
lost ;; Earnings_and_losses || fought ;; Hostile_encounter || fired ;; Process_end
causing ;; Causation || fired ;; Process_end
ranked ;; Placing || marking ;; Recording || return ;; Motion
given ;; Giving || wrestling ;; Competition || rated ;; Placing
became ;; Becoming || stream ;; Motion || demand ;; Request || launch ;; Hold
Championships ;; Competition || tournament ;; Competition || played ;; Competition
held ;; Hold || Championships ;; Competition
event ;; Competition
championships ;; Competition || organised ;; Arranging
lost ;; Competition || eliminated ;; Removing
marked ;; Recording || eliminated ;; Cause_change_of_position_on_a_scale || quarterfinals ;; Competition
became ;; Becoming || win ;; Getting
became ;; Becoming || completed ;; Process_end || hold ;; Hold
competitions ;; Competition || match ;; Competition || contested ;; Competition
reached ;; Arriving
beaten ;; Competition || beat ;; Competition
record ;; Recording || set ;; Placing
match ;; Competition || took place ;; Hold
defeated ;; Conquering || win ;; Getting
earthquake ;; Catastrophe
sustained ;; Causation || casualties ;; Killing || taken ;; Creating
quake ;; Catastrophe || preceded ;; Presence || quake ;; Motion || occurred ;; Coming_to_be
earthquake ;; Catastrophe || earthquakes ;; Catastrophe || listed ;; Recording
earthquake ;; Catastrophe || dead ;; Death || earthquakes ;; Catastrophe
mention ;; Statement || death ;; Death
disaster ;; Catastrophe || occurred ;; Coming_to_be
destroyed ;; Destroying || dock ;; Motion || caught fire ;; Damaging
fatalities ;; Death
disaster ;; Catastrophe || coverage ;; Social_event || recorded ;; Recording
cause ;; Causation || put ;; Placing
shattered ;; Destroying || end ;; Process_end || marked ;; Recording
played ;; Competition || held ;; Hold || beat ;; Competition
contested ;; Competition
playing ;; Competition || making ;; Action || finish ;; Process_end
took ;; Action || equalise ;; Defending
moved ;; Becoming
reduced ;; Cause_change_of_position_on_a_scale || taking ;; Getting || lead ;; Causation
increased ;; Cause_change_of_position_on_a_scale
squeezed ;; Motion || win ;; Getting
organised ;; Arranging || festival ;; Social_event
launched ;; Process_start || festival ;; Social_event
gave ;; Giving || play ;; Participation
existence ;; Presence || acts ;; Action || festival ;; Social_event
included ;; Cause_to_be_included || gaming ;; Competition
took place ;; Process_start
took part ;; Dispersal || playing ;; Participation
played ;; Competition || played ;; Competition
became ;; Becoming
included ;; Cause_to_be_included
playing ;; Participation
finish ;; Process_end || returning ;; Arriving || defeating ;; Preventing_or_letting || played ;; Competition
lose ;; Earnings_and_losses
play ;; Competition
finishing ;; Process_end || playing ;; Participation
finished ;; Process_end || faced ;; Hostile_encounter
resulting in ;; Causation
control ;; Control
inspired ;; Influence || wished ;; Request || create ;; Creating || Festival ;; Social_event || festival ;; Social_event || founded ;; Creating || asserted ;; Statement
organized ;; Arranging || called ;; Request || festival ;; Social_event
festival ;; Social_event
acts ;; Action
performed ;; Hold
organized ;; Arranging
came ;; Motion || came ;; Motion
made history ;; Creating || win ;; Earnings_and_losses
competition ;; Competition || hosted ;; Social_event
occur ;; Presence
landing ;; Self_motion || occurred ;; Coming_to_be
ending ;; Process_end || died ;; Death || fought ;; Hostile_encounter
wounded ;; Bodily_harm || died ;; Death
casualties ;; Killing
casualties ;; Killing
repulsed ;; Defending || counter-attacked ;; Attack
provoked ;; Causation
asked ;; Request
lost ;; Earnings_and_losses
attracted ;; Influence
tournament ;; Competition || played ;; Competition
event ;; Competition
ran ;; Process_start || took place ;; Process_start
defending ;; Defending || events ;; Competition
defended ;; Defending || defeated ;; Conquering || quarterfinals ;; Competition
Championship ;; Competition || organised ;; Arranging || Championship ;; Competition || championship ;; Competition
held ;; Defending || tournament ;; Competition
played ;; Participation || tournament ;; Competition
tournament ;; Competition
tournament ;; Competition
organised ;; Arranging || tournament ;; Competition
competition ;; Competition || contested ;; Competition
hosted ;; Social_event
beat ;; Competition || remains ;; Defending
