communicator == athletes ;; place == Russia ;; recipient == us , and the world ;; trigger == deceive ;; contact.prevarication.broadcast
victim == Russians ;; trigger == slaughtered ;; life.die.deathcausedbyviolentevents
destination == Turkey ;; origin == Syria ;; passenger == oil ;; trigger == smuggled ;; movement.transportperson.smuggleextract
yielder == Japan ;; trigger == surrender ;; conflict.yield.n/a
beneficiary == the Clinton Foundation ;; giver == the Qataris ;; recipient == millions ;; trigger == donated ;; transaction.transferownership.giftgrantprovideaid
artifact == agreement ;; trigger == wrecking ;; artifactexistence.damagedestroy.n/a
employee == team of government regulators ;; trigger == employed ;; personnel.startposition.hiring
defendant == at least 27 of them ;; executioner == Iranian security forces ;; place == public squares ;; trigger == executed ;; justice.judicialconsequences.execute
destination == the Trump Tower lobby ;; transporter == Japanese tourists ;; trigger == roamed ;; movement.transportperson.selfmotion
participant == Bush ’s administration ;; trigger == negotiated ;; contact.discussion.meet
beneficiary == environmental groups ;; giver == Kremlin ;; trigger == funding ;; transaction.transferownership.giftgrantprovideaid
communicator == President Obama ;; place == the White House ;; recipient == China ;; trigger == urged ;; contact.commandorder.broadcast
killer == a nanny ;; place == Moscow ;; victim == a child ;; trigger == murder ;; life.die.deathcausedbyviolentevents
beneficiary == Trump ;; giver == Sheldon Adelson ;; recipient == his election ;; trigger == donated ;; transaction.transaction.giftgrantprovideaid
trigger == killing ;; life.die.deathcausedbyviolentevents
place == France ;; victim == people ;; trigger == injured ;; life.injure.n/a
employee == men ;; place == a special forces base ;; placeofemployment == Wagner ;; trigger == recruits ;; personnel.startposition.hiring
detainee == rival politicians ;; jailer == it ;; place == Pakistan ;; trigger == jailed ;; justice.arrestjaildetain.arrestjaildetain
crime == meeting with Igor Sechin and Sergei Ivanov ;; defendant == Page ;; investigator == FBI ;; place == Moscow ;; trigger == investigating ;; justice.investigate.investigatecrime
attacker == ISIS ;; place == al - Bagilya ;; target == families ;; trigger == massacre ;; conflict.attack.selfdirectedbattle
inspectedentity == damage ;; inspector == he ;; place == Ankara ;; trigger == inspected ;; inspection.sensoryobserve.physicalinvestigateinspect
trigger == capitulation ;; conflict.yield.n/a
place == Istanbul ;; yielder == Soldiers ;; trigger == surrender ;; conflict.yield.n/a
trigger == death penalty ;; justice.judicialconsequences.execute
attacker == advancing army ;; place == the country ;; target == the country ;; trigger == battle ;; conflict.attack.selfdirectedbattle
place == Yemeni ;; trigger == funeral ;; contact.funeralvigil.n/a
artifact == oil ;; destination == Turkey ;; origin == Syria and Iraq ;; transporter == Erdogan ;; vehicle == trucks ;; trigger == importing ;; movement.transportartifact.receiveimport
beneficiary == Police ;; giver == Police ;; place == Zakharchenko ’s ] house ;; recipient == a cache ;; trigger == confiscated ;; transaction.transaction.transfercontrol
origin == jail ;; passenger == Harold Martin ;; preventer == jail ;; trigger == jail ;; movement.transportperson.preventexit
trigger == genocide ;; life.die.n/a
injurer == Trump ;; victim == women ;; trigger == assaulted ;; life.injure.n/a
attacker == terrorist ;; target == civilians ;; trigger == stabbings ;; conflict.attack.stabbing
crime == economic espionage ;; defendant == five Chinese military hackers ;; prosecutor == United States ;; trigger == charged ;; justice.initiatejudicialprocess.n/a
artifact == barbecue grills ;; giver == sheriff 's office ;; trigger == purchase ;; transaction.transferownership.purchase
injurer == man ;; place == Donald Trump rally ;; victim == protester ;; trigger == punching ;; life.injure.injurycausedbyviolentevents
recipient == bitcoin , the cryptocurrency ;; trigger == auction ;; transaction.transfermoney.purchase
communicator == Damascus ;; place == border ;; recipient == intervene ;; trigger == vowing ;; contact.commitmentpromiseexpressintent.broadcast
destination == America ;; passenger == human ;; trigger == smuggling ;; movement.transportperson.smuggleextract
recipient == some people ;; trigger == obtain ;; transaction.transfermoney.purchase
place == Syria ;; surrenderer == United States ;; trigger == capitulation ;; conflict.yield.surrender
deceased == their dead ;; participant == they ^^ they ;; place == a place ;; trigger == burials ;; contact.funeralvigil.meet
beneficiary == Russia ;; observedentity == Turkey ;; place == along the Turkey - Syria border ;; spy == Russian military ;; trigger == surveillance ;; government.spy.spy
communicator == Trump ;; trigger == lied ;; contact.prevarication.broadcast
crime == did n't have money to pay the fines ;; detainee == people ;; place == Birmingham ;; trigger == arrested ;; justice.arrestjaildetain.arrestjaildetain
place == Washington , D.C. ;; victim == Rich ;; trigger == murder ;; life.die.deathcausedbyviolentevents
deceased == opponents ;; trigger == Funeral ;; contact.funeralvigil.meet
place == Middle East ;; victim == Khan ;; trigger == died ;; life.die.nonviolentdeath
attacker == Dahir Adan ;; place == Minnesota ;; target == Nine people ;; trigger == stabbings ;; conflict.attack.n/a
injurer == allegation ;; victim == Trump ;; trigger == rape ;; life.injure.n/a
beneficiary == economy ;; giver == banks ;; money == $ 10 billion ;; place == Saudi Arabia ;; recipient == country ;; trigger == loan ;; transaction.transfermoney.borrowlend
detainee == Clinton ;; jailer == he ;; trigger == incarcerate ;; justice.arrestjaildetain.arrestjaildetain
founder == Kurdish groups ;; gpe == de facto autonomy ;; place == Syria ;; trigger == established ;; government.formation.startgpe
participant == Special prosecutor Robert Mueller ^^ senior Senate Judiciary Committee ;; trigger == meet ;; contact.discussion.meet
beneficiary == Trump ;; observedentity == Clinton ;; spy == Russia ;; trigger == spy ;; government.spy.spy
giver == Trumps ;; trigger == bought ;; transaction.transfermoney.purchase
defendant == Ms. Abedin ;; prosecutor == officials ;; trigger == testified ;; justice.initiatejudicialprocess.trialhearing
participant == adults ;; trigger == telephone ;; contact.collaborate.n/a
beneficiary == she ;; giver == elites ;; recipient == she ;; trigger == funded ;; transaction.transferownership.giftgrantprovideaid
participant == Trump ^^ Clinton ;; place == Las Vegas ;; trigger == debate ;; contact.discussion.meet
beneficiary == al Qaeda / ISIS ;; place == Iraq ;; recipient == weapons ;; trigger == seized ;; transaction.transaction.transfercontrol
killer == Ottoman authorities ;; place == their capital , Constantinople , present - day Istanbul ;; victim == 250 Armenian intellectuals ;; trigger == genocide ;; life.die.deathcausedbyviolentevents
place == the square ;; target == Issa ;; trigger == hanged ;; conflict.attack.hanging
communicator == America ;; place == America ;; recipient == Russia ;; trigger == blackmail ;; contact.threatencoerce.broadcast
communicator == Turkey ;; trigger == proposed ;; contact.publicstatementinperson.broadcast
beneficiary == top earners ;; money == wages ;; recipient == top earners ;; trigger == wages ;; transaction.transfermoney.n/a
artifact == Mr. Assange ;; destination == Sweden ;; origin == London ;; preventer == Ecuadorean Embassy ;; transporter == Sweden ;; trigger == arrested ;; movement.transportartifact.preventexit
participant == Trump ^^ Clinton ;; trigger == debate ;; contact.negotiate.correspondence
placeofemployment == Islamist militancy ;; trigger == recruits ;; personnel.startposition.n/a
crashobject == asteroid ;; trigger == collision ;; disaster.accidentcrash.accidentcrash
attacker == Russia ;; place == Aleppo ;; target == Aleppo ;; trigger == bombarding ;; conflict.attack.selfdirectedbattle
origin == the metro station entrance in north - west Moscow ;; transporter == a suicide bomber ;; trigger == walked ;; movement.transportperson.selfmotion
instrument == destroyers ;; killer == destroyers ;; place == the water ;; victim == they ;; trigger == shot ;; life.die.deathcausedbyviolentevents
participant == Russia ^^ U.S. ;; place == Syria ;; trigger == ceasefire ;; government.agreements.n/a
employee == chairwoman ;; place == U.S. ;; placeofemployment == party ;; trigger == resigned ;; personnel.endposition.n/a
damagerdestroyer == Neoconservative wrecking crew ;; trigger == wrecking ;; artifactexistence.damagedestroy.n/a
giver == Senators McCain and Corker ;; money == taxpayer dollars ;; place == the Arms Control Association ;; recipient == taxpayer dollars ;; trigger == acquire ;; transaction.transfermoney.purchase
attacker == Russian air forces ;; place == Aleppo province ;; target == ISIS oil tankers ;; trigger == annihilate ;; conflict.attack.n/a
instrument == polonium ;; trigger == fatal ;; life.die.deathcausedbyviolentevents
employee == Bashar al - Assad ;; trigger == oust ;; personnel.endposition.n/a
place == Turkey ;; victim == warplane ;; trigger == shot ;; life.die.n/a
artifact == Trump ;; damagerdestroyer == dark secret society ;; trigger == wreck ;; artifactexistence.damagedestroy.n/a
giver == Canadian authorities ;; recipient == a kilogram of carfentanil ;; trigger == seized ;; transaction.transaction.transfercontrol
attacker == perpetrator ;; target == Russians ;; trigger == hanged ;; conflict.attack.hanging
detainee == several people ;; jailer == police ;; trigger == detained ;; justice.arrestjaildetain.arrestjaildetain
transporter == African - American baby ;; trigger == incarcerated ;; movement.transportperson.preventexit
participant == United States ^^ Iran ;; trigger == agreement ;; government.agreements.acceptagreementcontractceasefire
participant == Alex Marlow ^^ former Navy SEAL and Blackwater CEO Erik Prince ;; trigger == discussed ;; contact.discussion.meet
victim == they ;; trigger == sick ;; life.injure.illnessdegradationphysical
injurer == extremists ;; trigger == violent ;; life.injure.n/a
origin == Aleppo ;; passenger == injured man ;; transporter == Syrians ;; trigger == evacuate ;; movement.transportperson.n/a
artifact == tax reform ;; instrument == muscle ;; trigger == manufacturing ;; manufacture.artifact.n/a
injurer == I ;; victim == him ;; trigger == punch ;; life.injure.n/a
participant == Russian ^^ DNC ;; trigger == emails ;; contact.collaborate.n/a
beneficiary == Million Mom March ;; giver == he ;; money == funds ;; place == the city ;; recipient == funds ;; trigger == reimbursed ;; transaction.transfermoney.payforservice
place == U.S.-Mexico border ;; target == Wall ;; trigger == bombarded ;; conflict.attack.n/a
communicator == the group ;; trigger == recommended ;; contact.requestadvise.correspondence
communicator == Hillary Clinton ;; trigger == reiterated ;; contact.publicstatementinperson.n/a
deceased == girl ;; place == Kiryat Arba ;; trigger == funeral ;; contact.funeralvigil.n/a
communicator == she ;; place == Germany ;; recipient == refugees from Iraq and Syria ;; trigger == urged ;; contact.requestadvise.correspondence
employee == Abedin ;; trigger == quits ;; personnel.endposition.n/a
communicator == Cuban lawsuit ;; recipient == US compensation ;; trigger == demanded ;; contact.commandorder.broadcast
beneficiary == Heede ;; giver == Greenpeace , 350.org , and the Union of Concerned Scientists ;; place == San Francisco Bay ;; recipient == Heede ;; trigger == rented ;; transaction.transfermoney.borrowlend
instrument == nuclear strike ;; place == either country ;; target == no military target ;; trigger == strike ;; conflict.attack.n/a
attacker == Saudi Arabia and its regional allies ;; instrument == Scud - type Borkan-1 ( Volcano-1 ) missile ;; place == Yemen ;; target == Yemen ;; trigger == airstrike ;; conflict.attack.airstrikemissilestrike
artifact == pests such as rats ;; destroyer == Biological warfare troops and civilian emergencies specialists ;; trigger == eradicate ;; artifactexistence.damagedestroy.destroy
communicator == Page ;; recipient == companies ;; trigger == advised ;; contact.requestadvise.meet
artifact == poverty and hunger ;; damagerdestroyer == Cuba ;; place == Cuba ;; trigger == eradicate ;; artifactexistence.damagedestroy.n/a
trigger == recruiting ;; personnel.startposition.hiring
candidate == Clinton ;; place == New York ;; trigger == winning election ;; personnel.elect.n/a
communicator == Vitaly Churkin ;; trigger == announced ;; contact.publicstatementinperson.n/a
attacker == he ;; target == Julian Assange ;; trigger == assailed ;; conflict.attack.n/a
place == Tehran ;; trigger == execution ;; justice.judicialconsequences.execute
deceased == a baby ;; place == Gross Fredenwalde ;; trigger == burials ;; contact.funeralvigil.meet
giver == he ;; recipient == very much ;; trigger == paid ;; transaction.transfermoney.payforservice
place == Syria ;; victim == 280 victims , including women , children and old people ;; trigger == massacre ;; life.die.n/a
artifact == carfentanil ;; origin == Chinese ;; trigger == trafficking ;; movement.transportartifact.smuggleextract
place == Minnesota mall ;; trigger == stabbings ;; conflict.attack.n/a
inspectedentity == handset ’s processor ;; inspector == hackers ;; trigger == inspecting ;; inspection.sensoryobserve.physicalinvestigateinspect
killer == She ;; victim == child ;; trigger == decapitated ;; life.die.deathcausedbyviolentevents
beneficiary == ISIS ;; giver == the Russian and Syrian governments ;; trigger == purchasing ;; transaction.transfermoney.purchase
destination == Calais ;; preventer == Police ;; transporter == a rowdy anti - migrant protest ;; trigger == detained ;; movement.transportartifact.preventexit
place == Syria ;; trigger == starvation ;; life.injure.illnessdegradationhungerthirst
trigger == suggest ;; contact.requestadvise.broadcast
artifact == evidence ;; manufacturer == campaign ;; trigger == assembled ;; manufacture.artifact.n/a
communicator == Trump ;; recipient == NATO ;; trigger == threatened ;; contact.threatencoerce.correspondence
injurer == He ;; victim == she ;; trigger == bruises ;; life.injure.n/a
beneficiary == Police ;; giver == Epstein ;; place == Palm Beach mansion ;; recipient == trash ;; territoryorfacility == Palm Beach mansion ;; trigger == confiscated ;; transaction.transaction.transfercontrol
communicator == 41-year - old mother of three told Rossiya 24 ;; place == broadcast ;; recipient == interview ;; trigger == broadcast ;; contact.mediastatement.n/a
passenger == Yulia Tymoshenko ;; trigger == imprisoned ;; movement.transportperson.preventexit
recipient == Obama ;; trigger == misled ;; contact.prevarication.broadcast
participant == Trump ^^ media ;; trigger == interviews ;; contact.discussion.meet
beneficiary == He ;; giver == He ;; recipient == Miss Universe pageant ;; trigger == purchasing ;; transaction.transfermoney.purchase
artifact == 30 billion euros ;; destination == Turkey ;; transporter == industry ;; trigger == brings in ;; movement.transportartifact.grantentry
beneficiary == Bill Clinton ;; giver == UBS and Barclays , mining giant BHP , and the for - profit educational company Laureate International Universities ;; recipient == Bill Clinton ;; trigger == payments ;; transaction.transfermoney.payforservice
attacker == us , service members and our country ;; place == Iraq and Libya and now in Syria ;; trigger == wars ;; conflict.attack.selfdirectedbattle
crime == charges relating to terrorism ;; detainee == 30 foreign - born individuals ;; place == U.S. ;; trigger == arrested ;; justice.arrestjaildetain.arrestjaildetain
detainee == Snowden ;; place == home ;; trigger == jail ;; justice.arrestjaildetain.arrestjaildetain
crime == the promotion of low - fat diets ;; defendant == nutritionists ;; trigger == charge ;; justice.initiatejudicialprocess.chargeindict
injurer == Pence ;; instrument == policies ;; victim == minorities and women ;; trigger == hurt ;; life.injure.injurycausedbyviolentevents
place == Iraq ;; victim == friends ;; trigger == die ;; life.die.deathcausedbyviolentevents
victim == patients ;; trigger == dehydrated ;; life.injure.illnessdegradationhungerthirst
communicator == Hillaryland ;; recipient == people ;; trigger == intimidated ;; contact.threatencoerce.broadcast
communicator == who or what ;; trigger == broadcasts ;; contact.mediastatement.broadcast
attacker == Seth Rich ;; trigger == robbery ;; conflict.attack.n/a
crime == giving information to the coalition or putting ( GPS ) chips in order for the aircraft to strike at a specific area ;; defendant == fighters ;; judgecourt == ISIS ;; place == Syria ;; trigger == executed ;; justice.judicialconsequences.n/a
participant == Sanders ^^ the unapologetically anti - communist panel ;; trigger == debate ;; contact.negotiate.meet
communicator == Democrats and Republicans who serve on defense , law enforcement or intelligence committees , as well as a bipartisan group of 31 security and counterterrorism experts ;; recipient == Obama ;; trigger == urged ;; contact.commandorder.meet
participant == Russians ^^ Crimea ;; place == Crimea ;; trigger == took over ;; government.formation.mergegpe
place == Leningrad ;; recipient == Nazi forces ;; surrenderer == residents ;; trigger == surrendering ;; conflict.yield.surrender
attacker == coup ;; place == Istanbul ;; target == victims ;; trigger == coup ;; conflict.attack.selfdirectedbattle
place == Libya ;; victim == Muammar Gaddafi ;; trigger == murder ;; life.die.deathcausedbyviolentevents
artifact == pathetic monuments ;; manufacturer == 1 percent ;; trigger == construct ;; manufacture.artifact.build
communicator == GOP members ;; recipient == Trump ;; trigger == urged ;; contact.requestadvise.meet
attacker == Russian air forces ;; place == Aleppo province ;; target == ISIS oil tankers ;; trigger == annihilate ;; conflict.attack.selfdirectedbattle
communicator == U.S. officials ;; place == U.S. ;; recipient == individual states ;; trigger == advised ;; contact.requestadvise.correspondence
artifact == The Trace Gas Orbiter ;; manufacturer == European manufacturer ;; trigger == assembled ;; manufacture.artifact.build
communicator == Schultz ;; recipient == Congress ;; trigger == urged ;; contact.commandorder.meet
defendant == Clinton ;; prosecutor == FBI ;; trigger == indict ;; justice.initiatejudicialprocess.chargeindict
artifact == The incident ;; trigger == captured ;; movement.transportartifact.preventexit
executioner == government ;; place == Saudi Arabia ;; trigger == executions ;; justice.judicialconsequences.execute
origin == Salekhard ;; passenger == boy ;; trigger == evacuated ;; movement.transportperson.n/a
place == around the world ;; trigger == protest ;; conflict.demonstrate.marchprotestpoliticalgathering
beneficiary == the campaign ;; giver == independent SuperPAC ;; trigger == funded ;; transaction.transferownership.giftgrantprovideaid
artifact == sex ;; transporter == suspect ;; trigger == trafficking ;; movement.transportartifact.smuggleextract
participant == rebels ;; place == Syria ;; trigger == ceasefire ;; government.agreements.acceptagreementcontractceasefire
beneficiary == black tenants ;; giver == real - estate company ;; recipient == properties ;; trigger == rent ;; transaction.transferownership.borrowlend
artifact == five minority teens ;; trigger == imprisoned ;; movement.transportartifact.preventexit
communicator == lead editorial writers ;; place == The New York Times and The Washington Post ;; recipient == the Iraq War ;; trigger == advocated ;; contact.requestadvise.meet
observedentity == the Millennium ;; place == the Millennium ;; spy == it ;; trigger == surveillance ;; government.spy.spy
crime == Rich 's death ;; investigator == investigators ;; trigger == investigation ;; justice.investigate.investigatecrime
participant == Tayyip Erdogan ^^ Vladimir Putin ;; place == Istanbul , Turkey ;; trigger == meeting ;; contact.discussion.n/a
injurer == Israeli fighter jet ;; place == Israel ;; victim == It ;; trigger == shot ;; life.injure.injurycausedbyviolentevents
communicator == Trump ;; recipient == a U.S. adversary ;; trigger == urge ;; contact.commandorder.correspondence
participant == U.S. ^^ Russians ;; trigger == ceasefire ;; government.agreements.acceptagreementcontractceasefire
otherparticipant == the Assad regime ;; place == Saraqeb ;; rejecternullifier == The SNC ;; trigger == agreements ;; government.agreements.rejectnullifyagreementcontractceasefire
beneficiary == charity ;; giver == he ;; money == $ 10,000 ;; place == Mar - a - Lago ;; trigger == auction ;; transaction.transfermoney.purchase
communicator == photographer ;; place == Atlantic City ;; recipient == him ;; trigger == blackmail ;; contact.threatencoerce.correspondence
communicator == Trump ;; recipient == Hillary created the birther movement ;; trigger == lied ;; contact.prevarication.n/a
injurer == strikes ;; place == Aleppo ;; victim == people ;; trigger == wounded ;; life.injure.injurycausedbyviolentevents
artifact == house ;; place == Miami - Dade County , Florida ;; trigger == demolished ;; artifactexistence.damagedestroy.destroy
giver == the sultan ;; money == the sultan ;; place == Los Angeles ;; preventer == the Human Rights Campaign ;; recipient == the property ;; trigger == boycotts ;; transaction.transfermoney.embargosanction
participant == Clinton ^^ Trump ;; trigger == debate ;; contact.negotiate.n/a
defendant == Mujib ;; judgecourt == United States ;; trigger == executing ;; justice.judicialconsequences.n/a
killer == Anderson ;; victim == Assange ;; trigger == assassinating ;; life.die.deathcausedbyviolentevents
inspectedentity == it ;; inspector == I ;; place == pageant ;; trigger == inspecting ;; inspection.sensoryobserve.inspectpeopleorganization
communicator == Egypt ;; recipient == abolishing ” daylight savings time ;; trigger == announced ;; contact.publicstatementinperson.n/a
participant == the Ruskies ^^ the Baltic States , Ukraine , and God knows what else ;; trigger == take over ;; government.formation.mergegpe
artifact == crude ;; destination == Europe ;; origin == Doha ;; transporter == country ;; trigger == shipment ;; movement.transportartifact.disperseseparate
beneficiary == United States ;; observedentity == operations around the world ;; place == United States ;; spy == NSA ;; trigger == surveillance ;; government.spy.spy
participant == adults ^^ survey ;; place == all 50 states and the District of Columbia ;; trigger == telephone ;; contact.collaborate.correspondence
artifact == oil refiner ;; beneficiary == Russia ;; giver == Russia ;; place == India ;; recipient == India ;; trigger == purchase ;; transaction.transferownership.purchase
destination == America ;; origin == overseas ;; retreater == Americans ;; trigger == withdraw ;; conflict.yield.retreat
artifact == material ;; destination == North Korea ;; transporter == activists ;; vehicle == drones ;; trigger == smuggled ;; movement.transportartifact.n/a
artifact == equipment ;; destination == the military training area ;; origin == Black Sea ;; trigger == transported ;; movement.transportartifact.disperseseparate
destination == such countries ;; origin == United States ;; passenger == US arms and arms components ;; transporter == networks ;; trigger == smuggling ;; movement.transportperson.smuggleextract
crime == attempted overthrow of the government ;; detainee == plotters ;; place == Turkey ;; trigger == detained ;; justice.arrestjaildetain.arrestjaildetain
artifact == TV ad ;; trigger == buy ;; transaction.transferownership.n/a
participant == Trump ^^ Japanese bankers ;; trigger == negotiated ;; contact.negotiate.correspondence
communicator == Trump ;; place == the media ;; trigger == misleading ;; contact.prevarication.broadcast
beneficiary == campaigns ;; giver == public ;; trigger == funding ;; transaction.transferownership.giftgrantprovideaid
giver == he ;; place == New York ;; recipient == the money ;; trigger == purchasing ;; transaction.transfermoney.purchase
participant == FBI ^^ Boston Marathon bomber Tamerlan Tsarnaev ;; trigger == interviewed ;; contact.discussion.meet
participant == United States ^^ Riyadh ;; trigger == discussing ;; contact.discussion.correspondence
instrument == " Little Boy " ;; place == Hiroshima ;; target == Hiroshima ;; trigger == bomb ;; conflict.attack.bombing
place == Moldova ’s capital ;; recipient == Monday ’s V - Day march ;; yielder == Chisinau ;; trigger == withdraw ;; conflict.yield.n/a
communicator == Trump ;; trigger == vow ;; contact.commitmentpromiseexpressintent.meet
employee == the big banks ;; place == America ;; trigger == sack ;; personnel.endposition.firinglayoff
candidate == politicians ;; place == the country ;; trigger == elected ;; personnel.elect.winelection
injurer == illnesses ;; place == United States ;; trigger == illnesses ;; life.injure.n/a
communicator == Clinton ’s top aides ;; trigger == oath ;; contact.commitmentpromiseexpressintent.meet
place == Jarablus ;; victim == leader ;; trigger == assassinated ;; life.die.n/a
giver == U.S. ;; place == Russia ;; preventer == U.S. ;; recipient == Stroygazmontazh ;; trigger == sanctions ;; transaction.transferownership.embargosanction
artifact == American forces ;; destination == into another large and costly war in the Middle East ;; trigger == plunge ;; movement.transportartifact.fall
attacker == rebels ;; place == Libya ;; target == blacks ;; trigger == massacring ;; conflict.attack.n/a
attacker == Russians ;; instrument == poisoning ;; place == Millennium hotel ;; target == victim ;; trigger == poisoning ;; conflict.attack.biologicalchemicalpoisonattack
giver == FBI ;; place == a New Jersey data center ;; recipient == it ;; trigger == seized ;; transaction.transaction.transfercontrol
hidingplace == USB drives ;; passenger == Western movies ;; transporter == guy ;; vehicle == drones ;; trigger == smuggled ;; movement.transportperson.hide
communicator == Trump ;; trigger == lied ;; contact.prevarication.broadcast
victim == he ;; trigger == surgery ;; life.injure.illnessdegradationphysical
participant == Clinton ;; trigger == emails ;; contact.collaborate.correspondence
artifact == arms and arms components ;; origin == US ;; transporter == networks ;; trigger == smuggling ;; movement.transportartifact.hide
communicator == husband ;; trigger == oath ;; contact.commitmentpromiseexpressintent.broadcast
communicator == Dennis Cheng ;; recipient == Podesta call certain donors to Priorities USA Action ;; trigger == recommend ;; contact.requestadvise.n/a
artifact == it ;; recipient == he ;; trigger == obtained ;; transaction.transferownership.purchase
investigator == FBI ;; trigger == investigate ;; justice.investigate.investigatecrime
fireexplosionobject == aircraft ;; instrument == crash ;; place == Rostov - on - Don Airport ;; trigger == blaze ;; disaster.fireexplosion.fireexplosion
killer == Assad ;; place == Aleppo ;; trigger == massacre ;; life.die.deathcausedbyviolentevents
attacker == U.S. ;; place == Syria or Iran ;; target == Syria or Iran ;; trigger == strike ;; conflict.attack.bombing
participant == Researchers ;; place == Europe ;; trigger == burials ;; contact.funeralvigil.n/a
judgecourt == government ;; place == Saudi Arabia ;; trigger == executions ;; justice.judicialconsequences.n/a
defendant == him ;; judgecourt == Mr. Trump ;; trigger == execute ;; justice.judicialconsequences.n/a
rejecternullifier == the Pentagon ;; trigger == ceasefire ;; government.agreements.rejectnullifyagreementcontractceasefire
artifact == men ;; destination == Syria ;; origin == Russia ;; transporter == Wagner ;; vehicle == planes ;; trigger == transport ;; movement.transportartifact.n/a
communicator == U.S. -led coalition ;; place == Syria ;; recipient == others ;; trigger == intimidate ;; contact.threatencoerce.correspondence
defendant == Bukharin ;; executioner == Soviet ;; trigger == execution ;; justice.judicialconsequences.execute
crime == raping ;; defendant == five black and Hispanic teens ;; place == Central Park ;; trigger == death penalty ;; justice.judicialconsequences.execute
communicator == Panama 's government ;; recipient == vigorously cooperate ;; trigger == vowed ;; contact.commitmentpromiseexpressintent.broadcast
injurer == terrorist ;; place == Pulse nightclub ;; victim == people ;; trigger == injured ;; life.injure.n/a
employee == Heede ;; place == Aspen , Colorado ;; placeofemployment == They ;; trigger == hired ;; personnel.startposition.hiring
crime == accepted bribes ;; defendant == Denis Nikandrov and Alexander Lamonov ;; judgecourt == court ;; place == Moscow ;; prosecutor == court ;; trigger == charges ;; justice.initiatejudicialprocess.chargeindict
participant == Trump ^^ CNBC ;; trigger == interview ;; contact.discussion.n/a
communicator == opposition ;; recipient == We ;; trigger == blackmailing ;; contact.threatencoerce.meet
victim == he ;; trigger == passed away ;; life.die.n/a
giver == agents ;; recipient == a Wi - Fi router ;; trigger == confiscated ;; transaction.transaction.transfercontrol
participant == officials ^^ officials ;; place == Moscow ;; trigger == met ;; contact.discussion.meet
communicator == columnist ;; recipient == unity day ;; trigger == proposed ;; contact.publicstatementinperson.n/a
governmentbody == President Andrzej Duda ;; law == controversial move bringing the attorney general under the control of the justice ministry ;; trigger == signed into law ;; government.legislate.legislate
giver == military - industrial complex ;; recipient == four - star general ;; trigger == compensated ;; transaction.transfermoney.payforservice
participant == Trump ^^ USSR ;; trigger == agreements ;; government.agreements.acceptagreementcontractceasefire
giver == servicers ;; recipient == borrowers ;; trigger == loan ;; transaction.transfermoney.borrowlend
artifact == government ;; attacker == faction of the military ;; place == Turkey ;; target == warplanes , helicopters and tanks ;; trigger == commandeered ;; conflict.attack.stealrobhijack
killer == Washington ;; place == not only in Cuba , but in Panama , Dominican Republic and Venezuela ;; victim == Cuban president Fidel Castro ;; trigger == assassinate ;; life.die.deathcausedbyviolentevents
victim == daughter ;; trigger == deceased ;; life.die.n/a
attacker == Pope Francis ;; instrument == warning ;; place == Syria ;; target == the assault on civilians ;; trigger == assailed ;; conflict.attack.selfdirectedbattle
communicator == Justice Minister Bekir Bozdag ;; recipient == anti - American feeling in Turkey ;; trigger == broadcast ;; contact.mediastatement.n/a
attacker == Washington - backed rebels ;; place == Libya ;; target == blacks ;; trigger == massacring ;; conflict.attack.selfdirectedbattle
injurer == North Vietnamese ;; victim == he ;; trigger == torture ;; life.injure.injurycausedbyviolentevents
employee == Kasyanov ;; place == Russia ;; placeofemployment == prime minister ;; trigger == ousted ;; personnel.endposition.firinglayoff
placeofemployment == team ;; trigger == hiring ;; personnel.startposition.n/a
communicator == Obama Administration officials ;; recipient == starting a war with Russia ;; trigger == advocating ;; contact.requestadvise.n/a
artifactmoney == Beverly Hills and Bel - Air hotels ;; place == Los Angeles ;; preventer == Human Rights Campaign ;; trigger == boycotts ;; transaction.transaction.embargosanction
participant == Putin ^^ territories ;; place == territories ;; trigger == annex ;; government.formation.mergegpe
employee == he ;; placeofemployment == the ticket ;; trigger == resign ;; personnel.endposition.quitretire
candidate == Gennady Zyuganov ;; trigger == elected ;; personnel.elect.n/a
victim == patients ;; trigger == dehydrated ;; life.injure.n/a
crime == trying to conceal the potentially embarrassing connections ;; defendant == Clinton ;; prosecutor == Republicans ;; trigger == accused ;; justice.initiatejudicialprocess.chargeindict
attacker == Russia ;; instrument == air strikes ;; place == Syria ;; target == Syria ;; trigger == airstrikes ;; conflict.attack.selfdirectedbattle
communicator == Trump ;; trigger == deceiving ;; contact.prevarication.meet
transporter == John McCain ;; trigger == captured ;; movement.transportperson.preventexit
participant == China ^^ large swaths of Russian wilderness ;; trigger == annex ;; government.formation.mergegpe
attacker == Russian President Vladimir Putin ;; instrument == poisoning ;; place == London ;; target == former KGB operative ;; trigger == poisoning ;; conflict.attack.biologicalchemicalpoisonattack
communicator == Air Force General Philip Breedlove ;; place == America ;; recipient == America ;; trigger == misleads ;; contact.prevarication.broadcast
artifact == access to the powerful family and their associates in government ;; beneficiary == themselves ;; trigger == auctioning ;; transaction.transferownership.n/a
beneficiary == Democrats ;; participant == Three members of Mueller 's team ^^ Democrats ;; trigger == donated ;; transaction.transaction.n/a
observedentity == damage ;; observer == People ;; place == Aleppo ;; trigger == inspect ;; inspection.sensoryobserve.n/a
communicator == major third - party candidates ;; place == Richmond , Virginia ;; trigger == broadcast ;; contact.mediastatement.broadcast
employee == President Bashar al - Assad ;; place == Syria ;; placeofemployment == President ;; trigger == oust ;; personnel.endposition.firinglayoff
giver == the Times ;; recipient == its front page ;; trigger == lent ;; transaction.transferownership.borrowlend
destination == Western countries ;; passenger == terrorists ;; transporter == Islamic State ;; trigger == smuggling ;; movement.transportperson.smuggleextract
communicator == he ;; recipient == thousands and thousands of ads ;; trigger == suggested ;; contact.requestadvise.broadcast
participant == Kerry ^^ Russian Foreign Minister Sergey Lavrov ;; place == Geneva , Switzerland ;; trigger == ceasefire ;; government.agreements.acceptagreementcontractceasefire
place == Paris ;; trigger == negotiated ;; contact.discussion.correspondence
trigger == roamed ;; movement.transportperson.n/a
artifact == crude shipment ;; destination == Europe ;; origin == Iran ;; transporter == Iran ;; trigger == shipment ;; movement.transportartifact.n/a
artifact == high - value targets ;; preventer == we ;; trigger == detaining ;; movement.transportartifact.preventexit
artifact == Buk 9M38 missile ;; destination == an area of eastern Ukraine controlled by Moscow - backed rebels ;; origin == Russia ;; transporter == close to 100 people ;; trigger == transport ;; movement.transportartifact.disperseseparate
governmentbody == her ;; law == sanctions ;; trigger == enact ;; government.legislate.legislate
beneficiary == contractors ;; giver == billionaire businessman ;; recipient == contractors ;; trigger == paid ;; transaction.transfermoney.payforservice
injurer == the three ;; victim == civilians ;; trigger == torture ;; life.injure.injurycausedbyviolentevents
artifact == buildings ;; instrument == air strike ;; place == Bustan al - Qasr ;; trigger == destroyed ;; artifactexistence.damagedestroy.n/a
destination == North Korea ;; origin == Russia ;; transporter == Russia ;; trigger == shipment ;; movement.transportperson.disperseseparate
artifact == migrants ;; destination == Europe ;; origin == Turkey ;; transporter == networks ;; trigger == smuggling ;; movement.transportartifact.smuggleextract
detainee == Zakharchenko ;; trigger == arrest ;; justice.arrestjaildetain.arrestjaildetain
governmentbody == President Vladimir Putin ;; law == a bill ;; place == the country ;; trigger == signed into law ;; government.legislate.legislate
crime == unsecure emails ;; defendant == her ;; trigger == indicted ;; justice.initiatejudicialprocess.chargeindict
artifact == opposition ;; place == Russia ;; trigger == battered ;; artifactexistence.damagedestroy.damage
defendant == Edward Snowden ;; place == the US ;; trigger == trial ;; justice.initiatejudicialprocess.trialhearing
recipient == confrontations ;; trigger == retreat ;; conflict.yield.n/a
beneficiary == Islamabad ;; giver == Islamabad ;; trigger == buy ;; transaction.transfermoney.n/a
artifact == oil ;; origin == Iran ;; transporter == Iran ;; trigger == exports ;; movement.transportartifact.sendsupplyexport
communicator == Kiselyov ;; recipient == viewers ;; trigger == misleading ;; contact.prevarication.broadcast
place == the square ;; target == Issa ;; trigger == hanged ;; conflict.attack.n/a
communicator == the message ;; place == Swedish media outlet ;; recipient == public ;; trigger == deceive ;; contact.prevarication.meet
destination == Syrian ;; trigger == smuggling ;; movement.transportartifact.smuggleextract
trigger == immigration ;; inspection.sensoryobserve.inspectpeopleorganization
artifact == North Korea ;; place == North Korea ;; preventer == President Barack Obama ;; trigger == sanctions ;; transaction.transferownership.embargosanction
killer == father ;; victim == JFK ;; trigger == assassinate ;; life.die.deathcausedbyviolentevents
victim == survivors ;; trigger == Holocaust ;; life.die.deathcausedbyviolentevents
attacker == a US - based cleric ;; instrument == violence ;; place == US ;; target == the Erdogan administration ;; trigger == coup ;; conflict.attack.selfdirectedbattle
attacker == Palestinian teen ;; place == Kiryat Arba settlement ;; target == Hallel Yafa Ariel ;; trigger == stabbing ;; conflict.attack.stabbing
participant == Senators ^^ Senators ;; place == Senate ;; trigger == debate ;; contact.discussion.correspondence
communicator == Edward Snowden ;; place == Moscow ;; trigger == broadcast ;; contact.mediastatement.broadcast
giver == buyers from outside Brazil ;; place == authorized ticket resale site CoSport ;; recipient == the same reduced prices as citizens ;; trigger == purchase ;; transaction.transfermoney.purchase
injurer == it ;; place == prison camps ;; victim == people ;; trigger == brutalized ;; life.injure.injurycausedbyviolentevents
communicator == he ;; recipient == produce damaging information about LBJ ;; trigger == promised ;; contact.commitmentpromiseexpressintent.n/a
founder == he ;; gpe == relationship ;; place == RT gala ;; trigger == inaugurate ;; government.formation.n/a
destination == US ;; origin == Iran ;; passenger == Americans ;; preventer == Iran ;; trigger == detained ;; movement.transportperson.preventexit
place == United States ;; trigger == homicides ;; life.die.deathcausedbyviolentevents
communicator == she ;; trigger == lied ;; contact.prevarication.broadcast
spy == agency ;; trigger == spy ;; government.spy.spy
candidate == first woman to ever hold the presidency ;; voter == I ;; trigger == vote ;; government.vote.n/a
beneficiary == DNC ;; giver == Shah ;; place == events ;; recipient == DNC ;; trigger == donating ;; transaction.transferownership.n/a
communicator == political and media elites ;; recipient == candidates ;; trigger == demand ;; contact.commandorder.correspondence
employee == Roffman ;; trigger == firing ;; personnel.endposition.firinglayoff
communicator == Milo Yiannopoulos ;; trigger == preaching ;; contact.mediastatement.broadcast
passenger == anyone ;; preventer == authorities ;; trigger == incarcerate ;; movement.transportperson.preventexit
inspectedentity == he and his parents ;; trigger == immigrated ;; inspection.sensoryobserve.inspectpeopleorganization
beneficiary == Vietnam ;; participant == United States ^^ Vietnam ;; trigger == embargo ;; transaction.transaction.n/a
attacker == Turkey ’s air force ;; instrument == bombing ;; place == northern Syria ;; target == Kurdish militias ;; trigger == bombing ;; conflict.attack.n/a
artifact == city ;; damager == Russian and Syrian government ;; place == city ;; trigger == battered ;; artifactexistence.damagedestroy.damage
giver == Dimon ;; money == millions ;; trigger == buy ;; transaction.transfermoney.purchase
communicator == Clinton ;; trigger == oath ;; contact.commitmentpromiseexpressintent.correspondence
crime == money laundering , fraud and extortion ;; defendant == Pavlo Lazarenko ;; place == Ukraine ;; trigger == convicted ;; justice.judicialconsequences.n/a
communicator == FBI Director James B. Comey ;; recipient == Clinton and her staff ;; trigger == recommending ;; contact.requestadvise.broadcast
artifact == the fabric of the proper function of responsible government ;; damager == WikiLeaks and its news media partners ;; instrument == disclosures like these ;; trigger == tear ;; artifactexistence.damagedestroy.damage
communicator == Trump ;; trigger == promised ;; contact.commitmentpromiseexpressintent.broadcast
beneficiary == Kurdish ;; giver == The Turkish army ;; recipient == Erdogan ;; trigger == seizing ;; transaction.transaction.transfercontrol
place == Iran ;; preventer == U.S. ;; trigger == sanctions ;; transaction.transfermoney.embargosanction
participant == ICANN ;; place == Marrakesh ;; trigger == meeting ;; contact.discussion.n/a
communicator == Melania ;; place == GOP convention in Cleveland ;; trigger == reiterating ;; contact.publicstatementinperson.broadcast
communicator == Richard Burt ;; recipient == the Trump campaign ;; trigger == advising ;; contact.requestadvise.correspondence
participant == DNC Chairwoman Debbie Wasserman Schultz and other party officials ^^ DNC Chairwoman Debbie Wasserman Schultz and other party officials ;; trigger == emails ;; contact.collaborate.correspondence
observedentity == missiles ;; observer == it ;; trigger == inspect ;; inspection.sensoryobserve.n/a
target == he ;; trigger == poisoned ;; conflict.attack.n/a
communicator == America ;; place == America ;; recipient == Russia ;; trigger == blackmail ;; contact.threatencoerce.correspondence
artifact == arms ;; destination == Pakistan ;; origin == US ;; transporter == US ;; trigger == shipments ;; movement.transportartifact.bringcarryunload
artifactmoney == oil ;; place == Iran ;; recipient == Iran ;; trigger == sanctions ;; transaction.transaction.embargosanction
place == al - Bagilya ;; trigger == massacre ;; life.die.n/a
artifact == bitcoin ;; giver == Arias ;; trigger == buy ;; transaction.transferownership.n/a
driverpassenger == chauffeur ;; place == Moscow ;; vehicle == presidential car ;; trigger == crash ;; disaster.accidentcrash.accidentcrash
beneficiary == the group ;; giver == they ;; money == half a billion dollars ;; recipient == half a billion dollars ;; trigger == auction ;; transaction.transfermoney.purchase
communicator == he ;; place == St. Louis ;; trigger == suggest ;; contact.requestadvise.meet
beneficiary == Tim Kaine , and his wife , Anne Holton ;; recipient == $ 300,000 ;; trigger == income ;; transaction.transfermoney.payforservice
beneficiary == soldiers ;; giver == Russian military ;; recipient == soldiers ;; trigger == pay ;; transaction.transfermoney.payforservice
artifact == head ;; preventer == a security guard and two policeman ;; transporter == her ;; trigger == detain ;; movement.transportartifact.preventexit
communicator == OROP ;; recipient == Prime Minister ;; trigger == demand ;; contact.commandorder.meet
artifact == oil ;; origin == ISIS - held territory in Syria and Iraq ;; transporter == he and his family ;; trigger == smuggling ;; movement.transportartifact.hide
communicator == Canada ;; recipient == 10 percent increase in its overall humanitarian assistance ;; trigger == pledged ;; contact.commitmentpromiseexpressintent.n/a
communicator == Trump ;; trigger == lied ;; contact.prevarication.meet
governmentbody == Congress ;; law == launch an invasion of Iraq ;; trigger == vote ;; government.legislate.legislate
victim == enrollees ;; trigger == sicker ;; life.injure.illnessdegradationphysical
deceased == baby ;; participant == Jungklaus ;; trigger == burial ;; contact.funeralvigil.n/a
beneficiary == minorities ;; giver == Justice Department ;; money == properties ;; place == properties ;; recipient == minorities ;; trigger == rent ;; transaction.transfermoney.n/a
beneficiary == I ;; place == U.S. ;; trigger == paid ;; transaction.transfermoney.payforservice
attacker == rebels ;; instrument == poison gas ;; place == Aleppo ;; target == seven people ;; trigger == poison ;; conflict.attack.n/a
employee == Simonyan ;; trigger == recruiting ;; personnel.startposition.n/a
origin == Upper Merion neighborhood ;; transporter == three 20-something men ;; trigger == walking ;; movement.transportperson.selfmotion
violator == us ;; trigger == ceasefire ;; government.agreements.violateagreement
crime == shoplifting ;; detainee == man ;; jailer == Gardendale police ;; place == Walmart ;; trigger == arrested ;; justice.arrestjaildetain.arrestjaildetain
employee == agent detail ;; placeofemployment == U.S. Secret Service ;; trigger == dismiss ;; personnel.endposition.firinglayoff
passenger == rival of Ukraine 's then - president ;; trigger == imprisoned ;; movement.transportperson.preventexit
place == Deir el - zour ;; victim == 280 victims ;; trigger == massacre ;; life.die.deathcausedbyviolentevents
instrument == death penalty ;; killer == U.S. ;; place == U.S. ;; victim == people ;; trigger == executed ;; life.die.deathcausedbyviolentevents
candidate == Democratic presidential hopeful ;; place == this country ;; trigger == elected ;; personnel.elect.winelection
beneficiary == he ;; giver == Yanukovich 's Party of Regions ;; money == $ 12 million ;; recipient == he ;; trigger == payments ;; transaction.transfermoney.n/a
beneficiary == Russia ;; participant == Russia ;; place == Crimea ;; trigger == land grab ;; transaction.transaction.n/a
artifact == American politics ;; damager == Clinton ’s campaign ;; instrument == money - driven secrecy and subversion ;; trigger == wrecking ;; artifactexistence.damagedestroy.damage
destination == Kilis State hospital ;; origin == Azaz ;; passenger == a girl ;; transporter == medical personnel ;; vehicle == ambulances ;; trigger == unloading ;; movement.transportperson.bringcarryunload
defendant == Assange ;; destination == US ;; extraditer == US ;; origin == Sweden ;; trigger == extradite ;; justice.judicialconsequences.extradite
participant == candidates ^^ 80 million people ;; trigger == debate ;; contact.discussion.meet
attacker == United States ;; instrument == hijacking ;; target == TPP process ;; trigger == hijacking ;; conflict.attack.n/a
instrument == shooting ;; killer == shooter ;; place == Pulse nightclub ;; victim == 49 people ;; trigger == killed ;; life.die.deathcausedbyviolentevents
communicator == America ;; recipient == Russia ;; trigger == blackmail ;; contact.threatencoerce.meet
beneficiary == Haiti and Algeria ;; giver == the Embassy of Algeria ;; place == the Clinton Foundation ;; recipient == an “ unsolicited ” donation ;; trigger == donated ;; transaction.transferownership.giftgrantprovideaid
attacker == Russia ;; place == Crimea ;; target == Ukraine ;; trigger == invaded ;; conflict.attack.invade
target == all ;; trigger == attack ;; conflict.attack.selfdirectedbattle
giver == she ;; place == Toledo , Ohio ;; trigger == rent ;; transaction.transfermoney.n/a
beneficiary == Trump ;; giver == Russian and ex - Soviet state billionaires ;; place == New York City ;; recipient == Trump ;; trigger == funded ;; transaction.transfermoney.giftgrantprovideaid
artifact == Chinese ;; giver == Philippines ;; place == China ;; preventer == Chinese ;; recipient == dried mango ;; trigger == boycotted ;; transaction.transferownership.embargosanction
participant == Clinton ^^ Banco Itau ;; trigger == emails ;; contact.collaborate.correspondence
attacker == billionaire ;; target == family of a slain U.S. soldier ;; trigger == attacked ;; conflict.attack.selfdirectedbattle
trigger == Buy ;; transaction.transfermoney.purchase
injurer == Military ;; victim == Victims ;; trigger == Assault ;; life.injure.injurycausedbyviolentevents
instrument == a missile ;; place == town of Azaz ;; victim == a children 's hospital ;; trigger == attack ;; life.injure.injurycausedbyviolentevents
trigger == imprisoned ;; movement.transportperson.preventexit
attacker == Saudi ;; place == the capital ;; target == funeral ;; trigger == airstrike ;; conflict.attack.airstrikemissilestrike
artifact == air defense systems ;; origin == Russian ;; transporter == Syrian government ;; trigger == imports ;; movement.transportartifact.receiveimport
trigger == agreement ;; government.agreements.n/a
injurer == Trump ;; place == Cleveland ;; trigger == bruising ;; life.injure.n/a
victim == humanity ;; trigger == genocides ;; life.die.deathcausedbyviolentevents
target == Rich ;; trigger == robbery ;; conflict.attack.n/a
place == his country ;; victim == Russian journalists ;; trigger == murdered ;; life.die.deathcausedbyviolentevents
beneficiary == those ;; giver == those ;; recipient == Clinton Foundation ;; trigger == donated ;; transaction.transaction.giftgrantprovideaid
artifact == tractors and four combines , along with a German machine ;; transporter == His ;; trigger == imported ;; movement.transportartifact.n/a
spy == National Security Agency ;; trigger == surveillance ;; government.spy.spy
artifact == The cache ;; instrument == information on Democratic donors , fundraising efforts and donor outreach ;; trigger == composed ;; manufacture.artifact.n/a
participant == it ;; trigger == negotiates ;; contact.negotiate.meet
artifact == truth ;; hidingplace == a " dust up " ;; transporter == Clintons ;; trigger == conceal ;; movement.transportartifact.hide
participant == Yildirim ^^ AK Party ;; place == parliament ;; trigger == meeting ;; contact.discussion.correspondence
trigger == battle ;; conflict.attack.n/a
communicator == we ;; recipient == black folks in the South ;; trigger == suggest ;; contact.requestadvise.meet
communicator == it ;; place == Canada ;; trigger == announced ;; contact.publicstatementinperson.broadcast
artifact == market ;; giver == Iran ;; preventer == European oil embargo ;; recipient == oil ;; trigger == sanctions ;; transaction.transferownership.embargosanction
participant == Washington ^^ us ;; trigger == dialogue ;; contact.discussion.n/a
ballot == Democratic primary ;; candidate == he ;; result == he won ;; trigger == voted ;; government.vote.n/a
communicator == she ;; place == Libya ;; recipient == military intervention ;; trigger == advocated ;; contact.requestadvise.n/a
attacker == corporate ;; instrument == corporate power ;; target == We ;; trigger == coup ;; conflict.attack.selfdirectedbattle
origin == Turkey ;; passenger == IS trainer ;; transporter == Free Syrian Army ;; trigger == captured ;; movement.transportperson.n/a
communicator == Putin ;; place == Russia ;; trigger == pledge ;; contact.commitmentpromiseexpressintent.meet
beneficiary == the group , its sister organizations and other Koch allies ;; giver == the 501(c)6 nonprofit group ;; money == $ 88 million ;; recipient == $ 88 million ;; trigger == grants ;; transaction.transfermoney.giftgrantprovideaid
participant == he ^^ Russia Today ;; trigger == interviewed ;; contact.discussion.n/a
victim == I ;; trigger == sick ;; life.injure.illnessdegradationphysical
passenger == Chinese workers ;; trigger == immigrating ;; movement.transportperson.n/a
artifact == assets ;; destination == offshore ;; transporter == world leaders ;; trigger == conceal ;; movement.transportartifact.n/a
driverpassenger == Israeli man ;; place == the settlement ;; vehicle == car ;; trigger == crash ;; disaster.accidentcrash.accidentcrash
attacker == the United States ;; instrument == the military ;; place == Honduras ;; target == the democratically - elected government ;; trigger == coup ;; conflict.attack.selfdirectedbattle
beneficiary == foundation ;; participant == Mr Trump ^^ foundation ;; trigger == donated ;; transaction.transaction.n/a
killer == Turkish ;; place == Armenian ;; victim == Armenian ;; trigger == Genocide ;; life.die.deathcausedbyviolentevents
killer == Iranian officials ;; victim == scientist ;; trigger == executed ;; life.die.deathcausedbyviolentevents
place == Prince George ’s County ;; target == ice cream truck ;; trigger == robbery ;; conflict.attack.stealrobhijack
communicator == enemies ;; recipient == her ;; trigger == blackmail ;; contact.threatencoerce.n/a
artifactmoney == China ;; giver == China ;; place == Cam Ranh Bay ;; preventer == U.S. ;; recipient == arms ;; trigger == embargo ;; transaction.transaction.embargosanction
recipient == socialism ;; trigger == professed ;; contact.publicstatementinperson.n/a
inspectedentity == wreckage of a Russian helicopter ;; inspector == Men ;; place == north of Syria 's rebel - held Idlib province ;; trigger == inspect ;; inspection.sensoryobserve.inspectpeopleorganization
defendant == Mr Amiri ;; place == Iran ;; trigger == execution ;; justice.judicialconsequences.execute
trigger == plummeted ;; movement.transportartifact.fall
place == Washington , D.C. ;; victim == Seth Rich ;; trigger == murdered ;; life.die.n/a
participant == Polite powers ;; place == Iran ;; trigger == seizing ;; transaction.transaction.n/a
granter == a ‘ grassroots ’ movement ;; passenger == support ;; transporter == voters ;; trigger == migrate ;; movement.transportperson.grantentryasylum
communicator == employee ;; place == Donetsk ;; recipient == Igor Strelkov ;; trigger == recommended ;; contact.requestadvise.correspondence
passenger == ancestors ;; trigger == migrating ;; movement.transportperson.n/a
founder == China ;; gpe == large swaths of Russian wilderness ;; trigger == annex ;; government.formation.n/a
participant == SO15 detectives , led by Det Supt Timmons , medical staff , a scientist from the UK ’s atomic weapons establishment , the forensic science service and Dr Nick Gent from Porton Down ^^ SO15 detectives , led by Det Supt Timmons , medical staff , a scientist from the UK ’s atomic weapons establishment , the forensic science service and Dr Nick Gent from Porton Down ;; place == the Met ’s counter - terrorism command ;; trigger == convened ;; contact.discussion.meet
artifact == drug ;; transporter == aggravated felons ;; trigger == trafficking ;; movement.transportartifact.smuggleextract
attacker == Russian and Syrian air strikes ;; instrument == bombing raids , alleged gas attacks , and the use of highly destructive barrel bombs ;; place == Mosul ;; target == More than 275,000 civilians in the city ;; trigger == battle ;; conflict.attack.n/a
communicator == Ravel ;; place == FEC ;; trigger == advocated ;; contact.requestadvise.meet
beneficiary == Manafort ;; giver == Party of Regions ;; money == 12.7 million ;; recipient == Manafort ;; trigger == payments ;; transaction.transfermoney.n/a
injurer == Ivana Trump ;; victim == Donald Trump ;; trigger == rape ;; life.injure.n/a
place == Washington ;; recipient == Hillary Clinton ;; trigger == funds ;; transaction.transfermoney.n/a
artifact == fast - track programs ;; manufacturer == company ;; trigger == created ;; manufacture.artifact.n/a
artifact == oil ;; transporter == Islamic State ( IS , formerly ISIL / ISIS ) terrorists ;; trigger == smuggling ;; movement.transportartifact.n/a
participant == rebels ^^ the regime of President Bashar al - Assad ;; place == Syria ;; trigger == ceasefire ;; government.agreements.acceptagreementcontractceasefire
place == D.C. ;; target == DNC employee ;; trigger == robbery ;; conflict.attack.stealrobhijack
artifact == equipment ;; destination == military training area ;; vehicle == by rail ;; trigger == transported ;; movement.transportartifact.bringcarryunload
attacker == Russia ;; place == Aleppo ;; target == Aleppo ;; trigger == bombarding ;; conflict.attack.airstrikemissilestrike
participant == he ^^ Mercer ;; trigger == conferred ;; contact.discussion.n/a
injurer == illness ;; victim == his ;; trigger == illness ;; life.injure.n/a
beneficiary == charity ;; giver == Clintons ;; money == $ 1 million ;; recipient == $ 1 million ;; trigger == donated ;; transaction.transfermoney.giftgrantprovideaid
instrument == a bullet ;; victim == he ;; trigger == shot ;; life.die.deathcausedbyviolentevents
participant == Syrian President Bashar al - Assad ^^ rebel ;; place == Aleppo ;; trigger == ceasefire ;; government.agreements.acceptagreementcontractceasefire
trigger == suggest ;; contact.requestadvise.meet
artifact == the deal ;; transporter == the government and Epstein ;; trigger == concealed ;; movement.transportartifact.hide
employee == Viktor Yanukovych ;; place == Ukrainian ;; placeofemployment == Ukrainian President ;; trigger == ousted ;; personnel.endposition.firinglayoff
communicator == Assad government ;; place == Damascus ;; recipient == foreign inspectors and their Syrian counterparts ;; trigger == misleading ;; contact.prevarication.correspondence
instrument == public execution ;; killer == the government of the Islamic Republic of Iran ;; place == Iran ;; victim == 2500 people ;; trigger == executed ;; life.die.deathcausedbyviolentevents
place == Aleppo ;; victim == hundreds of thousands ;; trigger == starvation ;; life.injure.illnessdegradationhungerthirst
participant == public ^^ public ;; trigger == debate ;; contact.negotiate.meet
artifact == wall ;; manufacturer == rulers of the city of Ur ;; place == Iraq ;; trigger == constructed ;; manufacture.artifact.n/a
inspectedentity == property ;; place == the capital ;; trigger == inspections ;; inspection.sensoryobserve.inspectpeopleorganization
passenger == five minority teens ;; transporter == he ;; trigger == imprisoned ;; movement.transportperson.n/a
artifact == $ 12,000 ;; giver == foundation ;; recipient == charity auction ;; trigger == auction ;; transaction.transferownership.n/a
driverpassenger == chauffeur ;; place == Moscow ;; vehicle == BMW ;; trigger == collision ;; disaster.accidentcrash.accidentcrash
communicator == That ;; trigger == misleading ;; contact.prevarication.n/a
observedentity == honor guards ;; observer == U.S. Vice President Joe Biden ( L ) and Serbia 's Prime Minister Aleksandar Vucic ;; place == Serbia ;; trigger == inspect ;; inspection.sensoryobserve.n/a
victim == 100,000 people ;; trigger == perished ;; life.die.n/a
giver == bloc members ;; territoryorfacility == Moscow ;; trigger == land grabs ;; transaction.transaction.transfercontrol
origin == Syrian Arab Red Crescent warehouse ;; transporter == convoy ;; vehicle == lorries ;; trigger == unloading ;; movement.transportperson.n/a
communicator == Bernie Sanders ;; recipient == Democratic socialist ;; trigger == proclaimed ;; contact.publicstatementinperson.n/a
founder == President Barack Obama ;; gpe == terrorist group ;; trigger == establishing ;; government.formation.startgpe
communicator == CIA ;; recipient == Candidates ;; trigger == advised ;; contact.requestadvise.meet
destination == Saudi Arabia ;; passenger == arms ;; transporter == the global arms trade ;; trigger == trafficking ;; movement.transportperson.smuggleextract
detainee == he ;; jailer == North Vietnam ;; place == North Vietnam ;; trigger == captured ;; justice.arrestjaildetain.arrestjaildetain
victim == daughter ;; trigger == deceased ;; life.die.nonviolentdeath
injurer == FGM ;; trigger == mutilating ;; life.injure.n/a
destination == the stage ;; origin == the stage ;; transporter == her rival ;; trigger == roamed ;; movement.transportperson.selfmotion
killer == he ;; trigger == genocide ;; life.die.deathcausedbyviolentevents
crashobject == Mercedes ;; driverpassenger == chauffeur ;; place == Kutuzovsky Avenue ;; vehicle == presidential BMW ;; trigger == collide ;; disaster.accidentcrash.accidentcrash
defendant == candidates ;; prosecutor == Comey ;; trigger == charges ;; justice.initiatejudicialprocess.n/a
communicator == Weiner ;; trigger == lies ;; contact.prevarication.broadcast
attacker == Russia ;; instrument == cluster munitions ;; place == At - Tanf ;; target == the base ;; trigger == strike ;; conflict.attack.bombing
attacker == Dahir Adan ;; place == Minnesota mall ;; target == Nine people ;; trigger == stabbings ;; conflict.attack.stabbing
communicator == giant corporations ;; recipient == Healy ;; trigger == intimidate ;; contact.threatencoerce.correspondence
crime == coup attempt ;; detainee == 60,000 people ;; jailer == Turkey ;; place == Turkey ;; trigger == detained ;; justice.arrestjaildetain.arrestjaildetain
governmentbody == Clinton ;; law == new sanctions on the regime ;; trigger == enact ;; government.legislate.legislate
artifact == arms ;; trigger == embargo ;; transaction.transferownership.embargosanction
artifact == sample documents ;; beneficiary == the group ;; giver == the group ;; recipient == they ;; trigger == auction ;; transaction.transferownership.purchase
artifact == energy ;; transporter == Russia ;; trigger == exports ;; movement.transportartifact.sendsupplyexport
crime == drugs ;; detainee == drug users ;; trigger == incarcerated ;; justice.arrestjaildetain.arrestjaildetain
communicator == She ;; recipient == millionaires and billionaires ;; trigger == proposed ;; contact.publicstatementinperson.n/a
artifact == something ;; transporter == people ;; vehicle == bitcoin ;; trigger == conceal ;; movement.transportartifact.hide
passenger == his son ;; preventer == security forces ;; trigger == detain ;; movement.transportperson.preventexit
injurer == Donald Trump ;; victim == Ivana Trump ;; trigger == rape ;; life.injure.injurycausedbyviolentevents
employee == workers ;; place == state ;; placeofemployment == state ;; trigger == quit ;; personnel.endposition.quitretire
communicator == Commissioner Moskalkova ;; recipient == Russia ;; trigger == blackmail ;; contact.threatencoerce.broadcast
giver == Republican lawmakers ;; money == $ 15 per hour ;; place == the U.S. ;; trigger == wage ;; transaction.transfermoney.payforservice
place == North Korea ;; victim == his own people ;; trigger == starving ;; life.injure.illnessdegradationhungerthirst
communicator == Kissinger ;; recipient == Nixon ;; trigger == recommended ;; contact.requestadvise.correspondence
communicator == Netanyahu ;; recipient == Russian - backed peace efforts in Syria ;; trigger == address ;; contact.mediastatement.n/a
killer == the CIA ;; victim == Fidel Castro ;; trigger == assassinate ;; life.die.deathcausedbyviolentevents
destination == Kilis State hospital ;; origin == Kaljibrin ;; passenger == Syrian woman ;; transporter == medical personnel ;; vehicle == ambulance ;; trigger == unloaded ;; movement.transportperson.bringcarryunload
beneficiary == Islamic ;; spy == Huma ;; trigger == spy ;; government.spy.spy
detainee == student ;; trigger == detained ;; justice.arrestjaildetain.arrestjaildetain
injurer == United States ;; place == Cuba ;; victim == people ;; trigger == injured ;; life.injure.injurycausedbyviolentevents
communicator == FBI ;; place == news conference ;; recipient == not to recommend charges ;; trigger == recommend ;; contact.requestadvise.broadcast
artifact == human body parts ;; trigger == trafficking ;; movement.transportartifact.n/a
artifact == malware ;; giver == The Shadow Brokers ;; recipient == highest bidder ;; trigger == auctioning ;; transaction.transferownership.n/a
communicator == FBI Director James Comey ;; recipient == Clinton ;; trigger == announced ;; contact.publicstatementinperson.n/a
victim == he ;; trigger == tortured ;; life.injure.injurycausedbyviolentevents
place == North Korea ;; preventer == U.N. ;; recipient == Pyongyang ;; trigger == sanctions ;; transaction.transaction.embargosanction
communicator == the three girls ;; place == the Muslim community in east London ;; recipient == their families ;; trigger == deceiving ;; contact.prevarication.correspondence
victim == leader ;; trigger == assassinated ;; life.die.deathcausedbyviolentevents
beneficiary == He ;; place == Colorado ;; trigger == borrowed ;; transaction.transfermoney.borrowlend
participant == post - debate survey ^^ respondents ;; trigger == telephone ;; contact.collaborate.correspondence
place == Madaya ;; trigger == starvation ;; life.injure.illnessdegradationhungerthirst
instrument == rope ;; target == he ;; trigger == hanged ;; conflict.attack.n/a
crime == hacking Clinton ’s server ;; defendant == Marcel Lehal Lazar ;; trigger == guilty ;; justice.judicialconsequences.n/a
artifact == oil ;; origin == Syria and Iraq ;; transporter == Bilal ;; trigger == smuggling ;; movement.transportartifact.hide
inspectedentity == buildings ;; inspector == Rescue crews ;; trigger == inspecting ;; inspection.sensoryobserve.physicalinvestigateinspect
artifact == expectations ;; manufacturer == poll ;; place == Michigan ;; trigger == creating ;; manufacture.artifact.n/a
participant == I ^^ Apache - Stronghold ;; place == Flagstaff ;; trigger == met ;; contact.discussion.meet
communicator == defense and security experts ;; recipient == Obama administration ;; trigger == urging ;; contact.commandorder.correspondence
participant == Poll ^^ adults 18 years of age or older ;; place == all 50 states and the District of Columbia ;; trigger == telephone ;; contact.collaborate.correspondence
crime == coup plot ;; defendant == he ;; destination == Turkey ;; extraditer == U.S. ;; origin == U.S. ;; trigger == extradited ;; justice.judicialconsequences.extradite
communicator == David Cameron ;; place == UK ;; recipient == Europe ;; trigger == blackmail ;; contact.threatencoerce.broadcast
trigger == immigration ;; inspection.sensoryobserve.physicalinvestigateinspect
communicator == Trump ;; recipient == Wagner ;; trigger == pledged ;; contact.commitmentpromiseexpressintent.broadcast
crime == this atrocity ;; defendant == those responsible ;; destination == Australia ;; extraditer == they ;; trigger == extradition ;; justice.judicialconsequences.extradite
giver == U.S. ;; trigger == wages ;; transaction.transfermoney.n/a
instrument == poisoning ;; killer == Russian President Vladimir Putin ;; victim == former KGB operative ;; trigger == fatal ;; life.die.deathcausedbyviolentevents
artifact == Iran ;; place == Iran ;; preventer == European ;; recipient == oil ;; trigger == embargo ;; transaction.transferownership.embargosanction
place == federal and state prisons ;; trigger == death penalty ;; justice.judicialconsequences.execute
detainee == prisoners of war ;; trigger == detained ;; justice.arrestjaildetain.arrestjaildetain
governmentbody == Clinton ;; law == real change ;; trigger == enact ;; government.legislate.legislate
communicator == national security establishment ;; recipient == the American people ;; trigger == urging ;; contact.requestadvise.correspondence
communicator == He ;; recipient == her ;; trigger == urged ;; contact.commandorder.correspondence
employee == Viktor Yanukovych ;; trigger == ousted ;; personnel.endposition.firinglayoff
communicator == John Large , a British independent nuclear consultant ;; recipient == the Russian government ;; trigger == advised ;; contact.requestadvise.correspondence
artifact == the fabric of democracy ;; damagerdestroyer == ACORN ;; trigger == destroying ;; artifactexistence.damagedestroy.n/a
injurer == man ;; instrument == violence ;; place == Trump rally ;; victim == protester ;; trigger == punched ;; life.injure.injurycausedbyviolentevents
participant == West Bank ^^ Jewish ;; trigger == annex ;; government.formation.mergegpe
beneficiary == combat ship ;; giver == Trump ;; trigger == funding ;; transaction.transaction.giftgrantprovideaid
artifact == arms ;; destination == Pakistan ;; origin == US ;; transporter == US ;; trigger == shipments ;; movement.transportartifact.disperseseparate
participant == Russians ^^ opposition ;; place == Syria ;; trigger == ceasefire ;; government.agreements.n/a
place == Rostov - on - Don ;; vehicle == Boeing ;; trigger == crashed ;; disaster.accidentcrash.accidentcrash
destination == Russian farms ;; granter == Putin ;; origin == across the border ;; transporter == Chinese workers ;; trigger == immigrating ;; movement.transportperson.grantentryasylum
origin == eastern Europe ;; retreater == Anyone who is that reckless ;; trigger == retreat ;; conflict.yield.retreat
deceased == the baby ;; trigger == burial ;; contact.funeralvigil.meet
killer == an off - duty police officer ;; place == Minnesota ;; victim == Adan ;; trigger == killed ;; life.die.deathcausedbyviolentevents
destination == Thailand ;; origin == China ;; passenger == Chinese dissidents ;; transporter == Chinese dissidents ;; trigger == flee ;; movement.transportperson.evacuationrescue
victim == American soldier ;; trigger == slain ;; life.die.n/a
artifact == goods ;; destination == America ;; origin == China ;; transporter == Trump ;; trigger == imports ;; movement.transportartifact.receiveimport
artifact == mosquito ;; destroyer == Cuba ;; instrument == pesticide ;; place == Cuba ;; trigger == eradicate ;; artifactexistence.damagedestroy.destroy
artifact == vestigial moderate wing ;; damagerdestroyer == right - wing primary challenges ;; trigger == eradicated ;; artifactexistence.damagedestroy.n/a
beneficiary == Yanukovych 's pro - Russia political party ;; giver == Ukrainian businessman ;; recipient == Yanukovych 's pro - Russia political party ;; trigger == donated ;; transaction.transferownership.giftgrantprovideaid
participant == Jill Stein ^^ major - party candidates ;; trigger == debate ;; contact.discussion.n/a
crime == marijuana ;; detainee == blacks ;; jailer == criminal justice system ;; place == America ;; trigger == incarcerated ;; justice.arrestjaildetain.arrestjaildetain
beneficiary == Iran ;; giver == Iran ;; place == Iran ;; recipient == centrifuges ;; trigger == acquire ;; transaction.transferownership.purchase
artifact == wheat and corn ;; destination == Soviet Union ;; origin == U.S. ;; transporter == U.S. ;; trigger == shipments ;; movement.transportartifact.bringcarryunload
artifact == shipments ;; origin == Iran ;; transporter == Essar Oil ;; trigger == shipments ;; movement.transportartifact.bringcarryunload
passenger == Clinton ;; preventer == he ;; trigger == incarcerate ;; movement.transportperson.preventexit
employee == Over 80,000 people ;; place == Europe ;; placeofemployment == soldiers , judges , teachers , policemen , businessmen and even football officials ;; trigger == sacked ;; personnel.endposition.firinglayoff
communicator == she ;; trigger == lies ;; contact.prevarication.meet
victim == man ;; trigger == slays ;; life.die.n/a
victim == President Kennedy and Martin Luther King , Jr ;; trigger == assassinated ;; life.die.deathcausedbyviolentevents
participant == Clinton ^^ Israel and Hamas ;; trigger == negotiated ;; contact.negotiate.meet
crime == a threat in cyberspace ;; defendant == those who pose a threat in cyberspace ;; investigator == FBI ;; place == cyberspace ;; trigger == investigate ;; justice.investigate.investigatecrime
observedentity == damage ;; observer == he ;; place == Ankara ;; trigger == inspected ;; inspection.sensoryobserve.n/a
destination == Florida ;; preventer == The U.S. Attorney for the Southern District of Florida ;; transporter == Epstein ;; trigger == arrest ;; movement.transportperson.preventexit
executioner == group ;; trigger == executions ;; justice.judicialconsequences.execute
employee == She ;; trigger == quit ;; personnel.endposition.n/a
injurer == terrorist ;; place == Orlando ;; trigger == shooting ;; life.injure.n/a
artifact == arms and arms components ;; destination == third parties like Iran or China ;; origin == US ;; transporter == networks ;; vehicle == front companies in such countries ;; trigger == smuggling ;; movement.transportartifact.smuggleextract
place == London ;; trigger == protest ;; conflict.demonstrate.marchprotestpoliticalgathering
artifact == large - scale barrier wall ;; manufacturer == rulers ;; place == Ur ;; trigger == constructed ;; manufacture.artifact.build
attacker == Donald Trump and Russian President Vladimir Putin ;; instrument == poisoned ;; target == Hillary Clinton ;; trigger == poisoned ;; conflict.attack.n/a
artifact == malware ;; giver == The Shadow Brokers ;; recipient == the highest bidder ;; trigger == auctioning ;; transaction.transferownership.purchase
destination == Turkey ;; origin == Anbar ;; trigger == fled ;; movement.transportperson.n/a
trigger == immigration ;; inspection.sensoryobserve.inspectpeopleorganization
attacker == Pope Francis ;; instrument == assault ;; place == Syria ;; target == civilians ;; trigger == assailed ;; conflict.attack.n/a
origin == rooftop ;; passenger == Russian daredevil ;; preventer == police ;; trigger == detained ;; movement.transportperson.preventexit
participant == she ^^ Anderson Cooper ;; trigger == discuss ;; contact.discussion.n/a
artifact == city ;; place == Aleppo ;; trigger == battered ;; artifactexistence.damagedestroy.n/a
founder == Israel ;; gpe == West Bank ;; trigger == annex ;; government.formation.n/a
participant == participants in focus groups ^^ pollsters ;; place == Washington ;; trigger == convened ;; contact.discussion.n/a
place == Homs , Hama , Lataki and Damascus provinces ;; trigger == ceasefire ;; government.agreements.n/a
participant == he ^^ Mercer ;; place == that event ;; trigger == conferred ;; contact.discussion.meet
communicator == Milo Yiannopoulos ;; recipient == P.C. ;; trigger == preaching ;; contact.mediastatement.n/a
employee == Kamayev ;; place == Russia ;; placeofemployment == Russia 's Anti - Doping Agency ( RUSADA ;; trigger == quit ;; personnel.endposition.quitretire
destination == hospital ;; origin == remote area ;; passenger == 90 people ;; trigger == evacuated ;; movement.transportperson.evacuationrescue
passenger == Campbell ;; trigger == stumbling ;; movement.transportperson.n/a
victim == she ;; trigger == illness ;; life.injure.illnessdegradationphysical
destination == Europe ;; origin == Iran ;; passenger == crude ;; transporter == the country ;; trigger == shipment ;; movement.transportperson.disperseseparate
artifact == middle class ;; manufacturer == I ;; place == America ;; trigger == building ;; manufacture.artifact.n/a
attacker == Republicans in Congress ;; place == Congress ;; target == administration ;; trigger == blasted ;; conflict.attack.n/a
beneficiary == Russia ;; giver == Hillary Clinton ;; recipient == Uranium One ;; trigger == transfer control ;; transaction.transaction.transfercontrol
attacker == Russian ;; instrument == airstrikes ;; place == the air ;; target == the moderate armed opposition ;; trigger == annihilated ;; conflict.attack.selfdirectedbattle
deceased == baby ;; place == Gross Fredenwalde ;; trigger == burials ;; contact.funeralvigil.n/a
passenger == we ;; trigger == stumble ;; movement.transportperson.n/a
giver == Vladimir Putin , Iran ’s Ayatollah Ali Khamenei and Chinese President Xi Jinping ;; trigger == bought ;; transaction.transfermoney.purchase
communicator == Trump ;; recipient == The media critics ;; trigger == misled ;; contact.prevarication.meet
inspectedentity == planes ;; inspector == They ;; place == the base ;; trigger == inspections ;; inspection.sensoryobserve.physicalinvestigateinspect
employee == advisers ;; placeofemployment == Trump ;; trigger == hiring ;; personnel.startposition.hiring
communicator == He ;; recipient == blacks , gays , women and Hispanics ;; trigger == promises ;; contact.commitmentpromiseexpressintent.broadcast
defendant == Stalin ;; trigger == trials ;; justice.initiatejudicialprocess.trialhearing
demonstrator == Syrians ;; place == Syria ;; trigger == protest ;; conflict.demonstrate.marchprotestpoliticalgathering
origin == New Jersey ;; passenger == Rahami ;; trigger == arrested ;; movement.transportperson.preventexit
injurer == Muslim ;; instrument == axe ;; place == Germany ;; victim == five ;; trigger == attack ;; life.injure.injurycausedbyviolentevents
crime == missile attack ;; defendant == those ;; prosecutor == United Nations Security Council ;; trigger == trial ;; justice.initiatejudicialprocess.trialhearing
injurer == He ;; instrument == grabbing ;; victim == her ;; trigger == bruises ;; life.injure.injurycausedbyviolentevents
giver == United States Treasury Department ;; recipient == Mr. Ivanov ;; trigger == sanctions ;; transaction.transfermoney.n/a
inspectedentity == wreckage ;; inspector == Rebel fighters and civilians ;; place == Idlib province , Syria ;; trigger == inspect ;; inspection.sensoryobserve.physicalinvestigateinspect
giver == United States ;; money == lethal weapons ;; place == Vietnam ;; preventer == United States ;; recipient == Vietnam ;; trigger == embargo ;; transaction.transfermoney.embargosanction
giver == Clintons ;; trigger == auctioning ;; transaction.transfermoney.n/a
preventer == United States Treasury Department ;; recipient == Ivanov ;; trigger == sanctions ;; transaction.transferownership.embargosanction
defendant == he ;; trigger == charged ;; justice.initiatejudicialprocess.chargeindict
inspectedentity == Site ;; inspector == experienced investigators ;; place == the crash site ;; trigger == inspection ;; inspection.sensoryobserve.physicalinvestigateinspect
attacker == Trump ;; target == national debt ;; trigger == explode ;; conflict.attack.n/a
communicator == Briefings ;; recipient == Candidates ;; trigger == advised ;; contact.requestadvise.n/a
crime == the coup ;; defendant == Gulen ;; extraditer == the U.S. ;; origin == Turkey ;; trigger == extradites ;; justice.judicialconsequences.extradite
crime == used a private server exclusively ;; defendant == Hillary Clinton ;; investigator == FBI ;; trigger == investigated ;; justice.investigate.investigatecrime
artifact == media coverage ;; manufacturer == Republicans ;; trigger == produce ;; manufacture.artifact.n/a
participant == Security Secretary Jeh Johnson ^^ state election officials ;; trigger == convene ;; contact.discussion.correspondence
artifact == weapons ;; origin == US ;; transporter == US ;; trigger == exports ;; movement.transportartifact.n/a
origin == Turkey ;; passenger == migrants ;; trigger == smuggling ;; movement.transportperson.hide
killer == Montoya ;; victim == the man who killed his father ;; trigger == slays ;; life.die.deathcausedbyviolentevents
crime == extremely careless ;; defendant == Clinton ;; judgecourt == the FBI ;; prosecutor == James Comey ;; trigger == testified ;; justice.initiatejudicialprocess.trialhearing
communicator == the Kochs ;; recipient == people ;; trigger == misleading ;; contact.prevarication.broadcast
destination == London 's Ecuadorean embassy ;; granter == Ecuadorean embassy ;; passenger == Julian Assange ;; trigger == asylum ;; movement.transportperson.grantentryasylum
participant == interventionists ;; trigger == discussing ;; contact.discussion.n/a
origin == Moscow metro station ;; passenger == nanny ;; transporter == Police ;; trigger == detained ;; movement.transportperson.n/a
attacker == Leon Panetta ;; place == Democratic National Convention ;; target == Donald Trump ;; trigger == blasted ;; conflict.attack.n/a
beneficiary == Russia ;; participant == Hillary Clinton ^^ Russian interests ;; trigger == transfer control ;; transaction.transaction.n/a
artifact == Islamic State ;; damagerdestroyer == US ;; trigger == eradicate ;; artifactexistence.damagedestroy.n/a
defendant == Central Park Five ;; judgecourt == he ;; trigger == guilty ;; justice.judicialconsequences.n/a
communicator == the message ’s polite tone ;; place == the Turkish embassy ;; recipient == the public ;; trigger == deceive ;; contact.prevarication.correspondence
artifact == Russia ;; preventer == Senate ;; trigger == sanctions ;; transaction.transferownership.embargosanction
attacker == Jeb ;; target == Trump ;; trigger == assailing ;; conflict.attack.n/a
communicator == Perry ;; recipient == United States ;; trigger == advocating ;; contact.requestadvise.meet
artifact == all parties ;; giver == we ;; place == Syria and in the Middle East ;; preventer == we ;; recipient == weapons ;; trigger == embargo ;; transaction.transferownership.embargosanction
artifact == Ryan , as well as senators Mitch McConnell , Marco Rubio , and Ted Cruz ;; damager == presidential candidate ;; instrument == personal humiliation ;; trigger == damage ;; artifactexistence.damagedestroy.damage
beneficiary == charity ;; giver == he ;; money == money ;; recipient == charity ;; trigger == donated ;; transaction.transfermoney.giftgrantprovideaid
participant == Hillary Clinton ^^ Donald Trump ;; place == Hofstra University ;; trigger == debate ;; contact.discussion.meet
artifact == economic sanctions ;; giver == Barack Obama ;; recipient == North Korea ;; trigger == sanctions ;; transaction.transferownership.n/a
place == New York City ;; trigger == homicides ;; life.die.n/a
deceased == a young man ;; trigger == burial ;; contact.funeralvigil.meet
trigger == Auction ;; transaction.transfermoney.purchase
destination == Ukraine ;; passenger == Ukrainians ;; preventer == Russians ;; trigger == captured ;; movement.transportperson.preventexit
giver == our ;; trigger == auction ;; transaction.transferownership.n/a
transporter == Donald Trump ;; trigger == trafficking ;; movement.transportperson.n/a
crime == a crime ;; defendant == Clinton ;; prosecutor == James B. Comey ;; trigger == charged ;; justice.initiatejudicialprocess.chargeindict
detainee == anyone they want ;; jailer == authorities ;; place == totalitarian regimes or Guantanamo ;; trigger == incarcerate ;; justice.arrestjaildetain.arrestjaildetain
passenger == HIV and hepatitis C ;; preventer == the 1980s war on drugs ;; transporter == drug users ;; trigger == incarcerated ;; movement.transportperson.preventexit
employee == Rep. Tulsi Gabbard ;; placeofemployment == Democratic National Committee ;; trigger == quit ;; personnel.endposition.quitretire
recipient == Clinton ;; trigger == blackmail ;; contact.threatencoerce.broadcast
communicator == advisers ;; recipient == her ;; trigger == recommending ;; contact.requestadvise.meet
victim == Edwin ;; trigger == illness ;; life.injure.illnessdegradationphysical
deceased == man ;; place == German cemetery ;; trigger == burial ;; contact.funeralvigil.n/a
defendant == fossil fuel companies ;; place == several other countries ;; trigger == charges ;; justice.initiatejudicialprocess.chargeindict
attacker == Soviet Union ;; instrument == cakes ;; target == Ukrainian nationalists ;; trigger == exploding ;; conflict.attack.bombing
place == Maryland ;; trigger == violent ;; life.injure.injurycausedbyviolentevents
giver == Tehran ;; money == Iran ;; preventer == India ;; recipient == $ 6 billion ;; trigger == sanctions ;; transaction.transfermoney.embargosanction
giver == government ;; recipient == Moscow ;; trigger == sanctions ;; transaction.transfermoney.n/a
participant == Trump ^^ China ;; place == Iowa ;; trigger == negotiating ;; contact.negotiate.correspondence
communicator == West ;; place == West ;; recipient == Russia ;; trigger == blackmail ;; contact.threatencoerce.correspondence
passenger == weapons ;; transporter == Lockheed Martin ;; vehicle == the F-35 combat aircraft ;; trigger == shipments ;; movement.transportperson.disperseseparate
trigger == Buy ;; transaction.transferownership.purchase
artifact == Trump ;; damager == dark secret society ;; trigger == wreck ;; artifactexistence.damagedestroy.damage
injurer == security ;; trigger == harming ;; life.injure.injurycausedbyviolentevents
victim == Seth Rich ;; trigger == murder ;; life.die.deathcausedbyviolentevents
trigger == trafficking ;; movement.transportperson.n/a
beneficiary == He ;; giver == He ;; trigger == purchasing ;; transaction.transferownership.n/a
attacker == Kovtun ;; instrument == poison ;; target == Litvinenko ;; trigger == poison ;; conflict.attack.n/a
defendant == The Foundation ;; investigator == New York Attorney General ;; trigger == investigation ;; justice.investigate.n/a
demonstrator == We ;; trigger == protest ;; conflict.demonstrate.n/a
communicator == Israel ;; place == Israel ;; recipient == French ;; trigger == proposed ;; contact.publicstatementinperson.n/a
communicator == a disarmament group ;; place == letter ;; recipient == him ;; trigger == urged ;; contact.requestadvise.n/a
artifact == helicopter ;; destination == Hmeimim ;; trigger == transport ;; movement.transportartifact.fall
participant == House ^^ House ;; place == Capitol Hill ;; trigger == meets ;; contact.collaborate.meet
communicator == his ;; recipient == shake things up ;; trigger == vow ;; contact.commitmentpromiseexpressintent.n/a
artifact == Russian anti - aircraft missiles ;; origin == Catalonia region ;; transporter == Two Indian men ;; trigger == smuggling ;; movement.transportartifact.hide
transporter == drug users ;; trigger == incarcerated ;; movement.transportartifact.preventexit
place == Yemeni capital ;; trigger == funeral ;; contact.funeralvigil.meet
participant == Elias ^^ Podesta ;; trigger == discuss ;; contact.discussion.meet
killer == blacks ;; victim == victims ;; trigger == homicide ;; life.die.deathcausedbyviolentevents
artifact == oil ;; destination == Turkey ;; transporter == Turkey ;; trigger == importing ;; movement.transportartifact.grantentry
giver == Western ;; place == Crimea ;; preventer == Western ;; recipient == Crimea ;; trigger == sanctions ;; transaction.transfermoney.embargosanction
placeofemployment == Chief - of - Staff ;; trigger == sacks ;; personnel.endposition.n/a
victim == Muslim American soldier ;; trigger == deceased ;; life.die.n/a
injurer == US - led coalition ;; victim == Syrian army unit ;; trigger == strike ;; life.injure.n/a
origin == their homes ;; passenger == thousands of people ;; transporter == thousands of people ;; trigger == flee ;; movement.transportperson.evacuationrescue
communicator == Warren ;; recipient == elect Hillary Clinton ;; trigger == vowed ;; contact.commitmentpromiseexpressintent.meet
communicator == Broaddrick ;; trigger == oath ;; contact.commitmentpromiseexpressintent.broadcast
crime == Russian meddling ;; prosecutor == Obama DHS chief ;; trigger == testify ;; justice.initiatejudicialprocess.n/a
communicator == Republicans ;; trigger == lie ;; contact.prevarication.n/a
defendant == political activists and most of them were Kurds ;; executioner == totalitarian regime ;; place == Iran ;; trigger == executions ;; justice.judicialconsequences.execute
communicator == Clinton ;; place == convention ;; recipient == Veterans of Foreign Wars ;; trigger == vowing ;; contact.commitmentpromiseexpressintent.correspondence
participant == Clinton ;; trigger == email ;; contact.collaborate.correspondence
artifact == carfentanil ;; destination == the United In States ;; origin == China ;; transporter == people willing to skirt the law ;; vehicle == packages ;; trigger == supply ;; movement.transportartifact.sendsupplyexport
trigger == suggests ;; contact.requestadvise.meet
victim == eminence ;; trigger == deceased ;; life.die.n/a
giver == Iran ;; preventer == Alinejad ;; trigger == boycott ;; transaction.transaction.embargosanction
trigger == shipment ;; movement.transportartifact.fall
destination == Europe ;; origin == Iran ;; transporter == Iran ;; trigger == shipment ;; movement.transportperson.n/a
participant == Lavrov ^^ Kerry ;; place == Lausanne ;; trigger == meet ;; contact.discussion.meet
participant == he ^^ Crimea ;; place == Crimea ;; trigger == took over ;; government.formation.mergegpe
artifact == the severed head of a four - year - old girl ;; preventer == Investigators ;; transporter == A burka - clad babysitter ;; trigger == arrested ;; movement.transportartifact.preventexit
participant == we ;; trigger == negotiated ;; contact.discussion.meet
communicator == Assange ;; recipient == interviewer ;; trigger == suggesting ;; contact.requestadvise.meet
communicator == Socialism ;; recipient == we ;; trigger == demands ;; contact.commandorder.meet
participant == Russia ^^ U.S. ;; trigger == negotiate ;; contact.negotiate.meet
place == Israel ;; recipient == Israel ;; trigger == Boycott ;; transaction.transfermoney.embargosanction
place == Sinai ;; vehicle == Metrojet Flight 9268 ;; trigger == crash ;; disaster.accidentcrash.accidentcrash
giver == hackers ;; trigger == auction ;; transaction.transfermoney.n/a
place == Russia ;; recipient == Russia ;; trigger == sanctions ;; transaction.transferownership.embargosanction
crime == Abu Hayjaa was killed ;; defendant == Suspects ;; investigator == Iraqi and Chechen security officials ;; place == road outside Raqqa ;; trigger == investigate ;; justice.investigate.investigatecrime
artifact == access to the powerful family and their associates ;; beneficiary == themselves ;; giver == themselves ;; trigger == auctioning ;; transaction.transferownership.purchase
artifact == phones ;; destination == prison ;; transporter == she ;; trigger == smuggled ;; movement.transportartifact.smuggleextract
origin == Aleppo ;; passenger == an injured man ;; transporter == Syrians ;; trigger == evacuate ;; movement.transportperson.evacuationrescue
artifact == AR-15 rifle ;; beneficiary == Mohamed Bailor Jalloh ;; giver == Blue Ridge Arsenal gun store ;; place == Chantilly , Va. ;; recipient == Mohamed Bailor Jalloh ;; trigger == purchase ;; transaction.transferownership.n/a
place == Sandy Hook ;; trigger == massacre ;; conflict.attack.n/a
beneficiary == the Clinton Foundation ;; giver == Judy Woodruff ;; trigger == donated ;; transaction.transfermoney.giftgrantprovideaid
employee == them ;; placeofemployment == Russians ;; trigger == hiring ;; personnel.startposition.n/a
communicator == Mike Roman ;; recipient == conservatives ;; trigger == misleading ;; contact.prevarication.broadcast
victim == American citizens ;; trigger == assassinate ;; life.die.n/a
artifact == three ;; preventer == Emirate ;; trigger == arrested ;; movement.transportartifact.preventexit
hidingplace == offshore accounts ;; transporter == at least 140 current or former world leaders ;; trigger == conceal ;; movement.transportperson.hide
attacker == Saudi Arabia and its regional allies ;; instrument == airstrike ;; place == Yemen ;; target == Yemen ;; trigger == airstrike ;; conflict.attack.n/a
beneficiary == countries like Venezuela ;; giver == It ;; place == China ;; trigger == loans ;; transaction.transferownership.borrowlend
governmentbody == Obama ;; law == sanctions ;; place == Tehran ;; trigger == signed into law ;; government.legislate.legislate
place == Iraq ;; trigger == war ;; conflict.attack.selfdirectedbattle
participant == Chinese ^^ U.S. ;; trigger == meeting ;; contact.collaborate.n/a
communicator == Trump ;; trigger == lied ;; contact.prevarication.correspondence
beneficiary == Trump ;; giver == who ;; trigger == loans ;; transaction.transfermoney.borrowlend
defendant == her ;; judgecourt == Justice Department ;; prosecutor == Justice Department ;; trigger == indicts ;; justice.initiatejudicialprocess.chargeindict
origin == UK ;; passenger == Assange ;; preventer == UK ;; trigger == jailed ;; movement.transportperson.preventexit
injurer == rape ;; trigger == rape ;; life.injure.n/a
money == weapons ;; place == Syria and in the Middle East ;; preventer == we ;; recipient == all parties ;; trigger == embargo ;; transaction.transfermoney.embargosanction
giver == U.S. authorities ;; place == U.S. ;; recipient == 3.7 kilograms ( 8.1 pounds ) of fentanyl ;; trigger == seized ;; transaction.transaction.transfercontrol
law == Syringe restrictions ;; trigger == legislated ;; government.legislate.legislate
demonstrator == We ;; place == TV4 ;; trigger == protest ;; conflict.demonstrate.marchprotestpoliticalgathering
spy == agencies ;; trigger == spy ;; government.spy.spy
place == Washington , D.C. ;; target == Seth Rich ;; trigger == robbery ;; conflict.attack.n/a
artifact == car ;; target == Mr. Mansoor ;; trigger == robbed ;; conflict.attack.stealrobhijack
participant == COOPER ^^ Senator Sanders ;; place == debate ;; trigger == debate ;; contact.negotiate.meet
passenger == Rubio ;; trigger == plummeted ;; movement.transportperson.fall
crime == sexual misconduct ;; defendant == Assange ;; destination == Sweden ;; extraditer == Sweden ;; origin == London ;; trigger == extradition ;; justice.judicialconsequences.extradite
inspectedentity == official explanation ;; place == New Jersey ;; trigger == inspection ;; inspection.sensoryobserve.physicalinvestigateinspect
artifact == shipment ;; destination == Chongjin ;; origin == Russia ;; transporter == Russia ;; trigger == shipment ;; movement.transportartifact.n/a
money == reliable news ;; place == dark corner of the battlefield ;; trigger == embargo ;; transaction.transfermoney.embargosanction
trigger == shoot ;; life.die.deathcausedbyviolentevents
artifact == crude shipment ;; destination == Europe ;; origin == Iran ;; transporter == Iran ;; trigger == exported ;; movement.transportartifact.sendsupplyexport
communicator == US and Seoul ;; trigger == announced ;; contact.publicstatementinperson.broadcast
artifact == food ;; origin == Black Sea ports ;; transporter == Russia ;; trigger == exports ;; movement.transportartifact.sendsupplyexport
beneficiary == Palmali Shipping and Transportation Agency ;; giver == BMZ ;; place == Istanbul ;; trigger == acquired ;; transaction.transfermoney.purchase
participant == Pentagon ^^ foreign buyer ;; trigger == negotiate ;; contact.negotiate.correspondence
demonstrator == Rahul Gandhi ;; place == Jantar Mantar ;; trigger == protesting ;; conflict.demonstrate.n/a
recipient == The Shadow Brokers ;; trigger == auctioning ;; transaction.transfermoney.purchase
communicator == Kasich ;; trigger == pledged ;; contact.commitmentpromiseexpressintent.broadcast
artifact == those ;; destination == Maryland ;; origin == Maryland ;; preventer == policies ;; trigger == imprisoned ;; movement.transportartifact.preventexit
trigger == War ;; conflict.attack.selfdirectedbattle
communicator == super PAC ;; recipient == her to run sets her apart from Clinton ;; trigger == urging ;; contact.requestadvise.n/a
crashobject == Chinese fighter jet ;; place == China ;; vehicle == US spy plane ;; trigger == colliding ;; disaster.accidentcrash.accidentcrash
beneficiary == government ;; recipient == government ;; trigger == bought ;; transaction.transfermoney.n/a
demonstrator == Senate Dems ;; trigger == protest ;; conflict.demonstrate.marchprotestpoliticalgathering
artifact == the Baltic states ;; destroyer == RUSSIA ;; place == Eastern Europe ;; trigger == demolish ;; artifactexistence.damagedestroy.destroy
inspectedentity == Iran ;; inspector == Obama administration ;; place == Iran ;; trigger == inspection ;; inspection.sensoryobserve.inspectpeopleorganization
participant == leader of Yemen ’s Houthi Ansarullah movement ;; place == Saudi Arabia ;; trigger == funeral ;; contact.funeralvigil.n/a
voter == adults ;; trigger == win elections ;; personnel.elect.winelection
employee == Louise Sunshine ;; place == Fifth Avenue office ;; placeofemployment == director of special projects ;; trigger == hired ;; personnel.startposition.hiring
employee == Manafort ;; placeofemployment == campaign ;; trigger == resigned ;; personnel.endposition.quitretire
attacker == SCUD ;; place == Iraq ;; trigger == stabbings ;; conflict.attack.n/a
participant == he ^^ Kim Jong - un ;; trigger == negotiating ;; contact.discussion.meet
communicator == party ;; recipient == American people ;; trigger == deceive ;; contact.prevarication.n/a
observedentity == The argument ;; trigger == inspection ;; inspection.sensoryobserve.n/a
communicator == We ;; recipient == Russian ;; trigger == urge ;; contact.commandorder.broadcast
place == North Korea ;; trigger == funerals ;; contact.funeralvigil.meet
killer == Ottoman Turks ;; victim == Armenians ;; trigger == genocide ;; life.die.deathcausedbyviolentevents
beneficiary == charity ;; giver == Clintons ;; recipient == charity ;; trigger == donated ;; transaction.transaction.giftgrantprovideaid
destination == hospital in the city of Salekhard ;; origin == the contaminated area ;; passenger == indigenous people ;; trigger == evacuated ;; movement.transportperson.evacuationrescue
artifact == rover ;; manufacturer == UK ;; trigger == assembled ;; manufacture.artifact.createmanufacture
participant == Russia ^^ Crimean Peninsula ;; place == Crimean Peninsula ;; trigger == annex ;; government.formation.mergegpe
spy == your ;; trigger == spy ;; government.spy.spy
communicator == we ;; recipient == them ;; trigger == urge ;; contact.commandorder.n/a
victim == Mujib ;; trigger == executing ;; life.die.n/a
artifact == any missile shield ;; damager == Russia ;; instrument == attack systems ;; place == over Europe ;; trigger == piercing ;; artifactexistence.damagedestroy.damage
participant == convenience stores ;; place == Alabama ;; trigger == confiscated ;; transaction.transaction.n/a
communicator == Those ;; recipient == Trump ’s alpha - male status ;; trigger == threaten ;; contact.threatencoerce.meet
destination == Russia ;; origin == Hawaii ;; passenger == he ;; trigger == fled ;; movement.transportperson.n/a
artifact == wheat and corn ;; transporter == U.S. ;; trigger == shipments ;; movement.transportartifact.n/a
communicator == Trump ;; recipient == to throw her in prison ;; trigger == vow ;; contact.commitmentpromiseexpressintent.n/a
preventer == Trump ;; transporter == Clinton ;; trigger == jail ;; movement.transportartifact.preventexit
communicator == Lawmakers ;; recipient == White House ;; trigger == urging ;; contact.commandorder.correspondence
beneficiary == Clinton ;; recipient == astroturfing efforts ;; trigger == funded ;; transaction.transaction.giftgrantprovideaid
attacker == his Russian companions - turned - executioners ;; instrument == polonium ;; place == London ;; target == He ;; trigger == poisoning ;; conflict.attack.biologicalchemicalpoisonattack
place == Afghanistan ;; trigger == conflict ;; conflict.attack.selfdirectedbattle
preventer == he ;; transporter == Clinton ;; trigger == incarcerate ;; movement.transportartifact.preventexit
giver == government ;; recipient == small businesses ;; trigger == loans ;; transaction.transfermoney.borrowlend
artifact == troops ;; destination == country ;; transporter == he ;; trigger == bringing in ;; movement.transportartifact.n/a
artifact == seal - fur products ;; destination == the European Union , the United States and Russia ;; origin == Canadian ;; trigger == export ;; movement.transportartifact.sendsupplyexport
employee == Chief - of - Staff ;; place == Russian President ’s inner circle ;; placeofemployment == Chief - of - Staff ;; trigger == sacks ;; personnel.endposition.firinglayoff
giver == Russia ;; preventer == European Union ;; trigger == embargo ;; transaction.transferownership.embargosanction
place == North Carolina ;; trigger == voting ;; government.vote.castvote
artifact == the inquiry finds ;; instrument == nuclear reactor ;; trigger == manufactured ;; manufacture.artifact.n/a
artifact == Rich ;; attacker == death ;; target == Rich ;; trigger == robbery ;; conflict.attack.stealrobhijack
communicator == the firm ;; recipient == itself ;; trigger == professes ;; contact.publicstatementinperson.broadcast
artifact == amorphous movement ;; manufacturer == we ;; trigger == build ;; manufacture.artifact.build
attacker == Muslim cleric ;; instrument == coup ;; place == Ankara ;; target == Ankara ;; trigger == coup ;; conflict.attack.selfdirectedbattle
attacker == Syrian government ;; instrument == barrel bomb ;; place == hospital ;; target == 27 people ;; trigger == bombs ;; conflict.attack.bombing
artifact == Cyber Weapons ;; beneficiary == they ;; place == country ;; recipient == Cyber Weapons ;; trigger == Auction ;; transaction.transferownership.purchase
passenger == Victims ;; trigger == Trafficking ;; movement.transportperson.smuggleextract
defendant == he ;; trigger == investigation ;; justice.investigate.investigatecrime
attacker == Mexican ;; place == Alamo ;; target == Texas ;; trigger == battle ;; conflict.attack.selfdirectedbattle
participant == Russia ^^ United States ;; trigger == treaty ;; government.agreements.acceptagreementcontractceasefire
employee == Steven L. Hall ;; place == Eurasia and Latin ;; placeofemployment == Central Intelligence Agency ;; trigger == retiring ;; personnel.endposition.n/a
place == Iran ;; trigger == boycott ;; transaction.transferownership.embargosanction
beneficiary == losers ;; giver == winners ;; trigger == compensated ;; transaction.transfermoney.payforservice
giver == Vietnam ;; money == Asia ;; place == Vietnam ;; preventer == President Obama ;; recipient == lethal arms ;; trigger == embargo ;; transaction.transfermoney.embargosanction
attacker == Polish villagers ;; target == 1,600 Jewish men , women and children ;; trigger == massacre ;; conflict.attack.selfdirectedbattle
inspectedentity == The argument ;; trigger == inspection ;; inspection.sensoryobserve.inspectpeopleorganization
recipient == He ;; trigger == loan ;; transaction.transfermoney.borrowlend
destination == the military training area ;; vehicle == rail ;; trigger == transported ;; movement.transportperson.bringcarryunload
governmentbody == LBJ ;; law == civil rights legislation ;; trigger == signed into law ;; government.legislate.legislate
attacker == Russia ;; place == Estonia , Latvia and Lithuania ;; target == Estonia , Latvia and Lithuania ;; trigger == encroached ;; conflict.attack.invade
crime == money laundering , fraud and extortion ;; defendant == Ukraine Pavlo Lazarenko ;; trigger == convicted ;; justice.judicialconsequences.n/a
artifact == firearms ;; transporter == immigrants ;; trigger == trafficking ;; movement.transportartifact.n/a
artifact == others ;; damager == megalomaniac ;; instrument == any means ;; trigger == tear ;; artifactexistence.damagedestroy.damage
communicator == Trump ;; recipient == tax returns ;; trigger == reiterated ;; contact.publicstatementinperson.n/a
participant == reporters ^^ they ;; place == Israel ;; trigger == dialogue ;; contact.discussion.correspondence
inspectedentity == Documents ;; inspector == the Daily Dot ;; trigger == inspected ;; inspection.sensoryobserve.physicalinvestigateinspect
origin == Maryland ;; passenger == imprisoned population ;; preventer == Maryland ;; trigger == imprisoned ;; movement.transportperson.preventexit
killer == Trump ;; victim == Clinton ;; trigger == assassinate ;; life.die.deathcausedbyviolentevents
place == Syria ;; trigger == starvation ;; life.injure.n/a
artifact == money ;; giver == asking ;; place == Saudi Arabia ;; recipient == Saudi Arabia ;; trigger == borrow ;; transaction.transferownership.borrowlend
attacker == the military and associated paramilitary squads ;; place == Latin America ;; target == peasants ;; trigger == massacres ;; conflict.attack.selfdirectedbattle
communicator == athletes ;; recipient == us ;; trigger == deceive ;; contact.prevarication.meet
injurer == Dr. Richard Cytowic ;; victim == Cruz ;; trigger == punch ;; life.injure.n/a
injurer == The YPG ;; place == the city ;; trigger == wounded ;; life.injure.injurycausedbyviolentevents
communicator == Sanders ;; trigger == vowing ;; contact.commitmentpromiseexpressintent.meet
beneficiary == their wealth and control ;; giver == Wealthy Elite ;; recipient == cyber weapons ;; trigger == auction ;; transaction.transferownership.purchase
artifact == carfentanil ;; destination == North America ;; origin == China ;; transporter == Several vendors ;; trigger == export ;; movement.transportartifact.sendsupplyexport
governmentbody == Britain ;; law == new sanctions ;; place == Britain ;; trigger == enact ;; government.legislate.legislate
communicator == Hillary Clinton ;; recipient == Bill Clinton 's accusers ;; trigger == intimidate ;; contact.threatencoerce.n/a
candidate == President Recep Tayyip Erdogan ;; place == the country ;; voter == the country ;; trigger == elected ;; personnel.elect.winelection
artifact == the terrorist group Islamic State ( IS , formerly ISIS / ISIL ) ;; destroyer == US ;; trigger == eradicate ;; artifactexistence.damagedestroy.destroy
attacker == Litvinenko ;; instrument == polonium ;; place == the table ;; target == Litvinenko ;; trigger == poisoned ;; conflict.attack.biologicalchemicalpoisonattack
attacker == Palestinian woman ;; place == Hebron ;; trigger == stabbing ;; conflict.attack.stabbing
communicator == Chelsea Clinton ;; recipient == foundation ’s rank ;; trigger == intimidating ;; contact.threatencoerce.n/a
crime == meddling ;; prosecutor == DHS chief ;; trigger == testify ;; justice.initiatejudicialprocess.n/a
communicator == Obama ;; place == news conference ;; recipient == caution ;; trigger == preached ;; contact.mediastatement.n/a
victim == NFL players ;; trigger == deceased ;; life.die.n/a
beneficiary == Russia ;; place == Crimea ;; recipient == Russia ;; territoryorfacility == Crimea ;; trigger == land grab ;; transaction.transaction.transfercontrol
artifact == Donald Trump ;; damager == Donald Trump ;; instrument == interview ;; trigger == wreck ;; artifactexistence.damagedestroy.damage
communicator == Trump ;; recipient == Ghazala ;; trigger == suggested ;; contact.requestadvise.n/a
injurer == Mr. Campbell ;; instrument == his arm ;; victim == Ms. Teeter ;; trigger == punched ;; life.injure.injurycausedbyviolentevents
crime == smuggling Russian anti - aircraft missiles ;; defendant == Two Indian men ;; judgecourt == United States ;; place == Spain ;; trigger == extradited ;; justice.judicialconsequences.n/a
artifact == crude ;; origin == Russia ;; transporter == Russia ;; trigger == shipments ;; movement.transportartifact.disperseseparate
participant == he ;; trigger == debate ;; contact.negotiate.meet
beneficiary == taxes ;; giver == family ;; money == 31 percent of their income ;; recipient == taxes ;; trigger == income ;; transaction.transfermoney.payforservice
beneficiary == Tehran ;; giver == Indian government ;; recipient == Tehran ;; trigger == payment ;; transaction.transfermoney.n/a
artifact == sample documents ;; trigger == auction ;; transaction.transferownership.n/a
communicator == Hillary ;; recipient == victims ;; trigger == intimidated ;; contact.threatencoerce.meet
