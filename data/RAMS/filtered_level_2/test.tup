communicator == athletes ;; place == Russia ;; recipient == us , and the world ;; trigger == deceive ;; contact.prevarication
victim == Russians ;; trigger == slaughtered ;; life.die
destination == Turkey ;; origin == Syria ;; passenger == oil ;; trigger == smuggled ;; movement.transportperson
yielder == Japan ;; trigger == surrender ;; conflict.yield
beneficiary == the Clinton Foundation ;; giver == the Qataris ;; recipient == millions ;; trigger == donated ;; transaction.transferownership
artifact == agreement ;; trigger == wrecking ;; artifactexistence.damagedestroy
employee == team of government regulators ;; trigger == employed ;; personnel.startposition
defendant == at least 27 of them ;; executioner == Iranian security forces ;; place == public squares ;; trigger == executed ;; justice.judicialconsequences
destination == the Trump Tower lobby ;; transporter == Japanese tourists ;; trigger == roamed ;; movement.transportperson
participant == Bush ’s administration ;; trigger == negotiated ;; contact.discussion
beneficiary == environmental groups ;; giver == Kremlin ;; trigger == funding ;; transaction.transferownership
communicator == President Obama ;; place == the White House ;; recipient == China ;; trigger == urged ;; contact.commandorder
killer == a nanny ;; place == Moscow ;; victim == a child ;; trigger == murder ;; life.die
beneficiary == Trump ;; giver == Sheldon Adelson ;; recipient == his election ;; trigger == donated ;; transaction.transaction
trigger == killing ;; life.die
place == France ;; victim == people ;; trigger == injured ;; life.injure
employee == men ;; place == a special forces base ;; placeofemployment == Wagner ;; trigger == recruits ;; personnel.startposition
detainee == rival politicians ;; jailer == it ;; place == Pakistan ;; trigger == jailed ;; justice.arrestjaildetain
crime == meeting with Igor Sechin and Sergei Ivanov ;; defendant == Page ;; investigator == FBI ;; place == Moscow ;; trigger == investigating ;; justice.investigate
attacker == ISIS ;; place == al - Bagilya ;; target == families ;; trigger == massacre ;; conflict.attack
inspectedentity == damage ;; inspector == he ;; place == Ankara ;; trigger == inspected ;; inspection.sensoryobserve
trigger == capitulation ;; conflict.yield
place == Istanbul ;; yielder == Soldiers ;; trigger == surrender ;; conflict.yield
trigger == death penalty ;; justice.judicialconsequences
attacker == advancing army ;; place == the country ;; target == the country ;; trigger == battle ;; conflict.attack
place == Yemeni ;; trigger == funeral ;; contact.funeralvigil
artifact == oil ;; destination == Turkey ;; origin == Syria and Iraq ;; transporter == Erdogan ;; vehicle == trucks ;; trigger == importing ;; movement.transportartifact
beneficiary == Police ;; giver == Police ;; place == Zakharchenko ’s ] house ;; recipient == a cache ;; trigger == confiscated ;; transaction.transaction
origin == jail ;; passenger == Harold Martin ;; preventer == jail ;; trigger == jail ;; movement.transportperson
trigger == genocide ;; life.die
injurer == Trump ;; victim == women ;; trigger == assaulted ;; life.injure
attacker == terrorist ;; target == civilians ;; trigger == stabbings ;; conflict.attack
crime == economic espionage ;; defendant == five Chinese military hackers ;; prosecutor == United States ;; trigger == charged ;; justice.initiatejudicialprocess
artifact == barbecue grills ;; giver == sheriff 's office ;; trigger == purchase ;; transaction.transferownership
injurer == man ;; place == Donald Trump rally ;; victim == protester ;; trigger == punching ;; life.injure
recipient == bitcoin , the cryptocurrency ;; trigger == auction ;; transaction.transfermoney
communicator == Damascus ;; place == border ;; recipient == intervene ;; trigger == vowing ;; contact.commitmentpromiseexpressintent
destination == America ;; passenger == human ;; trigger == smuggling ;; movement.transportperson
recipient == some people ;; trigger == obtain ;; transaction.transfermoney
place == Syria ;; surrenderer == United States ;; trigger == capitulation ;; conflict.yield
deceased == their dead ;; participant == they ^^ they ;; place == a place ;; trigger == burials ;; contact.funeralvigil
beneficiary == Russia ;; observedentity == Turkey ;; place == along the Turkey - Syria border ;; spy == Russian military ;; trigger == surveillance ;; government.spy
communicator == Trump ;; trigger == lied ;; contact.prevarication
crime == did n't have money to pay the fines ;; detainee == people ;; place == Birmingham ;; trigger == arrested ;; justice.arrestjaildetain
place == Washington , D.C. ;; victim == Rich ;; trigger == murder ;; life.die
deceased == opponents ;; trigger == Funeral ;; contact.funeralvigil
place == Middle East ;; victim == Khan ;; trigger == died ;; life.die
attacker == Dahir Adan ;; place == Minnesota ;; target == Nine people ;; trigger == stabbings ;; conflict.attack
injurer == allegation ;; victim == Trump ;; trigger == rape ;; life.injure
beneficiary == economy ;; giver == banks ;; money == $ 10 billion ;; place == Saudi Arabia ;; recipient == country ;; trigger == loan ;; transaction.transfermoney
detainee == Clinton ;; jailer == he ;; trigger == incarcerate ;; justice.arrestjaildetain
founder == Kurdish groups ;; gpe == de facto autonomy ;; place == Syria ;; trigger == established ;; government.formation
participant == Special prosecutor Robert Mueller ^^ senior Senate Judiciary Committee ;; trigger == meet ;; contact.discussion
beneficiary == Trump ;; observedentity == Clinton ;; spy == Russia ;; trigger == spy ;; government.spy
giver == Trumps ;; trigger == bought ;; transaction.transfermoney
defendant == Ms. Abedin ;; prosecutor == officials ;; trigger == testified ;; justice.initiatejudicialprocess
participant == adults ;; trigger == telephone ;; contact.collaborate
beneficiary == she ;; giver == elites ;; recipient == she ;; trigger == funded ;; transaction.transferownership
participant == Trump ^^ Clinton ;; place == Las Vegas ;; trigger == debate ;; contact.discussion
beneficiary == al Qaeda / ISIS ;; place == Iraq ;; recipient == weapons ;; trigger == seized ;; transaction.transaction
killer == Ottoman authorities ;; place == their capital , Constantinople , present - day Istanbul ;; victim == 250 Armenian intellectuals ;; trigger == genocide ;; life.die
place == the square ;; target == Issa ;; trigger == hanged ;; conflict.attack
communicator == America ;; place == America ;; recipient == Russia ;; trigger == blackmail ;; contact.threatencoerce
communicator == Turkey ;; trigger == proposed ;; contact.publicstatementinperson
beneficiary == top earners ;; money == wages ;; recipient == top earners ;; trigger == wages ;; transaction.transfermoney
artifact == Mr. Assange ;; destination == Sweden ;; origin == London ;; preventer == Ecuadorean Embassy ;; transporter == Sweden ;; trigger == arrested ;; movement.transportartifact
participant == Trump ^^ Clinton ;; trigger == debate ;; contact.negotiate
placeofemployment == Islamist militancy ;; trigger == recruits ;; personnel.startposition
crashobject == asteroid ;; trigger == collision ;; disaster.accidentcrash
attacker == Russia ;; place == Aleppo ;; target == Aleppo ;; trigger == bombarding ;; conflict.attack
origin == the metro station entrance in north - west Moscow ;; transporter == a suicide bomber ;; trigger == walked ;; movement.transportperson
instrument == destroyers ;; killer == destroyers ;; place == the water ;; victim == they ;; trigger == shot ;; life.die
participant == Russia ^^ U.S. ;; place == Syria ;; trigger == ceasefire ;; government.agreements
employee == chairwoman ;; place == U.S. ;; placeofemployment == party ;; trigger == resigned ;; personnel.endposition
damagerdestroyer == Neoconservative wrecking crew ;; trigger == wrecking ;; artifactexistence.damagedestroy
giver == Senators McCain and Corker ;; money == taxpayer dollars ;; place == the Arms Control Association ;; recipient == taxpayer dollars ;; trigger == acquire ;; transaction.transfermoney
attacker == Russian air forces ;; place == Aleppo province ;; target == ISIS oil tankers ;; trigger == annihilate ;; conflict.attack
instrument == polonium ;; trigger == fatal ;; life.die
employee == Bashar al - Assad ;; trigger == oust ;; personnel.endposition
place == Turkey ;; victim == warplane ;; trigger == shot ;; life.die
artifact == Trump ;; damagerdestroyer == dark secret society ;; trigger == wreck ;; artifactexistence.damagedestroy
giver == Canadian authorities ;; recipient == a kilogram of carfentanil ;; trigger == seized ;; transaction.transaction
attacker == perpetrator ;; target == Russians ;; trigger == hanged ;; conflict.attack
detainee == several people ;; jailer == police ;; trigger == detained ;; justice.arrestjaildetain
transporter == African - American baby ;; trigger == incarcerated ;; movement.transportperson
participant == United States ^^ Iran ;; trigger == agreement ;; government.agreements
participant == Alex Marlow ^^ former Navy SEAL and Blackwater CEO Erik Prince ;; trigger == discussed ;; contact.discussion
victim == they ;; trigger == sick ;; life.injure
injurer == extremists ;; trigger == violent ;; life.injure
origin == Aleppo ;; passenger == injured man ;; transporter == Syrians ;; trigger == evacuate ;; movement.transportperson
artifact == tax reform ;; instrument == muscle ;; trigger == manufacturing ;; manufacture.artifact
injurer == I ;; victim == him ;; trigger == punch ;; life.injure
participant == Russian ^^ DNC ;; trigger == emails ;; contact.collaborate
beneficiary == Million Mom March ;; giver == he ;; money == funds ;; place == the city ;; recipient == funds ;; trigger == reimbursed ;; transaction.transfermoney
place == U.S.-Mexico border ;; target == Wall ;; trigger == bombarded ;; conflict.attack
communicator == the group ;; trigger == recommended ;; contact.requestadvise
communicator == Hillary Clinton ;; trigger == reiterated ;; contact.publicstatementinperson
deceased == girl ;; place == Kiryat Arba ;; trigger == funeral ;; contact.funeralvigil
communicator == she ;; place == Germany ;; recipient == refugees from Iraq and Syria ;; trigger == urged ;; contact.requestadvise
employee == Abedin ;; trigger == quits ;; personnel.endposition
communicator == Cuban lawsuit ;; recipient == US compensation ;; trigger == demanded ;; contact.commandorder
beneficiary == Heede ;; giver == Greenpeace , 350.org , and the Union of Concerned Scientists ;; place == San Francisco Bay ;; recipient == Heede ;; trigger == rented ;; transaction.transfermoney
instrument == nuclear strike ;; place == either country ;; target == no military target ;; trigger == strike ;; conflict.attack
attacker == Saudi Arabia and its regional allies ;; instrument == Scud - type Borkan-1 ( Volcano-1 ) missile ;; place == Yemen ;; target == Yemen ;; trigger == airstrike ;; conflict.attack
artifact == pests such as rats ;; destroyer == Biological warfare troops and civilian emergencies specialists ;; trigger == eradicate ;; artifactexistence.damagedestroy
communicator == Page ;; recipient == companies ;; trigger == advised ;; contact.requestadvise
artifact == poverty and hunger ;; damagerdestroyer == Cuba ;; place == Cuba ;; trigger == eradicate ;; artifactexistence.damagedestroy
trigger == recruiting ;; personnel.startposition
candidate == Clinton ;; place == New York ;; trigger == winning election ;; personnel.elect
communicator == Vitaly Churkin ;; trigger == announced ;; contact.publicstatementinperson
attacker == he ;; target == Julian Assange ;; trigger == assailed ;; conflict.attack
place == Tehran ;; trigger == execution ;; justice.judicialconsequences
deceased == a baby ;; place == Gross Fredenwalde ;; trigger == burials ;; contact.funeralvigil
giver == he ;; recipient == very much ;; trigger == paid ;; transaction.transfermoney
place == Syria ;; victim == 280 victims , including women , children and old people ;; trigger == massacre ;; life.die
artifact == carfentanil ;; origin == Chinese ;; trigger == trafficking ;; movement.transportartifact
place == Minnesota mall ;; trigger == stabbings ;; conflict.attack
inspectedentity == handset ’s processor ;; inspector == hackers ;; trigger == inspecting ;; inspection.sensoryobserve
killer == She ;; victim == child ;; trigger == decapitated ;; life.die
beneficiary == ISIS ;; giver == the Russian and Syrian governments ;; trigger == purchasing ;; transaction.transfermoney
destination == Calais ;; preventer == Police ;; transporter == a rowdy anti - migrant protest ;; trigger == detained ;; movement.transportartifact
place == Syria ;; trigger == starvation ;; life.injure
trigger == suggest ;; contact.requestadvise
artifact == evidence ;; manufacturer == campaign ;; trigger == assembled ;; manufacture.artifact
communicator == Trump ;; recipient == NATO ;; trigger == threatened ;; contact.threatencoerce
injurer == He ;; victim == she ;; trigger == bruises ;; life.injure
beneficiary == Police ;; giver == Epstein ;; place == Palm Beach mansion ;; recipient == trash ;; territoryorfacility == Palm Beach mansion ;; trigger == confiscated ;; transaction.transaction
communicator == 41-year - old mother of three told Rossiya 24 ;; place == broadcast ;; recipient == interview ;; trigger == broadcast ;; contact.mediastatement
passenger == Yulia Tymoshenko ;; trigger == imprisoned ;; movement.transportperson
recipient == Obama ;; trigger == misled ;; contact.prevarication
participant == Trump ^^ media ;; trigger == interviews ;; contact.discussion
beneficiary == He ;; giver == He ;; recipient == Miss Universe pageant ;; trigger == purchasing ;; transaction.transfermoney
artifact == 30 billion euros ;; destination == Turkey ;; transporter == industry ;; trigger == brings in ;; movement.transportartifact
beneficiary == Bill Clinton ;; giver == UBS and Barclays , mining giant BHP , and the for - profit educational company Laureate International Universities ;; recipient == Bill Clinton ;; trigger == payments ;; transaction.transfermoney
attacker == us , service members and our country ;; place == Iraq and Libya and now in Syria ;; trigger == wars ;; conflict.attack
crime == charges relating to terrorism ;; detainee == 30 foreign - born individuals ;; place == U.S. ;; trigger == arrested ;; justice.arrestjaildetain
detainee == Snowden ;; place == home ;; trigger == jail ;; justice.arrestjaildetain
crime == the promotion of low - fat diets ;; defendant == nutritionists ;; trigger == charge ;; justice.initiatejudicialprocess
injurer == Pence ;; instrument == policies ;; victim == minorities and women ;; trigger == hurt ;; life.injure
place == Iraq ;; victim == friends ;; trigger == die ;; life.die
victim == patients ;; trigger == dehydrated ;; life.injure
communicator == Hillaryland ;; recipient == people ;; trigger == intimidated ;; contact.threatencoerce
communicator == who or what ;; trigger == broadcasts ;; contact.mediastatement
attacker == Seth Rich ;; trigger == robbery ;; conflict.attack
crime == giving information to the coalition or putting ( GPS ) chips in order for the aircraft to strike at a specific area ;; defendant == fighters ;; judgecourt == ISIS ;; place == Syria ;; trigger == executed ;; justice.judicialconsequences
participant == Sanders ^^ the unapologetically anti - communist panel ;; trigger == debate ;; contact.negotiate
communicator == Democrats and Republicans who serve on defense , law enforcement or intelligence committees , as well as a bipartisan group of 31 security and counterterrorism experts ;; recipient == Obama ;; trigger == urged ;; contact.commandorder
participant == Russians ^^ Crimea ;; place == Crimea ;; trigger == took over ;; government.formation
place == Leningrad ;; recipient == Nazi forces ;; surrenderer == residents ;; trigger == surrendering ;; conflict.yield
attacker == coup ;; place == Istanbul ;; target == victims ;; trigger == coup ;; conflict.attack
place == Libya ;; victim == Muammar Gaddafi ;; trigger == murder ;; life.die
artifact == pathetic monuments ;; manufacturer == 1 percent ;; trigger == construct ;; manufacture.artifact
communicator == GOP members ;; recipient == Trump ;; trigger == urged ;; contact.requestadvise
attacker == Russian air forces ;; place == Aleppo province ;; target == ISIS oil tankers ;; trigger == annihilate ;; conflict.attack
communicator == U.S. officials ;; place == U.S. ;; recipient == individual states ;; trigger == advised ;; contact.requestadvise
artifact == The Trace Gas Orbiter ;; manufacturer == European manufacturer ;; trigger == assembled ;; manufacture.artifact
communicator == Schultz ;; recipient == Congress ;; trigger == urged ;; contact.commandorder
defendant == Clinton ;; prosecutor == FBI ;; trigger == indict ;; justice.initiatejudicialprocess
artifact == The incident ;; trigger == captured ;; movement.transportartifact
executioner == government ;; place == Saudi Arabia ;; trigger == executions ;; justice.judicialconsequences
origin == Salekhard ;; passenger == boy ;; trigger == evacuated ;; movement.transportperson
place == around the world ;; trigger == protest ;; conflict.demonstrate
beneficiary == the campaign ;; giver == independent SuperPAC ;; trigger == funded ;; transaction.transferownership
artifact == sex ;; transporter == suspect ;; trigger == trafficking ;; movement.transportartifact
participant == rebels ;; place == Syria ;; trigger == ceasefire ;; government.agreements
beneficiary == black tenants ;; giver == real - estate company ;; recipient == properties ;; trigger == rent ;; transaction.transferownership
artifact == five minority teens ;; trigger == imprisoned ;; movement.transportartifact
communicator == lead editorial writers ;; place == The New York Times and The Washington Post ;; recipient == the Iraq War ;; trigger == advocated ;; contact.requestadvise
observedentity == the Millennium ;; place == the Millennium ;; spy == it ;; trigger == surveillance ;; government.spy
crime == Rich 's death ;; investigator == investigators ;; trigger == investigation ;; justice.investigate
participant == Tayyip Erdogan ^^ Vladimir Putin ;; place == Istanbul , Turkey ;; trigger == meeting ;; contact.discussion
injurer == Israeli fighter jet ;; place == Israel ;; victim == It ;; trigger == shot ;; life.injure
communicator == Trump ;; recipient == a U.S. adversary ;; trigger == urge ;; contact.commandorder
participant == U.S. ^^ Russians ;; trigger == ceasefire ;; government.agreements
otherparticipant == the Assad regime ;; place == Saraqeb ;; rejecternullifier == The SNC ;; trigger == agreements ;; government.agreements
beneficiary == charity ;; giver == he ;; money == $ 10,000 ;; place == Mar - a - Lago ;; trigger == auction ;; transaction.transfermoney
communicator == photographer ;; place == Atlantic City ;; recipient == him ;; trigger == blackmail ;; contact.threatencoerce
communicator == Trump ;; recipient == Hillary created the birther movement ;; trigger == lied ;; contact.prevarication
injurer == strikes ;; place == Aleppo ;; victim == people ;; trigger == wounded ;; life.injure
artifact == house ;; place == Miami - Dade County , Florida ;; trigger == demolished ;; artifactexistence.damagedestroy
giver == the sultan ;; money == the sultan ;; place == Los Angeles ;; preventer == the Human Rights Campaign ;; recipient == the property ;; trigger == boycotts ;; transaction.transfermoney
participant == Clinton ^^ Trump ;; trigger == debate ;; contact.negotiate
defendant == Mujib ;; judgecourt == United States ;; trigger == executing ;; justice.judicialconsequences
killer == Anderson ;; victim == Assange ;; trigger == assassinating ;; life.die
inspectedentity == it ;; inspector == I ;; place == pageant ;; trigger == inspecting ;; inspection.sensoryobserve
communicator == Egypt ;; recipient == abolishing ” daylight savings time ;; trigger == announced ;; contact.publicstatementinperson
participant == the Ruskies ^^ the Baltic States , Ukraine , and God knows what else ;; trigger == take over ;; government.formation
artifact == crude ;; destination == Europe ;; origin == Doha ;; transporter == country ;; trigger == shipment ;; movement.transportartifact
beneficiary == United States ;; observedentity == operations around the world ;; place == United States ;; spy == NSA ;; trigger == surveillance ;; government.spy
participant == adults ^^ survey ;; place == all 50 states and the District of Columbia ;; trigger == telephone ;; contact.collaborate
artifact == oil refiner ;; beneficiary == Russia ;; giver == Russia ;; place == India ;; recipient == India ;; trigger == purchase ;; transaction.transferownership
destination == America ;; origin == overseas ;; retreater == Americans ;; trigger == withdraw ;; conflict.yield
artifact == material ;; destination == North Korea ;; transporter == activists ;; vehicle == drones ;; trigger == smuggled ;; movement.transportartifact
artifact == equipment ;; destination == the military training area ;; origin == Black Sea ;; trigger == transported ;; movement.transportartifact
destination == such countries ;; origin == United States ;; passenger == US arms and arms components ;; transporter == networks ;; trigger == smuggling ;; movement.transportperson
crime == attempted overthrow of the government ;; detainee == plotters ;; place == Turkey ;; trigger == detained ;; justice.arrestjaildetain
artifact == TV ad ;; trigger == buy ;; transaction.transferownership
participant == Trump ^^ Japanese bankers ;; trigger == negotiated ;; contact.negotiate
communicator == Trump ;; place == the media ;; trigger == misleading ;; contact.prevarication
beneficiary == campaigns ;; giver == public ;; trigger == funding ;; transaction.transferownership
giver == he ;; place == New York ;; recipient == the money ;; trigger == purchasing ;; transaction.transfermoney
participant == FBI ^^ Boston Marathon bomber Tamerlan Tsarnaev ;; trigger == interviewed ;; contact.discussion
participant == United States ^^ Riyadh ;; trigger == discussing ;; contact.discussion
instrument == " Little Boy " ;; place == Hiroshima ;; target == Hiroshima ;; trigger == bomb ;; conflict.attack
place == Moldova ’s capital ;; recipient == Monday ’s V - Day march ;; yielder == Chisinau ;; trigger == withdraw ;; conflict.yield
communicator == Trump ;; trigger == vow ;; contact.commitmentpromiseexpressintent
employee == the big banks ;; place == America ;; trigger == sack ;; personnel.endposition
candidate == politicians ;; place == the country ;; trigger == elected ;; personnel.elect
injurer == illnesses ;; place == United States ;; trigger == illnesses ;; life.injure
communicator == Clinton ’s top aides ;; trigger == oath ;; contact.commitmentpromiseexpressintent
place == Jarablus ;; victim == leader ;; trigger == assassinated ;; life.die
giver == U.S. ;; place == Russia ;; preventer == U.S. ;; recipient == Stroygazmontazh ;; trigger == sanctions ;; transaction.transferownership
artifact == American forces ;; destination == into another large and costly war in the Middle East ;; trigger == plunge ;; movement.transportartifact
attacker == rebels ;; place == Libya ;; target == blacks ;; trigger == massacring ;; conflict.attack
attacker == Russians ;; instrument == poisoning ;; place == Millennium hotel ;; target == victim ;; trigger == poisoning ;; conflict.attack
giver == FBI ;; place == a New Jersey data center ;; recipient == it ;; trigger == seized ;; transaction.transaction
hidingplace == USB drives ;; passenger == Western movies ;; transporter == guy ;; vehicle == drones ;; trigger == smuggled ;; movement.transportperson
communicator == Trump ;; trigger == lied ;; contact.prevarication
victim == he ;; trigger == surgery ;; life.injure
participant == Clinton ;; trigger == emails ;; contact.collaborate
artifact == arms and arms components ;; origin == US ;; transporter == networks ;; trigger == smuggling ;; movement.transportartifact
communicator == husband ;; trigger == oath ;; contact.commitmentpromiseexpressintent
communicator == Dennis Cheng ;; recipient == Podesta call certain donors to Priorities USA Action ;; trigger == recommend ;; contact.requestadvise
artifact == it ;; recipient == he ;; trigger == obtained ;; transaction.transferownership
investigator == FBI ;; trigger == investigate ;; justice.investigate
fireexplosionobject == aircraft ;; instrument == crash ;; place == Rostov - on - Don Airport ;; trigger == blaze ;; disaster.fireexplosion
killer == Assad ;; place == Aleppo ;; trigger == massacre ;; life.die
attacker == U.S. ;; place == Syria or Iran ;; target == Syria or Iran ;; trigger == strike ;; conflict.attack
participant == Researchers ;; place == Europe ;; trigger == burials ;; contact.funeralvigil
judgecourt == government ;; place == Saudi Arabia ;; trigger == executions ;; justice.judicialconsequences
defendant == him ;; judgecourt == Mr. Trump ;; trigger == execute ;; justice.judicialconsequences
rejecternullifier == the Pentagon ;; trigger == ceasefire ;; government.agreements
artifact == men ;; destination == Syria ;; origin == Russia ;; transporter == Wagner ;; vehicle == planes ;; trigger == transport ;; movement.transportartifact
communicator == U.S. -led coalition ;; place == Syria ;; recipient == others ;; trigger == intimidate ;; contact.threatencoerce
defendant == Bukharin ;; executioner == Soviet ;; trigger == execution ;; justice.judicialconsequences
crime == raping ;; defendant == five black and Hispanic teens ;; place == Central Park ;; trigger == death penalty ;; justice.judicialconsequences
communicator == Panama 's government ;; recipient == vigorously cooperate ;; trigger == vowed ;; contact.commitmentpromiseexpressintent
injurer == terrorist ;; place == Pulse nightclub ;; victim == people ;; trigger == injured ;; life.injure
employee == Heede ;; place == Aspen , Colorado ;; placeofemployment == They ;; trigger == hired ;; personnel.startposition
crime == accepted bribes ;; defendant == Denis Nikandrov and Alexander Lamonov ;; judgecourt == court ;; place == Moscow ;; prosecutor == court ;; trigger == charges ;; justice.initiatejudicialprocess
participant == Trump ^^ CNBC ;; trigger == interview ;; contact.discussion
communicator == opposition ;; recipient == We ;; trigger == blackmailing ;; contact.threatencoerce
victim == he ;; trigger == passed away ;; life.die
giver == agents ;; recipient == a Wi - Fi router ;; trigger == confiscated ;; transaction.transaction
participant == officials ^^ officials ;; place == Moscow ;; trigger == met ;; contact.discussion
communicator == columnist ;; recipient == unity day ;; trigger == proposed ;; contact.publicstatementinperson
governmentbody == President Andrzej Duda ;; law == controversial move bringing the attorney general under the control of the justice ministry ;; trigger == signed into law ;; government.legislate
giver == military - industrial complex ;; recipient == four - star general ;; trigger == compensated ;; transaction.transfermoney
participant == Trump ^^ USSR ;; trigger == agreements ;; government.agreements
giver == servicers ;; recipient == borrowers ;; trigger == loan ;; transaction.transfermoney
artifact == government ;; attacker == faction of the military ;; place == Turkey ;; target == warplanes , helicopters and tanks ;; trigger == commandeered ;; conflict.attack
killer == Washington ;; place == not only in Cuba , but in Panama , Dominican Republic and Venezuela ;; victim == Cuban president Fidel Castro ;; trigger == assassinate ;; life.die
victim == daughter ;; trigger == deceased ;; life.die
attacker == Pope Francis ;; instrument == warning ;; place == Syria ;; target == the assault on civilians ;; trigger == assailed ;; conflict.attack
communicator == Justice Minister Bekir Bozdag ;; recipient == anti - American feeling in Turkey ;; trigger == broadcast ;; contact.mediastatement
attacker == Washington - backed rebels ;; place == Libya ;; target == blacks ;; trigger == massacring ;; conflict.attack
injurer == North Vietnamese ;; victim == he ;; trigger == torture ;; life.injure
employee == Kasyanov ;; place == Russia ;; placeofemployment == prime minister ;; trigger == ousted ;; personnel.endposition
placeofemployment == team ;; trigger == hiring ;; personnel.startposition
communicator == Obama Administration officials ;; recipient == starting a war with Russia ;; trigger == advocating ;; contact.requestadvise
artifactmoney == Beverly Hills and Bel - Air hotels ;; place == Los Angeles ;; preventer == Human Rights Campaign ;; trigger == boycotts ;; transaction.transaction
participant == Putin ^^ territories ;; place == territories ;; trigger == annex ;; government.formation
employee == he ;; placeofemployment == the ticket ;; trigger == resign ;; personnel.endposition
candidate == Gennady Zyuganov ;; trigger == elected ;; personnel.elect
victim == patients ;; trigger == dehydrated ;; life.injure
crime == trying to conceal the potentially embarrassing connections ;; defendant == Clinton ;; prosecutor == Republicans ;; trigger == accused ;; justice.initiatejudicialprocess
attacker == Russia ;; instrument == air strikes ;; place == Syria ;; target == Syria ;; trigger == airstrikes ;; conflict.attack
communicator == Trump ;; trigger == deceiving ;; contact.prevarication
transporter == John McCain ;; trigger == captured ;; movement.transportperson
participant == China ^^ large swaths of Russian wilderness ;; trigger == annex ;; government.formation
attacker == Russian President Vladimir Putin ;; instrument == poisoning ;; place == London ;; target == former KGB operative ;; trigger == poisoning ;; conflict.attack
communicator == Air Force General Philip Breedlove ;; place == America ;; recipient == America ;; trigger == misleads ;; contact.prevarication
artifact == access to the powerful family and their associates in government ;; beneficiary == themselves ;; trigger == auctioning ;; transaction.transferownership
beneficiary == Democrats ;; participant == Three members of Mueller 's team ^^ Democrats ;; trigger == donated ;; transaction.transaction
observedentity == damage ;; observer == People ;; place == Aleppo ;; trigger == inspect ;; inspection.sensoryobserve
communicator == major third - party candidates ;; place == Richmond , Virginia ;; trigger == broadcast ;; contact.mediastatement
employee == President Bashar al - Assad ;; place == Syria ;; placeofemployment == President ;; trigger == oust ;; personnel.endposition
giver == the Times ;; recipient == its front page ;; trigger == lent ;; transaction.transferownership
destination == Western countries ;; passenger == terrorists ;; transporter == Islamic State ;; trigger == smuggling ;; movement.transportperson
communicator == he ;; recipient == thousands and thousands of ads ;; trigger == suggested ;; contact.requestadvise
participant == Kerry ^^ Russian Foreign Minister Sergey Lavrov ;; place == Geneva , Switzerland ;; trigger == ceasefire ;; government.agreements
place == Paris ;; trigger == negotiated ;; contact.discussion
trigger == roamed ;; movement.transportperson
artifact == crude shipment ;; destination == Europe ;; origin == Iran ;; transporter == Iran ;; trigger == shipment ;; movement.transportartifact
artifact == high - value targets ;; preventer == we ;; trigger == detaining ;; movement.transportartifact
artifact == Buk 9M38 missile ;; destination == an area of eastern Ukraine controlled by Moscow - backed rebels ;; origin == Russia ;; transporter == close to 100 people ;; trigger == transport ;; movement.transportartifact
governmentbody == her ;; law == sanctions ;; trigger == enact ;; government.legislate
beneficiary == contractors ;; giver == billionaire businessman ;; recipient == contractors ;; trigger == paid ;; transaction.transfermoney
injurer == the three ;; victim == civilians ;; trigger == torture ;; life.injure
artifact == buildings ;; instrument == air strike ;; place == Bustan al - Qasr ;; trigger == destroyed ;; artifactexistence.damagedestroy
destination == North Korea ;; origin == Russia ;; transporter == Russia ;; trigger == shipment ;; movement.transportperson
artifact == migrants ;; destination == Europe ;; origin == Turkey ;; transporter == networks ;; trigger == smuggling ;; movement.transportartifact
detainee == Zakharchenko ;; trigger == arrest ;; justice.arrestjaildetain
governmentbody == President Vladimir Putin ;; law == a bill ;; place == the country ;; trigger == signed into law ;; government.legislate
crime == unsecure emails ;; defendant == her ;; trigger == indicted ;; justice.initiatejudicialprocess
artifact == opposition ;; place == Russia ;; trigger == battered ;; artifactexistence.damagedestroy
defendant == Edward Snowden ;; place == the US ;; trigger == trial ;; justice.initiatejudicialprocess
recipient == confrontations ;; trigger == retreat ;; conflict.yield
beneficiary == Islamabad ;; giver == Islamabad ;; trigger == buy ;; transaction.transfermoney
artifact == oil ;; origin == Iran ;; transporter == Iran ;; trigger == exports ;; movement.transportartifact
communicator == Kiselyov ;; recipient == viewers ;; trigger == misleading ;; contact.prevarication
place == the square ;; target == Issa ;; trigger == hanged ;; conflict.attack
communicator == the message ;; place == Swedish media outlet ;; recipient == public ;; trigger == deceive ;; contact.prevarication
destination == Syrian ;; trigger == smuggling ;; movement.transportartifact
trigger == immigration ;; inspection.sensoryobserve
artifact == North Korea ;; place == North Korea ;; preventer == President Barack Obama ;; trigger == sanctions ;; transaction.transferownership
killer == father ;; victim == JFK ;; trigger == assassinate ;; life.die
victim == survivors ;; trigger == Holocaust ;; life.die
attacker == a US - based cleric ;; instrument == violence ;; place == US ;; target == the Erdogan administration ;; trigger == coup ;; conflict.attack
attacker == Palestinian teen ;; place == Kiryat Arba settlement ;; target == Hallel Yafa Ariel ;; trigger == stabbing ;; conflict.attack
participant == Senators ^^ Senators ;; place == Senate ;; trigger == debate ;; contact.discussion
communicator == Edward Snowden ;; place == Moscow ;; trigger == broadcast ;; contact.mediastatement
giver == buyers from outside Brazil ;; place == authorized ticket resale site CoSport ;; recipient == the same reduced prices as citizens ;; trigger == purchase ;; transaction.transfermoney
injurer == it ;; place == prison camps ;; victim == people ;; trigger == brutalized ;; life.injure
communicator == he ;; recipient == produce damaging information about LBJ ;; trigger == promised ;; contact.commitmentpromiseexpressintent
founder == he ;; gpe == relationship ;; place == RT gala ;; trigger == inaugurate ;; government.formation
destination == US ;; origin == Iran ;; passenger == Americans ;; preventer == Iran ;; trigger == detained ;; movement.transportperson
place == United States ;; trigger == homicides ;; life.die
communicator == she ;; trigger == lied ;; contact.prevarication
spy == agency ;; trigger == spy ;; government.spy
candidate == first woman to ever hold the presidency ;; voter == I ;; trigger == vote ;; government.vote
beneficiary == DNC ;; giver == Shah ;; place == events ;; recipient == DNC ;; trigger == donating ;; transaction.transferownership
communicator == political and media elites ;; recipient == candidates ;; trigger == demand ;; contact.commandorder
employee == Roffman ;; trigger == firing ;; personnel.endposition
communicator == Milo Yiannopoulos ;; trigger == preaching ;; contact.mediastatement
passenger == anyone ;; preventer == authorities ;; trigger == incarcerate ;; movement.transportperson
inspectedentity == he and his parents ;; trigger == immigrated ;; inspection.sensoryobserve
beneficiary == Vietnam ;; participant == United States ^^ Vietnam ;; trigger == embargo ;; transaction.transaction
attacker == Turkey ’s air force ;; instrument == bombing ;; place == northern Syria ;; target == Kurdish militias ;; trigger == bombing ;; conflict.attack
artifact == city ;; damager == Russian and Syrian government ;; place == city ;; trigger == battered ;; artifactexistence.damagedestroy
giver == Dimon ;; money == millions ;; trigger == buy ;; transaction.transfermoney
communicator == Clinton ;; trigger == oath ;; contact.commitmentpromiseexpressintent
crime == money laundering , fraud and extortion ;; defendant == Pavlo Lazarenko ;; place == Ukraine ;; trigger == convicted ;; justice.judicialconsequences
communicator == FBI Director James B. Comey ;; recipient == Clinton and her staff ;; trigger == recommending ;; contact.requestadvise
artifact == the fabric of the proper function of responsible government ;; damager == WikiLeaks and its news media partners ;; instrument == disclosures like these ;; trigger == tear ;; artifactexistence.damagedestroy
communicator == Trump ;; trigger == promised ;; contact.commitmentpromiseexpressintent
beneficiary == Kurdish ;; giver == The Turkish army ;; recipient == Erdogan ;; trigger == seizing ;; transaction.transaction
place == Iran ;; preventer == U.S. ;; trigger == sanctions ;; transaction.transfermoney
participant == ICANN ;; place == Marrakesh ;; trigger == meeting ;; contact.discussion
communicator == Melania ;; place == GOP convention in Cleveland ;; trigger == reiterating ;; contact.publicstatementinperson
communicator == Richard Burt ;; recipient == the Trump campaign ;; trigger == advising ;; contact.requestadvise
participant == DNC Chairwoman Debbie Wasserman Schultz and other party officials ^^ DNC Chairwoman Debbie Wasserman Schultz and other party officials ;; trigger == emails ;; contact.collaborate
observedentity == missiles ;; observer == it ;; trigger == inspect ;; inspection.sensoryobserve
target == he ;; trigger == poisoned ;; conflict.attack
communicator == America ;; place == America ;; recipient == Russia ;; trigger == blackmail ;; contact.threatencoerce
artifact == arms ;; destination == Pakistan ;; origin == US ;; transporter == US ;; trigger == shipments ;; movement.transportartifact
artifactmoney == oil ;; place == Iran ;; recipient == Iran ;; trigger == sanctions ;; transaction.transaction
place == al - Bagilya ;; trigger == massacre ;; life.die
artifact == bitcoin ;; giver == Arias ;; trigger == buy ;; transaction.transferownership
driverpassenger == chauffeur ;; place == Moscow ;; vehicle == presidential car ;; trigger == crash ;; disaster.accidentcrash
beneficiary == the group ;; giver == they ;; money == half a billion dollars ;; recipient == half a billion dollars ;; trigger == auction ;; transaction.transfermoney
communicator == he ;; place == St. Louis ;; trigger == suggest ;; contact.requestadvise
beneficiary == Tim Kaine , and his wife , Anne Holton ;; recipient == $ 300,000 ;; trigger == income ;; transaction.transfermoney
beneficiary == soldiers ;; giver == Russian military ;; recipient == soldiers ;; trigger == pay ;; transaction.transfermoney
artifact == head ;; preventer == a security guard and two policeman ;; transporter == her ;; trigger == detain ;; movement.transportartifact
communicator == OROP ;; recipient == Prime Minister ;; trigger == demand ;; contact.commandorder
artifact == oil ;; origin == ISIS - held territory in Syria and Iraq ;; transporter == he and his family ;; trigger == smuggling ;; movement.transportartifact
communicator == Canada ;; recipient == 10 percent increase in its overall humanitarian assistance ;; trigger == pledged ;; contact.commitmentpromiseexpressintent
communicator == Trump ;; trigger == lied ;; contact.prevarication
governmentbody == Congress ;; law == launch an invasion of Iraq ;; trigger == vote ;; government.legislate
victim == enrollees ;; trigger == sicker ;; life.injure
deceased == baby ;; participant == Jungklaus ;; trigger == burial ;; contact.funeralvigil
beneficiary == minorities ;; giver == Justice Department ;; money == properties ;; place == properties ;; recipient == minorities ;; trigger == rent ;; transaction.transfermoney
beneficiary == I ;; place == U.S. ;; trigger == paid ;; transaction.transfermoney
attacker == rebels ;; instrument == poison gas ;; place == Aleppo ;; target == seven people ;; trigger == poison ;; conflict.attack
employee == Simonyan ;; trigger == recruiting ;; personnel.startposition
origin == Upper Merion neighborhood ;; transporter == three 20-something men ;; trigger == walking ;; movement.transportperson
violator == us ;; trigger == ceasefire ;; government.agreements
crime == shoplifting ;; detainee == man ;; jailer == Gardendale police ;; place == Walmart ;; trigger == arrested ;; justice.arrestjaildetain
employee == agent detail ;; placeofemployment == U.S. Secret Service ;; trigger == dismiss ;; personnel.endposition
passenger == rival of Ukraine 's then - president ;; trigger == imprisoned ;; movement.transportperson
place == Deir el - zour ;; victim == 280 victims ;; trigger == massacre ;; life.die
instrument == death penalty ;; killer == U.S. ;; place == U.S. ;; victim == people ;; trigger == executed ;; life.die
candidate == Democratic presidential hopeful ;; place == this country ;; trigger == elected ;; personnel.elect
beneficiary == he ;; giver == Yanukovich 's Party of Regions ;; money == $ 12 million ;; recipient == he ;; trigger == payments ;; transaction.transfermoney
beneficiary == Russia ;; participant == Russia ;; place == Crimea ;; trigger == land grab ;; transaction.transaction
artifact == American politics ;; damager == Clinton ’s campaign ;; instrument == money - driven secrecy and subversion ;; trigger == wrecking ;; artifactexistence.damagedestroy
destination == Kilis State hospital ;; origin == Azaz ;; passenger == a girl ;; transporter == medical personnel ;; vehicle == ambulances ;; trigger == unloading ;; movement.transportperson
defendant == Assange ;; destination == US ;; extraditer == US ;; origin == Sweden ;; trigger == extradite ;; justice.judicialconsequences
participant == candidates ^^ 80 million people ;; trigger == debate ;; contact.discussion
attacker == United States ;; instrument == hijacking ;; target == TPP process ;; trigger == hijacking ;; conflict.attack
instrument == shooting ;; killer == shooter ;; place == Pulse nightclub ;; victim == 49 people ;; trigger == killed ;; life.die
communicator == America ;; recipient == Russia ;; trigger == blackmail ;; contact.threatencoerce
beneficiary == Haiti and Algeria ;; giver == the Embassy of Algeria ;; place == the Clinton Foundation ;; recipient == an “ unsolicited ” donation ;; trigger == donated ;; transaction.transferownership
attacker == Russia ;; place == Crimea ;; target == Ukraine ;; trigger == invaded ;; conflict.attack
target == all ;; trigger == attack ;; conflict.attack
giver == she ;; place == Toledo , Ohio ;; trigger == rent ;; transaction.transfermoney
beneficiary == Trump ;; giver == Russian and ex - Soviet state billionaires ;; place == New York City ;; recipient == Trump ;; trigger == funded ;; transaction.transfermoney
artifact == Chinese ;; giver == Philippines ;; place == China ;; preventer == Chinese ;; recipient == dried mango ;; trigger == boycotted ;; transaction.transferownership
participant == Clinton ^^ Banco Itau ;; trigger == emails ;; contact.collaborate
attacker == billionaire ;; target == family of a slain U.S. soldier ;; trigger == attacked ;; conflict.attack
trigger == Buy ;; transaction.transfermoney
injurer == Military ;; victim == Victims ;; trigger == Assault ;; life.injure
instrument == a missile ;; place == town of Azaz ;; victim == a children 's hospital ;; trigger == attack ;; life.injure
trigger == imprisoned ;; movement.transportperson
attacker == Saudi ;; place == the capital ;; target == funeral ;; trigger == airstrike ;; conflict.attack
artifact == air defense systems ;; origin == Russian ;; transporter == Syrian government ;; trigger == imports ;; movement.transportartifact
trigger == agreement ;; government.agreements
injurer == Trump ;; place == Cleveland ;; trigger == bruising ;; life.injure
victim == humanity ;; trigger == genocides ;; life.die
target == Rich ;; trigger == robbery ;; conflict.attack
place == his country ;; victim == Russian journalists ;; trigger == murdered ;; life.die
beneficiary == those ;; giver == those ;; recipient == Clinton Foundation ;; trigger == donated ;; transaction.transaction
artifact == tractors and four combines , along with a German machine ;; transporter == His ;; trigger == imported ;; movement.transportartifact
spy == National Security Agency ;; trigger == surveillance ;; government.spy
artifact == The cache ;; instrument == information on Democratic donors , fundraising efforts and donor outreach ;; trigger == composed ;; manufacture.artifact
participant == it ;; trigger == negotiates ;; contact.negotiate
artifact == truth ;; hidingplace == a " dust up " ;; transporter == Clintons ;; trigger == conceal ;; movement.transportartifact
participant == Yildirim ^^ AK Party ;; place == parliament ;; trigger == meeting ;; contact.discussion
trigger == battle ;; conflict.attack
communicator == we ;; recipient == black folks in the South ;; trigger == suggest ;; contact.requestadvise
communicator == it ;; place == Canada ;; trigger == announced ;; contact.publicstatementinperson
artifact == market ;; giver == Iran ;; preventer == European oil embargo ;; recipient == oil ;; trigger == sanctions ;; transaction.transferownership
participant == Washington ^^ us ;; trigger == dialogue ;; contact.discussion
ballot == Democratic primary ;; candidate == he ;; result == he won ;; trigger == voted ;; government.vote
communicator == she ;; place == Libya ;; recipient == military intervention ;; trigger == advocated ;; contact.requestadvise
attacker == corporate ;; instrument == corporate power ;; target == We ;; trigger == coup ;; conflict.attack
origin == Turkey ;; passenger == IS trainer ;; transporter == Free Syrian Army ;; trigger == captured ;; movement.transportperson
communicator == Putin ;; place == Russia ;; trigger == pledge ;; contact.commitmentpromiseexpressintent
beneficiary == the group , its sister organizations and other Koch allies ;; giver == the 501(c)6 nonprofit group ;; money == $ 88 million ;; recipient == $ 88 million ;; trigger == grants ;; transaction.transfermoney
participant == he ^^ Russia Today ;; trigger == interviewed ;; contact.discussion
victim == I ;; trigger == sick ;; life.injure
passenger == Chinese workers ;; trigger == immigrating ;; movement.transportperson
artifact == assets ;; destination == offshore ;; transporter == world leaders ;; trigger == conceal ;; movement.transportartifact
driverpassenger == Israeli man ;; place == the settlement ;; vehicle == car ;; trigger == crash ;; disaster.accidentcrash
attacker == the United States ;; instrument == the military ;; place == Honduras ;; target == the democratically - elected government ;; trigger == coup ;; conflict.attack
beneficiary == foundation ;; participant == Mr Trump ^^ foundation ;; trigger == donated ;; transaction.transaction
killer == Turkish ;; place == Armenian ;; victim == Armenian ;; trigger == Genocide ;; life.die
killer == Iranian officials ;; victim == scientist ;; trigger == executed ;; life.die
place == Prince George ’s County ;; target == ice cream truck ;; trigger == robbery ;; conflict.attack
communicator == enemies ;; recipient == her ;; trigger == blackmail ;; contact.threatencoerce
artifactmoney == China ;; giver == China ;; place == Cam Ranh Bay ;; preventer == U.S. ;; recipient == arms ;; trigger == embargo ;; transaction.transaction
recipient == socialism ;; trigger == professed ;; contact.publicstatementinperson
inspectedentity == wreckage of a Russian helicopter ;; inspector == Men ;; place == north of Syria 's rebel - held Idlib province ;; trigger == inspect ;; inspection.sensoryobserve
defendant == Mr Amiri ;; place == Iran ;; trigger == execution ;; justice.judicialconsequences
trigger == plummeted ;; movement.transportartifact
place == Washington , D.C. ;; victim == Seth Rich ;; trigger == murdered ;; life.die
participant == Polite powers ;; place == Iran ;; trigger == seizing ;; transaction.transaction
granter == a ‘ grassroots ’ movement ;; passenger == support ;; transporter == voters ;; trigger == migrate ;; movement.transportperson
communicator == employee ;; place == Donetsk ;; recipient == Igor Strelkov ;; trigger == recommended ;; contact.requestadvise
passenger == ancestors ;; trigger == migrating ;; movement.transportperson
founder == China ;; gpe == large swaths of Russian wilderness ;; trigger == annex ;; government.formation
participant == SO15 detectives , led by Det Supt Timmons , medical staff , a scientist from the UK ’s atomic weapons establishment , the forensic science service and Dr Nick Gent from Porton Down ^^ SO15 detectives , led by Det Supt Timmons , medical staff , a scientist from the UK ’s atomic weapons establishment , the forensic science service and Dr Nick Gent from Porton Down ;; place == the Met ’s counter - terrorism command ;; trigger == convened ;; contact.discussion
artifact == drug ;; transporter == aggravated felons ;; trigger == trafficking ;; movement.transportartifact
attacker == Russian and Syrian air strikes ;; instrument == bombing raids , alleged gas attacks , and the use of highly destructive barrel bombs ;; place == Mosul ;; target == More than 275,000 civilians in the city ;; trigger == battle ;; conflict.attack
communicator == Ravel ;; place == FEC ;; trigger == advocated ;; contact.requestadvise
beneficiary == Manafort ;; giver == Party of Regions ;; money == 12.7 million ;; recipient == Manafort ;; trigger == payments ;; transaction.transfermoney
injurer == Ivana Trump ;; victim == Donald Trump ;; trigger == rape ;; life.injure
place == Washington ;; recipient == Hillary Clinton ;; trigger == funds ;; transaction.transfermoney
artifact == fast - track programs ;; manufacturer == company ;; trigger == created ;; manufacture.artifact
artifact == oil ;; transporter == Islamic State ( IS , formerly ISIL / ISIS ) terrorists ;; trigger == smuggling ;; movement.transportartifact
participant == rebels ^^ the regime of President Bashar al - Assad ;; place == Syria ;; trigger == ceasefire ;; government.agreements
place == D.C. ;; target == DNC employee ;; trigger == robbery ;; conflict.attack
artifact == equipment ;; destination == military training area ;; vehicle == by rail ;; trigger == transported ;; movement.transportartifact
attacker == Russia ;; place == Aleppo ;; target == Aleppo ;; trigger == bombarding ;; conflict.attack
participant == he ^^ Mercer ;; trigger == conferred ;; contact.discussion
injurer == illness ;; victim == his ;; trigger == illness ;; life.injure
beneficiary == charity ;; giver == Clintons ;; money == $ 1 million ;; recipient == $ 1 million ;; trigger == donated ;; transaction.transfermoney
instrument == a bullet ;; victim == he ;; trigger == shot ;; life.die
participant == Syrian President Bashar al - Assad ^^ rebel ;; place == Aleppo ;; trigger == ceasefire ;; government.agreements
trigger == suggest ;; contact.requestadvise
artifact == the deal ;; transporter == the government and Epstein ;; trigger == concealed ;; movement.transportartifact
employee == Viktor Yanukovych ;; place == Ukrainian ;; placeofemployment == Ukrainian President ;; trigger == ousted ;; personnel.endposition
communicator == Assad government ;; place == Damascus ;; recipient == foreign inspectors and their Syrian counterparts ;; trigger == misleading ;; contact.prevarication
instrument == public execution ;; killer == the government of the Islamic Republic of Iran ;; place == Iran ;; victim == 2500 people ;; trigger == executed ;; life.die
place == Aleppo ;; victim == hundreds of thousands ;; trigger == starvation ;; life.injure
participant == public ^^ public ;; trigger == debate ;; contact.negotiate
artifact == wall ;; manufacturer == rulers of the city of Ur ;; place == Iraq ;; trigger == constructed ;; manufacture.artifact
inspectedentity == property ;; place == the capital ;; trigger == inspections ;; inspection.sensoryobserve
passenger == five minority teens ;; transporter == he ;; trigger == imprisoned ;; movement.transportperson
artifact == $ 12,000 ;; giver == foundation ;; recipient == charity auction ;; trigger == auction ;; transaction.transferownership
driverpassenger == chauffeur ;; place == Moscow ;; vehicle == BMW ;; trigger == collision ;; disaster.accidentcrash
communicator == That ;; trigger == misleading ;; contact.prevarication
observedentity == honor guards ;; observer == U.S. Vice President Joe Biden ( L ) and Serbia 's Prime Minister Aleksandar Vucic ;; place == Serbia ;; trigger == inspect ;; inspection.sensoryobserve
victim == 100,000 people ;; trigger == perished ;; life.die
giver == bloc members ;; territoryorfacility == Moscow ;; trigger == land grabs ;; transaction.transaction
origin == Syrian Arab Red Crescent warehouse ;; transporter == convoy ;; vehicle == lorries ;; trigger == unloading ;; movement.transportperson
communicator == Bernie Sanders ;; recipient == Democratic socialist ;; trigger == proclaimed ;; contact.publicstatementinperson
founder == President Barack Obama ;; gpe == terrorist group ;; trigger == establishing ;; government.formation
communicator == CIA ;; recipient == Candidates ;; trigger == advised ;; contact.requestadvise
destination == Saudi Arabia ;; passenger == arms ;; transporter == the global arms trade ;; trigger == trafficking ;; movement.transportperson
detainee == he ;; jailer == North Vietnam ;; place == North Vietnam ;; trigger == captured ;; justice.arrestjaildetain
victim == daughter ;; trigger == deceased ;; life.die
injurer == FGM ;; trigger == mutilating ;; life.injure
destination == the stage ;; origin == the stage ;; transporter == her rival ;; trigger == roamed ;; movement.transportperson
killer == he ;; trigger == genocide ;; life.die
crashobject == Mercedes ;; driverpassenger == chauffeur ;; place == Kutuzovsky Avenue ;; vehicle == presidential BMW ;; trigger == collide ;; disaster.accidentcrash
defendant == candidates ;; prosecutor == Comey ;; trigger == charges ;; justice.initiatejudicialprocess
communicator == Weiner ;; trigger == lies ;; contact.prevarication
attacker == Russia ;; instrument == cluster munitions ;; place == At - Tanf ;; target == the base ;; trigger == strike ;; conflict.attack
attacker == Dahir Adan ;; place == Minnesota mall ;; target == Nine people ;; trigger == stabbings ;; conflict.attack
communicator == giant corporations ;; recipient == Healy ;; trigger == intimidate ;; contact.threatencoerce
crime == coup attempt ;; detainee == 60,000 people ;; jailer == Turkey ;; place == Turkey ;; trigger == detained ;; justice.arrestjaildetain
governmentbody == Clinton ;; law == new sanctions on the regime ;; trigger == enact ;; government.legislate
artifact == arms ;; trigger == embargo ;; transaction.transferownership
artifact == sample documents ;; beneficiary == the group ;; giver == the group ;; recipient == they ;; trigger == auction ;; transaction.transferownership
artifact == energy ;; transporter == Russia ;; trigger == exports ;; movement.transportartifact
crime == drugs ;; detainee == drug users ;; trigger == incarcerated ;; justice.arrestjaildetain
communicator == She ;; recipient == millionaires and billionaires ;; trigger == proposed ;; contact.publicstatementinperson
artifact == something ;; transporter == people ;; vehicle == bitcoin ;; trigger == conceal ;; movement.transportartifact
passenger == his son ;; preventer == security forces ;; trigger == detain ;; movement.transportperson
injurer == Donald Trump ;; victim == Ivana Trump ;; trigger == rape ;; life.injure
employee == workers ;; place == state ;; placeofemployment == state ;; trigger == quit ;; personnel.endposition
communicator == Commissioner Moskalkova ;; recipient == Russia ;; trigger == blackmail ;; contact.threatencoerce
giver == Republican lawmakers ;; money == $ 15 per hour ;; place == the U.S. ;; trigger == wage ;; transaction.transfermoney
place == North Korea ;; victim == his own people ;; trigger == starving ;; life.injure
communicator == Kissinger ;; recipient == Nixon ;; trigger == recommended ;; contact.requestadvise
communicator == Netanyahu ;; recipient == Russian - backed peace efforts in Syria ;; trigger == address ;; contact.mediastatement
killer == the CIA ;; victim == Fidel Castro ;; trigger == assassinate ;; life.die
destination == Kilis State hospital ;; origin == Kaljibrin ;; passenger == Syrian woman ;; transporter == medical personnel ;; vehicle == ambulance ;; trigger == unloaded ;; movement.transportperson
beneficiary == Islamic ;; spy == Huma ;; trigger == spy ;; government.spy
detainee == student ;; trigger == detained ;; justice.arrestjaildetain
injurer == United States ;; place == Cuba ;; victim == people ;; trigger == injured ;; life.injure
communicator == FBI ;; place == news conference ;; recipient == not to recommend charges ;; trigger == recommend ;; contact.requestadvise
artifact == human body parts ;; trigger == trafficking ;; movement.transportartifact
artifact == malware ;; giver == The Shadow Brokers ;; recipient == highest bidder ;; trigger == auctioning ;; transaction.transferownership
communicator == FBI Director James Comey ;; recipient == Clinton ;; trigger == announced ;; contact.publicstatementinperson
victim == he ;; trigger == tortured ;; life.injure
place == North Korea ;; preventer == U.N. ;; recipient == Pyongyang ;; trigger == sanctions ;; transaction.transaction
communicator == the three girls ;; place == the Muslim community in east London ;; recipient == their families ;; trigger == deceiving ;; contact.prevarication
victim == leader ;; trigger == assassinated ;; life.die
beneficiary == He ;; place == Colorado ;; trigger == borrowed ;; transaction.transfermoney
participant == post - debate survey ^^ respondents ;; trigger == telephone ;; contact.collaborate
place == Madaya ;; trigger == starvation ;; life.injure
instrument == rope ;; target == he ;; trigger == hanged ;; conflict.attack
crime == hacking Clinton ’s server ;; defendant == Marcel Lehal Lazar ;; trigger == guilty ;; justice.judicialconsequences
artifact == oil ;; origin == Syria and Iraq ;; transporter == Bilal ;; trigger == smuggling ;; movement.transportartifact
inspectedentity == buildings ;; inspector == Rescue crews ;; trigger == inspecting ;; inspection.sensoryobserve
artifact == expectations ;; manufacturer == poll ;; place == Michigan ;; trigger == creating ;; manufacture.artifact
participant == I ^^ Apache - Stronghold ;; place == Flagstaff ;; trigger == met ;; contact.discussion
communicator == defense and security experts ;; recipient == Obama administration ;; trigger == urging ;; contact.commandorder
participant == Poll ^^ adults 18 years of age or older ;; place == all 50 states and the District of Columbia ;; trigger == telephone ;; contact.collaborate
crime == coup plot ;; defendant == he ;; destination == Turkey ;; extraditer == U.S. ;; origin == U.S. ;; trigger == extradited ;; justice.judicialconsequences
communicator == David Cameron ;; place == UK ;; recipient == Europe ;; trigger == blackmail ;; contact.threatencoerce
trigger == immigration ;; inspection.sensoryobserve
communicator == Trump ;; recipient == Wagner ;; trigger == pledged ;; contact.commitmentpromiseexpressintent
crime == this atrocity ;; defendant == those responsible ;; destination == Australia ;; extraditer == they ;; trigger == extradition ;; justice.judicialconsequences
giver == U.S. ;; trigger == wages ;; transaction.transfermoney
instrument == poisoning ;; killer == Russian President Vladimir Putin ;; victim == former KGB operative ;; trigger == fatal ;; life.die
artifact == Iran ;; place == Iran ;; preventer == European ;; recipient == oil ;; trigger == embargo ;; transaction.transferownership
place == federal and state prisons ;; trigger == death penalty ;; justice.judicialconsequences
detainee == prisoners of war ;; trigger == detained ;; justice.arrestjaildetain
governmentbody == Clinton ;; law == real change ;; trigger == enact ;; government.legislate
communicator == national security establishment ;; recipient == the American people ;; trigger == urging ;; contact.requestadvise
communicator == He ;; recipient == her ;; trigger == urged ;; contact.commandorder
employee == Viktor Yanukovych ;; trigger == ousted ;; personnel.endposition
communicator == John Large , a British independent nuclear consultant ;; recipient == the Russian government ;; trigger == advised ;; contact.requestadvise
artifact == the fabric of democracy ;; damagerdestroyer == ACORN ;; trigger == destroying ;; artifactexistence.damagedestroy
injurer == man ;; instrument == violence ;; place == Trump rally ;; victim == protester ;; trigger == punched ;; life.injure
participant == West Bank ^^ Jewish ;; trigger == annex ;; government.formation
beneficiary == combat ship ;; giver == Trump ;; trigger == funding ;; transaction.transaction
artifact == arms ;; destination == Pakistan ;; origin == US ;; transporter == US ;; trigger == shipments ;; movement.transportartifact
participant == Russians ^^ opposition ;; place == Syria ;; trigger == ceasefire ;; government.agreements
place == Rostov - on - Don ;; vehicle == Boeing ;; trigger == crashed ;; disaster.accidentcrash
destination == Russian farms ;; granter == Putin ;; origin == across the border ;; transporter == Chinese workers ;; trigger == immigrating ;; movement.transportperson
origin == eastern Europe ;; retreater == Anyone who is that reckless ;; trigger == retreat ;; conflict.yield
deceased == the baby ;; trigger == burial ;; contact.funeralvigil
killer == an off - duty police officer ;; place == Minnesota ;; victim == Adan ;; trigger == killed ;; life.die
destination == Thailand ;; origin == China ;; passenger == Chinese dissidents ;; transporter == Chinese dissidents ;; trigger == flee ;; movement.transportperson
victim == American soldier ;; trigger == slain ;; life.die
artifact == goods ;; destination == America ;; origin == China ;; transporter == Trump ;; trigger == imports ;; movement.transportartifact
artifact == mosquito ;; destroyer == Cuba ;; instrument == pesticide ;; place == Cuba ;; trigger == eradicate ;; artifactexistence.damagedestroy
artifact == vestigial moderate wing ;; damagerdestroyer == right - wing primary challenges ;; trigger == eradicated ;; artifactexistence.damagedestroy
beneficiary == Yanukovych 's pro - Russia political party ;; giver == Ukrainian businessman ;; recipient == Yanukovych 's pro - Russia political party ;; trigger == donated ;; transaction.transferownership
participant == Jill Stein ^^ major - party candidates ;; trigger == debate ;; contact.discussion
crime == marijuana ;; detainee == blacks ;; jailer == criminal justice system ;; place == America ;; trigger == incarcerated ;; justice.arrestjaildetain
beneficiary == Iran ;; giver == Iran ;; place == Iran ;; recipient == centrifuges ;; trigger == acquire ;; transaction.transferownership
artifact == wheat and corn ;; destination == Soviet Union ;; origin == U.S. ;; transporter == U.S. ;; trigger == shipments ;; movement.transportartifact
artifact == shipments ;; origin == Iran ;; transporter == Essar Oil ;; trigger == shipments ;; movement.transportartifact
passenger == Clinton ;; preventer == he ;; trigger == incarcerate ;; movement.transportperson
employee == Over 80,000 people ;; place == Europe ;; placeofemployment == soldiers , judges , teachers , policemen , businessmen and even football officials ;; trigger == sacked ;; personnel.endposition
communicator == she ;; trigger == lies ;; contact.prevarication
victim == man ;; trigger == slays ;; life.die
victim == President Kennedy and Martin Luther King , Jr ;; trigger == assassinated ;; life.die
participant == Clinton ^^ Israel and Hamas ;; trigger == negotiated ;; contact.negotiate
crime == a threat in cyberspace ;; defendant == those who pose a threat in cyberspace ;; investigator == FBI ;; place == cyberspace ;; trigger == investigate ;; justice.investigate
observedentity == damage ;; observer == he ;; place == Ankara ;; trigger == inspected ;; inspection.sensoryobserve
destination == Florida ;; preventer == The U.S. Attorney for the Southern District of Florida ;; transporter == Epstein ;; trigger == arrest ;; movement.transportperson
executioner == group ;; trigger == executions ;; justice.judicialconsequences
employee == She ;; trigger == quit ;; personnel.endposition
injurer == terrorist ;; place == Orlando ;; trigger == shooting ;; life.injure
artifact == arms and arms components ;; destination == third parties like Iran or China ;; origin == US ;; transporter == networks ;; vehicle == front companies in such countries ;; trigger == smuggling ;; movement.transportartifact
place == London ;; trigger == protest ;; conflict.demonstrate
artifact == large - scale barrier wall ;; manufacturer == rulers ;; place == Ur ;; trigger == constructed ;; manufacture.artifact
attacker == Donald Trump and Russian President Vladimir Putin ;; instrument == poisoned ;; target == Hillary Clinton ;; trigger == poisoned ;; conflict.attack
artifact == malware ;; giver == The Shadow Brokers ;; recipient == the highest bidder ;; trigger == auctioning ;; transaction.transferownership
destination == Turkey ;; origin == Anbar ;; trigger == fled ;; movement.transportperson
trigger == immigration ;; inspection.sensoryobserve
attacker == Pope Francis ;; instrument == assault ;; place == Syria ;; target == civilians ;; trigger == assailed ;; conflict.attack
origin == rooftop ;; passenger == Russian daredevil ;; preventer == police ;; trigger == detained ;; movement.transportperson
participant == she ^^ Anderson Cooper ;; trigger == discuss ;; contact.discussion
artifact == city ;; place == Aleppo ;; trigger == battered ;; artifactexistence.damagedestroy
founder == Israel ;; gpe == West Bank ;; trigger == annex ;; government.formation
participant == participants in focus groups ^^ pollsters ;; place == Washington ;; trigger == convened ;; contact.discussion
place == Homs , Hama , Lataki and Damascus provinces ;; trigger == ceasefire ;; government.agreements
participant == he ^^ Mercer ;; place == that event ;; trigger == conferred ;; contact.discussion
communicator == Milo Yiannopoulos ;; recipient == P.C. ;; trigger == preaching ;; contact.mediastatement
employee == Kamayev ;; place == Russia ;; placeofemployment == Russia 's Anti - Doping Agency ( RUSADA ;; trigger == quit ;; personnel.endposition
destination == hospital ;; origin == remote area ;; passenger == 90 people ;; trigger == evacuated ;; movement.transportperson
passenger == Campbell ;; trigger == stumbling ;; movement.transportperson
victim == she ;; trigger == illness ;; life.injure
destination == Europe ;; origin == Iran ;; passenger == crude ;; transporter == the country ;; trigger == shipment ;; movement.transportperson
artifact == middle class ;; manufacturer == I ;; place == America ;; trigger == building ;; manufacture.artifact
attacker == Republicans in Congress ;; place == Congress ;; target == administration ;; trigger == blasted ;; conflict.attack
beneficiary == Russia ;; giver == Hillary Clinton ;; recipient == Uranium One ;; trigger == transfer control ;; transaction.transaction
attacker == Russian ;; instrument == airstrikes ;; place == the air ;; target == the moderate armed opposition ;; trigger == annihilated ;; conflict.attack
deceased == baby ;; place == Gross Fredenwalde ;; trigger == burials ;; contact.funeralvigil
passenger == we ;; trigger == stumble ;; movement.transportperson
giver == Vladimir Putin , Iran ’s Ayatollah Ali Khamenei and Chinese President Xi Jinping ;; trigger == bought ;; transaction.transfermoney
communicator == Trump ;; recipient == The media critics ;; trigger == misled ;; contact.prevarication
inspectedentity == planes ;; inspector == They ;; place == the base ;; trigger == inspections ;; inspection.sensoryobserve
employee == advisers ;; placeofemployment == Trump ;; trigger == hiring ;; personnel.startposition
communicator == He ;; recipient == blacks , gays , women and Hispanics ;; trigger == promises ;; contact.commitmentpromiseexpressintent
defendant == Stalin ;; trigger == trials ;; justice.initiatejudicialprocess
demonstrator == Syrians ;; place == Syria ;; trigger == protest ;; conflict.demonstrate
origin == New Jersey ;; passenger == Rahami ;; trigger == arrested ;; movement.transportperson
injurer == Muslim ;; instrument == axe ;; place == Germany ;; victim == five ;; trigger == attack ;; life.injure
crime == missile attack ;; defendant == those ;; prosecutor == United Nations Security Council ;; trigger == trial ;; justice.initiatejudicialprocess
injurer == He ;; instrument == grabbing ;; victim == her ;; trigger == bruises ;; life.injure
giver == United States Treasury Department ;; recipient == Mr. Ivanov ;; trigger == sanctions ;; transaction.transfermoney
inspectedentity == wreckage ;; inspector == Rebel fighters and civilians ;; place == Idlib province , Syria ;; trigger == inspect ;; inspection.sensoryobserve
giver == United States ;; money == lethal weapons ;; place == Vietnam ;; preventer == United States ;; recipient == Vietnam ;; trigger == embargo ;; transaction.transfermoney
giver == Clintons ;; trigger == auctioning ;; transaction.transfermoney
preventer == United States Treasury Department ;; recipient == Ivanov ;; trigger == sanctions ;; transaction.transferownership
defendant == he ;; trigger == charged ;; justice.initiatejudicialprocess
inspectedentity == Site ;; inspector == experienced investigators ;; place == the crash site ;; trigger == inspection ;; inspection.sensoryobserve
attacker == Trump ;; target == national debt ;; trigger == explode ;; conflict.attack
communicator == Briefings ;; recipient == Candidates ;; trigger == advised ;; contact.requestadvise
crime == the coup ;; defendant == Gulen ;; extraditer == the U.S. ;; origin == Turkey ;; trigger == extradites ;; justice.judicialconsequences
crime == used a private server exclusively ;; defendant == Hillary Clinton ;; investigator == FBI ;; trigger == investigated ;; justice.investigate
artifact == media coverage ;; manufacturer == Republicans ;; trigger == produce ;; manufacture.artifact
participant == Security Secretary Jeh Johnson ^^ state election officials ;; trigger == convene ;; contact.discussion
artifact == weapons ;; origin == US ;; transporter == US ;; trigger == exports ;; movement.transportartifact
origin == Turkey ;; passenger == migrants ;; trigger == smuggling ;; movement.transportperson
killer == Montoya ;; victim == the man who killed his father ;; trigger == slays ;; life.die
crime == extremely careless ;; defendant == Clinton ;; judgecourt == the FBI ;; prosecutor == James Comey ;; trigger == testified ;; justice.initiatejudicialprocess
communicator == the Kochs ;; recipient == people ;; trigger == misleading ;; contact.prevarication
destination == London 's Ecuadorean embassy ;; granter == Ecuadorean embassy ;; passenger == Julian Assange ;; trigger == asylum ;; movement.transportperson
participant == interventionists ;; trigger == discussing ;; contact.discussion
origin == Moscow metro station ;; passenger == nanny ;; transporter == Police ;; trigger == detained ;; movement.transportperson
attacker == Leon Panetta ;; place == Democratic National Convention ;; target == Donald Trump ;; trigger == blasted ;; conflict.attack
beneficiary == Russia ;; participant == Hillary Clinton ^^ Russian interests ;; trigger == transfer control ;; transaction.transaction
artifact == Islamic State ;; damagerdestroyer == US ;; trigger == eradicate ;; artifactexistence.damagedestroy
defendant == Central Park Five ;; judgecourt == he ;; trigger == guilty ;; justice.judicialconsequences
communicator == the message ’s polite tone ;; place == the Turkish embassy ;; recipient == the public ;; trigger == deceive ;; contact.prevarication
artifact == Russia ;; preventer == Senate ;; trigger == sanctions ;; transaction.transferownership
attacker == Jeb ;; target == Trump ;; trigger == assailing ;; conflict.attack
communicator == Perry ;; recipient == United States ;; trigger == advocating ;; contact.requestadvise
artifact == all parties ;; giver == we ;; place == Syria and in the Middle East ;; preventer == we ;; recipient == weapons ;; trigger == embargo ;; transaction.transferownership
artifact == Ryan , as well as senators Mitch McConnell , Marco Rubio , and Ted Cruz ;; damager == presidential candidate ;; instrument == personal humiliation ;; trigger == damage ;; artifactexistence.damagedestroy
beneficiary == charity ;; giver == he ;; money == money ;; recipient == charity ;; trigger == donated ;; transaction.transfermoney
participant == Hillary Clinton ^^ Donald Trump ;; place == Hofstra University ;; trigger == debate ;; contact.discussion
artifact == economic sanctions ;; giver == Barack Obama ;; recipient == North Korea ;; trigger == sanctions ;; transaction.transferownership
place == New York City ;; trigger == homicides ;; life.die
deceased == a young man ;; trigger == burial ;; contact.funeralvigil
trigger == Auction ;; transaction.transfermoney
destination == Ukraine ;; passenger == Ukrainians ;; preventer == Russians ;; trigger == captured ;; movement.transportperson
giver == our ;; trigger == auction ;; transaction.transferownership
transporter == Donald Trump ;; trigger == trafficking ;; movement.transportperson
crime == a crime ;; defendant == Clinton ;; prosecutor == James B. Comey ;; trigger == charged ;; justice.initiatejudicialprocess
detainee == anyone they want ;; jailer == authorities ;; place == totalitarian regimes or Guantanamo ;; trigger == incarcerate ;; justice.arrestjaildetain
passenger == HIV and hepatitis C ;; preventer == the 1980s war on drugs ;; transporter == drug users ;; trigger == incarcerated ;; movement.transportperson
employee == Rep. Tulsi Gabbard ;; placeofemployment == Democratic National Committee ;; trigger == quit ;; personnel.endposition
recipient == Clinton ;; trigger == blackmail ;; contact.threatencoerce
communicator == advisers ;; recipient == her ;; trigger == recommending ;; contact.requestadvise
victim == Edwin ;; trigger == illness ;; life.injure
deceased == man ;; place == German cemetery ;; trigger == burial ;; contact.funeralvigil
defendant == fossil fuel companies ;; place == several other countries ;; trigger == charges ;; justice.initiatejudicialprocess
attacker == Soviet Union ;; instrument == cakes ;; target == Ukrainian nationalists ;; trigger == exploding ;; conflict.attack
place == Maryland ;; trigger == violent ;; life.injure
giver == Tehran ;; money == Iran ;; preventer == India ;; recipient == $ 6 billion ;; trigger == sanctions ;; transaction.transfermoney
giver == government ;; recipient == Moscow ;; trigger == sanctions ;; transaction.transfermoney
participant == Trump ^^ China ;; place == Iowa ;; trigger == negotiating ;; contact.negotiate
communicator == West ;; place == West ;; recipient == Russia ;; trigger == blackmail ;; contact.threatencoerce
passenger == weapons ;; transporter == Lockheed Martin ;; vehicle == the F-35 combat aircraft ;; trigger == shipments ;; movement.transportperson
trigger == Buy ;; transaction.transferownership
artifact == Trump ;; damager == dark secret society ;; trigger == wreck ;; artifactexistence.damagedestroy
injurer == security ;; trigger == harming ;; life.injure
victim == Seth Rich ;; trigger == murder ;; life.die
trigger == trafficking ;; movement.transportperson
beneficiary == He ;; giver == He ;; trigger == purchasing ;; transaction.transferownership
attacker == Kovtun ;; instrument == poison ;; target == Litvinenko ;; trigger == poison ;; conflict.attack
defendant == The Foundation ;; investigator == New York Attorney General ;; trigger == investigation ;; justice.investigate
demonstrator == We ;; trigger == protest ;; conflict.demonstrate
communicator == Israel ;; place == Israel ;; recipient == French ;; trigger == proposed ;; contact.publicstatementinperson
communicator == a disarmament group ;; place == letter ;; recipient == him ;; trigger == urged ;; contact.requestadvise
artifact == helicopter ;; destination == Hmeimim ;; trigger == transport ;; movement.transportartifact
participant == House ^^ House ;; place == Capitol Hill ;; trigger == meets ;; contact.collaborate
communicator == his ;; recipient == shake things up ;; trigger == vow ;; contact.commitmentpromiseexpressintent
artifact == Russian anti - aircraft missiles ;; origin == Catalonia region ;; transporter == Two Indian men ;; trigger == smuggling ;; movement.transportartifact
transporter == drug users ;; trigger == incarcerated ;; movement.transportartifact
place == Yemeni capital ;; trigger == funeral ;; contact.funeralvigil
participant == Elias ^^ Podesta ;; trigger == discuss ;; contact.discussion
killer == blacks ;; victim == victims ;; trigger == homicide ;; life.die
artifact == oil ;; destination == Turkey ;; transporter == Turkey ;; trigger == importing ;; movement.transportartifact
giver == Western ;; place == Crimea ;; preventer == Western ;; recipient == Crimea ;; trigger == sanctions ;; transaction.transfermoney
placeofemployment == Chief - of - Staff ;; trigger == sacks ;; personnel.endposition
victim == Muslim American soldier ;; trigger == deceased ;; life.die
injurer == US - led coalition ;; victim == Syrian army unit ;; trigger == strike ;; life.injure
origin == their homes ;; passenger == thousands of people ;; transporter == thousands of people ;; trigger == flee ;; movement.transportperson
communicator == Warren ;; recipient == elect Hillary Clinton ;; trigger == vowed ;; contact.commitmentpromiseexpressintent
communicator == Broaddrick ;; trigger == oath ;; contact.commitmentpromiseexpressintent
crime == Russian meddling ;; prosecutor == Obama DHS chief ;; trigger == testify ;; justice.initiatejudicialprocess
communicator == Republicans ;; trigger == lie ;; contact.prevarication
defendant == political activists and most of them were Kurds ;; executioner == totalitarian regime ;; place == Iran ;; trigger == executions ;; justice.judicialconsequences
communicator == Clinton ;; place == convention ;; recipient == Veterans of Foreign Wars ;; trigger == vowing ;; contact.commitmentpromiseexpressintent
participant == Clinton ;; trigger == email ;; contact.collaborate
artifact == carfentanil ;; destination == the United In States ;; origin == China ;; transporter == people willing to skirt the law ;; vehicle == packages ;; trigger == supply ;; movement.transportartifact
trigger == suggests ;; contact.requestadvise
victim == eminence ;; trigger == deceased ;; life.die
giver == Iran ;; preventer == Alinejad ;; trigger == boycott ;; transaction.transaction
trigger == shipment ;; movement.transportartifact
destination == Europe ;; origin == Iran ;; transporter == Iran ;; trigger == shipment ;; movement.transportperson
participant == Lavrov ^^ Kerry ;; place == Lausanne ;; trigger == meet ;; contact.discussion
participant == he ^^ Crimea ;; place == Crimea ;; trigger == took over ;; government.formation
artifact == the severed head of a four - year - old girl ;; preventer == Investigators ;; transporter == A burka - clad babysitter ;; trigger == arrested ;; movement.transportartifact
participant == we ;; trigger == negotiated ;; contact.discussion
communicator == Assange ;; recipient == interviewer ;; trigger == suggesting ;; contact.requestadvise
communicator == Socialism ;; recipient == we ;; trigger == demands ;; contact.commandorder
participant == Russia ^^ U.S. ;; trigger == negotiate ;; contact.negotiate
place == Israel ;; recipient == Israel ;; trigger == Boycott ;; transaction.transfermoney
place == Sinai ;; vehicle == Metrojet Flight 9268 ;; trigger == crash ;; disaster.accidentcrash
giver == hackers ;; trigger == auction ;; transaction.transfermoney
place == Russia ;; recipient == Russia ;; trigger == sanctions ;; transaction.transferownership
crime == Abu Hayjaa was killed ;; defendant == Suspects ;; investigator == Iraqi and Chechen security officials ;; place == road outside Raqqa ;; trigger == investigate ;; justice.investigate
artifact == access to the powerful family and their associates ;; beneficiary == themselves ;; giver == themselves ;; trigger == auctioning ;; transaction.transferownership
artifact == phones ;; destination == prison ;; transporter == she ;; trigger == smuggled ;; movement.transportartifact
origin == Aleppo ;; passenger == an injured man ;; transporter == Syrians ;; trigger == evacuate ;; movement.transportperson
artifact == AR-15 rifle ;; beneficiary == Mohamed Bailor Jalloh ;; giver == Blue Ridge Arsenal gun store ;; place == Chantilly , Va. ;; recipient == Mohamed Bailor Jalloh ;; trigger == purchase ;; transaction.transferownership
place == Sandy Hook ;; trigger == massacre ;; conflict.attack
beneficiary == the Clinton Foundation ;; giver == Judy Woodruff ;; trigger == donated ;; transaction.transfermoney
employee == them ;; placeofemployment == Russians ;; trigger == hiring ;; personnel.startposition
communicator == Mike Roman ;; recipient == conservatives ;; trigger == misleading ;; contact.prevarication
victim == American citizens ;; trigger == assassinate ;; life.die
artifact == three ;; preventer == Emirate ;; trigger == arrested ;; movement.transportartifact
hidingplace == offshore accounts ;; transporter == at least 140 current or former world leaders ;; trigger == conceal ;; movement.transportperson
attacker == Saudi Arabia and its regional allies ;; instrument == airstrike ;; place == Yemen ;; target == Yemen ;; trigger == airstrike ;; conflict.attack
beneficiary == countries like Venezuela ;; giver == It ;; place == China ;; trigger == loans ;; transaction.transferownership
governmentbody == Obama ;; law == sanctions ;; place == Tehran ;; trigger == signed into law ;; government.legislate
place == Iraq ;; trigger == war ;; conflict.attack
participant == Chinese ^^ U.S. ;; trigger == meeting ;; contact.collaborate
communicator == Trump ;; trigger == lied ;; contact.prevarication
beneficiary == Trump ;; giver == who ;; trigger == loans ;; transaction.transfermoney
defendant == her ;; judgecourt == Justice Department ;; prosecutor == Justice Department ;; trigger == indicts ;; justice.initiatejudicialprocess
origin == UK ;; passenger == Assange ;; preventer == UK ;; trigger == jailed ;; movement.transportperson
injurer == rape ;; trigger == rape ;; life.injure
money == weapons ;; place == Syria and in the Middle East ;; preventer == we ;; recipient == all parties ;; trigger == embargo ;; transaction.transfermoney
giver == U.S. authorities ;; place == U.S. ;; recipient == 3.7 kilograms ( 8.1 pounds ) of fentanyl ;; trigger == seized ;; transaction.transaction
law == Syringe restrictions ;; trigger == legislated ;; government.legislate
demonstrator == We ;; place == TV4 ;; trigger == protest ;; conflict.demonstrate
spy == agencies ;; trigger == spy ;; government.spy
place == Washington , D.C. ;; target == Seth Rich ;; trigger == robbery ;; conflict.attack
artifact == car ;; target == Mr. Mansoor ;; trigger == robbed ;; conflict.attack
participant == COOPER ^^ Senator Sanders ;; place == debate ;; trigger == debate ;; contact.negotiate
passenger == Rubio ;; trigger == plummeted ;; movement.transportperson
crime == sexual misconduct ;; defendant == Assange ;; destination == Sweden ;; extraditer == Sweden ;; origin == London ;; trigger == extradition ;; justice.judicialconsequences
inspectedentity == official explanation ;; place == New Jersey ;; trigger == inspection ;; inspection.sensoryobserve
artifact == shipment ;; destination == Chongjin ;; origin == Russia ;; transporter == Russia ;; trigger == shipment ;; movement.transportartifact
money == reliable news ;; place == dark corner of the battlefield ;; trigger == embargo ;; transaction.transfermoney
trigger == shoot ;; life.die
artifact == crude shipment ;; destination == Europe ;; origin == Iran ;; transporter == Iran ;; trigger == exported ;; movement.transportartifact
communicator == US and Seoul ;; trigger == announced ;; contact.publicstatementinperson
artifact == food ;; origin == Black Sea ports ;; transporter == Russia ;; trigger == exports ;; movement.transportartifact
beneficiary == Palmali Shipping and Transportation Agency ;; giver == BMZ ;; place == Istanbul ;; trigger == acquired ;; transaction.transfermoney
participant == Pentagon ^^ foreign buyer ;; trigger == negotiate ;; contact.negotiate
demonstrator == Rahul Gandhi ;; place == Jantar Mantar ;; trigger == protesting ;; conflict.demonstrate
recipient == The Shadow Brokers ;; trigger == auctioning ;; transaction.transfermoney
communicator == Kasich ;; trigger == pledged ;; contact.commitmentpromiseexpressintent
artifact == those ;; destination == Maryland ;; origin == Maryland ;; preventer == policies ;; trigger == imprisoned ;; movement.transportartifact
trigger == War ;; conflict.attack
communicator == super PAC ;; recipient == her to run sets her apart from Clinton ;; trigger == urging ;; contact.requestadvise
crashobject == Chinese fighter jet ;; place == China ;; vehicle == US spy plane ;; trigger == colliding ;; disaster.accidentcrash
beneficiary == government ;; recipient == government ;; trigger == bought ;; transaction.transfermoney
demonstrator == Senate Dems ;; trigger == protest ;; conflict.demonstrate
artifact == the Baltic states ;; destroyer == RUSSIA ;; place == Eastern Europe ;; trigger == demolish ;; artifactexistence.damagedestroy
inspectedentity == Iran ;; inspector == Obama administration ;; place == Iran ;; trigger == inspection ;; inspection.sensoryobserve
participant == leader of Yemen ’s Houthi Ansarullah movement ;; place == Saudi Arabia ;; trigger == funeral ;; contact.funeralvigil
voter == adults ;; trigger == win elections ;; personnel.elect
employee == Louise Sunshine ;; place == Fifth Avenue office ;; placeofemployment == director of special projects ;; trigger == hired ;; personnel.startposition
employee == Manafort ;; placeofemployment == campaign ;; trigger == resigned ;; personnel.endposition
attacker == SCUD ;; place == Iraq ;; trigger == stabbings ;; conflict.attack
participant == he ^^ Kim Jong - un ;; trigger == negotiating ;; contact.discussion
communicator == party ;; recipient == American people ;; trigger == deceive ;; contact.prevarication
observedentity == The argument ;; trigger == inspection ;; inspection.sensoryobserve
communicator == We ;; recipient == Russian ;; trigger == urge ;; contact.commandorder
place == North Korea ;; trigger == funerals ;; contact.funeralvigil
killer == Ottoman Turks ;; victim == Armenians ;; trigger == genocide ;; life.die
beneficiary == charity ;; giver == Clintons ;; recipient == charity ;; trigger == donated ;; transaction.transaction
destination == hospital in the city of Salekhard ;; origin == the contaminated area ;; passenger == indigenous people ;; trigger == evacuated ;; movement.transportperson
artifact == rover ;; manufacturer == UK ;; trigger == assembled ;; manufacture.artifact
participant == Russia ^^ Crimean Peninsula ;; place == Crimean Peninsula ;; trigger == annex ;; government.formation
spy == your ;; trigger == spy ;; government.spy
communicator == we ;; recipient == them ;; trigger == urge ;; contact.commandorder
victim == Mujib ;; trigger == executing ;; life.die
artifact == any missile shield ;; damager == Russia ;; instrument == attack systems ;; place == over Europe ;; trigger == piercing ;; artifactexistence.damagedestroy
participant == convenience stores ;; place == Alabama ;; trigger == confiscated ;; transaction.transaction
communicator == Those ;; recipient == Trump ’s alpha - male status ;; trigger == threaten ;; contact.threatencoerce
destination == Russia ;; origin == Hawaii ;; passenger == he ;; trigger == fled ;; movement.transportperson
artifact == wheat and corn ;; transporter == U.S. ;; trigger == shipments ;; movement.transportartifact
communicator == Trump ;; recipient == to throw her in prison ;; trigger == vow ;; contact.commitmentpromiseexpressintent
preventer == Trump ;; transporter == Clinton ;; trigger == jail ;; movement.transportartifact
communicator == Lawmakers ;; recipient == White House ;; trigger == urging ;; contact.commandorder
beneficiary == Clinton ;; recipient == astroturfing efforts ;; trigger == funded ;; transaction.transaction
attacker == his Russian companions - turned - executioners ;; instrument == polonium ;; place == London ;; target == He ;; trigger == poisoning ;; conflict.attack
place == Afghanistan ;; trigger == conflict ;; conflict.attack
preventer == he ;; transporter == Clinton ;; trigger == incarcerate ;; movement.transportartifact
giver == government ;; recipient == small businesses ;; trigger == loans ;; transaction.transfermoney
artifact == troops ;; destination == country ;; transporter == he ;; trigger == bringing in ;; movement.transportartifact
artifact == seal - fur products ;; destination == the European Union , the United States and Russia ;; origin == Canadian ;; trigger == export ;; movement.transportartifact
employee == Chief - of - Staff ;; place == Russian President ’s inner circle ;; placeofemployment == Chief - of - Staff ;; trigger == sacks ;; personnel.endposition
giver == Russia ;; preventer == European Union ;; trigger == embargo ;; transaction.transferownership
place == North Carolina ;; trigger == voting ;; government.vote
artifact == the inquiry finds ;; instrument == nuclear reactor ;; trigger == manufactured ;; manufacture.artifact
artifact == Rich ;; attacker == death ;; target == Rich ;; trigger == robbery ;; conflict.attack
communicator == the firm ;; recipient == itself ;; trigger == professes ;; contact.publicstatementinperson
artifact == amorphous movement ;; manufacturer == we ;; trigger == build ;; manufacture.artifact
attacker == Muslim cleric ;; instrument == coup ;; place == Ankara ;; target == Ankara ;; trigger == coup ;; conflict.attack
attacker == Syrian government ;; instrument == barrel bomb ;; place == hospital ;; target == 27 people ;; trigger == bombs ;; conflict.attack
artifact == Cyber Weapons ;; beneficiary == they ;; place == country ;; recipient == Cyber Weapons ;; trigger == Auction ;; transaction.transferownership
passenger == Victims ;; trigger == Trafficking ;; movement.transportperson
defendant == he ;; trigger == investigation ;; justice.investigate
attacker == Mexican ;; place == Alamo ;; target == Texas ;; trigger == battle ;; conflict.attack
participant == Russia ^^ United States ;; trigger == treaty ;; government.agreements
employee == Steven L. Hall ;; place == Eurasia and Latin ;; placeofemployment == Central Intelligence Agency ;; trigger == retiring ;; personnel.endposition
place == Iran ;; trigger == boycott ;; transaction.transferownership
beneficiary == losers ;; giver == winners ;; trigger == compensated ;; transaction.transfermoney
giver == Vietnam ;; money == Asia ;; place == Vietnam ;; preventer == President Obama ;; recipient == lethal arms ;; trigger == embargo ;; transaction.transfermoney
attacker == Polish villagers ;; target == 1,600 Jewish men , women and children ;; trigger == massacre ;; conflict.attack
inspectedentity == The argument ;; trigger == inspection ;; inspection.sensoryobserve
recipient == He ;; trigger == loan ;; transaction.transfermoney
destination == the military training area ;; vehicle == rail ;; trigger == transported ;; movement.transportperson
governmentbody == LBJ ;; law == civil rights legislation ;; trigger == signed into law ;; government.legislate
attacker == Russia ;; place == Estonia , Latvia and Lithuania ;; target == Estonia , Latvia and Lithuania ;; trigger == encroached ;; conflict.attack
crime == money laundering , fraud and extortion ;; defendant == Ukraine Pavlo Lazarenko ;; trigger == convicted ;; justice.judicialconsequences
artifact == firearms ;; transporter == immigrants ;; trigger == trafficking ;; movement.transportartifact
artifact == others ;; damager == megalomaniac ;; instrument == any means ;; trigger == tear ;; artifactexistence.damagedestroy
communicator == Trump ;; recipient == tax returns ;; trigger == reiterated ;; contact.publicstatementinperson
participant == reporters ^^ they ;; place == Israel ;; trigger == dialogue ;; contact.discussion
inspectedentity == Documents ;; inspector == the Daily Dot ;; trigger == inspected ;; inspection.sensoryobserve
origin == Maryland ;; passenger == imprisoned population ;; preventer == Maryland ;; trigger == imprisoned ;; movement.transportperson
killer == Trump ;; victim == Clinton ;; trigger == assassinate ;; life.die
place == Syria ;; trigger == starvation ;; life.injure
artifact == money ;; giver == asking ;; place == Saudi Arabia ;; recipient == Saudi Arabia ;; trigger == borrow ;; transaction.transferownership
attacker == the military and associated paramilitary squads ;; place == Latin America ;; target == peasants ;; trigger == massacres ;; conflict.attack
communicator == athletes ;; recipient == us ;; trigger == deceive ;; contact.prevarication
injurer == Dr. Richard Cytowic ;; victim == Cruz ;; trigger == punch ;; life.injure
injurer == The YPG ;; place == the city ;; trigger == wounded ;; life.injure
communicator == Sanders ;; trigger == vowing ;; contact.commitmentpromiseexpressintent
beneficiary == their wealth and control ;; giver == Wealthy Elite ;; recipient == cyber weapons ;; trigger == auction ;; transaction.transferownership
artifact == carfentanil ;; destination == North America ;; origin == China ;; transporter == Several vendors ;; trigger == export ;; movement.transportartifact
governmentbody == Britain ;; law == new sanctions ;; place == Britain ;; trigger == enact ;; government.legislate
communicator == Hillary Clinton ;; recipient == Bill Clinton 's accusers ;; trigger == intimidate ;; contact.threatencoerce
candidate == President Recep Tayyip Erdogan ;; place == the country ;; voter == the country ;; trigger == elected ;; personnel.elect
artifact == the terrorist group Islamic State ( IS , formerly ISIS / ISIL ) ;; destroyer == US ;; trigger == eradicate ;; artifactexistence.damagedestroy
attacker == Litvinenko ;; instrument == polonium ;; place == the table ;; target == Litvinenko ;; trigger == poisoned ;; conflict.attack
attacker == Palestinian woman ;; place == Hebron ;; trigger == stabbing ;; conflict.attack
communicator == Chelsea Clinton ;; recipient == foundation ’s rank ;; trigger == intimidating ;; contact.threatencoerce
crime == meddling ;; prosecutor == DHS chief ;; trigger == testify ;; justice.initiatejudicialprocess
communicator == Obama ;; place == news conference ;; recipient == caution ;; trigger == preached ;; contact.mediastatement
victim == NFL players ;; trigger == deceased ;; life.die
beneficiary == Russia ;; place == Crimea ;; recipient == Russia ;; territoryorfacility == Crimea ;; trigger == land grab ;; transaction.transaction
artifact == Donald Trump ;; damager == Donald Trump ;; instrument == interview ;; trigger == wreck ;; artifactexistence.damagedestroy
communicator == Trump ;; recipient == Ghazala ;; trigger == suggested ;; contact.requestadvise
injurer == Mr. Campbell ;; instrument == his arm ;; victim == Ms. Teeter ;; trigger == punched ;; life.injure
crime == smuggling Russian anti - aircraft missiles ;; defendant == Two Indian men ;; judgecourt == United States ;; place == Spain ;; trigger == extradited ;; justice.judicialconsequences
artifact == crude ;; origin == Russia ;; transporter == Russia ;; trigger == shipments ;; movement.transportartifact
participant == he ;; trigger == debate ;; contact.negotiate
beneficiary == taxes ;; giver == family ;; money == 31 percent of their income ;; recipient == taxes ;; trigger == income ;; transaction.transfermoney
beneficiary == Tehran ;; giver == Indian government ;; recipient == Tehran ;; trigger == payment ;; transaction.transfermoney
artifact == sample documents ;; trigger == auction ;; transaction.transferownership
communicator == Hillary ;; recipient == victims ;; trigger == intimidated ;; contact.threatencoerce
